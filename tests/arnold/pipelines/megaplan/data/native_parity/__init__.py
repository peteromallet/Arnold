"""M4 native parity fixture catalog — scenario inventory and golden output locations.

This package pins the eight-scenario parity matrix defined in the M4 plan:
happy finalize, revise loop, tiebreaker, escalate, override force-proceed,
override abort, suspension/resume, and execute/review artifact path.

Only stable input descriptors, expected branch labels, and golden output
locations live here. The parity runner is built in a later step (T4+).
"""
