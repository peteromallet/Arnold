manifest_id: megaplan
manifest_hash: sha256:74563f60ae604b96822a308178eff6a4e7d308a43f7ecd726e02824cbafbfb96
topology_hash: sha256:295e0ad28430ff465334a36c6ff5add25fba1d21d7ba2449da6b081150098260
nodes:
- prep
- plan
- critique
- gate
- revise
- tiebreaker_researcher
- tiebreaker_challenger
- tiebreaker_synthesis
- tiebreaker_decision
- finalize
- execute
- review
- halt
- override
capabilities:
- megaplan:planning
- human:gate
- human:review
gate_targets:
- label: proceed
  target: finalize
- label: iterate
  target: revise
- label: tiebreaker
  target: tiebreaker_researcher
- label: escalate
  target: override
- label: abort
  target: halt
- label: suspend
  target: halt
- label: blocked_preflight
  target: override
- label: force_proceed
  target: finalize
tiebreaker_targets:
- label: iterate
  target: revise
- label: proceed
  target: finalize
- label: escalate
  target: override
- label: replan
  target: revise
review_targets: []
canonical_authoring:
  source_path: arnold_pipelines/megaplan/workflows/workflow.pypeline
  workflow_id: megaplan
  workflow_version: m4-phase3
  semantic_nodes:
  - prep
  - plan
  - critique-fanout
  - gate
  - revise
  - tiebreaker
  - finalize
  - execute-batches
  - review-fan-in
  - override
  - tiebreaker-execute-batches
  - execute
  - halt
  excluded_wrapper_nodes:
  - blocked_override
  - fallback_execute
  - fallback_finalize
  - force_execute
  - force_finalize
  - gate_abort
  - gate_suspend
  - override_execute
  - override_finalize
  - override_halt
  - override_unknown
  - review_halt
  - review_revise
  - tiebreaker_finalize
  - tiebreaker_override
  gate_routes:
  - route_id: gate-finalize
    label: proceed
    target: finalize
    lowered_target: finalize
    condition_ref: gate.gate_route_signal.eq.proceed
  - route_id: gate-revise
    label: iterate
    target: revise
    lowered_target: revise
    condition_ref: gate.gate_route_signal.eq.iterate
  - route_id: gate-tiebreaker
    label: tiebreaker
    target: tiebreaker
    lowered_target: tiebreaker
    condition_ref: gate.gate_route_signal.eq.tiebreaker
  - route_id: gate-override
    label: escalate
    target: override
    lowered_target: override
    condition_ref: gate.gate_route_signal.eq.escalate
  - route_id: gate-gate_abort
    label: abort
    target: halt
    lowered_target: gate_abort
    condition_ref: gate.gate_route_signal.eq.abort
  - route_id: gate-gate_suspend
    label: suspend
    target: halt
    lowered_target: gate_suspend
    condition_ref: gate.gate_route_signal.eq.suspend
  - route_id: gate-blocked_override
    label: blocked_preflight
    target: override
    lowered_target: blocked_override
    condition_ref: gate.gate_route_signal.eq.blocked_preflight
  - route_id: gate-force_proceed
    label: force_proceed
    target: finalize
    lowered_target: force_finalize
    condition_ref: gate.gate_route_signal.eq.force_proceed
  dynamic_maps:
  - id: execute-batches
    items_ref: plan.plan_payload.tasks
    path_template: execute/{index}
  - id: critique-fanout
    items_ref: plan.plan_payload.evaluators
    path_template: critique/{index}
  - id: review-fan-in
    items_ref: plan.plan_payload.reviewers
    path_template: review/{index}
