manifest_id: megaplan
manifest_hash: sha256:763a9bc84568b324a4f5565dcb92a843873c106195782e6e5500f62d1d764381
topology_hash: sha256:92f1882dbea99145e93c76944b28105ec17cc91c9246b45c7b5dbb3dbcf4ef2c
canonical_authoring:
  workflow_id: megaplan
  workflow_version: m4-phase3
  semantic_nodes:
  - prep
  - plan
  - critique-fanout
  - gate
  - revise
  - tiebreaker
  - finalize
  - execute-batches
  - review-fan-in
  - halt
  - override
  excluded_wrapper_nodes:
  - gate_abort
  - gate_suspend
  - blocked_override
  - force_finalize
  - force_execute
  - fallback_finalize
  - fallback_execute
  - override_halt
  - override_finalize
  - override_execute
  - override_revise
  - override_unknown
  - review_revise
  - review_override
  - review_deferred_human
  - review_halt
  - tiebreaker_finalize
  - tiebreaker_revise
  - tiebreaker_override
  - tiebreaker_fallback_override
  - tiebreaker-execute-batches
  - gate_retry_revise
  - gate_reprompt_revise
  - tiebreaker_execute
  gate_routes:
  - label: proceed
    lowered_target: finalize
  - label: iterate
    lowered_target: revise
  - label: retry_gate
    lowered_target: gate_retry_revise
  - label: reprompt_downgrade
    lowered_target: gate_reprompt_revise
  - label: tiebreaker
    lowered_target: tiebreaker
  - label: escalate
    lowered_target: override
  - label: abort
    lowered_target: gate_abort
  - label: suspend
    lowered_target: gate_suspend
  - label: blocked_preflight
    lowered_target: blocked_override
  - label: force_proceed
    lowered_target: force_finalize
  dynamic_maps:
  - id: critique-fanout
    items_ref: megaplan.policy.critique_lenses
    path_template: critique/{item_id}
  - id: execute-batches
    items_ref: megaplan.execute.batches
    path_template: execute/{index}
  - id: review-fan-in
    items_ref: execute_payload
    path_template: review/{item_id}
  - id: tiebreaker-execute-batches
    items_ref: megaplan.execute.batches
    path_template: execute/{index}
