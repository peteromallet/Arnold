# Focused execute-policy tests relocated during M5b (T17).
# Mixed-domain tests (e.g. tests/test_execute.py) remain at the top level
# and are documented as exceptions in docs/arnold/m5b-plugin-relocation-map.md.
