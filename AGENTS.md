# VibeComfy Agent Bootstrap

This file is intentionally short. The canonical long-form agent instructions for
this repository live in [CLAUDE.md](CLAUDE.md).

Before making changes, read `CLAUDE.md` and follow it as the source of truth for:

- repository rules and shared-worktree safety
- VibeComfy vocabulary and public API names
- workflow/template authoring patterns
- CLI, validation, runtime, and RunPod guidance
- in-repo reference docs

Keep this bootstrap minimal so `AGENTS.md` and `CLAUDE.md` do not drift.
