diff --git a/arnold/pipeline/native/hooks.py b/arnold/pipeline/native/hooks.py
--- a/arnold/pipeline/native/hooks.py
+++ b/arnold/pipeline/native/hooks.py
@@ -459,6 +459,12 @@ class EffectLedgerHooks:
         result: Any,
     ) -> Any:
         result = self._inner.on_step_end(instr, ctx, result)
+        effect_ctx = ctx.get("effect")
+        if isinstance(effect_ctx, dict) and effect_ctx.get("duplicate_action") is not None:
+            if self._active_effect_metadata is not None:
+                self._last_effect_metadata = dict(self._active_effect_metadata)
+            self._active_effect_metadata = None
+            return result
         if self._active_effect_metadata is not None and instr.idempotency_key:
             self._ledger.mark_fulfilled(instr.idempotency_key)
             self._active_effect_metadata["lifecycle_state"] = (
@@ -483,6 +489,12 @@ class EffectLedgerHooks:
         exc: BaseException,
     ) -> None:
         self._inner.on_step_error(instr, ctx, exc)
+        effect_ctx = ctx.get("effect")
+        if isinstance(effect_ctx, dict) and effect_ctx.get("duplicate_action") is not None:
+            if self._active_effect_metadata is not None:
+                self._last_effect_metadata = dict(self._active_effect_metadata)
+            self._active_effect_metadata = None
+            return
         if self._active_effect_metadata is not None and instr.idempotency_key:
             self._ledger.mark_failed(instr.idempotency_key)
             self._active_effect_metadata["lifecycle_state"] = (
diff --git a/arnold/workflow/effect_protocol.py b/arnold/workflow/effect_protocol.py
--- a/arnold/workflow/effect_protocol.py
+++ b/arnold/workflow/effect_protocol.py
@@ -36,11 +36,11 @@ from dataclasses import dataclass, field
 from typing import Any, Callable, Optional
 
+from arnold.workflow import SqliteAttemptLedgerStore
 from arnold.workflow.attempt_ledger_store import (
     GlobalEffectConflict,
     GlobalEffectConflictError,
     GlobalEffectOutcome,
     GlobalEffectReservation,
-    SqliteAttemptLedgerStore,
 )
 from arnold.workflow.effect_reconciliation import (
     ContradictoryEvidenceError,
diff --git a/arnold_pipelines/megaplan/cloud/wrappers/arnold-watchdog b/arnold_pipelines/megaplan/cloud/wrappers/arnold-watchdog
--- a/arnold_pipelines/megaplan/cloud/wrappers/arnold-watchdog
+++ b/arnold_pipelines/megaplan/cloud/wrappers/arnold-watchdog
@@ -2882,7 +2882,9 @@ repair_loop_busy_state() {
   }
   if [[ -r "$pidfile" ]]; then
     read -r existing_pid < "$pidfile" || existing_pid=""
-    if repair_loop_pid_matches_session "$existing_pid" "$session"; then
+    # Avoid a fresh Python interpreter for obviously stale ownership.  Besides
+    # being cheaper, this keeps stale-pidfile recovery inside the claim window.
+    if kill -0 "$existing_pid" 2>/dev/null && repair_loop_pid_matches_session "$existing_pid" "$session"; then
       exec {guard_fd}>&-
       echo "same_session"
       return 0
