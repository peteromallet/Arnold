# Agent Interface Declarations

This directory contains visible agent interface declarations, such as
`openai.yaml`, that describe how an external assistant should present or invoke
VibeComfy.

This is separate from:

- `agentic/`: the Sisypy agentic-test harness.
- `.claude/`: local or force-tracked Claude skill/config files.
- `.agents/`: local agent skill/config files.
