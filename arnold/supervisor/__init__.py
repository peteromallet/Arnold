"""Generic supervisor tier – shared data model and orchestration primitives.

This package houses the data model, outcome normalization, state persistence,
driver abstractions, and ladder logic that can be reused across pipeline
variants (chain, bakeoff, etc.).
"""
