# Deliberation Pipeline

The deliberation pipeline refines an input idea through layered critique:
question generation, a human answer gate, and three abstraction layers
(high strategic → mid architectural → low ground-truth), each running a
fan-out panel of critics whose outputs are skeptically synthesized into a
revised plan with a changelog.  The full v0→vN lineage is reconstructable
from the event journal.
