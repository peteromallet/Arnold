Here's the compact verification table:

| # | Claim | Status | File:Line | Note | Quote |
|---|-------|--------|-----------|------|-------|
| 1 | `handle_gate` runs a REPROMPT loop + tiebreaker downgrade cascade reading `state["meta"]` counters; both inside the handler | **PARTIAL** | reprompt: `gate.py:494-549`; tiebreaker dispatch: `gate.py:490` → `critique.py:629-726` | REPROMPT loop IS inside `handle_gate` (spawns retry worker, rebuilds artifact, auto-downgrades to ITERATE). Tiebreaker cascade is dispatched from `handle_gate` but the cascade logic lives in `_validate_tiebreaker` (`critique.py:629`). It reads `state["meta"]["tiebreaker_count"]` (L653). Both are control-flow. | `"if blocking_unresolved_ids:\n            reprompt_prompt = _build_gate_prompt_override(...)` (gate.py:494-501); `"tiebreaker_count = state["meta"].get("tiebreaker_count", 0)"` (critique.py:653) |
| 2 | `handle_execute` runs an internal AUTO-LOOP | **DRIFTED** | `execute.py:178` dispatches → `megaplan/execute/batch.py` | `handle_execute` is a dispatcher; the auto-loop lives in `handle_execute_auto_loop` in `megaplan/execute/batch.py`. Not "internal" to the handler function body. | `"response = handle_execute_auto_loop(...)"` (execute.py:178-191) |
| 3 | Execute approval gate checks `auto_approve` OR `user_approved` (injected flag) | **CONFIRMED** | `execute.py:108-116` | Checks `auto_approve` (config) OR `user_approved_gate` (meta, set by `--user-approved` CLI flag). Exact: `if not auto_approve and not state["meta"].get("user_approved_gate", False): raise CliError`. | `"auto_approve = bool(state["config"].get("auto_approve", False))"` (L108); `"if not auto_approve and not state["meta"].get("user_approved_gate", False):"` (L112) |
| 4 | Override handler supports ~8 actions; `OverrideAction` enum; ~4 "promotable" | **CONFIRMED** (9, not ~8) | handler: `override.py:898-910`; enum: `_pipeline/types.py:77` | 9 handler actions: `add-note`, `abort`, `force-proceed`, `replan`, `recover-blocked`, `resume-clarify`, `set-robustness`, `set-profile`, `set-model`. `OverrideAction` = `Literal["force_proceed", "abort", "replan", "add_note"]` — exactly 4 pipeline-primitive actions (the "promotable" set). | `"_OVERRIDE_ACTIONS: dict[...] = {\n    "add-note": ...,\n    "abort": ...,\n    ..."` (override.py:898-910); `"OverrideAction = Literal["force_proceed", "abort", "replan", "add_note"]"` (types.py:77) |
| 5 | `FaultRegistry` has method `addressed_then_reopened_count` | **PARTIAL** | `faults.py:57` (property on `Fault`, not `FaultRegistry`) | `addressed_then_reopened_count` is a `@property` on the `Fault` dataclass (L57), NOT a method on `FaultRegistry`. `FaultRegistry.reopened_repeatedly` (L200-205) uses it. Trigger: counts transitions from "addressed"→"open" in fault history. The tiebreaker path uses `compute_iteration_pressure`/`has_mechanical_recurrence` (audits/iteration.py:145) — a parallel signal, not the `FaultRegistry` directly. | `"@property\n    def addressed_then_reopened_count(self) -> int:"` (faults.py:56-57); `"if entry["addressed_then_reopened_count"] >= 2:\n            return True"` (iteration.py:145-146) |

---

**Most deeply impure handlers:** `handle_gate` (gate.py) and `handle_execute` (execute.py). Gate has nested control flow (REPROMPT loop + tiebreaker cascade + downgrade logic), spawns multiple workers, and reads/writes `state["meta"]` counters mid-flight. Execute dispatches into a full auto-loop subsystem with batch iteration, tier routing, and phase-result emission — both are deeply entangled with I/O, state mutation, and external process spawning, making them the hardest to reduce to pure `(root, state, hctx) -> StepResponse` functions.