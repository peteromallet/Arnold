Now I have all the data. Here's the table:

| # | Claim | Verdict | Current location | Real-code evidence |
|---|---|---|---|---|
| 1 | `InProcessHandlerStep` carries its own next-step routing table (`_label_for` / `_gate_next_step`) — a second, independent encoding of next-step logic | **CONFIRMED** | `megaplan/_pipeline/stages/inprocess_step.py:141–200` | `_label_for()` (line 141) hardcodes `prep→plan, plan→critique, critique→gate_unset:gate, execute→review, …`; `_gate_next_step()` (line 192) maps gate recs to bare step names. These run inside `InProcessHandlerStep.run()` (line 78) **in parallel with** the Pipeline graph edges in `planning.py:85–121`. Two sources of truth. |
| 2 | `TiebreakerStep` is a `SubloopStep` that runs a nested pipeline | **PARTIAL** (composition, not inheritance) | `megaplan/_pipeline/stages/tiebreaker.py:70–87` | `TiebreakerStep` is a standalone `@dataclass` (line 70), NOT a subclass. It **instantiates** a `SubloopStep` on line 81–86 and delegates `run()`. It IS-A free dataclass, HAS-A `SubloopStep`. The nested pipeline is real: `_build_tiebreaker_child_pipeline()` (line 32) returns a `Pipeline(stages={run→decide})`. |
| 3 | `ExecuteStep` injects `user_approved=True` inside the Step, NOT inside the handler | **CONFIRMED** | `megaplan/_pipeline/stages/execute.py:17–20,34` | `_DEFAULTS = {"user_approved": True, "confirm_destructive": True}` (line 17). Merged in `run()` via `{**_DEFAULTS, **self.arg_overrides}` (line 34), then passed as `arg_overrides` to `InProcessHandlerStep`. The handler's own default is `user_approved=False` (`inprocess_step.py:62`). The Step overrides it. |
| 4 | Planning Steps' `prompt_key` fields are decorative/dead — bypassed by `InProcessHandlerStep` calling handlers directly | **PARTIAL** (dead for prompt resolution; alive for receipts + overlays) | `megaplan/_pipeline/stages/plan.py:16`, `prep.py:25`, `execute.py:27`; `inprocess_step.py:43–96`; `prompts.py:10–14` | `InProcessHandlerStep.run()` **never reads `self.prompt_key`** — it calls `self.handler(root, args)` directly. `prompts.py:12` confirms: *"production handlers … keep their own per-phase prompts and ignore this registry."* **But** `prompt_key` IS read by `StepReceipt` (`receipt.py:94`) for audit logging and by `prompt_key_overlay` (`pattern_topology.py:249`) for mode-swapped pipelines. Not fully dead. |

**Implication for the unification plan bridge layer:** The bridge must collapse two independent next-step encodings (Claim 1's `_label_for` vs. graph edges) into a single source of truth, and must decide whether `prompt_key` on handler-backed Steps graduates from a receipt-only annotation to an actual dispatch hook — otherwise the planning pipeline remains split-brain between the graph structure and the in-process hardcoded routing.