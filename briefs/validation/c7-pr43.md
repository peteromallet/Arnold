# C7 Validation — PR #43 (`worktree-execute-redesign`): current truth & re-home implications

_Investigated 2026-05-28. Supersedes the 2026-05-23 design brief §14 assumption that PR #43 is an OPEN draft._

## 1. Current state of PR #43

| Field | Value |
|---|---|
| Title | "Serial worktree-execute subsystem (preserved; blocked on execute-contract reconciliation)" |
| State | **CLOSED** (not merged) |
| createdAt | 2026-05-23T15:42 |
| closedAt | 2026-05-28T12:05 |
| mergedAt | null |
| baseRefName | main |
| headRefName | worktree-execute-redesign |
| head sha | `4ef3640245f0f641bc427779f74a5976bfe52647` ("checkpoint(worktree-execute-redesign): serial worktree-execute subsystem") |
| Size | +11,420 / −793 across 98 files |

**Why closed (closing comment, owner):** "Closing as part of branch cleanup after hardening/cost/prep/staleness work landed on main. This draft branch was preserved as WIP but is not being kept as a parked branch." → Administrative cleanup close, not a rejection on merits. The PR body asserts the work is genuinely unique (a read-only DeepSeek investigation confirmed it is orthogonal to main's pipeline-steps refactor).

**What "blocked on execute-contract reconciliation" means concretely (from PR body):** A trial merge into main resolved cleanly EXCEPT for an execute-contract collision. The branch was built on the **old per-task artifact model**; main has since moved to a **batch / `current_state` reconciliation contract** (PR body cites commits `bc6a14e2` and `a03d624b` — both confirmed present on main). Against main's `tests/test_auto.py`, **5 tests fail**:
- `test_plan_liveness_mtime_uses_state_and_execution_batches` — branch's `auto._plan_liveness_mtime` ignores `execution_batch_*`
- `test_execute_callback_failure_reconciles_latest_batch_and_clears_active_step`
- `test_failed_execute_callback_resume_restores_executed_state`
- `test_failed_execute_callback_resume_restores_blocked_execute_to_finalized`
- `test_worker_blocked_after_max_retries_emits_terminal_status`

PR body's own verdict: mechanical conflicts (`cli.py`, `workers/_impl.py`, `test_chain_in_worktree.py`) are trivial; the execute-model reconciliation is "engineering, not a rebase."

## 2. What it touched

- **New `megaplan/worktrees/` package (9 modules):** `identity`, `integration`, `lifecycle`, `patches`, `recovery`, `registry`, `report`, `secrets`, `paths` — serial per-task disposable worktrees, patch bundles, custody registry (`.megaplan/worktrees/registry/`), restartable integration FSM.
- **New top-level `megaplan/execute_resume_cursor.py`** + `megaplan/execute/migration.py`, `migration_close.py`, `migration_restart.py`, `workers/context.py`.
- **`megaplan/execute/core.py`** — worktree-lifecycle orchestration grafted onto the then-current core (+718 indicated in brief).
- **~18 new worktree/migration test files** (crash-injection / custody / patch-validation / recovery / registry / secrets) + 2 sprint briefs (`sprint-a-safety-substrate`, `sprint-b-serial-worktree-execute`).
- Migration commands surfaced via `cli.py` + `test_migrate_plan_cli.py` / `test_execute_migration.py`.

## 3. Did any surface land on main independently? — NO

Verified against main tree:
- `megaplan/worktrees/` package: **ABSENT on main** (main's only worktree code is `megaplan/bakeoff/worktree.py` and `megaplan/agent/tests/test_worktree*.py` — unrelated subsystems).
- `megaplan/execute_resume_cursor.py` (top-level): **ABSENT on main.**
- `megaplan/execute/migration*.py`: **ABSENT on main** (main's `migration` files are DB/store/legacy/openclaw — unrelated).
- Custody registry / crash-injection tests / restartable integration FSM: **none on main.**

**False-friend collisions (different concept, same words):** main DID land a *pipeline*-level resume cursor — `megaplan/_pipeline/resume.py`, `tests/test_pipeline_resume.py`, `tests/test_pipeline_resume_cursor.py`, `docs/pipeline-resume.md`, and `supabase/migrations/202605050001_add_plan_resume_cursor.sql`. This is NOT PR43's `execute_resume_cursor.py` — it is the pipeline-steps refactor's own resume mechanism. So zero PR43 surface landed; the namespace is now partly occupied by an orthogonal main feature.

**Execute/ refactor on main since the branch diverged:** Heavy. Merge-base is `4eab5113` (2026-05-21). At merge-base `megaplan/execute/` had only `__init__, core, merge, quality, step_edit, timeout`. Main now has additionally **`aggregation.py`, `batch.py`** — both introduced AFTER the merge-base (M5c "merge/aggregation/batch extraction + facade conversion", `6e69814c`, 2026-05-27; plus M5c evaluation split `05a433d4`; M6b dead-code removal `8f4019dd`, 2026-05-28). `execute/core.py` was split into a facade. PR43's +718-line graft onto the OLD monolithic `core.py` collides directly with this extraction/facade work.

## 4. Branch state / reachability

- **origin:** branch `worktree-execute-redesign` is **deleted** (`git ls-remote origin | grep worktree-execute` → none; `git branch -r` → none; no local branch).
- **Recoverable:** YES. `refs/pull/43/head` still resolves on GitHub to `4ef36402`, and that commit object is already present in the local object DB (`git cat-file -t 4ef36402` → commit). So the work is not lost — fetch `refs/pull/43/head` to revive.

## 5. Staleness assessment

- Merge-base `4eab5113` = 2026-05-21. Main tip = 2026-05-28. **97 commits on main since the merge-base; 17 of them touch `execute/`, `handlers/execute.py`, or `auto.py`** — exactly the contract surface PR43 redefines.
- The divergence is not time-large (~7 days) but **semantically large**: the per-task artifact → batch/`current_state` contract change and the core.py→facade/batch/aggregation split both happened squarely in PR43's blast radius. A `git rebase` would replay PR43's old-contract assumptions onto a tree whose execute contract and module layout have moved underneath it. The 5 failing `test_auto.py` cases are the canary — they encode the new contract PR43 doesn't satisfy.

## 6. Disposition recommendation

**Re-home as "Body 3 CodeRealizer" is still viable as a CONCEPT vehicle, but reviving/rebasing the branch as-is is NOT the path.**

Recommended disposition: **cherry-pick the concepts / re-implement against the new contract — do NOT rebase the branch.**

1. **Keep the branch as a reference artifact, not a merge candidate.** It is recoverable from `refs/pull/43/head`; no need to re-park it on origin. Pin the sha (`4ef36402`) in the Body 3 brief.
2. **Lift the `megaplan/worktrees/` package largely intact.** It is orthogonal/new (no main collision) — identity, paths, patches, registry, recovery, secrets, report can transplant with mostly mechanical fixups. This is the high-value, low-conflict 9-module core of the work.
3. **Re-implement the execute integration layer (`execute/core.py` graft, `execute_resume_cursor.py`, migration*.py) against main's current contract.** The old per-task artifact wiring is dead-on-arrival vs batch/`current_state` and the core→facade/batch/aggregation split. Treat the branch's `core.py` diff as a design spec, not patchable code. Use the 5 failing `test_auto.py` cases as the acceptance contract for the re-implementation.
4. **Salvage the test suite (~18 files) as behavioral specs.** Crash-injection / custody / patch-validation tests describe desired CodeRealizer behavior; port them, re-pointing at new module paths and the batch contract.

Effort reality: ~30% lift-and-shift (worktrees package), ~70% re-implementation (execute integration + contract reconciliation). The brief §14 framing of "kept and re-homed" survives; the implicit assumption that it's a live rebase-able draft does not.
