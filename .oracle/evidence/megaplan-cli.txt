Launcher resolution attempts: `python -m arnold_pipelines.megaplan config show`, `./.venv/bin/python -m arnold_pipelines.megaplan config show`, and `uv run python -m arnold_pipelines.megaplan config show` all reached the module but failed because this checkout's CLI has no `config` subcommand. `python -m arnold_pipelines.megaplan --help` succeeded; current commands expose `setup` rather than `config`. T11 work proceeded with the verified module launcher and repository defaults.
