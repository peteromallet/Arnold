1. **P0 — Discovery contract.** `discoverAgents(cwd)` precedence is **nearest project `.omp/agents` → user `~/.omp/agent/agents` → extension/plugin agents → bundled**, with first exact `agent.name` winning; bundled fills remaining names ([discovery.ts:62-67](.../discovery.ts#L62-L67), [discovery.ts:80-91](.../discovery.ts#L80-L91), [discovery.ts:120-137](.../discovery.ts#L120-L137)). Project lookup walks from `cwd` to filesystem root and selects the nearest `.omp/agents` directory ([config.ts:213-239](.../config.ts#L213-L239)). It does not require a Git root.

2. **P0 — Generator format.** `name` and `description` must be string-valued and truthy; omp enforces **no charset or length limit** ([helpers.ts:254-260](.../helpers.ts#L254-L260)). Use a path-safe name and make `frontmatter.name` equal the `<name>.md` basename: the task loader keys on frontmatter, while the CLI resolves filenames. `tools` accepts a string list or CSV; names are lowercased/deduped, `search→grep` and `find→glob`, and `yield` is appended ([helpers.ts:262-268](.../helpers.ts#L262-L268), [builtin-names.ts:40-61](.../builtin-names.ts#L40-L61)). `model` accepts string/CSV or string array. `thinking-level` is normalized to `thinkingLevel`; values are `auto`, `inherit`, `off`, `minimal`, `low`, `medium`, `high`, `xhigh`, `max`, plus unambiguous ≥2-character prefixes ([frontmatter.ts:9-18](.../frontmatter.ts#L9-L18), [thinking.ts:67-89](.../thinking.ts#L67-L89), [thinking.ts:204-206](.../thinking.ts#L204-L206)).

3. **P0 — Shadowing.** A project agent with the same exact name shadows user and bundled agents. A second-repo bot may safely reuse an existing name: inside that repo, its project definition wins; outside it, the user/bundled definition remains.

4. **P1 — CLI.** From the fresh repo root: `agent list`; one-shot `agent run <name> "message"`; interactive `agent run <name>` ([scripts/agent:25-33](.../scripts/agent#L25-L33), [scripts/agent:108-125](.../scripts/agent#L108-L125)). `OMP_BIN` overrides the `omp` executable ([scripts/agent:16-21](.../scripts/agent#L16-L21)).

5. **P1 — Gotchas.** CLI checks only `./.omp/agents`—no ancestor walk ([scripts/agent:51-60](.../scripts/agent#L51-L60)); task discovery does walk ancestors. `.gitignore` is irrelevant: existing files are read directly. Direct `.md` files and symlinks load; nested directories, non-`.md`, missing required fields, and parse failures skip ([discovery.ts:42-58](.../discovery.ts#L42-L58)). Name matching is exact/case-sensitive in code, but macOS filesystem case behavior can collide. No project discovery cache; bundled agents are lazily cached. `discoverAgents`’s `home` argument does not control the user directory; config resolves `os.homedir()` ([discovery.ts:70-78](.../discovery.ts#L70-L78), [config.ts:126-147](.../config.ts#L126-L147)).

**Verification:** targeted discovery suite: 5 passed; CLI help confirms invocation.
0