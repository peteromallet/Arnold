# [normal] Combined rework attempt 2, F2+F3 — GPT-5.6 Luna

Worktree: `/Users/peteromalley/Documents/arnold-oracle`. Execute `.oracle/rework/batch-6-7-attempt-2.md` findings **F2 and F3 ONLY**:
- F2: ONE coherent deployment story — `.agentbox/<name>.env` is required and the sole secret/deployment source. Fix `docs/custom-resident-agents.md` (env example described as token/mode/allowlists only) AND `agentbox/templates/resident/service.tmpl` (remove drop-in instructions + empty `Environment=DISCORD_BOT_TOKEN=`; replace with a comment requiring the name-specific env file before startup).
- F3: build + inspect the sdist, save output with the five shipped template paths to `.oracle/evidence/sdist-contents.txt`; add Batch 7 rows to `.oracle/evidence-matrix.md` (wheel/sdist shipping + clean-install generation + docs) referencing that receipt.
- Service-template assertion: add it to `tests/agentbox/test_resident_profile.py` (NOT test_cli.py — another agent owns it): generated service has no token `Environment=` directive and references the env-file requirement.
FILE CONSTRAINT: do not touch `tests/agentbox/test_cli.py`, `arnold_pipelines/megaplan/cli/__init__.py`, or `agentbox/cli.py`. Run `python -m pytest tests/agentbox/test_package_smoke.py tests/agentbox/test_resident_profile.py -q`. Do not commit. Report under 200 words.
