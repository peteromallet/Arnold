# [XHARD] Rework batch 5 (attempt 4) — stealth/ox-alpha via OpenRouter

Worktree: `/Users/peteromalley/Documents/arnold-oracle`. Execute `.oracle/rework/batch-5-attempt-4.md` verbatim: preflight-then-create ordering in `standalone_dispatch_paths()` — first `_require_standalone_operational_dir(..., create=False)` for ALL of `seeds`/`receipts`/`status` (existing symlink/non-dir/non-0700 rejects; absence recorded as eligible), then a second pass creates missing entries (`create=True`). Fresh publication unchanged. Add the regression test asserting zero directories created when publication rejects on unsafe `status` with missing siblings. Run the named validations + the standalone and two-file suites. Only `runtime_attestation.py` + its standalone test file may change. Do not commit. Report: hunks, test results, acceptance confirmations. Under 200 words.
