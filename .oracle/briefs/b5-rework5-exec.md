# [XHARD] Rework batch 5 (attempt 5) — stealth/ox-alpha via OpenRouter

Worktree: `/Users/peteromalley/Documents/arnold-oracle`. Execute `.oracle/rework/batch-5-attempt-5.md` verbatim: in `write_standalone_runtime_publication()`, accept `FileExistsError` reuse of an existing seed/receipt ONLY when it is a regular non-symlink file, mode exactly 0600, AND digest matches the expected immutable object — validate BOTH reused objects BEFORE any pointer update; otherwise reject without repair/chmod/mutation (pointer bytes unchanged). Valid 0600 idempotent reuse unchanged. Add named regression test `test_standalone_publication_rejects_unsafe_mode_reuse_without_advancing_pointer`. Run the named validations + standalone and two-file suites. Only `runtime_attestation.py` + its standalone test file may change. Do not commit. Report: hunks, test results, acceptance confirmations. Under 200 words.
