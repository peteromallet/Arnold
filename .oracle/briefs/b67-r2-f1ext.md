# [XHARD] Combined rework attempt 2, F1-ext — stealth/ox-alpha via OpenRouter

Worktree: `/Users/peteromalley/Documents/arnold-oracle`. Execute `.oracle/rework/batch-6-7-attempt-2.md` finding **F1-ext ONLY**: skip `maybe_auto_sync_repo_editor_support(Path.cwd())` for every top-level `resident` dispatch (`arnold_pipelines/megaplan/cli/__init__.py` ~3561), preserving it for all non-resident commands. Extend `test_resident_dispatch_creates_only_resident_owned_state` in `tests/agentbox/test_cli.py`: absent `.gitattributes` stays absent; pre-existing sentinel content byte-identical; `.vscode/settings.json` not created. Run the named validation + `python -m pytest tests/agentbox/test_cli.py -q`. Only `cli/__init__.py` + `tests/agentbox/test_cli.py`. Do not commit. Report: hunks, results, confirmations. Under 150 words.
