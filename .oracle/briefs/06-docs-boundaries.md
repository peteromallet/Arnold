# Brief — Area 6: Documentation boundaries

Explore this area in depth in `/Users/peteromalley/Documents/arnold-oracle` (worktree). Link: `.oracle/agent_goal.md`, `.oracle/northstar.md`.

Goal: R3 adds `docs/custom-resident-agents.md`. Establish existing terminology, doc conventions, and boundary docs so the new doc reuses established language and doesn't duplicate or contradict.

Read:
- `docs/agentbox-resident-boundary.md` (if present) — the resident/agentbox boundary terminology.
- List `docs/` (top-level) and note any docs covering: resident, agentbox, discord, omp agents, megaplan resident config, systemd/cloud deployment (`docs/cloud.md`), configuration (`docs/configuration.md` — env map), agent-runtime integration (`docs/agent-runtime-integration.md`).
- The README's "Some other features" section style (short, command-first).
- `.oracle/northstar.md` and `.oracle/agent_goal.md` for the terms the run uses.

Report (verified facts, file:line): (1) existing docs that touch resident/agentbox/omp-agent topics (titles + one-line content + path); (2) the boundary doc's key terminology (what "resident", "profile", "transport" mean there); (3) doc conventions (heading style, command blocks, where docs link from README); (4) gaps the new doc must fill without duplicating; (5) unknowns and risks. Ranked findings, <250 words.
