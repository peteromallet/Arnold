# [XHARD] Rework batch 5 (attempt 3) — stealth/ox-alpha via OpenRouter

Worktree: `/Users/peteromalley/Documents/arnold-oracle`. Execute `.oracle/rework/batch-5-attempt-3.md` verbatim: add a non-mutating standalone state resolver/validator used by every load/read path (validates existing root/state/operational dirs without mkdir/chmod/writes); creation + 0700 normalization preserved ONLY for true publication and process-create paths; cloud behavior unchanged. Add `test_standalone_load_rejection_does_not_mutate_filesystem` (rejected loads create no dirs, change no modes — missing parents/state/children and unsafe existing state). Run the three named validations. Only `runtime_attestation.py` + its standalone test file may change. Do not commit. Report: hunks, test results, acceptance confirmations. Under 200 words.
