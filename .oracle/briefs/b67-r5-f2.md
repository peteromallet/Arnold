# Rework batch 6-7 attempt 5, F2 — stealth/ox-alpha via OpenRouter

Worktree: `/Users/peteromalley/Documents/arnold-oracle`. Execute `.oracle/rework/batch-6-7-attempt-5.md` finding **F2 ONLY**: immediately before pointer replacement in `write_standalone_runtime_publication()`, `lstat` any existing pointer (`seeds/dispatch-current.json`) and require regular file, non-symlink, exact `0600` — otherwise reject without changing bytes or mode. Missing pointers remain creatable. Keep it standalone-specific; do not alter shared `_atomic_write()` semantics. Add named test `test_standalone_publication_rejects_unsafe_existing_pointer_without_replacing`; retain valid `0600` replacement + seed/receipt reuse coverage. Run the two named tests + the standalone suite. Only `cloud/runtime_attestation.py` + `tests/cloud/test_standalone_runtime_attestation.py` may change. Do not commit. Report under 150 words.
