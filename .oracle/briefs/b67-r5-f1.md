# [XHARD] Rework batch 6-7 attempt 5, F1 — stealth/ox-alpha via OpenRouter

Worktree: `/Users/peteromalley/Documents/arnold-oracle`. Execute `.oracle/rework/batch-6-7-attempt-5.md` finding **F1 ONLY**: thread the resolved project `root` through `_resident_discord` → `_require_discord_runtime_launch` → standalone launch validation; for standalone authority reject `seed["project_root"] != root` with typed `runtime_launch_attestation_mismatch` BEFORE process-status mutation or profile/runner/service construction. Listener recovery + cloud authority unchanged. Add named test `test_generated_resident_startup_rejects_cross_project_seed_before_profile_or_service_start` (B seed + A launch → typed failure, no process receipt written, profile/runner/service counters zero; matching seed succeeds). Run the named test + the standalone and resident-profile suites. Only `resident/cli.py`, `cloud/runtime_attestation.py`, `tests/agentbox/test_resident_profile.py`, `tests/cloud/test_standalone_runtime_attestation.py` may change. Do not commit. Report under 200 words.
