[Unit]
Description=Ensure Megaplan Discord resident tmux session is running
After=docker.service
Requires=docker.service

[Service]
Type=oneshot
# Execute the ensure script from the workspace checkout so resident-ensure
# surfaces cannot materialize unguarded legacy repair authority.
ExecStart=/workspace/arnold/arnold_pipelines/megaplan/cloud/systemd/ensure-megaplan-resident
