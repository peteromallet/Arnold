# `solo` — tier 1. The all-cheap rung of the rubric ladder.
#
# Canonical name for the tier-1 profile (legacy alias: `basic` — both names
# remain loadable while consumers migrate).
#
# DeepSeek V4 Pro handles reasoning phases end-to-end — author, mechanical,
# and sense-check (plan, prep, critique, revise, gate, review, loop_*,
# tiebreakers). Finalize uses the premium floor when premium credentials are
# available, with DeepSeek fallback handled by the resolver.
#
# Use for discovery, docs, and most routine programming — the rubric's tier-1
# bucket where the patterns are stable and the variables are few. Don't
# over-reach: this tier handles a wider range of "I know what to do, just need
# it written" work than the instinct to bump up to `partnered` suggests.
#
# Flag interactions:
#
#   * `--vendor claude|codex` — picks the premium finalize and high execute
#     tier vendor when that credential is available. Reasoning phases stay
#     DeepSeek.
#   * `--critic cross` — would expand to "the other premium" relative to the
#     resolved vendor, but since there's no premium in the profile to begin
#     with this is mostly a footgun — prefer to step up to `directed` or
#     `partnered` if you want a premium critic.
#   * `--phase-model phase=spec` — always honored; the per-phase escape
#     hatch overrides whatever this profile sets.
#
# See docs/megaplan-prep.md for the full rubric.
[profiles.solo]
plan                  = "hermes:deepseek:deepseek-v4-pro"
prep                  = "hermes:deepseek:deepseek-v4-pro"
critique              = "hermes:deepseek:deepseek-v4-pro"
critique_evaluator    = "hermes:deepseek:deepseek-v4-pro"
revise                = "hermes:deepseek:deepseek-v4-pro"
gate                  = "hermes:deepseek:deepseek-v4-pro"
finalize              = "premium:low"
execute               = "hermes:deepseek:deepseek-v4-pro"
feedback              = "hermes:deepseek:deepseek-v4-pro"
loop_plan             = "hermes:deepseek:deepseek-v4-pro"
loop_execute          = "hermes:deepseek:deepseek-v4-pro"
review                = "hermes:deepseek:deepseek-v4-pro"
tiebreaker_researcher = "hermes:deepseek:deepseek-v4-pro"
tiebreaker_challenger = "hermes:deepseek:deepseek-v4-pro"

[profiles.solo.tier_models.execute]
1 = "hermes:deepseek:deepseek-v4-flash"
2 = "hermes:deepseek:deepseek-v4-flash"
3 = "hermes:deepseek:deepseek-v4-pro"
4 = "hermes:deepseek:deepseek-v4-pro"
5 = "claude:claude-sonnet-4-6"
6 = "claude:claude-sonnet-4-6"
7 = "claude:claude-sonnet-4-6"
8 = "claude:claude-sonnet-4-6"
9 = "claude:claude-sonnet-4-6"
10 = "claude:claude-sonnet-4-6"

[profiles.solo.tier_models.critique]
1 = "hermes:deepseek:deepseek-v4-flash"
2 = "hermes:deepseek:deepseek-v4-pro"
3 = "hermes:deepseek:deepseek-v4-pro"
4 = "hermes:deepseek:deepseek-v4-pro"
5 = "hermes:deepseek:deepseek-v4-pro"
