You are already running inside the megaplan harness for this step. Do the requested planning/review/execution work directly. Do NOT invoke the `megaplan` CLI, do NOT read or activate the `megaplan` skill, do NOT start nested megaplan plans, and do NOT recurse into another planning harness. Treat mentions of megaplan in the repository or environment as implementation context only.

WRITE ACCESS CONTRACT: You are running with auto-approved writes inside a writable workspace. Treat the working directory as fully writable. Do NOT preemptively skip or block tasks on permission, sandbox, or read-only grounds. Attempt every required edit; only report failure AFTER a real OS-level rejection from a specific shell command. Do not infer 'read-only' from absence of activity; absence is not denial. If a single shell command unexpectedly fails, retry with a different invocation before concluding the environment is restricted.

## Anchor Context: North Star

These anchors are durable alignment targets captured at plan initialization. They are not ordinary notes, generated success criteria, or optional background. Use them to keep this stage aligned with the end-state intent.

Scope map:
- Epic North Star: overall chain/epic objective. Preserve this across all milestones.
- Plan/Sprint/Milestone North Star: current plan or milestone objective. It extends or operationalizes the epic North Star for this sprint; it does not override it.

If local instructions, generated plan content, or a plan-level anchor appear to conflict with an epic anchor, do not resolve the conflict silently. Surface an explicit anchor conflict and explain what decision, replan, or user approval would be needed.

### Epic North Star: North Star: Extension Reality Convergence
Source: `.megaplan/initiatives/extension-reality-convergence-epic/NORTHSTAR.md`
Captured artifact: `anchors/north_star/epic.md`
Checksum: `5ed92f91a987b5fafb1e8011624176237e2b9fe3164b07cde7fa7d1f59b4baf9`

```md
# North Star: Extension Reality Convergence

## End State

The Reigh video-editor extension system is truthful and converged across trust, composition authority, and export readiness. Extensions are presented as trusted local code unless a later untrusted-runtime epic adds real isolation. Composition facts used by planner/export/preview flow through a canonical authority spine instead of scattered legacy timeline reads. Export readiness is decided by planner-owned blockers, with guard scans and provider route checks demoted to structured planner inputs.

## Success In One Sentence

From a clean checkout on top of the completed extension-foundation work, Reigh can prove that extension trust claims are honest, the first composition fact family has moved to graph authority, and every user-facing export block originates from canonical planner readiness.

## Immutable Constraints

- Do not implement fake or partial permission enforcement and call it sandboxing.
- Do not claim marketplace, third-party, or untrusted extension safety.
- Keep extension code trusted and unsandboxed unless a separate future epic introduces isolation and brokered APIs.
- Do not delete legacy timeline storage fields before compatibility projection and regression tests exist.
- Do not let migrated fact families remain authoritative through direct legacy timeline reads.
- Do not add a second export readiness authority beside the planner.
- Do not weaken quality gates, docs checks, or release checks to make the epic pass.
- Preserve existing editor behavior for timelines and packages that do not use migrated composition features.
- Keep generated-module, provider, and export-guard scans where useful, but make them feed planner readiness instead of bypassing it.

## Required Proof

- SDK, JSON schema, examples, docs, and manager UI agree on the trusted-code/access-disclosure model.
- A regression gate fails if docs or UI imply sandboxing, runtime permission enforcement, marketplace safety, or third-party safety.
- The first composition authority ratchet proves shader/ref readiness comes from graph-projected facts, not direct legacy shader fields.
- A fixture proves legacy-only shader/ref fields do not count as authoritative after projection is disabled.
- User-facing export/render blocked states are derived from planner blockers.
- Generated-module artifact state, provider availability, output-format availability, contribution conflicts, shader/material blockers, and live-binding blockers are represented in planner readiness.
- Quality checks pass from a clean checkout with the convergence gates enabled.

## Explicitly Deferred

- True permission enforcement through iframe, Worker, process, SES-like isolation, or a brokered host API.
- Marketplace install, remote extension discovery, update/delete flows, code signing, and dependency resolution.
- Full composition-spine completion beyond the fact families named by this epic.
- Full process/sidecar runtime hardening unless required as an input to export-readiness diagnostics.
```

### Current Stage
Stage: `finalize`
Plan directory: `/workspace/extension-reality-convergence-epic/reigh-app/.megaplan/plans/m4-evidence-and-drift-gates-20260708-0120`
Current milestone: `m4-evidence-and-drift-gates`

### Phase Instruction
Convert the approved plan into executable tasks while preserving North Star constraints and visible alignment checks.

### Conflict Note
Plan-level anchors extend epic anchors. They do not override them silently. When in doubt, flag the tension rather than optimizing around it.

You are DECOMPOSING the approved plan below into an ordered task DAG. The plan has been
        critiqued, gated, and possibly revised. Do NOT re-evaluate strategy or re-litigate
        decisions - gate has settled those.

        Project directory:
        /workspace/extension-reality-convergence-epic/reigh-app

        Brief summary: # M4: Evidence And Drift Gates
(Full brief in state.idea; success criteria in plan_v1.meta.json.)

        Approved plan:
        # Implementation Plan: M4 Evidence And Drift Gates

## Overview
M4 should prove the epic’s claims rather than add new product scope. The repository largely matches the brief, with two corrections from targeted inspection: `scripts/quality/check-readiness.mjs` already has a valid `extractTestReadinessFiles()` tokenizer in this checkout, and render/export files live under `src/tools/video-editor/runtime/`, not `rendering/`. The main anchor tension is the brief’s suggestion to document the fast-path render bypass as a limitation: that would contradict the North Star requirement that user-facing export/render blocks originate from planner blockers. Treat the bypass as a bridge only if it is converted into planner-provenance behavior during M4.

## Phase 1: Evidence Baseline And Failed Audits

### Step 1: Verify readiness infrastructure before changing it (`scripts/quality/check-readiness.mjs`)
**Complexity: 1**
1. Run `node scripts/quality/check-readiness.mjs --strict` and `npm run test:readiness` first.
2. If the parser still fails, patch only `extractTestReadinessFiles()` to preserve the current intended behavior: split the `test:readiness` script, skip `--config` plus its value, ignore flags, and return `.test/.spec` file paths.
3. Do not weaken strict readiness checks or remove required evidence rows.

### Step 2: Complete A2 and A5 as explicit audit notes (`.megaplan/plans/m4-evidence-and-drift-gates-20260708-0120/audits/`)
**Complexity: 2**
1. Create `a2-trust-gate-gaps.md` listing trust-sensitive M2/M3 surfaces and whether `check-trust-model-truth.mjs` currently scans them.
2. Create `a5-docs-matrix-reconciliation.md` listing docs rows that contradict code/tests, starting with D-020/D-021/D-022 and B-002.
3. Keep the audits factual and scoped to trust, composition authority, export readiness, and docs matrices.

## Phase 2: Strengthen Drift Gates

### Step 3: Expand the trust-model truth gate (`scripts/quality/check-trust-model-truth.mjs`)
**Complexity: 2**
1. Update docs target selection so trust-sensitive video-editor docs such as `shader-execution-model.md`, planner/export docs, render docs, and composition-spine docs are scanned even when the filename does not contain `extension`, `trust`, or `readiness`.
2. Add a fixture that proves an unsupported sandbox/permission claim in a newly covered M2/M3-style doc fails the release gate.
3. Extend `scripts/quality/check-trust-model-truth.test.mjs` to assert both the new scan coverage and the existing package-script wiring.

### Step 4: Expand shader/ref authority coverage (`src/tools/video-editor/runtime/composition/shaderRefAuthority.test.ts`)
**Complexity: 2**
1. Expand `MIGRATED_READINESS_FILES` from two files to the six readiness paths named by the brief: `runtime/renderPlanner.ts`, `runtime/exportGuard.ts`, `lib/renderRouter.ts`, `hooks/useRenderState.ts`, `runtime/composition/routeScopeValidation.ts`, and `runtime/renderability.ts`.
2. Re-run the test before adding any allowlist entry. Current regexes do not appear to match `hasTimelineShaderMetadata`, so avoid documenting a false exception unless the expanded test actually fails.
3. If a legitimate wrapper trips the scanner, add the narrowest line/path exception with a reason that says why the graph-projected path remains authoritative.

### Step 5: Add the missing graph-authority negative fixture (`src/tools/video-editor/runtime/composition/compositionGraphProjector.test.ts`)
**Complexity: 2**
1. Add a dedicated test for the North Star fixture: legacy-only shader/ref fields must not produce authoritative consumes edges when projection is disabled for that clip/ref.
2. Use existing `projectCompositionGraph()` helpers and assert the projected graph lacks the shader consumes edge for the disabled projection case.
3. Keep `shaderValidation.test.ts` as supporting evidence, not the primary proof, because it validates shader ref projection rather than graph projector authority loss.

### Step 6: Make fast-path render blocks planner-owned (`src/tools/video-editor/hooks/useRenderState.ts`)
**Complexity: 3**
1. Convert `getFastRenderRouteDecision()` blocked decisions into planner diagnostics/blockers before setting user-facing `renderStatus` or `renderLog`.
2. Preserve current route behavior for missing generated Remotion artifacts and worker/external routes, but have the displayed block message come from a `RenderBlocker` returned by `planRender()`.
3. Update `useRenderState.test.tsx` so preview-only and worker/external fast-path blocks fail if they bypass planner blockers or only assert raw route strings.
4. This is a bridge from the existing compatibility optimization to the North Star, not a contradiction: the fast route may remain an optimization, but the user-facing blocked state must be planner-provenanced.

## Phase 3: Reconcile Docs And Proof

### Step 7: Reclassify render planner docs after the code fix (`docs/video-editor/extension-platform-supported-deferred.md`)
**Complexity: 3**
1. Reconcile D-020/D-021/D-022 and B-002 against the actual planner implementation and the fast-path planner-provenance fix.
2. Update `extension-platform-contract-recheck.md` rows M12-001, M12-002, and M12-007 so their status/evidence matches `renderPlanner.ts`, `exportGuard.ts`, and `useRenderState.ts`.
3. Update `frontend-closure-matrix.md` only where it repeats stale export blocker or planner-readiness claims.
4. Do not claim future process/sidecar/export capabilities as supported unless tests already prove them.

### Step 8: Produce the North Star proof note (`.megaplan/plans/m4-evidence-and-drift-gates-20260708-0120/proof-note.md`)
**Complexity: 2**
1. Create a concise proof note mapping all seven North Star required proof items to exact files/tests/scripts.
2. Include the command evidence needed to prove the gates: trust model, shader/ref authority, export blocker provenance, docs checks, readiness, and aggregate quality.
3. Call out any remaining deferred work only if it is outside current release claims and does not hide behind supported language.

## Validation Order
1. Run the cheapest blockers first: `node scripts/quality/check-readiness.mjs --strict`, `node scripts/quality/check-trust-model-truth.mjs --release`, and focused vitest files.
2. Run targeted tests after each gate change: trust gate test, shader authority test, composition projector test, render planner/export guard tests, and `useRenderState.test.tsx`.
3. Run docs checks after docs reconciliation: `npm run check:contract-recheck`, `npm run check:deferred-claims`, `npm run check:frontend-closure`, and `npm run check:docs-maturity-sync`.
4. Finish with `npm run test:readiness` and `npm run quality:check` from a clean working state, preserving unrelated user/generated work.

## Execution Order
1. Verify readiness and complete A2/A5 audit notes before editing gates.
2. Land trust and composition drift gates before relying on the final proof note.
3. Fix export blocker provenance before reclassifying D-020/D-021/D-022 as supported.
4. Write the proof note last so it cites final files and passing commands, not stale intermediate evidence.


        Plan metadata:
        {
  "version": 1,
  "timestamp": "2026-07-08T01:39:32Z",
  "hash": "sha256:e1eb478dbcd602ad2fb6b65f1edce0d8d6d7b84f9ae2bcec3ca41600fe2cf27f",
  "questions": [
    "Should D-021 and D-022 be reclassified as supported only if A5 finds direct tests for UI next actions and public TimelineSnapshot/TimelineReader inspection, or should partial implementation remain explicitly deferred?",
    "For final validation, is a clean local checkout required, or is a clean cloud/worktree run acceptable if this workspace contains unrelated generated `.megaplan` artifacts?"
  ],
  "success_criteria": [
    {
      "criterion": "`npm run test:readiness` passes, and any readiness parser change preserves strict evidence validation instead of skipping rows.",
      "priority": "must",
      "requires": [
        "run_tests",
        "read_build_output"
      ]
    },
    {
      "criterion": "The trust-model gate scans newly identified M2/M3 trust-sensitive docs and fails on an active unsupported sandboxing, permission-enforcement, marketplace, third-party, or untrusted-extension claim in those surfaces.",
      "priority": "must",
      "requires": [
        "read_files",
        "run_tests",
        "parse_diff"
      ]
    },
    {
      "criterion": "The shader/ref authority gate covers all six migrated readiness files and fails on raw legacy shader/ref authority reads in those files.",
      "priority": "must",
      "requires": [
        "read_files",
        "run_tests",
        "parse_diff"
      ]
    },
    {
      "criterion": "`compositionGraphProjector.test.ts` proves legacy-only shader/ref fields do not become authoritative consumes edges when projection is disabled.",
      "priority": "must",
      "requires": [
        "run_tests",
        "read_files"
      ]
    },
    {
      "criterion": "Every user-facing render/export blocked state touched by the fast path is derived from a planner `RenderBlocker`, with tests that fail on direct route-string-only blocking.",
      "priority": "must",
      "requires": [
        "run_tests",
        "parse_diff",
        "read_files"
      ]
    },
    {
      "criterion": "Docs matrices no longer classify implemented planner/blocker behavior as deferred or hide unresolved blockers behind supported language.",
      "priority": "must",
      "requires": [
        "read_files",
        "parse_diff",
        "run_linter"
      ]
    },
    {
      "criterion": "The final proof note maps all seven North Star required proof items to concrete files, tests, scripts, or docs.",
      "priority": "must",
      "requires": [
        "read_files",
        "parse_diff"
      ]
    },
    {
      "criterion": "`npm run quality:check` passes after the gate, docs, and proof-note changes.",
      "priority": "must",
      "requires": [
        "run_tests",
        "read_build_output",
        "run_linter"
      ]
    },
    {
      "criterion": "Changes stay scoped to M4 evidence, drift gates, and docs reconciliation without adding sandboxing, marketplace, or new composition fact-family migrations.",
      "priority": "should",
      "requires": [
        "parse_diff",
        "subjective_judgment"
      ]
    },
    {
      "criterion": "Audit notes are concise enough for a reviewer to use as evidence and do not duplicate unrelated historical plan material.",
      "priority": "should",
      "requires": [
        "read_files",
        "subjective_judgment"
      ]
    }
  ],
  "assumptions": [
    "The current checkout\u2019s `check-readiness.mjs` syntax is valid; strict readiness validation, not the stale line-number report, determines whether a patch is needed.",
    "The fast-path route optimization may remain, but any user-facing blocked status/log must be planner-provenanced to satisfy the epic North Star.",
    "D-020 can only move to supported after the fast-path planner-provenance fix lands; D-021/D-022 depend on A5 evidence and may remain deferred if proof is incomplete.",
    "The proof note belongs in `.megaplan/plans/m4-evidence-and-drift-gates-20260708-0120/proof-note.md` because M4 explicitly asks for a milestone artifact.",
    "No destructive git cleanup is needed; unrelated `.megaplan` or generated working-tree artifacts should be preserved."
  ],
  "test_blast_radius": {
    "strategy": "scoped",
    "confidence": "low",
    "selectors": [
      {
        "kind": "path",
        "value": "scripts/quality/check-readiness.mjs",
        "reason": "Strict readiness parser and evidence gate are the first pipeline blocker."
      },
      {
        "kind": "path",
        "value": "scripts/quality/check-trust-model-truth.test.mjs",
        "reason": "Covers trust gate scan targets, fixtures, and package wiring."
      },
      {
        "kind": "path",
        "value": "src/tools/video-editor/runtime/composition/shaderRefAuthority.test.ts",
        "reason": "Composition authority drift gate."
      },
      {
        "kind": "path",
        "value": "src/tools/video-editor/runtime/composition/compositionGraphProjector.test.ts",
        "reason": "North Star legacy-not-authoritative fixture."
      },
      {
        "kind": "path",
        "value": "src/tools/video-editor/hooks/useRenderState.test.tsx",
        "reason": "User-facing render/export blocked-state provenance."
      },
      {
        "kind": "path",
        "value": "src/tools/video-editor/runtime/renderPlanner.test.ts",
        "reason": "Planner blocker behavior and route plans are part of the provenance chain."
      },
      {
        "kind": "path",
        "value": "src/tools/video-editor/runtime/exportGuard.test.ts",
        "reason": "Export guard diagnostics feed planner readiness."
      },
      {
        "kind": "path",
        "value": "docs/video-editor/extension-platform-supported-deferred.md",
        "reason": "Docs matrix reconciliation target."
      }
    ],
    "changed_surfaces": [
      "scripts/quality/check-readiness.mjs",
      "scripts/quality/check-trust-model-truth.mjs",
      "scripts/quality/check-trust-model-truth.test.mjs",
      "scripts/quality/__fixtures__/trust-model-truth/active-unsupported/docs/video-editor/shader-execution-model.md",
      "src/tools/video-editor/runtime/composition/shaderRefAuthority.test.ts",
      "src/tools/video-editor/runtime/composition/compositionGraphProjector.test.ts",
      "src/tools/video-editor/hooks/useRenderState.ts",
      "src/tools/video-editor/hooks/useRenderState.test.tsx",
      "docs/video-editor/extension-platform-supported-deferred.md",
      "docs/video-editor/extension-platform-contract-recheck.md",
      "docs/video-editor/frontend-closure-matrix.md",
      ".megaplan/plans/m4-evidence-and-drift-gates-20260708-0120/audits/a2-trust-gate-gaps.md",
      ".megaplan/plans/m4-evidence-and-drift-gates-20260708-0120/audits/a5-docs-matrix-reconciliation.md",
      ".megaplan/plans/m4-evidence-and-drift-gates-20260708-0120/proof-note.md"
    ],
    "always_run": [
      "node scripts/quality/check-readiness.mjs --strict",
      "node scripts/quality/check-trust-model-truth.mjs --release",
      "npm run check:contract-recheck",
      "npm run check:deferred-claims",
      "npm run check:frontend-closure",
      "npm run check:docs-maturity-sync",
      "npm run test:readiness",
      "npm run quality:check"
    ],
    "full_suite_fallback": true,
    "rationale": "Floor: Non-Python data/fixture/golden changes force the full suite because they can affect tests outside the Python import graph: scripts/quality/check-readiness.mjs, scripts/quality/check-trust-model-truth.mjs, scripts/quality/check-trust-model-truth.test.mjs, src/tools/video-editor/hooks/useRenderState.test.tsx, src/tools/video-editor/hooks/useRenderState.ts, src/tools/video-editor/runtime/composition/compositionGraphProjector.test.ts, src/tools/video-editor/runtime/composition/shaderRefAuthority.test.ts. Ignored documentation-only change(s) for selector derivation: docs/video-editor/extension-platform-contract-recheck.md, docs/video-editor/extension-platform-supported-deferred.md, docs/video-editor/frontend-closure-matrix.md, megaplan/plans/m4-evidence-and-drift-gates-20260708-0120/audits/a2-trust-gate-gaps.md, megaplan/plans/m4-evidence-and-drift-gates-20260708-0120/audits/a5-docs-matrix-reconciliation.md, megaplan/plans/m4-evidence-and-drift-gates-20260708-0120/proof-note.md, scripts/quality/__fixtures__/trust-model-truth/active-unsupported/docs/video-editor/shader-execution-model.md. Candidate: Use focused checks while developing each gate, but M4 is an evidence milestone and must finish with the aggregate quality pipeline from a clean state.",
    "import_graph": {
      "degraded": false,
      "dependent_tests": 0,
      "unresolved": []
    }
  },
  "structure_warnings": []
}

        Gate summary:
        {
  "version": 1,
  "recommendation": "PROCEED",
  "passed": true,
  "rationale_brief": "All preflight checks pass, critique assessment is clear across all five dimensions (correctness, scope, all_locations, verification, prerequisite_ordering), and there are zero significant, unresolved, or addressed flags. The plan is well-formed with explicit execution and validation ordering, correctly handles the one anchor tension (fast-path render bypass as planner-provenance bridge, not a limitation), and respects all North Star constraints including the immutable constraint against claiming sandboxing/marketplace/third-party safety. Scope is appropriately constrained to evidence producti...",
  "settled_decisions": [
    {
      "id": "SD1",
      "decision": "The fast-path render bypass remains as an optimization but all user-facing blocked states must originate from planner RenderBlockers",
      "rationale": "This resolves the anchor tension between the brief's suggestion to document the bypass as a limitation and the North Star requirement that export blocks originate from planner blockers. The plan correctly treats this as a conversion (bridge), not a contradiction. The fast route may remain for optimization, but the user-facing blocked status/log must be planner-provenanced. This is captured in Step 6 and Criterion 5."
    },
    {
      "id": "SD2",
      "decision": "D-020 moves to supported only after the fast-path planner-provenance fix lands; D-021/D-022 depend on A5 evidence and may remain deferred if proof is incomplete",
      "rationale": "This is the correct ordering constraint. The plan explicitly ties docs reclassification to code changes landing first (Step 7 follows Step 6), preventing the anti-pattern of claiming support before implementation exists. The assumption document reinforces this dependency."
    },
    {
      "id": "SD3",
      "decision": "The proof note belongs in .megaplan/plans/m4-evidence-and-drift-gates-20260708-0120/proof-note.md as a milestone artifact, not in the epic initiative directory",
      "rationale": "The plan explicitly places the proof note in the milestone plan directory. This keeps M4 evidence self-contained and avoids polluting the epic-level initiative directory with per-milestone artifacts. The epic North Star's required proof items are mapped to M4 deliverables, not duplicated."
    },
    {
      "id": "SD4",
      "decision": "No destructive git cleanup; unrelated .megaplan or generated working-tree artifacts preserved",
      "rationale": "Explicitly called out in plan assumptions and aligns with the North Star constraint to preserve existing editor behavior for timelines and packages that do not use migrated composition features. A clean checkout validation is not the same as destructive cleanup."
    }
  ],
  "warnings": [
    "Criteria 9 (scope containment) and 10 (audit conciseness) are 'should' priority and involve subjective judgment. Do not let these block a PROCEED if all 8 'must' criteria are satisfied and the deviations are minor.",
    "The plan's test_blast_radius.import_graph shows 'dependent_tests: 0' which may undercount transitive dependencies through shared fixtures; the full_suite_fallback is correctly set to true as a safety net.",
    "Ensure the proof note (Step 8) is written last as planned \u2014 stale intermediate evidence would undermine the milestone's credibility."
  ],
  "orchestrator_guidance": "First iteration; follow gate recommendation: PROCEED.",
  "carried_flags": [],
  "iteration": 1,
  "produced_at": "2026-07-08T01:47:00Z"
}

        Your output template is at: /workspace/extension-reality-convergence-epic/reigh-app/.megaplan/plans/m4-evidence-and-drift-gates-20260708-0120/finalize_output.json
        Read this file first — it contains the expected JSON structure (tasks, user_actions, sense_checks, watch_items, meta_commentary).
        Fill the JSON structure with your results and write the file back.
        If you cannot use file tools, return the populated JSON structure inline as your response instead.

        Requirements:
        - Produce structured JSON only.
        - For each `## Step N:` in the plan, emit 1-N tasks.
        - For each task, emit one sense_check.
        - Default `user_actions` to `[]`. Identify a user_action ONLY when the work is genuinely non-mechanical and the executor literally cannot do it (secrets the human alone holds, identity-bound infra access, legal/license signatories, manual UI smoke tests on production). If a check is mechanical, make it a task — not a user_action. See the detailed guidance below.
        - Do not invent tasks that don't trace to a plan step.
        - How batching works at runtime — shape `depends_on` with this in mind, not just literal sequencing. Tasks that share the same `depends_on` set form one batch (capped at 5). Each batch dispatches as a SINGLE LLM conversation to a SINGLE model, picked by the max(complexity) in that batch. So a c=4 audit plus three c=2 tweaks sharing a batch all run in one turn on the c=4 model. Two consequences you control via DAG shape:
          - Routing: every task in a batch runs on the highest-tier task's model — bundle a c=2 task beside a c=4 sibling and it runs on the pricier model.
          - Cognitive load: one conversation holds all of the batch's tasks in working context. The more disparate the tasks, the higher the risk the model loses the thread or claims completion without doing the work. Wide, mixed batches are the dominant quality failure mode.
        - Three principles for shaping batches — judgment, not arithmetic (there is no mechanical split rule):
          1. Isolate heavyweights. A c=4 or c=5 task usually deserves its own batch. Chain lighter tasks through it via `depends_on` rather than placing them as siblings.
          2. Bundle context-related light work. Several c=2/c=3 tasks touching the same files or contracts batch well together.
          3. Never emit more than 5 actionable parallel siblings. If a step legitimately fans out wider, linearize via `depends_on` so the runtime batcher sees at most 5 at a time.
        - Do not include `validation` or `coverage_complete` fields - the harness computes those.
        - The FINAL task MUST run tests; harness validation will reject finalize output without it.
        - `tasks` must be an ordered array of task objects. Every task object must include:
          - `id`: short stable task ID like `T1`
          - `description`: concrete work item
          - `depends_on`: array of earlier task IDs or `[]`
          - `status`: always `"pending"` at finalize time
          - `executor_notes`: always `""` at finalize time
          - `reviewer_verdict`: always `""` at finalize time
          - `kind`: indicating the type of work. One of:
            - `code`: writes or modifies source files (executor must produce `files_changed`).
            - `test`: writes or modifies test files, or runs the test suite (executor must produce `files_changed` in tests/ OR `commands_run` containing pytest/test invocations).
            - `audit`: read-only investigation — grep, code review, schema inspection (executor evidence is `executor_notes` describing findings; no `files_changed` expected).
            - `research`: external research or non-code investigation (executor evidence is `executor_notes`).
            - `docs`: writes documentation files (executor must produce `files_changed`).
            If unsure, default to `code`.
          - `complexity`: integer 1–5 complexity score. This score routes the task to a
            model at execution time, so adjudicate it deliberately — do not guess. Use the rubric:
            - 1 = trivial, mechanical, single-file change with no logic to reason about
                  (rename, constant bump, comment, import). A weak model cannot get it wrong.
            - 2 = simple, localized change plus the obvious test update; one file or two,
                  logic is linear and the failure mode is obvious.
            - 3 = multi-file change with non-trivial logic, control flow, or data shape the
                  executor must hold in its head; correctness is not self-evident from the diff.
            - 4 = a wrong implementation would be NON-LOCAL and not caught by an obvious test:
                  the error propagates through a shared contract or invariant that code you did
                  NOT edit relies on. Touching several files is NOT sufficient on its own — a
                  mechanical multi-file change whose failure is local and test-evident is tier 2-3.
            - 5 = fundamental system-invariant change with high regression risk: concurrency that
                  cascades, a schema/wire-format others build on, a state machine, or a security/
                  auth boundary. ONLY when a subtle error would pass the full suite and corrupt
                  state or break a system-wide contract — not merely "important" or central code.
            Score on the hardest REALISTIC aspect of the task, not a worst-case imagining. When
            genuinely torn between two tiers, choose the LOWER one UNLESS you can name the specific
            cascading, test-evading failure that earns the higher tier in the justification.
            You are not the model that executes this task, and the score neither saves nor
            costs you anything — so do not lowball to seem efficient and do not highball to
            play safe. Both distort routing: a too-low score sends a hard task to a model
            that fails it; a too-high score burns a premium model on trivial work. Score
            what the task honestly requires, no higher and no lower.
            FLOOR: a task is NEVER below tier 4 only when it GENUINELY changes the semantics of
            concurrency, a schema/state-machine, a security/auth boundary, or a contract other
            code actually depends on — because a subtle error there passes tests but is wrong.
            This floor does NOT apply to mechanical edits that merely sit near such code, or that
            add a new interface nothing relies on yet; score those on the rubric (usually 2-3).
          - `complexity_justification`: REQUIRED. One or two sentences that argue, specifically,
            why this task sits at exactly that tier — cite the concrete files, interfaces, or
            risk that places it there (e.g. "touches the auth middleware contract used by 4
            call sites, so a mistake is non-local → tier 4"). A bare restatement of the rubric
            ("this is complex") is not acceptable; the justification must be defensible against
            a reviewer who disagrees.
        - `watch_items` must be an array of strings covering runtime risks, critique concerns, and assumptions to keep visible during execution.
        - `sense_checks` must be an array with one verification question per task. Every sense-check object must include:
          - `id`: short stable ID like `SC1`
          - `task_id`: the related task ID
          - `question`: reviewer verification question
          - `verdict`: always `""` at finalize time
        - `user_actions` should default to `[]`. Emitting a user_action stalls execution on a human gate — it is a load-bearing escape hatch, not a convenience. Prefer to design every step to be fully mechanical: the executor can read files, run commands, query APIs, fetch URLs, parse JSON, edit code, and run tests. If a check is mechanical, write it as a task — not a user_action.
- Emit a user_action ONLY when the work is *genuinely non-mechanical* and the executor has no path to do it itself: secrets the human alone holds (a real API key the executor cannot mint), infrastructure access bound to the human's identity (cloud console, VPN), legal/license/security-policy judgments that require a human signatory, or out-of-band manual UI smoke tests on production. If you cannot name a specific reason the executor cannot do it, the task is mechanical — make it a task.
- Anything that touches code, configuration, fixtures, or local files MUST be a task. Reading docs, grepping, editing, running tests, writing SQL, and producing JSON resolution files are tasks. Negative example: writing the migration SQL is a task, not a user_action; verifying a license URL is reachable is a task, not a user_action.
- When you do emit a user_action, set `requires_human_only_reason` to a specific sentence naming why the executor literally cannot perform it (not just "it's important").
- If `user_actions` is non-empty, expect that execution will block until the human resolves each one — only emit them when that block is unavoidable.
- Positive examples (rare, last-resort): `U1: Set ANTHROPIC_API_KEY in .env (before_execute, requires_human_only_reason: "secret the executor cannot mint")`; `U2: Confirm legal sign-off to commit the third-party JSON corpus (before_execute, requires_human_only_reason: "license judgment with legal liability")`.
        - `meta_commentary` must be a single string with execution guidance, gotchas, or judgment calls that help the executor succeed.
        - Preserve information that strong existing artifacts already capture well: execution ordering, watch-outs, reviewer checkpoints, and practical context.
        - The structured output should be self-contained: an executor reading only `finalize.json` should have everything needed to work.
        - Keep the task count proportional to the work. A simple 1-2 file fix should be 2 tasks: (1) apply the fix, (2) run tests. Do NOT create separate "inspect" or "read" tasks for simple changes — the executor can read and fix in one step. Only create more tasks when the work has genuinely independent stages.
        - One task is the unit of ONE worker turn: the executor must IMPLEMENT it, VERIFY it, and emit its result envelope inside a single ~15-minute conversation. A task that cannot realistically finish in one turn is mis-sized and WILL fail — the worker reports `blocked: "turn ended before implementation"` or runs out the clock mid-edit. Size every task to fit one turn; raising `complexity` does NOT buy more turns, it only routes to a stronger model for the SAME single turn. When a step is a large mechanical refactor, SPLIT it across tasks instead of bundling: "add the new abstraction + its unit tests" (T1) → "migrate consumer A" (T2, depends_on T1) → "migrate consumer B + parity tests" (T3, depends_on T2). A single task that says "extract this 2000-line file into modules and write tests" or "consolidate the roots AND migrate three registries AND add parity tests" is a god-task — decompose it into one task per consumer/module, each independently completable and verifiable.
        - - The harness owns test verification — do NOT author a run-until-pass task. If you include a test-related task, scope it to: introduce no new failures vs the recorded baseline; do not try to make pre-existing baseline failures pass; do not narrow to individual functions. The harness will run the authoritative post-execute suite.
- For code-mode plans, preserve a scoped test contract. Prefer the plan's machine-readable `test_blast_radius`; when it is missing, ensure at least one finalized task carries a concrete scoped pytest command in `commands_run` (for example `pytest tests/test_relevant.py -q`) or mappable `files_changed` paths. Do not rely on an unscoped `pytest` command unless the plan explicitly opts into `test_selection=full`.