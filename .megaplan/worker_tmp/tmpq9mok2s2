Execute the approved plan in the repository.

        Project directory:
        /workspace/extension-reality-convergence-epic/reigh-app

        Brief summary: # M3: Export Readiness Convergence
(Full brief in state.idea; success criteria in plan_v1.meta.json.)

        Batch framing:
        - Execute batch 1 of 8.
        - Actionable task IDs for this batch: ['T1']
        - Already completed task IDs available as dependency context: []

        Actionable tasks for this batch:
        [
  {
    "id": "T1",
    "depends_on": [],
    "status": "pending",
    "kind": "audit",
    "complexity": 1,
    "reviewer_verdict": "",
    "description": "Confirm current M3 touchpoints before edits: inspect the cited hook, client render, planner, router/export guard/readiness tests, and docs; verify `getFastRenderRouteDecision()` is unused and export guard scan output feeds planner readiness rather than bypassing it.",
    "complexity_justification": "This is a read-only confirmation across explicitly named files and symbol searches. The task does not require design judgment beyond reporting whether the approved plan's assumptions still match the checkout.",
    "executor_notes": ""
  }
]

        Completed task context (already satisfied, do not re-execute unless directly required by current edits):
        []

        Prior batch deviations (address if applicable):
        None

        User action prerequisites:
        No user_action prerequisites for this batch.


        Batch-scoped sense checks:
        [
  {
    "id": "SC1",
    "task_id": "T1",
    "verdict": "",
    "question": "Did the audit inspect only the approved touchpoint files and confirm the dead route-decision symbol plus export-guard planner-input assumptions before edits?"
  }
]


        ## Execution context (settled - DO NOT re-litigate)

        - SD1: Export guard scans (currently runExportGuard) are demoted to planner-input adapters, not removed. They remain useful as structured diagnostics that feed planner readiness rather... - Aligned with the Epic North Star constraint: 'Keep generated-module, provider, and export-guard scans where useful, but make them feed planner readiness instead of bypassing it.' The plan renames the function to refle...
- SD2: Scoped test selectors use seven existing Vitest test files as the deterministic baseline for finalize, with full_suite_fallback enabled for safety. - The prior iteration failed finalize because test_blast_radius had no machine-readable selectors. These seven paths cover all surfaces named by the plan (hooks, planner, router, export guard, readiness docs) and are co...
- SD3: WebCodecs unsupported failure in useClientRender.ts gets structured RenderBlocker-shaped provenance without introducing new readiness APIs or planner integration beyond what exi... - The plan keeps the change local to client execution unless existing types make planner integration natural. This avoids scope creep into new readiness APIs while meeting the 'must' criterion for structured blocker pro...

        ## Plan execution order rationale

        1. Confirm insertion points before editing.
2. Apply hook/source changes before tests that assert new behavior.
3. Update docs/comments after source behavior is settled.
4. Re-audit message paths after all edits.
5. Keep the scoped test selector list synchronized with any newly added or removed test file.

        ## Inter-task guidance from finalize

        Execute in the listed order. Keep the hook changes narrowly scoped to provenance and logging, then add focused tests that would have failed on the old behavior. Documentation edits should reference canonical planner/readiness sources rather than restating blocker enums. The final validation task is the authoritative test step; do not turn it into a run-until-pass loop or weaken gates to pass.

        Note: User chose auto-approve mode. This execution was not manually reviewed at the gate. Exercise extra caution on destructive operations.
        Robustness level: thorough.

        Requirements:
        - Execute only the actionable tasks in this batch.
        - Treat completed tasks as dependency context, not new work.
        - Return structured JSON only.
        - Fill in the following template and return it as your JSON response. Only update the entries for this batch's tasks/sense checks.
        - Template file already written for reference: /workspace/extension-reality-convergence-epic/reigh-app/.megaplan/plans/m3-export-readiness-20260710-0146/execute_batch_1_output.json
        - Only produce `task_updates` for these tasks: [T1]
        - `task_updates[].status` must be one of `done`, `skipped`, `completed`, or `blocked`; never `pending` or `in_progress`.
        - Only produce `sense_check_acknowledgments` for these sense checks: [SC1]
        - Do not include updates for tasks or sense checks outside this batch.
        - Some prior file lists may be capped prompt projections with `items`, `omitted_count`, and `full_set_artifact_ref`; use the artifact reference when you need the full set.
        - Keep `executor_notes` verification-focused.
        - Best-effort progress checkpointing: if `/workspace/extension-reality-convergence-epic/reigh-app/.megaplan/plans/m3-export-readiness-20260710-0146/execution_batch_1.json` is writable, checkpoint task and sense-check updates there (not `finalize.json`). The harness owns `finalize.json`.
        - All terminal verification commands must run from the Project directory (`/workspace/extension-reality-convergence-epic/reigh-app`) or a descendant. Do not `cd` to `/workspace/arnold`, an editable-engine mirror, or any shared engine checkout for tests; those paths are outside the session worktree and the sandbox will refuse them.
        - When verifying changes, run the entire test file or module, not individual test functions. Individual tests miss regressions.
        - Run tests ONCE, in the FOREGROUND, and wait for them to finish (you have a large time budget). Do NOT background a long test run and poll it in a loop. Slowness is NOT a stall — never relaunch a test command because it "seems stuck"; duplicate concurrent runs contend for CPU and make everything slower. Never run more than one heavy test invocation at a time. Prefer scoping to the changed files; run the full suite only when the task explicitly requires it, and then exactly once.
        - finalize.json includes baseline_test_failures — a list of test IDs that were already failing before your changes. If a test fails and its ID appears in baseline_test_failures, it is pre-existing — do not scope-creep into fixing it. If baseline_test_failures is null, the baseline could not be captured; use your judgment but err on the side of assuming failures are regressions. A mechanical post-execute suite run by the harness — not you — is the authoritative regression check. Run tests for your own fix loop if needed, then stop; do not loop the suite to make pre-existing failures pass.
        - If this batch includes the final verification task, write a short script that reproduces the exact bug described in the task, run it to confirm the fix resolves it, then delete the script.

        Batch JSON response template:
        ```json
        {
  "output": "",
  "files_changed": [],
  "commands_run": [],
  "deviations": [],
  "task_updates": [
    {
      "task_id": "T1",
      "status": "pending",
      "executor_notes": "",
      "files_changed": [],
      "commands_run": [],
      "auto_attributed_files": false
    }
  ],
  "sense_check_acknowledgments": [
    {
      "sense_check_id": "SC1",
      "executor_note": ""
    }
  ]
}
        ```