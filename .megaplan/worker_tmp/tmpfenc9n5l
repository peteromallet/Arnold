Execute the approved plan in the repository.

        Project directory:
        /workspace/extension-reality-convergence-epic/reigh-app

        Brief summary: # M4: Evidence And Drift Gates
(Full brief in state.idea; success criteria in plan_v1.meta.json.)

        Batch framing:
        - Execute batch 3 of 8.
        - Actionable task IDs for this batch: ['T3']
        - Already completed task IDs available as dependency context: ['T1', 'T2']

        Actionable tasks for this batch:
        [
  {
    "id": "T3",
    "depends_on": [
      "T2"
    ],
    "status": "pending",
    "kind": "docs",
    "complexity": 2,
    "reviewer_verdict": "",
    "description": "Step 2 audit artifacts: create `.megaplan/plans/m4-evidence-and-drift-gates-20260708-0120/audits/a2-trust-gate-gaps.md` and `a5-docs-matrix-reconciliation.md`. A2 must list trust-sensitive M2/M3 surfaces and whether `check-trust-model-truth.mjs` scans them; A5 must reconcile docs rows that contradict code/tests, starting with D-020, D-021, D-022, and B-002. Keep both notes factual and scoped to trust, composition authority, export readiness, and docs matrices.",
    "complexity_justification": "This writes two scoped audit notes from local grep/code inspection and does not alter runtime or gate behavior. The work requires judgment about evidence rows, but the surfaces and starting rows are named in the approved plan.",
    "executor_notes": "",
    "commands_run": []
  }
]

        Completed task context (already satisfied, do not re-execute unless directly required by current edits):
        [
  {
    "id": "T1",
    "depends_on": [],
    "status": "skipped",
    "kind": "test",
    "complexity": 1,
    "reviewer_verdict": "deferred_baseline_unavailable",
    "description": "Introduce no new failures vs the recorded baseline; do not try to make pre-existing baseline failures pass; do not narrow to individual functions. The harness will run the authoritative post-execute verification \u2014 do not loop the suite.",
    "complexity_justification": "This is a mechanical baseline check of existing readiness commands and one narrow parser-failure triage point. It does not require changing source behavior or reasoning across files.",
    "executor_notes": "Deferred by harness: baseline_test_failures is null, so this no-new-failures checkpoint cannot compare against a recorded baseline. The harness-owned final verification/review phase remains authoritative.",
    "files_changed": [],
    "commands_run": [],
    "evidence_files": []
  },
  {
    "id": "T2",
    "depends_on": [
      "T1"
    ],
    "status": "skipped",
    "kind": "code",
    "complexity": 2,
    "reviewer_verdict": "",
    "description": "Step 1 conditional repair: if T1 proves the readiness failure is a parser bug, patch only `extractTestReadinessFiles()` in `scripts/quality/check-readiness.mjs` so it splits the `test:readiness` script, skips `--config` and its value, ignores flags, and returns `.test`/`.spec` paths. If T1 passes or fails for another reason, mark this task skipped with the T1 evidence and do not weaken strict evidence validation.",
    "complexity_justification": "The only allowed edit is a localized tokenizer fix in `scripts/quality/check-readiness.mjs` with direct readiness commands as verification. The risk is local because the task explicitly forbids relaxing strict rows or changing unrelated evidence requirements.",
    "executor_notes": "Skipped per conditional task rule. Completed dependency T1 is recorded as skipped/deferred_baseline_unavailable with baseline_test_failures null, no commands run, and no evidence that readiness failed because of extractTestReadinessFiles(). Inspected scripts/quality/check-readiness.mjs: extractTestReadinessFiles() already splits the script, skips --config and its value, ignores flags, and returns .test/.spec paths; node --check passed. No strict readiness evidence validation was weakened.",
    "files_changed": [],
    "commands_run": [
      "node --check scripts/quality/check-readiness.mjs"
    ]
  }
]

        Prior batch deviations (address if applicable):
        [
  "Advisory audit finding: Sense check SC3 is missing an executor acknowledgment.",
  "Advisory audit finding: Sense check SC4 is missing an executor acknowledgment.",
  "Advisory audit finding: Sense check SC5 is missing an executor acknowledgment.",
  "Advisory audit finding: Sense check SC6 is missing an executor acknowledgment.",
  "Advisory audit finding: Sense check SC7 is missing an executor acknowledgment.",
  "Advisory audit finding: Sense check SC8 is missing an executor acknowledgment.",
  "Advisory audit finding: Sense check SC9 is missing an executor acknowledgment.",
  "Advisory audit finding: Tasks left pending after execute (executor never started them): T3, T4, T5, T6, T7, T8, T9"
]

        User action prerequisites:
        No user_action prerequisites for this batch.


        Batch-scoped sense checks:
        [
  {
    "id": "SC3",
    "task_id": "T3",
    "verdict": "",
    "question": "Do the A2 and A5 audit notes exist at the required paths and stay factual, concise, and scoped to trust, composition authority, export readiness, and docs matrix reconciliation?"
  }
]


        ## Execution context (settled - DO NOT re-litigate)

        - SD1: The fast-path render bypass remains as an optimization but all user-facing blocked states must originate from planner RenderBlockers - This resolves the anchor tension between the brief's suggestion to document the bypass as a limitation and the North Star requirement that export blocks originate from planner blockers. The plan correctly treats this...
- SD2: D-020 moves to supported only after the fast-path planner-provenance fix lands; D-021/D-022 depend on A5 evidence and may remain deferred if proof is incomplete - This is the correct ordering constraint. The plan explicitly ties docs reclassification to code changes landing first (Step 7 follows Step 6), preventing the anti-pattern of claiming support before implementation exis...
- SD3: The proof note belongs in .megaplan/plans/m4-evidence-and-drift-gates-20260708-0120/proof-note.md as a milestone artifact, not in the epic initiative directory - The plan explicitly places the proof note in the milestone plan directory. This keeps M4 evidence self-contained and avoids polluting the epic-level initiative directory with per-milestone artifacts. The epic North St...

        ## Plan execution order rationale

        1. Verify readiness and complete A2/A5 audit notes before editing gates.
2. Land trust and composition drift gates before relying on the final proof note.
3. Fix export blocker provenance before reclassifying D-020/D-021/D-022 as supported.
4. Write the proof note last so it cites final files and passing commands, not stale intermediate evidence.

        ## Inter-task guidance from finalize

        Execute the tasks in DAG order and keep batches narrow: T5 and T6 intentionally share composition context after the trust gate, while the render provenance fix is isolated behind them. Treat conditional work in T2 as a real conditional, not a license to touch readiness code unnecessarily. The final proof note must be evidence-driven and must not hide failures behind supported language; if final validation exposes a pre-existing baseline failure, record it explicitly and leave gates intact.

        Note: User chose auto-approve mode. This execution was not manually reviewed at the gate. Exercise extra caution on destructive operations.
        Robustness level: thorough.

        Requirements:
        - Execute only the actionable tasks in this batch.
        - Treat completed tasks as dependency context, not new work.
        - Return structured JSON only.
        - Fill in the following template and return it as your JSON response. Only update the entries for this batch's tasks/sense checks.
        - Template file already written for reference: /workspace/extension-reality-convergence-epic/reigh-app/.megaplan/plans/m4-evidence-and-drift-gates-20260708-0120/execute_batch_3_output.json
        - Only produce `task_updates` for these tasks: [T3]
        - `task_updates[].status` must be one of `done`, `skipped`, `completed`, or `blocked`; never `pending` or `in_progress`.
        - Only produce `sense_check_acknowledgments` for these sense checks: [SC3]
        - Do not include updates for tasks or sense checks outside this batch.
        - Some prior file lists may be capped prompt projections with `items`, `omitted_count`, and `full_set_artifact_ref`; use the artifact reference when you need the full set.
        - Keep `executor_notes` verification-focused.
        - Best-effort progress checkpointing: if `/workspace/extension-reality-convergence-epic/reigh-app/.megaplan/plans/m4-evidence-and-drift-gates-20260708-0120/execution_batch_3.json` is writable, checkpoint task and sense-check updates there (not `finalize.json`). The harness owns `finalize.json`.
        - All terminal verification commands must run from the Project directory (`/workspace/extension-reality-convergence-epic/reigh-app`) or a descendant. Do not `cd` to `/workspace/arnold`, an editable-engine mirror, or any shared engine checkout for tests; those paths are outside the session worktree and the sandbox will refuse them.
        - When verifying changes, run the entire test file or module, not individual test functions. Individual tests miss regressions.
        - Run tests ONCE, in the FOREGROUND, and wait for them to finish (you have a large time budget). Do NOT background a long test run and poll it in a loop. Slowness is NOT a stall — never relaunch a test command because it "seems stuck"; duplicate concurrent runs contend for CPU and make everything slower. Never run more than one heavy test invocation at a time. Prefer scoping to the changed files; run the full suite only when the task explicitly requires it, and then exactly once.
        - finalize.json includes baseline_test_failures — a list of test IDs that were already failing before your changes. If a test fails and its ID appears in baseline_test_failures, it is pre-existing — do not scope-creep into fixing it. If baseline_test_failures is null, the baseline could not be captured; use your judgment but err on the side of assuming failures are regressions. A mechanical post-execute suite run by the harness — not you — is the authoritative regression check. Run tests for your own fix loop if needed, then stop; do not loop the suite to make pre-existing failures pass.
        - If this batch includes the final verification task, write a short script that reproduces the exact bug described in the task, run it to confirm the fix resolves it, then delete the script.

        Batch JSON response template:
        ```json
        {
  "output": "",
  "files_changed": [],
  "commands_run": [],
  "deviations": [],
  "task_updates": [
    {
      "task_id": "T3",
      "status": "pending",
      "executor_notes": "",
      "files_changed": [],
      "commands_run": [],
      "auto_attributed_files": false
    }
  ],
  "sense_check_acknowledgments": [
    {
      "sense_check_id": "SC3",
      "executor_note": ""
    }
  ]
}
        ```