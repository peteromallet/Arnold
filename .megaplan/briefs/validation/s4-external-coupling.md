Here's the compact validation:

| # | Verdict | Current file:line | Note + quote |
|---|---------|-------------------|--------------|
| 1 | **CONFIRMED** | `megaplan/cloud/supervise.py:54,80,248,257` | `_remote_sync_refresh_command` constructs `python3 -c "from megaplan.chain import _capture_sync_state, ChainState, save_chain_state, load_chain_state; ..."` (L54), wraps in `cd ... && python3 -c ...` (L80), called in supervisor tick loop at L248 and executed via `ssh_meth(sync_cmd)` at L257. Mutates `ChainState` remotely via `_capture_sync_state` + `save_chain_state`. |
| 2 | **PARTIAL** | `megaplan/chain/__init__.py:970` (glob); `:662,941,1020` (state.json r/w); `megaplan/bakeoff/live_status.py:11`, `lifecycle.py:12`, `handlers.py:14` (bakeoff import) | `chain/__init__.py` (was `chain.py`) globs `execution_batch_*.json` at L970 and reads/writes `state.json` via `write_plan_state` delegating to `_core.state`. But `finalize.json` is **not** read by chain — it's consumed in `store/plan_repository.py:249,332`. Bakeoff modules do `from megaplan._core.state import load_plan` in 3 files. Quote (chain): `plan_dir.glob("execution_batch_*.json")`. |
| 3 | **DRIFTED** | 26 `MEGAPLAN_*` vars across ~15 files | `MEGAPLAN_MOCK_WORKERS` confirmed (`workers/_impl.py:2390`). Original audit claimed ~14; now **26** distinct vars: `ACTOR_ID`, `AUTO_HEARTBEAT_SECONDS`, `BACKEND`, `CHAIN_NO_PUSH`, `CODEX_PRE_FIRST_BYTE_TIMEOUT_S`, `DB_BLOB_ROOT`, `FINALIZE_STRICT_VALIDATION`, `LOG_LEVEL`, `MOCK_WORKERS`, `NARROW_SANDBOX`, `TRUSTED_CONTAINER`, `PIPELINE_AUTO`, `REGISTRY_HOME`, `SHANNON_READINESS_PROBE/TIMEOUT/EXECUTE_TIMEOUT/HANDSHAKE_PROBABILITY/AUTO_PATCH/NONROOT_USER/NONROOT_HOME`, `TURN_ID`, `API_KEYS_PATH`, `AUDIT_DIR`, `REPO_AUDIT_MIRROR`, `RESIDENT_MODE`, `RESIDENT_ALLOWED_GUILDS`. Read directly via `os.getenv`/`os.environ.get`, no config bus mediation. |
| 4 | **CONFIRMED** | `megaplan/__init__.py:48-72` (17 handler symbols); `megaplan/handlers/__init__.py:75-90` (14 handler symbols) | Both `__all__` lists export `handle_*` functions. Top-level has 17 (`handle_init`, `handle_plan`, `handle_critique`, `handle_revise`, `handle_gate`, `handle_finalize`, `handle_execute`, `handle_review`, `handle_step`, `handle_status`, `handle_audit`, `handle_progress`, `handle_list`, `handle_override`, `handle_setup`, `handle_setup_global`, `handle_config`). Handlers sub-package has 14 (adds `handle_prep`, `handle_audit_verifiability`, `handle_verify_human`, `handle_tiebreaker_run`, `handle_tiebreaker_decide`; omits CLI-facing ones). Overlap on core phase handlers but sets are non-identical. |

---

**Biggest risk to "planning just another pack on one execution path":** The SSH-remote `python3 -c "from megaplan.chain import ..."` pattern (claim 1) is the most brittle — it bakes the exact import path and internal function names (`_capture_sync_state`, `save_chain_state`, `load_chain_state`) into a shell string sent over the wire, making any refactor of `megaplan.chain` a **remote-breaking change** with no static check. The 26 ambient env reads (claim 3) are a close second: every `os.getenv("MEGAPLAN_*")` bypasses any unified dispatch or config-bus, so "one execution path" would still carry 26 invisible, scattered toggles that can silently change behavior per deployment.