The report is written to `/Users/peteromalley/Documents/megaplan/.megaplan/briefs/hardening-epic/analysis/m6a-surface-config-facts.md` (1,156 words, 177 lines).

**Key findings:**

- **Only 3 of 15 manifest files** actually belong to M6a. The other 12 are either unrelated vibecomfy emitter sessions (files 3, 5) or May 28 sessions with zero M6a matches (files 7–15). The manifest over-captured by 4×.

- **This was planning-only** — 3 turns: plan (gpt-5.5), critique evaluator assignment (gpt-5.5 assigned 9 lenses: 6→deepseek-v4-pro, 3→deepseek-v4-flash), and task decomposition (gpt-5.5 produced 18-task DAG). Zero execute→review→rework cycles completed. Critics were assigned but never returned results within these sessions.

- **Zero errors, zero retries, zero stalls.** All 18 function calls (Turn 1 only) completed with exit code 0. Turns 2 and 4 were pure LLM turns with no tool calls.

- **100% premium model** (gpt-5.5) for all 3 main-agent turns. Cheap models (deepseek-v4-pro/flash) were assigned as critics but didn't execute within the captured window.

- **174,492 total tokens** across 3 turns. ~90K of that was repeated base-instructions overhead (3 × ~30K system prompt). Wall clock: 18min 42s with a 14.5min idle gap (unrelated session ran between turns 2 and 4).