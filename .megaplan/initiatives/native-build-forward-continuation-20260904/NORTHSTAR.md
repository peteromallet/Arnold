# Native Build Forward — fresh C2 continuation North Star

This continuation starts at Native C2 from a reviewed, content-addressed
source while preserving the original chain as immutable paused history. Its
destination is the same Native Parity then Platformization outcome described
by the canonical NBF North Star, but it has a new chain/session/runtime and no
shared owner with the retired chain.

The six historical predecessors are admitted only by the handoff manifest;
they are not represented as completed rows in this new chain. C2 is the first
executable milestone. Source, package, routing, CAS, receipt, replay, hold,
and liveness identities remain authoritative and observable. Durable success
requires one owner/provider/fixer dispatch, supervised milestone progress,
and post-shell restart/replay/recovery evidence—not merely a launch receipt.

Canonical architecture and authority remain in
`.megaplan/initiatives/native-build-forward/NORTHSTAR.md`, the representation
report, and accepted NBF briefs. This document is a continuation anchor, not a
rewrite of the old chain's history.
