# Resume C1 and eliminate repair-unavailable recurrence

Operate on the existing canonical Workflow Boundary Contracts cloud/session only. Do not create or launch a duplicate epic or chain.

Objectives:

1. Inspect the canonical C1 plan and its latest gate/finalize evidence using Megaplan introspection and constrained/local canonical state.
2. Revise the C1 machine-readable test contract so `test_blast_radius` references real current tests (or explicitly selects the full suite when justified), preserving approved scope and North Star.
3. Resume the same `workflow-boundary-contracts-corrective-20260710` chain/session through the canonical control path. Verify custody and that it is genuinely advancing. Do not approve a human gate or force-proceed.
4. Implement the clean-checkout root corrective described in `notes/2026-07-11-c1-repair-unavailable-root-cause.md`: post-action evidence refresh/custody revalidation, truthful liveness, direct-relaunch fallback execution, and non-collapsing reason reporting. Include focused regression tests for the invalid-field retry loop, stale pre-gate evidence, dead finalize worker, swallowed fallback, stale activity, and unrelated-process false liveness.
5. Preserve unrelated dirty work. If overlapping edits prevent a safe implementation, produce a precise patch/handoff in this initiative and still complete the safe C1 revision/resume if possible.
6. Report whether the same epic/session resumed, its observed state after intervention, tests run, commits/PRs if any, and any remaining human gate.

This is autonomous, high-risk cross-cutting repair work. Use canonical Megaplan mechanisms; no arbitrary remote shell and no duplicate execution.
