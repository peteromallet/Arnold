# Extension Reality Clean-Lane Recovery North Star

Get `extension-reality-convergence-epic` moving again from the current real
state without replaying the contaminated T5 loop.

The work is complete only when a cloud megaplan is running on top of the
current merged guard work (#191/#192), uses a clean verification lane instead of
the contaminated execution workspace, and continues the remaining recovery:
forensic backup, M1-M4 product proof, custody reconciliation, status semantics
verification, and direct restart/resume or supervised replacement of the
original epic.

Do not relaunch the corrupted original epic as-is. Do not redo the already
landed PR #191/#192 work. Do not use dirty `/workspace/arnold` or nested runtime
mirrors/worktrees as the verification root.
