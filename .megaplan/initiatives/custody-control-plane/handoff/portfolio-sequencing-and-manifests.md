---
type: handoff
date: 2026-07-11
---

# Post-WBC sequencing and completion evidence

## Canonical sequence

1. Finish and land all milestones in the canonical Workflow Boundary Contracts
   chain.
2. Generate its content-addressed `completion-manifest.json` from a deliberate
   proof map; validate that it matches the current WBC chain, North Star, briefs,
   state, merged publication evidence, and proof artifacts.
3. Record the remaining custody launch approvals in
   `decisions/single-authoritative-runtime-history.md`.
4. Only then may the one pending custody milestone be launched through the
   normal reviewed Megaplan lifecycle.

The current durable prerequisite session is
`workflow-boundary-contracts-corrective-20260710`; initiative notes identify its
C1 plan as `c1-contract-reality-20260711-1433`. These identifiers are lifecycle
correlation, not completion proof. Attach/resume semantics belong to operations;
this planning reshape does not start, resume, approve, or alter either run.

## Intentional launch blocker

No WBC completion manifest exists in this checkout. The custody chain therefore
requires `kind: chain_completed` with `require_manifest: true` and is expected to
reject launch until WBC produces current proof. A terminal label, live process,
green test, merged subset, or hand-authored manifest is insufficient.

## Custody completion evidence

The single milestone handoff must record the accepted WBC and Run Authority
contract hashes, the bounded surface map, fixture/fault/shadow/rollback results,
and proof that production enforcement and mutating repair remain disabled. Any
later custody completion manifest must be generated by the resident chain
lifecycle from the unchanged chain, North Star, milestone brief, chain state,
merged publication evidence, and deliberate proof artifacts; it must not be
hand-authored during planning.
