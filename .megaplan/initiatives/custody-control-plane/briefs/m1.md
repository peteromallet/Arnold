---
type: brief
slug: m1
title: Resolver foundation + read-coherence + incident fixtures
epic: custody-control-plane
created_at: '2026-07-07T19:24:43.691784+00:00'
---

# Resolver foundation + read-coherence + incident fixtures
**Profile: partnered-5/full/high (+with_prep)**

## Outcome
A deployed, tested `resolve_run_state() -> CanonicalRunState` resolver module that reads the 6 custody sources with READ-COHERENT snapshots and classifies every known incident shape correctly.

## Scope
- Establish/restore `arnold_pipelines/megaplan/run_state/` (resolver.py, model.py, evidence.py, classifiers.py). NOTE: the module is NOT currently deployed (uncommitted dirty work, lost on re-clone) — pin the baseline from #162 or rebuild; do not wire against a phantom module.
- `resolve_run_state()` reading 6 sources in ground-truth order; the `CanonicalRunState` enum (RUNNING, REPAIRING, RETRYABLE_EXECUTION_BLOCK, REAL_IMPLEMENTATION_BLOCK, HUMAN_ACTION_REQUIRED, COMPLETED, STALE_DERIVED_STATE, BROKEN_STATE_MACHINE, UNKNOWN) + payload (source_of_truth, stale_sources, confidence, next_action, evidence).
- **GAP #1 (read-coherence):** atomic/version-token snapshot — NOT 6 independent reads. Handle the two competing state.json write paths (`_core/state.py:840` locked vs `auto.py:4093` unlocked atomic-rename) + `events.ndjson` append-without-read-lock (`events.py:592`).
- **GAP #10:** `BROKEN_STATE_MACHINE` for missing workspace (filesystem pre-condition; detect, don't restore).
- Fixture tests for ALL known July-2026 incident shapes: manual_review+finalized+live-worker, execution_blocked budget-exhausted, AWF018 route-binding, failed/no_next_step+deferred-baseline, merged-PR+stale-failed, stale needs-human, live-initialized+active_step, missing-workspace.
- Anti-scope: NO wiring into status/dispatch/watchdog (m2/m3); NO profile routing (m3).

## Constraints
- Resolver is pure + fixture-testable (no side effects; deployment persists output).
- Ground-truth order is fixed. `confidence` is meaningful (consumers gate on low).

## Done Criteria
- Correct `CanonicalRunState` for every fixture.
- Read-coherence verified (concurrency test: no torn reads under concurrent writes).
- Module committed + deployed to `editible-install` + all sibling mirrors (deploy-tree self-consistency).
