---
type: brief
slug: m3
title: Watchdog + chain guards + L2 fail-closed + profile-pin-vs-tier residual
epic: custody-control-plane
created_at: '2026-07-07T19:24:43.692281+00:00'
---

# Watchdog + chain guards + L2 fail-closed + profile-pin-vs-tier residual
**Profile: partnered-5/full/high (+with_prep)**

## Outcome
Watchdog + chain guards consume the resolver; L2 `verify_retrigger_success` is mandatory (fail-closed); the profile-pin-vs-tier residual is closed.

## Scope
- Wire resolver into `arnold-watchdog` — suppress stale needs-human only when canonical state is machine-actionable/running; pass `lock_evidence` to `classify_repair_dispatch` (closes the TOCTOU double-dispatch window).
- Wire into chain guards — canonicalize before refusing `failed/no_next_step` or stale merged-PR (`chain/__init__.py`).
- Make L2 `verify_retrigger_success()` (`meta_repair.py:1346-1422`) MANDATORY — the ONLY writer of terminal success; fail-closed on non-zero retrigger returncode. Wire into `arnold-meta-repair-loop` post-retrigger.
- **GAP #2 (profile-pin-vs-tier residual):** close the case where a profile co-defines `phase_model execute=...` AND `tier_models.execute` — neither `2f15007a` guard fires (profile's own pin enters `profile_steps`, tier survives + overrides → the glm-deadlock shape). Fix `profiles/policy.py`; add a test reproducing the native-parity deadlock.
- Anti-scope: NO L3 reconciler / external coverage (m4).

## Constraints
- Watchdog/guards defer to canonical for needs-human suppression + refusal decisions.
- L2 success is fail-closed (verifier-gated).

## Done Criteria
- Watchdog/guards consume the resolver (test: stale needs-human cleared only when canonical says retryable/running).
- L2 cannot record `FIXED` with non-zero retrigger returncode (test).
- Profile co-defining pin+tier no longer routes execute wrong (test reproducing the deadlock shape).
