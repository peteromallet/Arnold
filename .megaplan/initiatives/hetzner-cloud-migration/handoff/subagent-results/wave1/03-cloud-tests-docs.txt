Now I have a thorough understanding of the codebase. Here's the focused analysis.

---

## 1. Tests to Create: `tests/test_cloud_spec.py`

This file is referenced in `README.md` line 98 but **does not exist yet**. It needs to validate the spec layer after the provider default shift.

### Tests to add:

```python
# ── Provider default: SSH is now the native path ──

def test_default_provider_is_ssh():
    """When cloud.yaml has no `provider` field, the default is 'ssh' (was 'railway')."""
    spec = load_spec(_yaml_path({"repo": {"url": "..."}}))
    assert spec.provider == "ssh"

def test_railway_not_in_valid_providers():
    """Railway is removed from VALID_PROVIDERS."""
    assert "railway" not in VALID_PROVIDERS

def test_ssh_is_in_valid_providers():
    assert "ssh" in VALID_PROVIDERS

def test_railway_spec_field_raises_or_warns():
    """A cloud.yaml with `provider: railway` should raise a clear migration error."""
    with pytest.raises(CliError, match="railway.*removed|railway.*migrate"):
        load_spec(_yaml_path({"provider": "railway", "repo": {"url": "..."}}))

# ── SSH spec defaults ──

def test_ssh_spec_host_is_required():
    """When provider=ssh, ssh.host is required."""
    with pytest.raises(CliError, match="ssh.host"):
        load_spec(_yaml_path({"provider": "ssh", "repo": {"url": "..."}}))

def test_ssh_spec_defaults():
    spec = load_spec(_yaml_path({
        "provider": "ssh",
        "repo": {"url": "https://github.com/example/app.git"},
        "ssh": {"host": "hetzner.example.com"},
    }))
    assert spec.ssh.host == "hetzner.example.com"
    assert spec.ssh.port == 22
    assert spec.ssh.remote_dir == "/tmp/megaplan-cloud"
    assert spec.ssh.container == "megaplan-cloud-agent"

def test_ssh_spec_hetzner_config():
    """Full Hetzner-like config with identity file and user."""
    spec = load_spec(_yaml_path({
        "provider": "ssh",
        "repo": {"url": "https://github.com/example/app.git"},
        "ssh": {
            "host": "myhost.hetzner.cloud",
            "user": "deployer",
            "port": 2222,
            "identity_file": "~/.ssh/id_ed25519",
            "remote_dir": "/srv/megaplan-cloud",
            "container": "megaplan-agent",
        },
    }))
    assert spec.ssh.user == "deployer"
    assert spec.ssh.port == 2222
    assert spec.ssh.identity_file == "~/.ssh/id_ed25519"
    assert spec.ssh.remote_dir == "/srv/megaplan-cloud"

# ── Persistent volume is wired through ──

def test_resources_volume_persisted():
    """resources.volume is preserved — used by SSH provider for named Docker volume."""
    spec = load_spec(_yaml_path({
        "repo": {"url": "..."},
        "ssh": {"host": "example.com"},
        "resources": {"volume": "megaplan-workspace"},
    }))
    assert spec.resources.volume == "megaplan-workspace"

# ── Backward compat: local provider still works ──

def test_local_provider_still_valid():
    spec = load_spec(_yaml_path({
        "provider": "local",
        "repo": {"url": "..."},
    }))
    assert spec.provider == "local"
    assert spec.local is not None

# ── Deprecation / forward compat: fly still reserved ──

def test_fly_still_reserved():
    with pytest.raises(CliError, match="future"):
        load_spec(_yaml_path({"provider": "fly", "repo": {"url": "..."}}))
```

### Expected assertions summary:

| Test | Key assertion |
|---|---|
| `test_default_provider_is_ssh` | `spec.provider == "ssh"` |
| `test_railway_not_in_valid_providers` | `"railway" not in VALID_PROVIDERS` |
| `test_railway_spec_field_raises_or_warns` | `pytest.raises(CliError, match="railway")` |
| `test_ssh_spec_host_is_required` | `pytest.raises(CliError, match="ssh.host")` |
| `test_ssh_spec_defaults` | `spec.ssh.host == "hetzner.example.com"`, port=22 |
| `test_ssh_spec_hetzner_config` | user, port, identity_file all propagate |
| `test_resources_volume_persisted` | `spec.resources.volume == "megaplan-workspace"` |
| `test_local_provider_still_valid` | `spec.provider == "local"` |
| `test_fly_still_reserved` | raises on `provider: fly` |

---

## 2. Tests to Create: `tests/test_cloud_template.py`

This file is also referenced in `README.md` line 98 but **does not exist**. It validates template rendering for the SSH-native path.

### Tests to add:

```python
# ── Entrypoint includes persistent volume setup ──

def test_entrypoint_includes_docker_volume_for_ssh():
    """Rendered entrypoint.sh for SSH provider references the named Docker volume."""
    spec = _ssh_spec(volume="megaplan-workspace")
    rendered = render_entrypoint(spec)
    # Should NOT reference Railway-specific commands
    assert "railway" not in rendered.lower()
    # Should contain workspace directory creation (standard entrypoint behavior)
    assert "mkdir -p /workspace" in rendered

def test_entrypoint_no_railway_references():
    """Entrypoint for SSH provider has zero Railway references."""
    spec = _ssh_spec()
    rendered = render_entrypoint(spec)
    assert "railway ssh" not in rendered
    assert "railway session" not in rendered

# ── Materialized deploy dir excludes Railway files ──

def test_materialize_deploy_dir_no_railway_toml_for_ssh():
    """When provider=ssh, railway.toml is NOT materialized."""
    spec = _ssh_spec()
    with TemporaryDirectory() as tmp:
        dest = Path(tmp)
        materialize_deploy_dir(spec, dest)
        assert not (dest / "railway.toml").exists()

def test_materialize_deploy_dir_has_dockerfile_and_entrypoint():
    """Core files always materialize."""
    spec = _ssh_spec()
    with TemporaryDirectory() as tmp:
        dest = Path(tmp)
        materialize_deploy_dir(spec, dest)
        assert (dest / "Dockerfile").exists()
        assert (dest / "entrypoint.sh").exists()
        assert (dest / "healthserver.py").exists()

# ── docker-compose only for local provider ──

def test_docker_compose_not_rendered_for_ssh():
    """docker-compose.yaml is only materialized for provider=local."""
    spec = _ssh_spec()
    with TemporaryDirectory() as tmp:
        dest = Path(tmp)
        materialize_deploy_dir(spec, dest)
        assert not (dest / "docker-compose.yaml").exists()

# ── Cloud init template defaults to SSH ──

def test_cloud_yaml_template_defaults_to_ssh():
    """The scaffolded cloud.yaml.tmpl has provider: ssh by default."""
    template_path = Path("arnold/pipelines/megaplan/cloud/templates/cloud.yaml.tmpl")
    content = template_path.read_text()
    assert "provider: ssh" in content
    assert "provider: railway" not in content
```

### Expected assertions summary:

| Test | Key assertion |
|---|---|
| `test_entrypoint_includes_docker_volume_for_ssh` | `"railway" not in rendered.lower()` |
| `test_entrypoint_no_railway_references` | No `railway ssh` or `railway session` in output |
| `test_materialize_deploy_dir_no_railway_toml_for_ssh` | `not (dest / "railway.toml").exists()` |
| `test_materialize_deploy_dir_has_dockerfile_and_entrypoint` | Core files present |
| `test_docker_compose_not_rendered_for_ssh` | No `docker-compose.yaml` for SSH |
| `test_cloud_yaml_template_defaults_to_ssh` | Template contains `provider: ssh`, not `railway` |

---

## 3. Tests to Change

### `tests/cloud/test_phase_command_shim.py` — **no changes needed**

This file tests the `_phase_command` substrate shim which is provider-agnostic. It imports `cloud_substrate` from `cli.py` but that constant (`"subprocess_isolated"`) is unchanged.

---

## 4. Docs Snippets That Must Change

### `docs/cloud.md` — high-priority changes:

| Line(s) | Current text | Must become |
|---|---|---|
| 9–12 | Provider table: `railway` row says "Good default for shared remote runs" | Remove `railway` row; make `ssh` row first with "Good default for shared remote runs. Use with any Docker host (Hetzner, AWS, etc.)." |
| 66 | `provider` default = `railway` | default = `ssh` |
| 15 | "`provider: fly` remains reserved" | Add: "`provider: railway` is removed as of [version]. Migrate to `provider: ssh` with a Hetzner or any Docker host." |
| 127–134 | Entire `### railway` subsection | Remove or replace with migration note |
| 195 | "That marker survives Railway's ephemeral deploy dir" | "That marker survives in the persistent deploy dir on the SSH host" |
| 335–339 | `### provider: railway` section | Remove entirely |
| 321–327 | `### provider: local` — keep as-is | No change |
| 329–333 | `### provider: ssh` — expand | Add: "Mounts a named Docker volume (`resources.volume`) at `<repo.workspace>` for persistent state across redeploys." |
| 343 | "Railway CLI install docs: https://docs.railway.app/develop/cli" | Remove this line |
| 122–125 | `resources.volume` description: "Provider-specific persistent volume name. `destroy` deletes it only when set." | Keep but note: "For SSH, this is a named Docker volume. For local, this is ignored." |

### `README.md` — one change:

| Line | Current | Replace with |
|---|---|---|
| 105 | "remote Railway box with a persistent workspace volume" | "remote Docker host (Hetzner or any SSH-reachable host) with a persistent workspace volume" |

### `cloud.yaml.tmpl` — comment/example changes:

| Line(s) | Change |
|---|---|
| 5 | `provider: railway` → `provider: ssh` |
| 2–5 | Comment block: remove "Supported providers: railway, local, ssh" → "Supported providers: ssh (default), local" |
| 123–134 | `railway:` block → comment it out entirely with a migration note, or remove. |
| 142–155 | `ssh:` block → uncomment, make it the primary example with Hetzner-like defaults |
| 119–121 | `resources.volume` comment: update to say "Named Docker volume for SSH provider persistence" |

---

## 5. Recommended Verification Command List

After making the implementation changes, run these in order:

```bash
# 1. Spec layer unit tests (new file)
python -m pytest tests/test_cloud_spec.py -v

# 2. Template rendering tests (new file)
python -m pytest tests/test_cloud_template.py -v

# 3. Full focused CI suite (same as README line 98)
python -m pytest tests/characterization/test_import_surface.py tests/test_pipeline_run_cli.py tests/test_cloud_template.py tests/test_cloud_spec.py -q

# 4. Existing cloud-adjacent tests (should still pass)
python -m pytest tests/cloud/test_phase_command_shim.py -v

# 5. Verify scaffolded cloud.yaml defaults to SSH
python -m arnold.pipelines.megaplan cloud init --force
grep "provider:" cloud.yaml
# Expected: provider: ssh

# 6. Verify spec loading rejects railway
python -c "
from pathlib import Path
import yaml, tempfile
with tempfile.NamedTemporaryFile(mode='w', suffix='.yaml') as f:
    yaml.dump({'provider': 'railway', 'repo': {'url': 'https://example.com/repo.git'}}, f)
    f.flush()
    from arnold.pipelines.megaplan.cloud.spec import load_spec
    try:
        load_spec(Path(f.name))
        print('FAIL: should have raised')
    except Exception as e:
        print(f'OK: {e}')
"
# Expected: OK with railway-removal error

# 7. Verify entrypoint rendering has no Railway references
python -c "
from arnold.pipelines.megaplan.cloud.spec import CloudSpec, RepoSpec, SshSpec, CodexSpec, MegaplanSpec, ResourcesSpec
from arnold.pipelines.megaplan.cloud.template import render_entrypoint
spec = CloudSpec(
    provider='ssh', repo=RepoSpec(url='https://github.com/example/app.git'),
    agents={'default': 'codex'}, codex=CodexSpec(), mode='idle',
    megaplan=MegaplanSpec(), resources=ResourcesSpec(), secrets=[],
    ssh=SshSpec(host='test.example.com'),
)
rendered = render_entrypoint(spec)
assert 'railway' not in rendered.lower(), f'Found railway ref: ...'
print('OK: no Railway references in entrypoint')
"
```

---

## Summary of What the Implementation Must Do (for context, not to execute)

The analysis above identifies the *test and doc surface*. For the implementation side to make those tests pass:

1. **`spec.py`**: Change `default="railway"` → `default="ssh"` on line 306; remove `"railway"` from `VALID_PROVIDERS`; raise a clear `CliError` if `provider: railway` is encountered (or add it to `FUTURE_PROVIDERS` if you want soft deprecation).

2. **`providers/ssh.py`**: Wire `resources.volume` into `deploy()` — add `-v <volume_name>:/workspace` (or to `repo.workspace`) on `docker run`. In `destroy()`, honor `volume` param with `docker volume rm`.

3. **`providers/base.py`**: Remove `_railway_provider` factory and its entry in `_PROVIDER_FACTORIES`.

4. **`providers/railway.py`**: Can be deleted entirely, or kept as a no-op with a migration error.

5. **`template.py`**: Skip `railway.toml` materialization when `provider != "railway"` (line 380-382).

6. **`cli.py`**: Remove the `RailwaySpec` import and the `--session` gating on railway provider name.

7. **`cloud.yaml.tmpl`**: Flip defaults as described above.