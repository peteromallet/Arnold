# Make Extension Reality Chain Directly Restartable

This milestone was generated by `megaplan cloud quickstart` from the source brief below.

## Source Brief

# Continue Extension Reality Chain Recovery Until Direct Restart Is Safe

You are running as the cloud Codex continuation agent on the Hetzner megaplan agentbox. Continue the recovery line until `extension-reality-convergence-epic` can be restarted/resumed directly and safely.

## Current External State To Verify First

Do not trust this brief blindly. Inspect current state before acting.

Cloud host/container:
- SSH host: `root@159.69.51.216`
- Container: `megaplan-cloud-agent`
- Engine checkout with dirty live hot patch: `/workspace/arnold`
- Clean prevention worktree: `/workspace/arnold-chain-guard-min`
- Original corrupted epic workspace: `/workspace/extension-reality-convergence-epic/reigh-app`
- Clean merged target verification worktree: `/workspace/extension-reality-final-verify`
- M3/M4 recovery worktree: `/workspace/extension-reality-m3-m4-recovery`

Recovered product target:
- Repo: `peteromallet/Arnold`
- Target branch: `local/extension-foundation-completion`
- Target head after recovery: `dfe60989d Recover M3 export readiness on top of M4`
- Branch history should include:
  - `60d2f43be` M1 trust model truth
  - `148a7b7d9` M2 composition shader ref authority
  - `99528056e` M4 evidence and drift gates
  - `dfe60989d` recovered M3 export readiness on top of M4
- Recovery PR merged: https://github.com/peteromallet/Arnold/pull/190
- M3 salvage branch preserved: `recovery/m3-export-readiness-salvage`
- Recovery branch was `recovery/extension-reality-m3-m4` and may have been deleted by merge.

Validated against merged target `/workspace/extension-reality-final-verify`:
- `node scripts/quality/check-readiness.mjs --strict` passed: 23/23 rows, 0 failures.
- `npx vitest run --config config/testing/vitest.config.ts scripts/quality/check-trust-model-truth.test.mjs src/tools/video-editor/runtime/composition/shaderRefAuthority.test.ts src/tools/video-editor/runtime/composition/compositionGraphProjector.test.ts src/tools/video-editor/runtime/renderPlanner.test.ts src/tools/video-editor/runtime/exportGuard.test.ts src/tools/video-editor/hooks/useRenderState.test.tsx` passed: 5 files, 248 tests.

Known broad CI state on PR #190 after merge:
- Dockerfile checks passed.
- Slot-first audit-mode checks still failed after workflow browser install was fixed. The original failure was missing Playwright browser install. After patching `.github/workflows/slot-first-quality.yml` from `npx playwright install chromium` to `npx playwright install --with-deps`, the remaining failure was broad e2e selectors missing / body hidden across extension harness tests. Treat this as unresolved evidence, not necessarily caused by M3 recovery. Investigate if needed for chain restart readiness.

Prevention patch state:
- Live `/workspace/arnold` has a hot patch for chain guards and status custody; focused tests there passed earlier: `tests/test_chain_completion_guard.py tests/cloud/test_status_snapshot.py` -> 113 passed. However `/workspace/arnold` is dirty with many unrelated edits, so do not commit from it wholesale.
- Clean prevention PR is open: https://github.com/peteromallet/Arnold/pull/191
- Branch: `fix/chain-custody-guards-min`
- Latest expected head after amend: `e4592338 Fix chain custody completion guards`
- It targets `editible-install`.
- It includes focused changes only:
  - `arnold_pipelines/megaplan/chain/__init__.py`
  - `arnold_pipelines/megaplan/cloud/status_snapshot.py`
  - `tests/test_chain_completion_guard.py`
  - `tests/cloud/test_status_snapshot.py`
  - `docs/arnold/package-authoring-contract.md` line 237 wording fix for existing docs lint.
- Focused validation on `/workspace/arnold-chain-guard-min` after amend:
  - `python3 -m pytest tests/cloud/test_status_snapshot.py -q` -> 61 passed
  - `python3 -m pytest tests/test_chain_completion_guard.py::test_merged_pr_completion_queries_gh_when_only_pr_number_is_recorded tests/test_chain_completion_guard.py::test_merged_pr_completion_prefers_gh_merge_commit_over_stale_chain_pr_head tests/test_chain_completion_guard.py::test_completion_guard_rejects_published_pr_target_not_in_chain_target tests/test_chain_completion_guard.py::test_reconcile_rolls_cursor_back_when_completed_record_not_authoritative -q` -> 4 passed
  - `python3 -m pytest tests/docs/test_m5_generated_scans.py -q` -> 10 passed after doc wording fix.
- Before the doc amend, PR #191 CI failed only on `docs/arnold/package-authoring-contract.md:237 '@step'`. After the amend, CI should rerun. Check current status and fix if needed.

## Root Cause To Preserve In Your Reasoning

`extension-reality-convergence-epic` previously appeared complete but was not. The chain accepted non-authoritative M3 publication evidence:

- M3 plan reached `done`, but its code was not in `origin/local/extension-foundation-completion`.
- M4 PR #183 merged on top of M2 (`148a7b7d9`), not M3.
- M3 state accepted bogus publication evidence using `gh.pr#95.mergeCommit`; PR #95 was unrelated/closed/unmerged.
- Reconciliation logged that M3's completed record was not authoritative, but the cursor still advanced to M4.
- Status/watchdog/L3 then derived a false complete story from stale chain state.

The prevention patch must make this class impossible or at least loud:

- Published PR evidence from `gh pr view` must require `state=merged` and `mergedAt`.
- Merged PR target must be contained in the chain target ref, not just semantically different somewhere else.
- If reconciliation removes a completed record, cursor must roll back to the first incomplete milestone and mark `authority_divergence`.
- Cloud status must not report complete when raw terminal chain state has fewer completed records than the spec milestone count; it should show attention/custody mismatch.

## What To Do Next

1. Inspect PR #191 current state.
   - If CI is still running, wait/poll.
   - If CI failed, fetch failed logs and fix the smallest relevant issue.
   - If CI passes and mergeable, merge it into `editible-install` using exact head SHA.

2. Once #191 is merged, ensure the cloud agent uses the durable engine fix.
   - Update/fetch `/workspace/arnold` or whatever editable engine path the cloud runner uses.
   - Do not overwrite unrelated dirty work without preserving it.
   - If you need a clean engine for chain restart, create a new clean worktree from `origin/editible-install` after #191 merge.

3. Regenerate or create corrected proof evidence for the extension reality chain against the corrected target.
   - Do not trust the old `.megaplan/plans/.chains/chain-c978c1583c3b.json` if it still records corrupted state.
   - Use fresh branch/commit evidence from `origin/local/extension-foundation-completion` at/after `dfe60989d`.
   - Prove M1+M2+M3+M4 are in the target branch and that M3 product changes are included.
   - Preserve the old corrupted chain state as forensic evidence; do not erase it silently.

4. Decide and execute the safest path to restart/resume the chain directly.
   - Preferred: after durable chain guard merge, run the chain/status reconciliation on the existing epic workspace and verify it does not falsely report complete from stale custody.
   - If restarting is safer than resuming, create a fresh continuation/recovery chain/session whose target starts from `origin/local/extension-foundation-completion` and whose state references the corrected branch head.
   - Do not relaunch the corrupted chain without first forcing/rebuilding custody state so it cannot skip M3 again.

5. Verification before claiming complete:
   - PR #191 merged or an equivalent durable engine fix is merged to the branch the cloud runner uses.
   - Fresh status snapshot does not report a stale terminal incomplete chain as complete.
   - Corrected target branch contains M1+M2+M3+M4 with evidence.
   - A direct chain restart/resume command has been run or a new supervised cloud chain is running with a valid marker and remote spec.
   - Any remaining broad CI failure is either fixed or captured as a separate non-blocking baseline with exact logs and rationale.

## Useful Commands

Check PR #191:

```
cd /workspace/arnold-chain-guard-min
token=$(git remote get-url origin | sed -n "s#https://x-access-token:\([^@]*\)@github.com/.*#\1#p")
GH_TOKEN="$token" gh pr view 191 --repo peteromallet/Arnold --json state,mergeable,mergeStateStatus,headRefOid,statusCheckRollup,url
```

Fetch failed CI logs if needed:

```
GH_TOKEN="$token" gh run view <run_id> --repo peteromallet/Arnold --job <job_id> --log-failed | tail -260
```

Focused prevention tests:

```
cd /workspace/arnold-chain-guard-min
python3 -m py_compile arnold_pipelines/megaplan/chain/__init__.py arnold_pipelines/megaplan/cloud/status_snapshot.py
python3 -m pytest tests/cloud/test_status_snapshot.py -q
python3 -m pytest \
  tests/test_chain_completion_guard.py::test_merged_pr_completion_queries_gh_when_only_pr_number_is_recorded \
  tests/test_chain_completion_guard.py::test_merged_pr_completion_prefers_gh_merge_commit_over_stale_chain_pr_head \
  tests/test_chain_completion_guard.py::test_completion_guard_rejects_published_pr_target_not_in_chain_target \
  tests/test_chain_completion_guard.py::test_reconcile_rolls_cursor_back_when_completed_record_not_authoritative -q
python3 -m pytest tests/docs/test_m5_generated_scans.py -q
```

Merged target verification:

```
cd /workspace/extension-reality-final-verify
git fetch origin local/extension-foundation-completion
git log --oneline -6 origin/local/extension-foundation-completion
node scripts/quality/check-readiness.mjs --strict
npx vitest run --config config/testing/vitest.config.ts \
  scripts/quality/check-trust-model-truth.test.mjs \
  src/tools/video-editor/runtime/composition/shaderRefAuthority.test.ts \
  src/tools/video-editor/runtime/composition/compositionGraphProjector.test.ts \
  src/tools/video-editor/runtime/renderPlanner.test.ts \
  src/tools/video-editor/runtime/exportGuard.test.ts \
  src/tools/video-editor/hooks/useRenderState.test.tsx
```

Cloud status / sessions:

```
cd /workspace/arnold
python3 -m arnold_pipelines.megaplan cloud status --all || true
ls /workspace/.megaplan/cloud-sessions | sort | tail -40
```

## Safety Rules

- Never `git reset --hard` or clean a dirty workspace unless you first create a preservation branch or worktree and can prove what will be discarded.
- Do not commit unrelated dirty files from `/workspace/arnold`.
- Do not force-push over recovery branches except the focused branch you own (`fix/chain-custody-guards-min`) and only after checking the current remote head.
- Use exact `--match-head-commit` when merging PRs via `gh pr merge`.
- Keep comments short but leave GitHub PR comments when you make important state changes.
