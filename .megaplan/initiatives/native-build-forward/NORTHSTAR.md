# Native Build Forward — North Star

## Destination

Deliver the representation report's two snapshots in strict order: **Native Parity, then Platformization**, and leave the system durably running under its own authority. Native Parity makes Megaplan's one-workflow-per-file `.pype` source the readable, deterministic, statically validated owner of product topology—loops, branches, fanout, named child workflows, suspension/reentry, policy, effects, and terminals—while generated `WorkflowManifest` artifacts own only admitted runtime and replay coordinates. Platformization starts only after Native S7 and turns the proven, qualified mechanics into independently installable, validated, recomposable, evolvable components that survive an unrelated second consumer.

## Substrate is not conformance

Completed Native Python, composition, platform, Custody-adjacent, and MRC work is valuable substrate and historical evidence. It is not representation-report conformance. A row is not implemented while route, retry, fanout, loop exit, suspension, effect, completion, or terminal authority remains in handlers, component tables, route bindings, mutable status, CLI logic, projections, or runtime re-derivation. The 31 alignment rows, H0–H9, D1–D15, false-pass scenarios, and negative invariants close only through current source-derived proof.

## Authority split

- `docs/arnold/megaplan-native-representation-report.md` is architecture authority for contract separation and the two-stage destination.
- Active chain specs, milestone briefs, validators, typed transition handlers, proof maps, and accepted receipts are executable authority.
- `docs/arnold/native-plan-reconciliation-2026-08-24.md` is the controlling dated adjudication.
- `docs/arnold/native-megaplan-build-forward-plan-2026-08-24.md`, including its post-reconciliation revision, is the preferred implementation guide.
- Run Authority grants/quarantines; Custody owns exclusive exact-action responsibility; WBC owns exact-version attempts/events/effects; source owns product semantics; manifests own admitted coordinates; projections are rebuildable and powerless.

## M11 admission gate

P0 is read-only MRC intake and crosswalk. P1 is an output-based Custody M11 admission resolution, not a presumed rerun. Native S1 is prohibited until canonical Run Authority disposition, approved zero-blocker ownership, accepted Custody completion manifest/proof map, accepted bounded projection handoff, installed/runtime/production-vector and canary acceptance, and the accepted milestone-gate bootstrap manifest plus all three readiness artifacts are content-addressed and validator-green. Missing evidence is a stop condition. Native never mints a substitute.

## Controlling sequence

`P0 → P1 → P2 → Native S1 → S2F → C1 → C2 → S2R → S3A → S3B → S4 → S5A → S5B → S6 → S7 → Platform S1 → S2A → S2B → S3 → S4 → S5 → S6`.

C1/C2 remain non-authoritative shadows. S2R is the sole completion-kernel enablement. Every authority cut lands inert, compares at merge HEAD, consumes a typed current receipt, transitions once, receives independent post-verification, proves the old producer inert, and then fences or deletes it. Effects are never dual-written. Platform S5 supplies the unrelated consumer; Platform S6 is the only stable-publication point.

## Completion standard

Every milestone closes with a content-addressed proof map and the applicable §6 evidence-pack fields from the build-forward plan. Stable validator issue codes, exact provenance, elapsed budgets, source/carrier mutations, raw traces, install/runtime identity, allowances with expiry, independent review, and zero dangling references are mandatory. Authority, effect, and publication cuts additionally require pre-transition readiness, one typed transition, independent post-transition proof, selected-state canary census, and pointer-only rollback. Historical prose, file existence, green status, or generated projections never satisfy a must.

## Durable-running boundary

The first real owner/provider/fixer dispatch is an intermediate result, not
completion. Durable-running requires an active supervisor, observed milestone
progress, restart/replay/recovery evidence, and a path that does not depend on
manual state surgery. The accepted launch/durability root crosswalk is
`.megaplan/initiatives/native-build-forward/handoff/launch-and-durability-root-map-20260903.md`.
Bucket A in that map is consumed by C2 before first dispatch; Bucket B is
consumed by S2R/S6 and the later Platform milestones before durable promotion
and final proof.

## Bounded planning and review artifacts

The planning budget is one North Star, one bounded `chain.yaml`, one brief per
milestone, and generated plan/review/receipt evidence. The forensic package is
immutable evidence, not a second execution ledger. Reviews follow each
milestone's configured Megaplan robustness; there are no mandatory Megado
per-batch Luna reviews, premium/cumulative review packets, duplicate hashes, or
seals-as-progress.
