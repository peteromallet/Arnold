Work only in /workspace/arnold-roadmap-superfix on branch fix/repository-strategy-roadmap-launch-custody. Read the complete superfixer-debug and megaplan-cloud SKILL.md files first. This is an isolated clean worktree based on origin/main (17fb30d3a2), which already contains three partial cloud fixes. Do not touch /workspace/arnold or any external runtime; do not deploy, push, or launch. Preserve scope and commit nothing; leave edits/tests for the primary operator.

Incident facts to address generically:
- `repository-strategy-roadmap` ran canonical on-box `cloud chain --fresh --no-editable-install-sync` with configured megaplan.ref `editible-install` after that remote branch had been deleted. Local preflight used `--skip-remote`; launch wrote a marker, tmux driver died before creating chain state, verification returned `warning`, but CLI still returned 0 and persisted no launch outcome/log tail.
- Normal sync was deliberately skipped because the shared source was dirty. The flag currently records only skipped/disabled. The configured engine ref therefore was never proven remotely reachable.
- Watchdog `scan_once_unlocked` calls `repair_trigger_scan` before `sync_editable_source_branch`/editable install bootstrap. The sweep died in Python import with `ModuleNotFoundError: yaml`; report stayed stale and no L1 artifact was created.
- L3 auditor can place a marker-present, stopped/no-process, absent-chain-state, absent-repair session in green_checks because no deterministic gather reason exists.
- Separate DeepSeek dispatch initially failed because `launch_hermes_agent.py` legacy probe treats ModuleNotFoundError(name='megaplan.agent') as fatal rather than an allowed fallback to `arnold.agent`; inspect whether the active source has this path and patch/test only if still present and relevant to L2/meta-repair.

Implement the smallest durable source fix with regression tests:
1. Before tmux for both chain and epic-chain, prove the exact configured `spec.megaplan.ref` is advertised by `spec.megaplan.repo` (branch/tag/ref; support configured full refs and fail closed with precise diagnostic). This must run even when `--no-editable-install-sync` is used and should be part of non-`--skip-remote` preflight if architecturally possible. Do not add an unsafe bypass or weaken guards.
2. Persist a durable, atomically written launch outcome on the session marker. On pre-tmux ref failure or post-tmux non-advancement, record failed status, stable code, exact useful stderr/log tail, observed time and verification; return nonzero via CliError. On verified advancement record running/success. Ensure a failed launch still has enough marker/spec/workspace custody for watchdog repair. Avoid clearing a live session.
3. Make watchdog observation bootstrap happen before eligibility scan: validate required imports using the intended source/runtime; attempt the existing ordinary source-sync/editable-refresh recovery first if invalid, then emit a durable loud watchdog report/error and nonzero if still blind. Never silently continue with stale report.
4. Add deterministic L3 gather reason for marker-present + nonterminal/stopped/no live process + absent chain state, especially with a failed launch outcome; it must not enter green_checks. Include exact marker launch failure in evidence.
5. Cover chain and epic siblings and focused tests. Required validation includes bash -n wrappers, tests/cloud/test_watchdog_wrappers.py, plus relevant cloud chain/preflight/progress auditor tests. Keep modifications tightly scoped.

Return a concise summary of changed files, design, tests run/results, and any unresolved issue. Do not claim runtime recovery.
