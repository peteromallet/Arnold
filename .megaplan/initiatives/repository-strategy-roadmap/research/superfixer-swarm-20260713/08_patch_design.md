Read-only adversarial patch-design review. Read megaplan-cloud and superfixer skills. Inspect current dirty-tree diffs first and avoid assuming ownership. Design the smallest generic fix that: verifies the required engine ref before tmux, makes `--no-editable-install-sync` safe/fail-loud, records a durable actionable launch failure, causes non-advancing verification to return nonzero, lets watchdog/L1 see exact stderr, bootstraps/checks watchdog observation dependencies before eligible scan, and lets L3 catch recurrence. Cover both chain and epic-chain siblings. Name exact functions/files/tests and overlap risks. No edits, under 900 words.
