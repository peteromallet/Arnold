# Archived original five-milestone chain

This directory non-destructively preserves the exact planning inputs used when
historical chain `chain-c256f171485f` initialized M1 plan
`m1-durable-capture-cursors-20260713-2045` on 2026-07-13.

- `chain.yaml` is the original five-milestone executable spec.
- `briefs/m1.md` through `briefs/m5.md` are the original milestone briefs.

The plan remained initialized and no product implementation completed. These
files are historical evidence, not current launch inputs. They are superseded
by the root `chain.yaml` successor front door and must not be resumed, edited,
or interpreted as completed predecessor work.
