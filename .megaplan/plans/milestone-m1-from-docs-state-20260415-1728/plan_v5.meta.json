{
  "version": 5,
  "timestamp": "2026-04-15T19:30:44Z",
  "hash": "sha256:da8a655a64778d31e6071644d8ccb2c8443fd614b1dd557f62c8d14e55dac15e",
  "changes_summary": "Tightened the plan at the concrete seams: it now adds an explicit external-entity bypass/sync path to the factory, makes `useAutoSaveSettings` the sole write owner in the Join Clips adapter, preserves `null`/`undefined` disabled behavior explicitly, treats the hook tests as required integration work, and adds a real blur-based flush path for prompt fields in addition to pre-generate flush.",
  "flags_addressed": [
    "issue_hints-1",
    "issue_hints-2",
    "correctness-1",
    "correctness-2",
    "all_locations",
    "callers-1",
    "callers-2",
    "issue_hints",
    "correctness",
    "callers",
    "scope",
    "FLAG-M1-006",
    "FLAG-M1-007",
    "FLAG-M1-008"
  ],
  "questions": [],
  "success_criteria": [
    {
      "criterion": "`createEntityStore` exposes the requested generic API and passes focused tests for bootstrap precedence, optimistic updates, text-field flush, revert, reset, selector isolation, external-entity load bypass, overwrite-protected external sync, and equality deduping for repeated authoritative snapshots.",
      "priority": "must",
      "requires": [
        "read_files",
        "run_tests"
      ]
    },
    {
      "criterion": "`useJoinClipsSettings(projectId)` preserves its current public API for callers while internally using the entity store only as a reader-mode synchronized read model.",
      "priority": "must",
      "requires": [
        "read_files",
        "parse_diff",
        "run_tests"
      ]
    },
    {
      "criterion": "For Join Clips, `useAutoSaveSettings` remains the sole owner of local writes, debounce/save scheduling, revert/reset, and text-field semantics, so the adapter does not duplicate write ownership with `createEntityStore`.",
      "priority": "must",
      "requires": [
        "read_files",
        "parse_diff",
        "run_tests"
      ]
    },
    {
      "criterion": "The Join Clips adapter preserves the current `null` / `undefined` disabled behavior even though `createEntityStore.useEntity` is string-based.",
      "priority": "must",
      "requires": [
        "read_files",
        "run_tests",
        "parse_diff"
      ]
    },
    {
      "criterion": "Prompt-like Join Clips fields save through at least two concrete paths: blur wiring in the prompt UI and explicit flush before generation.",
      "priority": "must",
      "requires": [
        "read_files",
        "run_tests",
        "parse_diff"
      ]
    },
    {
      "criterion": "The Join Clips pilot handles the real current settings surface, including scalar fields and composite settings such as `clips`, `transitionPrompts`, and LoRA state, without changing RQ ownership semantics.",
      "priority": "must",
      "requires": [
        "read_files",
        "run_tests",
        "parse_diff"
      ]
    },
    {
      "criterion": "TypeScript remains clean with `npx tsc --noEmit`.",
      "priority": "must",
      "requires": [
        "run_shell",
        "read_build_output"
      ]
    },
    {
      "criterion": "`npm test` passes with no regressions from the baseline suite.",
      "priority": "must",
      "requires": [
        "run_tests",
        "read_build_output"
      ]
    },
    {
      "criterion": "The final diff stays limited to the foundation store, the Join Clips adapter, the minimum prompt blur/flush wiring required for text-field validation, tests, and dependency metadata if needed.",
      "priority": "should",
      "requires": [
        "parse_diff",
        "subjective_judgment"
      ]
    },
    {
      "criterion": "The factory internals stay simple enough to reuse for later milestones without adding new cross-cutting abstractions beyond the external-entity sync support required for reader mode.",
      "priority": "should",
      "requires": [
        "read_files",
        "subjective_judgment"
      ]
    },
    {
      "criterion": "M0 telemetry behavior remains unchanged because no instrumented telemetry surfaces are modified in this milestone.",
      "priority": "info",
      "requires": [
        "parse_diff",
        "subjective_judgment"
      ]
    }
  ],
  "assumptions": [
    "The user-settled tiebreaker verdict remains authoritative: for RQ-backed settings, `useEntity(id)` is reader-only, RQ remains authoritative through the existing boundary, and `createEntityStore.load` is reserved for store-owned non-server entities.",
    "For the Join Clips pilot, `useAutoSaveSettings` remains the sole owner of local-write lifecycle and persistence semantics; the new store is validated as a synchronized read model, not as a second writer.",
    "Minimal shared-component changes are in scope when they are strictly necessary to satisfy the approved text-field requirement that prompts save on blur or flush.",
    "Any existing uncommitted M1-related files in the worktree are draft context to verify and refine, not proof that the milestone is already correctly implemented."
  ],
  "delta_from_previous_percent": 90.23,
  "structure_warnings": []
}
