{
  "version": 12,
  "timestamp": "2026-04-15T20:30:57Z",
  "hash": "sha256:969d33a16a62bbb696f87abd2d289ccd5ff523516ae534b14eb6edb5e0a8c56b",
  "changes_summary": "Narrowed M1 back to the requested store-foundation plus hook port: the plan now treats the user prompt as the authoritative brief because the referenced refactor doc is missing, explicitly preserves `saveImmediate(dataToSave?: JoinClipsSettings)`, and removes the extra shared-form and generate-path rewiring from this milestone.",
  "flags_addressed": [
    "issue_hints-1",
    "issue_hints-2",
    "all_locations",
    "correctness",
    "callers",
    "scope",
    "scope-join-clips-ui"
  ],
  "questions": [],
  "success_criteria": [
    {
      "criterion": "`createEntityStore` exposes the requested generic API and passes focused tests for bootstrap precedence, optimistic updates, text-field flush, revert, reset, selector isolation, external-entity load bypass, eager disabled-sentinel bypass, React-safe registration ordering, same-tick external patching, overwrite-protected external sync, and equality deduping for repeated authoritative snapshots.",
      "priority": "must",
      "requires": [
        "read_files",
        "run_tests"
      ]
    },
    {
      "criterion": "`useJoinClipsSettings(projectId)` preserves its current public API for callers while internally using the entity store only as a reader-mode synchronized read model.",
      "priority": "must",
      "requires": [
        "read_files",
        "parse_diff",
        "run_tests"
      ]
    },
    {
      "criterion": "The Join Clips adapter preserves the full existing public surface from `useAutoSaveSettings`, including `entityId`, `isDirty`, `hasShotSettings`, `updateTextFields`, `initializeFrom`, the current error/save semantics, and `saveImmediate(dataToSave?: JoinClipsSettings)` with its optional payload parameter.",
      "priority": "must",
      "requires": [
        "read_files",
        "run_tests",
        "parse_diff"
      ]
    },
    {
      "criterion": "For Join Clips, `useAutoSaveSettings` remains the sole owner of local writes, debounce/save scheduling, revert/reset, and text-field semantics, so the adapter does not duplicate write ownership with `createEntityStore`.",
      "priority": "must",
      "requires": [
        "read_files",
        "parse_diff",
        "run_tests"
      ]
    },
    {
      "criterion": "The Join Clips adapter preserves the current `null` / `undefined` disabled behavior via a hook-safe strategy, and falsy mounts do not trigger `config.load()` for the internal sentinel entity.",
      "priority": "must",
      "requires": [
        "read_files",
        "run_tests",
        "parse_diff"
      ]
    },
    {
      "criterion": "The Join Clips store instance is created with explicit inert compatibility callbacks for the mandatory `load` and `save` config fields, and normal Join Clips reader-mode flows do not rely on those callbacks for persistence or authoritative reads.",
      "priority": "must",
      "requires": [
        "read_files",
        "parse_diff",
        "run_tests"
      ]
    },
    {
      "criterion": "The Join Clips adapter guarantees same-tick consistency for `joinSettings.settings` after delegated write calls, including composite snapshot updates used by `updateFields`, `initializeFrom`, clip hydration, `transitionPrompts`, `revert`, and `reset`, so existing consumers do not observe stale settings.",
      "priority": "must",
      "requires": [
        "read_files",
        "run_tests",
        "parse_diff"
      ]
    },
    {
      "criterion": "Factory-level tests, rather than new caller rewiring, prove `updateTextField` and `flushTextFields` semantics in M1.",
      "priority": "must",
      "requires": [
        "read_files",
        "run_tests",
        "parse_diff"
      ]
    },
    {
      "criterion": "M1 does not introduce new shared-form blur plumbing, prompt-specific UI rewiring, or pre-generate flush behavior in Join Clips production code.",
      "priority": "must",
      "requires": [
        "parse_diff",
        "read_files"
      ]
    },
    {
      "criterion": "The milestone explicitly validates `createEntityStore` in two places: direct factory-owned write actions through dedicated store tests, and reader/sync integration through the Join Clips production adapter.",
      "priority": "must",
      "requires": [
        "read_files",
        "run_tests",
        "parse_diff"
      ]
    },
    {
      "criterion": "The generic store-owned `load` path remains part of the factory contract and is proven in M1 through factory-level injected-callback tests, without claiming a new repo-backed read integration for a production consumer.",
      "priority": "must",
      "requires": [
        "read_files",
        "run_tests",
        "parse_diff"
      ]
    },
    {
      "criterion": "`docs/structure_detail/settings_system.md` is updated to reflect the new reader-mode registration/sync layer and the accepted Join Clips pilot narrowing without promising later-milestone behavior early.",
      "priority": "must",
      "requires": [
        "read_files",
        "parse_diff"
      ]
    },
    {
      "criterion": "The plan and execution reflect the actual branch state: `zustand` and a first-pass `createEntityStore` already exist, so M1 refines those artifacts rather than introducing them from zero.",
      "priority": "must",
      "requires": [
        "read_files",
        "parse_diff"
      ]
    },
    {
      "criterion": "The plan identifies the user prompt as the authoritative brief for this run because the referenced `docs/state_management_refactor_plan.md` is not present in the repository.",
      "priority": "must",
      "requires": [
        "read_files",
        "parse_diff"
      ]
    },
    {
      "criterion": "TypeScript remains clean with `npx tsc --noEmit`.",
      "priority": "must",
      "requires": [
        "run_shell",
        "read_build_output"
      ]
    },
    {
      "criterion": "`npm test` passes with no regressions from the baseline suite.",
      "priority": "must",
      "requires": [
        "run_tests",
        "read_build_output"
      ]
    },
    {
      "criterion": "The final diff stays limited to the existing foundation store implementation, the Join Clips hook adapter, the required settings-system doc update, targeted hook/store tests, and dependency metadata if needed.",
      "priority": "should",
      "requires": [
        "parse_diff",
        "subjective_judgment"
      ]
    },
    {
      "criterion": "The factory internals stay simple enough to reuse for later milestones without adding new cross-cutting abstractions beyond the external-entity sync support required for reader mode.",
      "priority": "should",
      "requires": [
        "read_files",
        "subjective_judgment"
      ]
    },
    {
      "criterion": "M0 telemetry behavior remains unchanged because no instrumented telemetry surfaces are modified in this milestone.",
      "priority": "info",
      "requires": [
        "parse_diff",
        "subjective_judgment"
      ]
    }
  ],
  "assumptions": [
    "The user-settled tiebreaker verdict remains authoritative: for RQ-backed settings, `useEntity(id)` is reader-only, RQ remains authoritative through the existing boundary, and `createEntityStore.load` is reserved for store-owned non-server entities.",
    "For the Join Clips pilot, `useAutoSaveSettings` remains the sole owner of local-write lifecycle and persistence semantics; the new store is validated as a synchronized read model, not as a second writer.",
    "Because `docs/state_management_refactor_plan.md` is not present in the repository, the user prompt is the authoritative M1 brief for this run, with `docs/state_management_refactor_baselines.md` and `docs/structure_detail/settings_system.md` used as supporting in-repo references.",
    "On this branch, `zustand` and a first-pass `createEntityStore` implementation already exist, so M1 execution refines those artifacts instead of adding them from scratch.",
    "This milestone intentionally does not add new Join Clips page wiring, shared-form blur plumbing, or generate-path flush behavior; those remain later work unless a hook-port-only compatibility fix proves unavoidable.",
    "Any existing uncommitted M1-related files in the worktree are draft context to verify and refine, not proof that the milestone is already correctly implemented."
  ],
  "delta_from_previous_percent": 46.84,
  "structure_warnings": []
}
