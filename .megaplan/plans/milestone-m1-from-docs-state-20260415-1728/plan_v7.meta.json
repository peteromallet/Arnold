{
  "version": 7,
  "timestamp": "2026-04-15T19:46:51Z",
  "hash": "sha256:b65b9fb3b4bf895e7eb720eecacfeee2d0dea776a4098cf1d664e8fb0040a72e",
  "changes_summary": "Specified the hook-safe disabled-path strategy with a stable sentinel entity id, narrowed text-field semantics to `prompt` and `negativePrompt` only, explicitly preserved the full `useJoinClipsSettings` public surface, and named the supporting shared-form test files that must be updated alongside the blur-prop wiring.",
  "flags_addressed": [
    "correctness-1",
    "correctness-2",
    "all_locations",
    "issue_hints",
    "callers",
    "FLAG-M1-011",
    "FLAG-M1-012"
  ],
  "questions": [],
  "success_criteria": [
    {
      "criterion": "`createEntityStore` exposes the requested generic API and passes focused tests for bootstrap precedence, optimistic updates, text-field flush, revert, reset, selector isolation, external-entity load bypass, React-safe registration ordering, overwrite-protected external sync, and equality deduping for repeated authoritative snapshots.",
      "priority": "must",
      "requires": [
        "read_files",
        "run_tests"
      ]
    },
    {
      "criterion": "`useJoinClipsSettings(projectId)` preserves its current public API for callers while internally using the entity store only as a reader-mode synchronized read model.",
      "priority": "must",
      "requires": [
        "read_files",
        "parse_diff",
        "run_tests"
      ]
    },
    {
      "criterion": "The Join Clips adapter preserves the full existing public surface from `useAutoSaveSettings`, including `entityId`, `isDirty`, `hasShotSettings`, `updateTextFields`, `initializeFrom`, and the current error/save semantics.",
      "priority": "must",
      "requires": [
        "read_files",
        "run_tests",
        "parse_diff"
      ]
    },
    {
      "criterion": "For Join Clips, `useAutoSaveSettings` remains the sole owner of local writes, debounce/save scheduling, revert/reset, and text-field semantics, so the adapter does not duplicate write ownership with `createEntityStore`.",
      "priority": "must",
      "requires": [
        "read_files",
        "parse_diff",
        "run_tests"
      ]
    },
    {
      "criterion": "The Join Clips adapter preserves the current `null` / `undefined` disabled behavior via a hook-safe strategy even though `createEntityStore.useEntity` is string-based.",
      "priority": "must",
      "requires": [
        "read_files",
        "run_tests",
        "parse_diff"
      ]
    },
    {
      "criterion": "Only the real text fields, `prompt` and `negativePrompt`, use `updateTextField` plus blur/flush plumbing; `enhancePrompt` stays on its existing boolean path.",
      "priority": "must",
      "requires": [
        "read_files",
        "run_tests",
        "parse_diff"
      ]
    },
    {
      "criterion": "Prompt-like Join Clips fields save through both required concrete paths: blur wiring through the shared form stack and explicit flush before generation.",
      "priority": "must",
      "requires": [
        "read_files",
        "run_tests",
        "parse_diff"
      ]
    },
    {
      "criterion": "The Join Clips pilot handles the real current settings surface, including scalar fields and composite settings such as `clips`, `transitionPrompts`, and LoRA state, without changing RQ ownership semantics.",
      "priority": "must",
      "requires": [
        "read_files",
        "run_tests",
        "parse_diff"
      ]
    },
    {
      "criterion": "The milestone explicitly validates `createEntityStore` in two places: direct factory-owned write actions through dedicated store tests, and reader/sync integration through the Join Clips production adapter.",
      "priority": "must",
      "requires": [
        "read_files",
        "run_tests",
        "parse_diff"
      ]
    },
    {
      "criterion": "TypeScript remains clean with `npx tsc --noEmit`.",
      "priority": "must",
      "requires": [
        "run_shell",
        "read_build_output"
      ]
    },
    {
      "criterion": "`npm test` passes with no regressions from the baseline suite.",
      "priority": "must",
      "requires": [
        "run_tests",
        "read_build_output"
      ]
    },
    {
      "criterion": "The final diff stays limited to the foundation store, the Join Clips adapter, the prompt blur/flush plumbing, tests, and dependency metadata if needed.",
      "priority": "should",
      "requires": [
        "parse_diff",
        "subjective_judgment"
      ]
    },
    {
      "criterion": "The factory internals stay simple enough to reuse for later milestones without adding new cross-cutting abstractions beyond the external-entity sync support required for reader mode.",
      "priority": "should",
      "requires": [
        "read_files",
        "subjective_judgment"
      ]
    },
    {
      "criterion": "M0 telemetry behavior remains unchanged because no instrumented telemetry surfaces are modified in this milestone.",
      "priority": "info",
      "requires": [
        "parse_diff",
        "subjective_judgment"
      ]
    }
  ],
  "assumptions": [
    "The user-settled tiebreaker verdict remains authoritative: for RQ-backed settings, `useEntity(id)` is reader-only, RQ remains authoritative through the existing boundary, and `createEntityStore.load` is reserved for store-owned non-server entities.",
    "For the Join Clips pilot, `useAutoSaveSettings` remains the sole owner of local-write lifecycle and persistence semantics; the new store is validated as a synchronized read model, not as a second writer.",
    "Minimal shared-form changes are in scope when they are strictly necessary to satisfy the approved prompt requirement that text fields save on blur or flush.",
    "Any existing uncommitted M1-related files in the worktree are draft context to verify and refine, not proof that the milestone is already correctly implemented."
  ],
  "delta_from_previous_percent": 32.8,
  "structure_warnings": []
}
