{
  "version": 3,
  "timestamp": "2026-04-15T17:40:47Z",
  "hash": "sha256:03ecea22ab26df8900083ad505eafcebf3337a66012de28d09f93be2cca1f480",
  "changes_summary": "Revised the plan to define a single authoritative first-read path, gate the React Query observer until after first bootstrap, and add explicit dedupe/refresh rules so the Join Clips bridge cannot double-fetch or repeatedly bootstrap the same entity.",
  "flags_addressed": [
    "all_locations",
    "FLAG-M1-004",
    "issue_hints",
    "correctness",
    "callers"
  ],
  "questions": [],
  "success_criteria": [
    {
      "criterion": "`createEntityStore` exposes the requested generic API and passes focused tests for bootstrap precedence, optimistic updates, text-field flush, revert, reset, selector isolation, late-load overwrite protection, and refresh deduping for repeated server snapshots.",
      "priority": "must",
      "requires": [
        "read_files",
        "run_tests"
      ]
    },
    {
      "criterion": "`useJoinClipsSettings(projectId)` preserves its current public API for callers while internally using the entity-store factory plus the explicit first-read/refresh-only bridge.",
      "priority": "must",
      "requires": [
        "read_files",
        "parse_diff",
        "run_tests"
      ]
    },
    {
      "criterion": "The Join Clips pilot handles the real current settings surface, including scalar fields and composite settings such as `clips`, `transitionPrompts`, and LoRA state, without introducing behavior regressions or repeated bootstrap churn.",
      "priority": "must",
      "requires": [
        "read_files",
        "run_tests",
        "parse_diff"
      ]
    },
    {
      "criterion": "The port has one authoritative initial read path for first mount and a separately gated refresh observer, so the store does not double-fetch or double-bootstrap the same entity.",
      "priority": "must",
      "requires": [
        "read_files",
        "parse_diff",
        "run_tests"
      ]
    },
    {
      "criterion": "The factory remains React Query-free, and the synchronization policy is implemented only in the Join Clips hook layer.",
      "priority": "must",
      "requires": [
        "read_files",
        "parse_diff"
      ]
    },
    {
      "criterion": "Prompt-like Join Clips fields are rewired to the text-field path and an explicit flush boundary is covered by tests, so M1 validates `updateTextField`/`flushTextFields` in a real consumer.",
      "priority": "must",
      "requires": [
        "read_files",
        "run_tests",
        "parse_diff"
      ]
    },
    {
      "criterion": "TypeScript remains clean with `npx tsc --noEmit`.",
      "priority": "must",
      "requires": [
        "run_shell",
        "read_build_output"
      ]
    },
    {
      "criterion": "`npm test` passes with no regressions from the baseline suite.",
      "priority": "must",
      "requires": [
        "run_tests",
        "read_build_output"
      ]
    },
    {
      "criterion": "The final diff stays limited to the foundation store, the Join Clips hook bridge, the minimum Join Clips caller rewiring required for text-field validation, tests, and dependency metadata if needed.",
      "priority": "should",
      "requires": [
        "parse_diff",
        "subjective_judgment"
      ]
    },
    {
      "criterion": "The factory internals stay simple enough to reuse for later milestones without adding new cross-cutting abstractions beyond the documented bridge and metadata needed for correctness.",
      "priority": "should",
      "requires": [
        "read_files",
        "subjective_judgment"
      ]
    },
    {
      "criterion": "M0 telemetry behavior remains unchanged because no instrumented telemetry surfaces are modified in this milestone.",
      "priority": "info",
      "requires": [
        "parse_diff",
        "subjective_judgment"
      ]
    }
  ],
  "assumptions": [
    "The referenced `docs/state_management_refactor_plan.md` is still absent from this checkout, so the brief plus current source remain the authoritative planning inputs inside this repository; that missing file is acknowledged context, not a repo-local blocker.",
    "Minimal rewiring inside the Join Clips consumer itself remains in scope when it is strictly necessary to validate the new factory behavior, especially `updateTextField` and `flushTextFields`.",
    "Using the existing non-hook settings services that `useToolSettings` delegates to still satisfies the requirement to keep the port on top of the existing tool-settings/Supabase persistence mechanism.",
    "Any existing uncommitted M1-related files in the worktree are draft context to verify and refine, not proof that the milestone is already correctly implemented."
  ],
  "delta_from_previous_percent": 89.49,
  "structure_warnings": []
}
