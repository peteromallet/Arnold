{
  "tasks": [
    {
      "id": "T1",
      "description": "Re-audit the live task-state surface while implementing M3 so the migration covers every current-project single-task and pending-generation path that materially contributes to rerender fanout. Classify each touched path as `store-backed now`, `store seeding boundary`, `query-authoritative by design`, or explicit defer. This audit must include `useGetTask` readers on gallery and variant UI, `useTaskPrefetch` including `usePrefetchTaskById()` as used by `VariantActions`, `generationTaskCache`, `GenerationTaskContext`, `useVariantSourceTask`, `usePendingGenerationTasks`, `useGenerationTaskDetails`, and an explicit defer-or-adopt decision for `useActiveTaskClips`. Lock the canonical normalization rule up front: realtime task hydration must use `isTaskDbRow(...)` and `mapTaskDbRowToTask(...)` from `src/shared/lib/taskRowMapper.ts`, must store canonical `Task` objects only, and must fetch or prefetch instead of creating partial snapshots when a raw realtime row is incomplete or invalid.",
      "depends_on": [],
      "status": "pending",
      "executor_notes": "",
      "files_changed": [],
      "commands_run": [],
      "evidence_files": [],
      "reviewer_verdict": ""
    },
    {
      "id": "T2",
      "description": "Implement `src/shared/state/realtimeStore.ts` and the centralized task-store seeding helpers. The store must be keyed by effective project scope and then `taskId`, expose single-task and pending-generation selectors only, support single upsert, batch upsert, and scoped reset, and preserve stable references for object or array selectors using `shallow` or equivalent equality control. Add canonical helper paths so boundary and query code can seed the store from either full `Task` snapshots or raw `unknown` realtime rows validated through the canonical task-row mapper. Do not add store-backed status-count ownership in M3.",
      "depends_on": [
        "T1"
      ],
      "status": "pending",
      "executor_notes": "",
      "files_changed": [],
      "commands_run": [],
      "evidence_files": [],
      "reviewer_verdict": ""
    },
    {
      "id": "T3",
      "description": "Rewrite `src/shared/providers/RealtimeProvider.tsx` into the selected-project store-writer boundary without changing Supabase connection ownership or the public status-only `useRealtime` API. The provider must hydrate `realtimeStore` from raw `RealtimeConnection` task `INSERT` and `UPDATE` rows, validate or map through the canonical task-row contract, fall back to targeted single-task fetch or prefetch when the raw row is incomplete or invalid, preserve existing invalidation or non-task fanout behavior through `RealtimeEventProcessor`, and clear scoped task snapshots when the selected project changes or disconnects so snapshots do not leak across projects.",
      "depends_on": [
        "T1",
        "T2"
      ],
      "status": "pending",
      "executor_notes": "",
      "files_changed": [],
      "commands_run": [],
      "evidence_files": [],
      "reviewer_verdict": ""
    },
    {
      "id": "T4",
      "description": "Adapt the single-task data path so React Query remains authoritative for fetch lifecycle while visible task readers subscribe narrowly through the store. Update `useGetTask` to preserve `resolveTaskProjectScope()` behavior, the empty-string disabled contract, and current return semantics while reading from store snapshots when available and seeding the store from successful query results. Integrate every live `taskQueryKeys.single(...)` writer or reader that is in scope into the same canonical seeding path, including `useTaskPrefetch`, `usePrefetchTaskById()`, `generationTaskCache`, `GenerationTaskContext`, and `useVariantSourceTask`. Migrate `useGenerationTaskDetails` and the gallery or variant surfaces that already flow through these hooks without introducing avoidable API churn.",
      "depends_on": [
        "T2",
        "T3"
      ],
      "status": "pending",
      "executor_notes": "",
      "files_changed": [],
      "commands_run": [],
      "evidence_files": [],
      "reviewer_verdict": ""
    },
    {
      "id": "T5",
      "description": "Rework generation-pending task reads while intentionally keeping aggregate counts query-authoritative. Update `usePendingGenerationTasks` so it preserves current cold-start correctness by bootstrapping the current-project queued or in-progress task set through the existing query path, validating and mapping those rows through the canonical helper, seeding `realtimeStore`, and then deriving generation-specific `pendingTasks` and `pendingCount` from a narrow selector keyed by effective project scope and generation id. Keep `useTaskStatusCounts` query-authoritative for both single-project and all-project modes, preserving current semantics including root-task exclusion, visible task-type filtering, processing rules, and recent success or failure windows. Do not silently claim the pre-existing `allProjectIds` cache-key aliasing defect is fixed unless you explicitly fix it as adjacent follow-up work.",
      "depends_on": [
        "T2",
        "T4"
      ],
      "status": "pending",
      "executor_notes": "",
      "files_changed": [],
      "commands_run": [],
      "evidence_files": [],
      "reviewer_verdict": ""
    },
    {
      "id": "T6",
      "description": "Run end-to-end code verification and only finish when the implementation is clean. Update existing tests for the store, provider, hooks, and facades so they cover task upsert, batch pending bootstrap, scoped reset, selector narrowing for unrelated task updates, stable selector references, canonical raw-event hydration, incomplete-row fetch fallback, empty-id disabled behavior, `resolveTaskProjectScope()` fallback, and coherent store seeding from prefetch, preload, and generation-task facade helpers. Then run grep audits for stale realtime-broadcast usage and `taskQueryKeys.single(...)` integration, write a short throwaway repro script for the specific bug shape in this milestone, run it to confirm unrelated task updates do not affect an unrelated selector subscription, delete the script, run the most relevant existing tests for changed files, run `npx tsc --noEmit`, and run `npm test`. If any automated check fails, fix the code and re-run until green. If a requested verification requires a browser, runtime app instance, telemetry overlay, or manual interaction, mark that verification as skipped immediately in execution evidence because it is deferred human validation.",
      "depends_on": [
        "T3",
        "T4",
        "T5"
      ],
      "status": "pending",
      "executor_notes": "",
      "files_changed": [],
      "commands_run": [],
      "evidence_files": [],
      "reviewer_verdict": ""
    }
  ],
  "watch_items": [
    "Use `src/shared/lib/taskRowMapper.ts` as the only canonical normalization path for realtime task snapshots; do not add a second realtime-only mapper.",
    "Store snapshots must be canonical `Task` objects scoped by effective project id, not raw `TaskRecord` fragments from realtime typing.",
    "If a raw realtime task row fails `isTaskDbRow(...)` or is missing required fields, fetch or prefetch the single task and seed from the fetched result; never create a partial snapshot.",
    "Preserve `RealtimeProvider` as the selected-project realtime boundary and preserve the public status-only `useRealtime` API.",
    "Keep `RealtimeEventProcessor` for invalidation and non-task fanout; do not turn it into the source of truth for full task snapshots.",
    "Preserve `useGetTask(taskId || '', projectId)` empty-id disabling and `resolveTaskProjectScope()` fallback semantics exactly.",
    "Cover `usePrefetchTaskById()` through the `useTaskPrefetch` migration, because `VariantActions` uses it on hover and it writes `taskQueryKeys.single(...)`.",
    "Integrate every live single-task cache writer or reader in scope so React Query, prefetch helpers, generation cache helpers, and the new store do not diverge.",
    "`usePendingGenerationTasks` must keep cold-start correctness by seeding from the current-project queued or in-progress query before relying on realtime updates.",
    "`useTaskStatusCounts` stays query-authoritative in M3; preserve existing derivation semantics and avoid broadening the milestone into full-project snapshot ownership.",
    "Do not silently claim the pre-existing `useTaskStatusCounts({ allProjectIds })` cache-key aliasing defect is fixed unless you explicitly change its query keying.",
    "Provide stable references for selectors returning arrays or objects and prove that with tests.",
    "Clear the scoped store on selected-project change or disconnect so task data does not leak across projects.",
    "`useActiveTaskClips` is intentionally deferred unless it can adopt the finished selector helper with negligible extra semantics and tests; document whichever decision is taken.",
    "Human-only runtime rerender checks are deferred: if encountered during execution, skip them immediately with evidence rather than blocking or retrying."
  ],
  "sense_checks": [
    {
      "id": "SC1",
      "task_id": "T1",
      "question": "Did the implementation inventory every live task-state surface in scope, explicitly classify each one, include the `usePrefetchTaskById()` hover path, and lock the canonical mapper and incomplete-row rules before store hydration logic was added?",
      "executor_note": "",
      "verdict": ""
    },
    {
      "id": "SC2",
      "task_id": "T2",
      "question": "Does `realtimeStore.ts` store canonical `Task` objects by effective project scope and `taskId`, expose only the M3 selectors and actions, and preserve stable references for semantically unchanged array or object selector results?",
      "executor_note": "",
      "verdict": ""
    },
    {
      "id": "SC3",
      "task_id": "T3",
      "question": "Does `RealtimeProvider` now seed the store from raw realtime task rows through the canonical validator or mapper path, preserve the existing selected-project lifecycle and `useRealtime` API, and fall back to targeted task fetch instead of partial writes on invalid rows?",
      "executor_note": "",
      "verdict": ""
    },
    {
      "id": "SC4",
      "task_id": "T4",
      "question": "Do `useGetTask`, prefetch helpers, generation cache helpers, variant-source readers, and generation-task facade paths now share one canonical store-seeding contract while preserving empty-id behavior, project-scope resolution, and caller-facing signatures?",
      "executor_note": "",
      "verdict": ""
    },
    {
      "id": "SC5",
      "task_id": "T5",
      "question": "Does `usePendingGenerationTasks` still cold-start from the existing current-project pending query before deriving generation-specific results from narrow selectors, while `useTaskStatusCounts` remains query-authoritative with unchanged counting semantics?",
      "executor_note": "",
      "verdict": ""
    },
    {
      "id": "SC6",
      "task_id": "T6",
      "question": "Did the executor prove the change through existing automated tests, grep audits, a temporary reproduction script that was run and deleted, `npx tsc --noEmit`, and `npm test`, and explicitly skip only the browser or human-only rerender checks with evidence?",
      "executor_note": "",
      "verdict": ""
    }
  ],
  "meta_commentary": "Execution should stay tightly scoped to selector narrowing for current-project single-task and pending-generation reads. The highest-risk failure mode is accidental drift between three task hydration paths: query results, prefetch helpers, and raw realtime rows. Solve that once with a canonical seeding layer and route all in-scope writers through it. Keep React Query authoritative for fetching and aggregate freshness; the store is only a subscription-narrowing layer. Treat the open `useTaskStatusCounts({ allProjectIds })` cache-key aliasing issue as accepted adjacent debt unless you deliberately choose to fix it without destabilizing scope. Do not reintroduce broadcast-style context reads or consumer-side `useEffect` bridges from query results into the store. For verification, prefer focused existing tests first, then full typecheck and `npm test`; runtime rerender observations requiring a browser or telemetry overlay are deferred-human and should be marked skipped on first encounter with concrete evidence.",
  "validation": {
    "plan_steps_covered": [
      {
        "plan_step_summary": "Reconfirm migration surface and classify live task readers or cache writers",
        "finalize_task_ids": [
          "T1"
        ]
      },
      {
        "plan_step_summary": "Freeze canonical task-row normalization contract",
        "finalize_task_ids": [
          "T1",
          "T2",
          "T3"
        ]
      },
      {
        "plan_step_summary": "Freeze caller-facing task contracts for single-task, pending bootstrap, and counts",
        "finalize_task_ids": [
          "T1",
          "T4",
          "T5"
        ]
      },
      {
        "plan_step_summary": "Build the scoped realtime task snapshot store",
        "finalize_task_ids": [
          "T2"
        ]
      },
      {
        "plan_step_summary": "Centralize canonical store-seeding helpers in task data layer",
        "finalize_task_ids": [
          "T2",
          "T4"
        ]
      },
      {
        "plan_step_summary": "Wire raw realtime task rows into the store at the provider boundary",
        "finalize_task_ids": [
          "T3"
        ]
      },
      {
        "plan_step_summary": "Adapt single-task readers and generation-task facade onto the store-backed path",
        "finalize_task_ids": [
          "T4"
        ]
      },
      {
        "plan_step_summary": "Rework pending-generation reads while keeping counts query-authoritative",
        "finalize_task_ids": [
          "T5"
        ]
      },
      {
        "plan_step_summary": "Remove obsolete assumptions and document the narrowed milestone outcome",
        "finalize_task_ids": [
          "T5",
          "T6"
        ]
      },
      {
        "plan_step_summary": "Add coverage for selector narrowing, canonical hydration, and boundary contracts",
        "finalize_task_ids": [
          "T6"
        ]
      }
    ],
    "orphan_tasks": [],
    "completeness_notes": "All ten numbered plan steps map to at least one execution task. Execution ordering is encoded directly in task dependencies: audit and contract lock first, store and seeding layer second, provider wiring third, single-task consumer migration fourth, pending-generation and count handling fifth, and all verification last. The final task intentionally absorbs grep audits, the required throwaway reproduction script, focused existing test runs, `npx tsc --noEmit`, and `npm test` so the executor closes the loop instead of stopping at implementation. Human-only rerender checks remain intentionally outside automated completion and should be skipped with evidence if encountered.",
    "coverage_complete": true
  },
  "baseline_test_failures": null,
  "baseline_test_command": null,
  "baseline_test_note": "No supported test runner detected on PATH (looked for: pytest). Configure test_command in state config to specify one."
}
