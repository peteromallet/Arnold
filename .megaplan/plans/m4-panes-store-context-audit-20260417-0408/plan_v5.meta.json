{
  "version": 5,
  "timestamp": "2026-04-17T04:40:02Z",
  "hash": "sha256:1d767d5621a5c5cf481993ed2ece732c0628331853f9c3628b5534e923f6440a",
  "changes_summary": "Narrowed the permanent automation back to the brief\u2019s actual requirement: guarding new fat `createContext(...)` declarations only. Added the missing panes-store reset/lifecycle contract and broadened the wrapper-hook inventory so the audit covers all exported safe/access hooks instead of a narrow name list.",
  "flags_addressed": [
    "issue_hints",
    "correctness",
    "scope",
    "all_locations-wrapper-hooks-0",
    "all_locations"
  ],
  "questions": [],
  "success_criteria": [
    {
      "criterion": "`src/shared/state/panesStore.ts` exists and exports selector-based panes state APIs used by production consumers.",
      "priority": "must",
      "requires": [
        "read_files",
        "parse_diff"
      ]
    },
    {
      "criterion": "No production file imports `PanesContext`, `PanesProvider`, or `usePanes()` after the migration.",
      "priority": "must",
      "requires": [
        "read_files",
        "run_shell",
        "parse_diff"
      ]
    },
    {
      "criterion": "All former `usePanes()` call sites are migrated to dedicated selector hooks or explicit narrow grouped selectors, with no broad panes compatibility facade left behind.",
      "priority": "must",
      "requires": [
        "read_files",
        "run_shell",
        "parse_diff"
      ]
    },
    {
      "criterion": "The panes store has an explicit singleton lifecycle contract for tests and bootstrap behavior, including a reset path that prevents cross-test state leakage.",
      "priority": "must",
      "requires": [
        "read_files",
        "parse_diff",
        "run_tests"
      ]
    },
    {
      "criterion": "`docs/state_management_context_audit.md` is committed and covers both remaining `createContext` provider declarations and exported recomposed wrapper hooks, including field count, external production consumer count, decision, and rationale.",
      "priority": "must",
      "requires": [
        "read_files",
        "parse_diff"
      ]
    },
    {
      "criterion": "The audit inventory method captures generic and namespaced context declarations in this repo and inventories exported safe/access hooks broadly enough to classify related context-access surfaces such as `useVideoEditSafe()`, `useImageEditFormSafe()`, and `useImageEditStatusSafe()`.",
      "priority": "must",
      "requires": [
        "read_files",
        "run_shell",
        "parse_diff"
      ]
    },
    {
      "criterion": "Every audited non-pane surface that exceeds both thresholds under the explicit external-production consumer rule is migrated or narrowed within the same milestone branch.",
      "priority": "must",
      "requires": [
        "read_files",
        "parse_diff",
        "run_tests"
      ]
    },
    {
      "criterion": "An automated guard exists for new over-threshold `createContext(...)` declarations, uses the same documented field-count rule as the audit, and is wired into an automated repo check.",
      "priority": "must",
      "requires": [
        "read_files",
        "parse_diff",
        "run_shell"
      ]
    },
    {
      "criterion": "`npx tsc --noEmit` passes cleanly after the migration and any required test updates.",
      "priority": "must",
      "requires": [
        "run_tests",
        "read_build_output"
      ]
    },
    {
      "criterion": "`npm test` passes at baseline after the panes migration and any additional required audit-driven migrations.",
      "priority": "must",
      "requires": [
        "run_tests",
        "read_build_output"
      ]
    },
    {
      "criterion": "High-level panes consumers such as `AgentChat.tsx`, `ShotEditorView.tsx`, `TasksPane.tsx`, `useImageLightboxEnvironment.ts`, and `useVideoLightboxEnvironment.ts` are migrated without recreating a broad grouped panes facade.",
      "priority": "should",
      "requires": [
        "read_files",
        "parse_diff",
        "subjective_judgment"
      ]
    },
    {
      "criterion": "The audit keeps scope tight by documenting framework/local UI token contexts and below-threshold wrapper-hook surfaces as `out-of-scope`, `keep`, or `narrow` instead of migrating them unnecessarily.",
      "priority": "should",
      "requires": [
        "read_files",
        "parse_diff",
        "subjective_judgment"
      ]
    },
    {
      "criterion": "Opening one pane does not visually re-render unrelated panes when checked with the telemetry overlay in a running app.",
      "priority": "info",
      "requires": [
        "drive_browser",
        "inspect_runtime_ui",
        "observe_runtime_logs"
      ]
    },
    {
      "criterion": "Mobile exclusive-pane behavior remains unchanged on a small viewport: opening pane B closes pane A as before.",
      "priority": "info",
      "requires": [
        "drive_browser",
        "inspect_runtime_ui",
        "verify_physical_device"
      ]
    }
  ],
  "assumptions": [
    "The main implementation target remains the panes subsystem; additional migrations happen only for audited non-pane surfaces that actually exceed both thresholds after applying the external-production consumer rule.",
    "A repo-level quality check under `scripts/quality/` is the preferred enforcement mechanism because it fits the repository\u2019s existing quality tooling.",
    "Broad recomposed access hooks remain part of the audit and migration work for this milestone, but the permanent automated guard required by the brief is limited to fat context declarations.",
    "High-level panes consumers that currently read multiple fields can still use grouped selectors where necessary, but only as explicit narrow groupings rather than a recreated `usePanes()`-style aggregate facade."
  ],
  "delta_from_previous_percent": 35.72,
  "structure_warnings": []
}
