{
  "version": 4,
  "timestamp": "2026-04-17T04:32:11Z",
  "hash": "sha256:9a5f9259292203e6e91c957b1342dfbf15f0ee9b602a89803aa9705a0af7065a",
  "changes_summary": "Pinned down the remaining ambiguous threshold rule: wrapper-hook consumer counts now use only external production consumers, excluding same-file helper wiring, tests, and mocks. The revision also makes the audit doc and automation share that exact rule so likely keep-or-narrow surfaces like `useVideoTravelSettings()` and `useImageEditCanvasSafe()` are not over-migrated by a naive grep count.",
  "flags_addressed": [
    "issue_hints",
    "correctness",
    "scope",
    "FLAG-M4-006"
  ],
  "questions": [],
  "success_criteria": [
    {
      "criterion": "`src/shared/state/panesStore.ts` exists and exports selector-based panes state APIs used by production consumers.",
      "priority": "must",
      "requires": [
        "read_files",
        "parse_diff"
      ]
    },
    {
      "criterion": "No production file imports `PanesContext`, `PanesProvider`, or `usePanes()` after the migration.",
      "priority": "must",
      "requires": [
        "read_files",
        "run_shell",
        "parse_diff"
      ]
    },
    {
      "criterion": "All former `usePanes()` call sites are migrated to dedicated selector hooks or explicit narrow grouped selectors, with no broad panes compatibility facade left behind.",
      "priority": "must",
      "requires": [
        "read_files",
        "run_shell",
        "parse_diff"
      ]
    },
    {
      "criterion": "`docs/state_management_context_audit.md` is committed and covers both remaining `createContext` provider declarations and exported recomposed wrapper hooks, including field count, external production consumer count, decision, and rationale.",
      "priority": "must",
      "requires": [
        "read_files",
        "parse_diff"
      ]
    },
    {
      "criterion": "The audit inventory method captures generic and namespaced context declarations in this repo, inventories recomposed context-access hooks, and applies one explicit consumer-count rule that excludes same-file helper wiring, tests, and mocks.",
      "priority": "must",
      "requires": [
        "read_files",
        "run_shell",
        "parse_diff"
      ]
    },
    {
      "criterion": "Every audited non-pane surface that exceeds both thresholds under the explicit external-production consumer rule is migrated or narrowed within the same milestone branch.",
      "priority": "must",
      "requires": [
        "read_files",
        "parse_diff",
        "run_tests"
      ]
    },
    {
      "criterion": "An automated guard exists, uses the same documented field-count and consumer-count rules as the audit, and covers both over-threshold `createContext` declarations and over-threshold recomposed compatibility hooks.",
      "priority": "must",
      "requires": [
        "read_files",
        "parse_diff",
        "run_shell"
      ]
    },
    {
      "criterion": "`npx tsc --noEmit` passes cleanly after the migration and any required test updates.",
      "priority": "must",
      "requires": [
        "run_tests",
        "read_build_output"
      ]
    },
    {
      "criterion": "`npm test` passes at baseline after the panes migration and any additional required audit-driven migrations.",
      "priority": "must",
      "requires": [
        "run_tests",
        "read_build_output"
      ]
    },
    {
      "criterion": "High-level panes consumers such as `AgentChat.tsx`, `ShotEditorView.tsx`, `TasksPane.tsx`, `useImageLightboxEnvironment.ts`, and `useVideoLightboxEnvironment.ts` are migrated without recreating a broad grouped panes facade.",
      "priority": "should",
      "requires": [
        "read_files",
        "parse_diff",
        "subjective_judgment"
      ]
    },
    {
      "criterion": "The audit keeps scope tight by documenting framework/local UI token contexts as out-of-scope instead of migrating them unnecessarily, and by documenting likely keep-or-narrow wrapper-hook surfaces that stay below threshold after external-production counting.",
      "priority": "should",
      "requires": [
        "read_files",
        "parse_diff",
        "subjective_judgment"
      ]
    },
    {
      "criterion": "Opening one pane does not visually re-render unrelated panes when checked with the telemetry overlay in a running app.",
      "priority": "info",
      "requires": [
        "drive_browser",
        "inspect_runtime_ui",
        "observe_runtime_logs"
      ]
    },
    {
      "criterion": "Mobile exclusive-pane behavior remains unchanged on a small viewport: opening pane B closes pane A as before.",
      "priority": "info",
      "requires": [
        "drive_browser",
        "inspect_runtime_ui",
        "verify_physical_device"
      ]
    }
  ],
  "assumptions": [
    "The main implementation target remains the panes subsystem; additional migrations happen only for audited non-pane surfaces that actually exceed both thresholds after applying the external-production consumer rule.",
    "A repo-level quality check under `scripts/quality/` is the preferred enforcement mechanism because it can cover both context declarations and recomposed exported hooks with one documented rule set.",
    "Broad recomposed access hooks are treated as part of the same architectural problem as fat contexts, not as a separate unrelated cleanup effort.",
    "High-level panes consumers that currently read multiple fields can still use grouped selectors where necessary, but only as explicit narrow groupings rather than a recreated `usePanes()`-style aggregate facade."
  ],
  "delta_from_previous_percent": 18.69,
  "structure_warnings": []
}
