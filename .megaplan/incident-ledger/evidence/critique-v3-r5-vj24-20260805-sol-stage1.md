# Sol stage 1: exploration scope

Model: GPT-5.6 Sol, high reasoning. The full prompt/result is retained at `/tmp/sol-stage1-superfixer-20260805.out`; this durable copy records the operative judgement and swarm contract.

## Executive judgement

VJ24 is the immediate deterministic stop, but this is not credibly one worker forgetting a file. The canonical run is stopped/blocked; T18/T23 never dispatched; VJ19 treated the replay selector as a declared T18 output while VJ24 rejected a missing selector; no durable occurrence-bound r5 repair request/dispatch exists; stopped lease/blocked plan contradict stale `should_run=true` and “alive” watchdog projections; the run crossed several rejected runtime/execution bindings; and executor claims do not match observed repository deltas. The incident spans declaration interpretation, evidence admission, occurrence custody, immutable binding, and observation. The swarm must distinguish existing-contract bypass from missing canonical structure; do not add a second fixer authority by default.

## Ranked hypotheses

1. VJ19 and VJ24 consumed different selector/task-output declarations (different normalization, task scope, plan revision, or runtime). Falsifier: identical normalized maps, hashes, validator source identity, and semantics for both jobs.
2. Execution-evidence policy admits unresolved test-budget, write-set, acknowledgement, and claim/diff defects until a later validator becomes the accidental hard stop. Falsifier: policy records proving every earlier finding was intentionally advisory and could not affect VJ24 inputs.
3. The blocked transition lacks an adopted occurrence-bound repair handoff. Falsifier: an accepted repair occurrence for the exact VJ24 fingerprint with current Run Authority decision, Custody claim/epoch, WBC attempt, and dispatch outcome.
4. Rebinding left one logical run across incompatible executable identities. Falsifier: immutable lineage joining original run, final runtime/source/chain-spec/import identity, and every VJ19/VJ24 artifact.
5. Read-only projections are acting as liveness/notification authority without occurrence/version fencing. Falsifier: fresh accepted snapshot plus effect ledger showing one notification per occurrence/state version and no effects from stale projections.

## Luna questions

1. **VJ24 declaration path.** On the r5 plan inspect `plan_v1.meta.json`, `plan_v2.meta.json`, `plan_v2.md`, `execute_v2_raw.txt`, `verification/validation_VJ19_deferred.json`, `execute_batch_15_output.json`, and batch-15 scope. In the pinned runtime locate the exact VJ24 error string and declaration reader. Report normalized selector→task maps, output paths, hashes, and source revisions for VJ19/VJ24. Decide stale selector vs legitimate missing T18 output vs plan/runtime divergence.
2. **Task evidence integrity.** Inspect all `execution*.json`, every execute-batch scope/result/envelope, worker logs, and repository status/hash observations. Trace batch quality/evidence/authority/budget code. Produce per-task declared writes, observed writes, claims, tests/budgets, acknowledgements, and acceptance. Decide whether only VJ24 needs recovery or history must be quarantined/re-established.
3. **Repair occurrence custody.** Inspect final state/history, chain JSON/log, all r5 `repair-data` requests/decisions/claims/attempts/events/incidents/meta-repair/dispatch records. Trace repair-request/contract, wrapper, watchdog, supervisor, meta-repair, and state classifier code. Decide missing producer, fingerprint/token mismatch, disabled path, or correct authority denial.
4. **Immutable run lineage.** Correlate marker, lease, chain JSON/log, plan metadata, runtime candidates, source/tree, chain-spec, interpreter/import roots across every binding refusal and relaunch. Trace drift emitters. Decide same-occurrence resume, accepted migration/new attempt, or quarantine.
5. **Observer and credential boundary.** Inspect lease/marker, status snapshots, watchdog reports/supervisor, cloud-status errors, provider config and redacted credential capability metadata. Trace status snapshot/CLI/format/host transport. Decide authoritative before/after proof surface and whether observation must run host-side.
6. **Notification custody/dedup.** Inspect scheduler, watchdog notifications, webhook/provider receipts, incident bridge, repair-data event/incident/escalation ledgers, message IDs/idempotency keys. Trace dedupe signature and notification paths. Group by occurrence, blocker fingerprint, accepted state version, target, and message ID. Decide stale-projection suppression vs durable notification-custody repair.
7. **Run Authority/WBC/Custody adherence.** Start from Custody Control Plane NORTHSTAR, M11 conformance/accepted manifests, and Critique Ledger architecture-fit/minimality docs; resolve installed APIs/stores and trace every writer from blocked detection through notification. Decide whether sidecars are adapters/projections, an existing owner was bypassed, or a canonical operation is missing. No second authority.
8. **Causal timeline.** Join plan history, validation/batch/provider failures, binding refusals, marker/lease updates, watchdog observations, repair records, and notifications by all available IDs. Identify unjoinable records and earliest trustworthy checkpoint. Decide current vs stale occurrences.
9. **Legal recovery routes.** Read state-machine transition/retry/resume policies and supported commands for `blocked`, `phase=execute`, `retry_strategy=repair_validation_failure` without invoking them. List authority/custody/binding/evidence/idempotency preconditions and preserved/superseded artifacts. Decide bounded stage-2 recovery alternatives.

## Required Luna report contract

Every report must have question ID, one-sentence verdict, `supported|refuted|undetermined`, scope/vantage and UTC observation times, absolute artifact inventory with existence/absence/type/size/mtime/SHA-256/embedded IDs, all available session/plan/run/revision/occurrence/attempt/task/validation/request/claim/lease/fence/runtime/source/notification identities, exact read-only commands plus cwd/exit/stdout/stderr/time, normalized event timeline separating event time/mtime/observation/inference, exact fields/raw excerpts, bounded negative searches, freshness/contradiction analysis, code producer→consumer→persistence→policy trace, strongest alternative and discriminator, confidence, explicit `adherence vs missing structure` classification against a named contract/manifest, and the stage-2 decision informed. Never expose secrets; preserve credential type/scope/resolver evidence. No mutation/dispatch/relaunch.

## Immediate safety constraints

Before stage 2 do not fabricate the replay test or any result/claim/receipt/hash; edit/clear plan/chain/marker/lease/repair/audit state; use `--fresh`, force-proceed, manual completion, hand-advance, or unrecorded retry; relaunch/rebind/refresh/restart/kill; treat marker/PID/tmux/heartbeat/ack as liveness; treat VJ19 deferral as a pass; trust worker prose without accepted envelopes and content evidence; copy credentials; emit notifications; introduce a second authority; or collapse binding drift into “just resume.”

A valid recovery proof needs authoritative before-state for the exact occurrence/cursor/fingerprint/lease/fence/plan-chain-source-runtime identities; current Run Authority decision plus Custody epoch and WBC attempt/effect history; occurrence-bound ordinary request/claim/attempt/outcome (or proof of another canonical owner); authoritative selector contract; accepted envelopes/write-set/test-budget/delta evidence and a real VJ24 result; reviewed source/runtime/test identities and lineage; fresh after-state proving the original cursor advanced; ordinary authorized path; notification effect dedup; and separation of any new blocker.

## Sol-only judgements after the swarm

Sol must decide whether the selector is legitimate/stale/divergent; scope of history quarantine; same occurrence vs migration/new attempt/quarantine; canonical repair owner and fate of legacy sidecars; adequacy of final runtime cutover; authoritative observation transport and credential boundary; notification suppression/supersession/emission; placement in existing Run Authority/WBC/Custody/validator/projection adapters; immediate recovery sequence/authority envelope/proof threshold; and whether a later blocker is distinct. Individual Luna reports cannot make these calls.
