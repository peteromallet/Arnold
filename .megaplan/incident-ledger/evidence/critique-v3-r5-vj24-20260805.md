# Evidence pack: critique-ledger v3 r5 stop at VJ24

Captured 2026-08-05 from the canonical Hetzner agentbox (`root@159.69.51.216`, container `megaplan-cloud-agent-critique-ledger-v3`). This is an analysis artifact only; no remote state was mutated while gathering it.

## Scope and identity

- Session: `critique-ledger-accountability-v3-r5-20260803`
- Workspace: `/workspace/critique-ledger-accountability-v3-r5-20260803/Arnold`
- Chain spec: `/workspace/critique-ledger-accountability-v3-r5-20260803/Arnold/.megaplan/initiatives/critique-ledger/chain.yaml`
- Plan: `cl2-wbc-backed-ledger-20260803-1357`
- Plan directory: `/workspace/critique-ledger-accountability-v3-r5-20260803/Arnold/.megaplan/plans/cl2-wbc-backed-ledger-20260803-1357`
- Chain state: `/workspace/critique-ledger-accountability-v3-r5-20260803/Arnold/.megaplan/plans/.chains/chain-a5c760402ea2.json`
- Remote source head at launch: `c116f38cc83de11a1a508eff6153205504d1ba5a`
- Pinned runtime source: `/workspace/runtime-candidates/arnold-wbc-full-20260804`
- Pinned runtime content SHA: `d0fa249a1310cd42920d345e6f664807318bd6fffbe699f1e0f3208563e92c7d`
- Relaunch was `resume_existing=true`, with an explicit `chain start --no-git-refresh --no-push` command.

## Ground truth at capture

- `state.json` current state: `blocked`
- `state.json` active step: `null`
- Resume cursor: `phase=execute`, `retry_strategy=repair_validation_failure`
- History length: 32
- Last history entry: `2026-08-04T20:30:48Z`, execute error, `validation job VJ24 references missing selectors that are not declared task outputs`.
- The exact raw output is `execute_v2_raw.txt` in the plan directory and contains only: `validation job VJ24 references missing selectors that are not declared task outputs`.
- There is no `verification/validation_VJ24*.json` artifact. The newest verification artifact is `verification/validation_VJ19_deferred.json`.
- `execute_batch_15_output.json` has no output, commands, or result envelopes. Its batch scope is tasks `T18`, `T23` and sense checks `SC18`, `SC23`; both task updates remain `pending`.
- The referenced selector, `tests/arnold/critique_ledger/test_replay_v2.py`, is listed by `plan_v1.meta.json`, `plan_v2.meta.json`, and the Step 15 `Files:` declaration in `plan_v2.md`, but the file does not exist in the remote r5 workspace.
- `validation_VJ19_deferred.json` reports `status=deferred_task_output`, `task_id=T18`, `missing_selectors=[tests/arnold/critique_ledger/test_replay_v2.py]`, `reason=selector_is_declared_task_output`, with a content evidence hash. This is not a passing test result; it is a deferred output/selector record.

## Prior failures in the same run

The plan history records a sequence of different contract failures, not one transient model miss:

1. `2026-08-04T10:24:44Z` and `10:24:54Z`: VJ2 exited `None` (expected `[0]`).
2. `2026-08-04T11:27:18Z`: a long persistent execute attempt blocked after model work. It used GLM 5.2 for batches 2–5.
3. `2026-08-04T15:21:17Z`: VJ8 exited `1`.
4. `2026-08-04T16:42:29Z` and `16:44:34Z`: DeepSeek v4 pro could not run because `DEEPSEEK_API_KEY` was missing.
5. `2026-08-04T16:59:49Z`: VJ9 exited `1`.
6. `2026-08-04T20:30:48Z`: VJ24 rejected a missing task-output selector.

## Execution/audit evidence

- `execution.json` and `execution_audit.json` state that every task was still pending after execute for the relevant tail; the executor never started T18/T23.
- `execute_batches/batch_15/tasks_35a34c851b8f.json` contains only `batch_scope`, `dispatch_identity`, and an empty `result_envelopes` list. No worker result was accepted.
- Earlier batches repeatedly report task-harness violations (`task_test_budget_exhausted`, missing admitted timeout wrapper, and undeclared write-set paths).
- Earlier batches also report executor claims that are not present in git status/content-hash deltas, plus git changes not claimed by any task. This is a task attribution/worktree observation mismatch, not merely a test failure.
- Many sense checks (including SC18 and SC23) have no executor acknowledgment. The audit labels these advisory, so the run can progress while its evidence is incomplete.
- `execution_audit.json` shows a broad diff/claim mismatch: source and test files claimed by workers are absent from observed status, while unrelated `arnold_pipelines/megaplan/*` files are changed but unclaimed.

## Runtime, chain, and observer evidence

- The liveness lease is stopped: `/workspace/.megaplan/cloud-sessions/critique-ledger-accountability-v3-r5-20260803.liveness-lease.json` has `status=stopped`, target PID `610293`, runner fence `11`; no chain process or tmux server remains.
- The primary session marker still says `should_run=true`, but its `updated_at` is `2026-08-04T19:35:41Z` and it has no current status/error. It is stale as an observation surface.
- There is no r5 repair-data request under `/workspace/.megaplan/cloud-sessions/repair-data/`, and the r5 dispatch file is absent/empty. No durable fixer dispatch was recorded after VJ24.
- The r5-scoped watchdog report at `/workspace/r5-watchdog-scope-c3b0be1398/watchdog-report.json` (2026-08-03T15:40:52Z) says `alive_sessions=0`, `issue_count=1`, `status=repair_unavailable`, with message: `deterministic phase-contract failure requires a claimed repair request before relaunch`.
- The generic `/workspace/watchdog-report.json` is older (2026-08-03T17:52:40Z), reports the session `alive`, and has `codex_repair_enabled=false`, `push_repairs_enabled=false`. It is stale and contradicted by the stopped lease/current plan state.
- The in-workspace watchdog supervisor outcome is `status=stalled`, `final_state=planned`, `iterations=7`, reason `stalled at planned for 5 iterations — manual intervention required`. Its events include invalid critique/schema and `ModuleNotFoundError: No module named 'megaplan'` reports.
- The canonical in-container cloud status path failed: snapshot unavailable, then legacy provider listing could not authenticate to the host from inside the container (`Permission denied (publickey,password)`). This means the observer could not establish a fresh canonical snapshot from the box.

## Runtime/chain binding drift evidence

The r5 chain log (`.megaplan/cloud-chain-critique-ledger-accountability-v3-r5-20260803.log`) records several refusal-to-resume events:

- `chain_runtime_binding_drift`: expected `e5de49a5ead7`, active `117b71d9caf9`.
- `chain_execution_binding_drift`: `drift_fields=['chain_spec_sha256']`, active error `editable_runtime_import_root_mismatch`.
- `chain_runtime_binding_drift`: expected `117b71d9caf9`, active `cb6afb801753`.
- `chain_runtime_binding_drift`: expected `d0fa249a1310`, active `bf86f59d7417`.

The later marker records a content-addressed runtime cutover and a successful launch verification, but it also shows that the system had to rebind the runtime twice immediately before the run. The drift refusals are therefore part of the causal recovery path, not noise.

## What is established vs unknown

Established:

- The current run is stopped/blocked, not cooking.
- VJ24 is a deterministic validator contract stop before T18/T23 execution.
- The declared replay test selector is absent from the workspace; the plan references it as a Step 15 output.
- The fixer did not produce a durable claimed repair request for this occurrence.
- Observation surfaces disagreed/staled, and runtime/chain bindings drifted during relaunch.
- The run’s execution evidence is not trustworthy enough to infer success from model prose or claimed files.

Still to determine:

- Whether the missing replay test is an unimplemented task output, a stale selector in the plan/task contract, or a plan/worktree revision mismatch.
- Why VJ24 classifies the selector as “not declared task outputs” while plan metadata lists it; identify the exact validator and declaration source.
- Whether task write-set/test-budget violations are intended hard stops or advisory paths, and why the executor can emit blocked claims without a durable repair envelope.
- Which component owns the authoritative occurrence-bound repair request and why the watchdog cannot create/claim it.
- Whether the runtime rebind path should have invalidated the old chain binding or generated a new immutable run revision.
- How to make `cloud status` and the observer work from the canonical host/provider path without nested SSH credentials.

## Initial hypotheses (not final judgements)

1. Immediate failure: a task-output/validation contract mismatch around T18/Step 15, with a missing file and no accepted batch-15 worker result.
2. Deeper execution problem: plan/task declarations, worker write-set attribution, and validator selectors do not share one machine-readable contract; warnings are allowed to accumulate until a late validator stop.
3. Deeper custody problem: relaunch/runtime bindings and chain identity are mutable across recovery, causing repeated drift refusals and weakening resume/fixer determinism.
4. Deeper control-plane problem: the watchdog can report “alive” from stale markers, but has no durable occurrence-bound repair request when a deterministic phase-contract failure occurs; the supervisor itself can stall.
5. UX problem: notifications repeat the same stale failure because there is no deduplicated incident identity/notification lease tied to a state transition.

## Required guardrail

Do not fabricate `test_replay_v2.py`, clear `state.json`, use `--fresh`, or relaunch until the Sol synthesis identifies the authoritative contract and an occurrence-bound repair action. Any eventual resume must use the canonical state machine, preserve the failed occurrence, and prove fresh authoritative liveness and progress.
