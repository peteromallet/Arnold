I have all the evidence needed. Here is the analysis:

---

## Failure-Shape Probe: `custody-control-plane-20260714 / m5-run-authority-receipt-20260714-1428`

### Canonical Cloud Status
- **Initiative:** `runauthority-epic` | **Session:** `runauthority-epic-cloud` | **Chain spec SHA-256:** `f87b408c14`
- **Plan state:** `blocked` | **Iteration:** 2 | **Current phase:** `review` (retry_strategy: `manual_review`)
- **Path:** `/workspace/custody-control-plane-20260714/Arnold/.megaplan/plans/m5-run-authority-receipt-20260714-1428/`

### Exact Counts: Repeated Same Error+State+Command

**Two repair-queue requests** exist for the same session, both `phase_callback_failed`, both `accepted`/`queued`:

| # | Request ID | Created | Phase Failed | Problem Signature Key |
|---|-----------|---------|--------------|----------------------|
| 1 | `d321f531...` | 2026-07-14T16:33:34Z | **execute** | `b48063b283...` |
| 2 | `1a922515...` | 2026-07-14T17:20:13Z | **review** | `99c1c20509...` |

Both share identical Discord delegation (`correlation_id: discord-corr-c5065afafd0848068626d1dc`, `root_run_id: subagent-20260714-134351-cb9255ac`). These are **distinct phase failures** (not repeated identical errors) — each hit a different phase boundary.

### Review Rework Budget vs. Repair Retry Budget — Distinguished

| Budget Type | Limit | Consumed | Exhausted? |
|-------------|-------|----------|------------|
| **Review rework cycles** | 2 | 2 | **Yes** — `review → needs_rework` (16:42) → re-execute → `review → needs_rework` (17:20) → `review → blocked` (18:04) |
| **Repair request retry budget** | 3 (`retry_budget`) | 2 repair requests filed, both accepted | **No** — budget not consumed for repair retries |

**Source:** `phase_result.json` line 12: *"Max review rework cycles (2) reached with 11 unresolved blocker(s)"*. `state.json` line 1253: *"review rework budget exhausted with unresolved quality blockers"*.

### Circuit Breaker Analysis: Timing & Validity

**Breaker fired:** 2026-07-14T18:04:22Z, `exit_kind: blocked_by_quality`, after 2nd review returned `needs_rework`. **Timing is correct** — the breaker tripped exactly at the rework budget ceiling, not early. The 11 blockers are deterministic and substantive:
- 3 × `accepted=false` milestone receipts (M1/M2/M3)
- Chain verification `divergence_count=3` (landed_diff: 460→62→3 unclaimed files across milestones)
- Collection errors in all 3 green-suites (7 baseline, all pre-existing)
- Stale M5 completion manifest digest
- Retirement/final-attestation blocked on prerequisite gates

**Recovery command validity:** Both repair requests were `accepted`/`queued` — the recovery pathway itself is valid. The issue is that the *executed rework* (batches T13–T21) produced artifacts that still fail review: `.retired` was created by T20 before gate passage (later removed), and `final-attestation.json` generated by T21 records `retirement_status=blocked` with 4 `unresolved_evidence` entries. The repair was correctly queued but produced non-convergent output.

### Firm Verdict

**DETERMINISTIC SPINNING/BUDGET FAILURE: CONFIRMED — REVIEW REWORK BUDGET EXHAUSTION, NOT REPAIR RETRY EXHAUSTION.** The circuit breaker fired correctly at the 2-cycle rework limit with 11 structural blockers. Recovery was validly queued twice but could not converge because the fundamental problem — cross-checkout drift between the cloud session's editable install (`arnold-custody-delivery-20260714`) and the local custody-control-plane checkout — means `compute_verdict()` subprocesses always see collection errors from the editable copy, regardless of local structural fixes. The breaker is therefore **timely and correct**; recovery commands were **validly accepted** but **structurally incapable of producing accepted receipts** under current runtime topology. Manual resolution is required.