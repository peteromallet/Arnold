Act as the sole synthesis/delivery owner for the user's request. Find and preserve the full architecture document produced by resident run subagent-20260716-123608-99c7fe9b, recorded at /workspace/arnold-consolidation-20260714/.megaplan/initiatives/session-knowledge-compiler/research/epic-wide-managed-agent-capture-architecture-20260716.md with expected SHA-256 a5e9a541101e210889e03df4f2d574b1c30a3fc2884a663bdc548b7dd8510553. Treat that prior run result as raw evidence, not current truth.

Integrate its complete, still-valid substance into the closest canonical artifact: the existing Durable Session Knowledge Compiler initiative under .megaplan/initiatives/session-knowledge-compiler/. Do not create a duplicate initiative or a document under .megaplan/briefs. Ensure README.md remains the current-truth front door and indexes the curated research/decision artifact. Reconcile the existing tightened three-sprint README, briefs/m1.md, briefs/m2.md, briefs/m3.md, and chain.yaml so the epic/initiative durably captures all relevant execution kinds without silently dropping scope: resident-managed agents; Megaplan phase/step workers; implementation and review agents; retries and rework; nested subagents; repair/meta-repair/watchdog-derived lifecycle events where authoritative; chain transitions; Hermes, Codex, Claude, and future backends; and higher-level workflows layered above Megaplan.

The durable design must retain stable workflow -> epic/chain -> sprint/milestone -> plan -> phase/step -> attempt -> agent-run lineage, parent/causal links, occurrence/capture/ordering time semantics, authority/custody distinctions, append-only observations, idempotent adapters, correctable projections from run through cross-epic reviewed knowledge, and a read-only reconciler. Logs and agent prose must remain evidence rather than authority. Keep execution authority, scheduling, acceptance, custody, and delivery outside the compiler. Preserve privacy, bounded context, schema evolution, late/concurrent/crash behavior, and a non-mutating real-epic proving strategy.

First inspect initiative, related documents, tickets, git status, the project checkout, and any pinned clean resident/runtime checkout. Verify the source document hash and compare it to current truth. Use an isolated worktree/feature branch based on the verified unambiguous target revision; preserve all concurrent dirty work. If the target is unambiguous, commit and locally integrate using the repository's non-destructive method after revalidating the target ref. Never infer literal main merely because no target was named. If multiple plausible writable targets remain, do not integrate; leave a clean isolated commit and report the exact approval gate. Do not push, launch/resume a chain, deploy, or restart anything.

Verify proportionally: review the exact diff; validate YAML/schema and all initiative-relative links; run relevant initiative/resident tests where applicable; run git diff --check; record source hash, base revision, target revision, commit SHA, clean isolated worktree, and ancestry evidence if locally integrated. Deliver a concise evidence-led Discord reply to the originating user only after durable terminal evidence exists, clearly separating documentation integration from implementation status.
