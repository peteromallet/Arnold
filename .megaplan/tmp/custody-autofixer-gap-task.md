Act as the internal implementation contributor for the custody-control-plane superfixer incident. The original session is `custody-control-plane-20260714`; its current plan is `m5a-atomic-fail-closed-20260715-0149`, canonically blocked with `needs_rework`, and no repair agent is active.

Diagnose why the automatic fixer did not deploy/dispatch and push the blocked plan forward. Follow the superfixer-debug chain of custody and inspect all six evidence layers, starting by proving the observation path works. Identify (1) the first failing fixer layer/axis among TRACKED, FIXED, INTENT, CONTEXT and (2) the backstop above it that failed to catch the problem. Separate evidence, inference, and missing telemetry.

Then implement and verify the durable source/runtime correction for that failure class, including the relevant higher-layer backstop and regression tests. Do not hand-advance or directly patch the custody plan, weaken acceptance guards, or count process liveness/exit codes/agent prose as recovery. Preserve concurrent dirty work. For git-backed changes, discover the pinned editable resident/runtime checkout and target branch, use an isolated worktree and feature branch, revalidate the target ref, commit, and locally integrate only when unambiguous. Do not push remote branches or deploy production broadly. You are authorized to activate the corrected fixer components in the current AgentBox execution environment through supported scoped operations if required for the second owner to exercise them; record exact installed revision, operation receipt, health, and outcome probe. Never use broad kill commands.

Required evidence: real process/marker/chain/plan/log/external-state correlation; root cause and missed-backstop explanation; reviewed diff; focused and proportional tests; commit SHA, base/target refs, clean isolated worktree, target ancestry if locally integrated; and any runtime activation receipt/revision. Write a concise durable result that the synthesis owner can consume. Do not produce the user-facing completion.
