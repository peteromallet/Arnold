/goal
Act as the only implementation/recovery agent for target "custody-control-plane-20260714 / m6-exact-contract-and-20260716-1303".

Diagnose the failed fixer and the backstop that missed it; implement and verify
the fixer repair; use the supported resident/cloud transport; retrigger ordinary
repair; and prove the actual epic or session advances beyond its frozen baseline.

Operator contract:
- Use $superfixer-debug fully and $megaplan-cloud when this is a cloud target.
- Launch no agents or subagents. You are the one mutation owner and synthesis/delivery owner for this request.
- Resolve canonical target IDs, the blocker occurrence, all custody sources,
  pinned source/runtime/installed identities, and effect authority from raw
  evidence. The target text is orientation, not proof.
- At launch, reconcile these observations rather than trusting them: Custody M6 was displayed as `critiqued`, execution inactive, iteration 7, with a dead/stale Hermes gate worker whose last durable activity was 2026-07-17T16:41:05Z; automatic L2 retry `managed-automatic-repair-retry-0be239a0813ce722608b` failed after read-only investigations `managed-automatic-research-subagent-41e06c61a5e257b2ceb3` and `managed-automatic-research-subagent-192acfb26ac4f4eb794c` completed. Inspect their raw artifacts and the exact repair request/attempt records.
- Walk TRACKED, FIXED, INTENT, and CONTEXT. Name the first failed fixer layer and
  the higher backstop that failed to catch it. Hunt bounded sibling instances.
- Preserve live productive work. Never weaken guards, edit chain state by hand,
  or accept a process, return code, commit, self-report, or heartbeat as recovery.
- For source changes, use a clean isolated worktree from the verified pinned
  target; preserve dirty checkouts; add regressions; review the diff; commit;
  revalidate lineage; and integrate only within inherited authority.
- Do not push, deploy, restart, use broad process control, or expand authority
  unless the immutable invocation envelope explicitly authorizes that effect.
  If a required effect is unauthorized, retain verified work and state the gate.
- Retrigger the ordinary fixer through its supported command/request seam and
  verify its exact claim, attempt, evidence, and action. The meta-fixer must not
  substitute for ordinary recovery.
- The user wants Custody M6 pushed through the finalized/next stage and kept moving. Reconcile the existing explicit `force-proceed` authorization from the authoritative conversation with the current state and supported override contract; use it only through the ordinary supported control path when evidence-based judgment and inherited authority permit. Do not equate override acknowledgement with movement.
- Prove the original blocker occurrence cleared and the canonical Custody cursor advanced beyond the frozen baseline. Continue through ordinary failures until the plan visibly moves into the next durable stage or a real approval/authority gate blocks it. Then prove L2/L3 would catch recurrence. A distinct new blocker may be reported only after original recovery is demonstrated.
- Persist raw run/request/attempt IDs, tests, reviewed diff, base/commit/target
  SHAs, clean worktree, ancestry, installed applicability, retrigger receipt,
  and before/after state. Separate evidence, inference, and unknowns.
- Deliver one concise evidence-backed completion message to the originating Discord request only after durable movement is proven, or report the exact real gate if one prevents it.

Continue through ordinary failures until every terminal gate passes or a real
target-lineage, external-authority, or human-approval gate prevents progress.
