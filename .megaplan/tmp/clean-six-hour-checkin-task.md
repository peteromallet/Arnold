Own and complete the user request: improve the automatic six-hour VP special-request check-in, clean up its stale/outdated reporting, and mark genuinely completed retained todo tasks complete.

Operate evidence-first and use only supported resident/CLI seams for durable state. Apply the progress-auditor-debug gather-then-judge method and the configured Hetzner/cloud context where relevant. Start by reading the full retained todo list (not the preview), then locate the exact scheduler/auditor implementation and latest audit evidence. For each retained todo, reconcile its conditions against canonical chain, git, runtime/editable-install, agent, and conversation provenance evidence. Mark an item complete only when durable evidence actually satisfies its condition; retain pending/failed status where proof is absent, explaining the exact missing proof. Do not fabricate provenance or infer completion from prose.

Fix the six-hour check-in so completed or superseded work is deterministically reconciled and stale retained items do not produce confusing current-sounding reports. The check-in must clearly label scheduled audit scope and distinguish current blockers from historical bookkeeping. Treat each miss as a deterministic gather/custody reason when appropriate, not merely a prompt wording issue. Add regression tests reproducing this stale-report/todo-reconciliation failure. Preserve report-only safety: the auditor must not silently launch execution or mutate audited workspaces beyond the explicitly authorized todo reconciliation mechanism.

This is authorized implementation. Use an isolated worktree and feature branch based on the verified intended resident-source target (prefer the pinned clean attached runtime branch if applicable), preserve dirty concurrent work, inspect both project and pinned runtime source, run focused and proportional tests, review the diff, commit, revalidate the target ref, and locally integrate non-destructively when unambiguous. Do not push, deploy, or restart the Discord resident. If installed scheduler/runtime behavior can be refreshed safely without a restart only under an established supported operation, do so and capture a receipt; otherwise state what remains installed versus source-fixed.

Before delivery, provide durable evidence: exact todo transitions and their evidence, tests/checks, reviewed diff summary, commit SHA, base and target revisions, clean isolated worktree, target ancestry if locally integrated, and an outcome probe showing the resulting next-check-in behavior or deterministic fixture. Deliver one concise user-facing completion to the originating Discord request. You are the sole synthesis/delivery owner for this logical request.
