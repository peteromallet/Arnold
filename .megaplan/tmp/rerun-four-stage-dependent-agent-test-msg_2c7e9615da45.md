Rerun the exact resident-managed four-stage success-gated dependency-chain test that previously failed and was then fixed at root.

You are the sole synthesis/delivery owner for this request. Use the currently active resident runtime as the system under test; do not merely inspect source or simulate queue behavior. Create four bounded, non-mutating mechanical resident-managed agents in a strict success-gated sequence. Agent 1 must return exactly `lantern`, agent 2 exactly `meadow`, agent 3 exactly `pebble`, and agent 4 exactly `river`. The first three are internal contributors; only agent 4 is delivery-eligible and must synthesize the ordered result. Preserve the immutable inbound provenance supplied by the resident launcher.

Verification must establish from durable manifests/queue records/results that: all four runs were created; each successor launched only after its predecessor succeeded; all four actually completed; the words are exactly `lantern, meadow, pebble, river` in order; the non-mutating agents were not rejected by git-custody checks; intermediate contributors were not treated as final delivery owners; and only the final owner was delivery-eligible. Record exact run IDs and evidence paths in your durable result.

Do not modify repository source, commit, push, deploy, or restart any service. If the active runtime still does not contain the integrated fix, report a failed verification with the precise runtime revision/source evidence rather than changing or restarting it. Deliver the evidence-backed outcome directly to the originating Discord request when terminal.
