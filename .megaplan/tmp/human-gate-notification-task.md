Act as the synthesis/delivery owner for one logical request with two workstreams. First read and consolidate the predecessor recovery owner's durable result. Then implement and activate a durable control-plane behavior so future unattended Megaplan sessions entering human-required states such as `awaiting_human_verify` cause a resident-managed agent turn to send the originating user a concise message containing the exact gate, why it needs a human, evidence, and the narrow action/decision required.

Scope and outcome:
- Search existing canonical initiatives, tickets, documents, resident scheduling/delivery code, watchdog/repair/meta-repair/progress-auditor code, and tests before creating anything. Reuse the closest existing mechanism/artifact; the user believes one may already exist.
- Diagnose this incident end-to-end: exact state writer, watchdog/status classification, repair dispatch, managed-agent launch/delivery, and why no user-visible agent turn occurred. Treat `awaiting_human_verify` distinctly from generic `blocked`; hunt sibling human-gate tokens and prevent silent token drift.
- Implement the smallest structural fix that launches or durably queues exactly one notification agent per stable human gate, with idempotency/deduplication, immutable Discord provenance, bounded retry, and no notification spam. The notification agent must not auto-approve or weaken a gate.
- Ensure failures to launch/deliver remain visible to the higher backstop/auditor. Add regression tests covering this incident token plus sibling human-gate states and repeat observations.
- For git-backed mutation, inspect both `/workspace/arnold` and the pinned resident runtime checkout. Use an isolated worktree and feature branch based on the verified resident-source target, preserve dirty work, run proportional focused tests, review the diff, commit, revalidate target lineage, and locally integrate when unambiguous. Do not push remotely.
- If activation requires refreshing the pinned editable install or restarting the Discord resident, reconcile the installed revision and use only the canonical supported lifecycle operation. A restart is authorized only insofar as required to activate this requested notification behavior; record the operation receipt, service health, installed revision, and an outcome probe. Never use process-wide kill shortcuts.
- Re-trigger or safely simulate the exact gate so there is durable proof that a resident-managed notification agent turn is created and delivery is routed to the correct originating user; avoid sending duplicate/noisy test messages to the user.
- Update the closest canonical initiative/ticket/document only if needed, citing raw run evidence. Do not create planning docs under `.megaplan/briefs`.

Deliver exactly one concise user-facing completion for both workstreams. Do not claim recovery, integration, activation, restart, or notification without returned durable evidence. Include recovery state evidence, notification-agent run/delivery evidence, tests, commit/base/target/ancestry, clean worktree, installed-runtime/restart evidence if applicable, and any remaining real approval gate.
