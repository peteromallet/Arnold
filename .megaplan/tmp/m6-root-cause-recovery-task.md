Own the root-cause diagnosis and safe recovery of the live custody-control-plane-20260714 chain, currently at plan m6-exact-contract-and-20260716-1303. This is authorized execution work: diagnose, implement and locally integrate any necessary fixes on the verified target using an isolated worktree, activate them in the execution environment only through supported operations when authorization permits, re-trigger the ordinary repair/recovery path, and verify the original chain genuinely resumes and makes durable forward progress into/through M6. Do not merely hand-advance state or weaken guards.

Trace the full chain of custody: live process/tmux, cloud marker, chain JSON, plan state/events/raw critique outputs, repair request/claim/attempt records, repair-progress projection, watchdog, repair-loop, meta-repair, and progress-auditor. Separate evidence, inference, and missing telemetry. Establish why critique generated duplicate FLAG IDs and empty evidence on all three attempts; whether the producer, parallel aggregation, prompt, parser/normalizer, or contract boundary is at fault; why the accepted deterministic_phase_failure repair request has zero claims and attempts; why canonical recovery is UNKNOWN; and why the higher backstops did not detect or correct that.

Preserve the already-integrated ledger correction e2956cd863 and unrelated dirty work. Inspect both the project checkout and pinned/installed resident runtime before source changes. Revalidate target revision before integration. Use exact supported Megaplan/cloud/resident operations; do not use destructive process commands. No remote push, PR merge, production deployment, or Discord resident restart is authorized unless an existing explicit project policy clearly grants it. If an exact approval gate is genuinely required, stop with the smallest precise gate and evidence.

Success requires durable evidence, not a PID or agent assertion: causal timeline; exact first broken fixer layer and failed backstop; reviewed diff; focused and regression tests; clean isolated worktree; commit SHA, base/target revisions, and target ancestry if code changes; installed-runtime revision reconciliation and supported-operation receipt if runtime activation occurs; a repaired/claimed repair request or other canonical recovery receipt; and an outcome probe showing the original session has an active phase/worker with fresh events or has durably completed M6 and advanced. Also add deterministic regression coverage so malformed parallel critique identities cannot deadlock this way and so an accepted unclaimed repair request/UNKNOWN custody state is caught by the appropriate backstop. Deliver one concise user-facing synthesis explaining why status moved forward/backward and the exact remaining blocker, if any.
