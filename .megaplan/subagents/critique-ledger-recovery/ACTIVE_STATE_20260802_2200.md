# Critique Ledger recovery active state — 2026-08-02 22:00 UTC

## Objective

Relaunch a fresh, bounded Critique Ledger CL2 planning canary on the Hetzner
agentbox, prove it advances through `init -> plan -> critique -> gate ->
finalize` exactly once, stop naturally without recovery/notification workers,
and leave every later production/root-hardening obligation in a pushed,
launch-gated follow-up epic.

## Stable containment already achieved

- Historical session: `critique-ledger-accountability-v2-20260728`.
- Historical plan: `cl2-wbc-backed-ledger-20260731-1411`.
- The old cloud container is preserved but stopped; no workspace, volume,
  container or incident evidence has been deleted.
- Exact container ID:
  `277d2e6dbc149e01b25881350238a7b0ff5de78cc27d8ef52c144dca7c35c5ab`.
- Exact image ID:
  `sha256:de249469ec93ae57eec650b743a08e5a9790dd9612755f2118b6a3ac7149db94`.
- Host bind: `/opt/megaplan-cloud/workspace -> /workspace`, read/write.
- Current lifecycle is `stopped`, exit code 137, OOM-killed false. Collector is
  unavailable by construction.
- Current host workspace capacity is a hard `NO-GO`: free bytes 0; free inodes
  33,138,248; required byte floor 1,073,741,824 plus a 1,048,576-byte durable
  receipt reserve.
- Exact typed live observation:
  `.megaplan/subagents/critique-ledger-recovery/CLOUD/live-containment-capacity-observation-20260802T214853Z.json`,
  SHA-256 `b1c42795d81d5c8f259ccbc21626fbf6e05c093e18c6c52dcf176d6fb133082b`.

## Accepted provider checkpoint

- Worktree:
  `/private/tmp/arnold-critique-recovery-cloud-observation-preflight-20260802`.
- Branch: `fix/critique-recovery-cloud-observation-preflight-20260802`.
- Accepted bounded source commit:
  `26aca6ace7f0af3279ca5b311e6983d4904a4d3a`.
- Tree: `5503c69c36bbd5a404742139d5c93cddad48edf3`.
- Independent review PASS SHA-256:
  `92e17377883c3affbcadb9716e69bb5403deff6ca6f7d2833e55e61fbb3c3028`.
- Live use found a remaining CLI integration gap: lifecycle `running` plus a
  typed unavailable collector could still enter legacy container-backed
  status paths and fail ENOSPC. This is now an explicit hostile test/fix.

## Current sole code mutation lane

Sol high agent `/root/sol_t19_review` owns the provider worktree until it
returns a committed candidate. Its in-progress scope is:

1. strict SSH+idle `zero_recovery_canary` spec;
2. healthserver-only entrypoint;
3. fresh expiring predeploy transaction bound to outer/embedded/final
   container, image, bind and capacity identities;
4. exact host systemd/tmux/process fence with a typed receipt;
5. Docker `restart=no` and zero-recovery environment binding;
6. lifecycle+collector-first status/preflight behavior with no legacy exec;
7. fixed read-only capacity inventory;
8. generic `finite_canary_receipt` gate;
9. self-contained fresh Critique Ledger CL2 canary initiative and deterministic
   single-pass runner;
10. hostile tests and terminal natural-stop verification.

No Sol cloud contact, cleanup, deploy or push is authorized. Safe reclaim will
be chosen only after the fixed read-only inventory. Protected targets are the
workspace, Docker volume, historical container/image and all incident evidence.

## Follow-up epic custody checkpoint

- Worktree:
  `/private/tmp/arnold-critique-recovery-follow-up-epic-20260802`.
- Branch:
  `epic/critique-ledger-post-relaunch-completion-20260802`.
- Pushed clean checkpoint: `e32621cab0ba3d7ee30b68eba4e37a86e150ca14`.
- The chain intentionally fails closed because the future
  `finite_canary_receipt` does not yet exist.
- Logical custody passes: all 55 task IDs, every recovery worktree, evidence
  hashes, provider v1/v2 disposition, T1.2/T1.3 sharing, T3.6 split, T8.3
  ownership and proof-map labels are represented.
- Physical custody audit currently fails until:
  - the exact canonical 55-task plan is tracked;
  - T1.1/T1.2/T1.7 staged, unstaged and untracked bytes are recoverable from
    durable remote refs or tracked packs;
  - ten otherwise-unreachable candidate commits receive durable namespaced
    remote refs or a tracked bundle;
  - the typed live ENOSPC receipt above is tracked and manifest-bound;
  - `T0.3/scoped-prelaunch-capacity-reclaim` is a bounded canary admission
    blocker, while `T0.3/platform-capacity-and-storage-hardening` remains F1;
  - the superseded all-blocking Luna cutline is explicitly indexed as
    superseded by the zero-recovery cut.

Custody audit report:
`.megaplan/subagents/critique-ledger-recovery/INTEGRATION/follow-up-epic-pushed-custody-stability-audit-luna.md`,
SHA-256 `bcd49a43173eec83f75a06fb54c3753d5923b1b90557454f2a17b1a8dd07d42b`.

## Remaining critical path

1. Finish, commit and independently review the exact zero-recovery candidate.
2. Run its supported read-only host capacity inventory.
3. Reclaim only proven regenerable cache/build bytes with a scoped receipt, or
   expand host storage if no safe cache win reaches the byte floor.
4. Re-observe capacity and create a fresh expiring predeploy transaction.
5. Apply and verify the exact host-unit/runtime fence.
6. Deploy the exact zero-recovery image/config to cloud.
7. Sync the fresh CL2 canary inputs and invoke the deterministic runner once.
8. Prove `finalized`, advancement beyond the old `gated` stall, natural stop,
   and absence of resident/watchdog/heartbeat/repair/notification processes.
9. Add exact canary/relaunch receipts and every remaining custody artifact to
   the follow-up epic; audit, commit and push it.
10. Final stable exit: historical v2 preserved/fenced; v3 canary finalized and
    stopped; no autonomous notifier/recovery loop; follow-up epic clean,
    pushed and fail-closed on exact receipts.

## Forbidden shortcuts

- No raw SSH, arbitrary host shell, broad prune, workspace deletion, volume
  deletion, container destruction, stale installed-package invocation,
  fabricated normal-chain completion, or unverified claim that a resident or
  diagnostic exists.
- Do not deploy while `free_bytes=0` or before an independently accepted exact
  candidate and fresh predeploy receipt.

## 2026-08-03 replacement-candidate update

- First zero-recovery implementation pair is preserved but rejected for launch:
  implementation `f22bd35bf4c107f921d90bdc018b4abdc3153124` (tree
  `a789b3c81061f455976eb73686afcde6b0acee3e`) and manifest-only child
  `a1690b5d98908f190f83ceb81cc217cb8f2055e4` (tree
  `7cbfbe0da3f5c98d77a79ff571f0dbc809b727c9`). The lineage itself passed.
- Independent exact-candidate review rejected the pair on eleven launch
  blockers: ambiguous inspect argv; missing advertised-branch-tip equality;
  missing terminal receipt on unhandled errors; non-atomic/symlink-following
  OAuth writes; structurally forgeable completion evidence; no actual
  model-dispatch proof; early CLI denial bypasses; stop skipped after observation
  failure; status trusting arbitrary JSON; ambient environment inheritance; and
  incomplete binding of the four manifest files.
- Sol `/root/sol_t19_review` is again the sole code mutation owner. It is
  building a replacement A2/B2 without rewriting the rejected identities.
  External monotonic/cryptographic independence remains deferred, but structural
  receipt forgery, empty fences, phase/model/subject binding and terminal stop
  are prelaunch blockers.
- No provider/cloud/reclaim/push/deploy/restart action occurred. Historical v2
  remains preserved and stopped; host capacity remains `NO-GO` at zero bytes.
- Follow-up epic worktree now has uncommitted mechanical copies of the canonical
  55-task plan, live capacity receipt, capacity adversarial review and canary
  launch-design review. Do not edit/commit that lane until Sol freezes A2/B2;
  then complete the custody anchor/tag and fresh-clone reconstruction audit.

## 2026-08-03 isolated-mount and custody update

- Rejected immutable candidate pairs now extend through A3
  `89a8f33982be8655f55085b89994083ef345c7eb` / B3
  `9f7e9277a36548cf2a2687c70ca5b8b61d3498ba`. B3 was rejected because missing
  rollout sessions could be mislabelled as observed models, `cloud init --force`
  could overwrite a zero config, and the unsandboxed model could mutate
  source/plan authority during a phase.
- Sol remains sole mutation owner of the A4 WIP. The latest mandatory blocker is
  host mount isolation: the new canary may mount only a fresh, mode-0700 direct
  child of `/opt/megaplan-cloud/workspace` at `/workspace`; the preserved parent,
  sibling workspaces and shared writable caches must not be visible. The
  predecessor is still observed against the historical parent. Exact mount
  identity must be receipt-bound and adversarially tested.
- Follow-up custody commit `538a127334c67f352b0d71b4f7b78bcb59163e64`
  is clean and pushed on
  `epic/critique-ledger-post-relaunch-completion-20260802`. It tracks the full
  55-task source, typed live ENOSPC/stopped-predecessor receipt, capacity and
  launch-design reviews, explicitly supersedes the historical all-blocking
  cutline, and splits T0.3 into bounded bootstrap versus F1 platform hardening.
- Follow-up HEAD advanced cleanly and is pushed at
  `a19d5d663e222ab97481fd7f38d5aa3babe46d50`. It now also gives the platform
  T0.3 subtask its own non-overlapping proof path, records the exact pending
  capacity authority, enumerates the required live isolation identity, and
  manifest-binds the three archive-only dirty snapshot commits.
- Follow-up HEAD is now clean, pushed and independently re-audited PASS at
  `5a3dcf29f4582400f4cc6c01cc52a26acf79f364`. Its current launch authority is
  the composite route
  `.megaplan/initiatives/critique-ledger-post-relaunch-completion/finite-canary-operational-route.json`
  (SHA-256 `fe726e549e85e868c5e7fac591fa2d3eeac56b543b5eeaa057140d740c8332ce`).
  It explicitly supersedes the old root-Codex launch review, makes non-root
  privilege/capability/env/process/resource/mount gates machine-readable, and
  assigns cross-pipeline generalization to F1 and server-attested model identity
  to F2.
- Recoverable Git snapshot commits exist for every paused dirty lane:
  T1.1 `b122d6fc936a119c81883ed3698b8be4d746fa4e`, T1.2
  `e552a159d9d531ae5ec913c6cefff40fecbf07c6`, and T1.7
  `7bb70ac85e96413eaa53c08c5a0976b72493dc20`. They still need a final
  namespaced anchor/tag that also parents the accepted candidate and all
  otherwise-unreachable rejected/evidence commits, followed by push and
  fresh-clone reconstruction.
- No new cloud mutation occurred. The predecessor remains presumed stopped and
  capacity remains NO-GO until a fresh supported observation and the narrowly
  admitted dangling-builder-cache reclaim.

## 2026-08-03 A4 qualification and stable-exit checkpoint

- Sol `/root/sol_t19_review` remains the sole mutation owner of the A4 WIP in
  `/private/tmp/arnold-critique-recovery-cloud-observation-preflight-20260802`.
  The WIP is deliberately uncommitted and not deployable yet.
- The focused zero-recovery suite is green at 43 passed with one expected
  root-only local skip; the modified Python modules compile and `git diff
  --check` passes.
- Present safeguards include the isolated direct-child bind, bounded tmpfs,
  zero-mode port/cache/volume denial, fixed UID/GID 65532, zero model
  capabilities plus NNP, strict phase environment, process cleanup, exact
  precreated output-inode sealing, resource bounds, per-phase privilege
  receipts and the honest `codex_cli_turn_context` evidence label.
- Remaining candidate blockers are exact terminal/workspace/privilege receipt
  validation, outer phase receipt schema updates, final canonical proof/trace
  wording and pins, broader cloud qualification, a built-container four-phase
  smoke, removal of generated `__pycache__`, and immutable A4 then manifest-only
  B4 commits followed by independent exact-commit review.
- Follow-up epic HEAD remains clean, pushed and independently accepted at
  `5a3dcf29f4582400f4cc6c01cc52a26acf79f364`. Its `UNFINISHED_WORK.md`, F1/F2
  root-fix briefs, six completion/deploy/acceptance/closeout/durability
  milestones, custody manifest and composite finite-canary route explicitly
  own all currently deferred work. A fresh read-only stable-exit/follow-up
  coverage audit is in flight as `/root/stable_exit_followup_audit`.
- Final exit remains gated on live evidence: predecessor re-observed stopped and
  fenced; capacity safely reclaimed above the reserve; exact candidate deployed
  in a distinct restart=no container; one four-phase canary finalized and
  stopped; no worker/timer/watchdog/recovery/notifier remains; receipts and
  custody tags pushed; fresh-clone reconstruction passes. No cloud mutation has
  occurred at this checkpoint.

### Independent stable-exit/follow-up audit result

- `/root/stable_exit_followup_audit` returned FAIL for current launch readiness
  but confirmed the pushed follow-up epic captures the deferred engineering
  scope and all 55 canonical task IDs.
- A4/B4 must additionally bind and hash the copied `custody-manifest.json`,
  `UNFINISHED_WORK.md`, `supersession-index.json` and
  `finite-canary-operational-route.json`; enforce exact equality of the sorted
  `NOT_CONSUMED_OPERATIONAL_CANARY` set; carry a
  `privilege_receipts_manifest` as a completion artifact and semantically
  revalidate every phase receipt; and reseal the same child workspace inode
  from runtime `root:65532 0750` to terminal `root:root 0700` in the stop/fence
  receipt. Sol received these as pre-freeze launch blockers.
- After the live canary, update the follow-up custody/unfinished/supersession
  files with exact candidate/tree/image/review/receipt identities, mark only
  stable-canary boundary items complete, explicitly split T1.9 finite-launcher
  consumption from F1 cross-pipeline generalization, add the final custody
  anchor manifest, push signed prelaunch/postcanary tags and one runnable
  integration ref, then prove it from a fresh clone.

### Qualification progress and local image-build disposition

- Focused qualification reached 49 passed / 1 expected non-root local skip
  before the final workspace test; a final focused rerun is pending.
- Initial broad `pytest -q tests/cloud` result was 2445 passed, 1 skipped, 18
  failed in 483.53s. One stale Railway symlink expectation was fixed. The other
  17 were temporary-cwd subprocess imports without the checkout on
  `PYTHONPATH`; `PYTHONPATH=$PWD pytest -q --lf tests/cloud` then passed all 18
  with 8 deselected. A complete final `PYTHONPATH=$PWD pytest -q tests/cloud`
  run is in flight and is required before freeze.
- Docker Desktop 28.0.4 was started to avoid consuming cloud capacity for the
  structural image smoke. Read-only daemon state recovered after one supported
  `docker desktop stop/start`, but both arm64 and corrected
  `--platform linux/amd64` writes failed with BuildKit/overlay storage I/O
  errors (`metadata_v2.db` and apt/dpkg fsync). No local prune, reset, deletion
  or data repair was attempted; Docker Desktop was stopped again.
- The exact built-image deterministic fake-Codex four-phase smoke is still a
  mandatory gate. It moves to after exact-source review and the narrowly
  receipted cloud capacity reclaim but before deployment. A smoke defect must
  mint A5/B5 descendants rather than amend A4/B4.

### Frozen A4/B4 and rejection disposition

- A4 implementation was frozen clean as
  `85449a046daba08642469ba4482d4ba5743ab316`, tree
  `02cf0b09ca54623fcd953f2833d88bdd5bab408e`. B4 manifest-only child was
  frozen as `a63cc09e3e1be0568b96dc84b86e1ac4aeefe733`, tree
  `3d6fb5b4f77364110c26710da43273725dd879b8`; its parent is exact A4 and
  `A4..B4` contains exactly the four canonical manifest files.
- Final qualification at B4: `PYTHONPATH=$PWD pytest -q tests/cloud` = 2468
  passed / 1 expected skip in 489.68s; focused zero-recovery suite = 50 passed /
  1 expected skip; compile, diff-check and strict JSON/YAML/spec admission pass.
- Targeted independent re-audit confirmed the four copied follow-up authority
  blobs are exact hash-bound roles from `5a3dcf29...`, actual per-phase
  privilege receipts are semantically revalidated through a manifest, and the
  same child dev/inode is terminally resealed `root:root 0700`.
- B4 is nevertheless rejected immutable for one semantic prelaunch defect:
  `not_consumed_operational_canary` is derived from all 14 custody artifacts,
  not from the true deferred obligations. It omits 15 unchecked F1/F2
  obligations and falsely labels consumed substrate (including accepted
  provider v2 and, after success, the finite T1.9 launcher) as not consumed.
- Sol is sole mutation owner for A5/B5 descendants. Required fix: typed
  per-item `operational_canary_disposition`, closed-schema exact
  `deferred_obligations`, receipt equality using only explicit NOT_CONSUMED
  items plus that array, and hostile omission/extra/duplicate/status/reordering
  tests. A2 full review of B4 remains in flight to batch any other defects.

### Accepted A5/B5, published custody, and first live reclaim failure

- A5 implementation `de68638fb38fcd1688c3088ec05d469d28ef9105`
  (tree `ad308df698f706df0809fa7da37060874906247a`) and B5
  manifest child `3c4dd1f60456143d08e24a47cc076e9629bb80bd` (tree
  `9b397b781aa5692f501cd6c51e9e4e822e61cbaf`) are immutable, clean and
  independently accepted with only the live built-image/capacity/fence gates
  outstanding. Focused final: 80 pass / 1 expected skip; shared-chain: 456
  pass. Independent reviewer rerun: 94 pass / 1 expected skip.
- Candidate branch is pushed exactly at B5 and draft PR #322 exists. Follow-up
  epic branch advanced cleanly/pushed to
  `867c5a5fd2559aef0dcedf4f91b32726aa8a9254`, draft PR #323; its four
  authority files are byte-identical to B5 and retain two consumed substrates,
  fifteen exact deferred obligations and all 55 source tasks.
- Fresh supported capacity inventory still showed 0 free bytes, 33,138,243
  free inodes, unchanged stopped predecessor ID
  `277d2e6dbc149e01b25881350238a7b0ff5de78cc27d8ef52c144dca7c35c5ab`,
  image `sha256:de249469ec93ae57eec650b743a08e5a9790dd9612755f2118b6a3ac7149db94`,
  historical RW bind, and exactly 3.83 GB dangling builder cache admitted for
  bootstrap reclaim. Dry-run transaction command was exactly `docker builder
  prune -f`; the 389.9 GB stopped-container layer remained out of scope.
- The supported apply failed closed before prune with
  `unit_not_inactive_before_prune:megaplan-watchdog-ensure.timer`; no builder
  cache or other data was deleted. Source inspection found stop + runtime mask
  followed by one immediate strict `ActiveState=inactive` read, with no
  reset-failed or bounded settling. Sol is sole mutation owner for A6/B6 to add
  deterministic failed/deactivating settlement, exact failure evidence and
  no-prune-before-all-eight tests. No further cloud mutation until exact review.

## 2026-08-03 A6/B6 rejection, A7/B7 review, and follow-up custody hardening

- A6 `7f23aae52b44579cacc5562829dfe43362344d04` / B6
  `a6be0e605f0a9834d1822c8e6fc6651f81fadd0d` are immutable rejected launch
  candidates. Bootstrap settlement improved, but the ordinary fence emitter
  omitted parser-required `systemd_jobs`, lacked bounded failed/deactivating
  settlement, and persistent masks followed prune without a crash-safe
  resumable boundary.
- A7 `427a982001c08c1c15f1c0bef7f5f7cd66c67c2c` (tree
  `98779f3806a4e13559fcd79769363d2091a78250`) and manifest-only B7
  `28c31930b4aaea8c0a90869153a8d9a03d9548ab` (tree
  `14c02b3e732b59d5e452ff2b9cb97bcce3cdfbf3`) are frozen. A7's runtime changes
  passed semantic review, but B7 is rejected immutable because an exact
  `git archive` checkout fails four structural-smoke tests: the tests read
  ignored, untracked `.megaplan/schemas/{plan,critique,gate,finalize}.json`
  projections that happened to exist in the development worktree. A8 must
  materialize those projections from tracked canonical sources and pass the
  focused suite from an exact clean archive. A7's accepted design moves marker,
  fence, bootstrap success/failure,
  and reconciliation authority to fixed root:root 0700 symlink-free
  `/var/lib/arnold-zero-recovery`; use dirfd-relative/no-follow O_EXCL writes,
  parent/file/directory fsync and strict identities; install and prove all
  persistent masks before prune; filter exact recovery jobs; publish a
  transaction-independent global containment marker only after settlement;
  and emit typed durable failure and per-transaction intent/receipt evidence.
  Local qualification is 111 focused pass / 1 platform skip, 131 adjacent
  provider pass and 456 shared-chain pass. Complete generated-script execution
  is deliberately not claimed locally and remains an unwaived staged live
  gate.
- Follow-up epic authority is clean and pushed at generation-neutral successor
  `1519ea7a04c35a3b7148f7b19b8f79db6695c832` (tree
  `1b6fdb0555326f310adec88d61172318216603b8`). Custody schema v3 preserves the
  exact fifteen deferred IDs and adds per-item owner, independent acceptance
  gate, evidence reference and required claim. Route v2 separately records the
  current trusted-state, bounded-fence/reclaim, retry/failure, built-image,
  capacity, stable-exit and remote-custody gates as T6.2 prelaunch work rather
  than silently deferring them to F1.
- A7/B7 carried the preceding custody copies, so a docs/contract-only A8 plus
  manifest-only B8 is being prepared to byte-bind follow-up commit
  `1519ea7...`. A8 also must fix the clean-clone schema-generation/integrity
  defect and accept custody v3 exactly; B8 will then repin four manifests. The
  corrected contract
  binds a transaction-independent global containment marker and
  transaction-bound per-attempt intents/receipts; pre-intent failure is
  no-mutation caller evidence, while every post-intent failure requires a
  durable O_EXCL host receipt. No push or cloud
  mutation is authorized until that exact pair passes independent review.
- Stable exit still requires: exact pushed candidate; fresh read-only
  predecessor/capacity observation; dry-run then parsed successful bounded
  bootstrap reclaim; capacity above floor; exact cloud image build; passing
  built-image four-phase offline smoke; fresh predeploy transaction; parsed
  apply/verify fence receipts; exactly one finalized finite canary followed by
  terminal stop; v2 stopped and persistently fenced; all eight units absent or
  inactive+persistently masked; no relevant systemd job/session/process,
  resident, watchdog, fixer, repairer or notifier; exact live evidence copied
  into custody; pushed custody anchor, prelaunch/postcanary tags and runnable
  ref; and independent fresh-clone reconstruction with strict live contract
  validation.
- After live success, replace the eight typed PENDING prelaunch evidence rows,
  add accepted A8/B8 and rejected A6/B6 identities without overwriting history,
  add reclaim/fence/smoke/run/stop/custody receipts, mark only proved stable
  boundary tasks complete, and run `validate_contract.py --require-live`. All
  F1-F8 post-canary obligations remain unfinished until their own independent
  completion gates pass.
