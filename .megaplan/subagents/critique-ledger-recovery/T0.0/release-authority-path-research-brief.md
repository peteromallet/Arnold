# T0.0 minimal containment release — current authority-path research

You are a GPT-5.6 Luna read-only research agent. Work in `/private/tmp/arnold-post-c7-release-recovery` and inspect the wider `/Users/peteromalley/Documents/Arnold` repository, branches, worktrees, tests, evidence, and local logs as needed. Do not edit files, commit, push, deploy, SSH-mutate, or change cloud state.

Find the current canonical, M11-conformant way to install a minimal RA-CONTAIN release on the Hetzner agentbox so the Run Authority owner can issue the exact poisoned-tuple containment decision. The old `megaplan-cloud` skill is zero-authority history: do not recommend `cloud chain --fresh`, tmux, watchdog markers, direct SSH copying, or any legacy launcher as authority.

Answer decisively:

1. What exact Release Authority / generation deployment contracts, CLIs, modules, receipts, and acceptance tests currently exist at commit `6787d6363e8fc0603092913ae877db14f3b9fff8` or later clean lineage?
2. Which surface, if any, can lawfully install the minimal RA-CONTAIN implementation and pin its runtime identity on cloud?
3. What owner/grant inputs are required, what immutable generation/runtime tuple is produced, and where are accepted receipts written?
4. Can a deployment candidate be created and validated locally without mutating cloud? Give exact read-only/local commands.
5. What is the narrowest remaining missing implementation or owner decision if no accepted `GEN-DEPLOY` path exists? Identify exact files/tests to add; do not invent an authority fallback.
6. After installation, what exact owner-side invocation would issue/status/check/terminate the containment decision, and how should an independent verifier attest it through the installed `RUNTIME_PYTHON -P`?
7. Identify stale/loose commits or worktrees that look relevant but are not accepted and explain why they cannot grant authority.

Use file:line, test names, commit IDs, and evidence paths. Separate “code exists” from “accepted deploy authority exists.” Produce a concrete critical-path handoff for the main orchestrator.

Write the report to:

`/Users/peteromalley/Documents/Arnold/.megaplan/subagents/critique-ledger-recovery/T0.0/release-authority-path-research.md`
