# T2.6 deploy-eligible decision — Luna preparation

Verdict: **NO APPROVAL; preparation only.** T2.6 remains incomplete.

## Decision contract

Subject: one independently supplied exact candidate `{commit,tree,parent/base=6787...,source-universe,installed-package/runtime-generation,contract-portfolio,completion-manifest}`. The decision is `CANDIDATE_DEPLOY_ELIGIBLE` or fail-closed rejection; it authorizes only the controlled T3 canary, never epic launch, ticket closure, or arbitrary deployment.

Release Authority signs the canonical decision through its owner-installed interface. Domain owners sign their T1 contract receipts; Run Authority, Custody and WBC sign only their own facts. A separate verifier recomputes evidence and cannot be producer, integrator, signer, deployer, or verdict author. Expected candidate identity must be supplied independently before verification; caller/self-attested HEAD is rejected. Quorum is Release Authority plus every named domain owner and one independent verifier; missing/conflicting signer is UNKNOWN/reject.

## Mandatory zero-blocker join

Bind exact accepted T1.1–T1.10 portfolio; T2.2 offline manifest; T2.3 isolated admit/run/verify; T2.4 installed replay/fault suite; T2.5 every allowed model route; both open ticket blobs and pending production obligations; source universe; zero-bypass scan; proof map; no-debt inventory; package/runtime equivalence; rollback/forward-fix; owner-store/storage readiness; and independent manifest recomputation. Historical, superseded, rejected, narrative, marker, checklist, tag, ancestry-only, caller-authored boolean, or local-production-identity claims cannot satisfy a join.

Freshness binds decision epoch, fence/revision, evidence capture window, candidate tree, runtime/package digests, schemas, routes, owner keys and revocation heads. Any later tree/config/route/schema/key/fence change, revocation, expiry, unresolved effect, new blocker, missing source row, or UNKNOWN invalidates eligibility and requires full recomputation. WBC provider-applied/ack-lost remains `INDETERMINATE`, prohibits redispatch, and blocks approval until owner reconciliation.

## Finite matrix

1. Exact independently expected commit/tree exists, clean, 6787-descended.
2. Complete source universe; no unclassified unique delta.
3. Every T1 owner receipt accepted/current/nonrevoked.
4. T2.2–T2.5 exact same candidate/runtime; all PASS.
5. Both ticket contracts mapped; production-only clauses explicitly pending for T3.
6. Zero bypass/unowned effect/model route; UNKNOWN count zero except explicitly no-redispatch pending, which rejects.
7. No-debt, installed parity, rollback/forward-fix and storage gates pass.
8. Independent verifier recomputes all hashes/joins and hostile substitutions.
9. Signer separation/quorum/key validity/freshness pass.
10. Decision scope/TTL/revoke path names only T3 canary.

Negative tests: forged HEAD/tree/runtime, mixed revisions, omitted/duplicate source, stale/revoked signer, self-signed verifier, missing owner, replayed decision, expired TTL, changed route/schema, historical receipt promotion, UNKNOWN relabeled failed/pass, ticket closure or epic-launch scope. Every case rejects before mutation.

## Now versus later

Now: freeze schema, roles, blocker taxonomy, evidence paths, negative matrix, expected-identity input channel and non-authority semantics. Later only: integrated T1 candidate, T2.2–T2.5 accepted receipts, installed generation, current owner/revocation heads, zero-blocker recomputation, signatures and actual decision.

Current facts prohibit approval: no integrated candidate; T1 hard failures/incomplete lanes; T2.2/T2.3 are preparation only; T2.4/T2.5 not accepted; production Release Authority backend remains unavailable. No mutation was performed; this report is the sole write.

Report SHA-256 is recorded externally.
