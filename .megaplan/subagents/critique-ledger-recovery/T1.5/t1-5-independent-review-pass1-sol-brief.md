# T1.5 canonical simple_fixer — independent GPT-5.6 Sol-high review

Take a definite `PASS` or `HARD FAIL`. Do not implement or repair.

## Frozen candidate

- read-only worktree:
  `/private/tmp/arnold-critique-recovery-simple-fixer-20260802`
- implementation commit:
  `bf6af7db8285e56379ed12ae94af5cdb14f4c1cd`
- implementation tree:
  `ae43595f3c6ec8ea7c48ab8ad95b802f2ddeec98`
- evidence-only head:
  `4bfd5fb2cc0d174297260aee6da7cb9a347f6a6a`
- evidence-head tree:
  `066c22a540ff9983380760088e2daa9113cbb539`
- exact ancestor before implementation:
  `6787d6363e8fc0603092913ae877db14f3b9fff8`
- implementation report inside candidate:
  `.megaplan/subagents/critique-ledger-recovery/T1.5/t1-5-sol-implementation-result.md`

Verify all identities and clean status first. Do not trust the report or green
tests. Review only the frozen T1.5 contract and this finite matrix.

## Finite acceptance matrix

1. One pipeline-neutral `arnold.recovery` owner contract binds the complete F01
   identity, exact RA/custody lease+epoch+fence and WBC GLEK. No label, mapping,
   projection, environment, marker, path or caller assertion can mint authority.
2. Production has one fixed owner-service boundary and no local SQLite/file/
   queue/marker fallback. Owner absence and ambiguity fail closed without effect.
3. Exact occurrence, claim, attempt intent, budget, ambiguity/result/receipt and
   fix-the-fixer obligations are durable, CAS/idempotent, replayable and bounded.
   Response loss and post-effect fence change are non-redispatchable UNKNOWN.
4. Ordinary fixer is a non-agentic leaf. It cannot launch/delegate an agent,
   command, callback, subprocess, scheduler, queue, child or fallback, directly
   or through any legacy wrapper/internal function/import alias.
5. Every old watchdog/repair/meta/Kimi/supervise/heartbeat/manual/progress/six-hour/
   resident/relaunch route is removed or an unconditional typed tombstone/delegate
   to the fixed canonical owner client. Flags, env, direct module execution,
   imported internals, materialized wrappers and hot upload cannot reactivate it.
6. `fix-the-fixer` is a separately authorized transaction with exact occurrence,
   fence, implementation/backstop digests and independent verifier. It is never
   automatically launched as an ordinary fallback. The v2 provenance failure
   creates one quiet obligation and no repair/notification amplification.
7. Immediate trigger and three-hour reconciler race to one claim/effect/result;
   200 observers, repeated timers, restarts, stale leases, corrupt files, ENOSPC,
   forged receipts and identical incident observations cannot multiply effects,
   agents, obligations or notifications.
8. Source, built wheel, installed entrypoints, materialized wrappers/systemd and
   container template are byte/semantic equivalent. Production owner absence is
   tested from installed artifacts, not merely source imports.
9. Neutral contract is reusable by every shipped non-Megaplan pipeline; inventory
   is dynamically complete and no alternative local recovery/effect route exists.
10. Retirement must not manufacture green evidence by deleting assertions or
    broadly skipping live behavior. Audit all 741 skips: each subject must be
    provably unreachable/retired with active replacement coverage. Unreachable
    historical bodies are unacceptable if direct import/internal/module/console
    paths can still execute them. Compare tests and public/internal call graph to
    the exact ancestor; do not equate an early public return with total retirement.

Threat boundary: do not demand resistance to arbitrary in-process interpreter
takeover. Do demand installed runtime-generation provenance and closure of normal
documented/importable/module/wrapper entrypoints.

Reproduce focused/concurrency/crash/retirement/bypass/source-wheel suites and add
read-only hostile probes in disposable scratch. Large suites single-flight. Never
touch cloud/provider/production owner state, git refs, candidate files or checklist.

## Output

Write one report to:
`/Users/peteromalley/Documents/Arnold/.megaplan/subagents/critique-ledger-recovery/T1.5/t1-5-independent-review-pass1-sol-result.md`

Include explicit verdict at top, exact identity re-verification, commands/results,
all ten matrix verdicts, reproducible blockers with file/line refs, limitations,
and report SHA-256 in final response. No formal T1.5 completion claim; local PASS
only makes the frozen candidate eligible for clean-lineage integration and later
owner/deployed evidence.
