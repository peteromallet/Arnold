You are a READ-ONLY DeepSeek V4 Flash investigator. Do not mutate any file or operational state. Do not launch, resume, rebind, restart, retry, fork, notify, enqueue, claim, acknowledge, install, fetch, or run Megaplan lifecycle commands. Use only read-only `rg`, `sed`, `stat`, `sha256sum`, and `git show/log/blame/diff` with `GIT_OPTIONAL_LOCKS=0`. Do not delegate.

Identity: session `critique-ledger-accountability-v3-r7-launch-20260805`; workspace `/workspace/critique-ledger-accountability-v3-r7-launch-20260805/Arnold`; plan `cl2-wbc-backed-ledger-20260805-2140`; runtime `/workspace/runtime-candidates/arnold-r7-fresh-child-20260805` at `234ac3524da29c1f630aee90ab1767a142a68a83`.

Read first: `.megaplan/incident-evidence/critique-r7-superfixer-20260806-145651-4c581f6a/evidence-pack.md` and `flash-report-contract.md` in that directory.

Question fq-01 — exact splitter contract and edge cases. Inspect `arnold_pipelines/megaplan/orchestration/task_splitter.py` (`split_task`, `split_high_complexity_tasks`, all helpers/constants) and `tests/arnold_pipelines/megaplan/test_task_splitter.py` in the pinned runtime. Establish exactly which fields carry task-id references, which split child should replace an original id for incoming downstream edges, how `dependency_reasons` keys/values are structured, and behavior for chains of multiple split tasks, ordering, duplicate deps, self-deps, absent IDs, proof/impl collisions, and already-suffixed IDs. Cite exact lines and distinguish tested from untested behavior.

Decision informed: minimal engine fix scope and mandatory edge-case regressions. Return only the 18-field FLASH REPORT.
