You are a READ-ONLY DeepSeek V4 Flash investigator. Do not mutate any file or operational state. Do not launch, resume, rebind, restart, retry, fork, notify, enqueue, claim, acknowledge, install, fetch, or run Megaplan lifecycle commands. Use only read-only `rg`, `sed`, `stat`, `sha256sum`, and `git show/log/blame/diff` with `GIT_OPTIONAL_LOCKS=0`. Do not delegate.

Identity and required first reads are in `.megaplan/incident-evidence/critique-r7-superfixer-20260806-145651-4c581f6a/evidence-pack.md` and `flash-report-contract.md`; pinned runtime is `/workspace/runtime-candidates/arnold-r7-fresh-child-20260805` at `234ac3524da29c1f630aee90ab1767a142a68a83`.

Question fq-02 — splitter blast radius. Search the entire pinned runtime source and tests for definitions, imports, wrappers, and call sites of `split_task`, `split_high_complexity_tasks`, and `_split_finalize_tasks`. For every production call site, trace input producer, output consumer, persistence/admission policy, and whether callers assume original IDs survive. Identify any public/exported use outside finalize and any path where dependency rewiring could affect semantics.

Decision informed: whether the defect is local to finalize or requires broader compatibility/regression scope. Return only the 18-field FLASH REPORT.
