# FQ-07 — Flash evidence brief

## question_id
FQ-07: Does existing test coverage lock only parent replacement, or also downstream reference closure and finalize admission?

## verdict
**Only parent replacement is locked. Neither downstream reference closure nor finalize admission of a split graph with cross-task dependencies is covered by any existing test.** All splitter and finalize-wiring fixtures are single-task or dependency-free; `dependency_unknown`/`dependency_graph_invalid` are never asserted anywhere in the test tree; `split_high_complexity_tasks` source (task_splitter.py:461–507) provably performs no cross-task rewiring pass, so the incident defect is entirely uncovered. A finalize-pipeline admission test **is required in addition to** a unit test — the existing m8a wiring tests would pass unchanged with the defect present.

## vantage_point
Independent READ-ONLY DeepSeek V4 Flash investigator, bound engine `/workspace/runtime-candidates/arnold-r7-fresh-child-20260805` @ `234ac3524da29c1f630aee90ab1767a142a68a83` (clean; verified `git rev-parse HEAD`). No mutation, no pytest execution, no file writes. Incident context from `evidence-pack.md` §4/§7 (root cause: splitter replaces complexity ≥7 parents with `{id}_impl`+`{id}_proof` without rewriting dependents' `depends_on`; feasibility rejects with 12×`dependency_unknown` + `dependency_graph_invalid` at the finalize boundary).

## utc_interval
2026-08-06T14:40:00Z – 2026-08-06T14:43:17Z (UTC clock read at close: `2026-08-06T14:43:17Z`).

## artifacts (path + sha256)
Inspected (all read-only):
- `tests/arnold_pipelines/megaplan/test_task_splitter.py` — `87231f42761d8fd350d486fd458b6bfaadd40744527e5920e5821093659dc6eb`
- `tests/arnold_pipelines/megaplan/test_task_feasibility.py` — `61a3df666c60da0102905b5758fb7d29a6f139d64626b652c3e888f8e52cab18`
- `tests/arnold_pipelines/megaplan/test_handler_behaviors.py` — `9da772338c2353d90c99e137568864b66963d69f21776db3902a88ac04f45b10`
- `tests/arnold_pipelines/megaplan/test_m8a_finalize_wiring.py` — `be41b8edc50d99d26cb0821da222c6c26e5fb5a612637bbf4dfbf18dc59071be`
- `tests/arnold_pipelines/megaplan/test_graph_admission.py` — `dd4614f4f175ba319489808788b78229142a62c174240252a1ccb96b988e303d`
- `tests/orchestration/test_finalize_authority.py` — `8e1140e2211835248961a6fa060ffab336cc92d121e769efff9acff5950fd9a6`
- `tests/arnold_pipelines/megaplan/test_finalize_one_shot_cli.py` — (call-site inspected; monkeypatches `_write_finalize_artifacts`, sha not collected)
- Source (for gap confirmation): `arnold_pipelines/megaplan/orchestration/task_splitter.py` — `851932dd007b2c9b3c7fa6f7092e08ad718f02a14e7a7c0cc9a98fadd1b3a462`; `arnold_pipelines/megaplan/orchestration/task_feasibility.py` — `c7932ece4baeab8f18a4deaea4331c30744c6adaa136db4586b28127907cd2a7`; `arnold_pipelines/megaplan/handlers/finalize.py` — `13efbdb7937bb73330f5aa4d19abeaf73904bea082d13cb0fbac650522695a4e`
- `…/incident-evidence/critique-r7-superfixer-20260806-142036-1ea42c25/evidence-pack.md` — `9cdeb6cb311c19c1787f053348e9668b39307752cf0ab445854b8ad8fe2b9a8e`

## commands (read-only, cwd, exit codes)
- `git rev-parse HEAD` / `git log --oneline -3 | cat` — cwd `/workspace/runtime-candidates/arnold-r7-fresh-child-20260805` — exit 0 (HEAD `234ac3524…`, clean lineage incl. v4 fix `9c41d0554`).
- `sha256sum <7 test files>` and `sha256sum <3 source files + evidence-pack.md>` — same cwd — exit 0.
- rg-equivalent content scans (search_files; read-only): `def test_|class ` over the 5 target test files; `_write_finalize_artifacts` over `tests/**`; `split_task|split_high_complexity_tasks` over `tests/**`; `dependency_unknown|dependency_graph_invalid|rewire|closure|remap` over repo `*.py`; `depends_on|complexity` over `tests/fixtures/m8a/` — all exit 0.
- `date -u` — exit 0. **pytest was NOT executed** (prohibited).

## raw_excerpts
1. `test_task_splitter.py:554-573` (TestBatchSplitter, the only batch-level split test): tasks T1–T4 constructed via `_task(...)` with **no `depends_on` argument** (helper default `depends_on: list[str] | None = None` → `deps = list(depends_on or [])`), then: `assert "T2" not in ids  # replaced` / `assert "T4" not in ids  # replaced` / `assert len(result) == 6`. **Only parent replacement is asserted; zero cross-task refs exist in the fixture.**
2. `test_m8a_finalize_wiring.py:24-52, 55-95`: `_low_complexity_task` → `"depends_on": []`; `_high_complexity_splittable_task` → `"depends_on": []`. Every `_write_finalize_artifacts` call (10 sites: lines 176, 200, 224, 255, 287, 319, 354, 385, 422, 465) uses `_payload([single_task])`. Admission assertions (`test_feasibility_admitted_hash_in_graph_report` line 366 `assert graph_report["admitted"] is True`; `test_task_feasibility_json_is_written` line 390) run on a **single low-complexity T1 — no split occurs in any admission assertion**.
3. `test_task_feasibility.py:516-541`: `test_m8a_complexity_7_8_9_fixture_split_or_fail_cases` compiles the m8a fixture directly (no splitter) — fixture tasks verified `"depends_on": []` in `tests/fixtures/m8a/strategy-validation.json`. `test_post_finalize_dependency_chain_mutation_prevents_dispatch` (577-591) locks post-finalize dispatch hash-binding, not finalize admission.
4. `test_graph_admission.py:48-82`: 3 tests lock the rejection contract (`rejected_candidate_never_replaces_admitted_graph`, `identical_candidate_failure_opens_bounded_repair_circuit`, `candidate_identity_is_deterministic`) — no splitter, no `_write_finalize_artifacts`.
5. `test_handler_behaviors.py:1913, 2005, 2459, 2496`: all `_write_finalize_artifacts` sites use single low-complexity tasks (`"depends_on": []`); the user-action-gate test creates T2+T1 (T1→T2) only via `_ensure_verification_task` insertion, both low complexity, asserting gate wiring (`payload["tasks"][1]["depends_on"][0] == "T2"`), never split admission.
6. Repo-wide grep: `dependency_unknown` / `dependency_graph_invalid` occur **only in source** `task_feasibility.py:306` and `:331`; no test asserts either code. No test outside `test_task_splitter.py`/`test_m8a_finalize_wiring.py` references `split_task`/`split_high_complexity_tasks`; `test_finalize_one_shot_cli.py:163` monkeypatches `_write_finalize_artifacts` away.
7. Source gap: `task_splitter.py:486-507` — `split_high_complexity_tasks` iterates each task, `output.extend(result)` for splits, `output.append(task)` otherwise; **no pass touches other tasks' `depends_on`/`dependency_reasons`** (only `split_task`'s own-dep handling at lines 322-325, 349).

## trace
1. Located 5 named test files under `tests/arnold_pipelines/megaplan/` (+ `tests/orchestration/test_finalize_authority.py`, `tests/prompts/test_finalize_task_feasibility_prompt.py`, `test_finalize_one_shot_cli.py`); verified engine HEAD and hashes.
2. Read evidence-pack.md → mapped question terms to the defect: parent replacement = split_task/split_high_complexity_tasks id swap; downstream reference closure = rewiring dependents' `depends_on` to impl/proof; finalize admission = `_write_finalize_artifacts` → `compile_task_feasibility` → `admitted`/`task_feasibility.json`.
3. Enumerated all test names in the 5 files; read full bodies of TestBatchSplitter, TestContractPreservation, all TestM8A* classes, graph_admission (whole file), and every `_write_finalize_artifacts` call site incl. handler_behaviors fixtures and TestFinalizeBaselineSelectionRecovery.
4. Ran repo-wide greps (splitter symbols, failure codes, rewiring vocabulary, `_impl`/`_proof` assertion search — all false positives unrelated to splitter rewiring); confirmed m8a fixture deps are empty; confirmed splitter source has no closure pass.
5. No pytest executed; no writes.

## confidence
**High.** Test-surface enumeration is exhaustive for the five named files and all `_write_finalize_artifacts` call sites; fixtures were read in full; repo-wide greps bound the negative (no test asserts `dependency_unknown`/`dependency_graph_invalid`, no splitter test has a cross-task edge). Residual uncertainty: I did not execute the suite (prohibited), so I rely on static reading; also, a small chance some sibling test outside the grepped scope exercises the pipeline — bounded by the `split_*` and `_write_finalize_artifacts` symbol greps which found no other callers.

## structure_classification
Coverage gap — unit-level split-shape tests + pipeline-level wiring tests exist but are **orthogonal to the failure mode**: fixtures never contain a dependent referencing a split parent, and no test asserts admission of a split graph. The regression surface for the defect is zero tests; both closure (unit) and admission (pipeline) invariants are unlocked.

## immediate_decision_informed
Smallest regression surface needed to lock the fix = **two tests**:
1. **Unit (closure):** in `test_task_splitter.py` (TestBatchSplitter), payload with e.g. `T1` (complexity 8, split) and `T2` with `depends_on=["T1"]` (+ `dependency_reasons`), assert post-`split_high_complexity_tasks`: `T2.depends_on` contains `T1_impl` (design decision: impl vs proof mapping per Sol), `dependency_reasons` rewritten consistently, and every `depends_on` id exists in the output task set.
2. **Pipeline (finalize admission) — REQUIRED in addition:** in `test_m8a_finalize_wiring.py`, `_write_finalize_artifacts` on a 2-task payload (low-complexity T2 `depends_on=["T1"]`, high-complexity T1), assert `graph_report["admitted"] is True`, `task_feasibility.json.admitted is True`, and no `dependency_unknown`/`dependency_graph_invalid` diagnostics. This is required because the incident blocker manifests exactly at this boundary (feasibility rejection after split), and existing m8a tests would pass with the defect present — they cannot catch this regression, while a unit test alone cannot prove the full mutation order (split → compile ×2 → write) admits.

## durable_decision_informed
Two invariants should be locked permanently for the finalize pipeline: (a) **reference closure** — after `split_high_complexity_tasks`, every task's `depends_on`/`dependency_reasons` must resolve to ids present in the post-split task set (no dangling parent ids); (b) **finalize admission** — `_write_finalize_artifacts` output must be `admitted: true` for any valid graph containing complexity ≥7 tasks with dependents, and `dependency_unknown`/`dependency_graph_invalid` should be unreachable for splitter-produced graphs. Any future change to splitter id-generation (impl/proof naming), threshold, or feasibility validation must re-run both the unit closure test and the pipeline admission test; a unit-only regression would leave the deterministic finalize blocker (occurrence `occ_critique_r7_superfixer_now_20260806_v5_63a701ce7ce09258e1c322f2`, fingerprint `382e25a2…`) free to recur.