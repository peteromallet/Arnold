All verification complete. Here is the FQ-06 evidence brief.

---

## Flash evidence brief — FQ-06

**question_id:** FQ-06

**verdict:** The four identity fields are all computed from the **fully transformed** candidate graph at the rejection point — never from the raw model candidate. Independent in-memory recomputation (engine's own documented serialization, mutation order replayed from `handlers/finalize.py::_write_finalize_artifacts`) reproduces **byte-for-byte** every persisted value in `planner_repair.json` and the plan-dir admission record. Transformed sense checks **do** perturb `candidate_id`/`candidate_graph_hash` (they are inside the graph digest), but **cannot** perturb `task_contract_hash` or `failure_fingerprint` (sense_checks are excluded from `_stable_contract` and never enter feasibility diagnostics). In this incident sense checks were **not** transformed (19→19); the identity moved from raw `candidate:2c41222e…` to transformed `candidate:36bcefaf…` solely via task-splitting (19→25 tasks) and validation-jobs compilation (0→16 jobs).

**vantage_point:** Independent READ-ONLY investigator; bound engine `/workspace/runtime-candidates/arnold-r7-fresh-child-20260805` @ `234ac3524da29c1f630aee90ab1767a142a68a83` (clean); evidence pack `…/critique-r7-superfixer-20260806-142036-1ea42c25/`. No files written; all recomputation in memory on deep copies.

**utc_interval:** Canonical record timestamps cap at `2026-08-06T14:16:28Z` (planner_repair + admission record `observed_at`). Investigation window: after pack write `2026-08-06T14:31Z`, in-session (≈14:31Z–14:45Z).

**artifacts (path + sha256):**

| Artifact | sha256 |
|---|---|
| `raw/planner_repair.json` (evidence pack) | `31f7c10acb8b999ea53fd4af71ffb2bd30697749eec643654b3c0e54dc7f5087` |
| `raw/local-strict-artifacts/finalize-59daae926dcd4447b40cbb790e68e36b.candidate.json` | `cfc7d701c18c070ba15679d5279cc943119ec9d4fa412b8ef4d02115a75c76c8` |
| `raw/local-strict-artifacts/finalize-15274aaf2cb2445487647129704dcccd.candidate.json` | `cfc7d701c18c070ba15679d5279cc943119ec9d4fa412b8ef4d02115a75c76c8` (byte-identical to 59daae92 — both attempts) |
| `…/plans/cl2-wbc-backed-ledger-20260805-2140/finalize_candidates/36bcefafd10ff23e6af1162c5b7186275630cec534cfd5aa0f257e9a9d69bc07.json` (persisted admission record) | `79b727252945e58989f1631531b4fddf511df0e59b7ed629029d85cef7645bf5` |
| `raw/repro_finalize_mutations.py` | `6bef84dec62c676b4ac2d1928f84dda4db8cf9ecc1d058e9437d80c8de8c22fd` |
| `raw/repro-output.txt` | `d89a46a3dbc7157c1a9f2f0aa9534ac6980fb161eeacf06c8adffec38d4a4c27` |
| engine `…/orchestration/graph_admission.py` | `5cfaaf9395737f2434fdec8c228c59371d733c8d8c35e0f2c7f5b4af2c4b6f41` |
| engine `…/orchestration/task_feasibility.py` | `c7932ece4baeab8f18a4deaea4331c30744c6adaa136db4586b28127907cd2a7` |
| engine `…/handlers/finalize.py` | `13efbdb7937bb73330f5aa4d19abeaf73904bea082d13cb0fbac650522695a4e` |
| engine `…/orchestration/task_splitter.py` | `851932dd007b2c9b3c7fa6f7092e08ad718f02a14e7a7c0cc9a98fadd1b3a462` |

**commands (read-only, cwd = evidence `raw/` unless noted; all exit 0):**
1. `python3 - <<EOF …` (structure scan of both candidate artifacts; JSON read only) — exit 0.
2. `python3 - <<EOF …` (response-finalize receipt stubs → both point to identical local-strict artifacts, sha256 `cfc7d701…`, 77557 bytes) — exit 0.
3. `sha256sum local-strict-artifacts/*.candidate.json && cmp -s … && echo IDENTICAL` — exit 0, **IDENTICAL**.
4. `MEGAPLAN_TRUSTED_CONTAINER=1 PYTHONPATH=$ENGINE python3 - <<EOF …` — full in-memory replay of handler mutations + `candidate_graph_record` vs `planner_repair.json` — exit 0 (recomputed `candidate_id`/`task_contract_hash`/`failure_fingerprint` all **MATCH: True**).
5. Same env — step-by-step identity trace (raw → each mutation) + sense-check sensitivity (append/mutate SC) — exit 0.
6. `sha256sum … finalize_candidates/*.json` + engine file sha256s + `git -C <engine> rev-parse HEAD` (`234ac3524da29c1f630aee90ab1767a142a68a83`) and `git status --porcelain` (clean) — exit 0.
7. Decomposition of `failure_fingerprint` digest input (codes-only signature; manual digest **MATCH: True**) — exit 0.

**raw_excerpts:**
- `planner_repair.json`: `"candidate_id": "candidate:36bcefafd10ff23e6af1162c5b7186275630cec534cfd5aa0f257e9a9d69bc07"`, `"failure_fingerprint": "382e25a279dbb2b7784f56d8a0b59eae9422a0625d48c9bb1185aa3fec4a33df"`, `"occurrences": 2`, `"circuit_open": true`.
- Persisted record `36bcefaf….json`: `candidate_graph_hash = 36bcefaf…`, `task_contract_hash = sha256:ae438ec6368397a331a556539cf3b662aa410adcd4a60ff40134ed1cc3bf1849`, `admitted = False`, `observed_at = 2026-08-06T14:16:28Z`.
- `graph_admission.py:37-42`: `graph = {"task_contract_version", "tasks", "validation_jobs", "sense_checks"}`; `:61` `candidate_id = f"candidate:{_digest(graph)}"`; `:62` `candidate_graph_hash = _digest(graph)`; `:63` `task_contract_hash = report.get("task_contract_hash")`; `:43-57,65` `failure_fingerprint = _digest({"task_contract_hash", "diagnostics": [{code, task_ids, path}]})` when not admitted.
- `task_feasibility.py:60-88` `_stable_contract` = `{task_contract_version, tasks[13 keys: id, objective, description, kind, complexity, estimated_minutes, depends_on, dependency_reasons, routing_group, write_set, narrow_tests, checkpoint], validation_jobs}`; `:91-104` `task_contract_hash = "sha256:" + sha256(_stable_contract)`; docstring: intentionally excludes `seed_epoch`/`source`; **sense_checks absent by construction**.
- `finalize.py:2050-2087`: mutations applied to the in-place payload before the first feasibility pass; `:2080-2086` rejection path calls `record_rejected_candidate(plan_dir, state, payload, feasibility)` with the **mutated** payload.
- `finalize.py:2012-2028` `_split_finalize_tasks` → `task_splitter.py:486-507` swaps complexity≥7 parents for `{id}_impl`/`{id}_proof` **without rewiring dependents** (T1–T5, T7 removed; dangling refs remain).

**trace** (recomputed in memory; all values match persisted):
1. Raw candidate: 19 tasks, 19 SCs, `validation_jobs=[]`, `user_actions=[]` → raw identity: `candidate_graph_hash/candidate_id = 2c41222ecf0b…`, `task_contract_hash = sha256:be10c4d5…`.
2. `_ensure_verification_task` → no change (no re-run-until-pass task matched; suffix not applied). `_ensure_user_actions_*_gate_task` → no-op (`user_actions=[]`; no SC injection at finalize.py:742-754). `_apply_programmatic_coverage` → writes `payload["validation"]` only — **not in either digest**. `_normalize_task_complexity` → no change. Identity still `2c41222e…`.
3. `_split_finalize_tasks` → 25 tasks (T1–T5, T7 → `_impl`/`_proof` pairs): `candidate_graph_hash = c8597cc5…`, `task_contract_hash = sha256:b8378468…`.
4. `compile_validation_jobs` → `validation_jobs = 16`: **final** `candidate_graph_hash/candidate_id = 36bcefaf…`, `task_contract_hash = sha256:ae438ec6…`. Feasibility: `admitted=False`, 13 diagnostics (`dependency_unknown`×12 + `dependency_graph_invalid`×1).
5. `failure_fingerprint = sha256({"task_contract_hash": "sha256:ae438ec6…", "diagnostics": [{code, task_ids:null, path:null}×13]}) = 382e25a2…` — because feasibility diagnostics carry `task_id`/`dependency_id` (not `task_ids`/`path`), the signature degenerates to **ordered codes only**.
6. All four fields match `planner_repair.json` and the persisted record exactly. Both finalize attempts (14:13, 14:16) used byte-identical raw candidates → identical transformed identity → `occurrences=2`, `circuit_open=true`.
7. Sense-check sensitivity (synthetic): appending or mutating an SC changes `candidate_graph_hash` (SC inside graph digest) but leaves `task_contract_hash` bit-identical; `failure_fingerprint` is likewise insensitive (no SC in contract or diagnostics).

**confidence:** High. Exact digest match (sha256) across all four fields against two independent persisted copies, using the engine's own `json.dumps(sort_keys=True, separators=(",",":"), ensure_ascii=False)` serialization and the exact mutation order from `_write_finalize_artifacts`.

**structure_classification:** Deterministic admission blocker (dependency graph invalid after split) with content-addressed identity bound to the **post-mutation** graph; `failure_fingerprint` is a codes-only digest (task_ids/path dropped by `graph_admission.diagnostic_signature`), so distinct dangling-reference configurations can share one fingerprint.

**immediate_decision_informed:** Identity is (and should remain) the **regenerated transformed-graph identity**, not the raw model candidate's. The canonical blocker identity is `candidate:36bcefaf…` / `task_contract_hash sha256:ae438ec6…` / `failure_fingerprint 382e25a2…`. Any repair (rewire dependents after split) changes all four fields, and because the fingerprint is codes-only, a correctly rewired graph yields `dependency_unknown=0` → a **new** fingerprint — the pre/post Sol guard must expect a new `candidate_id` and must not look for `36bcefaf…` to persist post-repair. The raw identity `candidate:2c41222e…` has no canonical standing and should not be used as the repair anchor.

**durable_decision_informed:** Keep the current semantics: `candidate_id`/`candidate_graph_hash` are content addresses of the fully transformed graph (they must include `sense_checks` as long as the digest includes them — note any future sense-check injection, e.g., pre-gate SC for `before_execute` user actions, will legally change `candidate_id`). `task_contract_hash` intentionally excludes `sense_checks`/`seed_epoch`/`source` — this is consistent with execute-entry revalidation (`assert_admitted_task_feasibility` compares only `task_contract_hash`), so sense-check drift should not (and does not) affect the admitted execute contract. Two durable findings worth a follow-up ticket: (a) `graph_admission.diagnostic_signature` drops `task_id`/`dependency_id`/`message`, making `failure_fingerprint` coarser than the diagnostics it summarizes — consider mapping `task_id`→`task_ids` to restore granularity; (b) the splitter's missing dependent-rewiring is the root cause (per sibling FQ evidence), and once fixed, the fingerprint's codes-only nature means the repair will be recognized only by the absence of `dependency_unknown`, not by any shared fingerprint with the current failure.