All evidence is gathered and the resolution call is decisive. Here is the FQ-04 report.

---

## FQ-04 — Flash evidence brief

**question_id**: FQ-04 — Is `_apply_programmatic_coverage` actually defective on the captured canonical state, or is the reproduced `KeyError: -1` only a fixture artifact?

**verdict**: **Split finding, decision = ADD (one defect) / EXCLUDE (one symptom).**
1. The reproduced `KeyError: -1` is **only a fixture artifact** — EXCLUDE as a defect. It is caused solely by the repro script's synthetic dict-shaped `plan_versions` (`{"current": {"path": "plan.md"}}`); it cannot occur on the captured canonical state, whose `plan_versions` is a list, and no canonical producer ever writes a dict shape.
2. **However, `_apply_programmatic_coverage` IS defective on the captured canonical state via a separate, silent mechanism**: `_PLAN_STEP_PATTERN` matches only h2 `## Step N:` headers, while the canonical latest plan (`plan_v5.md`, hash-verified) uses h3 `### Step N:` headers (a form the plan worker explicitly accepts). `_extract_plan_steps` returns **0 steps**, so the function emits a **vacuous false-completeness attestation** (`plan_steps_covered: []`, `coverage_complete: true`, "All detected plan steps mapped to tasks."). This is a genuine **second latent coverage defect** — ADD to source scope (non-blocking for this incident, but an attestation-integrity defect).

**vantage_point**: Independent READ-ONLY DeepSeek V4 Flash investigator; no mutation, no writes, no launches. Evidence pack + bound engine `/workspace/runtime-candidates/arnold-r7-fresh-child-20260805` @ `234ac3524da29c1f630aee90ab1767a142a68a83` inspected only.

**utc_interval**: 2026-08-06T14:37Z–14:55Z (approx.; read-only inspection + one non-writing resolution call).

**artifacts (path + sha256)**:
| Artifact | sha256 |
|---|---|
| `.../incident-evidence/critique-r7-superfixer-20260806-142036-1ea42c25/raw/repro_finalize_mutations.py` | `6bef84dec62c676b4ac2d1928f84dda4db8cf9ecc1d058e9437d80c8de8c22fd` |
| `.../incident-evidence/critique-r7-superfixer-20260806-142036-1ea42c25/raw/plan-state.json` | `2f7827e75ff6cef15c253b0397735d73793b22178b19fb65ade0f9571694a754` |
| `.../incident-evidence/critique-r7-superfixer-20260806-142036-1ea42c25/raw/repro-output.txt` | `d89a46a3dbc7157c1a9f2f0aa9534ac6980fb161eeacf06c8adffec38d4a4c27` |
| `/workspace/runtime-candidates/arnold-r7-fresh-child-20260805/arnold_pipelines/megaplan/handlers/finalize.py` | `13efbdb7937bb73330f5aa4d19abeaf73904bea082d13cb0fbac650522695a4e` |
| `/workspace/runtime-candidates/arnold-r7-fresh-child-20260805/arnold_pipelines/megaplan/_core/state.py` | `8ccc588af130db3c4599b8c600890bfcf779cf0e916c88048909108f0ef9f5d1` |
| `.../plans/cl2-wbc-backed-ledger-20260805-2140/plan_v5.md` | `4537c985a9e8f1258af71d97d1d631b8ba6d0bcfc83b9a56fbd29cb327160f46` (== canonical `plan_versions[4].hash`) |
| `.../plans/cl2-wbc-backed-ledger-20260805-2140/.megaplan/worker_tmp/local-strict-artifacts/finalize-15274aaf2cb2445487647129704dcccd.candidate.json` | `cfc7d701c18c070ba15679d5279cc943119ec9d4fa412b8ef4d02115a75c76c8` (== heartbeat-bound artifact) |

**commands (read-only, cwd, exit codes)**:
1. `sha256sum` over the 7 artifacts above — cwd `/workspace` — exit 0 (hashing only; also cross-checked candidate hash against evidence-pack heartbeat and `plan_v5.md` hash against canonical `plan_versions[4].hash`).
2. Non-writing resolution call, cwd `/tmp` (neutral dir), exit 0:
   `env -u PYTHONHOME PYTHONSAFEPATH=1 PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=/workspace/runtime-candidates/arnold-r7-fresh-child-20260805 MEGAPLAN_TRUSTED_CONTAINER=1 python -P -B - <<'PY' …` (stdin script; no file created; `-B` + `PYTHONDONTWRITEBYTECODE=1` guarantee no `__pycache__` writes; imports + reads only).

**raw_excerpts**:
- Repro fixture (repro_finalize_mutations.py:34–41) — the artifact source of the KeyError:
  ```python
  state = {"config": {"mode": "code", "project_dir": str(PLAN_DIR.parent.parent.parent)},
           "plan_versions": payload.get("plan_versions") or {},
           "seed_epoch": None}
  # A minimal plan_versions so _apply_programmatic_coverage can resolve the plan md.
  if not state["plan_versions"]:
      state["plan_versions"] = {"current": {"path": "plan.md"}}
  ```
  Candidate has **no** `plan_versions` key (grep: 0 hits), so the fallback **dict** is always installed.
- Canonical shape (plan-state.json:282–313, condensed): `"plan_versions": [ {"version": 1, "file": "plan_v1.md", …}, …, {"version": 5, "file": "plan_v5.md", "hash": "sha256:4537c985…", "timestamp": "2026-08-06T00:21:31Z"} ]` — a **list** of records.
- `_core/state.py:2186–2194`:
  ```python
  def latest_plan_record(state: PlanState) -> PlanVersionRecord:
      plan_versions = state["plan_versions"]
      if not plan_versions:
          raise CliError("missing_plan_version", "No plan version exists yet")
      return plan_versions[-1]

  def latest_plan_path(plan_dir: Path, state: PlanState) -> Path:
      return plan_dir / latest_plan_record(state)["file"]
  ```
  `plan_versions[-1]` is **list indexing**; on a dict it raises `KeyError: -1`.
- `handlers/finalize.py:333–338` (coverage pattern) and `:390` (usage):
  ```python
  _PLAN_STEP_PATTERN = re.compile(
      r"^##\s+Step\s+\d+\s*:\s*(?P<title>.+?)\s*$"
      r"(?P<body>.*?)(?=^##\s+Step\s+\d+\s*:|\Z)",
      re.MULTILINE | re.DOTALL)
  ...
  plan_text = latest_plan_path(plan_dir, state).read_text(encoding="utf-8")
  ```
  Matches **h2 only**. plan_v5.md contains 51 `### Step N:` (h3) headers and **zero** `## Step N:` (grep: 0 hits); the plan worker explicitly accepts both (`## Step N:` **or** `### Step N:` under a phase — plan history entry @ 21:57:37Z).
- Repro symptom (repro-output.txt:2): `NOTE: programmatic coverage skipped: KeyError: -1`.
- Resolution call output (canonical shape):
  ```
  canonical plan_versions type: list len: 5
  latest_plan_path -> …/plans/cl2-wbc-backed-ledger-20260805-2140/plan_v5.md | exists: True
  extracted steps on canonical plan: 0
  validation after canonical coverage: {"plan_steps_covered": [], "orphan_tasks": [], "completeness_notes": "All detected plan steps mapped to tasks.", "coverage_complete": true}
  fixture dict shape -> KeyError KeyError(-1)
  ```
- Canonical producers all write list shape: `schemas/sprint1.py:231` `plan_versions: list[dict[str, Any]]`; `handlers/plan.py:203` `state["plan_versions"].append({…})`; `handlers/shared.py:1249` `state.get("plan_versions", [])[-1]`; `cloud/m11_workflow_canary_runner.py:418` list literal.

**trace**:
1. `_apply_programmatic_coverage` (finalize.py:381) gates on `state.get("plan_versions")`, then calls `latest_plan_path(plan_dir, state)` (line 390).
2. `latest_plan_path` → `latest_plan_record` → `plan_versions[-1]` (state.py:2190) — valid only for a **list**.
3. Canonical `plan_versions` is a list of 5 records; latest = `plan_v5.md`, hash-identical to the on-disk file → **no KeyError on canonical state**; the real finalize call site (`_write_finalize_artifacts`, finalize.py:2057) passes the canonical PlanState (list shape), so the real 14:13/14:16 runs never hit `KeyError: -1`.
4. The repro injects a dict (candidate has no `plan_versions`; fallback `{"current": {"path": "plan.md"}}`) → `plan_versions[-1]` → `KeyError: -1` → coverage skipped. **Fixture artifact**, proven by the resolution call (list → resolves; dict → KeyError(-1)).
5. On canonical state the function instead hits a **different real defect**: `_PLAN_STEP_PATTERN` (h2-only) extracts **0 steps** from the h3-formatted plan_v5.md → empty `plan_steps_covered`, `uncovered == []` → `coverage_complete: true` with "All detected plan steps mapped to tasks." — a **vacuous false-completeness attestation**, silently overriding any model `validation` block.
6. Impact scoping: this second defect does **not** contribute to the deterministic blocker (12× `dependency_unknown` + `dependency_graph_invalid` come from `task_splitter.py` dangling parent refs; coverage runs before feasibility and never touches `depends_on`). Also note `_apply_programmatic_coverage` replaces the whole `validation` dict, which would discard pre-gate coverage entries — irrelevant here since `user_actions: []` made both gate-task helpers no-ops.

**confidence**: High (0.85) — KeyError attribution is *proven* by the read-only resolution call on both shapes plus four independent canonical producers agreeing on list shape; h3-vs-h2 step miss is *proven* by hash-verified canonical plan content (0 extracted steps) and the resulting vacuous attestation. Residual uncertainty: whether Sol's remediation scope counts silent attestation defects as in-scope source items, and whether other finalize plans in the fleet use `## Step` (which would mask the h3 defect).

**structure_classification**: Engine defect attribution / fixture-artifact adjudication within a deterministic finalize blocker incident (primary root cause: `task_splitter.py` split-parent rewiring gap). This finding is a secondary/qualifying classification: one symptom excluded as test-harness artifact, one genuine latent defect (coverage attestation integrity) added.

**immediate_decision_informed**: Do **not** add `KeyError: -1` to source scope — it is unreachable on canonical state (fixture-only, dict-shaped `plan_versions`). The existing minimal source change for the blocker (splitter dependent rewiring) is unaffected by `_apply_programmatic_coverage`; no urgent action from this finding for the current recovery.

**durable_decision_informed**: **Add** a second latent coverage defect to source scope: `handlers/finalize.py::_PLAN_STEP_PATTERN` must also accept `### Step N:` (h3, permitted by the plan worker) — e.g. `^#{2,3}\s+Step\s+\d+\s*:` — and `_apply_programmatic_coverage` should fail-closed (or at least flag) when `_extract_plan_steps` returns zero steps instead of emitting a vacuously-true `coverage_complete` attestation. Optionally harden `latest_plan_record` to reject non-list `plan_versions` (type guard), which would convert any future dict-shaped fixture into a clear `CliError` instead of `KeyError: -1`.