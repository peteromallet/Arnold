Now I have a complete picture. Here's the inventory:

---

## Command/Surface Inventory: Direct Agent Launch Surfaces

### 1. `pyproject.toml` console_scripts

| Command | Entry point | Current launch behavior | Migration handling |
|---|---|---|---|
| `arnold` | `arnold.cli:cli_entry` → `main()` | Dispatches to `workflow` (6 subcommands) or 4 operator commands (`status`, `trace`, `inspect`, `override`). Legacy megaplan subcommands are **already removed** (M6 purge executed). | ✅ Clean. No migration needed. |
| `agentbox` | `agentbox.cli:main` | Standalone `argparse` CLI — 12 top-level subcommands (`run`, `status`, `logs`, `attach`, `reconcile`, `cleanup`, `guardian`, `creds`, `bootstrap`, `doctor`, `services`, `notify`, `version`). Own parser, own dispatch. | 🟡 Should become an `agent ...` subcommand or delegate to facade. Currently a completely separate binary with no shared dispatch. |

### 2. Arnold CLI subcommands (current, neutral surface)

| Command | Path | Launch behavior | Migration handling |
|---|---|---|---|
| `arnold workflow check` | `arnold/cli/workflow.py` | Validates a `--module package.module:build_pipeline` target. | ✅ Permanent neutral surface. |
| `arnold workflow manifest` | `arnold/cli/workflow.py` | Emits compiled manifest YAML/JSON. | ✅ Permanent. |
| `arnold workflow dot` | `arnold/cli/workflow.py` | Emits DOT graph. | ✅ Permanent. |
| `arnold workflow dry-run` | `arnold/cli/workflow.py` | Dry-run report without execution. | ✅ Permanent. |
| `arnold workflow run` | `arnold/cli/workflow.py` | Execute compiled manifest via `arnold.execution.run`. | ✅ Permanent. |
| `arnold workflow resume` | `arnold/cli/workflow.py` | Resume from checkpoint/cursor. | ✅ Permanent. |
| `arnold workflow describe` | `arnold/cli/workflow.py` | Manifest-backed describe. | ✅ Permanent. |
| `arnold status` | `arnold/cli/operators.py` | Reads event journal/artifact root. | ✅ Permanent operator command. |
| `arnold trace` | `arnold/cli/operators.py` | Prints event journal. | ✅ Permanent operator command. |
| `arnold inspect` | `arnold/cli/operators.py` | Inspects manifest + control transitions. | ✅ Permanent operator command. |
| `arnold override` | `arnold/cli/operators.py` | Control transition overrides. | ✅ Permanent operator command. |
| `python -m arnold` | `arnold/__main__.py` | Delegates to `arnold.cli.main()`. | ✅ Same as `arnold` console script. |

### 3. AgentBox CLI (standalone binary)

| Command | Path | Launch behavior | Migration handling |
|---|---|---|---|
| `agentbox run` | `agentbox/cli.py` | Launch an AgentBox operation adapter (needs `--repo`, `--kind`, `--spec`). | 🟡 Direct agent launch surface — should become `agent agentbox run` or delegate via facade. |
| `agentbox status` | `agentbox/cli.py` | Show host operation status. | 🟡 `agent agentbox status` |
| `agentbox logs` | `agentbox/cli.py` | Show bounded host logs. | 🟡 |
| `agentbox attach` | `agentbox/cli.py` | Attach to live tmux session. | 🟡 |
| `agentbox reconcile` | `agentbox/cli.py` | Read-only host-local state report. | 🟡 |
| `agentbox cleanup survey/apply` | `agentbox/cli.py` | Survey/apply cleanup recommendations. | 🟡 |
| `agentbox guardian run/run-once/pause/resume/status` | `agentbox/cli.py` | Guardian worker lifecycle. | 🟡 |
| `agentbox creds list/test/push` | `agentbox/cli.py` | Credential management. | 🟡 |
| `agentbox bootstrap` | `agentbox/cli.py` | Ensure workspace layout + systemd units. | 🟡 |
| `agentbox doctor` | `agentbox/cli.py` | Read-only health checks. | 🟡 |
| `agentbox services list/logs/restart` | `agentbox/cli.py` | Systemd service management. | 🟡 |
| `agentbox notify test` | `agentbox/cli.py` | Test Discord notifications. | 🟡 |
| `agentbox version` | `agentbox/cli.py` | Show version. | 🟡 |

### 4. Legacy Megaplan CLI (shim, M6 deletion target)

| Command | Path | Launch behavior | Migration handling |
|---|---|---|---|
| `python -m arnold_pipelines.megaplan` | `arnold_pipelines/megaplan/__main__.py` → `arnold_pipelines/megaplan/cli/__init__.py:main()` | Full legacy `argparse` CLI with ~30 subcommands (`init`, `prep`, `plan`, `critique`, `gate`, `revise`, `finalize`, `execute`, `review`, `status`, `override`, `resume`, `brief`, `initiative`, `audit`, `pipelines`, `setup`, `chain`, `epic-chain`, `auto`, `progress`, `watch`, `verify-human`, `audit-verifiability`, `migrate-layout`, `introspect`, `trace`, `doctor`, `record-tag`, `feedback`, `quality-gate`, `user-action`). | 🔴 **M6 delete.** All planning-step subcommands should be accessible only via `arnold workflow run` on compiled manifests. The `arnold` top-level dispatch no longer routes here. |
| `python -m arnold.pipelines.megaplan` | `arnold/pipelines/megaplan/__main__.py` (legacy location) | Legacy alias. | 🔴 **M6 delete** (already listed in M6 deletion ledger). |
| `arnold megaplan run` / `arnold megaplan resume` | Formerly `arnold/cli/__init__.py` subcommands | **Already removed.** The `arnold` dispatch no longer accepts `megaplan` as a subcommand. | ✅ Already deleted from dispatch. |

### 5. Product-Specific CLIs (megaplan sub-command registrations)

These register on the megaplan `argparse` parser and are only reachable via `python -m arnold_pipelines.megaplan <subcommand>`:

| Subcommand group | Path | Launch behavior | Migration handling |
|---|---|---|---|
| `megaplan incident list/brief` | `arnold_pipelines/megaplan/incident/cli.py` | Incident listing/briefing via projection layer. | 🟡 Should become `arnold incident ...` if kept; or fold into operator `arnold inspect` projection. |
| `megaplan cloud ...` | `arnold_pipelines/megaplan/cloud/cli.py` | Cloud deploy/spec management (5134 lines, deep SSH exec). | 🟡 Likely needs to become `arnold cloud ...` — it's a major operational surface. |
| `megaplan bakeoff run` | `arnold_pipelines/megaplan/bakeoff/cli.py` | Run idea through multiple profiles concurrently. | 🟡 `arnold bakeoff ...` |
| `megaplan resident discord/scheduler-once/health` | `arnold_pipelines/megaplan/resident/cli.py` | Resident orchestration (Discord bot, scheduler, health). | 🟡 `arnold resident ...` |

### 6. Shell Files

| File | Launch behavior | Migration handling |
|---|---|---|
| `sync-skills.sh` | Symlinks `arnold_pipelines/megaplan/skills/` into `~/.claude/skills`, `~/.codex/skills`, `~/.agents/skills`, `~/.hermes/skills`. Removes stale links from retired `data/_codex_skills`. | 🟡 Should be wrapped as `arnold skills sync` to avoid raw shell invocation. |
| `arnold_pipelines/megaplan/data/pre-commit-hook.sh` | Git pre-commit hook that runs `python -m arnold_pipelines.megaplan setup --regen-composed`, stages regenerated bundles, and fails if bundles changed. | 🟡 Tightly coupled to `python -m arnold_pipelines.megaplan` which is M6-delete. Needs migration to `arnold setup --regen-composed` or equivalent workflow command. |
| `.megaplan/initiatives/agentbox-persistent-machine/assets/launch-after-python-shaped-m3.sh` | Bootstraps agentbox after M3 landing. | 🟡 One-shot bootstrap script — validate post-M6. |

### 7. `scripts/` Operator Scripts

All run as `python scripts/<name>.py` from repo root. Not installed, not in `pyproject.toml`.

| Script | Purpose | Migration handling |
|---|---|---|
| `scripts/chain_done_gate.py` | Blocks chain completion when state is inconsistent. | 🟡 Could become `arnold chain done-gate` or remain as operator tool. |
| `scripts/m6_purge_gate.py` | Blocks clean-break when legacy `_pipeline/` or `stages/` dirs remain. | ✅ Self-limiting — becomes irrelevant after M6 purge. |
| `scripts/adopt_plan.py` | Adopts finalized plan dir into existing chain. | 🟡 `arnold chain adopt` or `arnold plan adopt`. |
| `scripts/check_workflow_pipeline_inventory.py` | Inventory scan. | 🟡 Could become `arnold workflow inventory`. |
| `scripts/check_pipeline_id_registry.py` | Pipeline ID registry validation. | 🟡 `arnold pipeline registry-check`. |
| `scripts/generate_arnold_docs.py` | Doc generation. | 🟡 `arnold docs generate`. |
| `scripts/validate_native_representation_conformance.py` | Conformance validation. | 🟡 `arnold conformance validate`. |
| `scripts/validate_package_disposition.py` | Package disposition validation. | 🟡 `arnold conformance disposition`. |
| `scripts/simulate_watchdog_end_to_end.py` | Watchdog E2E simulation. | 🟡 Operator-only; could stay as script. |
| `scripts/cloud_hot_upload.py` | Cloud hot-upload. | 🟡 `arnold cloud upload`. |
| `scripts/megaplan_live_watchdog.py` | Live watchdog. | 🟡 Operator-only. |
| `scripts/render_package_disposition_md.py` | Renders disposition markdown. | 🟡 `arnold docs disposition`. |

### 8. Docs Command Examples (potential agent launch surfaces)

Examples found in `docs/` that reference direct commands:

| Doc reference | Command example | Migration handling |
|---|---|---|
| `docs/arnold/authoring-guide.md` | `arnold pipelines new my-module` | 🔴 Legacy command. M5 CLI mapping marks `arnold pipelines new` as **delete**. Docs need update. |
| `docs/arnold/authoring-guide.md` | Scaffolding references `@pipeline`/`@phase`/`@decision` decorators | ✅ These are workflow-authoring concepts, not CLI commands. |
| `docs/cloud-hot-upload.md` | `--wrapper arnold-watchdog`, `--wrapper arnold-kimi-goal-operator` | 🟡 Wrapper references — validate post-M6. |

---

## ⚠️ Commands Flagged for Facade Delegation

**Priority 1 — Should become `agent ...` subcommands:**
- Every `agentbox ...` command (13 subcommands). Currently a standalone binary with no shared dispatch.

**Priority 2 — Should become `arnold ...` subcommands:**
- `python -m arnold_pipelines.megaplan incident|cloud|bakeoff|resident ...` — 4 product-specific CLI modules buried in megaplan CLI that need surfacing under the `arnold` namespace.
- `sync-skills.sh` → `arnold skills sync`
- `scripts/chain_done_gate.py` → `arnold chain done-gate`
- `scripts/adopt_plan.py` → `arnold chain adopt`
- `scripts/cloud_hot_upload.py` → `arnold cloud upload`
- `scripts/generate_arnold_docs.py` → `arnold docs generate`
- Conformance scripts → `arnold conformance ...`

**Priority 3 — Docs-only cleanup:**
- `docs/arnold/authoring-guide.md` still references `arnold pipelines new` (marked `delete` in M5 mapping).