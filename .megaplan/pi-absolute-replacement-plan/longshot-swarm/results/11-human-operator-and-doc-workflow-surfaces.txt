I now have comprehensive evidence. Here is the ranked analysis.

---

## Ranked Human-Facing Surfaces

### 1. `python -m arnold_pipelines.megaplan` (CRITICAL — highest touch count)
**170+ invocations across all docs.** This is *the* muscle-memory verb. Every operator and every agent skill teaches it: `init`, `plan`, `critique`, `gate`, `finalize`, `execute`, `review`, `chain`, `cloud`, `bakeoff`, `brief`, `config`, `status`, `override`, `doctor`, `introspect`, `trace`, `setup`. The README quick-start, `megaplan-prep`, `megaplan-epic`, `cloud.md`, and `configuration.md` all center on this one module path.

**Compat needed:** The module launcher must keep working. The future `agent` facade can sit *alongside* it for the full observation window, but the existing invocation string must not break or start emitting "deprecated" noise until Epic 7 metrics prove adoption has shifted.

### 2. Agent skill documents (CRITICAL — agent muscle memory)
Five skills installed via symlink to `~/.claude/skills/` and `~/.codex/skills/`:
- `megaplan-prep` — consulted "before every `init`"
- `megaplan-epic` — chain orchestration
- `megaplan-observe` — in-flight diagnosis + `watcher.sh`
- `megaplan-tickets` — lightweight repo notes
- `megaplan` (main) — composed bundle with agent-specific appendices

Every skill teaches the `python -m arnold_pipelines.megaplan` verb. Every skill must be rewritten to teach `agent ask / launch / fan / review / apply` during Epic 7, but cannot break during the migration — agents reading old skills mid-transition must still succeed.

**Compat needed:** Skills ship their canonical forms from `megaplan/data/`. Old-skill compat for at least the observation window (Epic 7). Symlink distribution stays until replaced.

### 3. `agentbox` CLI surface (HIGH)
8+ documented commands: `bootstrap`, `doctor`, `services list|logs|restart`, `creds push|test`, `notify test`, `reconcile`, `version`. The `agentbox/bootstrap-and-day2.md` runbook teaches this exact surface. These commands dispatch agents through cloud wrappers, `launch_hermes_agent.py`, `codex exec`, and raw Claude calls — all bypass paths.

**Compat needed:** The `agentbox` verb should remain during Epic 8 migration. Internally it must route through the facade, but the external command name should not change until parity is proven. The `agentbox creds push` flow must survive credential mediation changes.

### 4. Cloud CLI surface (HIGH)
`python -m arnold_pipelines.megaplan cloud *`: `init`, `build`, `deploy`, `bootstrap`, `preflight`, `sync-megaplan`, `launch-epic`, `status`, `logs`, `attach`. The `cloud.md` doc has 35 invocation examples. The cloud bootstrap flow is a documented onboarding path.

**Compat needed:** Same module path compat as surface #1. Cloud wrappers internally call `launch_hermes_agent.py` / `codex exec` / raw Claude — these must route through the facade before the old paths are deleted (Epic 8 gate).

### 5. `~/.hermes/.env` credential file (HIGH)
Deeply embedded: README quick-start, `configuration.md` reference table, `megaplan setup` interactive flow, and gap swarm findings confirm it's "invisible to some adapters and preflight paths." Every operator who has set up megaplan knows this path.

**Compat needed:** The file must continue to be read during migration. The plan says "bridge or deprecate `~/.hermes/.env` deliberately" in Epic 3. Do NOT delete or stop reading it until facade credential mediation is proven and docs are updated. Likely needed through Epic 7.

### 6. Watchdog/repair/meta-loop shell patterns (HIGH)
The Hetzner meta-loop (`docs/hetzner-watchdog-meta-loop.md`) teaches a specific SSH + docker exec pattern. The live watchdog (`docs/megaplan_live_watchdog.md`) teaches `python scripts/megaplan_live_watchdog.py --once` plus `launchd` plist and cron installation. Operators have memorized `ssh root@159.69.51.216 'docker exec megaplan-cloud-agent bash -lc "..."` with specific grep/pgrep/json one-liners.

**Compat needed:** Watchdog wrappers and repair agents dispatch through direct launchers. These must route through the facade before the direct paths are deleted (Epic 8). The external commands (`arnold-watchdog`, `arnold-kimi-goal-operator`, `arnold-progress-auditor`) should keep their names.

### 7. Profile names and agent specs (MEDIUM)
`partnered-3/4/5`, `solo`, `directed`, `premium`, `apex` — used in CLI flags, chain.yaml, TOML profiles, and skill notation shorthand. `hermes:*` specs (e.g., `hermes:deepseek:deepseek-v4-pro`) appear in system profiles and pipeline-local profiles. The `premium:*` symbolic spec is the vendor-neutral placeholder.

**Compat needed:** Profile names must keep working. `hermes:*` specs need a compatibility resolution layer (Epic 3) that emits `legacy_route` telemetry. The mapping must persist until telemetry proves the canonical-rewrite is correct. `premium:*` resolution must be transparent.

### 8. Quick-start install sequence (MEDIUM)
The README teaches: `git clone → cd arnold → pip install -e . → python -m arnold_pipelines.megaplan setup`. This is the onboarding funnel. The setup command detects installed agents and walks through credentials.

**Compat needed:** The install sequence must produce a working system throughout migration. `megaplan setup` must install the new facade alongside existing tools during the observation window.

---

## Alias / Compat Commands Needed

| Surface | What must keep working | Duration |
|---|---|---|
| `python -m arnold_pipelines.megaplan *` | All documented subcommands | Full observation window (Epic 7 adoption gate) |
| `megaplan chain` | Epic driver verb | Until chain consumers migrate to facade |
| `agentbox *` (8 commands) | Full day-2 surface | Until Epic 8 cloud/resident/AgentBox gate passes |
| `~/.hermes/.env` | Read by ALL credential resolution paths | Until Epic 3 parity + Epic 7 adoption metrics pass |
| `hermes:*` profile specs | Resolve through compatibility mapping with telemetry | Until rewrite is proven correct (Epic 3 gate), then deprecated but readable until Epic 7 deadline |
| `premium:*` symbolic spec | Resolve to concrete vendor transparently | Permanent (or until profile system is rearchitected) |
| Watchdog/repair script paths | `python scripts/megaplan_live_watchdog.py`, container wrapper names | Until Epic 8 parity gate |
| Skill symlink targets | `~/.claude/skills/megaplan-*/SKILL.md`, `~/.codex/skills/megaplan-*/SKILL.md` | Until skills are rewritten to teach `agent` verbs (Epic 7) |
| `megaplan config set/show/reset` | Full config management | Until facade-owned config replaces it |

---

## The Critical Principle

The unified surface plan explicitly says: **"Ship a thin recording/control facade first. Replace runtimes only after the facade has captured contracts, baselines, telemetry, adoption data, and deletion evidence."** Every human-facing surface above must be *wrapped* before it is *replaced*. The existing verb strings must remain functional during the observation window, even if internally routed through the new facade. Delete only after telemetry shows zero bypass usage.