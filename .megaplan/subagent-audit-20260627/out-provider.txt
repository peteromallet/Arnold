**hypothesis:** The plan converged locally — both gate iterations (v1 and v2) returned PROCEED with valid artifacts — but the cloud chain hits a triple credential miss. `parse_agent_spec("hermes:deepseek:deepseek-v4-pro")` correctly resolves to `AgentSpec(agent='hermes', model='deepseek:deepseek-v4-pro')`, which `resolve_model` unwraps to direct DeepSeek API via `DEEPSEEK_API_KEY`. On cloud, that key is unset, so `agent_kwargs` arrives empty at the AIAgent constructor, which falls through to `resolve_provider_client("openrouter")` → OPENROUTER_API_KEY also unset → final fallback constructs an OpenAI client with no key → the `RuntimeError: Failed to initialize OpenAI client: Missing credentials` that repeats across iterations 95–109. This is a pure credential-routing bug, not plan non-convergence.

**recommendation:** Set `DEEPSEEK_API_KEY` in Railway project secrets (the directed profile expects direct DeepSeek, per `cloud_skill.md` line 174). Also set `OPENROUTER_API_KEY` as a fallback channel. Then `cloud chain` will resume from `critiqued→gate` and pass immediately — no rerun or plan revision needed.
