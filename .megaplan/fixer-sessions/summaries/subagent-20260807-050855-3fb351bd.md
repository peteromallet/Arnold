Session subagent-20260807-050855-3fb351bd (deepseek:deepseek-v4-flash)
  provenance: Flash-generated summary
  evidence: result.md, git-custody-evidence.json
  summary: Diagnosed why the prior watchdog backstop fix never took effect:
  the running watchdog's python3() is the supervisor function
  arnold_supervisor_python which runs `env -u PYTHONPATH PYTHONSAFEPATH=1
  $MEGAPLAN_SUPERVISOR_PYTHON -P` (MEGAPLAN_SUPERVISOR_ISOLATED=1), so
  session_markers resolves to the supervisor venv STATIC site-packages copy
  (pip-installed from read-only arnold-4ed98585-live), not the project
  branch (189ea0b73) or r7-fresh-child (280ef259d) that prior fixers
  patched — spurious workspace_missing flags for r5/r6/r7 continued every
  sweep. Repair: added liveness-fence/lease to CANONICAL_SIDECAR_SUFFIXES in
  the venv site-packages copy (verified via exact isolated invocation) and
  mirrored to /workspace/arnold (commit 7d8426ca07, dirty set preserved).
  Live proof: first watchdog sweep after fix (05:59:01Z) processed r5/r6/r7
  with ZERO flag lines; last spurious flag was 05:51:25Z. Ordinary retrigger
  refused by chain_execution_binding_drift (operator-authorized rebind
  required) and plan is at terminal STATE_BLOCKED designed human gate (54
  flags) — both external-authority gates; milestone idx 0 remains frozen.
