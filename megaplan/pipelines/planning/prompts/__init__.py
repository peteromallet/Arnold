"""Planning pipeline prompt registry placeholder.

Per-stage prompt files for the planning pipeline are registered under
``megaplan/_pipeline/stages/`` (the canonical stage implementations).
This package exists as the conventional sibling location for any
planning-specific prompt overrides or extensions registered via
``register_pipeline_prompt``.
"""
