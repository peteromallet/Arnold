Now I have a complete picture. Let me summarize my findings.

---

## Audit Findings: status.md vs. Current Tree

I inspected four layers from `status.md` (Root, Docs, Docs Reference Audit, Package Dead-Module) against the actual filesystem. All four layers' primary claims are **correct** — the file moves, deletions, and additions they describe match the tree. However, I found **one significant stale reference** and **one minor cosmetic inconsistency** related to the boundary between `docs/historical/` and `docs/plans/`.

---

### Discrepancy 1 (Significant): `docs/historical/README.md` claims a move that never happened

**Stale statement** at `docs/historical/README.md`, lines 7–9:

```
> **Note:** `agent_readable_templates_v2.md` has been moved to
> [`docs/plans/agent_readable_templates_v2.md`](../plans/agent_readable_templates_v2.md)
> — it is an active project plan and does not belong here.
```

**Current tree reality:**
- `docs/historical/agent_readable_templates_v2.md` **still exists** — the file was never moved.
- `docs/plans/agent_readable_templates_v2.md` **does not exist** — the target path is a dead link.

**Root cause:** The `historical_docs_results` sub-audit (in `docs/structure_cleanup/historical_docs_results/02-historical-folder-contract.txt`) applied the README edit (✅ Fixed) but **deferred** the actual shell `mv` command (🔴 Deferred). The main `status.md` never records this deferred item because the historical-docs audit was not a top-level layer.

**Proposed replacement text** for `docs/historical/README.md` lines 7–9 — remove the stale note entirely (restoring the original pre-edit state):

```markdown
> **Note:** `agent_readable_templates_v2.md` declares `**Status:** active` but lives
> in `docs/historical/`. It should be moved to `docs/plans/` in a dedicated
> historical-docs pass.
```

Or, if the intent is to keep the note brief and not make a forward reference:

```markdown
> **Note:** `agent_readable_templates_v2.md` is an active project plan and should
> not live in `docs/historical/`. A move to `docs/plans/` is pending.
```

---

### Discrepancy 2 (Minor): `docs/historical/agent_readable_templates_v2.md` has contradictory internal status

**Stale statement** at `docs/historical/agent_readable_templates_v2.md`, line 3:

```
**Status:** active
```

**Problem:** The file lives in `docs/historical/` (which its own README defines as "Archived spikes, old rationale, and completed follow-up plans… should not be treated as the current source of truth"). An internal `**Status:** active` directly contradicts its location.

**Proposed replacement text** for line 3:

```
**Status:** archived / superseded
```

---

### Discrepancy 3 (Minor — status.md omission): Docs Layer section doesn't acknowledge the deferred `agent_readable_templates_v2.md` move

**Location:** `status.md` lines 26–51 (Docs Layer).

**Problem:** The Docs Layer section lists three historical files moved into `docs/historical/` and one plan moved into `docs/plans/`, but never mentions `agent_readable_templates_v2.md`. Meanwhile, the `historical_docs_results/` sub-audit identified it as a deferred item, and the historical README was prematurely edited to claim the move was done.

**Proposed addition** — a deferred-item bullet under the Docs Layer section (after line 40, before the `created docs/agent-edit/` block):

```markdown
Deferred for a dedicated historical-docs pass:
- moving `docs/historical/agent_readable_templates_v2.md` to `docs/plans/`;
  the sub-audit recommended the move but it was not executed in this layer
```

---

### Verification: Other four layers are clean

| Layer | Key Claims | Tree Match |
|---|---|---|
| **Root** (lines 7–24) | Audits → `docs/audits/`, plans → `docs/plans/`, scripts → `scripts/maintenance/` | ✅ All verified |
| **Docs** (lines 26–51) | Indexes added, 3 historical files moved, 1 plan moved, new doc dirs created | ✅ All verified (except `agent_readable_templates_v2.md` gap above) |
| **Package Dead-Module** (lines 413–461) | `port.py`, `_analyze_names.py`, `router.py`, `resize_schema.py` deleted; `router_rules.py` kept as shim | ✅ All verified |
| **Docs Reference Audit** (lines 463–500) | RunPod/router references updated; plan/history rewrites deferred | ✅ Verified |

---

### Summary

The only actionable stale content is **Discrepancy 1** — the `docs/historical/README.md` note that claims a completed move for a file that was never relocated. Fixing that one note (either deleting it or rephrasing to say "pending") resolves the direct contradiction. Discrepancies 2 and 3 are cosmetic/omission issues on the same root cause.