Good — confirmed: `agent_readable_templates_v2.md` only exists in `docs/historical/`, not in `docs/plans/`. The README claim is stale.

Here is my full audit with recommendations:

---

## Audit Results: `docs/plans/` and `docs/historical/`

### Summary

| Verdict | Count |
|---|---|
| **Delete (junk)** | 0 |
| **Retain as-is** | 8 |
| **Retain (label needed)** | 4 |
| **Move (misplaced)** | 1 |

**No files are safe to delete as junk.** Every file contains authored project history with specific, non-generated content. Below is the per-file breakdown.

---

### `docs/plans/` (5 files)

| File | Verdict | Reasoning |
|---|---|---|
| **README.md** | **RETAIN** | Navigation index; low-value but useful. |
| **revised_plan.md** | **RETAIN → label: `superseded`** | M0 reorg plan — detailed execution steps. Authored, not junk. Likely executed/superseded by now. Add a header noting "Superseded — M0 reorg complete." |
| **plan_v2.md** | **RETAIN (no label)** | M3 corpus proof/polish plan. Detailed and appears potentially still active or recent. Keep as current context. |
| **finalize.json** | **RETAIN → label: `archived`** | Agent execution manifest for Unit 14/15 moves. Machine-oriented JSON but contains real task descriptions, dependencies, and verification steps — not junk. If all tasks are done, label as `archived-execution-manifest`. |
| **loose-work-consolidation-plan.md** | **RETAIN → label: `superseded`** | Dated 2026-06-09 consolidation plan. Authored with specific branch/merge instructions. If consolidation is complete, label accordingly. |

---

### `docs/historical/` (8 files)

| File | Verdict | Reasoning |
|---|---|---|
| **README.md** | **RETAIN** | Navigation index. Note: the claim that `agent_readable_templates_v2.md` was moved to `docs/plans/` is **stale** — the file is still here. Update the README. |
| **graphbuilder_spike.md** | **RETAIN (already correctly labeled)** | Clear "Historical spike" header. Proper provenance value. |
| **frontier-hardening-runbook.md** | **RETAIN (already correctly labeled)** | "Historical completion record." Proper provenance. |
| **sprint5_followups.md** | **RETAIN (already correctly labeled)** | "Historical follow-up plan." Proper provenance. |
| **comfyscript_spike.md** | **RETAIN (already correctly labeled)** | "Historical spike." Proper provenance. |
| **template_cleanup_followups.md** | **RETAIN (already correctly labeled)** | "Historical record." Proper provenance. |
| **old_vibecomfy_port_rationale.md** | **RETAIN (already correctly labeled)** | "Historical rationale." Proper provenance. |
| **agent_readable_templates_v2.md** | **MOVE → `docs/plans/`** | **This is misplaced.** File declares `Status: active` and contains a current project plan (v2 template format). It belongs in `docs/plans/`, not `docs/historical/`. The README already says it was moved — but it wasn't. **Move this file** and fix the README. |

---

### Recommendations

1. **No deletions** — all files contain non-duplicated, non-generated authored content.

2. **Label three plan files** as `superseded`/`archived` once their work is confirmed complete:
   - `docs/plans/revised_plan.md` → add "**Status: Superseded** — M0 reorg complete."
   - `docs/plans/finalize.json` → add comment or rename to indicate "archived-execution-manifest"
   - `docs/plans/loose-work-consolidation-plan.md` → add "**Status: Superseded** — consolidation complete."

3. **Move `docs/historical/agent_readable_templates_v2.md` → `docs/plans/agent_readable_templates_v2.md`** — it's an active plan, not historical.

4. **Fix `docs/historical/README.md`** — the note claiming the file was moved is now correct in intent but incorrect in fact. Update it to reflect current state (or fix after the move).

5. **No file meets the bar for junk deletion.** The generated appearance of `finalize.json` (JSON agent manifest) is misleading — it contains detailed task descriptions, dependency chains, and sense-check questions that document project decisions.