Now I have a complete picture. Here's the analysis and recommendation.

---

## Audit Findings

### Current plan (from `README.md` lines 89–93)

> *"The entire `structure_cleanup/` directory is a one-time audit artifact. After all layers are complete and the applied cleanup is verified, move the entire directory to `docs/historical/structure-cleanup/`."*

### Current state

- **All 13+ audit layers completed** (root → docs → docs root → docs reference → agentic → artifacts → scripts/tools → megaplan → config/ops → tests → package internals → template/corpus/recipe → dead-module verification).
- **`status.md`** (line 502–510) lists only two "next" items: a schema/runtime refactor pass and a **dedicated historical-docs pass** — neither is a structure-cleanup layer.
- **Deferred items remain open** across every layer: e.g., batched link repair for docs-root files, `.megaplan/` bulk move, `_graph_utils.py` deletion with reference repair, etc.
- **`docs/README.md` line 38** still lists `structure_cleanup/` as a live "Main Area" alongside `architecture/`, `plans/`, `audits/`, and `historical/`.
- **`docs/historical/README.md`** makes no mention of `structure_cleanup/` as a planned arrival.
- The **docs-reference layer audit** (`06-structure-cleanup-folder.txt`) confirmed the destination as `docs/historical/structure-cleanup/` and updated the README accordingly, but deferred the actual move.

### Staleness check

The plan in `README.md` is **not stale** but the trigger condition is partially met: all layers are complete, but the "applied cleanup is verified" clause is ambiguous — verification passed for each layer, yet deferred items remain unactioned. The plan remains accurate as a forward commitment.

---

## Decision: KEEP NOW → MOVE LATER

**Recommendation: Keep `docs/structure_cleanup/` in place at `docs/structure_cleanup/` for now. Move it to `docs/historical/structure-cleanup/` as part of the dedicated historical-docs pass after deferred items are resolved or explicitly closed out.**

### Reasoning

| Factor | Weight | Detail |
|---|---|---|
| **Active references** | ⚠️ | `docs/README.md` lists it as a "Main Area." Moving would require a docs-README edit plus a cross-reference check. |
| **Deferred items not closed** | 🚫 | `status.md` lists deferred items per layer that someone may still reference while resolving them. Moving the evidence tree out from under its known path adds friction. |
| **Self-describing plan** | ✅ | The README already says it will move *after* completion+verification. Moving prematurely contradicts the plan's own timing. |
| **`status.md` as reference** | 📄 | The status document is the canonical record of what was done and what's left — keeping it at an easy-to-find, non-historical path helps anyone picking up deferred work. |
| **Historical bucket not ready** | 🗄️ | `docs/historical/README.md` doesn't mention this as a planned arrival yet. A batch move (structure-cleanup + resolved deferred items) is cleaner than a trickle. |

### When to pull the trigger

Move when **all** of these are true:

1. All deferred items across layers are either resolved or explicitly closed as "won't do / out of scope."
2. `docs/README.md` is updated to either remove the `Structure cleanup` link or redirect it to `docs/historical/structure-cleanup/`.
3. The historical-docs pass (named in `status.md` line 508) is underway and can batch-archive structure-cleanup alongside any other completed plans.

---

## Recommended wording updates (if editing were allowed)

If the plan *were* being updated today, I'd suggest modest clarifications, not rewrites:

| File | Line(s) | Issue | Suggested tweak |
|---|---|---|---|
| `docs/README.md` | 38 | `[Structure cleanup](structure_cleanup/)` looks like an active nav area | Change to `[Structure cleanup (audit record)](structure_cleanup/)` or immediately note *(audit evidence — cleanup pending)* |
| `docs/structure_cleanup/README.md` | 89–93 | "after all layers are complete and the applied cleanup is verified" is ambiguous about deferred items | Add: *"Deferred items remain open per-layer in `status.md`; the move should happen when those are resolved or formally descoped."* |
| `docs/structure_cleanup/status.md` | 502–510 | "Next Recommended Layer" item #2 says "archive the full `docs/structure_cleanup/` evidence tree" but doesn't name the destination | Add destination: *"...archive the full `docs/structure_cleanup/` evidence tree to `docs/historical/structure-cleanup/`."* |
| `docs/historical/README.md` | anywhere | No mention that `structure-cleanup/` is incoming | Add a placeholder note: *"`docs/structure_cleanup/` will move here as `structure-cleanup/` after deferred cleanup items are closed."* |

---

**Summary verdict: Keep in place now, move to `docs/historical/structure-cleanup/` during the historical-docs batch pass when deferred items are closed.** The plan is current, not stale — it just needs the deferred-items qualifier added for clarity.