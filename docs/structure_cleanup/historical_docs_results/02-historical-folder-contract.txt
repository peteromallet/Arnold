---

## Final audit report

The contract for `docs/historical/` had **3 categories of issues**. Here's what was done and what remains.

### ✅ Fixed — 4 safe edits applied

| File | Edit |
|------|------|
| `README.md` | Removed link to `agent_readable_templates_v2.md` (active plan); added a note block pointing to its new home |
| `frontier-hardening-runbook.md` | Added `**Status:** Historical completion record. All frontier-hardening sprints have landed on main.` |
| `sprint5_followups.md` | Added `**Status:** Historical follow-up plan. Sprint 5 delivery is complete; this doc is kept for context.` |
| `template_cleanup_followups.md` | Added `**Status:** Historical record of cleanup ideas captured 2026-05-23. Items listed here may no longer be current...` |

All four now have an explicit status header matching the README contract. The three that lacked one (`frontier-hardening-runbook.md`, `sprint5_followups.md`, `template_cleanup_followups.md`) now conform to the pattern set by `comfyscript_spike.md`, `graphbuilder_spike.md`, and `old_vibecomfy_port_rationale.md`.

### 🔴 Deferred — file move requires shell

**`agent_readable_templates_v2.md`** declares `**Status:** active` and is a forward-looking project plan with phases, owners, and ticket references. It **must not live in `docs/historical/`**. The exact command:

```bash
mv docs/historical/agent_readable_templates_v2.md docs/plans/agent_readable_templates_v2.md
```

If `docs/plans/` doesn't exist, create it first:
```bash
mkdir -p docs/plans
mv docs/historical/agent_readable_templates_v2.md docs/plans/agent_readable_templates_v2.md
```

### 📋 Clean contract for `docs/historical/` going forward

All files in `docs/historical/` should satisfy **all three** criteria:

1. **Status header exists** — `**Status:** Historical ...` after the title, matching `comfyscript_spike.md`'s convention.
2. **Content is retrospective** — describes something already decided, completed, or superseded. No active-phase status trackers, no "not started" items, no "Effort: N hours" actionable tickets.
3. **README listing is up to date** — every file present has a link; every link resolves; no file contradicts the "archived spikes, old rationale, completed follow-up plans" contract.

**Deferred improvement for `template_cleanup_followups.md`:** Its §§B.1–B.4 (failing tests, error graft, scratchpad emitter) are actionable work items with hour estimates — they'd be better moved to `.megaplan/ideas/` or `docs/plans/`. The cosmetic §§A.1–A.3 are genuinely historical (opinions captured, no longer current). If you'd like, I can help split that file.