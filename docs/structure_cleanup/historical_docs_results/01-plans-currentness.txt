Now I have a complete picture. Here is the audit.

---

## Audit Results: `docs/plans/` Boundary

### Classification Legend

| Label | Meaning |
|---|---|
| **Current plan (active)** | Forward-looking work not yet executed, or still being worked through |
| **Current plan (partial exec)** | In-progress execution with some steps confirmed done |
| **Completed execution record** | All steps executed, document is now a historical trace |
| **Superseded/abandoned** | Replaced by a newer plan or no longer relevant |
| **Active project context** | Index, reference, or framing doc that helps navigate the project |

---

### Files

| # | Exact Path | Classification | Recommended Action | Rationale |
|---|---|---|---|---|
| 1 | `docs/plans/README.md` | **Active project context (index)** | **Stay in `docs/plans/`** — consider adding execution-status badges to entries | Frames the directory as "useful as project context but no longer top-level." Serves as a table of contents. No stale forward-references found. |
| 2 | `docs/plans/plan_v2.md` | **Current plan (active)** | **Stay in `docs/plans/`** — add a status header (`Status: In Progress / Queued / Blocked`) to the top of the file | "Implementation Plan: M3 — Corpus proof + polish." Forward-looking with 5 phases, 5 steps, detailed execution/validation order. No completion markers — work is either underway or queued. Content is specific and actionable. |
| 3 | `docs/plans/revised_plan.md` | **Current plan (partial exec)** | **Stay in `docs/plans/`** — update the status table to reflect current reality; either mark remaining steps done or add a "Remaining" section | "M0 — Tier 1: All 11 LOW-risk reorg units." Dated 2026-06-11. Has a verified-current-state table: Units 1,3,5 are ✅ DONE; Units 2,11 are Partial; Units 4,6,7,8,9,10 are NOT done. Steps 4 and 6 are individually ✅ DONE inside the plan text. This is an in-flight execution document, not a completed record. |
| 4 | `docs/plans/finalize.json` | **Current plan (active)** | **Stay in `docs/plans/`** — label as "Next-batch execution manifest" in the README; consider adding a status line | JSON execution manifest with 13 tasks (T1–T13) covering Units 14 (comfy_nodes agent/ package), 12 (_compile bundle), 15 (loader move). All tasks show `"status": "pending"`. Contains dependency chains, watch items, and sense checks. Clearly the next batch of work after the M0 reorg completes. |
| 5 | `docs/plans/loose-work-consolidation-plan.md` | **Current plan (active / one-shot)** | **Stay in `docs/plans/`** with a note in the README that it is a branch-consolidation plan; move to `docs/historical/` **once executed** | Dated 2026-06-09. Describes a discrete branch-cleanup operation (L1–L4 keep-and-land, leave-alone branches, delete list). Ephemeral by nature — once the consolidation is merged, this becomes a historical record. Currently still valuable for tracking what branches to land/delete. |

---

### Summary

**All 4 content files + the README are correctly placed in `docs/plans/`.** None are stale enough to move to `docs/historical/` yet. The directory's stated purpose ("still useful as project context but no longer at top") fits every file.

### Recommendations (no file edits)

1. **Add a status row** at the top of each plan file (`Status: Active / Partial / Queued / Executed`) so future readers know immediately whether the plan is live or done.

2. **Update the README** when `loose-work-consolidation-plan.md` has been executed — it should then move to `docs/historical/`.

3. **No files need to move** to `docs/structure_cleanup/` — the constraint says to treat that directory as audit evidence only, which it already is.

4. **No provenance loss risk** — all authored plans stay in their authored locations with their history intact. `docs/historical/` is an option only for post-execution archiving.