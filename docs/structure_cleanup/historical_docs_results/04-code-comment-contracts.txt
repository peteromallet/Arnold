I now have the complete picture. Here is my detailed analysis and recommendation.

---

## Analysis

### The file itself: `docs/plans/plan_v2.md`

The file contains **"M3 — Corpus proof + polish (P3 + P2)"** — an EditSession corpus-testing plan (corpus schema adapter, byte-identity harness, const-fold polish, etc.). It has **zero** content about:
- Runtime-backed intent nodes (what `RUNTIME_CONTRACT.md` claims it contains)
- Lowering design decisions (what `lowering.py` claims it contains)

The cleanup audit already flagged this: `docs/structure_cleanup/docs_reference_results/03-plans-and-historical-status.txt` (line 69) correctly recommends moving it to `docs/historical/m3-corpus-plan.md`.

### Reference 1: `RUNTIME_CONTRACT.md` line 4

```
**Plan**: `s6-runtime-backed-intent-nodes` ([docs/plans/plan_v2.md](../../docs/plans/plan_v2.md)).
```

The plan_v2.md has nothing about sprint 6 runtime intent nodes. The actual strategic discussion of runtime-backed intent nodes lives in:

- **`docs/text-to-graph/mvp.md`** §Sprint 6 (lines 1335–1351) — strategic "deliver only if still needed" framing
- **`docs/runtime/code_modes.md`** — execution mode definitions (already cross-linked by RUNTIME_CONTRACT on line 5)
- **`docs/runtime/lifecycle.md`** — session lifecycle, warm policy, memory profiles
- **`docs/runtime/surface.md`** — CLI/HTTP/embedded surface, "Decisions" section
- **`docs/runtime/README.md`** — index of all runtime docs
- **`docs/architecture/vibecomfy_exec_node_design.md`** — design for `vibecomfy.exec` (the full-power code node, separate from sandboxed `vibecomfy.code`; postdates the contract)

The `docs/runtime/README.md` is the natural entry point for "the runtime design docs" — it indexes the authoritative runtime documentation set.

### Reference 2: `lowering.py` lines 7–11

```python
Design decisions (see docs/plans/plan_v2.md):
- Lowering is atomic: any unsupported loop fails the entire lower stage.
- Supported loops: bounded literal seed/prompt/text sweeps only.
- Unsupported: runtime-dependent counts, dynamic termination, unresolved
  variable expressions, and unsupported variable names.
```

The four decisions are **already enumerated inline** in the docstring. The `(see docs/plans/plan_v2.md)` is pure provenance. The closest doc that discusses lowering strategy is `docs/text-to-graph/mvp.md` §Sprint 5 (lines 1318–1333), which describes the goals ("bounded loop unroll to repeated native nodes / subgraph") but does **not** contain these specific implementation rules — they are properly documented at the code level.

---

## Recommendation

### 1. Move the file

`docs/plans/plan_v2.md` → `docs/historical/m3-corpus-plan.md`

(Not my task to execute, but the necessary precondition for the reference edits below.)

### 2. Edit `RUNTIME_CONTRACT.md` line 4

Replace the stale `docs/plans/plan_v2.md` link with `docs/runtime/README.md` — the runtime design docs index. The sprint name label is kept as provenance.

**Before:**
```
**Plan**: `s6-runtime-backed-intent-nodes` ([docs/plans/plan_v2.md](../../docs/plans/plan_v2.md)).
```

**After:**
```
**Plan**: `s6-runtime-backed-intent-nodes` ([docs/runtime/README.md](../../docs/runtime/README.md)).
```

### 3. Edit `lowering.py` lines 7–8

Drop the `(see docs/plans/plan_v2.md)` reference since the decisions are self-contained and plan_v2.md has no relevant content.

**Before:**
```python
Design decisions (see docs/plans/plan_v2.md):
```

**After:**
```python
Design decisions:
```

(Or, if you prefer a doc pointer: `Design decisions (see docs/text-to-graph/mvp.md §Sprint 5):` — but this adds a cross-reference with no extra information since the rules are already enumerated.)

### 4. Update `docs/plans/README.md`

If the file moves to historical, remove the `plan_v2.md` entry from the index:
```
- [M3 corpus proof and polish](plan_v2.md)
```
And add it to `docs/historical/README.md`.

### Summary table

| File | Current reference | Correct target | Action |
|------|-------------------|----------------|--------|
| `docs/plans/plan_v2.md` | (no callers match its content) | `docs/historical/m3-corpus-plan.md` | Move file |
| `RUNTIME_CONTRACT.md:4` | `docs/plans/plan_v2.md` | `docs/runtime/README.md` | Update link |
| `lowering.py:7` | `docs/plans/plan_v2.md` | (none needed — self-contained) | Remove reference |
| `docs/plans/README.md` | Lists plan_v2.md as active | Remove entry | Update index |
| `docs/historical/README.md` | (may need entry) | Add m3-corpus-plan.md | Update index |