Working directory: /Users/peteromalley/Documents/reigh-workspace/vibecomfy

Audit the conceptual boundary between tracked `scripts/` and `tools/`.

Return:

1. The simplest rule for what belongs in `scripts/`.
2. The simplest rule for what belongs in `tools/`.
3. Files that currently violate the boundary.
4. A low-risk first cleanup batch.

Use references/tests before recommending moves. Under 600 words.
