Working directory: /Users/peteromalley/Documents/reigh-workspace/vibecomfy

Find deletion candidates under `scripts/` and `tools/`.

Classify:
- safe delete now
- delete only after approval
- keep

Pay attention to `.DS_Store`, `__pycache__`, ignored `tools/_legacy/`, and
tracked one-off scripts. Under 600 words.
