Confirmed from file contents: `runtime/eval/__init__.py` re-exports `compile_eval_subgraph` from `.core`, and `commands/runtime.py:12` does `from vibecomfy.runtime.eval import compile_eval_subgraph`. Python resolves `runtime.eval` to the package (directory with `__init__.py`), not the module (`eval.py`). The flat file is completely shadowed.

Here is the audit.

---

## Runtime Audit — Deletion-First Cleanup

### Current State

Four flat files at `vibecomfy/runtime/` have near-identical duplicates inside `vibecomfy/runtime/eval/`. The plan (per `revised_plan.md`) was to turn the flat files into thin re-export shims, but **that step was never executed** — both sides are still fully populated clones.

### Candidate Table

| Path | Verdict | Evidence | Risk | Verification |
|---|---|---|---|---|
| `runtime/eval.py` | **DELETE** | Shadowed by `runtime/eval/` package. `from vibecomfy.runtime.eval import compile_eval_subgraph` (used by `commands/runtime.py:12`) resolves to `eval/__init__.py` → `eval/core.py`, not this file. 437 lines identical to `eval/core.py` (401 lines, minor docstring diff only). | **None**. Python never loads this module. | `python -c "from vibecomfy.runtime.eval import compile_eval_subgraph; print(compile_eval_subgraph.__module__)"` → `vibecomfy.runtime.eval.core` |
| `runtime/eval_plan.py` | **DELETE** (replace with 1-line shim) | Content identical to `eval/plan.py` (187 lines, same functions/dataclass). Live importers: `runtime/eval_prompt.py:11` (transitive — itself a deletion candidate), `test_agentic_affordances.py:25` (`from vibecomfy.runtime.eval_plan import plan_eval_node`). | **Low**. Replace body with `from vibecomfy.runtime.eval.plan import *`. Test and `eval_prompt.py` keep working. | Shim: `python -c "from vibecomfy.runtime.eval_plan import plan_eval_node; print('OK')"` |
| `runtime/eval_prompt.py` | **DELETE** (replace with shim, then fix test) | Content identical to `eval/prompt.py` (146 lines). Live importer: `test_agentic_affordances.py:12` (`import vibecomfy.runtime.eval_prompt as eval_prompt_module`). Test monkeypatches `eval_prompt_module.comfy_server`, `.ComfyClient`, `._wait_for_server_history` (lines 205–207) then calls `eval_prompt_module.eval_node` (line 209). | **Moderate**. A `from .eval.prompt import *` shim makes names available but monkeypatches hit the shim namespace, not `eval.prompt`'s. `eval_node` uses `comfy_server` from `vibecomfy.runtime.eval.prompt` globals, so patches would silently no-op. **Fix**: update test to patch `vibecomfy.runtime.eval.prompt` instead. | After fix: `python -c "from vibecomfy.runtime.eval.prompt import eval_node; print('OK')"` |
| `runtime/preview_types.py` | **DELETE** (replace with 1-line shim) | Content identical to `eval/preview_types.py` (83 lines). Only live importer: `runtime/eval_plan.py:8` (itself a deletion candidate). | **Low**. Replace body with `from vibecomfy.runtime.eval.preview_types import *`. If `eval_plan.py` is also shimmed, update its import to `.preview_types` (it already does in `eval/plan.py:8`). | Shim: `python -c "from vibecomfy.runtime.preview_types import preview_plan_for_type; print('OK')"` |

### Cross-Reference Map (reduced to live code paths)

```
commands/runtime.py:12  ──→  runtime/eval/__init__.py  ──→  runtime/eval/core.py
                                                                      ↑
tests/test_runtime_run.py  ──→  (same path, 5 uses)                  │
                                                                      │
tests/test_agentic_affordances.py:25  ──→  runtime/eval_plan.py ──→  runtime/eval/plan.py (eventually)
                                                                      ↑
tests/test_agentic_affordances.py:12  ──→  runtime/eval_prompt.py ──→ runtime/eval/prompt.py (eventually)
                                                                      ↑
runtime/eval_plan.py:8  ──→  runtime/preview_types.py  ──→  runtime/eval/preview_types.py (eventually)
```

### Summary

| File | Now | To Do |
|---|---|---|
| `runtime/eval.py` | Dead (shadowed) | Delete — zero risk |
| `runtime/eval_plan.py` | Duplicate, 2 importers | Replace with `from vibecomfy.runtime.eval.plan import *` |
| `runtime/eval_prompt.py` | Duplicate, 1 test importer using monkeypatch | Replace with shim **and** update test to patch `vibecomfy.runtime.eval.prompt` |
| `runtime/preview_types.py` | Duplicate, 1 internal importer | Replace with `from vibecomfy.runtime.eval.preview_types import *` |

**Total LOC removed after shim-ification**: ~853 lines (437 + 187 + 146 + 83) replaced by 4 lines of re-exports. The canonical code lives only in `runtime/eval/{core,plan,prompt,preview_types}.py` with `__init__.py` as the barrel.