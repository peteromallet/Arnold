Now I have the full picture. Here is the contract map.

---

## Runtime Cleanup Contract Map

### Source Files — Live / Dead Classification

| Path | Status | Evidence |
|---|---|---|
| `vibecomfy/runtime/eval.py` (flat) | **DEAD** — shadowed by `runtime/eval/` package | `commands/runtime.py:12` imports `compile_eval_subgraph` — Python resolves to package `eval/__init__.py` → `eval/core.py`. Flat file is never loaded. |
| `vibecomfy/runtime/eval_plan.py` (flat) | **DEAD** — unreachable from production | Only importers: `eval_prompt.py:11` (itself dead) and `tests/test_agentic_affordances.py:25` (not running — broken import per 05-runtime-exec.md). The richer design lives in `eval/plan.py`. |
| `vibecomfy/runtime/eval_prompt.py` | **DEAD** — no production callers | Only importer is `tests/test_agentic_affordances.py:12`. That test's own import of `plan_eval_node` from `eval` (line 25) is factually wrong — would `ImportError`. |
| `vibecomfy/runtime/watchdog_runtime.py` | **DEAD** — zero live importers | `grep` across `vibecomfy/` finds zero references. The real watchdog lives in `runtime/watchdog.py`. Noted as drift in docs. |
| `vibecomfy/runtime/server_process.py` | **LIVE** — two internal importers | `runtime/config.py:141` imports `_comfyui_executable`; `runtime/server.py:10` imports `_spawn_comfy_server`. Both reachable via `session.py` and `run.py`. |

### Test Files — True Live Contracts (13 files)

These **must pass** after any runtime cleanup:

1. **`test_runtime_session_embedded.py`** (351 lines) — EmbeddedSession lifecycle: reuse, flush, reconfigure, auto-flush, warm policy, stop-with-inflight, reload, concurrent-run rejection.
2. **`test_runtime_session_server.py`** (216 lines) — ServerSession lifecycle: start (CLI arg mapping), two-runs-share-process, flush (api/free), reconfigure restart, stop via SIGTERM→kill, reload.
3. **`test_runtime_session_config.py`** (688 lines) — SessionConfig construction, memory profile overlay (5 profiles), server argv generation, embedded config round-trip, IO directories, run metadata (artifacts, origins, chain fields).
4. **`test_runtime_session_validation.py`** (431 lines) — Schema validation, unknown class types, code contract enforcement, warm schema provider.
5. **`test_runtime_session_run_untracked.py`** (751 lines) — One-shot untracked runs, URL validation, untracked vs tracked routing.
6. **`test_runtime_run.py`** (1485 lines) — Full orchestrator `run()` / `run_embedded()` / `run_sync()` / `run_embedded_sync()`: server startup ordering, embedded startup, validation-before-queue, queue failure surfacing, managed vs external server config, cleanup bug absorption (hiddenswitch `NoneType`, `comfy-kitchen`), output resolution, chain forwarding.
7. **`test_runtime_execution.py`** (161 lines) — Primitive queue helpers: `queue_embedded_prompt`, `queue_server_prompt`, `collect_output_paths`, `normalize_prompt_id`, error wrapping (typed vs asyncio timeout).
8. **`test_runtime_eval_absence.py`** (54 lines) — **Critical contract**: asserts zero live code imports `vibecomfy.runtime.eval` or submodules. Scans entire repo. This directly blocks keeping flat `eval.py`/`eval_plan.py`/`eval_prompt.py` accessible through imports.
9. **`test_runtime_ensure_env.py`** (930 lines) — Environment setup, pack installation, workflow loading.
10. **`test_runtime_code_modes.py`** (236 lines) — Agent runtime code dispatch (different subsystem; not session/runtime cleanup).
11. **`test_runtime_model_policy.py`** (109 lines) — Model preflight policy resolution.
12. **`tests/security/test_runtime_code_policy.py`** (131 lines) — Code execution policy validation.
13. **`tests/edgecases/test_runtime_failures.py`** (72 lines) — Failure message clarity.

**Likely broken / not running:** `tests/test_agentic_affordances.py` — imports `plan_eval_node` from `vibecomfy.runtime.eval` (line 25), which is wrong; the function lives in `eval_plan.py`. The audit doc (05-runtime-exec.md) flags this as "guaranteed `ImportError`" with "no CI is actually running this test."

### Smoke Tests — No Runtime Constraints

`tests/smoke/` has 7 files (RunPod + image-only). Zero `*runtime*` files. No constraint on runtime cleanup.

### Documentation — Historical / Agenda (No Contract Weight)

| Path | Classification | Rationale |
|---|---|---|
| `docs/structure_cleanup/runtime_briefs/01-.. 05-.. 06-.. 07-.. 08-.. 09-.. 10-..` | **AGENDA docs** — agent prompts for investigation | Describe cleanup tasks, don't encode contracts. Can be updated or archived. |
| `docs/structure_cleanup/package_briefs/04-runtime-subsystem.md` | **AGENDA doc** | Same pattern. |
| `docs/structure_cleanup/tests_briefs/05-runtime-and-smoke-tests.md` | **AGENDA doc** | Same pattern. |
| `docs/structure_cleanup/runtime_results/01-delete-runtime-shims.txt` | **OUTPUT doc** — agent findings | Confirms `eval.py` is shadowed (page 1). Informative, not binding. |
| `docs/structure_cleanup/runtime_results/02-eval-package-split.txt` | **OUTPUT doc** | Analysis of eval ambiguity. |
| `docs/structure_cleanup/runtime_results/05-drift-watchdog-runtime.txt` | **OUTPUT doc** | Confirms `watchdog_runtime.py` is drift. |
| `docs/structure_cleanup/dead_module_results/10-safe-first-batch.txt` | **OUTPUT doc** | Batch plan referencing `watchdog_runtime`. |
| `docs/structure_cleanup/status.md` | **STATUS doc** | Mentions `watchdog_runtime` as drift. |
| `docs/megaplan_chains/pristine_cleanup/audit/05-runtime-exec.md` | **HISTORICAL AUDIT** | Detailed findings (overlapping eval, broken test import, triplication). Valuable context, not a contract. |
| `docs/megaplan_chains/excellence_epic/m6a-runtime-factory.md` | **HISTORICAL PLAN** | Past epic, can be archived. |
| `docs/plans/revised_plan.md` | **HISTORICAL PLAN** | Mentions eval shim strategy. Out of date. |

**`docs/runtime/` directory does not exist** — no runtime docs to constrain there.

---

### Minimal Focused Test Commands

After cleanup (e.g., deleting/replacing flat `eval.py`, `eval_plan.py`, `eval_prompt.py`, `watchdog_runtime.py`):

```bash
# 1. The critical absence contract (must be first)
python -m pytest tests/test_runtime_eval_absence.py -v

# 2. Session lifecycle contracts (both embedded and server)
python -m pytest tests/test_runtime_session_embedded.py -v
python -m pytest tests/test_runtime_session_server.py -v
python -m pytest tests/test_runtime_session_config.py -v
python -m pytest tests/test_runtime_session_validation.py -v
python -m pytest tests/test_runtime_session_run_untracked.py -v

# 3. Execution primitives
python -m pytest tests/test_runtime_execution.py -v

# 4. Full run orchestration (heaviest — covers cleanup absorption)
python -m pytest tests/test_runtime_run.py -v

# 5. Ensure env (pack installation path)
python -m pytest tests/test_runtime_ensure_env.py -v

# 6. Confirm flat eval modules are gone (import-resolve check)
python -c "
from vibecomfy.runtime.eval import compile_eval_subgraph
print('eval module:', compile_eval_subgraph.__module__)
# Should print: vibecomfy.runtime.eval.core (not vibecomfy.runtime.eval directly)
"

# 7. Verify dead files can't be imported
python -c "
import sys, importlib
for mod in ['vibecomfy.runtime.watchdog_runtime',
            'vibecomfy.runtime.eval_plan',
            'vibecomfy.runtime.eval_prompt']:
    try:
        importlib.import_module(mod)
        print(f'WARNING: {mod} still importable')
    except ModuleNotFoundError:
        print(f'OK: {mod} gone')
"
```

The **essential minimal** set (post-deletion safety net):

```bash
python -m pytest tests/test_runtime_eval_absence.py tests/test_runtime_session_embedded.py tests/test_runtime_session_server.py tests/test_runtime_session_config.py tests/test_runtime_execution.py -v
```