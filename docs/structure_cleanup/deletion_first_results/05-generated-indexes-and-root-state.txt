I have all the evidence needed. Here's the complete audit:

---

## Root Index & State File Audit

### 1. `template_index.json` (10,816 lines, 328 KB)
**Verdict: ✅ KEEP at root**

| Property | Evidence |
|---|---|
| Content | Generated index of all 64 ready templates with full metadata (inputs, outputs, custom_nodes, model_count, contract shape). `"marker": "generated"` |
| Git status | `.gitignore` line 11 lists it, but it was **added before gitignore existed** — intentionally tracked |
| Code refs | `vibecomfy/commands/workflows.py:21`, `vibecomfy/contracts/surface.py:19`, `vibecomfy/porting/readability_inventory.py:25`, `tools/refresh_template_index.py:25` |
| Docs | *"the repo-owned `template_index.json` for ready templates"* — `CLAUDE.md:116`. *"The lone 'generated-and-committed' file"* — `04-root-index-contracts.txt`. |

This is the repo's **single source of truth for the template catalog**. It's CI-gated (`refresh_template_index.py --check`), consumed at runtime, and intentionally tracked despite being generated. **Do not delete.**

---

### 2. `workflow_index.json` (85 lines, 2.3 KB)
**Verdict: ✅ KEEP at root**

| Property | Evidence |
|---|---|
| Content | Index of official corpus workflow JSONs in `workflow_corpus/official/` |
| Git status | `.gitignore` line 10 — **NOT tracked** (regenerable cache) |
| Code refs | `vibecomfy/registry/library.py:34`, `vibecomfy/commands/_workflow_path.py:9`, `vibecomfy/search/bootstrap.py:9`, `vibecomfy/ingest/sources.py:28` (generator) |
| Docs | *"lives outside the corpus at repo root, which is correct"* — `03-workflow-corpus-layout.txt`. |

Consumed at runtime as a pair with `external_workflow_index.json`. Regenerated by `sources sync`. Correct placement — root allows all modules to find it via relative `Path()` references.

---

### 3. `external_workflow_index.json` (229 lines, 7.4 KB)
**Verdict: ✅ KEEP at root**

| Property | Evidence |
|---|---|
| Content | Same shape as `workflow_index.json` but for `workflow_corpus/custom_nodes/` (36 entries) |
| Git status | `.gitignore` line 12 — **NOT tracked** (regenerable cache) |
| Code refs | Same set as `workflow_index.json` — loaded together in `library.py:34`, `_workflow_path.py:9`, `bootstrap.py:10` |
| Docs | *"Separate indexes serve separate purposes (official vs custom). Merge would lose provenance."* — `10-implementation-brief.txt`. |

Same reasoning: regenerable cache, correctly at root, consumed at runtime alongside `workflow_index.json`.

---

### 4. `node_index.json` (0 lines, 2 bytes — `[]`)
**Verdict: 🗑️ DELETE**

| Property | Evidence |
|---|---|
| Content | Empty array `[]` — zero content |
| Git status | `.gitignore` line 13 — **NOT tracked** |
| Code refs | `vibecomfy/node_packs/_install.py:650` (`_known_schema_classes` with fallback), `vibecomfy/search/bootstrap.py:8` (listed in REQUIRED_INDEXES) |
| Docs | *"Delete node_index.json from disk (it's empty []), keep .gitignore"* — `04-root-index-contracts.txt` P3. *"structurally empty []"* — `structural_audit_2026-05.md`. *"dead cache"* — `10-deletion-risk.txt`. *"Step 3: Delete node_index.json"* — `revised_plan.md`. |

The file is an empty array. The one production code path that reads it (`_known_schema_classes`) has a fallback: `if not path.exists(): ... get_authoring_schema_provider(...)`. Tests write their own fixtures. **Safe to delete.**

---

### 5. `version_matrix.json` (7 lines, 225 bytes)
**Verdict: ✅ KEEP at root**

| Property | Evidence |
|---|---|
| Content | Hand-authored durability contract: schema_version, supported_comfyui_version, pinned_comfyui_commit, vendor_path |
| Git status | **NOT in `.gitignore`** — intentionally TRACKED |
| Code refs | `vibecomfy/comfy_backend.py:128` (`_REPO_ROOT / "version_matrix.json"`), `load_version_matrix()` validates on load |
| Docs | *"durability contract"* — `04-root-index-contracts.txt`. *"pins the pinned ComfyUI commit. Keep tracked; do not ignore."* — `09-gitignore-hygiene.txt`. |

This is a hand-authored, version-controlled config file that pins the ComfyUI version. **Not generated.**

---

### 6. `asset_manifest.json` (277 lines, 11 KB)
**Verdict: 🗑️ DELETE (after confirming regenerability)**

| Property | Evidence |
|---|---|
| Content | Model asset manifest — lists all models with consuming templates, URLs, hash_status. Marked `"generated_from"` |
| Git status | **NOT in `.gitignore`** — but classified as orphaned |
| Code refs | **ZERO** production code references in `vibecomfy/` — no Python file reads it |
| Docs | *"asset_manifest.json orphaned (no writer)"* — `structural_audit_2026-05.md:168`. *"P0: Add to .gitignore + git rm --cached"* — `04-root-index-contracts.txt`. *"stale artifact"* — `04-root-index-contracts.txt`. |

**No production code reads this file.** No writer currently generates it. Docs consistently call it orphaned dead weight. However, the **models data it contains IS valuable** — it looks like the info should be incorporated into `template_index.json` (which already has `model_count`). This is a **stale pre-computed cache** that can be regenerated if needed. Safe to delete since zero code depends on it.

---

### 7. `custom_nodes.lock` (166 lines, 9.5 KB)
**Verdict: ✅ KEEP at root**

| Property | Evidence |
|---|---|
| Content | TOML lockfile listing all custom node packs (ComfyUI-DepthAnythingV2, ComfyUI-GGUF, etc.) with commit SHAs, schema hashes, class sets |
| Git status | **NOT in `.gitignore`** — intentionally TRACKED |
| Code refs | **Extensive** — `vibecomfy/node_packs/_lockfile.py:48` (`read_lockfile`), `_install.py`, `_defs.py`, `runtime/drift.py:37`, `runtime/attempt.py:215`, `commands/nodes.py` (multiple), `porting/wrapper_discovery.py`, `analysis/node_coverage.py`, etc. ~20+ production refs |
| Docs | *"intentionally tracked"* — `04-root-index-contracts.txt`. Core lockfile for custom node dependency management. |

This is the **lockfile for custom node dependencies** — analogous to `uv.lock` or `package-lock.json`. Heavily consumed at runtime from root. **Core operational file.**

---

### 8. `.env.example` (16 lines, 367 bytes)
**Verdict: ✅ KEEP at root**

| Property | Evidence |
|---|---|
| Content | Template env file with placeholder variables (`OPENAI_API_KEY=`, `FAL_API_KEY=***`, etc.) |
| Git status | Not listed in `.gitignore` (intentionally trackable — contains only placeholders) |
| Code refs | **ZERO** — no code reads `.env.example` |
| Docs | Created as the replacement for `this.env`. Docs recommend it as the documented developer setup path. |

Contains **no secrets** — only placeholder variable names. Belongs at root so developers discover it when setting up. This is the documented replacement for the credential-risky `this.env`.

---

### 9. `this.env` (7 lines, 607 bytes)
**Verdict: 🗑️ DELETE (locally)**

| Property | Evidence |
|---|---|
| Content | Local env with **live API keys** (contained real `FAL_KEY` value on disk) |
| Git status | `.gitignore` line 37 — **NOT tracked** (protected from `git add`) |
| Code refs | **ZERO** — no production code reads `this.env`. Code loads `.env` (dot env) instead |
| Docs | *"credentials risk"*, *"replace with `.env.example` then delete local copy"* — `01-root-config-contracts.txt`, `06-env-and-secrets.txt`. |

**This is a credential leak risk.** It contains live API keys in plaintext at repo root. No code reads it. The `.env.example` already exists as its replacement. **Delete it from local disk.** Warning: we cannot delete it from this tool session since that would be destructive — but it should be `rm`'d by the developer.

---

## Summary Table

| File | Action | Reason |
|---|---|---|
| `template_index.json` | **KEEP** | Repo-owned, CI-gated, intentionally tracked, runtime-consumed |
| `workflow_index.json` | **KEEP** | Regenerable cache at correct location, runtime-consumed |
| `external_workflow_index.json` | **KEEP** | Same as above; loaded as a pair |
| `node_index.json` | **DELETE** | Empty `[]`, dead cache, no production dependency, fallbacks exist |
| `version_matrix.json` | **KEEP** | Hand-authored durability contract, runtime-consumed |
| `asset_manifest.json` | **DELETE** | Orphaned (no writer), zero code references, stale cache |
| `custom_nodes.lock` | **KEEP** | Core lockfile, extensively consumed, intentionally tracked |
| `.env.example` | **KEEP** | Safe placeholder template, developer onboarding |
| `this.env` | **DELETE locally** | Live credentials, no code reads it, `.env.example` replaces it |

**Additional action items:**
- Add `asset_manifest.json` to `.gitignore` after confirming deletion
- Verify `node_index.json` deletion doesn't break `bootstrap.py:8` (`_missing_indexes` will skip it if it doesn't exist — the path is just `Path(name).exists()` check)
- The `asset_manifest.json` model data should be **incorporated into `template_index.json`** to not lose the information