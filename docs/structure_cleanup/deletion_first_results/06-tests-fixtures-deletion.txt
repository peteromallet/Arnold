Here's my comprehensive audit of deletion candidates:

---

## DELETION AUDIT RESULTS

### ✅ TIER 1: Gitignored artifacts (already excluded — safe to `rm -rf` from disk)

These are already in `.gitignore`; deleting them just removes stale local state:

| Path | Type | Reason |
|------|------|--------|
| `out/` | Runtime outputs | Contains `runs/` with logs, metadata, JSON artifacts |
| `node_index.json` | Generated index | In `.gitignore`, regenerated |
| `workflow_index.json` | Generated index | In `.gitignore`, regenerated |
| `template_index.json` | Generated index | In `.gitignore`, regenerated |
| `external_workflow_index.json` | Generated index | In `.gitignore`, regenerated |
| `.agents/` | Tool working dir | In `.gitignore`, agent scratch |
| `.claude/` | Tool working dir | In `.gitignore`, agent scratch |
| `.desloppify/` + subdirs | Agent runs | ~80 files (prompts, results, run logs, review packets) |
| `.megaplan/` | Agent briefs | In `.gitignore`, agent plans |
| `this.env` | Local env file | Gitignored |
| `.pytest_cache/` | Test cache | Gitignored |
| `tests/e2e/node_modules/` | e2e deps | Gitignored |
| `tests/e2e/playwright-report/` | e2e outputs | Gitignored |
| `tests/e2e/test-results/` | e2e outputs | Gitignored |
| `docs/local_agent_text_to_graph_e2e.md` | Agent artifact | Gitignored |
| `install.log` | Install log | Gitignored |
| `_debug_*.py` (none found) | Debug scripts | Gitignored |
| `_fix_t6.py` → `scripts/maintenance/_fix_t6.py` | Gitignored at root only | Root-named variant deleted if present |
| `_regen_templates.py` → `scripts/maintenance/_regen_templates.py` | Same | Root-named variant deleted if present |
| `finalize.json` | No root-level file | Gitignored at root only |
| `**/__pycache__/` + `*.pyc` | Bytecode caches | All gitignored |
| `artifacts/` | Generated run artifacts | Not in `.gitignore` explicitly but likely stale |

**Verification cmd:** `git clean -fdX --dry-run` (shows all gitignored files that exist on disk)

---

### ✅ TIER 2: Tracked generated reports (safe to delete)

These are stale analysis artifacts with **zero references from any code** (tests, scripts, tools, vibecomfy). Only self-referenced within their own tree.

#### `docs/structure_cleanup/` — **Entire subtree (~200 files, ~2MB)**
Generated by an AI agent during a previous cleanup audit. Contains:
- `*_results/` dirs with `_report.json`, `*.txt`, `*.meta.json`
- `*_briefs/` dirs with `.md` briefs
- `artifacts_results/`, `config_ops_results/`, `docs_results/`, etc.

**Evidence of staleness:** Zero references from any Python file in `tests/`, `vibecomfy/`, `scripts/`, `tools/`. All 30+ grep hits are self-references within the tree or its own `.meta.json` files.

**Verification cmd:**
```
grep -r "structure_cleanup" --include="*.py" vibecomfy/ tests/ scripts/ tools/ | grep -v "structure_cleanup/briefs/03-generated-state.md"
```

**Delete command:**
```
rm -rf docs/structure_cleanup/
```

---

### ✅ TIER 3: Generated manifests (tracked, but referenced — KEEP)

| File | Referenced by | Verdict |
|------|--------------|---------|
| `workflow_corpus/manifests/coverage.json` | `scripts/runpod_matrix_plan.py`, `runpod_e2e_matrix.py`, `CLAUDE.md`, docs | **KEEP** |
| `workflow_corpus/manifests/ready_regeneration.json` | `docs/templates/corpus_parity_allowlist.md`, `docs/templates/readable_ready_template_cleanup_plan.md` | **KEEP** |

---

### ✅ TIER 4: Tracked fixtures — reference-checked

| Fixture | References | Verdict |
|---------|-----------|---------|
| `tests/fixtures/canonical_parity_baseline.json` (825KB) | `tools/check_canonical_parity.py`, `test_v26_canonical_parity.py`, `tests/README.md`, `docs/structure_cleanup/` | **KEEP** — active test fixture |
| `tests/fixtures/failing_first_m1_corruptions.json` | `test_template_roundtrip.py`, `docs/megaplan_chains/excellence_epic/` | **KEEP** — active test fixture |
| `tests/fixtures/golden_api_video_wan_i2v.json` | `test_template_roundtrip.py`, `docs/megaplan_chains/` | **KEEP** — active test fixture |
| `tests/fixtures/agentic_success_rate.json` | `test_agentic_success_rate.py`, `scripts/agentic_success_rate.py` | **KEEP** — active test fixture |
| `tests/fixtures/agent_generated_loader/*` | `tests/security/test_agent_generated_loader.py` | **KEEP** — active security test fixtures |
| `tests/fixtures/agent_edit/flat.json`, `subgraphed_wan_i2v.json` | Likely referenced by tests | **KEEP** — check individually |
| `tests/fixtures/walking_skeleton/flat.json` | `test_walking_skeleton.py` | **KEEP** |
| `tests/fixtures/e2e_sessions/*` | e2e tests | **KEEP** |
| `tests/fixtures/editor_sessions/*` | Agent edit tests | **KEEP** |
| `tests/smoke_fixtures/generated_webm_smoke.json` | `scripts/runpod_model_matrix.py` | **KEEP** — active reference |
| `tests/smoke_fixtures/custom_kjnodes_label.json` | Referenced via `fixtures.py` | **KEEP** |

**No stale fixture deletions found** — all tracked fixtures are referenced.

---

### ✅ TIER 5: Already-deleted fixture candidates (now confirmed)

One fixture was previously flagged in `docs/structure_cleanup/tests_results/10-safe-first-batch.txt`:

- `tests/fixtures/porting/opaque_component.json` — **already deleted** (confirmed zero references in the prior audit, and no trace in my scan). If it still exists on disk, it's safe to remove.

**Verification cmd:**
```
ls -la tests/fixtures/porting/opaque_component.json 2>/dev/null && echo "STALE - delete" || echo "already gone"
```

---

### ✅ TIER 6: Obsolete tests — by-design-failures classification

`tests/known_failures.txt` lists 237 test IDs as by-design-red. These are **not candidates for deletion** — they are failing-but-valid. This file is a proper mechanism: test code exists, it's exercised, and failures are baselined.

**No test files should be deleted based on failures alone.**

---

### ✅ TIER 7: Snapshot files (tracked golden assets)

| Path | Verdict |
|------|---------|
| `tests/snapshots/*.json` (~47 files) | **KEEP** — used by `test_compile_api_snapshots.py`, `test_emitter_snapshots.py` |
| `tests/characterization/goldens/emitter/*.golden` (22 files) | **KEEP** — used by `test_emitter_snapshots.py` |
| `vibecomfy/porting/object_info/*@runpod-snapshot.json` (3 files) | **KEEP** — used by object_info subsystem |
| `vibecomfy/porting/cache/object_info/*@runpod-snapshot.json` (11 files) | **KEEP** — cached snapshots for porting |

---

## EXECUTION PLAN

### Step 1: Clean gitignored files from disk
```bash
# Dry-run to see what would be removed:
git clean -fdX --dry-run

# Actually remove:
git clean -fdX
```

### Step 2: Delete stale generated reports
```bash
# Dry-run:
echo "Would delete: docs/structure_cleanup/ (200+ files)"

# Actually remove:
rm -rf docs/structure_cleanup/
```

### Step 3: Verify `opaque_component.json` if it still exists
```bash
ls -la tests/fixtures/porting/opaque_component.json 2>/dev/null && \
echo "rm tests/fixtures/porting/opaque_component.json" || \
echo "Already deleted"
```

### Step 4: Collect CI savings
After running `git clean -fdX`, run `du -sh docs/structure_cleanup/ 2>/dev/null` (before deletion) to quantify saved space.

---

## SUMMARY

| Category | Count | Size (approx) |
|----------|-------|---------------|
| Gitignored artifacts (cleanable) | 100+ files | ~50-100MB |
| `docs/structure_cleanup/` (reports) | ~200 files | ~2-5MB |
| Tracked fixtures referenced | ~50+ files | Keep |
| Tracked snapshots | ~80 files | Keep |
| Obsolete tests to delete | **0** | None identified |

**Total deletion candidates:** `docs/structure_cleanup/` directory + gitignored local artifacts. **No tracked fixtures or snapshot files should be deleted.**