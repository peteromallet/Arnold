Now I have a complete picture. Here's the full audit report.

---

## Deletion Audit: vibecomfy checkout

### ⚠️ PATHS TO KEEP (intentionally retained / secret)

| Path | Reason |
|------|--------|
| `this.env` | **Secret** — leave alone per constraint |
| `.env.example` | Tracked template, not a real secret |
| `.claude/` | User's Claude tool state (skills, settings, scheduled tasks) — intentional |
| `.megaplan/` | Project planning briefs and chain configs — intentional working documents |
| `.agents/` | Agent skill definitions — intentional |
| `custom_nodes.lock` | Lockfile tracking custom node state — intentional |
| `vendor/ComfyUI/` | Git submodule — treat its tracked content as intentional; only delete untracked junk inside it |
| `vendor/direct_templates/` | JSON workflow templates — tracked, intentional |
| `vendor/workflow_templates/` | Mentioned in .gitignore, but check if it exists; if empty placeholder, it's intentional |
| `vendor/external_workflows/` | Same as above — intentional if contains data |
| `workflow_corpus/` | Official workflow corpus — tracked, intentional |
| `tests/` | Test code — intentional |
| `docs/` (most of it) | Documentation — tracked, intentional (except specific junk files below) |

---

### ✅ PATHS TO DELETE (safe local junk)

#### 1. Python caches (auto-generated, always safe)

```
.pytest_cache/                              # pytest run cache
ready_templates/image/__pycache__/z_image.cpython-311.pyc   # compiled bytecode
vendor/ComfyUI/comfy_compatibility/__pycache__/__init__.cpython-311.pyc
vendor/ComfyUI/comfy_compatibility/__pycache__/__init__.cpython-312.pyc
vendor/ComfyUI/comfy_compatibility/__pycache__/__init__.cpython-314.pyc
vendor/ComfyUI/comfy_compatibility/__pycache__/workspace.cpython-311.pyc
vendor/ComfyUI/comfy_compatibility/__pycache__/workspace.cpython-312.pyc
vendor/ComfyUI/comfy_compatibility/__pycache__/workspace.cpython-314.pyc
vendor/ComfyUI/comfy_compatibility/__pycache__/vanilla.cpython-311.pyc
vendor/ComfyUI/comfy_api/torch_helpers/__pycache__/__init__.cpython-311.pyc
vendor/ComfyUI/comfy_api/torch_helpers/__pycache__/torch_compile.cpython-311.pyc
```

→ The root `.gitignore` covers `__pycache__/` and `*.pyc`. The vendor submodule's own `.gitignore` also covers these. They are untracked build artifacts.

#### 2. Generated indexes (regenerable)

```
node_index.json
workflow_index.json
template_index.json
external_workflow_index.json
```

→ All listed in `.gitignore` as `# Generated indexes and runtime artifacts`. Safe to delete; regenerated on next tool run.

#### 3. Runtime output directory

```
out/     # entire tree — logs, cache/object_info snapshots, pre-mortem results, migration reports, review briefs, schema-freshness output, runs/
```

→ `.gitignore` has `out/`. Contains:
- 17+ `.log` files (agent edit chains, megaplan runs, validation logs)
- `cache/` — downloaded `object_info.*.json` snapshots (regenerable)
- `runs/` — 3 `metadata.json` files, 1 `.png`, various `.log` and `.out` files
- `pre_mortem_text_to_graph/` — briefs and result files
- `review_briefs/` — audit outputs
- `migration_reports/`, `emit_survey_baseline.json`, `k4/MISSING.txt`

#### 4. Build/test artifacts

```
artifacts/     # entire tree
```

→ Contains:
- `m5a-emitter-baseline/` — 2 pytest `.log` files, manifest, regenerated `.py` files
- `m5a-emitter-after/` — generated `.py` files, manifest, `.gitkeep`
- `m5_baseline_red.txt`, `m5_agent_edit_imports.txt`, `m1-safety-gate.md`, `m2-symbol-map.md`
- `README.md`

All are build/test artifacts, not source.

#### 5. Agent tool working directories — deletable subpaths

```
.desloppify/config.json.bak       # backup of config
.desloppify/plan.json.bak         # backup of plan
.desloppify/state-python.json     # regenerable state
.desloppify/state-python.json.bak # backup of state
.desloppify/query.json            # ephemeral query
.desloppify/review_packet_blind.json  # generated review
.desloppify/review_packets/       # entire dir: 3 holistic_packet_*.json files
.desloppify/subagents/runs/       # entire dir: 20 batch prompts, 6 results, run.log, run_summary.json
```

→ **KEEP** `.desloppify/config.json` (has user preferences: `target_strict_score`, `review_max_age_days`, etc.)
→ **KEEP** `.desloppify/plan.json` (active plan)

#### 6. Vendor submodule junk

```
vendor/ComfyUI/.DS_Store                                      # macOS metadata
vendor/ComfyUI/comfy/comfyui.db                               # runtime SQLite DB
vendor/ComfyUI/pkg/sageattention-2.2.0-cp312-cp312-linux_x86_64.whl   # downloaded wheel
vendor/ComfyUI/pkg/sageattention-2.2.0-cp39-abi3-linux_x86_64.whl    # downloaded wheel
```

→ The submodule's `.gitignore` covers `__pycache__/`, `*.py[cod]`, `*.log`, `*.db` (via `db.sqlite3` and `db.sqlite3-journal` patterns — `comfyui.db` is adjacent). The `.whl` files and `.DS_Store` are also untracked.

#### 7. Orphaned PID file (leftover from a previous run)

```
docs/structure_cleanup/deletion_first_results/_fan.pid
```

---

### 📋 VERIFICATION COMMANDS

After deletion, run these to confirm cleanliness:

```bash
# 1. Check nothing is tracked that you just deleted (should show only untracked files)
cd /Users/peteromalley/Documents/reigh-workspace/vibecomfy
git status

# 2. Verify no .pyc files remain
find . -name '*.pyc' -not -path './.git/*' 2>/dev/null

# 3. Verify no __pycache__ dirs remain
find . -type d -name '__pycache__' -not -path './.git/*' 2>/dev/null

# 4. Verify no .bak files remain
find . -name '*.bak' -not -path './.git/*' 2>/dev/null

# 5. Verify vendor submodule is clean
cd vendor/ComfyUI && git status && cd ../..

# 6. Check the deleted indexes are gone
ls -la node_index.json workflow_index.json template_index.json external_workflow_index.json 2>&1
```

### 🧹 EXECUTION (dry-run first, then delete)

Dry run to confirm what would be deleted:

```bash
# Show total sizes
du -sh .pytest_cache/ ready_templates/image/__pycache__/ out/ artifacts/ \
  .desloppify/config.json.bak .desloppify/plan.json.bak .desloppify/state-python.json \
  .desloppify/state-python.json.bak .desloppify/query.json .desloppify/review_packet_blind.json \
  .desloppify/review_packets/ .desloppify/subagents/runs/ \
  docs/structure_cleanup/deletion_first_results/_fan.pid \
  vendor/ComfyUI/.DS_Store vendor/ComfyUI/comfy/comfyui.db \
  vendor/ComfyUI/pkg/sageattention-*.whl \
  2>/dev/null

# Delete caches/indexes
rm -rf .pytest_cache/
rm -rf ready_templates/image/__pycache__/
rm -f node_index.json workflow_index.json template_index.json external_workflow_index.json

# Delete runtime outputs
rm -rf out/ artifacts/

# Delete .desloppify junk (keep config.json and plan.json)
rm -f .desloppify/config.json.bak .desloppify/plan.json.bak
rm -f .desloppify/state-python.json .desloppify/state-python.json.bak
rm -f .desloppify/query.json .desloppify/review_packet_blind.json
rm -rf .desloppify/review_packets/ .desloppify/subagents/runs/

# Delete vendor submodule junk
rm -f vendor/ComfyUI/.DS_Store
rm -f vendor/ComfyUI/comfy/comfyui.db
rm -f vendor/ComfyUI/pkg/sageattention-*.whl

# Delete orphaned PID
rm -f docs/structure_cleanup/deletion_first_results/_fan.pid

# Delete vendor __pycache__ dirs
rm -rf vendor/ComfyUI/comfy_compatibility/__pycache__/
rm -rf vendor/ComfyUI/comfy_api/torch_helpers/__pycache__/
```

### 📊 Summary

| Category | Count / Dirs | Estimated Size |
|----------|-------------|----------------|
| **Delete** | ~40 files + 7 directories | ~50–100 MB |
| **Keep** | `.claude/`, `.megaplan/`, `.agents/`, `.desloppify/config.json`, `.desloppify/plan.json`, `this.env`, `.env.example` | intentionally retained |
| **Risk** | None — all deleted items are gitignored or untracked in submodule | — |