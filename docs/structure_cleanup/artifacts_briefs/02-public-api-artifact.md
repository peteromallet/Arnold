Working directory: /Users/peteromalley/Documents/reigh-workspace/vibecomfy

Audit `artifacts/m6-public-api.md` and all references to it. Decide whether it
should stay in `artifacts/`, move to `docs/api/`, or be renamed.

Return:

1. What references it.
2. Whether it appears active/user-facing.
3. Best destination and filename if moving.
4. Required link updates and tests to run.

Keep the answer under 450 words.
