Working directory: /Users/peteromalley/Documents/reigh-workspace/vibecomfy

Audit the tracked `artifacts/` directory. Determine what kind of repository
surface it is: API contract docs, historical evidence, generated snapshots,
baseline logs, or something else.

Return:

1. A category table for every tracked file/directory under `artifacts/`.
2. Which files are active contracts versus historical snapshots.
3. Whether `artifacts/` earns a top-level directory.
4. A safe first cleanup recommendation.

Keep the answer under 550 words and cite exact paths.
