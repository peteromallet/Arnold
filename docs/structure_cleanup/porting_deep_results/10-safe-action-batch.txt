Here is the full synthesis of my investigation and the prioritized action plan.

---

## Deep Audit 10 — Safe Action Batch

### Current State Summary

After the previously completed deletions (old `edit_*` shims, `uid/scope/widget_*` root duplicates, `router_rules.py`), what remains in `vibecomfy/porting/`:

| Category | Count | Files |
|---|---|---|
| **`edit_session_*` root files (actual impl, NOT shims)** | 9 | `edit_session_types.py`, `_parse.py`, `_ir_utils.py`, `_diff.py`, `_resolve.py`, `_describe.py`, `_gates.py`, `_render.py`, `_parse_execute.py` |
| **Stale JSON duplicates in `object_info/`** | 3 | `ComfyUI-KJNodes@runpod-snapshot.json`, `ComfyUI-LTXVideo@runpod-snapshot.json`, `rgthree-comfy@runpod-snapshot.json` |
| **Root UI emitter (duplicate of `emit/ui.py`)** | 1 | `ui_emitter.py` (identical 95615 bytes, but **still imported** externally) |
| **Old emitter monolith (not yet decomposed)** | 1 | `emitter.py` (5088 lines, **still imported** by `convert.py`, `workbench.py`, `commands/`) |
| **Other `emit_*` root helpers (active impls)** | 6 | `emit_constants.py`, `emit_kwargs.py`, `emit_prepare.py`, `emit_ready.py`, `emit_subgraph.py`, `emit_agent_edit.py`, `emit_scratchpad.py` |
| **Other standalone root modules** | ~20 | `provenance.py`, `strict_ready.py`, `report.py`, `convert.py`, `resolution.py`, etc. |

---

### 1. Highest-Confidence Next Batch

**Execute two independent moves/deletes — both are provably zero-risk:**

#### Batch A: Move 9 `edit_session_*` files into `edit/` subpackage

These are the **actual implementations** (not shims), consumed **only** by `edit/session.py` via mixin inheritance and cross-imports among themselves. No external consumer imports them.

| Root file → | `edit/` target |
|---|---|
| `edit_session_types.py` | `edit/_session_types.py` |
| `edit_session_parse.py` | `edit/_parse.py` |
| `edit_session_ir_utils.py` | `edit/_ir_utils.py` |
| `edit_session_diff.py` | `edit/_diff.py` |
| `edit_session_resolve.py` | `edit/_resolve.py` |
| `edit_session_describe.py` | `edit/_describe.py` |
| `edit_session_gates.py` | `edit/_gates.py` |
| `edit_session_render.py` | `edit/_render.py` |
| `edit_session_parse_execute.py` | `edit/_parse_execute.py` |

**Import updates required** (all mechanical, in-file replacements):
1. `edit/session.py`: Change 9 absolute imports → relative (`. _session_types`, `._parse`, etc.)
2. `edit/_types.py` (lazy import): `vibecomfy.porting.edit_session_diff` → `._diff`
3. `edit/_parse.py`: `vibecomfy.porting.edit_session_types` → `._session_types`
4. `edit/_resolve.py`: 3 imports → relative
5. `edit/_describe.py`: 3 imports → relative
6. `edit/_gates.py`: 2 imports → relative
7. `edit/_render.py`: 1 import → relative
8. `edit/_parse_execute.py`: 3 imports → relative
9. `edit/_diff.py`: 1 import → relative

**Zero changes needed** to:
- `edit/__init__.py` — doesn't import root modules
- `porting/__init__.py` — re-exports from `edit.session` (unchanged path)
- `comfy_nodes/agent/*.py` — already uses `vibecomfy.porting.edit.session`
- `tests/` — same

#### Batch B: Delete 3 stale JSON files from `object_info/`

| File | Status |
|---|---|
| `object_info/ComfyUI-KJNodes@runpod-snapshot.json` (702 KB) | ✅ Duplicate of `cache/object_info/ComfyUI-KJNodes@runpod-snapshot.json` |
| `object_info/ComfyUI-LTXVideo@runpod-snapshot.json` | ✅ Duplicate of `cache/object_info/ComfyUI-LTXVideo@runpod-snapshot.json` |
| `object_info/rgthree-comfy@runpod-snapshot.json` | ✅ Duplicate of `cache/object_info/rgthree-comfy@runpod-snapshot.json` |

**Proof of safety:** `object_info/consume.py` line 21 defines `CACHE_DIR = Path(__file__).resolve().parent.parent / "cache" / "object_info"` — the consumer **never reads** from `object_info/`. These 3 files are dead code.

---

### 2. What Should Explicitly NOT Be Touched Yet

| File | Reason to hold |
|---|---|
| **`ui_emitter.py`** (root) | Identical to `emit/ui.py`, BUT still actively imported by 6 modules: `routes.py`, `edit.py`, `layout/sizing.py`, `layout/placement.py`, `layout/engine.py`, `layout/groups.py`, `widget_shape_fence.py`, `latency.py`. Needs coordinated import update first. |
| **`emitter.py`** (root, old monolith) | 5088 lines, **not** a duplicate of `emit/emitter.py` (2230 lines). Still imported by `convert.py`, `workbench.py`, `commands/analyze_names.py`, `commands/validate.py`. Needs M2/M5 decomposition completion first. |
| **`emit_constants.py`, `emit_kwargs.py`, `emit_prepare.py`, `emit_ready.py`, `emit_subgraph.py`** | Active implementations used by both old `emitter.py` AND new `emit/emitter.py`. Moving them requires a coordinated refactor with `emit/` subpackage. |
| **Other root singletons** (`provenance.py`, `strict_ready.py`, `report.py`, `convert.py`, `resolution.py`, `workbench.py`, etc.) | No subpackage migration target exists yet. Standalone with scattered internal importers. |
| **`cache/` directory** | Generated build artifact (267 KB `class_inventory.json`, 23 snapshot JSONs). Intentionally committed for offline validation. Needs `.gitignore` for `__pycache__` but deletion is not right. |

---

### 3. Commands to Prove the Batch Did Not Break Behavior

After executing Batch A + Batch B:

```bash
# 1. Import test — the most sensitive check
cd /Users/peteromalley/Documents/reigh-workspace/vibecomfy
python -c "from vibecomfy.porting import EditSession; print('EditSession OK')"
python -c "from vibecomfy.porting.edit.session import EditSession; print('edit.session OK')"
python -c "from vibecomfy.porting.edit import EditSession; print('edit OK')"

# 2. All edit-* modules import cleanly
python -c "
from vibecomfy.porting.edit import (
    BatchResult, EditSession, FieldChange, EditLedger,
    AddNodeOp, LintResult, ProjectionResult, ApplyResult,
    normalize_ui_json
)
print('All edit/ re-exports OK')
"

# 3. Object info consumer still works
python -c "
from pathlib import Path
p = Path('vibecomfy/porting/object_info')
assert not list(p.glob('@runpod-snapshot.json'))
from vibecomfy.porting.object_info.consume import CACHE_DIR
assert CACHE_DIR.name == 'object_info' and CACHE_DIR.parent.name == 'cache'
print('Cache dir OK, stale files gone')
"

# 4. Run the porting edit-session tests
python -m pytest tests/test_porting_edit_session.py -x --no-header -q 2>&1 | tail -5

# 5. Run the emitter tests (proves no import chain broken)
python -m pytest tests/test_porting_emitter.py -x --no-header -q 2>&1 | tail -5

# 6. Run agent edit tests (most downstream consumer)
python -m pytest tests/test_comfy_nodes_agent_edit.py -x --no-header -q 2>&1 | tail -5

# 7. Full porting test suite
python -m pytest tests/ -k "porting" -x --no-header -q 2>&1 | tail -10
```

Expected result: All pass identically before and after. The refactor is purely internal — no public API paths change.

---

### 4. Status Docs That Should Be Updated

| Document | Update |
|---|---|
| `docs/structure_cleanup/porting_deep_results/02-edit-session-root-modules.txt` | Mark the 9 `edit_session_*` moves as **DONE** (the plan is already written in this file, just needs a status annotation) |
| `docs/structure_cleanup/porting_deep_results/03-stale-object-info-json.txt` (if exists) or `docs/structural_issues.md` | Record deletion of the 3 stale JSON snapshots from `object_info/` |
| `vibecomfy/porting/README.md` | Add a line under `edit/` row: "`_session_types.py`, `_parse.py`, `_ir_utils.py`, `_diff.py`, `_resolve.py`, `_describe.py`, `_gates.py`, `_render.py`, `_parse_execute.py` — private mixin modules (moved from root)" |
| `docs/megaplan_chains/pristine_cleanup/RESULTS.md` (if active) | Log Batch A+B as completed milestone |

---

### Quick Execution Order

```
1. git mv edit_session_types.py  edit/_session_types.py
2. git mv edit_session_parse.py  edit/_parse.py
3. git mv edit_session_ir_utils.py  edit/_ir_utils.py
4. git mv edit_session_diff.py  edit/_diff.py
5. git mv edit_session_resolve.py  edit/_resolve.py
6. git mv edit_session_describe.py  edit/_describe.py
7. git mv edit_session_gates.py  edit/_gates.py
8. git mv edit_session_render.py  edit/_render.py
9. git mv edit_session_parse_execute.py  edit/_parse_execute.py
10. rm object_info/ComfyUI-KJNodes@runpod-snapshot.json
11. rm object_info/ComfyUI-LTXVideo@runpod-snapshot.json
12. rm object_info/rgthree-comfy@runpod-snapshot.json
13. Update imports in edit/session.py + all 9 moved _*.py files (relative paths)
14. Run verification commands
15. Update status docs
```