I'm unable to find the specified repo root on this system. The path `/Users/peteromalley/Documents/reigh-workspace/vibecomfy` does not exist.

Here's what I found in `/Users/peteromalley/Documents/`:
- `megaplan_blast_radius.md`
- `superbadass_batch_upscaler/`
- `mclaniel/`
- `video_parsing/`
- `writing/`
- `Competition Bot/`
- `poms_skills/`
- `runpod_agent/`

No `reigh-workspace/` or `vibecomfy/` directory exists anywhere under `/Users/peteromalley`.

Could you check:
1. **Is the repo on a different volume or mount?** (e.g., `/Volumes/`, external drive)
2. **Has the directory name changed?** (Maybe it's now `vibecomfy` directly somewhere else, or the parent is different)
3. **Could it be in `~/Projects/`, `~/Developer/`, or another base path?**

Once you point me to the correct root, I'll run the inventory immediately.