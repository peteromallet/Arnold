Now I have a complete picture. Here is my analysis.

---

## Audit Results: Object Info And Snapshot Boundaries

### Question 1 — Are committed object-info snapshots active fixtures or stale generated state?

**Verdict: Stale duplicates. Delete the 3 JSON files; keep the Python modules.**

`vibecomfy/porting/object_info/` contains four Python files and three `.json` files. The Python files are **actively used** (22+ production imports across the codebase), but the JSON files are stale.

**Evidence:**

| File | Status | Evidence |
|------|--------|----------|
| `__init__.py` | ✅ ACTIVE | Re-exports from `consume.py`/`serialize.py`; imported by 34 call sites |
| `consume.py` | ✅ ACTIVE | `CACHE_DIR = .../cache/object_info` — the runtime consumer. Reads from cache dir, NOT from this directory (line 21) |
| `serialize.py` | ✅ ACTIVE | Writes to `CACHE_DIR` (cache/object_info), NOT to legacy dir |
| `core_regen.py` | ✅ ACTIVE | Heavy core introspection for `schemas regen-core` command |
| `ComfyUI-KJNodes@runpod-snapshot.json` | ❌ **STALE** | ~702 KB. Identical copy exists in `cache/object_info/`. |
| `ComfyUI-LTXVideo@runpod-snapshot.json` | ❌ **STALE** | Identical copy in `cache/object_info/`. |
| `rgthree-comfy@runpod-snapshot.json` | ❌ **STALE** | ~68 KB. Identical copy in `cache/object_info/`. |

The discovery pipeline (`discovery.py` lines 114–158) uses default precedence `("cache", "snapshot", "source")`. Since the three packs exist in the cache directory, **the snapshot fallback is never reached in production**. No test depends on the committed files (all tests use `tmp_path` fixtures).

---

### Question 2 — Are cache paths generated and ignored correctly?

**Verdict: The `cache/object_info/` directory is committed but should remain committed (it's the canonical schema cache). `class_inventory.json` is a problematic generated artifact.**

**Evidence:**
- `.gitignore` has `out/` (line 14) — this correctly covers the runtime schema cache at `out/cache/object_info.*.json` managed by `vibecomfy/schema/cache.py`.
- `.gitignore` does NOT contain `porting/cache/` or `cache/` patterns.
- The cache files under `vibecomfy/porting/cache/object_info/` are **intentionally committed** — they are the authoritative offline schema source that `consume.py` reads at import time. They are contracts, not transient build artifacts.
- However, `vibecomfy/porting/cache/class_inventory.json` (~268 KB, 5,938 lines) **contains absolute filesystem paths** pointing to a user's home directory (e.g. `/Users/peteromalley/Documents/.megaplan-worktrees/...`). This is a generated artifact that should either be gitignored or regenerated only when needed.

---

### Question 3 — Does object-info belong under `porting/`, `schema/`, `tests/fixtures/`, or another abstraction?

**Verdict: Keep the Python module under `porting/`. It's the right abstraction layer.**

The `vibecomfy/porting/object_info/` Python module (`consume.py`, `serialize.py`, `core_regen.py`) is the **offline-compatible, deterministic schema source** for the porting pipeline. It provides `get_class()`, `output_names()`, `widget_order()`, etc. without needing a running ComfyUI or network.

There are two distinct cache layers:

| Layer | Path | Use case | Committed? |
|-------|------|----------|------------|
| **porting cache** | `porting/cache/object_info/` | Offline schema lookup for porting, codegen, validation | ✅ Yes — it's a contract |
| **runtime cache** | `out/cache/object_info.*.json` | Live runtime session (fingerprinted per server/embedding) | ❌ No — `out/` is gitignored |

The `vibecomfy/schema/cache.py` manages the runtime cache. Moving `object_info` into `schema/` would mix these concerns. The `porting/` placement is correct — this is a porting subsystem that ingests ComfyUI schemas into the VibeComfy model.

---

### Question 4 — Which files can be deleted now?

#### DELETE — stale JSON snapshots (3 files):

```
vibecomfy/porting/object_info/ComfyUI-KJNodes@runpod-snapshot.json
vibecomfy/porting/object_info/ComfyUI-LTXVideo@runpod-snapshot.json
vibecomfy/porting/object_info/rgthree-comfy@runpod-snapshot.json
```

**Prerequisite:** Before deleting, update `scripts/demo_wrapper_codegen.py` (lines 40, 114) — change `sources=("snapshot",)` to `sources=("cache",)`. Without this fix, the demo script will break (it's the only caller that explicitly selects the snapshot source).

#### CONSIDER — gitignore `class_inventory.json` (1 file):

```
vibecomfy/porting/cache/class_inventory.json
```

This file contains absolute user home-directory paths and is generated by `tools/class_inventory_audit.py`. It should either be added to `.gitignore` or regenerated only on demand.

#### KEEP — active Python source (4 files):

```
vibecomfy/porting/object_info/__init__.py      # 22+ production imports
vibecomfy/porting/object_info/consume.py        # Runtime consumer
vibecomfy/porting/object_info/serialize.py      # Cache builder
vibecomfy/porting/object_info/core_regen.py     # Core introspection
```

#### KEEP — canonical cache directory (25+ files):

```
vibecomfy/porting/cache/object_info/            # All files — active schema contracts
```

#### KEEP — test files (2 files):

```
tests/test_wrapper_codegen.py                   # Uses tmp_path fixtures only
tests/test_wrapper_discovery.py                 # Uses tmp_path fixtures only
```

---

### Summary Table

| Path | Delete? | Why |
|------|---------|-----|
| `porting/object_info/ComfyUI-KJNodes@runpod-snapshot.json` | ✅ Yes | Stale duplicate (cache/ has copy); no runtime/test dep |
| `porting/object_info/ComfyUI-LTXVideo@runpod-snapshot.json` | ✅ Yes | Same |
| `porting/object_info/rgthree-comfy@runpod-snapshot.json` | ✅ Yes | Same |
| `porting/object_info/__init__.py` | ❌ No | Active — re-exports consumer/serializer APIs |
| `porting/object_info/consume.py` | ❌ No | Active — 34 import sites across codebase |
| `porting/object_info/serialize.py` | ❌ No | Active — cache builder used by CLI |
| `porting/object_info/core_regen.py` | ❌ No | Active — `schemas regen-core` command |
| `porting/cache/class_inventory.json` | ⚠️ Gitignore | Generated artifact with absolute user paths |
| `porting/cache/object_info/*.json` | ❌ No | Active schema contracts (canonical cache) |
| `scripts/demo_wrapper_codegen.py` | 🔧 Update first | Change `sources=("snapshot",)` → `sources=("cache",)` before deleting JSON files |