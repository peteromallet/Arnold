# Dead Module Audit 02: `_analyze_names.py`

Verify whether `vibecomfy/commands/_analyze_names.py` is dead compared with
`vibecomfy/commands/analyze_names.py`.

Check imports, tests, docs, and behavior differences.

Return exact action and verification commands.
