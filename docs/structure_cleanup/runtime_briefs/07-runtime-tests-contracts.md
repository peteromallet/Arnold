Working directory: /Users/peteromalley/Documents/reigh-workspace/vibecomfy

Task: Map tests and docs that constrain runtime cleanup.

Focus:
- `tests/test_runtime*.py`, `tests/smoke/*runtime*`, `docs/runtime/`,
  docs mentioning `runtime/eval.py`, `eval_plan.py`, `watchdog_runtime.py`,
  or `server_process.py`.

Questions:
- Which paths are true live contracts?
- Which are historical docs that can be updated or ignored?
- What minimal focused test commands should be run after cleanup?

Do not edit. Output a contract map under 900 words.
