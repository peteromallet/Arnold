Here's the audit summary.

---

**1. Consumers of `agents/openai.yaml`**

Only one consumer: `scripts/sync_agent_skill.py` (line 19). The script reads it, checks it matches an embedded `EXPECTED_METADATA` constant, and writes it during `--apply`. No tests, no runtime code, no other scripts reference it. The only docs mentioning it are the cleanup briefs themselves.

**2. Conflicts with `agentic/` or `.agents/`**

- A `.agents/` directory exists (gitignored, line 26 of `.gitignore`; contains `.agents/skills/desloppify/SKILL.md`). It's a tool working directory for Claude/Cursor skills — runtime scratch, not tracked.
- No `agentic/` directory exists at root.
- **Conceptual conflict**: Yes. `agents/` (tracked, root-level) and `.agents/` (gitignored, runtime scratch) have near-identical names with opposite lifecycles. This is confusing for anyone reading the project root.

**3. Safe recommendation**

The `agents/` directory does not earn its place as a root-level directory. It holds one 6-line YAML file consumed by exactly one sync script as a metadata assertion. **Safest path**: inline `EXPECTED_METADATA` directly into `sync_agent_skill.py` (it's already defined there as a constant — just delete the file-read branch), delete `agents/`, and add `agents/` to `.gitignore` or simply let the cleanup delete it. Document in the sync script or a brief comment that this was a former root-level directory.

**4. Moves to avoid**

- **Do not move under `.agents/`** — that directory is gitignored runtime scratch; checked-in config doesn't belong there.
- **Do not move into `vibecomfy/`** — the YAML is an agent-platform metadata assertion, not package code.
- **Do not leave as-is** — a root directory for a single 6-line file with one consumer is dead weight. Inline and delete.