Here is the full audit.

---

## `.megaplan/` Audit

### 0. Tracking status

The entire `.megaplan/` directory is gitignored (`.gitignore` line 29). **No files are tracked** — nothing in `.megaplan/` has ever been force-added to the index. A secondary gitignore entry (`docs/megaplan_briefs/`, line 33) suggests a prior attempt to keep these artifacts out of `docs/` as well.

---

### 1. Tracked `.megaplan` surfaces — NONE

Zero files are tracked in git. There is nothing to preserve or protect.

---

### 2. Ignored/local surfaces by category

**Design/authoritative documents (candidate for commitment):**
| Category | Contents |
|---|---|
| `briefs/` | ~50+ Markdown feature/epic briefs, milestone plans, `chain.yaml` orchestration files, `STRATEGY.md` docs. Covers structural-decomposition, text-to-graph, agent-edit, sisypy-suite, reorg, hardening, etc. |
| `schemas/` | 14 JSON schemas: `plan.json`, `execution.json`, `critique.json`, `review.json`, `gate.json`, `finalize.json`, `prep.json`, `loop_plan.json`, `loop_execute.json`, `revise.json`, `research.json`, `feedback.json`, etc. |
| `chains/` | YAML chain definitions (e.g., `frontier-hardening.yaml`). |
| `ideas/` | Forward-looking design docs (frontier-hardening proposals). |
| `debt.json` | Technical debt registry (3663 lines, ~235 KB). |

**Local runtime state (should stay ignored):**
| Category | Contents |
|---|---|
| `plans/` | Per-plan-session execution artifacts: `execution_batch_*.json`, `critique_*.json`, `review.json`, `finalize.json`, `faults.json`, `gate_signals.json`, `completion_verdict.json`, `evaluator_verdict.json`, `plan_v*.md`, `*_shannon_prompt.txt`, `RECOVERY-HANDOFF.md`, `.plan.lock`, `.events.seq`, `.events.init_ts`. Largest session (`m5-position-furniture-20260530-0856`) alone has ~50 files. |
| `epics/` | Timeline event logs per milestone: `events.jsonl` files (5 found). |
| `.state-locks/` | Runtime `.lock` files for active plan sessions (~20 found). |
| `wakeup/` | Daemon PID file and log. |
| `logs/` | Driver session logs (`*.log`, `*.md`). |
| Root `*.log` | `init-local-library.log`, `plan-native-test.log`, `plan-retest.log`, `plan-sandbox-test.log`, `auto-local-library.log`. |
| Nested `.megaplan/plans/.chains/` | Ephemeral chain trace JSON (e.g., `scratchpad-emitter-*.json`). |
| Nested brief `/.megaplan/plans/.chains/` | Chain state within `text-to-graph-agent-epic` (e.g., `chain-5da176d49554.json` — tracks PR state, milestone completion, sync status). |

---

### 3. What can be documented as local runtime state

Everything in **plans/**, **epics/**, **.state-locks/**, **wakeup/**, **logs/**, all `*.log` files, and all nested `.*` lock/event/chain files. These are transient execution traces, LLM prompt outputs, evaluation verdicts, lock files, and session logs — safe to ignore/regenerate. Total likely several hundred files, mostly small JSON artifacts.

---

### 4. What should move to `docs/` or another committed surface

**Move to `docs/` (or `docs/planning/`):**
- **`briefs/`** — These are the most valuable artifacts in `.megaplan/`. They document planned features, architectural strategy, and milestone decomposition. Despite the `docs/megaplan_briefs/` gitignore entry (which appears to be a blanket exclusion), these are not scratchpad notes — they are authored design documents. Recommend tracking under `docs/planning/briefs/`.
- **`schemas/`** — Pure specifications. Belong in `docs/planning/schemas/` or alongside their consuming code.
- **`chains/`** — YAML orchestration definitions. Should be tracked near their consumers or under `docs/planning/chains/`.
- **`ideas/`** — Design RFCs. Worth tracking under `docs/planning/rfcs/`.
- **`debt.json`** — Curated technical debt. Could be tracked as `docs/debt.json` (though 235 KB may warrant splitting into per-subsystem markdown in `docs/debt/`).

**Do not move:**
- The `briefs/text-to-graph-agent-epic/.megaplan/` nested directory is *also* runtime chain state (PR tracking, milestone completion data) — that nested `.megaplan/` should continue to be ignored.