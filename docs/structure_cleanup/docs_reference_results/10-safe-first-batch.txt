Both edits verified. Here's the complete Safe First Batch synthesis.

---

## ✅ Safe First Batch — Complete

### Method

Audited every local file reference in:
- `README.md` (8 local paths) — **all resolve** ✅
- `CLAUDE.md` (9 reference docs, line 361–369) — **all resolve** ✅
- `docs/migration/README.md` (3 migration links) — **all resolve** ✅
- `docs/templates/README.md` (8 links) — **all resolve** ✅
- `docs/workflow-coverage/README.md` (5 links) — **all resolve** ✅
- `docs/api/README.md` (1 link) — **resolves** ✅
- `docs/audits/README.md` (4 links) — **all resolve** ✅
- `docs/release_notes.md` (redirect link) — **resolves** ✅

Also verified: `docs/comparisons/`, `docs/testing/`, `docs/runpod/` directories all have the files referenced by README and CLAUDE.md.

**No broken links remain** in any active user-facing doc.

---

### Edits Applied (2 items)

| # | File | Change | Reason |
|---|------|--------|--------|
| 1 | `docs/historical/sprint5_followups.md` | `## Out Of Scope For Sprint 4` → `## Out Of Scope For Sprint 5` | Stale copy-paste header in a Sprint 5 doc (audit lens 9 finding #7) |
| 2 | `docs/authoring.md` | Replace dead `scripts/materialize_ready_templates.py` ref with `cli port convert` | Non-existent script referenced as current guidance (structural audit finding) |

---

### Explicitly Not Changed (with rationale)

| Item | Rationale |
|------|-----------|
| Missing v2.4→v2.5 migration guide | Intentional deferral per M7 results — not safe (requires content creation, not reference fix) |
| `docs/local_agent_text_to_graph_e2e.md` tracked despite claiming gitignore | Not a *reference* issue — the file content describes itself as local-only but is tracked; code/repo-hygiene concern, not docs-reference |
| `docs/agent-edit/` failure evidence | Contains actual session evidence; not presented as current guidance |
| `CLAUDE.md` hardcoded absolute path (line 8) | Skill frontmatter convention; changing it could break skill loading |
| `docs/megaplan_chains/` planning artifacts | Audit evidence; not presented as current user guidance |

---

### Verification Commands

```bash
# 1. Verify header fix
grep -n 'Sprint [45]' docs/historical/sprint5_followups.md
# Expected: line 1 "Sprint 5", line 5 "Sprint 5"

# 2. Verify dead-script reference removed
grep -rn 'materialize_ready_template' docs/authoring.md
# Expected: no output

# 3. Verify all README links resolve
for f in docs/comparisons/why_python_not_json.md docs/comparisons/comfyscript.md docs/local_agent_text_to_graph_e2e.md docs/authoring.md docs/templates/porting_workbench.md docs/templates/adding_templates_models.md docs/testing/user_code.md; do
  test -f "$f" && echo "OK $f" || echo "MISSING $f"
done

# 4. Verify all CLAUDE.md reference docs resolve
for f in docs/authoring.md docs/vibeworkflow.md docs/architecture/python_composition_dsl_plan.md docs/custom_nodes.md docs/runpod/setup.md docs/runpod/smoke.md docs/runtime/lifecycle.md docs/runtime/surface.md docs/errors_and_doctor.md; do
  test -f "$f" && echo "OK $f" || echo "MISSING $f"
done
```

**Batch status:** 2 safe edits applied, 0 deletions, 0 moves. All active user-facing doc references verified as current.