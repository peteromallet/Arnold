I now have a complete picture. Here is the full audit report.

---

## Audit Result: RunPod And Layer-2 Router Docs

### Current State Summary

| Artifact | Status |
|---|---|
| `vibecomfy/router/` | **Live** package — `__init__.py` barrels `pick`, `RouterResult` from `_core`; `Rule`, `register_route`, `rules` from `_rules` |
| `vibecomfy/router_rules.py` | **Live thin shim** (9 lines) — `from vibecomfy.router._rules import *` for back-compat |
| `vibecomfy/router.py` (top-level) | **Dead file** — shadowed by `vibecomfy/router/` package, zero imports |
| `vibecomfy/ops/` | **Live package** — no structural change |

---

### Files Requiring Updates

#### 1. `docs/runpod/smoke.md` — **2 lines**

**Line 14** (table row `test_layer2_runpod_ops.py`):
```
Per change to `vibecomfy/ops/`, `router.py`, or templates
```
⬇️ **Replace** `router.py` → `vibecomfy/router/`:
```
Per change to `vibecomfy/ops/`, `vibecomfy/router/`, or templates
```

**Lines 97–98** (Sequencing section):
```
- **Phase 1** (`test_layer2_runpod_ops.py`) — run on every change to
  `vibecomfy/ops/`, `router.py`, or templates touched by the verb-native API.
```
⬇️ **Replace** `router.py` → `vibecomfy/router/`:
```
- **Phase 1** (`test_layer2_runpod_ops.py`) — run on every change to
  `vibecomfy/ops/`, `vibecomfy/router/`, or templates touched by the verb-native API.
```

---

#### 2. `docs/runpod/plans/layer2_test_plan.md` — **1 line**

**Line 193** (Estimate table, Phase 1 row):
```
Every change to `vibecomfy/ops/`, `router.py`, or templates
```
⬇️ **Replace** `router.py` → `vibecomfy/router/`:
```
Every change to `vibecomfy/ops/`, `vibecomfy/router/`, or templates
```

---

#### 3. `docs/megaplan_chains/excellence_epic/m7-plugin-verbs-release.md` — **1 line**

**Line 47** (Touchpoints enumerating files to touch):
```
`ops/{image,video,audio}.py`, `ops/registry.py`, `router.py`, `router_rules.py`, `blocks/__init__.py`, ...
```
⬇️ **Replace** bare `router.py` → `vibecomfy/router/`:
```
`ops/{image,video,audio}.py`, `ops/registry.py`, `vibecomfy/router/`, `router_rules.py`, `blocks/__init__.py`, ...
```
*(`router_rules.py` stays — the shim still exists and is the back-compat touchpoint for `extras.py` consumers.)*

---

#### 4. `CLAUDE.md` — **1 line**

**Line 315** (Verb-native router section):
```
Rules live in `vibecomfy/router_rules.py`.
```
⬇️ **Replace** → point to the canonical package:
```
Rules live in `vibecomfy/router/` (package) or `vibecomfy/router/_rules.py` for rule internals.
```
(Or more briefly: `vibecomfy/router/` covers both `pick` and `register_route`.)

---

#### 5. `docs/megaplan_chains/pristine_cleanup/audit/03-public-api-naming.md` — **1 line**

**Line 34** (Finding #9 about router naming):
```
`router.py:51` — `__all__ = ["RouterResult", "pick"]`.
```
⬇️ **Replace** line+file → current location:
```
`vibecomfy/router/__init__.py:4` — `__all__ = ["RouterResult", "pick", "Rule", "register_route", "rules"]`.
```
*(Also note the `__all__` expanded — the audit finding can add the full current list for accuracy.)*

---

#### 6. `docs/structure_cleanup/docs_reference_briefs/02-runpod-and-layer2-router-docs.md`

**No changes needed** — this is the task brief we are currently executing.

---

### Deferrals (No Change Recommended)

| File | Line(s) | Reason for Deferral |
|---|---|---|
| `docs/megaplan_chains/excellence_epic/m7-plugin-verbs-release.md` | 14, 40 | **Scope/planning doc** describing M7 intent. `router_rules.py` as "data-driven" and `router.pick()` as "contract" are functionally accurate — the shim re-exports the same symbols. Updating would misrepresent what M7 was scoped to deliver (the plan described `router_rules.py` as the target; the implementation actually moved to `router/_rules.py` with a shim left behind). These are historical planning references, not active run/test guidance. |
| `README.md` | (entire file) | **No stale references found.** The README only mentions `vibecomfy/cli`, `ready_templates/`, and the package layout at a high level. No `router.py` or `router_rules.py` mentions. |
| `docs/megaplan_chains/pristine_cleanup/audit/04-layer2-architecture.md` | 22–23 | References `ops/registry.py:10-29` and `patches/registry.py:10-34` — both still live files. Correct as-is. |
| All other `docs/structure_cleanup/` files | — | These are **audit artifacts**, not active run/test guidance. They accurately document the dead-module state as discovered. Updating them would erase the audit record. |

---

### Summary of Edits Needed

| Priority | File | Lines | Change |
|---|---|---|---|
| **HIGH** | `docs/runpod/smoke.md` | 14, 97–98 | `router.py` → `vibecomfy/router/` |
| **HIGH** | `docs/runpod/plans/layer2_test_plan.md` | 193 | `router.py` → `vibecomfy/router/` |
| **HIGH** | `CLAUDE.md` | 315 | `router_rules.py` → `vibecomfy/router/` |
| **MEDIUM** | `docs/megaplan_chains/excellence_epic/m7-plugin-verbs-release.md` | 47 (touchpoints) | `router.py` → `vibecomfy/router/` |
| **LOW** | `docs/megaplan_chains/pristine_cleanup/audit/03-public-api-naming.md` | 34 | `router.py:51` → `vibecomfy/router/__init__.py:4` + update `__all__` |

**Total: 6 lines across 5 files to update; 4 deferrals recorded.**