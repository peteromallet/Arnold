Here is the complete audit with safe moves, root-earning docs, and deferred files.

---

## Docs Root Earning Place — Audit Results

### ✅ Safe Moves (low link burden, clear thematic home)

These four files can move **now** with minimal, well-scoped link fixes:

| File | Move To | Rationale | Links to Update |
|---|---|---|---|
| **`api_stability.md`** | `docs/api/api_stability.md` | API policy doc belongs with the API docs folder. Only 1 reader link. | `docs/README.md:10` — change `(api_stability.md)` → `(api/api_stability.md)` |
| **`cloud.md`** | `docs/runpod/cloud.md` | Cloud deployment doc belongs with RunPod docs. Only ref uses absolute `docs/cloud.md` path which still resolves from any subdirectory. | **None** — the sole external ref (`docs/megaplan_chains/pristine_cleanup/README.md:84`) uses an absolute path. |
| **`release_notes.md`** | Merge into `docs/release_notes/README.md` | Already a 6-line stub pointing at `release_notes/`. Replace root stub with a proper `README.md` inside the folder, then delete root file. | `docs/README.md:13` — change `(release_notes.md)` → `(release_notes/)` |
| **`roadmap_agentic_comfyui.md`** | `docs/plans/roadmap_agentic_comfyui.md` | Strategic planning document belongs in `docs/plans/`. Only 1 non-docs ref (a docstring comment). | `scripts/roundtrip_fidelity_spike.py:1` — update docstring path |

**Batch link-fix summary:** 3 link changes total (README.md ×2, one script docstring). No production code, no agent configs, no test assertions.

---

### 🏠 Earns Root Placement (keep in place)

These five files are primary entry-point docs or cross-cutting references without a clear subfolder home. Moving them gains negligible organization benefit at disproportionate link cost.

| File | Lines | Why It Earns Root |
|---|---|---|
| **`authoring.md`** | 281 | Main user-facing authoring guide; listed as "Core Guide" in `docs/README.md`; cross-cuts templates, architecture, porting, and CLI. |
| **`vibeworkflow.md`** | 14 | Foundational IR concept doc; listed as "Core Guide" in `docs/README.md`. |
| **`custom_nodes.md`** | 49 | Operational custom-node catalog and policy; "Core Guide" in `docs/README.md`; cross-cuts runtime, templates, and testing. |
| **`errors_and_doctor.md`** | 34 | Cross-cutting tooling reference (doctor, port check, validate); no single subfolder owns it. |
| **`comfy_version_support.md`** | 69 | Cross-version compatibility policy; no existing subfolder for this concern. Could eventually live in `docs/testing/` but that folder currently only has user-code examples. |

---

### ⏸️ Defer (not safe to move now)

These five files have high link-update burdens that make moving them unsafe without a coordinated batch pass. The existing `structure_cleanup/` analysis concurs.

| File | Why Deferred | Link Burden |
|---|---|---|
| **`local_agent_text_to_graph_blockers.md`** | Bridge doc between `agent-edit/` and `text-to-graph/` clusters. Three files use `../` relative paths to reach it. Moving would break links in both clusters. | 3 refs using `../` from subfolder files |
| **`local_agent_text_to_graph_e2e.md`** | Gitignored (local-only with API keys). Referenced by blockers.md which stays at root. Moving alone would break that inline link. | 1 ref from blockers.md (which stays) |
| **`node_pack_reconciliation.md`** | ~20 hardcoded string references in production code (`vibecomfy/schema/validate.py`). Cosmetic gain not worth 20+ code changes. | ~20 string literals in production code |
| **`structural_audit_2026-05.md`** | 17+ cross-references from `megaplan_chains/excellence_epic/*` using `../structural_audit_2026-05.md`. Moving would break every one. | 17+ refs across megaplan chains |
| **`structural_issues.md`** | 5+ refs including `AGENTS.md` and `CLAUDE.md` (agent config files). Agent config path changes are higher-risk than doc moves. | 5+ refs incl. agent configs |

---

### Execution Order

```
# 1. Move api_stability.md
mv docs/api_stability.md docs/api/api_stability.md
# Fix: docs/README.md line 10: (api_stability.md) → (api/api_stability.md)

# 2. Move cloud.md
mv docs/cloud.md docs/runpod/cloud.md
# No link fix needed (only ref is absolute path)

# 3. Merge release_notes.md into release_notes/README.md
cp docs/release_notes.md docs/release_notes/README.md
rm docs/release_notes.md
# Fix: docs/README.md line 13: (release_notes.md) → (release_notes/)

# 4. Move roadmap
mv docs/roadmap_agentic_comfyui.md docs/plans/roadmap_agentic_comfyui.md
# Fix: scripts/roundtrip_fidelity_spike.py line 1: update docstring path
```

**Total: 4 file moves, 3 link updates across 2 files.** No test breaks, no agent config changes, no production code modifications.