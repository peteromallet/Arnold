Now I have a thorough view of all the support subpackages. Here's my complete audit.

---

## Audit Report: Analysis, Security, Testing & Support Subpackages

### Inventory of Targeted Subpackages

| Subpackage | Exists? | Files | Tests Exist? |
|---|---|---|---|
| `vibecomfy/analysis/` | **No** | — | — |
| `vibecomfy/diagnostics/` | Yes | `__init__.py`, `health.py`, `findings.py` | No dedicated test |
| `vibecomfy/security/` | Yes | 7 `.py` files | No dedicated test |
| `vibecomfy/lens/` | Yes | `__init__.py`, `core.py` | No dedicated test |
| `vibecomfy/testing/` | Yes | 12 `.py` files | Partial |
| `vibecomfy/search/` | Yes | 5 `.py` files | No dedicated test |
| `vibecomfy/ingest/` | Yes | 6 `.py` files | No dedicated test |

---

### 1. Coherence & Discoverability

**`diagnostics/`** — Generally coherent but has **duplicated type definitions** between `__init__.py` and `findings.py`:

| Symbol | Defined in `__init__.py` | Defined in `findings.py` |
|---|---|---|
| `DiagnosticFinding` | Yes (lines 12-32) | Yes (lines 10-30) |
| `PatchSuggestion` | Yes (lines 36-41) | Yes (lines 33-39) |
| `Severity` | Yes (line 8) | Yes (line 6) |
| `finding_messages` | Yes (lines 44-45) | Yes (lines 42-43) |
| `findings_payload` | Yes (lines 48-49) | Yes (lines 46-47) |

`__init__.py` should re-export from `findings.py` instead of duplicating the definitions.

**`security/`** — Very clean. Clear one-way dependency isolation via `_seed.py`. The `agent_generated_loader.py` is a deliberate 3-line `sys.modules` swap-out shim.

**`lens/`** — Cleanest of the bunch. Two files, clear API surface.

**`testing/`** — Generally well-structured but has a **stale forward-declaration issue**: `__init__.py` defines `_not_yet_implemented()` stubs for `vibecomfy_workflow_factory`, `vibecomfy_handle_factory`, `dry_runtime`, `make_workflow_factory`, `make_handle_factory` (lines 76-80). These names are **actually implemented** in `fixtures.py` but `__init__.py` does **not** import them, so the stubs shadow the real implementations.

**`search/`** — Very clean separation: corpus building, scoring, bootstrap, aliases.

**`ingest/`** — Contains a stale back-compat alias and a heavy `normalize.py` file.

**Discoverability summary**: `lens`, `search`, `security` are well-organized. `diagnostics` and `testing` have surface-level issues. No `analysis/` subpackage exists at all.

---

### 2. Misplaced Files Relative to Imports/Tests

**`testing/_fixtures.py`** — This is about **smoke fixture files** (audio/video assets for corpus matrix runs), not about Python test fixtures. It exposes `copy_smoke_fixtures`, `regenerate_smoke_fixtures`, etc. and even has a `python -m` CLI entrypoint. This module:
- Imports from `vibecomfy.utils` (OK)
- Has `av`, `Pillow`, `wave` lazily imported inside functions
- Has nothing to do with `vibecomfy.testing` as an assertion/dry-run surface
- **Suggestion**: Move to `vibecomfy/cli/fixtures.py` or keep as `vibecomfy/_fixtures.py` at the top level. The current location under `testing/` is misleading.

---

### 3. Stale Modules & Compatibility Shims

| File | Issue | Action |
|---|---|---|
| `vibecomfy/ingest/loader.py:17` | `load_template = load_workflow_json` back-compat alias | ✗ **Safe to remove** if no external callers reference `load_template` |
| `vibecomfy/testing/__init__.py:76-80` | Forward-declared stubs shadow real `fixtures.py` imports | ✗ **Fix required** — replace stubs with `from .fixtures import ...` |
| `vibecomfy/security/agent_generated_loader.py` | `sys.modules` swap-out shim to `vibecomfy.loader` | This is a deliberate re-export, not stale |
| `vibecomfy/security/_seed.py:45` | `OUTPUT_CLASS_NAMES` alias | Defensible — keeps API stable |

---

### 4. What Can Be Documented Without Moving Public Paths

All of these subpackages are importable without breaking existing callers. Documentation-only improvements:

**`diagnostics/`**: Can document `HealthReport`, `SubcheckResult`, `SubcheckFinding` workflow diagnostics surface. The `run_health_checks()` entry point is the high-level API.

**`lens/`**: Can document `WorkflowLens` as the primary graph-query surface. The stateless functions (`upstream`, `downstream`, `nodes_by_class_type`, `diagnostics`) are stable.

**`testing/`**: The docstring in `__init__.py` already states the intent well. Can document the full assertion API (`assert_node_present`, `assert_edge`, etc.) and the dry-run pattern. **But** the forward-declared stubs must be fixed first so the documented names actually resolve.

**`search/`**: Can document `search_entries()`, `build_search_corpus()`, and the alias system.

**`ingest/`**: Can document `load_workflow_json`, `normalize_to_api`, `convert_to_vibe_format`, and the `sync_sources` pipeline.

**`security/`**: The docstrings are already thorough. The `CapabilityFenceError` + `require_confirmation` gate is the primary surface.

---

### Exact Safe Actions (No Public Path Moves)

1. **Fix duplicated types in `diagnostics/__init__.py`**: Remove local definitions of `DiagnosticFinding`, `PatchSuggestion`, `Severity`, `finding_messages`, `findings_payload`, `patch_suggestions_payload` and re-export from `findings.py` instead.

2. **Fix stale forward-declared stubs in `testing/__init__.py`**: Replace the `_not_yet_implemented` stubs (lines 76-80) with real imports from `fixtures.py`:
   ```python
   from vibecomfy.testing.fixtures import (
       dry_runtime,
       make_handle_factory,
       make_workflow_factory,
       vibecomfy_handle_factory,
       vibecomfy_workflow_factory,
   )
   ```

3. **Remove stale `load_template` alias** from `ingest/loader.py` (line 17) if nothing references it.

4. **Write docstrings** for `vibecomfy/diagnostics/__init__.py`, `vibecomfy/lens/__init__.py`, `vibecomfy/search/__init__.py`, and `vibecomfy/ingest/__init__.py` (they lack package-level docstrings).

5. **Add dedicated tests** for `diagnostics/`, `lens/`, `search/`, and `ingest/` subpackages (currently none exist).

6. **Move `testing/_fixtures.py`** to a better location (e.g., `vibecomfy/cli/fixtures.py` or `vibecomfy/_fixtures.py`) since it's about smoke-test media assets, not Python test fixtures. Update imports in any callers.

Would you like me to execute any of these safe actions?