Now I have a comprehensive picture. Here's the full audit.

---

## `.gitignore` Audit Report — vibecomfy

### Q1: Are ignore rules too broad, stale, missing, or misleading?

| Issue | Severity | Detail |
|---|---|---|
| **Redundant: `input/.DS_Store` (line 18)** | Low | `input/` (line 17) already ignores everything inside, and `.DS_Store` (line 6) is already global. The specific entry implies special handling that doesn't exist. Harmless but misleading — remove or comment. |
| **Missing: `vibecomfy/porting/cache/`** | **High** | 28 generated JSON files live here — object_info snapshots from 6 environments (runpod, local, stub), `class_inventory.json`, `index.json`. These are build/cache artifacts not under source dirs. Not in `.gitignore`. Risk of accidental commits. |
| **Missing: `workflow_corpus/manifests/`** | **Medium** | Contains `coverage.json` and `ready_regeneration.json` — generated manifest files. Not ignored. |
| **Missing: `custom_nodes.lock`** | Medium | At project root. If this is a generated lockfile from `custom_nodes/` resolution, it should be ignored. |
| **Missing: `*.log`** | Medium | Log files exist on disk (`focused_pytest.log`, `full_pytest.log`). No catch-all `*.log` rule. |
| **Missing: `*.bak`** | Low | `.bak` files exist (under already-ignored `.desloppify/`), but a defensive `*.bak` rule won't hurt. |
| **Stale: `.import_linter_cache/`** (line 5) | None | Directory doesn't exist on disk. Defensive entry — fine to keep but could be documented. |
| **Stale: `.hypothesis/`** (line 42) | None | Doesn't exist on disk. Defensive entry for property-based testing — fine. |
| **`vendor/ComfyUI/` submodule** | None | Confirmed as a git submodule (`.gitmodules` exists). Its own `.gitignore` (190 lines) covers its internals. Parent `.gitignore` doesn't apply to submodule working tree. No action needed. |

### Q2: Are ignored-but-important surfaces documented?

| Surface | Documented? | Assessment |
|---|---|---|
| `vendor/workflow_templates/`, `vendor/external_workflows/`, `custom_nodes/` | Comment says "Large local runtime/vendor checkouts" | Adequate. |
| `.agents/`, `.claude/`, `.desloppify/`, `.megaplan/` | Comment says "Tool working directories" | Adequate. |
| `docs/megaplan_briefs/`, `docs/subagent_prompts/`, `docs/local_agent_text_to_graph_e2e.md` | Comment says "Agent task briefs and prompts — historical scratchpads" | Adequate. |
| `tests/e2e/node_modules/`, `tests/e2e/playwright-report/`, `tests/e2e/test-results/` | Comment says "e2e test tier — isolated dependency management" | Adequate. |
| `vibecomfy/porting/cache/` | **Not documented at all** | Missing. This is the biggest documentation gap. |
| `workflow_corpus/manifests/*.json` | **Not documented** | Missing. |

### Q3: Are there ignored files under source dirs that should be removed?

Files on disk under source-controlled directories that are gitignored (thus orphaned junk):

| File(s) | Location | Status | Action |
|---|---|---|---|
| `__pycache__/*.pyc` (6 files) | `vibecomfy/router/__pycache__/` | Ignored by `__pycache__/` and `*.pyc` | **Should be removed** — stale cached bytecode under source dir. Run `find vibecomfy -name __pycache__ -type d -exec rm -rf {} + 2>/dev/null` |
| `.DS_Store` (4 files) | `vendor/`, `vibecomfy/`, `tests/`, `vendor/ComfyUI/` | Ignored by `.DS_Store` | **Should be removed** — OS junk. `find . -name .DS_Store -delete` |
| `node_index.json`, `template_index.json`, `external_workflow_index.json`, `workflow_index.json` | Project root | Ignored by explicit rules | **Check if tracked** — if untracked, are they stale on disk or actively regenerated? |
| `this.env` | Project root | Ignored | **Check if stale** — if this was an old env file, delete it. |
| `.pytest_cache/` (5 files) | Project root | Ignored by `.pytest_cache/` | **Should be removed** — stale test cache. `rm -rf .pytest_cache/` |
| `.desloppify/*.bak` (3 files) | `.desloppify/` | Ignored (dir is ignored) | **Should be cleaned** — stale backups inside an already-ignored directory. |

### Q4: Exact safe recommendations

#### A. `.gitignore` changes (add these lines):

```
# Generated porting cache — object_info snapshots, class inventories
vibecomfy/porting/cache/

# Generated workflow corpus manifests
workflow_corpus/manifests/

# Custom nodes lockfile (generated by node resolution)
custom_nodes.lock

# Stray log files
*.log

# Backup files
*.bak
```

#### B. Remove redundant `input/.DS_Store` (line 18):

Delete the specific line — it's already covered by both `input/` (line 17) and `.DS_Store` (line 6).

#### C. Cleanup actions (safe to run):

```bash
# Remove stale .pyc bytecode under source dirs
find vibecomfy -name __pycache__ -type d -exec rm -rf {} + 2>/dev/null

# Remove macOS .DS_Store junk
find . -name .DS_Store -delete

# Remove stale pytest cache
rm -rf .pytest_cache/

# Remove stale .bak files inside already-ignored .desloppify/
rm -f .desloppify/*.bak

# Remove stale this.env if no longer needed
rm -f this.env

# Check if root-level index JSONs are tracked; if not, remove stale ones
# git ls-files node_index.json template_index.json external_workflow_index.json workflow_index.json
# If none tracked: rm -f node_index.json template_index.json external_workflow_index.json workflow_index.json
```

#### D. Documentation recommendation

Add a comment in `.gitignore` above the cache section:

```
# Generated porting cache — object_info snapshots, class inventories
# These are fetched from remote/runpod environments and regenerated on demand.
# Do not commit. See vibecomfy/porting/cache/README.md for regeneration.
```

#### E. Check tracking status (manual verification needed)

Run these to verify nothing important is accidentally ignored or stale:

```bash
git ls-files node_index.json template_index.json external_workflow_index.json workflow_index.json
git ls-files version_matrix.json
git ls-files custom_nodes.lock
git ls-files 'vibecomfy/porting/cache/**'
git ls-files 'workflow_corpus/manifests/**'
```

If any return results, those files are tracked and the `.gitignore` rule won't affect them (git doesn't ignore tracked files). If they're tracked and should remain tracked, no action needed. If they're tracked but shouldn't be, `git rm --cached` them first.