# Tests Layer Audit 07: E2E And Browser Tests

Audit `tests/e2e/` and browser-oriented tests.

Questions:
- What local dependency outputs are present (`node_modules`, reports, test results)?
- Are ignore rules sufficient?
- Are browser tests documented in `docs/testing/`?
- Are any stale generated artifacts checked in?

Return safe cleanup and deferrals.
