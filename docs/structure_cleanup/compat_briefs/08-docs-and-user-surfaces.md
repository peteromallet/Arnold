# Compatibility Layer Audit 08: Docs And User Surfaces

Working directory: `/Users/peteromalley/Documents/reigh-workspace/vibecomfy`.

Goal: find docs/user-facing surfaces for fixture/debug/schema/diagnostics paths.

Inspect active docs only first:
- `README.md`
- `docs/`
- `AGENTS.md`, `CLAUDE.md`
- scripts comments for user-facing commands.

Return under 450 words:
1. Active docs that must stay valid.
2. Docs to update if implementation moves.
3. Historical docs that can remain as provenance.
4. Any path names that should be documented as public exceptions.
