Now I have the full picture. Here's the exact batch.

---

## 1. Files to Edit Now

### A. `vibecomfy/nodes/__init__.py` and `__init__.pyi`
Replace all `from vibecomfy.nodes.{module} import *` → `from vibecomfy.nodes._generated.{module} import *`

### B. `tools/generate_node_shims.py`
- Remove lines 261–262 (`_write_reexport_module(module)` and `_write_reexport_stub_module(module)`)
- Update `_write_nodes_init()` line 689: `f"from vibecomfy.nodes.{module} import *"` → `f"from vibecomfy.nodes._generated.{module} import *"`
- Update `_write_nodes_init_stub()` line 700: same change
- Update `_prune_stale_thin_shims()`: stop scanning `NODES_DIR` — only scan `GENERATED_DIR`

### C. Three emitter files (same change, all emit template import lines):
- `vibecomfy/porting/emitter.py:1535`: `from vibecomfy.nodes.{module_name}` → `from vibecomfy.nodes._generated.{module_name}`
- `vibecomfy/porting/emit_ready.py:712`: same
- `vibecomfy/porting/emit/emitter.py:994`: same

### D. Tests and scripts that import through shims:
- `tests/test_node_shims.py` lines 17, 40–51, 52: change all `vibecomfy.nodes.{pack}` → `vibecomfy.nodes._generated.{pack}`
- `tests/test_generated_node_wrappers.py` line 9: `vibecomfy.nodes.core` → `vibecomfy.nodes._generated.core`
- `scripts/demo_wrapper_codegen.py` line 78: `vibecomfy.nodes.rgthree` → `vibecomfy.nodes._generated.rgthree`

### E. Docs with live import paths:
- `docs/templates/examples/gold_template_wan_i2v.py` line 55: `from vibecomfy.nodes.core` → `from vibecomfy.nodes._generated.core`
- `docs/megaplan_chains/node_resolution_epic/testing/fixtures/ideogram4_t2i.expected_emit.py` line 7: same

### F. Bulk replace in ready_templates (50 files, 145 import lines) and golden test fixtures (13 files):
`sed -i '' 's|from vibecomfy\.nodes\.\([a-z_]*\) import|from vibecomfy.nodes._generated.\1 import|' ready_templates/**/*.py tests/characterization/goldens/emitter/*.golden`

---

## 2. Files NOT to Touch

| File | Why |
|------|-----|
| `vibecomfy/nodes/index.py` | Live module (`index_custom_node_examples`, `index_runtime_nodes`), not a shim |
| `vibecomfy/nodes/_generated/*` | Source of truth — keep all 40 files |
| `vibecomfy/checks.py` lines 240–245 | Already scans `_generated` path in `.pyi` files; shim stubs just won't be found anymore — no breakage |
| `vibecomfy/comfy_nodes/` | Web JS/other node surfaces, unrelated |
| Root `node_index.json` | Already deleted in earlier pass |
| `vendor/ComfyUI/` shim code | Upstream submodule, out of scope |

---

## 3. Commands to Verify

```bash
# 1. Delete all 38 shim files
rm vibecomfy/nodes/{core,kjnodes,ltxvideo,videohelpersuite,controlnet_aux,depthanythingv2,wanvideowrapper,qwentts,qwen3tts,gguf,rgthree,sam2,wananimatepreprocess,ailab_audioduration,custom_scripts,florence2,gimm_vfi,melbandroformer,vibecomfy_internal}.py
rm vibecomfy/nodes/{core,kjnodes,ltxvideo,videohelpersuite,controlnet_aux,depthanythingv2,wanvideowrapper,qwentts,qwen3tts,gguf,rgthree,sam2,wananimatepreprocess,ailab_audioduration,custom_scripts,florence2,gimm_vfi,melbandroformer,vibecomfy_internal}.pyi

# 2. Verify only __init__.py/i, index.py, _generated/ remain
ls vibecomfy/nodes/*.py | grep -v __init__ | grep -v index | grep -v _generated
# → empty

# 3. Regenerate (generator will now skip re-export shims)
python -m tools.generate_node_shims

# 4. Import smoke: public surface still works
python -c "from vibecomfy.nodes import KSampler, EmptyImage, WanVideoSampler; print(f'{len(__all__)} symbols')"

# 5. Verify no stale imports remain outside docs/cleanup
rg "from vibecomfy\.nodes\.(core|kjnodes|ltxvideo|videohelpersuite|controlnet_aux|depthanythingv2|wanvideowrapper|qwentts|qwen3tts|gguf|rgthree|sam2|wananimatepreprocess|ailab_audioduration|custom_scripts|florence2|gimm_vfi|melbandroformer|vibecomfy_internal) import" --type py --glob '!vendor/**' --glob '!docs/**'
# → should only match the sed-updated ready_templates/ files

# 6. Run focused tests
python -m pytest tests/test_node_shims.py -x
python -m pytest tests/test_generated_node_wrappers.py -x
python -m pytest tests/test_cli_sources_workflows_nodes.py -x

# 7. Emitter characterization tests
python -m pytest tests/characterization/ -x -k "golden"

# 8. CLI smoke
python -m vibecomfy.cli workflows list --ready --json

# 9. git clean check
git diff --check
```

---

## 4. Status Update for `docs/structure_cleanup/status.md`

Append a **Nodes Layer** section:

```markdown
### Nodes Layer

Ran deletion-first node-shim batch. Applied safe cleanup:

- deleted all 19 `.py` + 19 `.pyi` re-export shims at `vibecomfy/nodes/<pack>.py/i` (zero logic, purely mechanical re-exports from `_generated/`)
- updated `vibecomfy/nodes/__init__.py` / `__init__.pyi` to import directly from `_generated.{module}`
- updated `tools/generate_node_shims.py` to stop writing re-export shims
- updated emitter (3 files: `emitter.py`, `emit_ready.py`, `emit/emitter.py`) to emit `nodes._generated.{module}` import paths
- updated test imports (`test_node_shims.py`, `test_generated_node_wrappers.py`), `scripts/demo_wrapper_codegen.py`, and doc/exemplar imports
- bulk-updated `ready_templates/` (50 files) and golden fixtures (13 files) with `from vibecomfy.nodes.{m}` → `from vibecomfy.nodes._generated.{m}`

Kept:
- `vibecomfy/nodes/index.py` — live node-listing utilities, not a shim
- `vibecomfy/nodes/_generated/` — actual generated wrappers, unchanged
- `vibecomfy/checks.py` — already scans `_generated` path in `.pyi` files

Verification:
- import smoke: `from vibecomfy.nodes import KSampler, EmptyImage`
- stale-import scan: no remaining references to shim paths outside vendored code
- `python -m tools.generate_node_shims` no longer writes re-export shims
- focused test suites passed
- `git diff --check` passed
```