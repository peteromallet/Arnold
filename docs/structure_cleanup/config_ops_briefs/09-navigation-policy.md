Working directory: /Users/peteromalley/Documents/reigh-workspace/vibecomfy

Task: audit navigation/documentation for config/ops root files.

Use:
- `README.md`
- `docs/README.md`
- current root layout table
- any README under `.github`, `vendor`, scripts/tools/docs/runpod if present

Do not edit files.

Questions:
1. Does root README explain the root config/ops files enough?
2. Should a new `docs/ops/`, `docs/config/`, `.github/README.md`, or `vendor/README.md` be added?
3. What is the smallest useful navigation improvement?

Return exact edits.
