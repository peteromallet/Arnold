Working directory: /Users/peteromalley/Documents/reigh-workspace/vibecomfy

Task: audit Python project config boundaries.

Focus:
- `pyproject.toml`
- `uv.lock`
- `.pre-commit-config.yaml`
- `.importlinter`
- `tests/` and package layout references

Use relevant reads/searches.

Do not edit files.

Questions:
1. Are root project config files conventional and necessary?
2. Are there stale tool configs or generated caches that should be ignored/removed?
3. Is there any README or docs gap?

Return exact recommendations.
