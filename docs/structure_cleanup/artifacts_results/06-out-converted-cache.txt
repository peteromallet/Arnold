**Classification:**

| Directory | Source | Status |
|---|---|---|
| `out/converted/` | Generated by `tools/convert_ready_templates.py` with `--dry-run` (line 577-587). It's a preview directory — mirrors `ready_templates/` layout for review before `--write` commits changes in-place. | **Generated — remain ignored** |
| `out/cache/` | Populated by `vibecomfy.porting.emitter` (reads `hf_metadata.json`), `vibecomfy.commands.schemas` (writes `object_info.*.json`), and the runtime schema provider. Contents are machine-captured ComfyUI `/object_info` snapshots and HF metadata — host/environment-specific, non-portable. | **Generated — remain ignored** |
| `out/migration_reports/` | Contains one report (`video__ltx2_3_first_last_frame_travel_iclora_control.md`). Likely from internal migration tooling. Single ad-hoc file, not referenced by any command in source. | **Probably generated — remain ignored** |

**Evidence from source:**

- `tools/convert_ready_templates.py` explicitly writes dry-runs to `out/converted/` (line 577).
- `vibecomfy/schema/cache.py` defaults to `out/cache/` for `object_info*.json` caching.
- `vibecomfy/commands/schemas.py` writes `object_info.schemas-refresh.json` and `object_info.comfy-core.*.json` there.
- CLI help text in `_register.py` and `port.py` says "Defaults to the newest `out/cache/object_info*.json` when present."
- `docs/workflow-coverage/onboarding_parity_tooling.md` documents using `--object-info-cache out/cache/object_info.*.json`.
- `docs/templates/adding_templates_models.md` documents `tools.fetch_hf_metadata` writing to `out/cache/hf_metadata.json`.

**Actual contents observed:**
- `out/converted/`: ~20+ generated Python template files (audio, edit, image, video). Transient dry-run output.
- `out/cache/`: 6 JSON files — ComfyUI `/object_info` snapshots + environment-specific dumps (including a Runpod hostname `gf0dxo1mbqz2ly`). These are *environment-specific* and shouldn't be fixtures.
- `out/migration_reports/`: 1 markdown file. Likely from one-off migration tooling.

**Safe action:** All three should **remain gitignored** (`out/` is already in `.gitignore`). They are generated outputs, not tracked fixtures or documentation. If desired, a `.gitkeep` or a `README.md` per directory could document intent, but that's optional — the code and docs already reference these paths transparently.