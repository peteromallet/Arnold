## Audit: `artifacts/` — 19 files, 3 subdirectories

### 1. Category Table

| Path | Category | Active Contract? |
|---|---|---|
| `artifacts/m1-step1-audit.md` | Historical review doc | No |
| `artifacts/m1-safety-gate.md` | Historical review doc (test gate results) | No |
| `artifacts/m2-diff-hygiene.md` | Historical review doc | No |
| `artifacts/m2-symbol-map.md` | Historical review doc | No |
| `artifacts/m6-public-api.md` | Historical review doc | No |
| `artifacts/m5_baseline_red.txt` | Generated snapshot (20K-line pytest log) | No |
| `artifacts/m5_agent_edit_imports.txt` | Generated snapshot (agent-grep output) | No |
| `artifacts/m4/m4_baseline_goldens.txt` | Generated snapshot (git status) | No |
| `artifacts/m4/m4_after.txt` | Generated snapshot | No |
| `artifacts/m4/m4_baseline_pre_apply.txt` | Generated snapshot | No |
| `artifacts/m5a-emitter-baseline/manifest.json` | Generated snapshot (baseline manifest) | No |
| `artifacts/m5a-emitter-baseline/focused_pytest.log` | Generated snapshot | No |
| `artifacts/m5a-emitter-baseline/full_pytest.log` | Generated snapshot | No |
| `artifacts/m5a-emitter-baseline/image__z_image.py` | Generated snapshot (baseline template copy) | No |
| `artifacts/m5a-emitter-baseline/video__ltx2_3_t2v.py` | Generated snapshot (baseline template copy) | No |
| `artifacts/m5a-emitter-after/manifest.json` | Generated snapshot (after manifest) | No |
| `artifacts/m5a-emitter-after/.gitkeep` | Placeholder | No |
| `artifacts/m5a-emitter-after/image_z_image.py` | Generated snapshot (after template copy) | No |
| `artifacts/m5a-emitter-after/video_ltx2_3_t2v.py` | Generated snapshot (after template copy) | No |

### 2. Active Contracts vs. Historical Snapshots

**Active contracts: zero.** No file under `artifacts/` is imported, referenced by CI, or consumed at runtime. The `m6-public-api.md` *describes* the API surface but is not itself a contract — it's a human review artifact. The two template `.py` files under `m5a-emitter-baseline/` and `m5a-emitter-after/` are verbatim copies of production emitter code (identical SHA-256s across baseline/after manifests), frozen for side-by-side comparison during M5a review.

**Historical snapshots: all 19.** Everything in `artifacts/` was generated during single-pass milestone reviews (M1, M2, M4, M5, M5a, M6) and is now static.

### 3. Does `artifacts/` Earn a Top-Level Directory?

**No.** A `docs/` directory already exists at the project root. `artifacts/` is a historical review log — semantically a subcategory of documentation. It contains no files consumed by build, test, or runtime tooling. The directory name itself is generic; nothing about it signals "active contract" vs. "one-off review dump." The project has identical content in `docs/historical/` (e.g., `docs/historical/graphbuilder_spike.md`), confirming the established pattern.

### 4. Safe First Cleanup Recommendation

**Archive the entire `artifacts/` tree under `docs/historical/review-artifacts/`.** Specifically:

1. `mkdir -p docs/historical/review-artifacts/m4 docs/historical/review-artifacts/m5a-emitter-{baseline,after}`
2. `mv artifacts/m1-*.md artifacts/m2-*.md artifacts/m6-public-api.md docs/historical/review-artifacts/`
3. `mv artifacts/m5_baseline_red.txt artifacts/m5_agent_edit_imports.txt docs/historical/review-artifacts/`
4. `mv artifacts/m4/* docs/historical/review-artifacts/m4/`
5. `mv artifacts/m5a-emitter-baseline/* docs/historical/review-artifacts/m5a-emitter-baseline/`
6. `mv artifacts/m5a-emitter-after/* docs/historical/review-artifacts/m5a-emitter-after/`
7. `rmdir -p artifacts/m4 artifacts/m5a-emitter-* artifacts/` (removes empty dirs)

No CI or test references any `artifacts/` path (confirmed by grep). The move is pure restructuring — zero behavior change. Verify with `git status` and `git diff --stat` showing only a directory rename and deleted empty root.