Here is the complete synthesis based on full inventory and cross-reference tracing.

---

## `artifacts/` — Ordered Actions

### KEEP IN PLACE (4 items with active cross-references)

| File | Referenced by | Reason |
|---|---|---|
| `artifacts/m1-safety-gate.md` | `docs/megaplan_chains/pristine_cleanup/ideas/m5a-emitter-split.md`, `.../m1-triage-safety-net.md` | Active planning doc dependency |
| `artifacts/m2-symbol-map.md` | `docs/megaplan_chains/pristine_cleanup/ideas/m2-shared-foundation.md` | Active planning doc dependency |
| `artifacts/m6-public-api.md` | `docs/.../review-adjustments-2026-05-27.md`, `docs/.../m6-conventions.md` | Active planning doc dependency |
| `artifacts/m4/m4_baseline_pre_apply.txt` | `docs/m4_resolution_context.md` (line 106, golden-diff gate) | Test/document path dependency |

### KEEP IN PLACE (generated snapshots — path-locked)

**`artifacts/m5a-emitter-baseline/`** — `manifest.json` hardcodes its own children paths (`"path": "artifacts/m5a-emitter-baseline/image__z_image.py"`). Moving would break the manifest's contract. Keep.

**`artifacts/m5a-emitter-after/`** — contains `.gitkeep`, intentionally preserved as an empty-structure placeholder. Keep.

**`artifacts/m4/m4_baseline_goldens.txt`, `artifacts/m4/m4_after.txt`** — companion to the referenced baseline file. Keep for consistency.

**`artifacts/m5_baseline_red.txt`, `artifacts/m5_agent_edit_imports.txt`** — m5-phase generated diffs. No external references found, but they are historical emissions of a process step. Safe to defer.

### MOVE TO `docs/milestones/` (2 orphaned milestone docs, zero references)

```
mv artifacts/m1-step1-audit.md   docs/milestones/m1-step1-audit.md
mv artifacts/m2-diff-hygiene.md  docs/milestones/m2-diff-hygiene.md
```

**Verification:**
```bash
grep -r "artifacts/m1-step1-audit" docs/ --include="*.md"  # expect 0 hits
grep -r "artifacts/m2-diff-hygiene" docs/ --include="*.md"  # expect 0 hits
```

---

## `out/` — Deferrals (gitignored, needs approval)

Per constraint: all deletions require user approval. `out/` is gitignored (`out/` on line 14 of `.gitignore`). These are runtime/agent logs, pre-mortem results, and migration reports.

| Path | Description | Recommendation |
|---|---|---|
| `out/runs/` (6 files) | Agent chat chains, harness output, megaplan logs | **Defer** — runtime logs, no tracking value |
| `out/emit_survey_baseline.json` | Emitter survey snapshot | **Defer** — likely a one-off survey output |
| `out/migration_reports/*.md` | Human-readable migration report (105 lines) | **Defer** — could be moved to `docs/migration_reports/` if user approves; currently in ignored tree |
| `out/pre_mortem_text_to_graph/` (12 files) | Briefs + results + meta.json | **Defer** — agent scratchpad output |
| `out/k4/MISSING.txt` | Placeholder | **Defer** — ask user |

**If user approves deletion:**
```bash
rm -rf out/runs out/emit_survey_baseline.json out/pre_mortem_text_to_graph out/k4
```

---

## Summary Execution Order

```
Step 1   mkdir -p docs/milestones
Step 2   mv artifacts/m1-step1-audit.md  docs/milestones/
Step 3   mv artifacts/m2-diff-hygiene.md docs/milestones/
Step 4   Verify: grep -r "artifacts/m1-step1-audit" docs/ --include="*.md"
         Verify: grep -r "artifacts/m2-diff-hygiene" docs/ --include="*.md"
Step 5   Verify: git status --short (confirm 2 deleted, 2 untracked in docs/)
Step 6   Submit out/ deletions for user approval
```

**Total affected:** 2 moves (`artifacts/`), 0 deletions executed, ~12 files deferred for user approval.