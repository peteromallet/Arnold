Here's the complete audit of `out/`:

---

## Out/ Directory — Full Audit

**.gitignore status**: `out/` is explicitly ignored (line 14). Everything below is invisible to git.

---

### 🗑️ Clearly Disposable Output (keep ignored)

| Path | Contents | Verdict |
|------|----------|---------|
| `out/design_option1.png`, `live_panel*.png` | Screenshots/mockups | Disposable |
| `out/runs/*.png` (e.g. `20260602-152425-…`) | Agent-generated run diagrams | Disposable |
| `out/runs/*.log`, `out/runs/metadata.json` | Run logs and metadata | Disposable |
| `out/runs/agent_edit_e2e_harness.out` | Test harness stdout | Disposable |
| `out/runs/*/metadata.json` | Per-run metadata | Disposable |
| `out/k4/MISSING.txt` | Missing-node frequency table | Disposable diagnostic |
| `out/cache/object_info.*.json` | Cached ComfyUI introspection | Disposable cache |
| `out/runpod_artifacts/*/manifest.json` | Runpod execution manifests | Disposable |
| `out/runpod_artifacts/*/report.md` | Runpod reports | Disposable |
| `out/runpod_artifacts/*/semantic_red_square.json` | API probe config | Disposable |
| `out/runpod_artifacts/*/qwen_image_2512/*.png` | Generated test images | Disposable |
| `out/emit_survey_baseline.json` | 212KB T1 emit baseline survey | Disposable |
| `out/migration_reports/*.md` | Migration analysis docs | Disposable |
| `out/pre_mortem_text_to_graph/briefs/*.md` | Pre-mortem analysis briefs | Disposable |
| `out/pre_mortem_text_to_graph/results/*.json` | Pre-mortem results | Disposable |
| `out/subagent_reviews/**/*.json` | Agent review metadata/reports | Disposable |
| `out/scratchpads/**/*.py` | Auto-generated template code (stamped "generated — will be overwritten") | Disposable |

### ⚠️ Misplaced Source Artifacts (3 candidates)

1. **`out/layer4/_litegraph_open_check.mjs`** — Hand-written Node.js script that validates workflow envelope files. This is source code, not output. It should live in `tests/e2e/helpers/` alongside the existing `canvas-debug-probes.mjs`, `dom-probes.mjs`, etc.

2. **`out/layer4/_litegraph_schema_v1.mjs`** — Hand-written Zod schema definition for litegraph workflow envelopes. This is a source-level schema, not generated output. Belongs in `tests/e2e/helpers/` as a test utility.

3. **`out/layer4/smoke_workflow.json`** — A literal ComfyUI workflow JSON used as a smoke-test fixture. If it's a known-good reference workflow for testing, it belongs in `tests/fixtures/`. If it was generated by a one-off probe, it's disposable.

---

### Recommendations

**Clean these 3 into source tree:**
```
mv out/layer4/_litegraph_open_check.mjs  tests/e2e/helpers/
mv out/layer4/_litegraph_schema_v1.mjs   tests/e2e/helpers/
# Review smoke_workflow.json — keep in tests/fixtures/ if valuable, else rm
```

**Everything else** (~100+ files in runs/, cache/, runpod_artifacts/, pre_mortem_text_to_graph/, subagent_reviews/, scratchpads/, migration_reports/) is correctly ignored disposable output. No action needed — it can be wiped on disk with `rm -rf out/` when disk pressure hits.

**One hygiene note**: the `out/scratchpads/` `.py` files reference `tools/convert_ready_templates.py` as their generator but the target `ready_templates/` directory doesn't exist in source. Either the converter never ran to completion, or scratchpads are intermediate artifacts. Either way they're correctly in `out/`.