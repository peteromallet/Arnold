All evidence collected. Here's the final audit:

---

## Dead Module Audit 05: `patches/resize_schema.py`

### Import Analysis

| Direction | Result |
|---|---|
| **Imports** (what it pulls in) | `vibecomfy.workflow.VibeWorkflow` — no problem |
| **Imported by** (who pulls it in) | **Zero.** Not imported anywhere in `vibecomfy/`, not in `tests/`, not in scripts or CLI. |
| **`patches/__init__.py`** | Not present — `__getattr__` lazy-loads 6 modules (`controlnet`, `gguf_unet`, `ltx_lowvram`, `resolution`, `save_prefix`, `seed`). `resize_schema` is absent. |
| **`patches/builtins.py`** | Not present — `BUILTIN_PATCHES` tuple only contains `controlnet`, `gguf_unet`, `ltx_lowvram`. |

### Test Analysis

Two tests in `tests/test_ready_templates.py` have `resize_schema` in their name:

- `test_ltx_raw_video_guide_uses_live_resize_schema_inputs` (line 307)
- `test_ltx_iclora_control_uses_live_resize_schema_inputs` (line 319)

**Neither imports or calls `ensure_resize_image_mask_schema()`.** They compile ready templates and assert the live inputs **don't** contain `resize_type.*` keys — i.e., they verify the problem the module was meant to fix doesn't exist anymore. Deleting the module won't affect these tests (they'll continue failing as they're in `known_failures.txt` for unrelated reasons).

### Doc Evidence

Every doc that mentions this file calls it an orphan and recommends deletion:
- `docs/plans/revised_plan.md` — "Step 4: Delete patches/resize_schema.py"
- `docs/structure_cleanup/package_results/07-blocks-patches-ops-router.txt` — "Safe to delete"
- `docs/megaplan_chains/pristine_cleanup/audit/04-layer2-architecture.md` — "orphan in the patches directory"
- `docs/megaplan_chains/pristine_cleanup/RESULTS.md` — "won't-fix"
- `docs/structure_cleanup/status.md` — listed as dead file

### Command / Export References

None. Not referenced in any CLI, registry, or packaging config.

---

### Verdict: **DELETE**

**Rationale:** Zero imports, zero usages, not exported from any package boundary, the function it contains is not a `Patch` dataclass, multiple docs explicitly say to delete it, and the two tests with "resize_schema" in their names are functionally independent of the module.