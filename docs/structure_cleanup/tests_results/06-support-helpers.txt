Now I have a complete view. Here is the comprehensive audit.

---

## Tests Support Helpers: Layer Audit

### 1. `tests/support/` — Already Exists, Underused

| File | Size | Purpose |
|---|---|---|
| `tests/support/__init__.py` | empty | Package marker |
| `tests/support/corpus_schema.py` | 452 lines | `GraphInferredSchemaProvider` — infers ComfyUI node schemas from graph link evidence |

**Imported by (exact references):**
```
tests/test_porting_edit_session_harness.py:24  → from tests.support.corpus_schema import GraphInferredSchemaProvider, graph_inferred_schema_provider
tests/test_porting_corpus_schema_adapter.py:19 → from tests.support.corpus_schema import GraphInferredSchemaProvider, graph_inferred_schema_provider
```

**Verdict:** Only 2 consumers. The module is fat (452 lines) and specific to schema-inference. It's correctly placed in `tests/support/` but is isolated — no other support helpers live here.

---

### 2. Cross-Directory Imports (Unstable Paths)

**`tests/conftest.py` imports from `tests/smoke/`:**

| Line | Import |
|---|---|
| 97 | `from tests.smoke import _runpod_helpers as _rh` |
| 113 | `from tests.smoke import _runpod_helpers as _rh` |

This is a **parent→child** directory import — `tests/conftest.py` (root) reaches into `tests/smoke/_runpod_helpers.py`. If `smoke` were ever restructured or removed, the root conftest would break. The conftest only uses `set_pytest_config()` and `get_budget_state()` — two small functions that handle config stashing and budget state.

**Other cross-directory references are all intra-package and stable:**
- `tests/characterization/ → ._canon` (relative import, within same package) ✅
- `tests/smoke/ → ._runpod_helpers` (relative import, within same package) ✅
- All `tests/_runtime_session_helpers.py` imports are from sibling test files (`tests.*`) ✅

---

### 3. Current Helper Module Inventory

#### a) `tests/_runtime_session_helpers.py` (220 lines)

Exports: `FakeConfiguration`, `_default_configuration`, `fake_comfy` (fixture), `_workflow`, `WarmProvider`, `FakeResponse`, `FakeAsyncClient`, `FakeProcess`, `fake_server` (fixture), `_patch_fast_runtime_run`

**Consumers (7 files):**
```
tests/test_runtime_session_config.py:29
tests/test_runtime_session_validation.py:29
tests/test_runtime_session_embedded.py:12
tests/test_runtime_session_run_untracked.py:17
tests/test_runtime_session_server.py:13  (also line 69)
```

#### b) `tests/_cli_helpers.py` (105 lines)

Exports: `_top_level_commands`, `_write_fetch_scratchpad`, `_write_port_node_index`, `_write_port_workflow`, `_load_emitted_provenance`

**Consumers (3 files):**
```
tests/test_cli_misc.py:16
tests/test_cli_port.py:15
tests/test_cli_models_fetch.py:10
```

#### c) `tests/smoke/_runpod_helpers.py` (381 lines)

Exports: `HOURLY_USD`, `POD_NAME_PREFIX`, `set_pytest_config`, `get_budget_state`, `reset_budget_state`, `precharge_budget`, `settle_budget`, `require_runpod_api_key`, `load_runpod_lifecycle`, `pod_name`, `resolve_repo_install_target`, `install_current_branch`, `ensure_node_packs`, `launch_with_retry`, `launch_with_budget`

**Consumers (5 smoke test files + conftest.py):**
```
tests/smoke/test_layer2_runpod_dropped.py:8
tests/smoke/test_layer2_runpod_ops.py:8
tests/smoke/test_layer2_runpod_matrix.py:21
tests/smoke/test_p1_runpod.py:8
tests/smoke/test_z_image_only.py:13
tests/conftest.py:97,113  ← cross-directory
```

#### d) `tests/characterization/_canon.py` (96 lines)

Exports: `neutralize_paths`, `neutralize_seeds`, `strip_volatile_ui`, `structural_hash`

**Consumers (2 files, both local — correct containment):**
```
tests/characterization/test_agent_edit_roundtrips.py:23 (from . import _canon)
tests/characterization/test_emitter_snapshots.py:26 (from . import _canon)
```

#### e) `tests/characterization/conftest.py` (31 lines)

Contains one autouse fixture `_require_pythonhashseed_zero` — characterization-gate enforcement. Properly contained.

---

### 4. Duplicated Helper Functions (Should Centralize)

#### a) `_wf()` / `_workflow()` factory — duplicated across ~10+ files

Every test file that exercises `VibeWorkflow` invents its own private factory:

| File | Signature |
|---|---|
| `test_porting_ui_emitter.py:22` | `def _wf(wf_id="test") -> VibeWorkflow` |
| `test_porting_workbench.py:618` | `def _node(wf, class_type, _id, ...)` |
| `test_porting_edit_session.py:55` | `def _wf_from_raw_ui(raw) -> VibeWorkflow` |
| `test_runtime_run.py:23` | `def _workflow() -> VibeWorkflow` (inline) |
| `test_reconcile.py:276` | `def _wf(wf_id) -> VibeWorkflow` |
| `test_porting_convert.py:14` | `def _wf(wf_id="test-cvw") -> VibeWorkflow` |
| `test_layout_store.py:30` | `def _wf(wf_id="test") -> VibeWorkflow` |
| `test_runtime_session_helpers.py:80` | `def _workflow(ckpt, *, seed) -> VibeWorkflow` |
| `test_schema.py:139` | `def _workflow(*nodes, edges=...) -> VibeWorkflow` |
| `test_schema_validate.py:29` | `def _workflow(*nodes) -> VibeWorkflow` |
| `test_porting_synthetic_fixtures.py:26` | `def _wf(wf_id="test") -> VibeWorkflow` |

#### b) `_schema()` factory — duplicated across ~4 files

| File | Signature |
|---|---|
| `test_porting_ui_emitter.py:36` | `def _schema(class_type, outputs, *, confidence, provider)` |
| `test_schema_validate.py:35` | `def _schema(class_type, inputs)` |
| `test_ui_emitter_widget_shape_verdict.py:25` | `def _schema(...)` |
| `test_porting_edit_session.py:3458` | `def _primitive_session()` (builds schema inline) |

#### c) `_reconstruct_api_from_links` — near-duplicate in 2 files

- `tests/property/test_emitter_fuzz.py:128-191` — full reconstruction logic
- `tests/parity/test_independent_readback.py:42-162` — same algorithm, slightly different signature

These are **not identical** but the core link-walking logic is the same algorithm. Worth extracting if more parity tests are added.

---

### 5. Sub-Packages Found in `tests/`

| Sub-package | Files | `__init__` | `conftest` | Notes |
|---|---|---|---|---|
| `tests/support/` | `corpus_schema.py` | ✅ (empty) | ❌ | Underpopulated, only 1 module |
| `tests/characterization/` | 4 test files + `_canon.py` | ✅ | ✅ | Well-organized |
| `tests/smoke/` | 5 test files + `_runpod_helpers.py` | ✅ (empty) | ❌ | Missing conftest for runpod imports |
| `tests/intent/` | 7 test files + sub-smoke/ dir | ✅ (empty) | ❌ | Clean |
| `tests/security/` | 11 test files | ✅ (empty) | ❌ | Largest sub-package |
| `tests/parity/` | 2 test files + fixtures/ | ✅ | ❌ | Clean |
| `tests/property/` | 1 test file | ✅ | ❌ | Sparse |
| `tests/e2e/` | JS helpers (different domain) | N/A | N/A | Separate JS ecosystem |
| `tests/fixtures/` | `agent_generated_loader/` (13 files) | ❌ | ❌ | Data fixtures, not code |

---

### 6. Duplicate `_write_scratchpad` / temp file helpers

- `tests/_cli_helpers.py:51-97` — `_write_port_node_index`, `_write_port_workflow`
- `tests/test_doctor_lockfile.py:13` — `_write_scratchpad(path) → Path`
- `tests/test_doctor_models.py:21` — `_write_scratchpad(path) → Path`
- `tests/test_testing_snapshot.py:18` — `_tiny_recipe(tmp_path) → Path`

These each write different content — not direct duplication but the pattern repeats.

---

### 7. Recommendations

**🔴 Immediate (smoke → conftest boundary):**
Move `set_pytest_config` and `get_budget_state` from `tests/smoke/_runpod_helpers.py` into `tests/conftest.py` directly (or `tests/_conftest_helpers.py`), eliminating the parent→child import at `tests/conftest.py:97,113`. The conftest only needs these two budget-state functions — not the entire 381-line runpod helper module.

**🟡 Propose centralizing into `tests/support/`:**

1. **Move `tests/_runtime_session_helpers.py` → `tests/support/runtime_session_helpers.py`**
   - Update 7 import sites from `from tests._runtime_session_helpers import ...` → `from tests.support.runtime_session_helpers import ...`
   - Exact import references to update:
     ```
     tests/test_runtime_session_config.py:29
     tests/test_runtime_session_validation.py:29
     tests/test_runtime_session_embedded.py:12
     tests/test_runtime_session_run_untracked.py:17
     tests/test_runtime_session_server.py:13  (also line 69)
     ```

2. **Move `tests/_cli_helpers.py` → `tests/support/cli_helpers.py`**
   - Update 3 import sites:
     ```
     tests/test_cli_misc.py:16
     tests/test_cli_port.py:15
     tests/test_cli_models_fetch.py:10
     ```

3. **Move `tests/characterization/_canon.py` → `tests/support/canon.py`** (if cross-directory consumers appear; currently only 2 local consumers use relative imports `from . import _canon`)
   - For now leave in place — it's well-contained.

**🟡 Propose deduplication:**

4. **Extract common `_wf()` factory** from the ~10 files listed above into `tests/support/workflow_factories.py` with named builders:
   ```python
   def simple_workflow(wf_id="test", *, ckpt="model-a.safetensors") -> VibeWorkflow
   def two_node_workflow(wf_id="test") -> VibeWorkflow
   ```

5. **Extract `FakeResponse`, `FakeAsyncClient`** from `_runtime_session_helpers.py` into `tests/support/fakes.py` — they're useful beyond just session tests.

**🟢 Safe cleanups (no import churn):**

6. `tests/smoke/_runpod_helpers.py` is well-organized; leave it unless the conftest boundary is fixed (see #1).

7. `tests/characterization/_canon.py` is clean, only used locally — leave it.

8. The `tests/fixtures/agent_generated_loader/` dir is data (loadable `.py` files), not code — leave it.

---

### 8. Proposed Final `tests/support/` Layout

```
tests/support/
├── __init__.py              (existing, empty)
├── corpus_schema.py         (existing, 452 lines — keep)
├── fakes.py                 (NEW — FakeResponse, FakeAsyncClient, FakeProcess, FakeComfy)
├── runtime_session_helpers.py  (MOVE from tests/_runtime_session_helpers.py)
├── cli_helpers.py              (MOVE from tests/_cli_helpers.py)
└── workflow_factories.py       (NEW — common _wf() / _workflow() builders)
```

Total churn: **~12 import updates** across 8 test files. No test logic changes.