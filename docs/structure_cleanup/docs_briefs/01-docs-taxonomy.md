Working directory: /Users/peteromalley/Documents/reigh-workspace/vibecomfy

You are a DeepSeek audit subagent reviewing only the `docs/` directory taxonomy.

Current top-level docs files include many clusters:
agent-edit docs, agent_text_to_graph docs, runtime docs, runpod docs, testing docs,
template/porting docs, migration docs, structural/audit docs, workflow coverage docs,
plus subfolders: agent_edit_failure_evidence, assets, audits, cookbook, historical,
megaplan-briefs, megaplan_chains, plans, release_notes, security, structure_cleanup,
testing-user-code-examples.

Task:
- Propose a docs taxonomy that makes top-level docs easy to scan.
- Identify top-level files that should move into subfolders.
- Identify subfolders that should be renamed, merged, or left alone.

Return under 450 words:
1. Proposed `docs/` taxonomy.
2. Highest-confidence file moves.
3. Moves that need link/reference checks.
4. What should remain top-level in `docs/`.
