I now have a comprehensive view of the entire `.megaplan/` tree. Here is the complete audit report.

---

## 🧹 `.megaplan/` Audit — Runtime/Cache/Log State Analysis

**Scope root:** `/Users/peteromalley/Documents/reigh-workspace/vibecomfy/.megaplan/`  
**Git status:** Entirely gitignored via `.gitignore` line 29 (`.megaplan/`)

---

### 1. IGNORED DIRECTORIES — Generated State (safe-delete-with-approval)

| Directory | Contents | Classification |
|---|---|---|
| `.state-locks/` | 20+ `.lock` files naming completed epics/sprints | 🔒 Stale advisory locks — safe to delete |
| `plans/` | ~15 plan subdirectories each with: `execution_batch_*.json`, `state.json`, `step_receipt_*.json`, `critique_*.json`, `finalize.json`, `faults.json`, `gate.json`, `events.ndjson`, `.plan.lock`, `.hermes_state/*.db`, `.events.seq` | 🗑️ Agent runtime execution traces — bulk of disk usage |
| `epics/` | 4 subdirs with `events.jsonl` — e.g. `s3-state-protocol-and-20260601-0553/` | 🗑️ Epic event logs — safe if milestones are closed |
| `telemetry/` | `.seq`, `.ndjson` files — `s3-state-protocol-*`, `s4-editable-*`, `s5-static-*` | 🗑️ Runtime telemetry — replayable |
| `logs/` | `structural-decomp-drive.log`, `reorg-chain-drive.log`, `m0-merge-fullsuite.log`, `loop-progress.md` | 🗑️ Agent drive logs — safe to delete |
| `wakeup/` | `excellence-epic-chain.log`, `excellence-epic-chain.pid` | 🔒 Stale wakeup/cron state — safe to delete |
| `chains/` | `frontier-hardening.yaml` | ⚠️ **Uncertain** — may be an authored chain definition (only 1 file) |

### Generated Top-Level Files

| File | Reason |
|---|---|
| `debt.json` | Accumulated debt tracker — historically generated |
| `init-local-library.log` | Runtime log from local-library init |
| `auto-local-library.log` | Runtime log |
| `plan-native-test.log` | Test run log |
| `plan-retest.log` | Test run log |
| `plan-sandbox-test.log` | Test run log |

### Nested Artifact Pollution

A deeply nested generated artifact was found at:
```
.megaplan/briefs/text-to-graph-agent-epic/.megaplan/plans/.chains/chain-5da176d49554.json
```
This is a **runaway nested `.megaplan/`** generated during agent execution — clearly garbage and should be removed.

---

### 2. IGNORED FILES/DIRECTORIES — Authored / Reusable (KEEP)

| Path | What it is | Recommendation |
|---|---|---|
| `briefs/*.md` (~40 files) | Author-written task briefs, STRATEGY documents | ✅ **KEEP** — authored content |
| `briefs/*/chain.yaml` (8 files) | Agent chain definitions (milestone sequencing) | ✅ **KEEP** — authored workflow definitions |
| `schemas/*.json` (18 files) | JSON schemas for critique, execution, plan, gate, review, etc. | ✅ **KEEP** — authored data contracts |
| `audit-adaptive-critique.md` | Author-written design doc | ✅ **KEEP** — authored document |

---

### 3. UNCERTAIN / Needs Closer Inspection

| Item | Reason | Suggested Action |
|---|---|---|
| `chains/frontier-hardening.yaml` | Single file in a `chains/` dir — could be authored chain config or generated | **Ask user** — if authored, move to `briefs/` or rename; if generated, delete |
| `debt.json` | Could be a maintained debt log or a one-off generation artifact | **Ask user** — if manually curated, keep; if auto-generated stale snapshot, delete |

---

### 4. Estimated Disk Impact

Based on the file count (~600+ files, many `execution_batch_*.json` multi-MB artifacts), the `plans/` directory is the dominant consumer. The `.hermes_state/*.db` SQLite databases (db + wal + shm triples per plan) also contribute significantly. Large files >5M likely exist inside `plans/` subdirectories (batch files, traces).

---

### 5. Proposed Cleanup Policy

```markdown
# .megaplan/ Cleanup Policy

## Auto-clean (requires user approval per run)
- `plans/*/` — All completed plan subdirectories (keep only if plan is still active)
- `.state-locks/*.lock` — Stale locks for completed work
- `epics/*/events.jsonl` — Epic event logs after milestone closure
- `telemetry/*` — Telemetry after epic completion
- `logs/*` — Agent drive logs (keep ~3 most recent)
- `wakeup/*` — Stale wakeup state
- `*.log` (top-level) — Single-run logs

## Never auto-clean (authored)
- `briefs/` — All briefs, chain.yaml, STRATEGY docs
- `schemas/` — All JSON schemas
- `audit-adaptive-critique.md`

## Requires explicit user decision
- `chains/frontier-hardening.yaml` — ambiguous origin
- `debt.json` — ambiguous origin
- Any active-plan plans/ subdirectory (check `.plan.lock` recency)
```

---

### 6. Summary Lists

#### ✅ SAFE TO DELETE (with approval)
```
.megaplan/.state-locks/           (21 stale lock files)
.megaplan/plans/                  (15 completed plan dirs, ~500+ artifacts)
.megaplan/epics/                  (4 epic event logs)
.megaplan/telemetry/              (6 telemetry files)
.megaplan/logs/                   (5 log files)
.megaplan/wakeup/                 (2 stale files)
.megaplan/init-local-library.log
.megaplan/auto-local-library.log
.megaplan/plan-native-test.log
.megaplan/plan-retest.log
.megaplan/plan-sandbox-test.log
.megaplan/briefs/text-to-graph-agent-epic/.megaplan/   (runaway nested garbage)
```

#### ❌ KEEP (authored / reusable)
```
.megaplan/briefs/                 (all .md files + chain.yaml files)
.megaplan/schemas/                (all .json schema files)
.megaplan/audit-adaptive-critique.md
```

#### ⚠️ UNCERTAIN (ask user)
```
.megaplan/chains/frontier-hardening.yaml   (single file — authored or generated?)
.megaplan/debt.json                         (manual debt tracker or auto-generated?)
```

**Bottom line:** The `plans/` directory is the heavy hitter — it contains hundreds of JSON artifacts from completed agent runs. The `briefs/` and `schemas/` directories are authored and should be preserved. Two files need a human judgment call.