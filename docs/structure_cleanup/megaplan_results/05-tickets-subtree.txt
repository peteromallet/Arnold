Here's my full analysis.

---

## Inspection Results

### Git Tracking vs On-Disk Inventory

| Check | Result |
|---|---|
| `.gitignore` line 29 | `.megaplan/` — **entire subtree is git-ignored** |
| `git ls-files .megaplan/tickets` | **Empty** — zero files tracked in the git index |
| `find .megaplan/tickets -maxdepth 1 -type f \| sort` | **29 files**, all on disk only |

**Conclusion: There are no "tracked" ticket files.** All 29 files are untracked/ignored. The two specific tickets you referenced (`01KRKQGP81Z5XR0FAK19T5CAC8` and `01KRNDP7S3BW6DMNKAWPNVVYMB`) are on disk but git-ignored, just like the other 27.

---

### Quality Assessment of the Two Example Tickets

Both are **high-quality, human-authored backlog items** with:

- Valid YAML frontmatter (id, title, `status: open`, `source: human`, meaningful tags, timestamps)
- Detailed problem/context sections (several paragraphs each)
- Structured acceptance criteria (numbered 1–8 and 1–5 respectively)
- Cross-repo scope awareness (VibeComfy ↔ Reigh ↔ Astrid)
- Non-goals sections bounding scope

These are clearly **backlog source-of-truth**, not stale artifacts. They describe real, unresolved engineering problems that other tracked docs depend on:

- `docs/historical/sprint5_followups.md` explicitly cites both as "related context only" and says "Do not mark either ticket resolved from the Sprint 4 changes alone"
- `docs/templates/strict_ready_exceptions.json` and `.md` reference `01KRNDP7S3BW6DMNKAWPNVVYMB` as a follow-up ticket for 10+ blocked exceptions

### Quality Assessment of Sampled Ignored Tickets

I spot-checked 4 more ignored tickets (different dates, tags, sizes). All have the same structure and quality:

| File | Topic | Quality |
|---|---|---|
| `01KT6PFVRTKTD4ZKXNQ6D567Q7` | Diff preview overlay UX | Well-scoped, structured |
| `01KT6PFVEQZEVYDBAHVQQH11B2` | Tagged arnold release, pin deps | Concrete, actionable |
| `01KTPNS191FA0P2WBP8HYVXJ8V` | Split 4.6k-line god file | Clear refactor plan, links epics |
| `01KTA9X8CK6J82G7J6K3TGG9ZX` | Cross-conversation agent memory | Future-scoped, well-bounded deferral |

Dates range **May 14 → June 9, 2026** (recent). Several have non-null `last_edited_at` after creation, showing active curation. One (`01KTPNS191FA0P2WBP8HYVXJ8V`) even participates in an epic structure (`structural-decomposition`).

---

## Answers to Your Questions

### 1. Are the two tracked tickets backlog source-of-truth or stale ticket artifacts?

**Neither is tracked**, but if they were they'd be **backlog source-of-truth**. They are:
- Current (`status: open`, recent dates)
- Referenced by tracked documentation as blocking/stakeholder issues
- Structurally complete with actionable acceptance criteria
- Not stale — the sprint5 doc explicitly says they remain unresolved

**Deletion risk if removed**: Loss of structured context for two cross-cutting issues that tracked docs (`sprint5_followups.md`, `strict_ready_exceptions.json/.md`) depend on. The `strict_ready_exceptions` file would have 10+ orphaned `ticket` fields pointing to a non-existent document.

### 2. Do the ignored ticket files look valuable enough to document as local backlog, or should they remain ignored runtime/user state?

**They are valuable and should be tracked, not ignored.** They are:

- **Human-authored planning documents**, not runtime artifacts
- **Referenced from tracked docs** — removing them breaks cross-references
- **Epic-aware** — some participate in dependency chains (e.g., structural-decomposition epic)
- **Not transient** — the `.gitignore` entry at line 29 was likely a "ignore runtime" blanket rule that accidentally swept in intentional planning documents

**Recommendation**: Remove `.megaplan/` from `.gitignore` (line 29) and `git add` the tickets directory, OR narrow the ignore pattern to only cover runtime subpaths while letting `tickets/` through. For example:

```
# In .gitignore, replace line 29:
.megaplan/
# with:
.megaplan/**/  !.megaplan/tickets/
# OR more conservatively:
.megaplan/*.json
.megaplan/*.log
.megaplan/state/
```

**Deletion risk if left ignored**: These 29 files are invisible to collaborators, CI, and future agents. They are one `git clean -fd` or `rm -rf .megaplan` away from permanent loss. Tracked docs that reference them become stale themselves.

### 3. Should `.megaplan/tickets/` be documented in a README?

**Yes, but a minimal one.** The tickets are self-describing (YAML frontmatter + narrative), so a README only needs:

1. **Purpose**: "Planning and backlog tickets for vibecomfy development."
2. **Format**: "Each ticket is a markdown file with YAML frontmatter: id, title, status (open/closed/deferred), tags, created_at, last_edited_at, epics."
3. **Status convention**: What `status` values mean and how to update them.
4. **Epic linking**: How tickets reference epics and what `resolves_on_complete` means.
5. **How to add**: Naming convention (ULID-slug), required fields, where to store.

---

## Exact Recommendations

### A. Immediate (low risk, high value)

1. **Stop ignoring tickets**: Change `.gitignore` line 29 from `.megaplan/` to a narrower pattern that still ignores runtime state but *tracks* the tickets directory.
2. **`git add .megaplan/tickets/`** to bring all 29 into version control.
3. **Write a README** (1–2 paragraphs + field table) in `.megaplan/tickets/README.md`.

### B. Stretch (medium effort, nice to have)

4. **Add a `closed`/`done` status convention** and optionally archive resolved tickets to `.megaplan/tickets/archive/` (still tracked).
5. **Add a simple index** — the `find \| sort` output could be a generated or maintained `INDEX.md` in the directory with title, status, date, tags columns.

### C. Deletion Risk Summary

| Risk | Severity | Description |
|---|---|---|
| Orphaned cross-references | **High** | `strict_ready_exceptions.json` and `sprint5_followups.md` reference these ticket IDs |
| Silent data loss | **Medium** | Untracked files are invisible to `git status` changes; `git clean -fd` or disk wipe loses them permanently |
| Collaboration blind spot | **Medium** | CI, other agents, and collaborators cannot see or update these tickets without local knowledge |
| No change history | **Low** (today) | Currently none of the tickets have git history anyway, so this is neutral vs status quo |