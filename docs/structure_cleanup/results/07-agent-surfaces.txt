Here's the analysis:

---

## 1. Classification

| Entry | Status | Type |
|---|---|---|
| **AGENTS.md** | ✅ Exists (619 lines) | Agent instructions — bootstrap/executive summary of the repo rules, points to CLAUDE.md for canonical guide |
| **CLAUDE.md** | ✅ Exists (377 lines) | Agent instructions — canonical long-form doc with full authoring surface, vocabulary, policy rules |
| **.agents/** | ✅ Exists (1 skill: `skills/desloppify/SKILL.md`) | Agent instructions — coded agent skill (desloppify code-review skill) |
| `.claude` | ❌ Does not exist | — |
| `.megaplan` | ❌ Does not exist | — |
| `agentic` | ❌ Does not exist | — |
| `agents` | ❌ Does not exist | — |
| **artifacts/** | ✅ Exists (19 files) | Run artifacts — milestone audit docs, emitter baselines, pytest logs, `m6-public-api.md` (which is a reference doc) |
| **docs/megaplan_chains/** | ✅ Exists (50+ files) | Plans — milestone chains (excellence_epic, pristine_cleanup, readable_ready_templates) with handoffs, prep docs, chain.yaml, evidence JSON |
| **docs/megaplan-briefs/** | ✅ Exists (2 files) | Plans — user/internal testing briefs |

Also spotted at root: **`plan_v2.md`** and **`revised_plan.md`** — stale plan files not in the original list but equally problematic.

---

## 2. Root entries that should remain

- **AGENTS.md** — short bootstrap agent instructions
- **CLAUDE.md** — canonical agent guide
- **.agents/** — agent skills directory (dotfile, standard convention)
- **README.md** — already references AGENTS.md as the entry point; correct behaviour

---

## 3. Entries that should move/archive

| Entry | Recommendation | Destination |
|---|---|---|
| **artifacts/** | Move (run output, not source) | `docs/artifacts/` or `out/artifacts/` — this is milestone audit detritus and emitter baselines, not living documentation |
| **docs/megaplan_chains/** | Stays (in `docs/`) ✓ | Already inside `docs/`, correctly placed |
| **docs/megaplan-briefs/** | Stays (in `docs/`) ✓ | Already inside `docs/`, correctly placed |
| **plan_v2.md** (root) | Archive | `docs/plans/plan_v2.md` |
| **revised_plan.md** (root) | Archive | `docs/plans/revised_plan.md` |

---

## 4. Naming/README changes for self-explanatory clarity

1. **README.md already does the right thing** — it says *"Give your coding agent AGENTS.md first"* and links to it. Good.

2. **Rename `artifacts/` → `docs/artifacts/`** or better `out/audit/` — the word "artifacts" is overloaded (CI artifacts, build artifacts, agent run artifacts). `m6-public-api.md` is the only truly reference-worthy file in there; the rest are milestone comparison baselines and pytest logs that belong in `out/` or a timestamped archive.

3. **Consider adding a `.gitignore` entry** so generated baselines (`m5a-emitter-baseline/`, `m5a-emitter-after/`, `m4/`) don't get checked in. If they must stay, move them under `out/` or `docs/audit/`.

4. **Add a `docs/README.md`** or a section in the root README that says:

   ```
   ## Repository structure

   AGENTS.md, CLAUDE.md, .agents/  — Agent configuration (start here)
   docs/                           — Plans, briefs, architecture docs
   out/                            — Generated run output and artifacts
   ```

   This disambiguates "is `artifacts/` agent config or run output?" in one line.

5. **No changes needed for naming** — AGENTS.md, CLAUDE.md, and .agents/ are all standard and clear. The phantom entries (`.claude`, `.megaplan`, `agentic`, `agents`) don't exist, so nothing to clean up there.