Here is my root-clutter audit for vibecomfy.

---

## 1. Delete Now

| Item | Why |
|---|---|
| `.DS_Store` | macOS metadata, zero project value |
| `install.log` | Gitignored; 6-line one-time uv install log, stale |
| `node_index.json` | Gitignored; empty array `[]` — dead cache |
| `_debug_json.py` | Gitignored (`_debug_*.py`); one-shot probe of a specific workflow link ID |
| `_debug_normalize.py` | Gitignored; one-shot `normalize_to_api` probe |
| `_fix_t6.py` | One-shot migration script for `agent_edit.py` refactor; task is done |
| `_regen_templates.py` | Template regeneration with a hardcoded external worktree path; stale |

## 2. Move / Archive

| Item | Destination | Why |
|---|---|---|
| `this.env` | `~/.config/vibecomfy/env` | Contains **live API keys** (OpenAI, Anthropic, Gemini, Fal). Root position is a security risk. Already gitignored. |
| `finalize.json` | `docs/plans/` or `agentic/plans/` | M0 reorg task list (328 lines). If all tasks are `"status": "done"`, archive as historical artifact. |
| `template_index.json` (327 KB) | Delete after verification (see §3) or archive to `workflow_corpus/manifests/` | Generated index. Large, regenerable. |
| `workflow_index.json` | Delete after verification or archive to `workflow_corpus/manifests/` | Generated corpus index. |

## 3. Keep Until Verified

| Item | Exact Verification |
|---|---|
| `template_index.json` | Run `python tools/refresh_template_index.py` and confirm output matches current file. If yes → delete from root (it's regenerable). |
| `workflow_index.json` | Check if `tools/refresh_template_index.py` or similar generates it. If yes → delete from root. |
| `asset_manifest.json` | Run `python scripts/populate_model_hashes.py` (or equivalent) and confirm manifest is regenerable. If yes → delete from root. |
| `revised_plan.md` | Active M0 reorg plan (320 lines). Keep until all `finalize.json` tasks confirm `"status": "done"`, then archive to `docs/plans/`. |
| `plan_v2.md` | Active M3 corpus-proof plan (150 lines). Keep until milestones delivered, then archive. |

## 4. Permanent Root Entries

- `.gitignore`, `README.md`, `LICENSE`, `pyproject.toml`, `uv.lock`
- `custom_nodes.lock`, `version_matrix.json` — version/fingerprint config
- `AGENTS.md`, `CUSTOM_NODES_AUDIT.md`, `SECURITY_AUDIT_NOTES.md` — audit artifacts
- `cloud.yaml` — Railway deployment config
- `.importlinter`, `.github/` — CI/lint config
- `vibecomfy/` (package), `tests/`, `ready_templates/`, `vendor/`, `scripts/`, `tools/`, `docs/`, `workflow_corpus/`, `recipes/`, `agentic/`, `agents/`

**Bottom line:** ~7 files to delete immediately (junk + one-shot probes), 4 to move/archive (secrets + task plans + large regenerable indexes after verification), 5 keep-until-verified entries, and ~15 permanent root entries. The root is actually fairly clean — most clutter is already gitignored and can be safely pruned.