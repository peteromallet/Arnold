Now I have the full picture. Here's the assessment:

---

## 1. Keep at Root (tool/user expectations)

| File | Rationale |
|------|-----------|
| **AGENTS.md** | Cursor/continous agent config — tools scan root for it |
| **CLAUDE.md** | Claude Code/CLI agent config — tool auto-reads from root |
| **README.md** | GitHub, package index, human devs — root requirement |

These three are **hard-wired by convention/tools** and should not move.

## 2. Move to docs/

| Current Path | Destination | Rationale |
|-------------|-------------|-----------|
| `CUSTOM_NODES_AUDIT.md` | `docs/audits/custom_nodes_audit.md` | Audit artifact, not a config. Existing `docs/audits/` doesn't exist yet — create it. |
| `SECURITY_AUDIT_NOTES.md` | `docs/audits/security_audit_notes.md` | Same — pairs naturally with the audit folder. |
| `plan_v2.md` | `docs/plans/plan_v2.md` | Implementation plan, not root-level. |
| `revised_plan.md` | `docs/plans/revised_plan.md` | Same. |

## 3. Archive Convention: `docs/historical/` vs `docs/plans/`

**Use `docs/plans/` for active/current plans** (plan_v2, revised_plan are active M3/M0 work docs, not historical spikes).

**Reserve `docs/historical/` for genuinely stale spikes** (graphbuilder_spike.md, comfyscript_spike.md, old port rationales — already there).

This matches your existing conventions: `docs/megaplan_chains/` holds chain-of-thought, `docs/plans/` holds flat plan docs, `docs/historical/` holds deprecated exploration.

## 4. Follow-up: Link/Reference Audit Needed

The **README.md** and **AGENTS.md** already reference internal docs by path. After moves, check these for stale `../../` references:

- `README.md` — likely links to plan docs → update to `docs/plans/...`
- `AGENTS.md` / `CLAUDE.md` — may reference `plan_v2.md` or `CUSTOM_NODES_AUDIT.md` inline → update paths
- Any `CUSTOM_NODES_AUDIT.md` cross-references from other docs → search with `search_files` for the old paths

Suggested grep: `search_files "pattern": "CUSTOM_NODES_AUDIT|plan_v2\.md|revised_plan\.md|SECURITY_AUDIT"` to find all stale links.

## 5. Taxonomy Improvement for Next Layer

`docs/` currently has flat files + megaplan subfolders + historical + structure_cleanup + evidence + user-code-examples. **Next layer could add:**

```
docs/
  audits/          ← new (custom_nodes, security)
  plans/           ← new (flat implementation plans)
  historical/      ← exists (stale spikes)
  megaplan_chains/ ← exists
  megaplan-briefs/ ← exists
  structure_cleanup/ ← exists
  assets/          ← exists
  guides/          ← maybe (runtime_lifecycle, adding_templates_models, etc.)
```

The flat `docs/*.md` files (runtime_lifecycle, adding_templates_models, characterization_gate, etc.) could stay flat or be sorted into `docs/guides/` and `docs/reference/` in a later pass — but that's scope-creep for now.