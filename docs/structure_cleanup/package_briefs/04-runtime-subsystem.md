# Package Layer Audit 04: Runtime Subsystem

Audit `vibecomfy/runtime/`, runtime eval/session helpers, and model staging hooks.

Questions:
- Which files define runtime API contracts?
- Are any runtime-local outputs/caches committed or ignored incorrectly?
- Are module names/layout coherent enough for navigation?
- What should be documented instead of moved?

Return exact safe actions and deferrals.
