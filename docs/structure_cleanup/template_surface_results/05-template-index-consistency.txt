Now I have the complete picture. Here are my findings.

---

## Consistency Inspection Report

### Raw Data Summary

| Source | Count | Matching Rule |
|--------|-------|---------------|
| **Files on disk** (`ready_templates/**/*.py` excluding `_*`, `__init__.py`) | **64** | `rglob("*.py")` excluding `__init__.py` and `_*` |
| **`template_index.json` entries** | **64** (all `"indexed": true`) | Same discovery logic |
| **`template_index.json` `template_count` field** | **64** ✅ | Matches actual count |

### 1. Are there obvious unindexed or orphaned ready templates?

**No.** The index and filesystem are perfectly in sync:

- All 64 `.py` files on disk have a corresponding entry in `template_index.json`
- All 64 entries in the index have a matching file on disk
- All 64 entries are marked `"indexed": true`
- No `_*-prefixed` files or `__init__.py` files exist in `ready_templates/` (so the exclusion rule is a no-op)
- No `__pycache__/` directories exist

The 14 additional files I initially thought were missing were just pagination-truncated results — the `ready_templates/` glob returns **64 total**, not 50.

### 2. Does any layout cleanup require regenerating indexes?

**Not currently**, but here's a latent risk:

The test helper in `tests/test_ready_templates.py:43` uses:
```python
sorted(Path("ready_templates").glob("*/*.py"))   # <-- single-depth only!
```
This would **miss** any template deeper than one subdirectory (e.g. `ready_templates/video/subdir/file.py`). Currently all 64 files are one level deep (`category/file.py`), so it works. The runtime code uses `rglob("*.py")` (line 328 of `ready.py`) which is correct. If you ever restructure into nested subdirectories, fix that test's glob to `rglob("*.py")`.

**If you DO restructure directories or rename files, you must regenerate these:**
| Artifact | Why |
|----------|-----|
| `template_index.json` | Contains all paths/IDs — stale paths break CLI listings |
| `vibecomfy/commands/workflows.py` `TEMPLATE_INDEX_PATH` | Reads the index |
| All tests enumerating template IDs | Snapshot lists, matrix scopes, coverage configs |

### 3. What verification should be run after any structure edit?

Since I can't execute commands in this environment, here are the **exact commands** to run:

```bash
# A. Count check — files vs index
echo "=== Files on disk ==="
find ready_templates -type f -name '*.py' ! -name '_*' | sort | wc -l

echo "=== Entries in template_index.json ==="
python3 -c "import json; d=json.load(open('template_index.json')); print(d['template_count'], len(d['templates']))"

# B. Path-by-path cross-check
python3 <<'PY'
import json
from pathlib import Path

index = json.load(open("template_index.json"))
idx_paths = {e["path"] for e in index["templates"]}

disk_paths = set()
for f in Path("ready_templates").rglob("*.py"):
    if f.name.startswith("_") or f.name == "__init__.py":
        continue
    disk_paths.add(str(f))

orphans = idx_paths - disk_paths     # in index, not on disk
unindexed = disk_paths - idx_paths    # on disk, not in index

if orphans:
    print(f"ORPHANED (in index but missing on disk): {len(orphans)}")
    for p in sorted(orphans): print(f"  🗑  {p}")
if unindexed:
    print(f"UNINDEXED (on disk but missing from index): {len(unindexed)}")
    for p in sorted(unindexed): print(f"  ❌  {p}")
if not orphans and not unindexed:
    print("✅ Perfect sync — 64/64 paths match.")
PY

# C. CLI listing (--json) — verify no runtime errors
python -m vibecomfy.cli workflows list --ready --json > /dev/null && echo "CLI OK"

# D. Test suite — the template inventory test
python -m pytest tests/test_ready_templates.py -v -k "test_no_helper_raw_call_in_ready_template" --co 2>&1 | tail -20
```

**All three questions answered:**
1. ❌ No unindexed/orphaned templates — disk and index are in perfect sync (64/64)
2. ⚠️ No regeneration needed now, but the `test_ready_templates.py` glob (`*/*.py`) is a trap if you ever nest deeper
3. ✅ The 4 commands above are your verification recipe after any structural edit