Now I have all the data. Let me compile the complete audit.

---

## Root Index/Manifest Audit — Complete Findings

### File Status Summary Table

| File | Size | `.gitignore` Listed | Git Tracked | Intentionally Committed? | Generator | Bare-Path Code References |
|---|---|---|---|---|---|---|
| **`template_index.json`** | ~342 KB | ✅ (line 11) | ✅ **TRACKED** (was added before gitignore) | ✅ **Yes** — repo-owned, CI-gated | `tools/refresh_template_index.py` | `workflows.py:21`, `test_ready_templates.py:147` |
| **`workflow_index.json`** | ~? | ✅ (line 10) | ❌ NOT TRACKED (ignored) | ❌ No — regenerable cache | `sources sync` via `sources.py:28` | `library.py:34`, `_workflow_path.py:9`, `bootstrap.py:9` |
| **`external_workflow_index.json`** | ~? | ✅ (line 12) | ❌ NOT TRACKED (ignored) | ❌ No — regenerable cache | `sources sync` via `sources.py:29` | `library.py:34`, `_workflow_path.py:9`, `bootstrap.py:10` |
| **`node_index.json`** | ~0.1 KB (empty `[]`) | ✅ (line 13) | ❌ NOT TRACKED (ignored) | ❌ No — regenerable cache | `sources sync` via `sources.py:30` | `bootstrap.py:8`, `test_nodes_index.py:39` |
| **`asset_manifest.json`** | ~11 KB | ❌ **NOT** listed | ❌ NOT TRACKED (orphan) | ❌ **No** — orphaned, no writer exists | **None** (stale artifact) | None in production code |
| **`version_matrix.json`** | 225 B | ❌ **NOT** listed | ✅ **TRACKED** | ✅ **Yes** — durability contract | Hand-authored | `comfy_backend.py:128` (via `REPO_ROOT`) |
| **`custom_nodes.lock`** | ~? | ❌ **NOT** listed | ✅ **TRACKED** | ✅ **Yes** — node-pin manifest | `vibecomfy nodes lock` CLI | `emitter.py:1312`, `emit_ready.py:444`, `wrapper_discovery.py:158`, `nodes.py:356,430,638` |

> Note: I was unable to run git commands directly to confirm sizes of untracked files, but the `.gitignore` patterns and code analysis are definitive.

---

### Q1: Which files are generated but intentionally committed?

**`template_index.json`** — The lone "generated-and-committed" file. Multiple docs confirm:

> *"the repo-owned `template_index.json` for ready templates"* — `CLAUDE.md:116`
> *"M1 hygiene corrected — `template_index.json` is repo-owned and stays tracked"* — `docs/structure_cleanup/README.md:101`
> *"`template_index.json` is a repo-owned artifact"* — `AGENTS.md:116`

It's regenerated by `tools/refresh_template_index.py` and CI-gated via `--check` flag. Although listed in `.gitignore` (line 11), it was tracked before the gitignore entry was added and remains intentionally committed.

---

### Q2: Which must stay at repo root because code/tests assume bare paths?

Six files use **bare `Path("filename")`** (no `REPO_ROOT` prefix), meaning they resolve relative to CWD and **must stay at repo root**:

| File | Bare-Path Consumers |
|---|---|
| **`template_index.json`** | `vibecomfy/commands/workflows.py:21` — `TEMPLATE_INDEX_PATH = Path("template_index.json")` |
| **`workflow_index.json`** | `vibecomfy/registry/library.py:34` — `index_paths = [Path("workflow_index.json"), ...]` |
| **`external_workflow_index.json`** | same line, plus `_workflow_path.py:9`, `bootstrap.py:10` |
| **`node_index.json`** | `vibecomfy/search/bootstrap.py:8`, `tests/test_nodes_index.py:39` |
| **`custom_nodes.lock`** | 5+ modules: `emitter.py:1312`, `emit_ready.py:444`, `wrapper_discovery.py:158`, `nodes.py:356,430,638` |

**`version_matrix.json`** uses `_REPO_ROOT / "version_matrix.json"` (absolute via `find_repo_root()`) so it is *more flexible* but still effectively root-bound.

---

### Q3: Is documentation adequate?

**Yes, for the core distinction.** Both `AGENTS.md:116` and `CLAUDE.md:128` clearly state:

> *"Indexes that back these: `workflow_index.json`, `node_index.json`, `external_workflow_index.json`, `custom_nodes.lock` (all generated by `sources sync`), plus the repo-owned `template_index.json` for ready templates."*

**Gaps:**
1. **No single "what these files are" table** exists in README or a discoverable doc. The project layout table in `README.md:223-230` omits all seven files entirely.
2. **`asset_manifest.json`** is undocumented — no writer, no lifecycle, no mention of its status (orphan). The audit doc says *"asset_manifest.json orphaned (no writer)"* but that's buried in `docs/structural_audit_2026-05.md:168`.
3. **`.gitignore` mismatch is undocumented** — no explanation why `template_index.json` is ignored *and* tracked (the "committed before gitignore" situation). New contributors will be confused.

---

### Q4: Safe `.gitignore` or README clarifications needed?

**Recommended `.gitignore` changes (safe):**
1. **Add `asset_manifest.json`** to `.gitignore` — it's an orphaned generated artifact with no writer. 11 KB of dead weight at root.
2. **No changes to `.gitignore` for the others** — `version_matrix.json` and `custom_nodes.lock` are intentionally tracked; the four `*_index.json` files are already ignored (the tracked `template_index.json` should stay tracked despite the gitignore pattern — it's a known pattern).

**Recommended README/docs clarifications (safe):**
1. **Add a "Generated Index Files" subsection** to the README layout table or a new section, documenting which files are committed intentionally vs. ignored.
2. **Document the `template_index.json` tracking paradox** — add a comment to `.gitignore:11`: `# template_index.json is intentionally tracked despite this pattern (repo-owned CI-gated artifact)`.
3. **Add a lifecycle note for `asset_manifest.json`** — either mark it as intentionally committed with a generator TODO, or add to `.gitignore` and `git rm --cached` it.

---

### Safe-First Recommendation

| Priority | Action | Rationale |
|---|---|---|
| **P0** | Add `asset_manifest.json` to `.gitignore` + `git rm --cached` | Orphaned artifact, no writer, 11 KB root clutter. Zero production code references. |
| **P1** | Add comment to `.gitignore:11`: `# template_index.json is intentionally TRACKED despite this pattern` | Prevents future cleanup from accidentally deleting the tracked artifact. |
| **P2** | Add a "Root Index & Manifest Files" table to `README.md` or `docs/structure.md` | Only `AGENTS.md` and `CLAUDE.md` mention them; README layout table omits all seven. |
| **P3** | Delete `node_index.json` from disk (it's empty `[]`), keep `.gitignore` | It's regenerated by `sources sync`; empty file serves no purpose. |
| **P3** | Move `version_matrix.json` doc link into README | It's a durability contract; new contributors won't know to look for it. |

**Do NOT:**
- Remove `template_index.json` from tracking (breaks CI gates and `workflows list --ready --json`)
- Move any bare-path-referenced file from root without updating 5+ source files
- Add `custom_nodes.lock` or `version_matrix.json` to `.gitignore` (both are intentionally tracked contracts)