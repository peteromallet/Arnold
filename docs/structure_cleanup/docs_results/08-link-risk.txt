Here's the audit for `/Users/peteromalley/Documents/reigh-workspace/vibecomfy`:

---

## 1. Low-Risk Moves (safe today)

- **`agentic/briefs/`** (20+ Markdown files) — Zero code references. Agent session logs. Already flagged as ephemeral by structure_cleanup. Move to `.agentic/briefs/` or `_archive/` with no breakage.
- **`agentic/actors_m4/`** and **`agentic/actors_m5/`** — Self-contained scripts that only `from vibecomfy import` (package import, not path-dependent). Safe to relocate to `scripts/` or `tests/agentic/` if you update their imports.
- **`docs/structure_cleanup/`** — Analysis artifacts only referenced by its own `_report.json`. Self-contained.
- **`.desloppify/`** — Hidden config, zero code references.
- **`ready_templates/README.md`** and **`ready_templates/VALIDATION.md`** — Standalone docs.

## 2. Medium/High-Risk Moves (require link update pass)

- **`docs/`** — **HIGHEST RISK.** ~40+ hardcoded string refs from tests (`Path("docs/corpus_parity_allowlist.md").read_text()`, `docs/cookbook/` assertions), production code (`vibecomfy/schema/validate.py` references `docs/node_pack_reconciliation.md` ~20 times; `edit.py` → `docs/agent_edit_concrete_tree.md`; `lowering.py` → `docs/plans/plan_v2.md`; `analyze.py`/`graph.py` → `docs/security/agent_data_boundary.md`), and **dense internal cross-links** between docs themselves. Moving any doc file breaks its incoming references.
- **`workflow_corpus/`** — **HIGH RISK.** Tests hardcode relative paths like `"workflow_corpus/official/image/z_image.json"` in `test_porting_emitter.py` (15+ refs), `test_ui_emitter_parity.py` uses glob `"workflow_corpus/**/*.json"`, and golden files in `tests/characterization/goldens/` bake in paths. Moving = broken test suites.
- **`ready_templates/`** — **MEDIUM-HIGH RISK.** Tests exec-compile templates via `"ready_templates/image/model_key_independent.py"`, manifest JSONs (`coverage.json`, `ready_regeneration.json`) reference by relative path. Changing the prefix means updating manifests + tests.
- **`vendor/ComfyUI`** — **MEDIUM RISK.** Hardcoded paths in `vibecomfy/comfy_backend.py`, `refuse.py`, `drift.py`, multiple test files. One ready template even embeds `source_path='vendor/ComfyUI/tests/...'`. Path logic is coupled with submodule initialization.
- **`agentic/`** (root) — **MEDIUM.** `agentic/actors.py` is imported by unknown CI consumers; `test_agentic_contract.py` asserts directory structure. Briefs subdir is fine to move alone.

## 3. Recommended Reference-Check Commands

```bash
# Hardcoded string references in production + test code
rg -n '"docs/' --glob '*.py' vibecomfy/ tests/ scripts/ tools/
rg -n 'workflow_corpus/' --glob '*.py' vibecomfy/ tests/ scripts/ tools/
rg -n 'ready_templates/' --glob '*.py' vibecomfy/ tests/ scripts/ tools/
rg -n 'vendor/ComfyUI' --glob '*.py' vibecomfy/ tests/ tools/

# Markdown internal cross-links
rg -n '\(docs/' --glob '*.md' --no-ignore
rg -n '\(ready_templates/' --glob '*.md' --no-ignore
rg -n '\(workflow_corpus/' --glob '*.md' --no-ignore

# Manifest JSON references
rg -n '"path"' workflow_corpus/manifests/ --glob '*.json'
```

## 4. Minimal Safe Batch Today

```bash
git mv agentic/briefs/ .agentic/briefs/   # or _archive/briefs/
git mv agentic/actors_m4/ scripts/legacy_actors_m4/
git mv agentic/actors_m5/ scripts/legacy_actors_m5/
# (update imports in actors_m5/ files: they use "from vibecomfy import load_workflow_any" — no path change needed)
```

Everything else — especially `docs/`, `workflow_corpus/`, and `ready_templates/` — requires running the `rg` commands above and scripting a link-update pass before any move.