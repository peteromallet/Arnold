Now I have the full picture. Here's the audit:

---

## Audit: `scripts/` and `tools/` importability & dependencies

### Load-bearing paths — must not move

These are imported by production code or tests, forming a coupled dependency graph.

**`tools/` (package heavily test-imported + 1 production importer):**

| Path | Imported from | Severity |
|---|---|---|
| `tools/__init__.py` | Package namespace (empty, but required) | **Must stay** |
| `tools/clone_and_extract_packs.py` | `vibecomfy/commands/schemas.py` (production!) + `test_schemas_ensure.py` | **Must stay** |
| `tools/refresh_template_index.py` | `backfill_custom_node_refs`, `check_strict_ready_templates`, `validate_template_traceability`, 3 test files | **Must stay** |
| `tools/format_as_python.py` | `convert_ready_templates.py` + `test_porting_emitter.py` | **Must stay** |
| `tools/convert_ready_templates.py` | `test_porting_emitter.py` (10+ distinct imports) | **Must stay** |
| `tools/check_pack_provenance.py` | `check_strict_ready_templates`, `validate_template_traceability`, `test_pack_provenance.py` | **Must stay** |
| `tools/backfill_custom_node_refs.py` | `check_pack_provenance`, `test_custom_node_ref_backfill.py` | **Must stay** |
| `tools/validate_templates_against_packs.py` | `check_pack_provenance`, `check_strict_ready_templates` | **Must stay** |
| `tools/check_strict_ready_templates.py` | `test_strict_ready_gate.py` | **Must stay** |
| `tools/validate_template_traceability.py` | `test_template_traceability.py` | **Must stay** |
| `tools/generate_node_shims.py` | `test_node_shims.py` | **Must stay** |
| `tools/profile_smoke_report.py` | `test_profile_smoke_report.py` | **Must stay** |
| `tools/check_canonical_parity.py` | `test_v26_canonical_parity.py` | **Must stay** |

**`scripts/` (package with cross-imports among scripts + test imports):**

| Path | Imported from | Severity |
|---|---|---|
| `scripts/__init__.py` | Package namespace — tests do `from scripts import runpod_runner` | **Must stay** |
| `scripts/runpod_runner.py` | `runpod_validate`, `runpod_corpus_matrix`, `runpod_model_matrix`, `test_runpod_runner.py` | **Must stay** |
| `scripts/runpod_artifacts.py` | `runpod_runner`, `runpod_e2e_matrix` (lazy) | **Must stay** |
| `scripts/runpod_matrix_plan.py` | `runpod_corpus_matrix`, `runpod_e2e_matrix`, `test_runpod_matrix.py` | **Must stay** |
| `scripts/runpod_matrix_remote.py` | `test_runpod_matrix.py` | **Must stay** |
| `scripts/runpod_corpus_matrix.py` | `test_models_registry.py` (imports `_remote_script`) | **Must stay** |
| `scripts/regenerate_snapshots.py` | `test_compile_api_snapshots.py`, `test_snapshot_api_workflows.py`, CI (`ci.yml`) | **Must stay** |

**No-op / dead** (no importers, no docs, no CI):
- `tools/_widget_schema.py`, `tools/_compile_equivalence.py`, `tools/_legacy/` (only `.pyc` remains)

---

### Direct-run-only scripts — can move if docs update

These are invoked as `python scripts/foo.py` or from shell scripts. No Python imports point at them. Moving requires updating `.md` docs, `CLAUDE.md`, `AGENTS.md`, and `README.md` references:

| Path | Called from |
|---|---|
| `scripts/runpod_validate.py` | `docs/runpod/setup.md`, `CLAUDE.md`, `AGENTS.md` |
| `scripts/runpod_model_matrix.py` | `docs/runpod/setup.md` |
| `scripts/runpod_e2e_matrix.py` | `.github/workflows/e2e_matrix.yml` (both dry-run and real) |
| `scripts/demo_wrapper_codegen.py` | Multiple `docs/` handoff files |
| `scripts/roundtrip_fidelity_spike.py` | `docs/roadmap_agentic_comfyui.md` |
| `scripts/agentic_success_rate.py` | No docs — cannot find callers |
| `scripts/warm_session_smoke.py` | No docs — cannot find callers |
| `scripts/generate_agent_contract_js.py` | No docs — only `test_agent_contract_codegen.py` references its *output* |
| `scripts/sync_agent_skill.py` | No docs |
| `scripts/populate_model_hashes.py` | No docs |
| `scripts/_catalog_size_probe.py` | No docs (one-off probe) |
| `scripts/_agent_edit_prompt_dump.py` | No docs |
| `scripts/vibecomfy_debug.py` | No docs |
| `scripts/maintenance/_regen_templates.py` | Historical (hardcoded absolute path) |
| `scripts/maintenance/_fix_t6.py` | Historical one-off |

**Shell scripts** (no Python imports):
| `scripts/run_local_agent_comfy.sh` | Self-contained |
| `scripts/patch_shannon_unattended_root.sh` | Called by two `megaplan_*.sh` scripts |
| `scripts/megaplan_cloud_operator_loop.sh` | Self-contained |
| `scripts/megaplan_cloud_recovery_loop.sh` | Self-contained |

**Direct-run `tools/` with zero importers:**
| `tools/refresh_comfy_metadata.py` | No callers found |
| `tools/fetch_hf_metadata.py` | No callers found |
| `tools/class_inventory_audit.py` | No callers found |

---

### Verification commands for cleanup

```bash
# 1. Confirm no hidden imports (should return nothing for clean removals)
grep -rn "from tools\." vibecomfy/ --include='*.py' | grep -v __pycache__
grep -rn "from scripts\." tests/ --include='*.py'

# 2. After moving a direct-run script, verify no stale Python imports
python -c "import scripts.runpod_validate" 2>&1  # should fail if removed

# 3. Ci integration check
grep -n 'scripts/' .github/workflows/*.yml

# 4. Doc reference sweep (after path changes)
grep -rn 'scripts/' docs/ --include='*.md' | grep -v 'materialize_ready_templates.py'  # doc refs to update

# 5. Ensure every tools/ subpackage still resolves
python -c "
import tools.backfill_custom_node_refs
import tools.check_pack_provenance
import tools.check_strict_ready_templates
import tools.clone_and_extract_packs
import tools.convert_ready_templates
import tools.format_as_python
import tools.generate_node_shims
import tools.profile_smoke_report
import tools.refresh_template_index
import tools.validate_template_traceability
import tools.validate_templates_against_packs
import tools.check_canonical_parity
import tools.refresh_comfy_metadata
print('All tools load OK')
"

# 6. Ensure every scripts/ subpackage still resolves
python -c "
from scripts import runpod_runner
from scripts import runpod_matrix_plan
from scripts import runpod_artifacts
from scripts import runpod_matrix_remote
from scripts import runpod_corpus_matrix
from scripts import regenerate_snapshots
print('All scripts load OK')
"
```

---

**Key insight:** `tools/` is the more load-bearing namespace — 12 modules imported from tests and 1 from production code (`vibecomfy/commands/schemas.py`). `scripts/` has a tighter dependency graph but `runpod_runner` and `regenerate_snapshots` are the two load-bearing hubs. The `maintenance/` subdir and `_legacy/` subdir are purely historical and can be archived.