Here's the audit:

---

### `scripts/maintenance/_fix_t6.py` — **Historical**
Hardcoded source-manipulation script that surgically removes five functions from `agent_edit.py` and adds re-exports. One-time T6 refactor artifact. The leading underscore signals historical intent. No arguments, no reusability.

### `scripts/maintenance/_regen_templates.py` — **Historical**
Migrates v2.3.1 templates → v2.3.2 format (inline `PRIVATE_KNOBS`, remove `OUTPUT_PREFIX`/`READY_REQUIREMENTS`). Hardcoded to an absolute worktree path (`/Users/.../vibecomfy-v232`). One-off version migration. The `_` prefix marks it correctly.

### `scripts/_catalog_size_probe.py` — **Historical / Candidate for `tools/`**
Investigative probe measuring prompt-message and signature-catalog character counts (with/without `focus_types`). Self-contained, imports from the main package. Could be a reusable `tools/catalog_size_probe.py` if kept alive for ongoing capacity tuning, but currently reads like a one-off measurement.

### `scripts/populate_model_hashes.py` — **Active → Candidate for `tools/`**
Properly engineered utility: argparse, `--write`/`--refresh` flags, HF API cache. Idempotently backfills `sha256`/`size_bytes` in the model registry. Repeatedly useful as models are added. Belongs in `tools/populate_model_hashes.py` to signal it's a maintained dev utility, not a one-off script.

### `scripts/roundtrip_fidelity_spike.py` — **Historical** (spike)
Self-documented as "exploratory/spike code, not production" — the seed for oracle-gated property tests (roadmap §11.3). Runs T1–T4 measurements against a live ComfyUI. Valuable research artifact but not active tooling. Should stay put or move to `docs/` as a reference.

### `scripts/warm_session_smoke.py` — **Active**
Runs two workflow references through one `EmbeddedSession` for manual GPU smoke testing. argparse, real imports. Used for warm-session/cold-start timing. Fits `scripts/` correctly — it's interactive/human-in-the-loop, not a programmatic tool.

### `scripts/regenerate_snapshots.py` — **Active → Candidate for `tools/`**
Well-engineered CI/developer utility: `--check` (CI drift detection), `--write` (default), `--filter`, atomic file writes, shared source of truth with `vibecomfy.testing.snapshot`. Used frequently during development. Strong candidate for `tools/regenerate_snapshots.py`.

---

**Summary table:**

| Script | Status | Recommend |
|---|---|---|
| `maintenance/_fix_t6.py` | Historical | Keep in `maintenance/` |
| `maintenance/_regen_templates.py` | Historical | Keep in `maintenance/` |
| `_catalog_size_probe.py` | Historical | Keep or → `tools/` if reused |
| `populate_model_hashes.py` | Active | → `tools/` |
| `roundtrip_fidelity_spike.py` | Historical / spike | Keep in `scripts/` |
| `warm_session_smoke.py` | Active | Keep in `scripts/` |
| `regenerate_snapshots.py` | Active | → `tools/` |

**Move candidates to `tools/`:** `populate_model_hashes.py`, `regenerate_snapshots.py`. Optionally `_catalog_size_probe.py` if it gains ongoing use.