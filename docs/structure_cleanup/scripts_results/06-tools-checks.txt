Here's my audit of the 8 tools:

---

## Pre-commit Gates (remain as-is, are documented)

Three tools are wired into `.pre-commit-config.yaml` as formal CI gates:

1. **`check_strict_ready_templates.py`** — `id: vibecomfy-strict-ready-templates`  
2. **`validate_templates_against_packs.py`** — `id: vibecomfy-templates-against-packs`  
3. **`validate_template_traceability.py`** — `id: vibecomfy-template-traceability` (invoked with `--strict`)

These are the correct home. No move needed. They're referenced in `AGENTS.md` as programmatic gates. ✅ **Remain as-is.**

---

## Utility Modules Imported by Other Code (remain as-is)

4. **`check_pack_provenance.py`** — imported by both `validate_template_traceability.py` (line 22) and `check_strict_ready_templates.py` (line 749). Provides `build_pack_provenance_report` and `diagnostics_for_template`. Stays.

5. **`backfill_custom_node_refs.py`** — imported by `check_pack_provenance.py` (line 11). Provides `BackfillTarget`, `_extract_requirements`, `_select_targets`. Part of the utility chain.

6. **`clone_and_extract_packs.py`** — imported by production code (`vibecomfy/commands/schemas.py` line 374) and tested in `tests/test_schemas_ensure.py`. Provides `SafeEval`, `static_env`, `dotted_name`. This is an active dependency, not just a tool script.

All three are cross-imported across `tools/` — an existing pattern in this codebase. ✅ **Remain as-is.**

---

## Needs Documentation or Move

7. **`class_inventory_audit.py`** — standalone script that writes to `vibecomfy/porting/cache/class_inventory.json`. Not imported by anything. Not in pre-commit. Not referenced in `AGENTS.md`. No `__main__` shebang invocation anywhere. It's a developer-run diagnostic. **Recommendation:** Either move it to a `scripts/` directory (if one exists) or add a brief docstring/README mention explaining when to run it (e.g., "Run after adding a new custom-node pack to regenerate class_inventory.json"). Currently **under-documented**.

8. **`check_canonical_parity.py`** — has a dedicated test (`tests/test_v26_canonical_parity.py`), is referenced in planning docs, but is **not** in pre-commit hooks. Referenced in `docs/templates/decorator_template_emitter_completion.md` as "feature-branch only." **Recommendation:** Either wire it into pre-commit (as its docs suggest) or add a `# TODO`/README note indicating it's an optional offline parity check, not a mandatory gate. Currently **ambiguous status**.

---

## Summary

| Tool | Verdict |
|---|---|
| `check_strict_ready_templates.py` | ✅ As-is (CI gate) |
| `validate_templates_against_packs.py` | ✅ As-is (CI gate) |
| `validate_template_traceability.py` | ✅ As-is (CI gate) |
| `check_pack_provenance.py` | ✅ As-is (imported utility) |
| `backfill_custom_node_refs.py` | ✅ As-is (imported utility) |
| `clone_and_extract_packs.py` | ✅ As-is (production dependency) |
| `class_inventory_audit.py` | ⚠️ Document usage or move to `scripts/` |
| `check_canonical_parity.py` | ⚠️ Document status (optional gate, feature-branch only) |