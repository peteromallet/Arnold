Thank you. I now have a complete picture of the codebase. Here is the synthesized batch.

---

## Safe First Cleanup Batch — `scripts/` & `tools/`

### Execute (ordered)

**1. Remove `.DS_Store` files**

8 files across the repo:

```bash
find scripts tools tests vendor vibecomfy ready_templates workflow_corpus \
  -name '.DS_Store' -print -delete
```

**2. Remove `__pycache__` directories**

```bash
rm -rf scripts/__pycache__ tools/__pycache__ tools/_legacy/__pycache__
```

**3. Remove orphaned `tools/_legacy/` directory**

Only contains `__pycache__/narrate_template.cpython-311.pyc` — the source `.py` was already deleted. Zero references to it remain anywhere in the codebase.

```bash
rm -rf tools/_legacy
```

### Deferred (keep in place)

| File | Rationale |
|---|---|
| `scripts/demo_wrapper_codegen.py` | Referenced in multiple docs/plans; tagged as potentially dead but needs human sign-off to delete |
| `scripts/populate_model_hashes.py` | Active utility; cleanup docs suggest move to `tools/` — not a deletion, defer move |
| `scripts/warm_session_smoke.py` | Active smoke-test harness; no test imports but tagged as actively used |
| `scripts/vibecomfy_debug.py` | Referenced by `docs/plans/revised_plan.md` |
| All `runpod_*` scripts | Each is referenced by tests, CI, or sibling imports (interdependent) |
| All `tools/*.py` | Each has tests imports, inter-tool imports, or doc references |
| `scripts/maintenance/_regen_templates.py` | Mentioned in safety-gate artifacts; defer |
| `scripts/maintenance/_fix_t6.py` | Same — defer |
| `scripts/_catalog_size_probe.py` | No external refs but under `_` prefix (private), minimal bytes — defer |
| `scripts/_agent_edit_prompt_dump.py` | Same — private/debug script, defer |

### Verification

```bash
# 1. Confirm nothing tracked was deleted
git diff --stat

# 2. Dry-run check that .DS_Store/__pycache__ removal is clean
git status --short | grep -E '\.DS_Store|__pycache__|_legacy'

# 3. Confirm tests still pass
uv run pytest tests/ -x -q --no-header 2>&1 | tail -5

# 4. Confirm CI-relevant scripts still executable
python -c "from scripts import runpod_artifacts, runpod_matrix_plan; print('scripts pkg ok')"
python -c "from tools import refresh_template_index, check_pack_provenance; print('tools pkg ok')"
```

### Summary

- **Deleted**: 8× `.DS_Store` + 2× `__pycache__/` dirs + `tools/_legacy/` dir → ~100 untracked cache/pyc files gone
- **Moved**: 0 tracked files
- **Deferred**: 15+ tracked scripts/tools with test/CI/doc cross-references
- **Risk**: Very low — only untracked caches and an already-orphaned legacy directory are touched