Working directory: /Users/peteromalley/Documents/reigh-workspace/vibecomfy

Audit `agents/openai.yaml` and the root `agents/` directory. Determine whether
this root directory earns its place, should be documented, or should move under
another config surface.

Read references in docs/tests/code. Return:

1. What consumes or documents `agents/openai.yaml`.
2. Whether `agents/` conflicts conceptually with `agentic/` or `.agents/`.
3. Safe cleanup/documentation recommendation.
4. Moves to avoid unless consumers are known.

Keep the answer under 400 words.
