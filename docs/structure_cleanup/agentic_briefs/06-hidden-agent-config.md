Working directory: /Users/peteromalley/Documents/reigh-workspace/vibecomfy

Audit hidden agent config: `.claude/`, `.agents/`, especially tracked
`docs/agent-skill/SKILL.md` versus ignored local files like
`.claude/settings.local.json`, `.claude/scheduled_tasks.json`, and `.agents/`.

Return:

1. Which hidden files are tracked repository surface.
2. Which are ignored local state.
3. Whether `docs/agent-skill/SKILL.md` should stay where it is.
4. Any safe `.gitignore` or README improvements.

Keep the answer under 500 words.
