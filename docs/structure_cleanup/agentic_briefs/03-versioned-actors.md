Working directory: /Users/peteromalley/Documents/reigh-workspace/vibecomfy

Audit `agentic/actors_m4/` and `agentic/actors_m5/`. Decide whether these
versioned actor directories are coherent as-is, or whether they should be
nested/renamed under a broader actor namespace.

Check imports and tests before recommending moves. Return:

1. What the m4/m5 split means from code evidence.
2. Whether the directory names are stable API.
3. Any low-risk cleanup inside those directories.
4. Any rename/move that should be deferred.

Keep the answer under 450 words and cite paths.
