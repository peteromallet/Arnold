Now I have a complete picture. Here is the full audit report.

---

# Comfy Nodes Audit 09: Tests and Fixtures

## Package Layout Under Audit

```
vibecomfy/comfy_nodes/         ← entry point (NODE_CLASS_MAPPINGS, WEB_DIRECTORY)
  __init__.py                   ← VibeComfyCodeIntent, VibeComfyStripConditioningKeys, etc.
  exec_node.py, exec_examples.py, session_io.py
  agent/                        ← internal agent runtime
    __init__.py, edit.py, session.py, contracts.py, gates.py, runtime_code.py
    routes.py, diagnostics.py, audit.py, provider.py, runtime.py, worker.py
    fixture_provider.py
  stages/                       ← batch_repl.py, agent.py, emit.py, etc.
  web/                          ← JS frontend files
```

**7 test files in scope**, plus 3 adjacent agent test files:

| File | Lines | Weight |
|---|---|---|
| `test_comfy_nodes.py` | 373 | Node-class behavior + runtime_code unit test |
| `test_comfy_nodes_entrypoint.py` | 51 | Module contract (entry point, NODE_CLASS_MAPPINGS) |
| `test_comfy_nodes_browser.py` | 285 | Cross-platform hash parity (JS ↔ Python) |
| `test_comfy_nodes_live_smoke.py` | 339 | Playwright live integration (opt-in) |
| `test_comfy_nodes_agent_backend_spine.py` | 7,639 | Session protocol, idempotency, accept/reject/rebaseline |
| `test_comfy_nodes_agent_edit.py` | 8,076 | Agent edit pipeline, routes, batch REPL, provider calls |
| `test_comfy_nodes_agent_contracts.py` | 1,171 | FailureKind enum, TurnContext, gates, failure envelopes |

---

## 1. Tests That Define Real Public Behavior

These test contracts that should be preserved across refactors. They validate behavior, not file layout.

### `test_comfy_nodes.py` — All 14 tests

| Test | What It Guards |
|---|---|
| `test_strip_conditioning_keys_removes_metadata_...` | `VibeComfyStripConditioningKeys.strip()` mutates metadata without touching embeddings |
| `test_code_intent_flag_off_input_types_unchanged` | Flag-OFF `INPUT_TYPES` shape |
| `test_code_intent_flag_off_return_types_unchanged` | Flag-OFF `RETURN_TYPES` / `RETURN_NAMES` |
| `test_code_intent_flag_on_input_types_has_16_optional_pool` | Flag-ON `INPUT_TYPES` has `in_0..in_15` + hidden fields |
| `test_code_intent_execute_flag_off_calls_execute_runtime_code` | Flag-OFF `execute()` delegates to `execute_runtime_code` |
| `test_code_intent_execute_dynamic_remaps_inputs_...` | Flag-ON remaps `in_i` → user names, returns 16-tuple |
| `test_code_intent_execute_dynamic_empty_io_returns_16_nones` | Empty outputs → all-None 16-tuple |
| `test_code_intent_execute_dynamic_missing_prompt_...` | Defensive `.get()` chain — None prompt doesn't crash |
| `test_execute_runtime_code_dynamic_empty_outputs...` | Empty `io.outputs` → `{"value": result}` sentinel |
| `test_execute_runtime_code_dynamic_single_output` | 1 output → `{out_name: result}` |
| `test_execute_runtime_code_dynamic_multiple_outputs...` | N>1 outputs with scalar → `runtime_output_shape_mismatch` |
| `test_execute_runtime_code_dynamic_multiple_outputs...` | N>1 outputs with dict → key-mapped |
| `test_execute_runtime_code_dynamic_asymmetric_inputs_only` | Inputs-only → sentinel wrap |
| `test_execute_runtime_code_dynamic_always_returns_dict` | Always returns dict regardless of worker output |

**Risk**: The monkeypatch targets (`vibecomfy.comfy_nodes.agent.runtime_code.execute_runtime_code`) are string paths that break if the `agent/` subpackage is restructured. The *behavior* is real; the fixture plumbing is layout-sensitive.

### `test_comfy_nodes_entrypoint.py` — All 4 tests

| Test | What It Guards |
|---|---|
| `test_entry_point_resolves_vibecomfy_in_comfyui_group` | `pyproject.toml` entry point registration |
| `test_comfy_nodes_exposes_web_directory` | `WEB_DIRECTORY = "./web"` contract |
| `test_comfy_nodes_exposes_node_class_mappings` | `NODE_CLASS_MAPPINGS` is non-empty dict |
| `test_comfy_nodes_ping_handler_defined_when_server_absent` | Module loads without ComfyUI server |

### `test_comfy_nodes_browser.py` — 3 tests (1 known-baseline)

| Test | What It Guards |
|---|---|
| `test_browser_harness_smoke` | Node.js test runner smoke (fails without Node; in known_failures.txt) |
| `test_browser_canonical_hash_matches_python_payload_hash` | **Cross-platform hash contract** — JS `payload_hash` must match Python's |
| `test_browser_structural_hash_matches_python_structural_graph_hash` | **Cross-platform hash contract** — JS structural projection hash must match Python's |

### `test_comfy_nodes_live_smoke.py` — 1 opt-in test

| Test | What It Guards |
|---|---|
| `test_live_comfyui_agent_panel_smoke` | Full Playwright integration: open panel, submit, accept, verify loadGraphData |

### `test_comfy_nodes_agent_contracts.py` — All tests

These tests define the **contract shape**: `FailureKind` enum values, `TurnContext` gate defaults, `failure_envelope.to_dict()` required keys, `SCAN_CODE_FAILURE_KIND` mapping, `public_outcome_from_turn_outcome` mapping, `FAILURE_SPECS` coverage. They are the source of truth for the agent edit contract.

### `test_comfy_nodes_agent_edit.py` — Behavior tests

| Test | What It Guards |
|---|---|
| `test_split_terminal_clarify_detects_terminal_line_...` | `clarify()` terminal protocol parsing |
| `test_split_terminal_clarify_rejects_strings_comments_...` | Non-terminal calls rejected |
| `test_ws_send_prefers_send_sync_and_targets_sid` | WebSocket dispatch priority |
| `test_ws_send_falls_back_to_send_json_and_swallows_errors` | Graceful fallback on send_sync absence |
| `test_turn_event_name_matches_between_backend_emit_and_frontend_...` | **Backend-frontend event name parity** — regex-scans `edit.py` and `.js` files |
| `test_agent_edit_turn_event_payload_compacts_...` | Sensitive fields excluded from turn event payload |
| `test_agent_edit_route_extracts_only_non_empty_string_client_id` | Client ID filtering in routes |
| `test_agent_edit_state_exposes_explicit_lowering_fields` | State shape invariant |
| `test_agent_edit_contract_defaults_to_batch_repl_...` | Contract selection logic with env var warnings |
| `test_run_batch_repl_product_path_...` | Batch REPL pipeline stages (`ingest` → `agent_batch`) |
| `test_handle_agent_edit_preserves_stage_blocked_...` | `_StageBlocked` propagates correctly |
| `test_handle_agent_edit_batch_repl_uses_product_builder_...` | Batch REPL uses correct response builder |
| `test_handle_agent_edit_dev_delta_uses_dev_builder_...` | Dev delta uses correct builder |
| `test_handle_agent_edit_round_trips_deepseek_python` | Full deepseek round-trip |

### `test_comfy_nodes_agent_backend_spine.py` — Session protocol tests

All tests in this file define **session protocol behavior**: turn allocation under lock, idempotency replay, accept/reject atomicity, baseline advancement, hash recording, rebaseline, scoped accept verification. These are the protocol-level contracts.

### `test_agent_edit_safety.py` — 1 test

| Test | What It Guards |
|---|---|
| `test_agentic_edit_preserve_and_determinism` | **Five safety invariants**: position preservation, no-overlap, removed_named, auto-minted UIDs, determinism |

### `test_agent_contract_codegen.py` — 1 test

| Test | What It Guards |
|---|---|
| `test_generated_js_matches_codegen` | Codegen drift guard — committed `.js` matches generator output |

### `test_agent_acceptance.py` — 6 tests

All acceptance tests for CLI commands `workflows list`, `contract inspect/doctor`, `port convert`, `set_input` + `compile`, `run_metadata`.

---

## 2. Tests That Preserve Accidental File Layout

These tests use import paths or monkeypatch paths that encode the current module tree. If internal modules are renamed, split, or relocated, the paths break — even if the tested behavior is preserved.

### Hot imports (hardcoded module paths that would break on restructure)

| File | Path Dependency | Risk Level |
|---|---|---|
| `test_comfy_nodes.py` lines 109-111 | `monkeypatch.setattr("vibecomfy.comfy_nodes.agent.runtime_code.execute_runtime_code", ...)` | HIGH — string path to a sub-sub-package |
| `test_comfy_nodes.py` lines 170-172 | `monkeypatch.setattr("vibecomfy.comfy_nodes.agent.runtime_code.execute_runtime_code_dynamic", ...)` | HIGH |
| `test_comfy_nodes_agent_edit.py` lines 15-68 | `from vibecomfy.comfy_nodes.agent.edit import _StageBlocked, _agent_edit_contract, _ws_send, _run_batch_repl_product_path, ...` (12 private `_`-prefixed symbols) | HIGH — binds to module-internal private names |
| `test_comfy_nodes_agent_edit.py` line 547 | `routes = importlib.import_module("vibecomfy.comfy_nodes.agent.routes")` | MEDIUM |
| `test_comfy_nodes_agent_backend_spine.py` lines 11-91 | `from vibecomfy.comfy_nodes.agent.audit import ...` 10 imports from 6 different sub-modules | HIGH — imports from `session`, `edit`, `contracts`, `gates`, `diagnostics`, `audit` |
| `test_comfy_nodes_agent_backend_spine.py` lines 24-29 | `from vibecomfy.comfy_nodes.agent.contracts import APPLY_ELIGIBILITY_REASONS, FailureKind, ...` | MEDIUM |
| `test_comfy_nodes_agent_backend_spine.py` lines 36-48 | `from vibecomfy.comfy_nodes.agent.edit import AgentEditState, ...` | MEDIUM |
| `test_comfy_nodes_agent_backend_spine.py` lines 42-48 | `from vibecomfy.comfy_nodes.agent.gates import EXPLICIT_QUEUE_BLOCKER_CODES, ...` | MEDIUM |
| `test_comfy_nodes_agent_backend_spine.py` lines 49-76 | `from vibecomfy.comfy_nodes.agent.session import _SENTINEL_LINK_ABSENT, _build_graph_index, ...` (17 private names) | HIGH |

### Private-name imports that have no public re-export

These tests import names that are not re-exported by any `__init__.py` or public surface. They are testing the internal module shape directly:

| Private Name | Test File | Module |
|---|---|---|
| `_SENTINEL_LINK_ABSENT` | backend_spine | `session.py` |
| `_SENTINEL_NO_VALUE` | backend_spine | `session.py` |
| `_SENTINEL_NODE_ABSENT` | backend_spine | `session.py` |
| `_build_graph_index` | backend_spine | `session.py` |
| `_build_scoped_validation_plan` | backend_spine | `session.py` |
| `_build_v2_accept_evidence` | backend_spine | `session.py` |
| `_canonical_node_uid` | backend_spine | `session.py` |
| `_find_node_in_graph` | backend_spine | `session.py` |
| `_load_turn_delta_ops` | backend_spine | `session.py` |
| `_load_turn_request_graph` | backend_spine | `session.py` |
| `_normalize_link_endpoint` | backend_spine | `session.py` |
| `_normalize_target_uid` | backend_spine | `session.py` |
| `_read_field_value_from_node` | backend_spine | `session.py` |
| `_read_link_source_endpoint` | backend_spine | `session.py` |
| `_resolve_submit_value_for_op` | backend_spine | `session.py` |
| `_split_field_path` | backend_spine | `session.py` |
| `_failure_response` | backend_spine | `edit.py` |
| `_stale_rebaseline_recovery_issue` | backend_spine | `edit.py` |
| `_StageBlocked` | agent_edit | `edit.py` |
| `_agent_edit_contract` | agent_edit | `edit.py` |
| `_ws_send` | agent_edit | `edit.py` |
| `_run_batch_repl_product_path` | agent_edit | `edit.py` |
| `_batch_warning_sentence` | agent_edit | `edit.py` |
| `_human_change_phrase` | agent_edit | `edit.py` |
| ...and more | agent_edit | `edit.py` |

---

## 3. Tests That Should Be Updated After Internal Path Moves

If the `vibecomfy/comfy_nodes/agent/` package is restructured (e.g., modules split into finer granularity or moved to a different parent), update these imports:

### Update monkeypatch string paths in `test_comfy_nodes.py`

```
Line 109:  "vibecomfy.comfy_nodes.agent.runtime_code.execute_runtime_code"
Line 170:  "vibecomfy.comfy_nodes.agent.runtime_code.execute_runtime_code_dynamic"
Line 222:  "vibecomfy.comfy_nodes.agent.runtime_code.execute_runtime_code_dynamic"
Line 201:  "vibecomfy.comfy_nodes.agent.runtime_code.execute_runtime_code_dynamic"
```

### Update import paths in `test_comfy_nodes_agent_edit.py`

All `from vibecomfy.comfy_nodes.agent.edit import ...` (lines 15-36), plus:
- `from vibecomfy.comfy_nodes.agent.contracts import ...` (lines 38-49)
- `from vibecomfy.comfy_nodes.agent.session import ...` (lines 51-56)
- `from vibecomfy.comfy_nodes.agent.provider import ProviderError` (line 50)
- `routes = importlib.import_module("vibecomfy.comfy_nodes.agent.routes")` (line 547)

### Update import paths in `test_comfy_nodes_agent_backend_spine.py`

All imports from lines 11-88, covering:
- `vibecomfy.comfy_nodes.agent.audit`
- `vibecomfy.comfy_nodes.agent.provider`
- `vibecomfy.comfy_nodes.agent.runtime`
- `vibecomfy.comfy_nodes.agent.contracts`
- `vibecomfy.comfy_nodes.agent.diagnostics`
- `vibecomfy.comfy_nodes.agent.edit`
- `vibecomfy.comfy_nodes.agent.gates`
- `vibecomfy.comfy_nodes.agent.session`

### Update import paths in `test_comfy_nodes_agent_contracts.py`

All `from vibecomfy.comfy_nodes.agent.contracts import ...` (lines 7-26).

### Other cross-module references to update

- `test_comfy_nodes_agent_edit.py` line 408: `(repo_root / "vibecomfy" / "comfy_nodes" / "agent" / "edit.py")` — file path reference in `test_turn_event_name_matches_between_backend_emit_and_frontend_listener`
- `test_comfy_nodes_agent_edit.py` line 412: `(repo_root / "vibecomfy" / "comfy_nodes" / "web" / "vibecomfy_roundtrip.js")` — same test
- `test_agent_contract_codegen.py` line 21-27: hardcoded paths to generator script and JS output
- `test_comfy_nodes_browser.py` line 21: hardcoded `node --test tests/browser/roundtrip_smoke.test.mjs`

---

## 4. Known Risks & Baseline Failures

### Pre-existing baseline failures (in `tests/known_failures.txt`)

These are by-design-red. They will not cause CI to exit non-zero unless **new** failures appear.

| Test ID | Reason |
|---|---|
| `tests/test_comfy_nodes_browser.py::test_browser_harness_smoke` | Requires Node.js (not installed in CI) |
| `tests/test_comfy_nodes_agent_backend_spine.py::test_run_agent_turn_batch_empty_content_is_malformed` | By-design (scratchpad-emitter epic) |
| `tests/test_comfy_nodes_agent_edit.py::test_agent_edit_submit_after_accept_still_blocks_real_structural_divergence` | By-design (in-flight agent-edit-chat run) |
| `tests/test_comfy_nodes_agent_edit.py::test_classify_import_failure_is_runtime_unavailable_not_retryable[agent_batch-exc0]` | `FailureKind.AGENT_RUNTIME_UNAVAILABLE` not yet on enum |
| `tests/test_comfy_nodes_agent_edit.py::test_classify_import_failure_is_runtime_unavailable_not_retryable[agent_response-exc1]` | Same |
| `tests/test_comfy_nodes_agent_edit.py::test_classify_import_failure_is_runtime_unavailable_not_retryable[agent_delta-exc2]` | Same |
| `tests/test_comfy_nodes_agent_edit.py::test_classify_provider_error_wrapping_import_message_is_runtime_unavailable` | Same |

### Environment-dependent risks

| Test | Dependency | Failure Mode |
|---|---|---|
| `test_browser_harness_smoke` | Node.js | Skips with `pytest.skip` |
| `test_browser_canonical_hash_matches_python...` | Node.js | Skips with `pytest.skip` |
| `test_browser_structural_hash_matches_python...` | Node.js, harness.mjs, system_stats stub | Skips with `pytest.skip` |
| `test_live_comfyui_agent_panel_smoke` | Playwright, `VIBECOMFY_COMFY_SMOKE=1`, running ComfyUI | Skipped by default (`pytest.mark.comfy`) |

### Fragile string-matching risk

`test_turn_event_name_matches_between_backend_emit_and_frontend_listener` (agent_edit.py line 402) does a regex-search of source files. It breaks if:
- The `_ws_send(...)` call in `edit.py` changes its string quoting style
- The `addEventListener(...)` call in the JS file changes whitespace
- The file path to either source file changes

---

## 5. Verification Command Sequence

Run these in order. Replace `pytest` with your preferred test runner.

```bash
# Navigate to project root
cd /Users/peteromalley/Documents/reigh-workspace/vibecomfy

# 1. Entry point + node class tests (fast, no deps)
pytest -x -v tests/test_comfy_nodes.py tests/test_comfy_nodes_entrypoint.py

# 2. Contract layer (fast, pure Python)
pytest -x -v tests/test_comfy_nodes_agent_contracts.py

# 3. Session protocol + idempotency (no external deps, but slow ~30-60s)
pytest -x -v tests/test_comfy_nodes_agent_backend_spine.py \
  -k "not test_run_agent_turn_batch_empty_content_is_malformed"

# 4. Agent edit pipeline (no external deps, slow)
pytest -x -v tests/test_comfy_nodes_agent_edit.py \
  -k "not (test_agent_edit_submit_after_accept or test_classify_import_failure or test_classify_provider_error)"

# 5. Codegen drift guard
pytest -x -v tests/test_agent_contract_codegen.py

# 6. Safety invariants (no external deps)
pytest -x -v tests/test_agent_edit_safety.py

# 7. Acceptance tests (no external deps except ready_template fixtures)
pytest -x -v tests/test_agent_acceptance.py \
  -k "not test_acceptance_set_public_input_and_compile_z_image_to_api"

# 8. Browser hash parity tests (requires Node.js)
node --version && pytest -x -v tests/test_comfy_nodes_browser.py \
  -k "not test_browser_harness_smoke"

# 9. ─────────────────────────────────────────────────────────────────
# Full self-check (excluding known-failures baseline + opt-in markers)
pytest -x -v \
  tests/test_comfy_nodes.py \
  tests/test_comfy_nodes_entrypoint.py \
  tests/test_comfy_nodes_agent_contracts.py \
  tests/test_comfy_nodes_agent_backend_spine.py \
  tests/test_comfy_nodes_agent_edit.py \
  tests/test_agent_contract_codegen.py \
  tests/test_agent_edit_safety.py \
  tests/test_agent_acceptance.py \
  --ignore-glob='*test_agent_edit_safety*test_classify*' \
  --ignore-glob='*test_agent_edit_safety*test_agent_edit_submit*' \
  --ignore-glob='*test_agent_edit_safety*test_provider_error*' \
  --ignore-glob='*test_run_agent_turn_batch_empty*' \
  --ignore-glob='*test_browser_harness*' \
  --ignore-glob='*test_acceptance_set_public*' \
  2>&1 | tail -20

# 10. known_failures.txt audit (reports stale entries)
pytest --collect-only --known-failures-audit -q 2>&1 | tail -10
```

---

## Summary

| Category | Count | Files |
|---|---|---|
| **Real public behavior tests** | ~50+ | All 7 `test_comfy_nodes*` files + `test_agent_edit_safety` + `test_agent_contract_codegen` + `test_agent_acceptance` |
| **Tests preserving accidental file layout** | ~30 private-import sites | `test_comfy_nodes_agent_backend_spine.py` (17 private names), `test_comfy_nodes_agent_edit.py` (12+ private names), monkeypatch paths in `test_comfy_nodes.py` |
| **Would break on path move** | All agent imports in 4 test files | If `vibecomfy/comfy_nodes/agent/` is restructured |
| **By-design-red baseline** | 7 tests | Documented in `known_failures.txt` |
| **Environment-gated** | 5 tests | Node.js, Playwright, live ComfyUI |

**Recommendation**: If you plan to restructure `vibecomfy/comfy_nodes/agent/`, the safest cleanup batch is:

1. **Extract a public API module** (`agent/public.py` or re-export from `agent/__init__.py`) so tests can import without reaching into private sub-modules
2. **Replace monkeypatch string paths** with dependency injection points
3. **Keep all behavior tests** — none are redundant; the issue is only *how* they reach the code under test