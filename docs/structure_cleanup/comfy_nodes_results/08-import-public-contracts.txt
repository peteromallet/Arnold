Now I have the complete picture. Here is the full audit.

---

## Import Graph Summary

### Package Structure

```
vibecomfy/comfy_nodes/
├── __init__.py              ← Public: ComfyUI entry point
├── _time_utils.py           ← DEAD
├── exec_examples.py         ← Internal (imported only by exec_node.py)
├── exec_node.py             ← Public: exported via __init__.py
├── session_io.py            ← DEAD (zero importers)
├── agent/                   ← Core subpackage
│   ├── __init__.py          ← Re-exports all agent.* submodules
│   ├── audit.py
│   ├── contracts.py
│   ├── diagnostics.py
│   ├── edit.py
│   ├── fixture_provider.py  ← env-var runtime backend
│   ├── gates.py
│   ├── provider.py
│   ├── routes.py
│   ├── runtime.py           ← env-var runtime backend
│   ├── runtime_code.py
│   ├── session.py
│   └── worker.py            ← subprocess entry point
├── stages/                  ← ALL DEAD/BROKEN (~2,700 LOC)
│   ├── __init__.py
│   ├── agent.py
│   ├── audit.py
│   ├── batch_repl.py
│   ├── emit.py
│   ├── humanize.py
│   └── summarize.py
└── web/                     ← Public: ComfyUI web extension
```

### Live Importers (production code inside `vibecomfy/`)

| Importer | Import Path | Contract |
|---|---|---|
| `comfy_nodes/__init__.py` | `from .exec_node import EXEC_CLASS_TYPE, VibeComfyExec` | ✅ Internal (relative) |
| `comfy_nodes/__init__.py:185` | `from vibecomfy.comfy_nodes.agent.runtime_code import execute_runtime_code` | ✅ Public |
| `comfy_nodes/__init__.py:264` | `from vibecomfy.comfy_nodes.agent.runtime_code import execute_runtime_code_dynamic` | ✅ Public |
| `comfy_nodes/agent/diagnostics.py:140` | `from vibecomfy.comfy_nodes import NODE_CLASS_MAPPINGS` | ✅ Public (top-level) |
| `ingest/normalize.py:440` | `from vibecomfy.comfy_nodes.exec_node import ExecNodeContractError, parse_io` | ⚠️ Public (but in-package) |
| `schema/provider.py:121` | `from vibecomfy.comfy_nodes.exec_node import EXEC_SLOT_COUNT` | ⚠️ Public (but in-package) |
| `porting/edit/ops.py:8` | `from vibecomfy.comfy_nodes.agent.provider import ...` | ⚠️ Cross-package |
| `runtime/attempt.py:51` | `from vibecomfy.comfy_nodes.agent.audit import ...` | ⚠️ Cross-package |

### Env-Var Runtime Backends (loaded via `importlib.import_module`, not direct imports)

| Module Path | Mechanism |
|---|---|
| `vibecomfy.comfy_nodes.agent.runtime` | `VIBECOMFY_ARNOLD_RUNTIME_MODULE` env var |
| `vibecomfy.comfy_nodes.agent.fixture_provider` | `VIBECOMFY_ARNOLD_RUNTIME_MODULE` env var |

### Script importers (dev tools, not shipped code)

- `scripts/_catalog_size_probe.py` → `agent.edit`, `agent.provider`
- `scripts/_agent_edit_prompt_dump.py` → `agent.edit`, `agent.provider`
- `scripts/generate_agent_contract_js.py` → `agent.contracts`

---

## Public Paths That MUST Be Preserved

These are ComfyUI contracts that external code depends on:

| Path | Rationale |
|---|---|
| `vibecomfy.comfy_nodes` (the package) | Registered in `pyproject.toml` as `[project.entry-points."comfyui.custom_nodes"].vibecomfy = "vibecomfy.comfy_nodes"` |
| `vibecomfy.comfy_nodes.NODE_CLASS_MAPPINGS` | Standard ComfyUI discovery contract |
| `vibecomfy.comfy_nodes.NODE_DISPLAY_NAME_MAPPINGS` | Standard ComfyUI discovery contract |
| `vibecomfy.comfy_nodes.WEB_DIRECTORY` | Standard ComfyUI web-extension path |
| `vibecomfy.comfy_nodes.agent.routes` | Lazy-imported by `__init__.py` to register HTTP routes |
| `vibecomfy.comfy_nodes.agent.runtime` | `VIBECOMFY_ARNOLD_RUNTIME_MODULE` env-var dispatch |
| `vibecomfy.comfy_nodes.agent.fixture_provider` | `VIBECOMFY_ARNOLD_RUNTIME_MODULE` env-var dispatch |
| `vibecomfy.comfy_nodes.agent.worker` | Subprocess entry point (invoked by module path) |
| `vibecomfy.comfy_nodes.agent.runtime_code.execute_runtime_code` | Imported by `__init__.py` at runtime |
| `vibecomfy.comfy_nodes.agent.runtime_code.execute_runtime_code_dynamic` | Imported by `__init__.py` at runtime |
| `vibecomfy.comfy_nodes.exec_node.EXEC_CLASS_TYPE` | Exported from `__init__.py` |
| `vibecomfy.comfy_nodes.exec_node.VibeComfyExec` | Exported from `__init__.py` |
| `vibecomfy.comfy_nodes.exec_node.ExecNodeContractError` | Imported by `ingest/normalize.py` |
| `vibecomfy.comfy_nodes.exec_node.parse_io` | Imported by `ingest/normalize.py` |
| `vibecomfy.comfy_nodes.exec_node.EXEC_SLOT_COUNT` | Imported by `schema/provider.py` |
| `vibecomfy.comfy_nodes.agent.provider` (various) | Imported by `porting/edit/ops.py` |
| `vibecomfy.comfy_nodes.agent.audit` (various) | Imported by `runtime/attempt.py` |

---

## Private / Dead Paths That Can Be Moved or Deleted

| Path | Verdict | Rationale |
|---|---|---|
| `vibecomfy/comfy_nodes/stages/` | ❌ **DELETE** | ~2,700 LOC of dead code. All 7 files use relative imports (`from ..agent_contracts import ...`) that target **non-existent modules** — the real modules live in `agent/`. Nothing imports `stages/`. The only consumer was the also-dead `session_io.py`. Existing audit docs confirm this (see `docs/structure_cleanup/comfy_nodes_results/01-top-level-boundary.txt`). |
| `vibecomfy/comfy_nodes/session_io.py` | ❌ **DELETE** | 730 lines, **zero importers** in any `.py` file (confirmed by grep). Superseded by `agent/session.py` and `agent/routes.py`. Its sub-imports from `stages/humanize.py` are also dead. |
| `vibecomfy/comfy_nodes/_time_utils.py` | ❌ **DELETE** | 8-line `_now()` helper, **zero importers** anywhere. |
| `vibecomfy/comfy_nodes/exec_examples.py` | ✅ **KEEP** (or inline) | Single importer (`exec_node.py`). Keeping it separated is defensible; optionally inline the 78 LOC. |

### What about the `stages/humanize.py` symbols used by `session_io.py`?

The three symbols (`_field_changes_payload`, `_json_safe`, `_safe_session_id`) are imported by `session_io.py`, which is itself **dead code**. So these symbols have no live consumers and can be deleted along with the rest of `stages/`. If you're nervous, a two-phase plan works: delete `stages/` first, let CI pass, then delete `session_io.py`.

---

## Proposed Stale-Import Guard Searches & Tests

Add these to CI or a pre-commit hook to prevent re-introduction of dead imports:

### 1. Guard: No imports from `stages/`

```bash
grep -rn "from vibecomfy.comfy_nodes.stages\|from \.stages" \
  vibecomfy/ tests/ scripts/ --include="*.py" && \
  echo "FAIL: stale stages/ import detected" && exit 1 || exit 0
```

### 2. Guard: No imports from `session_io`

```bash
grep -rn "session_io" vibecomfy/ tests/ --include="*.py" && \
  echo "FAIL: stale session_io import detected" && exit 1 || exit 0
```

### 3. Guard: No imports from `_time_utils`

```bash
grep -rn "_time_utils" vibecomfy/ tests/ --include="*.py" && \
  echo "FAIL: stale _time_utils import detected" && exit 1 || exit 0
```

### 4. Guard: Cross-package imports into `comfy_nodes.agent.*` from outside `comfy_nodes/`

These are legitimate but fragile — if `runtime/attempt.py` or `porting/edit/ops.py` are ever moved/refactored the contracts could break. Flag them for review periodically:

```bash
grep -rn "from vibecomfy.comfy_nodes.agent\." \
  vibecomfy/ --include="*.py" \
  --exclude-dir="tests" --exclude-dir="scripts" | \
  grep -v "vibecomfy/comfy_nodes/"
```

This should only show:
- `vibecomfy/runtime/attempt.py` (agent.audit)
- `vibecomfy/porting/edit/ops.py` (agent.provider)
- `vibecomfy/ingest/normalize.py` (exec_node — top-level, not agent)
- `vibecomfy/schema/provider.py` (exec_node — top-level, not agent)

### 5. Recommended CI test: Package-load smoke test

```python
# tests/test_comfy_nodes_entrypoint.py (exists, but verify it covers all)
python -c "
from vibecomfy.comfy_nodes import (
    NODE_CLASS_MAPPINGS,
    NODE_DISPLAY_NAME_MAPPINGS,
    WEB_DIRECTORY,
)
assert 'VibeComfyExec' in NODE_CLASS_MAPPINGS
assert len(NODE_CLASS_MAPPINGS) == 4
print('Entry-point contract OK')
"
```

### 6. Recommended CI test: Agent subpackage loads cleanly

```python
python -c "
# Verify all public agent submodules are importable
from vibecomfy.comfy_nodes.agent import (
    audit, contracts, diagnostics, edit,
    gates, provider, routes, runtime_code, session
)
print('Agent subpackage OK')
"
```

### 7. Env-var dispatch test (optional, for CI)

```bash
# Verify the runtime backends resolve without ImportError
python -c "
import importlib
for mod in [
    'vibecomfy.comfy_nodes.agent.runtime',
    'vibecomfy.comfy_nodes.agent.fixture_provider',
]:
    m = importlib.import_module(mod)
    print(f'{mod} → OK')
"
```

---

## Summary

| Contract | Status | Action |
|---|---|---|
| `vibecomfy.comfy_nodes` (package) | ✅ Public | Preserve |
| `__init__.py` (mappings, web dir, routes) | ✅ Public | Preserve |
| `agent/*` (16 modules) | ✅ Public | Preserve — all have importers |
| `exec_node.py` | ✅ Public | Preserve — 3 production importers |
| `exec_examples.py` | 🔶 Internal | Preserve or inline |
| `stages/` (7 files) | ❌ Dead/broken | Delete |
| `session_io.py` | ❌ Dead | Delete |
| `_time_utils.py` | ❌ Dead | Delete |