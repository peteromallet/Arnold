Here is the complete audit.

---

# Comfy Nodes Audit 01: Top-Level Boundary — Results

## Ranked Verdict (Worst Offender → Safe Keep)

### 1. ❌ `stages/` — DELETE IMMEDIATELY (broken, orphaned, ~2,700 LOC of dead code)

**Status: OBSOLETE — broken imports across all 7 files**

Every file in this package uses relative imports that target **non-existent modules**:

| File | Broken Import | Should Be |
|------|--------------|-----------|
| `__init__.py` | `from ..agent_contracts import ...` | `from ..agent.contracts import ...` |
| | `from ..agent_gates import ...` | `from ..agent.gates import ...` |
| `agent.py` | `from ..agent_contracts`, `..agent_audit`, `..agent_provider` | `..agent.contracts`, etc. |
| `audit.py` | `from ..agent_audit`, `..agent_contracts`, `..agent_session` | `..agent.audit`, etc. |
| `batch_repl.py` | `from ..agent_audit`, `..agent_contracts`, `..agent_provider` | `..agent.*` |
| `emit.py` | `from ..agent_audit`, `..agent_contracts`, `..agent_diagnostics` | `..agent.*` |
| `humanize.py` | `from ..agent_audit`, `..agent_contracts`, `..agent_provider`, `..agent_session` | `..agent.*` |
| `summarize.py` | `from ..agent_contracts`, `..agent_diagnostics`, `..agent_gates` | `..agent.*` |

**No importer links to this package** — the only consumer was `session_io.py` (itself dead code — see below). The maintenance script `_fix_t6.py` mentions `stages.agent`/`stages.batch_repl` in string-search context only; no live code reaches into `stages/`. This package was written before `agent_contracts.py` etc. were moved into `agent/` and was never updated to match.

- **Delete**: `rm -rf vibecomfy/comfy_nodes/stages/`
- **Import risk**: None — nothing imports it (the imports within it are themselves broken, so they can't succeed anyway)
- **Test risk**: None — zero test files import `stages`
- **Verification**: `grep -r "stages" vibecomfy/comfy_nodes/ --include="*.py" | grep -v ".pyc"` → should show only the now-removed `session_io.py` reference, then nothing
- **Risk if kept**: Misleading future developers; dead weight ~2,700 lines; ImportError waiting to happen if someone tries `from vibecomfy.comfy_nodes import stages`

---

### 2. ❌ `session_io.py` — DELETE (730 lines of dead code)

**Status: OBSOLETE — zero importers**

A 730-line file with helpers `read_session_chat`, `read_session_bundle`, `read_session_json`, etc. that is **never imported** by any Python module. The grep across all `.py` files in the repo returned **zero matches** for `from ... session_io import ...` or `import session_io`.

These session-reading utilities appear to have been superseded by `agent/session.py` and `agent/routes.py` (which expose the same functionality via HTTP routes).

- **Delete**: `rm vibecomfy/comfy_nodes/session_io.py`
- **Import risk**: None — dead code, no importers
- **Test risk**: None — zero test files reference it
- **If you're Nervous™**: Before deleting, check if any of these functions are business-critical by verifying `.stages.humanize` functions it imports (`_field_changes_payload`, `_json_safe`, `_safe_session_id`) are also dead (they are — see stages audit above)
- **Verification**: `grep -rn "session_io" vibecomfy/ tests/ --include="*.py"` → should return zero results

---

### 3. ⚠️ `_time_utils.py` — DELETE (8-line trinket, unused)

**Status: OBSOLETE — zero importers**

```python
def _now() -> str:
    return time.strftime("%Y-%m-%dT%H:%M:%SZ", time.gmtime())
```

The function `_now` is **never called anywhere** in the codebase. Zero importers. It was presumably extracted to share timestamp formatting but nobody adopted it.

- **Delete**: `rm vibecomfy/comfy_nodes/_time_utils.py`
- **Import risk**: None
- **Test risk**: None
- **Verification**: `grep -rn "_time_utils\|from.*_time_utils\|import.*_time_utils" vibecomfy/ tests/ --include="*.py"` → should return zero results

---

### 4. ✅ `exec_examples.py` — KEEP (or optionally inline into `exec_node.py`)

**Status: LIVE — imported by `exec_node.py`**

| Importer | Line | Import |
|----------|------|--------|
| `exec_node.py` | 9 | `from .exec_examples import EXEC_EXAMPLES, EXEC_HELP_TEXT` |

This file provides example data and help text for the exec node. Keeping it separate is defensible (data/logic separation), but it's only 78 lines and has exactly one consumer. Inlining would be low-value cleanup.

- **Recommendation**: **Keep** unless you're doing a consolidation pass, in which case inline the two constants into `exec_node.py`
- **Risk if moved**: `exec_node.py` import breaks; trivial to fix
- **Verification**: `grep -rn "exec_examples" vibecomfy/ tests/ --include="*.py"` → should only show `exec_node.py` and test files

---

### 5. ✅ `exec_node.py` — KEEP (core runtime node)

**Status: LIVE — actively imported**

| Importer | What's imported |
|----------|----------------|
| `__init__.py` (line 32) | `EXEC_CLASS_TYPE, VibeComfyExec` |
| `ingest/normalize.py` (line 440) | `ExecNodeContractError, parse_io` |
| `schema/provider.py` (line 121) | `EXEC_SLOT_COUNT` |
| `tests/test_comfy_exec_node.py` | Full node test |
| `tests/test_intent_nodes.py` | `EXEC_CLASS_TYPE, EXEC_SLOT_COUNT, VibeComfyExec` |

239 lines, clean structure, well-tested. **No action needed.**

---

### 6. ✅ `agent/` — KEEP (core subpackage)

**Status: LIVE — 16 modules, heavily imported**

Used by `__init__.py`, tests (`test_comfy_nodes_agent_backend_spine.py`, `test_comfy_nodes_agent_edit.py`, `test_comfy_roundtrip_route.py`, `test_fixture_provider.py`). Contains the agent backend: contracts, session, edit, routes, runtime, provider, gates, diagnostics, audit, etc.

**No action needed.**

---

### 7. ✅ `web/` — KEEP (frontend assets)

**Status: LIVE — referenced by `__init__.py` line 37**

```python
WEB_DIRECTORY = "./web"
```

Contains 14+ JS files, `package.json`, `.gitkeep`. This is the ComfyUI custom-node web extension directory. The `.bak` files (`panel_thread.js.bak`, `vibecomfy_roundtrip.js.bak`) could be cleaned up in a separate pass, but the directory itself is essential.

**No action needed.**

---

## Summary Table

| Entry | Verdict | Lines | Action | Risk |
|-------|---------|-------|--------|------|
| `stages/` | ❌ DELETE | ~2,700 | `rm -rf` | None (already broken) |
| `session_io.py` | ❌ DELETE | 730 | `rm` | None (zero importers) |
| `_time_utils.py` | ❌ DELETE | 8 | `rm` | None (zero importers) |
| `exec_examples.py` | ✅ KEEP | 78 | — | None |
| `exec_node.py` | ✅ KEEP | 239 | — | — |
| `agent/` | ✅ KEEP | — | — | — |
| `web/` | ✅ KEEP | — | — | — |
| `__init__.py` | ✅ KEEP | 302 | — | — |

---

## Focused Verification Commands

Run these **before** and **after** changes to confirm no regressions:

```bash
# 1. Confirm stages/ has no live importers (should show 0 matches)
grep -rn "from vibecomfy.comfy_nodes.stages\|from \.stages" vibecomfy/ tests/ --include="*.py"

# 2. Confirm session_io has no live importers
grep -rn "session_io" vibecomfy/ tests/ --include="*.py"

# 3. Confirm _time_utils has no live importers
grep -rn "_time_utils\|from.*_time_utils" vibecomfy/ tests/ --include="*.py"

# 4. Confirm exec_node tests still pass after any moves
cd /Users/peteromalley/Documents/reigh-workspace/vibecomfy
python -m pytest tests/test_comfy_exec_node.py -x --no-header -q 2>&1 | tail -5

# 5. Confirm agent package still loads
python -c "from vibecomfy.comfy_nodes.agent import contracts, session, edit; print('agent OK')"

# 6. Confirm top-level __init__ still loads (requires ComfyUI server context)
python -c "import sys; sys.path.insert(0, '.'); from vibecomfy.comfy_nodes import NODE_CLASS_MAPPINGS; print(f'{len(NODE_CLASS_MAPPINGS)} nodes registered')"
```

---

## One Caveat

If `session_io.py` functions (`read_session_chat`, `read_session_bundle`, `read_session_json`) look like they **should** be alive (i.e., a planned route handler depends on them but hasn't been written yet), consider **archiving** rather than deleting. But the import analysis shows they were never wired up — they're a fossil from a refactor that chose a different architecture (the agent subpackage's `session.py` + HTTP routes in `routes.py`). Delete with confidence.