Here is the complete audit.

---

## Decision Table

### ── KEEP at root (public console boundaries) ──

| File | Action | Evidence | Safe-to-move tests |
|------|--------|----------|--------------------|
| **`cli.py`** | **KEEP + CONSOLIDATE** | Console entry point via `pyproject.toml` line 70: `vibecomfy = "vibecomfy.cli:main"`. 6 test files + `cli/__main__.py` import it. | N/A — stays. But `cli/__init__.py` packages extra features (`--quiet`, nudge) that are **shadowed** by this file. Must merge nudge logic into `cli.py`, then delete `cli/__init__.py`. |
| **`cli_loader.py`** | **KEEP** | Public API re-exported from `vibecomfy.__init__` (line 16). 17 internal callers across commands, router, ops, tests, recipes. Orchestrator, not implementation detail. | N/A — stays. |

### ── STALE (split personality, shadowed dead code) ──

| File | Action | Evidence | Safe-to-delete tests |
|------|--------|----------|----------------------|
| **`cli/__init__.py`** | **DELETE after merge** | `vibecomfy/cli.py` (module) **shadows** `vibecomfy/cli/__init__.py` (package). Python resolves `import vibecomfy.cli` to the `.py` file, not the package. The enhanced `_maybe_print_nudge()` and `--quiet` flag in `__init__.py` **never execute** via the console entry point. No code imports `vibecomfy.cli.__init__`. | Merge `_maybe_print_nudge` logic into `cli.py`, update `cli.py`'s `build_parser()` to accept `--quiet`. Delete `cli/__init__.py`. Confirm `vibecomfy --help` works, `python -m vibecomfy.cli` works, all `test_cli_*` + `test_gate` pass. |
| **`cli/__main__.py`** | **KEEP** (update import) | Valid `python -m vibecomfy.cli` pattern. Currently `from vibecomfy.cli import main` — resolves to `cli.py`, fine as-is after merge. | N/A. |

### ── MOVE (implementation details that belong under packages) ──

| File | Action | Evidence | Safe-to-move tests |
|------|--------|----------|--------------------|
| **`comfy_backend.py`** | **MOVE → `vibecomfy/backend/`** | Backend adoption hook: version matrix, `ensure_nodes()`, compatibility check. Not a public console boundary. Called internally by `ingest/normalize.py`, `porting/edit/normalize.py`, `porting/refuse.py`, `tests/`. | Update 15 imports. After move: `test_comfy_backend.py`, `test_porting_normalize_ingest.py`, `test_refuse.py`, `test_porting_ui_emitter.py` pass. |
| **`comfy_command.py`** | **MOVE → `vibecomfy/runtime/`** | Finds comfyui executable for session spawning. Only 30 lines. Called by `runtime/session.py`, `nodes/index.py`, `schema/cache.py`, `schema/provider.py`. Natural home is `runtime/`. | After move: `test_runtime_session_config.py` passes. |
| **`scratchpad_loader.py`** | **MOVE → `vibecomfy/loader/`** | Loader implementation (imports `.py` as workflows). `loader/__init__.py` is a **dead empty file** — this belongs there alongside `agent_generated_loader.py`. Called by `cli_loader.py`, `registry/library.py`, `porting/workbench.py`. | After move: `test_scratchpad_loader.py`, `test_loader_gates.py`, `test_virtual_wire_round_trip.py` pass. Update 3 production imports + 3 test imports. |

### ── DISCERN (borderline, reasonable arguments both ways) ──

| File | Action | Evidence | Suggested test |
|------|--------|----------|----------------|
| **`local_library.py`** | **KEEP at root** (could move to `config/`) | Cross-cutting config domain: 352 lines. Used by CLI nudge (`cli/__init__.py`/after-merge `cli.py`), `commands/config.py`, `fetch.py`, `runtime/drift.py`, `runtime/_local_library_yaml.py`, `node_packs/_install.py`, `porting/wrappers/discovery.py`. It's a **config model** consumed by nearly every subsystem — root level signals it's foundational. Moving to a new `config/` package is valid but adds a layer for no clear win. | `test_local_library.py` passes either way. |
| **`runtime/_local_library_yaml.py`** | **KEEP** | Already in `runtime/` — correctly placed. Imports `vibecomfy.local_library` (root) which is fine. | — |

---

## Summary of actions ordered by impact

1. **CONSOLIDATE**: Merge `cli/__init__.py`'s `_maybe_print_nudge` + `--quiet` into `cli.py`. Delete `cli/__init__.py`. (Fixes dead code.)
2. **MOVE**: `scratchpad_loader.py` → `loader/`. (Fills empty `loader/` package.)
3. **MOVE**: `comfy_command.py` → `runtime/`. (Natural home, called by session.)
4. **MOVE**: `comfy_backend.py` → `backend/`. (Implementation detail, not public API.)
5. **KEEP**: `cli.py`, `cli_loader.py`, `local_library.py`. (Public boundaries / cross-cutting config.)

After all moves, `vibecomfy/` root should contain only: `__init__.py`, `cli.py`, `cli_loader.py`, `local_library.py`, `cli/__main__.py`, and all sub-packages.