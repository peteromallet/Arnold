Now I have a complete picture. Here's my synthesis:

---

## Cleanup Batch for `vibecomfy/*.py`

### Inventory of top-level `vibecomfy/*.py`

**37 files** in the package root. I've audited import chains for each.

---

### ✅ DO NOW (low-risk deletions)

**1. Delete `vibecomfy/source_map.py`** — Zero external Python imports. Only appears in docs & `m1-safety-gate.md` artifacts. Dead code.

**2. Delete `vibecomfy/_graph_utils.py`** — Zero importers anywhere. Dead.

**3. Delete `vibecomfy/_workflow_helpers.py`** — Zero importers. Dead.

**4. Delete `vibecomfy/_helper_resolve.py`** — Zero importers. Dead.

**Total: 4 deletes, ~400 lines removed. Zero behavioral risk.**

Verify:
```bash
# Confirm no importers
rg -n "from vibecomfy\.(source_map|_graph_utils|_workflow_helpers|_helper_resolve) import\|import vibecomfy\.(source_map|_graph_utils|_workflow_helpers|_helper_resolve)" vibecomfy/ tests/ --include='*.py'
# Should return nothing (docs refs only)
```

---

### 🟡 DO LATER (straightforward moves, need import migration)

**5. Move `vibecomfy/node_packs_git.py` → `vibecomfy/node_packs/_git.py`** (already exists as real module). The root file is a 3-line `from ._git import *` shim. Only **5 importers** to update:
- `vibecomfy/runtime/ensure_env.py`
- `tests/test_node_packs_git.py`
- `docs/plans/revised_plan.md` (doc ref)

**6. Move `vibecomfy/fixtures.py` → `vibecomfy/testing/fixtures.py`** — Only **1 importing test** (`tests/test_fixtures.py`). Has a `__main__` block; just update the `-m vibecomfy.fixtures` call sites in shell scripts. Low risk.

**7. Move `vibecomfy/comfy_command.py` → `vibecomfy/runtime/_comfy_command.py`** — Only **5 importers**: `runtime/session.py`, `nodes/index.py`, `schema/cache.py`, `schema/provider.py`, `tests/test_runtime_session_config.py`. Straightforward path update.

**8. Delete shim `vibecomfy/node_packs_install.py`** — 14-line re-export shim, but **defers**: has **26 importers** across tests, runtime, tools. Needs bulk find-and-replace, wait until a migration tool is ready.

Verify pattern:
```bash
# After each move, update the import paths and run
pytest tests/test_node_packs_git.py -x --no-header -q
pytest tests/test_fixtures.py -x --no-header -q
pytest tests/test_runtime_session_config.py -x --no-header -q
python -c "from vibecomfy.node_packs._git import find_installed_pack_ref; print('OK')"
```

---

### ❌ DO NOT DO

| File | Reason |
|------|--------|
| `handles.py` | Core type, **30+ importers**, re-exported from `__init__.py` public API |
| `workflow.py` | Core `VibeWorkflow`, heaviest-used symbol in the project |
| `workflow_context.py` | Context vars for workflow binding, **18 importers** |
| `extras.py` | Plugin system, public API re-export `ensure_plugins_loaded` |
| `node_packs.py` | Public re-export package, referred to from `node_packs_lockfile.py` |
| `node_packs_lockfile.py` | **30 importers**, real logic (334 lines), not a shim. Absorbing into `node_packs/` is later work |
| `cli_loader.py` | **20 importers**, public API export `load_workflow_any` |
| `artifacts.py` | Public API export `Artifact`, `Image`, `Video` etc. from `__init__.py` |
| `metadata.py` | **8 importers** across porting, ingest, patches, ir |
| `memory_profile.py` | **7 importers** across runtime, tests, tools |
| `_agent_edit_debug.py` | **5 importers** (scripts, commands, tests) — large file, active |
| `_widget_aliases.py` | 3-line sys.modules swap, still actively imported by `workflow.py:10` |
| `checks.py` | Used by `commands/check.py` and tests |
| `fetch.py` | **13 importers** across commands and tests |
| `local_library.py` | Config resolution for custom_nodes/models paths |
| `templates.py` | Core template system w/ golden artifacts dependency |
| `origin.py` | **4 importers** in patches/ops/agentic |