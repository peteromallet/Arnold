Now I have a complete picture. Here's the audit.

---

## Ranked Action List

### TIER 1 — Confirmed Dead (safe to delete)

**1. `source_map.py`** (149 LOC)
- **Evidence**: Zero `import` or `from` references across entire repo. Doc audit in `docs/structure_cleanup/dead_module_results/10-safe-first-batch.txt` confirms dead.
- **Verification**: `rg -n "from vibecomfy.source_map import|import vibecomfy.source_map" .` → no results.
- **Action**: Delete file.

**2. `node_packs.py`** (465 LOC)
- **Evidence**: Python resolves `vibecomfy.node_packs` to the `node_packs/` package (directory + `__init__.py`), not this `.py` file. The `.py` file is completely shadowed. The `node_packs/` package lazily re-exports identical `CustomNodePack` with static pack defs from `._defs`.
- **Verification**: `python -c "import vibecomfy.node_packs; print(vibecomfy.node_packs.__file__)"` → points to `__init__.py`, never `node_packs.py`.
- **Action**: Delete file.

---

### TIER 2 — Full Duplicate (consolidate, then delete)

**3. `node_packs_lockfile.py`** (334 LOC)
- **Evidence**: Identical content to `node_packs/_lockfile.py` (334 lines each). Both have `LockEntry`, `read_lockfile`, `compute_schema_hash`, etc. The shims `node_packs_install.py`/`node_packs_git.py` use `from ... import *` — but this file is *not* a shim, it's a full copy.
- **Internal consumers** (20+): `porting/emitter.py`, `porting/emit/emitter.py`, `porting/convert.py`, `porting/emit_ready.py`, `porting/workbench.py`, `commands/doctor.py`, `commands/nodes.py`, `analysis/node_coverage.py`, `runtime/session.py`, `runtime/drift.py`, etc.
- **Action**: Replace all internal imports `from vibecomfy.node_packs_lockfile import ...` → `from vibecomfy.node_packs import ...`, then delete `node_packs_lockfile.py`. The package already re-exports all same symbols via lazy `__getattr__`.

---

### TIER 3 — Thin Re-export Shims (trivial to move)

**4. `node_packs_install.py`** (14 LOC)
- **Content**: `from vibecomfy.node_packs._install import *` + 3 explicit re-exports.
- **Consumers** (3): `porting/provenance.py`, `runtime/ensure_env.py`, `runtime/session.py`.
- **Action**: Switch consumers to `from vibecomfy.node_packs import ...`, delete shim.

**5. `node_packs_git.py`** (3 LOC)
- **Content**: `from vibecomfy.node_packs._git import *`
- **Consumers** (1): `runtime/ensure_env.py`.
- **Action**: Switch to `from vibecomfy.node_packs import find_installed_pack_ref`, delete shim.

---

### TIER 4 — Misplaced Real Boundaries (move to sub-packages)

| Module | LOC | Consumers | Belongs in | Reasoning |
|--------|-----|-----------|------------|-----------|
| **6. `model_assets.py`** | 382 | 7 internal (porting/assets, commands/doctor, commands/_model_entries, runtime/session, runtime/attempt, ir/workflow, workflow) | `registry/` | Tightly coupled to `registry.models_loader.ModelEntry`; deals with registry-backed resolution and HF split-file URL parsing |
| **7. `fetch.py`** | 149 | 7 internal + 5 test (commands/fetch, commands/models, commands/workflows, commands/doctor, registry/models_loader, runtime/model_policy) | `registry/` | Download utilities for model assets; imports from `model_assets`; CLIs treat it as `fetch_assets` |
| **8. `custom_node_refs.py`** | 159 | 8 internal (porting/convert, porting/workbench, commands/doctor, templates, registry/ready_template, registry/static_contract, runtime/session) | `registry/` or `node_packs/` | Normalizes custom-node lock entries and workflow refs; bridges `LockEntry` → workflow requirements |
| **9. `metadata.py`** | 107 | 8 internal (porting/lowering, porting/workbench, ingest/normalize, patches/seed, ir/workflow, workflow, registry/static_contract) | `schema/` | Schema-level constants (`OUTPUT_NODE_NAMES`, `PROMPT_KEYS`, etc.) and inference helpers |
| **10. `index_types.py`** | 32 | 2 internal (ingest/index, nodes/index) | `schema/` or `ingest/` | TypedDict row types for search indexes; no business logic |

---

### Summary: What stays where

Everything not listed above (e.g. `workflow.py`, `handles.py`, `artifacts.py`, `ops/`, `router/`, `patches/`, `vibecomfy/__init__.py`) are correctly at root as public API boundaries and should stay.

**Verification commands** (run from repo root):
```bash
# Dead modules
grep -rn "from vibecomfy.source_map import\|import vibecomfy.source_map" . --include="*.py"
grep -rn "from vibecomfy.node_packs import" . --include="*.py" | head -5  # confirm hits go to package not .py

# Duplicate consolidation candidates
grep -rn "from vibecomfy.node_packs_lockfile import" . --include="*.py" | wc -l
grep -rn "from vibecomfy.node_packs_install import" . --include="*.py" | wc -l
grep -rn "from vibecomfy.node_packs_git import" . --include="*.py" | wc -l
```