Here is the complete audit:

---

## Audit: Artifact-like files under `vibecomfy/`

### 1. `vibecomfy/comfy_metadata.json` — **Keep, sanitize local path**

| Field | Detail |
|---|---|
| **Type** | Generated snapshot |
| **Evidence** | Contains `"install_dir": "/Users/peteromalley/Documents/reigh-workspace/vibecomfy/vendor/ComfyUI"`, `"version": "0.18.2"`, `"commit": "f7b38d2..."`, `"captured_at"` timestamp. Generated by `tools/refresh_comfy_metadata.py`. |
| **Consumers** | `vibecomfy/porting/object_info/consume.py` (METADATA_PATH), `vibecomfy/templates.py`, `vibecomfy/registry/static_contract.py` |
| **Impact** | Ships in wheel (under `vibecomfy/`). The absolute `install_dir` path is environment-specific and will be wrong on any other machine, but it's only used for narration/fallback — not for critical routing. The version/commit fields are useful for provenance. |
| **Verdict** | **Keep** — actively consumed at runtime, not dead junk. However the `install_dir` field should be made relative or stripped before check-in to avoid leaking local FS structure. |
| **Verify** | `python -c "import json; d=json.load(open('vibecomfy/comfy_metadata.json')); print(d['version'], d['commit'][:8])"` |

---

### 2. `vibecomfy/py.typed` — **Keep**

| Field | Detail |
|---|---|
| **Type** | PEP 561 marker |
| **Evidence** | Empty file (1 byte). Explicitly listed in `pyproject.toml` `[tool.hatch.build.targets.wheel].artifacts`. |
| **Impact** | Required for downstream type-checkers to recognize the package as typed. Harmless. |
| **Verdict** | **Keep** — standard, intentional, documented in build config. |
| **Verify** | `test -f vibecomfy/py.typed && echo exists` |

---

### 3. `vibecomfy/nodes/_generated/*.pyi` (20 files) — **Keep**

| Field | Detail |
|---|---|
| **Type** | Generated-but-checked-in stubs |
| **Evidence** | Each has `# vibecomfy:generated` header. Regenerated by `vibecomfy nodes generate-wrappers`. Default out-dir is `vibecomfy/nodes/`. The `_generated/` subdirectory is explicitly the canonical output location. |
| **Impact** | These are the intended cache/fixture for the typed-wrapper system. They ship in the wheel via `vibecomfy/**/*.pyi` artifact glob. Top-level `vibecomfy/nodes/*.pyi` re-export from them. Removing them would break `vibecomfy.nodes.core.*` and all downstream template code. |
| **Verdict** | **Keep** — this IS the repo's designated cache location for generated stubs. Architecture intentionally checks them in. |
| **Verify** | `head -5 vibecomfy/nodes/_generated/core.pyi \| grep "vibecomfy:generated"` |

---

### 4. `vibecomfy/nodes/*.pyi` (top-level, 20 files) — **Keep**

| Field | Detail |
|---|---|
| **Type** | Hand-written re-export stubs |
| **Evidence** | Each is a thin `from vibecomfy.nodes._generated.X import *; __all__: list[str]`. e.g. `core.pyi` is 3 lines. |
| **Impact** | Public API surface for `from vibecomfy.nodes.core import KSampler`. Without these, imports would break. |
| **Verdict** | **Keep** — intentional public re-export layer. |
| **Verify** | `cat vibecomfy/nodes/core.pyi` |

---

### 5. `vibecomfy/porting/cache/object_info/*.json` (27 files) — **Keep, but sanitize provenance.json**

| Field | Detail |
|---|---|
| **Type** | Checked-in object_info snapshots |
| **Evidence** | Files are named `{pack}@{source}.json` (e.g. `comfy_core@runpod-snapshot.json`). `index.json` maps class names → snapshot files. `provenance.json` records `install_dir` (absolute local paths), `schema_sha256`, `locked_commit`. |
| **Impact** | These are the fixture/cache data backing node discovery and wrapper generation. They ship in the wheel. The `provenance.json` contains absolute local `install_dir` paths (e.g. `/Users/peteromalley/.../vendor/ComfyUI`). |
| **Verdict** | **Keep** — this IS the repo's designated cache directory for object_info fixtures. However `provenance.json` should have its `install_dir` values sanitized to relative paths or stripped. |
| **Verify** | `python -c "import json; d=json.load(open('vibecomfy/porting/cache/object_info/provenance.json')); print([p.get('install_dir','')[:50] for p in d['packs'].values() if p.get('install_dir')][:3])"` |

---

### 6. `vibecomfy/porting/cache/class_inventory.json` — **Move or sanitize**

| Field | Detail |
|---|---|
| **Type** | Generated snapshot with local absolute paths |
| **Evidence** | 5938-line JSON. Contains absolute paths like `/Users/peteromalley/Documents/.megaplan-worktrees/vibecomfy-v25/ready_templates/...` in the `typed_wrappers.*.files` arrays. This is a local developer's worktree path baked into the shipped package. |
| **Impact** | Ships in wheel. Contains hardcoded absolute home-directory paths. Useless on any other machine and leaks local FS structure. |
| **Verdict** | **Move or sanitize**. Either strip the `files` paths (they appear to be metadata, not runtime-critical) or move the file to `tests/fixtures/` or `tools/` if it's only used by tooling. If it IS runtime-critical, the paths must be made relative. |
| **Verify** | `grep -c "Users/peteromalley" vibecomfy/porting/cache/class_inventory.json` (shows how many absolute-path leaks) |

---

### 7. `vibecomfy/comfy_nodes/web/agent_edit_response_contract_generated.js` — **Keep**

| Field | Detail |
|---|---|
| **Type** | Generated-but-drift-guarded JS contract |
| **Evidence** | Generated by `scripts/generate_agent_contract_js.py`. Drift guard in `tests/test_agent_contract_codegen.py` compares byte-for-byte against fresh generator output. |
| **Impact** | Ships in wheel (under `vibecomfy/comfy_nodes/web/`). This is intentionally checked in — the project design checks generated JS contracts with an automated drift test. |
| **Verdict** | **Keep** — intentional, well-guarded. |
| **Verify** | `head -3 vibecomfy/comfy_nodes/web/agent_edit_response_contract_generated.js | grep "Generated"` |

---

### 8. `vibecomfy/comfy_nodes/web/package.json` — **Keep**

| Field | Detail |
|---|---|
| **Type** | Hand-written config |
| **Evidence** | Content: `{"type": "module"}`. Tiny, no generated headers. |
| **Impact** | Configures ES module resolution for the JS files in that directory. Harmless. |
| **Verdict** | **Keep** — intentional config. |
| **Verify** | `cat vibecomfy/comfy_nodes/web/package.json` |

---

### Summary table

| File | Action | Why |
|---|---|---|
| `comfy_metadata.json` | **Keep** (sanitize `install_dir`) | Runtime consumer; local path is the only issue |
| `py.typed` | **Keep** | Standard PEP 561 marker |
| `nodes/_generated/*.pyi` | **Keep** | Designated stub cache location |
| `nodes/*.pyi` | **Keep** | Public re-export API surface |
| `porting/cache/object_info/*.json` | **Keep** (sanitize `provenance.json`) | Designated fixture cache; provenance has local paths |
| `porting/cache/class_inventory.json` | **Move or sanitize** | Absolute worktree paths shipped in wheel |
| `comfy_nodes/web/agent_edit_response_contract_generated.js` | **Keep** | Intentionally checked in, drift-guarded |
| `comfy_nodes/web/package.json` | **Keep** | Hand-written config |

**Two concrete cleanup items:**
1. `class_inventory.json` — either strip the `files` path arrays (they appear to be descriptive metadata only, not loaded at runtime), or move to `tools/fixtures/` if only used by tooling.
2. `provenance.json` in `object_info/` — strip `install_dir` or make paths relative to repo root.