> **Authority status (T44):** Zero-authority historical planning artifact — for reference only; not live operational authority. Canonical delegation is via the megaplan CLI and the migrated wrappers.

# Unknown exploration results (U1-U13) — evidence for the SOL solidify pass

## U1 — omp provider/model/credential contract (RESOLVED)
All 8 routes exist: deepseek, fireworks, zai (z.ai/GLM, model glm-5.2), moonshot, kimi-code (OAuth), openrouter, xai, anthropic. Cold-start contract: `RpcClient(provider=, model=, thinking=)` → argv `--provider/--model/--thinking <level>` (client.py:1798-1806; set_model :924, set_thinking_level :939). Thinking levels {off,minimal,low,medium,high,xhigh,max,auto} (thinking-levels.ts:7); RpcClient type omits `auto`. modelId must be exact catalog `provider/modelId` (models.md). Effort map per host in packages/catalog/src/model-thinking.ts (OpenRouter high-only; Kimi K3 low/high/max; GLM-5.2 high/max; Fireworks minimal→none). Credential env vars per provider in docs/environment-variables.md (deepseek→DEEPSEEK_API_KEY, zai→ZAI_API_KEY, xai→XAI_API_KEY, openrouter→OPENROUTER_API_KEY, anthropic→ANTHROPIC_API_KEY/OAUTH, moonshot→MOONSHOT_*).

## U2 — reproducible deployment (RESOLVED, Dockerfile is the gap)
Fresh box needs: (1) bun >=1.3.14 PINNED; (2) omp checkout (source mode) or compiled binary; (3) platform natives (linux-x64/arm64 prebuilt; empty native cache benign in source mode — falls back to node_modules; missing linux .node = hard fail, version-sentinel locked); (4) omp_rpc pip package (pure-Python, stdlib-only, >=3.11); (5) `omp` on PATH. Cloud Dockerfile gap: bun UNPINNED, `/root/.bun/bin` not on PATH, installs NEITHER omp NOR omp_rpc NOR natives. Missing linux .node in source mode is a hard failure → B9 must install natives explicitly.

## U3 — structured output (RESOLVED; strong path exists)
No enforced main-session structured output (confirmed; plan correct). Final assistant text = clean prose (no tool traces in get_last_assistant_text), but NOT guaranteed pure JSON (markdown fences/prose possible). STRONG PATH: emulate the codex local-strict mechanism — `capture_recovery.output_path` + `response_enforcement_attestation` (workers/_impl.py:6139-6166, model_seam.py:443 `_capture_local_strict_json`, exact-JSON parse `_parse_exact_json_object` :428) — adapter has the model WRITE the payload to a file via a tool, then reads that file. Weak/medium: prompt delimiters; `_extract_json_candidates_from_raw` (workers/_impl.py:4244) extraction (codex pattern). Gate schema is lossless/strict (most extra-key sensitive).

## U4 — bwrap on agentbox (RESOLVED: NOT VIABLE)
Hetzner resident image deliberately has NO unprivileged user namespaces (data/superfixer_debug_skill.md:372-376). bwrap inside container fails 'Permission denied' (workers/_impl.py:3432-3437); codebase already gates `bwrap --ro-bind / / true` and falls back to MEGAPLAN_TRUSTED_CONTAINER=1 (entrypoint.sh.tmpl:38). DECISION: accept+document (container isolation + TRUSTED_CONTAINER + relocated in-process path validators). Mount map + test matrix documented as fallback (reuse managed_agent.py:969-976 bind pattern; preserve process-group kill — no pid-ns; network inherited; rw roots = plan_dir, ~/.omp, /tmp, /var/tmp, /dev/shm, git identity files).

## U5 — cost authority (RESOLVED)
Authoritative per-attempt source = `usage` field on each RPC AssistantMessage (protocol.py:478-525). get_session_stats is a DERIVED idempotent sum over messages (session-stats.ts:69-138) — safe for reconciliation. Arnold codex worker pattern: delta-from-cumulative-total exactly-once (_impl.py:3807-3863), emits AgentResult.cost_usd/tokens → ledger receipt (:6361-6390). Adapter target: contracts.py:283-360 TokenUsage{prompt,completion,total}/CostUsage{cost_usd}.

## U6 — dispatch bypasses (RESOLVED: none new)
Every AIAgent construction/spawn/allowlist is already in the plan's census (workers/_impl, workers/hermes, adapters/deepseek, arnold.agent/__init__ module-level dispatcher, run_agent, parallel_critique dead path, cron DEAD, cloud wrappers, bakeoff/judge, subagent-launcher, poms_skills mirror). Minor additions: `_compatibility.py:315-361` parses `--hermes`/`--phase-model` as passthrough (routing vector — add census line, not a bypass); `cloud/summarize_fixer_session.py:10` hardcoded LAUNCHER path (B9 scope).

## U7 — relocation import graph (RESOLVED: vendoring, not rewiring)
megaplan/runtime/{sandbox,key_pool}.py are ALREADY physically decoupled (all importers use megaplan paths; zero importers reference arnold.agent for these). They are THIN RE-EXPORT SHIMS: sandbox.py = 14 names re-exported, defines nothing; key_pool.py re-exports 10 names + defines ~20 locals. Work = VENDOR the SSoT definitions: sandbox.py (8 names from arnold.agent.tools.sandbox + 6 from _sandbox_wrappers) + key_pool 10 names; transitive: SandboxViolation subclasses arnold.runtime.errors.ArnoldError, `_current_envelope` imports arnold.runtime.envelope._envelope_ctx (key_pool.py:74), install_sandbox wires into the dying tool registry (that wiring dies; omp pre-exec wrapper replaces it). Importers that stay (path unchanged): resident/runtime, agent_loop, profiles/policy, cloud/preflight, parallel_critique, review/parallel, tests.

## U8 — watchdog correlation (RESOLVED: blind spot worse than planned)
Python watchdog DROPS omp processes entirely — `_categorize_cmdline` returns None → `if category is None: continue` (processes.py) — not merely mislabels. pgid NOT read (no pgid in ProcessRecord); `--session-dir` NOT parsed by session-token extractor. Bash watchdog (arnold-watchdog) treats omp as non-target (never reaped) but never recognizes it as live (classify_kind:6168-6241; kimi_operator_running:2760-2860 uses pgid sidecar + kill -0 — the pattern to copy; reap uses ps pid,ppid,pgid,etimes). arnold-heartbeat tracks ONLY `codex exec` (pgrep -f). omp argv shape confirmed: `bun <cliPath> --mode rpc --provider --model --session-dir --no-session` (client.py:1797-1828; Dockerfile:161-164). Correlation keys: pgid + session-dir path + cwd + birth time + heartbeat file (write_watchdog_heartbeat mtime pattern exists).

## U9 — resident stateless (RESOLVED: one NEW guard-rail gap)
Stateless-per-turn holds by construction (Python composes full prompt; `--no-session` → in-memory SessionManager, NO disk/auto-resume = the guard rail, main.ts:771-834). NEW FINDING: `_run_locked` HARD-REQUIRES resolved session identity on success — `assert resolved_session_id is not None` (agent_loop.py:863) + `session_identity_missing` failure (:854-856); `reserve_session_id('omp')` returns None, `valid_session_id('omp',…)` False (provider_runtime.py:173-183) → an unmodified omp path would FAIL EVERY TURN. B7 must either (a) `_normalize_omp` yields a synthetic marker identity + skips session-file persistence for stateless omp, or (b) bypass the identity assert for omp.

## U10 — RPC fanout parity (RESOLVED: 1:1 confirmed)
omp RPC fanout CAN mirror hermes exactly: one RpcClient = one 'bun cli' child with configurable shared cwd (rpc-client.ts:302-305); worktree is OPT-IN — ExecutorOptions.worktree optional, effectiveCwd = worktree ?? cwd (executor.ts:2913); TaskIsolationMode 'none' → no isolation (worktree.ts:345-359); isolation gated on request.isolation?.requested === true (structured-subagent.ts:296). Hermes mechanism: mp spawn children share parent cwd, no worktrees (runtime/batch.py:510-513); cancellation pre-launch only + wall-timeout SIGTERM→SIGKILL + _cleanup_active. Parity fixtures: success (N concurrent, submission order, cost aggregation), timeout (per-unit deadline, sentinel, siblings continue), cancel (pre-launch + mid-flight — omp must add mid-flight), partial (one unit errors → sentinel), child-cleanup (no orphaned 'bun cli').

## U11 — fork-clean repro (RESOLVED: diff is minimal + deterministic)
Dirty diff: 1 tracked modified (src/task/agents.ts, +2 lines: residentMd import + EMBEDDED_AGENT_DEFS entry), 3 untracked (docs/agents.md, scripts/agent, src/prompts/agents/resident.md). discovery.ts CLEAN. HEAD bundled set = scout/designer/reviewer/security-reviewer/librarian/task/sonic (no resident). Revert = delete untracked resident.md + revert 2 lines. Deterministic from empty HOME provided ~/.omp/agent/agents + .prompts + .omp/agents also empty; env overrides PI_CODING_AGENT_DIR/PI_CONFIG_DIR. Caches that can mask: on-disk unpacked agents + .prompts cache + in-memory bundledAgentsCache (per-process, clean on fresh build).

## U12 — cloud census (RESOLVED: 5 EXTRA live surfaces beyond B9)
FIVE additional live migrate surfaces not enumerated in the plan's B9 list: (1) arnold-repair-loop (L464 HERMES_LAUNCHER default; L8364 spawns launcher; L8644-45 dispatch_kind=hermes; L8778 run_agent; L9560 DEV_DISPATCH_KIND=hermes); (2) arnold-kimi-goal-operator (L169-170 probes /root/.hermes/skills/subagent-launcher/SKILL.md; L291 codex exec; L339 run_agent); (3) arnold-meta-repair-loop (L109 LAUNCHER default; L1332-1419 run_direct_hermes_fallback; 14 hermes refs); (4) arnold-progress-auditor (L165 LAUNCHER default; L6453 codex exec); (5) arnold-resident-schedule-run-once-r7 (seeds+guards /root/.hermes/.env). Plus: cloud.yaml.tmpl (auth-seed doc), simulate_watchdog_end_to_end.py (test fixture), arnold-watchdog L1563 --model hermes:deepseek spec + L2136 syncs $HOME/.hermes/skills + classifiers L6168-6241/6357-6420 must learn omp argv. Clean: arnold-supervisor-runtime-lib, arnold-supervise, arnold-supervisor-runtime, arnold-supervisor-gap-scan, arnold-repair-trigger, arnold-promote (no refs).

## U13 — Astrid surface (RESOLVED: real toolkit, fully specifiable)
Astrid = Python toolkit at ~/Documents/reigh-workspace/Astrid. CLI gateway: console script `astrid` → astrid.core.gateway:main; `astrid next` prints exactly ONE legal action (bootstrap | run: cmd | ack …) — the universal port-of-call; `--engine arnold` supported (Arnold StepInvocationAdapter exists: astrid/core/integrations/arnold/). ~22 packs (fal h3_video/fal_foley, understanding video/visual/audio, generation generate_video/audio/image, video_editing hype/vary_grid/animate_image/cut, editorial transcribe/refine/validate/human_review, iteration prepare/assemble/experiment_*, training dataset_build/training_run, rendering render/timeline_storyboard/sprite_sheet, vibecomfy, comfy_wrap, runpod provision/exec/session/teardown, moirae, youtube, reigh publish/data, foley, stream_content distill). Credentials: repo-root .env/.env.local provider keys (OPENAI/GEMINI/ANTHROPIC/RUNPOD_API_KEY, HF_TOKEN, REPLICATE_API_TOKEN) + ASTRID_BANODOCO_*; identity = session/file-bound (attach --as agent:<id>, writer_epoch lease for takeover). Edit surface = projects/<slug>/ + runs/<slug>/; outputs = file paths + typed media (video/mp4, audio/wav, x-astrid-timeline). astrid-resident agent: prompt = attach project, loop `astrid next` and execute exactly what it prints (never freelance), re-orient via `astrid status`, ack --decision approve human gates, sessions takeover on lease conflict; tools = astrid gateway + file tools over run-dir; evidence = typed media + MediaUsage cost (AR3).
