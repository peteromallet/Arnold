<!--
This file is generated by scripts/render_package_disposition_md.py.
Do not edit by hand.  The YAML manifest is authoritative.
Run `python scripts/render_package_disposition_md.py --write` to regenerate.
-->

# Package Disposition Manifest

> **Source of truth:** `docs/arnold/package-disposition.yaml`
> **Validator:** `scripts/validate_package_disposition.py`
> **Renderer:** `scripts/render_package_disposition_md.py`


## Overview

**Schema version:** 1
**Disposition rows:** 275
**Exclusions:** 4
**Parity gates:** 0
**Runtime settings gates:** 0

### Disposition counts

- arnold-core: 102
- arnold-service-interface: 1
- arnold-adapter: 3
- arnold-shared-leaf: 25
- megaplan-plugin: 108
- product-app: 0
- legacy-hold: 18
- delete-merge: 0
- split-required: 18

### Granularity counts

- directory: 29
- file: 158
- symbol: 70
- split: 18

## Coverage Exclusions


Exclusions are limited to generated/cache artifacts. 4 exclusion(s) defined.

### Exclusion 1: `arnold/pipeline/*.py`

- **Reason:** Legacy neutral pipeline surfaces retained during M5 migration; disposition rows will be completed in M6.
- **Evidence:** m5-pipeline-disposition.md

### Exclusion 2: `arnold/pipeline/**/*.py`

- **Reason:** Legacy neutral pipeline surfaces retained during M5 migration; disposition rows will be completed in M6.
- **Evidence:** m5-pipeline-disposition.md

### Exclusion 3: `arnold_pipelines/megaplan/*.py`

- **Reason:** Megaplan plugin surfaces retained during M5 migration; disposition rows will be completed in M6.
- **Evidence:** m5-pipeline-disposition.md

### Exclusion 4: `arnold_pipelines/megaplan/**/*.py`

- **Reason:** Megaplan plugin surfaces retained during M5 migration; disposition rows will be completed in M6.
- **Evidence:** m5-pipeline-disposition.md

## Disposition Rows by Classification

### arnold-core (60 rows)

#### megaplan/_pipeline/discovery/trust.py
**Disposition:** `arnold-core` | **Granularity:** `file` | **Target:** arnold/pipeline/discovery/trust.py

**Reason:** Trust-tier classification and capability allowlisting are SDK/runtime policy for pipeline discovery, not Megaplan plugin logic.

**Blockers:**
- In-tree path fragments are still hardcoded to `megaplan/pipelines`.
- Capability allowlist literals still reflect current Megaplan-facing phase names.

**Import policy:**
- Forbidden: `megaplan.handlers`, `megaplan.execute`, `megaplan.finalize`

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| trust_tiers | arnold | ['AUTO_EXEC', 'QUARANTINED', 'BLESSED'] |

**String policy:**
| Category | Owner | Policy |
| --- | --- | --- |
| discovery_paths | megaplan | Rename in-tree path fragments and capability names before final Arnold extraction. |

**Extraction prerequisites:**
- Replace `megaplan`-specific path fragments with Arnold package roots.
- Confirm public capability names at the Arnold service boundary.

**First extraction unit:** `arnold/pipeline/discovery/trust.py`

**Tests / gates:**
- static-gate: arnold/pipeline/discovery/trust.py must stay free of planning handler imports

---

#### megaplan/_core/scheduler
**Disposition:** `arnold-core` | **Granularity:** `directory` | **Target:** arnold/core/scheduler

**Reason:** The `_core/scheduler/**` subtree is a generic task-DAG scheduler that stays intentionally free of classification, tier-routing, and handler-specific behavior.

**Blockers:**
- `topo.py` still relies on `_core.io.compute_task_batches` and `split_oversized_batches`.
- `run.py` carries an inline temporary `ProcessDriver` protocol until a neutral export lands.

**Import policy:**
- Forbidden: `megaplan.execute`, `megaplan.handlers`, `megaplan.finalize`

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| scheduler_primitives | arnold | ['schedule_batches', 'run_scheduler', 'Reduce', 'ProcessDriver'] |

**Extraction prerequisites:**
- Extract topo-sort helpers from `_core.io` into the Arnold-owned scheduler package.
- Export a neutral `ProcessDriver` protocol from Arnold-owned interfaces.

**First extraction unit:** `arnold/core/scheduler`

**Tests / gates:**
- static-gate: arnold/core/scheduler/** must not import execute/handlers subpackages

---

#### megaplan/store/warrant.py
**Disposition:** `arnold-core` | **Granularity:** `file` | **Target:** arnold/trust/warrant.py

**Reason:** `store/warrant.py` is a reusable Warrant signing and verification primitive over canonical projections and typed warrant schemas.

**Blockers:**
- Default key resolution still flows through `ConfigResolver().effective('signing', 'warrant_key')`.
- The warrant and source-projection models still live under `megaplan.schemas`.

**Import policy:**
- Allowed: `megaplan._core.canonical`, `megaplan._core.config_resolver`, `megaplan.schemas`
- Forbidden: `megaplan.handlers`, `megaplan.cli`

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| warrant_primitives | arnold | ['WarrantError', 'WarrantBuildResult', 'build_warrant', 'verify_warrant', 'warrant_signed_envelope'] |

**String policy:**
| Category | Owner | Policy |
| --- | --- | --- |
| config_keys | megaplan | Re-home the current warrant signing key lookup behind Arnold-neutral config namespaces before extraction. |

**Extraction prerequisites:**
- Re-home warrant schemas and signing-key config lookup behind Arnold-neutral packages.

**First extraction unit:** `arnold/trust/warrant.py`

**Tests / gates:**
- static-gate: arnold/trust/warrant.py must stay limited to canonical signing/verification over typed warrant schemas

**Configurable seams:**
| Declaration Site | Inherits Via | Override Mechanism | Meaning Owner | Supported State |
| --- | --- | --- | --- | --- |
| megaplan/store/warrant.py::_resolve_warrant_key | ConfigResolver.effective lookup when the caller does not pass a key explicitly | callers may inject an explicit warrant_key or a resolver implementation | arnold | supported once the config namespace is neutralized |

---

#### megaplan/schemas/__init__.py
**Disposition:** `arnold-core` | **Granularity:** `file` | **Target:** arnold/schemas/__init__.py

**Reason:** Re-exports Arnold-owned storage models (from .models) and runtime JSON schemas (from .runtime). While runtime.py contains Megaplan-specific schemas, the __init__.py itself is a neutral re-export facade. Classified as arnold-core because its primary export surface is the Arnold storage model (AutomationActor, Epic, Sprint, Plan, Warrant, etc.).

**Blockers:**
- Re-exports from .runtime include Megaplan-specific schemas alongside generic strict_schema.

---

#### megaplan/schemas/base.py
**Disposition:** `arnold-core` | **Granularity:** `file` | **Target:** arnold/schemas/base.py

**Reason:** Pydantic base types and shared helpers (StorageModel, NormalizedDict, NormalizedList, Backend literal, utc_now). Pure infrastructure with zero Megaplan imports or vocabulary. These base types underpin all Arnold storage models.

**Blockers:**
- _(none)_


**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| schema-base | arnold | ['StorageModel', 'NormalizedDict', 'NormalizedList', 'Backend', 'utc_now'] |

---

#### megaplan/schemas/arnold.py
**Disposition:** `arnold-core` | **Granularity:** `file` | **Target:** arnold/schemas/arnold.py

**Reason:** "Pydantic mirrors of the Arnold Supabase tables" per docstring. Defines Epic, Sprint, Plan, Warrant, Ticket, CodeArtifact, BotTurn, ChecklistItem, ExternalRequest, and related storage models. References only .base (arnold-core). No Megaplan-specific imports or vocabulary beyond the ARNOLD_TO_MEGAPLAN_EPIC_STATE transitional mapping.

**Blockers:**
- ARNOLD_TO_MEGAPLAN_EPIC_STATE mapping is a transitional adapter for Arnold→Megaplan state compatibility; should be split out before clean extraction.

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| arnold-storage | arnold | ['Epic', 'Sprint', 'Plan', 'Warrant', 'Ticket', 'CodeArtifact', 'BotTurn', 'ChecklistItem', 'ExternalRequest', 'PlanArtifact', 'Capsule', 'CapsuleContract', 'CapsuleDefinition', 'CapsuleEvidence', 'CapsuleLineage', 'EpicEvent', 'EpicLock', 'EpicSummary', 'EpicSnapshot', 'Feedback', 'Image', 'Message', 'ResidentConversation', 'SecondOpinion', 'SprintItem', 'SystemLog', 'MigrationRun'] |

**Extraction prerequisites:**
- Extract ARNOLD_TO_MEGAPLAN_EPIC_STATE transitional mapping to an adapter layer before clean extraction.

---

#### megaplan/schemas/models.py
**Disposition:** `arnold-core` | **Granularity:** `file` | **Target:** arnold/schemas/models.py

**Reason:** Aggregated Sprint 1 storage model exports. Re-exports all Arnold storage models from arnold.py plus automation actor, warrant, and ticket models. Zero Megaplan imports — purely Arnold-owned model aggregation.

**Blockers:**
- _(none)_


**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| arnold-models | arnold | ['AutomationActor', 'CloudRun', 'ExecutionLease', 'WarrantAccount', 'WarrantAuthority', 'WarrantRationaleAnchor', 'WarrantSignature', 'WarrantSourceCompleteness', 'WarrantSourceProjection', 'ToolCall', 'ControlMessage', 'PlanConfig', 'PlanMeta', 'PlanState'] |

---

#### megaplan/_core/__init__.py
**Disposition:** `arnold-core` | **Granularity:** `file` | **Target:** arnold/core/__init__.py

**Reason:** Core infrastructure module with some Megaplan coupling; targeted for extraction as Arnold shared substrate.

**Blockers:**
- Imports from megaplan.types, megaplan.schemas, or megaplan._pipeline.
- May reference Megaplan-specific state keys, agent constants, or workflow vocabulary.

**Extraction prerequisites:**
- Audit and decouple Megaplan-specific imports before extraction.

**First extraction unit:** `arnold/core/__init__.py`

---

#### megaplan/_core/activation.py
**Disposition:** `arnold-core` | **Granularity:** `file` | **Target:** arnold/core/activation.py

**Reason:** Core infrastructure module with some Megaplan coupling; targeted for extraction as Arnold shared substrate.

**Blockers:**
- Imports from megaplan.types, megaplan.schemas, or megaplan._pipeline.
- May reference Megaplan-specific state keys, agent constants, or workflow vocabulary.

**Extraction prerequisites:**
- Audit and decouple Megaplan-specific imports before extraction.

**First extraction unit:** `arnold/core/activation.py`

---

#### megaplan/_core/canonical.py
**Disposition:** `arnold-core` | **Granularity:** `file` | **Target:** arnold/core/canonical.py

**Reason:** Core infrastructure module with some Megaplan coupling; targeted for extraction as Arnold shared substrate.

**Blockers:**
- Imports from megaplan.types, megaplan.schemas, or megaplan._pipeline.
- May reference Megaplan-specific state keys, agent constants, or workflow vocabulary.

**Extraction prerequisites:**
- Audit and decouple Megaplan-specific imports before extraction.

**First extraction unit:** `arnold/core/canonical.py`

---

#### megaplan/_core/config_resolver.py
**Disposition:** `arnold-core` | **Granularity:** `file` | **Target:** arnold/core/config_resolver.py

**Reason:** Core infrastructure module with some Megaplan coupling; targeted for extraction as Arnold shared substrate.

**Blockers:**
- Imports from megaplan.types, megaplan.schemas, or megaplan._pipeline.
- May reference Megaplan-specific state keys, agent constants, or workflow vocabulary.

**Extraction prerequisites:**
- Audit and decouple Megaplan-specific imports before extraction.

**First extraction unit:** `arnold/core/config_resolver.py`

---

#### megaplan/_core/dispatch.py
**Disposition:** `arnold-core` | **Granularity:** `file` | **Target:** arnold/core/dispatch.py

**Reason:** Core infrastructure module with some Megaplan coupling; targeted for extraction as Arnold shared substrate.

**Blockers:**
- Imports from megaplan.types, megaplan.schemas, or megaplan._pipeline.
- May reference Megaplan-specific state keys, agent constants, or workflow vocabulary.

**Extraction prerequisites:**
- Audit and decouple Megaplan-specific imports before extraction.

**First extraction unit:** `arnold/core/dispatch.py`

---

#### megaplan/_core/hermes_fanout.py
**Disposition:** `arnold-core` | **Granularity:** `file` | **Target:** arnold/core/hermes_fanout.py

**Reason:** Core infrastructure module with some Megaplan coupling; targeted for extraction as Arnold shared substrate.

**Blockers:**
- Imports from megaplan.types, megaplan.schemas, or megaplan._pipeline.
- May reference Megaplan-specific state keys, agent constants, or workflow vocabulary.

**Extraction prerequisites:**
- Audit and decouple Megaplan-specific imports before extraction.

**First extraction unit:** `arnold/core/hermes_fanout.py`

---

#### megaplan/_core/io.py
**Disposition:** `arnold-core` | **Granularity:** `file` | **Target:** arnold/core/io.py

**Reason:** Core infrastructure module with some Megaplan coupling; targeted for extraction as Arnold shared substrate.

**Blockers:**
- Imports from megaplan.types, megaplan.schemas, or megaplan._pipeline.
- May reference Megaplan-specific state keys, agent constants, or workflow vocabulary.

**Extraction prerequisites:**
- Audit and decouple Megaplan-specific imports before extraction.

**First extraction unit:** `arnold/core/io.py`

---

#### megaplan/_core/modes.py
**Disposition:** `arnold-core` | **Granularity:** `file` | **Target:** arnold/core/modes.py

**Reason:** Core infrastructure module with some Megaplan coupling; targeted for extraction as Arnold shared substrate.

**Blockers:**
- Imports from megaplan.types, megaplan.schemas, or megaplan._pipeline.
- May reference Megaplan-specific state keys, agent constants, or workflow vocabulary.

**Extraction prerequisites:**
- Audit and decouple Megaplan-specific imports before extraction.

**First extraction unit:** `arnold/core/modes.py`

---

#### megaplan/_core/phase_runtime.py
**Disposition:** `arnold-core` | **Granularity:** `file` | **Target:** arnold/core/phase_runtime.py

**Reason:** Core infrastructure module with some Megaplan coupling; targeted for extraction as Arnold shared substrate.

**Blockers:**
- Imports from megaplan.types, megaplan.schemas, or megaplan._pipeline.
- May reference Megaplan-specific state keys, agent constants, or workflow vocabulary.

**Extraction prerequisites:**
- Audit and decouple Megaplan-specific imports before extraction.

**First extraction unit:** `arnold/core/phase_runtime.py`

---

#### megaplan/_core/process_fanout.py
**Disposition:** `arnold-core` | **Granularity:** `file` | **Target:** arnold/core/process_fanout.py

**Reason:** Core infrastructure module with some Megaplan coupling; targeted for extraction as Arnold shared substrate.

**Blockers:**
- Imports from megaplan.types, megaplan.schemas, or megaplan._pipeline.
- May reference Megaplan-specific state keys, agent constants, or workflow vocabulary.

**Extraction prerequisites:**
- Audit and decouple Megaplan-specific imports before extraction.

**First extraction unit:** `arnold/core/process_fanout.py`

---

#### megaplan/_core/registries.py
**Disposition:** `arnold-core` | **Granularity:** `file` | **Target:** arnold/core/registries.py

**Reason:** Core infrastructure module with some Megaplan coupling; targeted for extraction as Arnold shared substrate.

**Blockers:**
- Imports from megaplan.types, megaplan.schemas, or megaplan._pipeline.
- May reference Megaplan-specific state keys, agent constants, or workflow vocabulary.

**Extraction prerequisites:**
- Audit and decouple Megaplan-specific imports before extraction.

**First extraction unit:** `arnold/core/registries.py`

---

#### megaplan/_core/state.py
**Disposition:** `arnold-core` | **Granularity:** `file` | **Target:** arnold/core/state.py

**Reason:** Core infrastructure module with some Megaplan coupling; targeted for extraction as Arnold shared substrate.

**Blockers:**
- Imports from megaplan.types, megaplan.schemas, or megaplan._pipeline.
- May reference Megaplan-specific state keys, agent constants, or workflow vocabulary.

**Extraction prerequisites:**
- Audit and decouple Megaplan-specific imports before extraction.

**First extraction unit:** `arnold/core/state.py`

---

#### megaplan/_core/state_store.py
**Disposition:** `arnold-core` | **Granularity:** `file` | **Target:** arnold/core/state_store.py

**Reason:** Core infrastructure module with some Megaplan coupling; targeted for extraction as Arnold shared substrate.

**Blockers:**
- Imports from megaplan.types, megaplan.schemas, or megaplan._pipeline.
- May reference Megaplan-specific state keys, agent constants, or workflow vocabulary.

**Extraction prerequisites:**
- Audit and decouple Megaplan-specific imports before extraction.

**First extraction unit:** `arnold/core/state_store.py`

---

#### megaplan/_core/topology.py
**Disposition:** `arnold-core` | **Granularity:** `file` | **Target:** arnold/core/topology.py

**Reason:** Core infrastructure module with some Megaplan coupling; targeted for extraction as Arnold shared substrate.

**Blockers:**
- Imports from megaplan.types, megaplan.schemas, or megaplan._pipeline.
- May reference Megaplan-specific state keys, agent constants, or workflow vocabulary.

**Extraction prerequisites:**
- Audit and decouple Megaplan-specific imports before extraction.

**First extraction unit:** `arnold/core/topology.py`

---

#### megaplan/_core/user_config.py
**Disposition:** `arnold-core` | **Granularity:** `file` | **Target:** arnold/core/user_config.py

**Reason:** Core infrastructure module with some Megaplan coupling; targeted for extraction as Arnold shared substrate.

**Blockers:**
- Imports from megaplan.types, megaplan.schemas, or megaplan._pipeline.
- May reference Megaplan-specific state keys, agent constants, or workflow vocabulary.

**Extraction prerequisites:**
- Audit and decouple Megaplan-specific imports before extraction.

**First extraction unit:** `arnold/core/user_config.py`

---

#### megaplan/_core/worker_fanout.py
**Disposition:** `arnold-core` | **Granularity:** `file` | **Target:** arnold/core/worker_fanout.py

**Reason:** Core infrastructure module with some Megaplan coupling; targeted for extraction as Arnold shared substrate.

**Blockers:**
- Imports from megaplan.types, megaplan.schemas, or megaplan._pipeline.
- May reference Megaplan-specific state keys, agent constants, or workflow vocabulary.

**Extraction prerequisites:**
- Audit and decouple Megaplan-specific imports before extraction.

**First extraction unit:** `arnold/core/worker_fanout.py`

---

#### megaplan/_core/workflow_data.py
**Disposition:** `arnold-core` | **Granularity:** `file` | **Target:** arnold/core/workflow_data.py

**Reason:** Core infrastructure module with some Megaplan coupling; targeted for extraction as Arnold shared substrate.

**Blockers:**
- Imports from megaplan.types, megaplan.schemas, or megaplan._pipeline.
- May reference Megaplan-specific state keys, agent constants, or workflow vocabulary.

**Extraction prerequisites:**
- Audit and decouple Megaplan-specific imports before extraction.

**First extraction unit:** `arnold/core/workflow_data.py`

---

#### megaplan/_pipeline/__init__.py
**Disposition:** `arnold-core` | **Granularity:** `file` | **Target:** arnold/pipeline/__init__.py

**Reason:** Pipeline runtime primitive — generic dispatch, execution, pattern, or validation infrastructure.

**Blockers:**
- May reference Megaplan phase strings or handler routing.
- May import from megaplan.types or megaplan.schemas for type contracts.

**Extraction prerequisites:**
- Audit for Megaplan-specific string literals and type references.

**First extraction unit:** `arnold/pipeline/__init__.py`

---

#### megaplan/_pipeline/artifacts.py
**Disposition:** `arnold-core` | **Granularity:** `file` | **Target:** arnold/pipeline/artifacts.py

**Reason:** Entirely neutral versioned-artifact path helpers — artifact_dir, latest_version, next_version_path, versioned_artifacts, latest_artifact_path. Only import is StepContext from types. No Megaplan phase vocabulary, handler references, or branded paths. Previously misclassified as megaplan-plugin.

**Blockers:**
- None — the file is already fully neutral.

**First extraction unit:** `arnold/pipeline/artifacts.py`

---

#### megaplan/_pipeline/demos/__init__.py
**Disposition:** `arnold-core` | **Granularity:** `file` | **Target:** arnold/pipeline/demos/__init__.py

**Reason:** Pipeline runtime primitive — generic dispatch, execution, pattern, or validation infrastructure.

**Blockers:**
- May reference Megaplan phase strings or handler routing.
- May import from megaplan.types or megaplan.schemas for type contracts.

**Extraction prerequisites:**
- Audit for Megaplan-specific string literals and type references.

**First extraction unit:** `arnold/pipeline/demos/__init__.py`

---

#### megaplan/_pipeline/discovery/__init__.py
**Disposition:** `arnold-core` | **Granularity:** `file` | **Target:** arnold/pipeline/discovery/__init__.py

**Reason:** Pipeline runtime primitive — generic dispatch, execution, pattern, or validation infrastructure.

**Blockers:**
- May reference Megaplan phase strings or handler routing.
- May import from megaplan.types or megaplan.schemas for type contracts.

**Extraction prerequisites:**
- Audit for Megaplan-specific string literals and type references.

**First extraction unit:** `arnold/pipeline/discovery/__init__.py`

---

#### megaplan/_pipeline/dispatch.py
**Disposition:** `arnold-core` | **Granularity:** `file` | **Target:** arnold/pipeline/dispatch.py

**Reason:** Pipeline runtime primitive — generic dispatch, execution, pattern, or validation infrastructure.

**Blockers:**
- May reference Megaplan phase strings or handler routing.
- May import from megaplan.types or megaplan.schemas for type contracts.

**Extraction prerequisites:**
- Audit for Megaplan-specific string literals and type references.

**First extraction unit:** `arnold/pipeline/dispatch.py`

---

#### megaplan/_pipeline/dispatch_async.py
**Disposition:** `arnold-core` | **Granularity:** `file` | **Target:** arnold/pipeline/dispatch_async.py

**Reason:** Pipeline runtime primitive — generic dispatch, execution, pattern, or validation infrastructure.

**Blockers:**
- May reference Megaplan phase strings or handler routing.
- May import from megaplan.types or megaplan.schemas for type contracts.

**Extraction prerequisites:**
- Audit for Megaplan-specific string literals and type references.

**First extraction unit:** `arnold/pipeline/dispatch_async.py`

---

#### megaplan/_pipeline/dispatch_subprocess.py
**Disposition:** `arnold-core` | **Granularity:** `file` | **Target:** arnold/pipeline/dispatch_subprocess.py

**Reason:** Pipeline runtime primitive — generic dispatch, execution, pattern, or validation infrastructure.

**Blockers:**
- May reference Megaplan phase strings or handler routing.
- May import from megaplan.types or megaplan.schemas for type contracts.

**Extraction prerequisites:**
- Audit for Megaplan-specific string literals and type references.

**First extraction unit:** `arnold/pipeline/dispatch_subprocess.py`

---

#### megaplan/_pipeline/faults.py
**Disposition:** `arnold-core` | **Granularity:** `file` | **Target:** arnold/pipeline/faults.py

**Reason:** Pipeline runtime primitive — generic dispatch, execution, pattern, or validation infrastructure.

**Blockers:**
- May reference Megaplan phase strings or handler routing.
- May import from megaplan.types or megaplan.schemas for type contracts.

**Extraction prerequisites:**
- Audit for Megaplan-specific string literals and type references.

**First extraction unit:** `arnold/pipeline/faults.py`

---

#### megaplan/_pipeline/feature_flags.py
**Disposition:** `arnold-core` | **Granularity:** `file` | **Target:** arnold/pipeline/feature_flags.py

**Reason:** Pipeline runtime primitive — generic dispatch, execution, pattern, or validation infrastructure.

**Blockers:**
- May reference Megaplan phase strings or handler routing.
- May import from megaplan.types or megaplan.schemas for type contracts.

**Extraction prerequisites:**
- Audit for Megaplan-specific string literals and type references.

**First extraction unit:** `arnold/pipeline/feature_flags.py`

---

#### megaplan/_pipeline/flags.py
**Disposition:** `arnold-core` | **Granularity:** `file` | **Target:** arnold/pipeline/flags.py

**Reason:** Pipeline runtime primitive — generic dispatch, execution, pattern, or validation infrastructure.

**Blockers:**
- May reference Megaplan phase strings or handler routing.
- May import from megaplan.types or megaplan.schemas for type contracts.

**Extraction prerequisites:**
- Audit for Megaplan-specific string literals and type references.

**First extraction unit:** `arnold/pipeline/flags.py`

---

#### megaplan/_pipeline/loop_node.py
**Disposition:** `arnold-core` | **Granularity:** `file` | **Target:** arnold/pipeline/loop_node.py

**Reason:** Pipeline runtime primitive — generic dispatch, execution, pattern, or validation infrastructure.

**Blockers:**
- May reference Megaplan phase strings or handler routing.
- May import from megaplan.types or megaplan.schemas for type contracts.

**Extraction prerequisites:**
- Audit for Megaplan-specific string literals and type references.

**First extraction unit:** `arnold/pipeline/loop_node.py`

---

#### megaplan/_pipeline/pattern_dynamic.py
**Disposition:** `arnold-core` | **Granularity:** `file` | **Target:** arnold/pipeline/pattern_dynamic.py

**Reason:** Pipeline runtime primitive — generic dispatch, execution, pattern, or validation infrastructure.

**Blockers:**
- May reference Megaplan phase strings or handler routing.
- May import from megaplan.types or megaplan.schemas for type contracts.

**Extraction prerequisites:**
- Audit for Megaplan-specific string literals and type references.

**First extraction unit:** `arnold/pipeline/pattern_dynamic.py`

---

#### megaplan/_pipeline/pattern_joins.py
**Disposition:** `arnold-core` | **Granularity:** `file` | **Target:** arnold/pipeline/pattern_joins.py

**Reason:** Pipeline runtime primitive — generic dispatch, execution, pattern, or validation infrastructure.

**Blockers:**
- May reference Megaplan phase strings or handler routing.
- May import from megaplan.types or megaplan.schemas for type contracts.

**Extraction prerequisites:**
- Audit for Megaplan-specific string literals and type references.

**First extraction unit:** `arnold/pipeline/pattern_joins.py`

---

#### megaplan/_pipeline/pattern_select.py
**Disposition:** `arnold-core` | **Granularity:** `file` | **Target:** arnold/pipeline/pattern_select.py

**Reason:** Pipeline runtime primitive — generic dispatch, execution, pattern, or validation infrastructure.

**Blockers:**
- May reference Megaplan phase strings or handler routing.
- May import from megaplan.types or megaplan.schemas for type contracts.

**Extraction prerequisites:**
- Audit for Megaplan-specific string literals and type references.

**First extraction unit:** `arnold/pipeline/pattern_select.py`

---

#### megaplan/_pipeline/pattern_stops.py
**Disposition:** `arnold-core` | **Granularity:** `file` | **Target:** arnold/pipeline/pattern_stops.py

**Reason:** Pipeline runtime primitive — generic dispatch, execution, pattern, or validation infrastructure.

**Blockers:**
- May reference Megaplan phase strings or handler routing.
- May import from megaplan.types or megaplan.schemas for type contracts.

**Extraction prerequisites:**
- Audit for Megaplan-specific string literals and type references.

**First extraction unit:** `arnold/pipeline/pattern_stops.py`

---

#### megaplan/_pipeline/pattern_types.py
**Disposition:** `arnold-core` | **Granularity:** `file` | **Target:** arnold/pipeline/pattern_types.py

**Reason:** Pipeline runtime primitive — generic dispatch, execution, pattern, or validation infrastructure.

**Blockers:**
- May reference Megaplan phase strings or handler routing.
- May import from megaplan.types or megaplan.schemas for type contracts.

**Extraction prerequisites:**
- Audit for Megaplan-specific string literals and type references.

**First extraction unit:** `arnold/pipeline/pattern_types.py`

---

#### megaplan/_pipeline/patterns.py
**Disposition:** `arnold-core` | **Granularity:** `file` | **Target:** arnold/pipeline/patterns.py

**Reason:** Pipeline runtime primitive — generic dispatch, execution, pattern, or validation infrastructure.

**Blockers:**
- May reference Megaplan phase strings or handler routing.
- May import from megaplan.types or megaplan.schemas for type contracts.

**Extraction prerequisites:**
- Audit for Megaplan-specific string literals and type references.

**First extraction unit:** `arnold/pipeline/patterns.py`

---

#### megaplan/_pipeline/receipt.py
**Disposition:** `arnold-core` | **Granularity:** `file` | **Target:** arnold/pipeline/receipt.py

**Reason:** Pipeline runtime primitive — generic dispatch, execution, pattern, or validation infrastructure.

**Blockers:**
- May reference Megaplan phase strings or handler routing.
- May import from megaplan.types or megaplan.schemas for type contracts.

**Extraction prerequisites:**
- Audit for Megaplan-specific string literals and type references.

**First extraction unit:** `arnold/pipeline/receipt.py`

---

#### megaplan/_pipeline/stages/__init__.py
**Disposition:** `arnold-core` | **Granularity:** `file` | **Target:** arnold/pipeline/stages/__init__.py

**Reason:** Pipeline runtime primitive — generic dispatch, execution, pattern, or validation infrastructure.

**Blockers:**
- May reference Megaplan phase strings or handler routing.
- May import from megaplan.types or megaplan.schemas for type contracts.

**Extraction prerequisites:**
- Audit for Megaplan-specific string literals and type references.

**First extraction unit:** `arnold/pipeline/stages/__init__.py`

---

#### megaplan/_pipeline/step_helpers.py
**Disposition:** `arnold-core` | **Granularity:** `file` | **Target:** arnold/pipeline/step_helpers.py

**Reason:** Pipeline runtime primitive — generic dispatch, execution, pattern, or validation infrastructure.

**Blockers:**
- May reference Megaplan phase strings or handler routing.
- May import from megaplan.types or megaplan.schemas for type contracts.

**Extraction prerequisites:**
- Audit for Megaplan-specific string literals and type references.

**First extraction unit:** `arnold/pipeline/step_helpers.py`

---

#### megaplan/_pipeline/steps/__init__.py
**Disposition:** `arnold-core` | **Granularity:** `file` | **Target:** arnold/pipeline/steps/__init__.py

**Reason:** Pipeline runtime primitive — generic dispatch, execution, pattern, or validation infrastructure.

**Blockers:**
- May reference Megaplan phase strings or handler routing.
- May import from megaplan.types or megaplan.schemas for type contracts.

**Extraction prerequisites:**
- Audit for Megaplan-specific string literals and type references.

**First extraction unit:** `arnold/pipeline/steps/__init__.py`

---

#### megaplan/_pipeline/subloop.py
**Disposition:** `arnold-core` | **Granularity:** `file` | **Target:** arnold/pipeline/subloop.py

**Reason:** Pipeline runtime primitive — generic dispatch, execution, pattern, or validation infrastructure.

**Blockers:**
- May reference Megaplan phase strings or handler routing.
- May import from megaplan.types or megaplan.schemas for type contracts.

**Extraction prerequisites:**
- Audit for Megaplan-specific string literals and type references.

**First extraction unit:** `arnold/pipeline/subloop.py`

---

#### megaplan/_pipeline/taint.py
**Disposition:** `arnold-core` | **Granularity:** `file` | **Target:** arnold/pipeline/taint.py

**Reason:** Pipeline runtime primitive — generic dispatch, execution, pattern, or validation infrastructure.

**Blockers:**
- May reference Megaplan phase strings or handler routing.
- May import from megaplan.types or megaplan.schemas for type contracts.

**Extraction prerequisites:**
- Audit for Megaplan-specific string literals and type references.

**First extraction unit:** `arnold/pipeline/taint.py`

---

#### megaplan/_pipeline/validator.py
**Disposition:** `arnold-core` | **Granularity:** `file` | **Target:** arnold/pipeline/validator.py

**Reason:** Pipeline runtime primitive — generic dispatch, execution, pattern, or validation infrastructure.

**Blockers:**
- May reference Megaplan phase strings or handler routing.
- May import from megaplan.types or megaplan.schemas for type contracts.

**Extraction prerequisites:**
- Audit for Megaplan-specific string literals and type references.

**First extraction unit:** `arnold/pipeline/validator.py`

---

#### arnold/pipeline/__init__.py
**Disposition:** `arnold-core` | **Granularity:** `file` | **Target:** arnold/pipeline/__init__.py

**Reason:** Arnold pipeline package init — re-exports neutral pipeline primitives.  Already created in M0; no Megaplan phase vocabulary or handler references.

**Blockers:**
- _(none)_


**Import policy:**
- Forbidden: `megaplan`

---

#### arnold/pipeline/executor.py
**Disposition:** `arnold-core` | **Granularity:** `file` | **Target:** arnold/pipeline/executor.py

**Reason:** Arnold pipeline executor — neutral stage-walk skeleton, atomic I/O helpers, and output verification extracted from megaplan/_pipeline/executor.py.  Already created in M0.  Megaplan policy (governor, activation, evaluand, port-binding) remains in the Megaplan adapter.

**Blockers:**
- _(none)_


**Import policy:**
- Forbidden: `megaplan.runtime.governor`, `megaplan._core.activation`, `megaplan.observability.events`

---

#### arnold/pipeline/state.py
**Disposition:** `arnold-core` | **Granularity:** `file` | **Target:** arnold/pipeline/state.py

**Reason:** Arnold pipeline state primitives — neutral state management extracted from megaplan/_pipeline state helpers.  Already created in M0.

**Blockers:**
- _(none)_


**Import policy:**
- Forbidden: `megaplan`

---

#### arnold/pipeline/types.py
**Disposition:** `arnold-core` | **Granularity:** `file` | **Target:** arnold/pipeline/types.py

**Reason:** Arnold pipeline types — neutral typed-port carriers (Port, PortRef, RoutingKey, ContentTypeRegistry, ReduceResult, SelectionResult, StateDelta) extracted from megaplan/_pipeline/types.py.  Already created in M0.  Excludes Megaplan policy types (GateRecommendation, OverrideAction, Pipeline.run_phase).

**Blockers:**
- _(none)_


**Import policy:**
- Forbidden: `megaplan.cli.feedback`, `megaplan._core`, `megaplan.types`

**Tests / gates:**
- static-gate: arnold/pipeline/types.py must not import megaplan._core, megaplan.cli.feedback, or megaplan.types
- static-gate: arnold/pipeline/types.py must not contain feedback|planning|confirm_self_review literals

---

#### arnold/pipeline/resources.py
**Disposition:** `arnold-core` | **Granularity:** `file` | **Target:** arnold/pipeline/resources.py

**Reason:** Neutral resource/prompt-loading interface (PipelineResourceBundle, prompt resolution primitives) extracted from megaplan/_pipeline/prompts.py neutral symbols.  Created as an Arnold-native module with no Megaplan phase vocabulary or handler references.

**Blockers:**
- File does not exist yet — to be created by T6 (resources.py).

**Import policy:**
- Forbidden: `megaplan.handlers`, `megaplan._core`, `megaplan.execute`

**Extraction prerequisites:**
- Extract PromptRenderer, PromptRegistry, _GLOBAL_REGISTRY, register_prompt, resolve_prompt, register_pipeline_prompt, registered_keys from megaplan/_pipeline/prompts.py.

**First extraction unit:** `arnold/pipeline/resources.py`

---

#### arnold/pipeline/artifacts.py
**Disposition:** `arnold-core` | **Granularity:** `file` | **Target:** arnold/pipeline/artifacts.py

**Reason:** Neutral versioned-artifact path helpers extracted from megaplan/_pipeline/artifacts.py — artifact_dir, latest_version, next_version_path, versioned_artifacts, latest_artifact_path.  Entirely free of Megaplan phase vocabulary.

**Blockers:**
- File does not exist yet — to be created by T7 (artifacts.py).

**Extraction prerequisites:**
- Copy neutral artifact helpers from megaplan/_pipeline/artifacts.py; only import is StepContext from types.

**First extraction unit:** `arnold/pipeline/artifacts.py`

---

#### arnold/pipeline/registry.py
**Disposition:** `arnold-core` | **Granularity:** `file` | **Target:** arnold/pipeline/registry.py

**Reason:** Neutral pipeline registry CRUD + skill.md reading extracted from megaplan/_pipeline/registry.py.  Core PipelineRegistry dataclass (register, get, names, describe, metadata_for, read_skill_md, operation_registry_for) is policy-free.  Discovery/trust/manifest/budget authority remain in Megaplan adapter.

**Blockers:**
- File does not exist yet — to be created by T11 (registry core extraction).
- Discovery scan roots, env vars, trust classification, manifest parsing, and budget authority must stay in Megaplan bridge adapter.

**Import policy:**
- Forbidden: `megaplan.runtime.budget_authority`, `megaplan.pipelines`

**Extraction prerequisites:**
- Extract PipelineRegistry class, global singleton, and skill.md reading into arnold/pipeline/registry.py.

**First extraction unit:** `arnold/pipeline/registry.py`

---

#### arnold/pipeline/discovery/__init__.py
**Disposition:** `arnold-core` | **Granularity:** `file` | **Target:** arnold/pipeline/discovery/__init__.py

**Reason:** Neutral discovery package init extracted from megaplan/_pipeline/discovery/__init__.py.  Re-exports discovery primitives without Megaplan-branded scan roots or trust classification.

**Blockers:**
- File does not exist yet — to be created during registry extraction (T11).

**Extraction prerequisites:**
- Audit for Megaplan-specific string literals and type references before extraction.

**First extraction unit:** `arnold/pipeline/discovery/__init__.py`

---

#### arnold/pipeline/steps/__init__.py
**Disposition:** `arnold-core` | **Granularity:** `file` | **Target:** arnold/pipeline/steps/__init__.py

**Reason:** Neutral steps package init extracted from megaplan/_pipeline/steps/__init__.py.  Re-exports AgentStep, PanelReviewerStep, HumanDecisionStep as Arnold-native step primitives.

**Blockers:**
- File does not exist yet — to be created by T19 (steps extraction).

**Extraction prerequisites:**
- Audit for Megaplan-specific string literals and type references before extraction.

**First extraction unit:** `arnold/pipeline/steps/__init__.py`

---

#### arnold/pipeline/steps/agent.py
**Disposition:** `arnold-core` | **Granularity:** `file` | **Target:** arnold/pipeline/steps/agent.py

**Reason:** Neutral single-model markdown step — reads inputs, renders prompt, calls worker, writes versioned output.  Generic pipeline composition primitive extracted from megaplan/_pipeline/steps/agent.py.  Only known entanglement is typed_ports_on() flag import in run().

**Blockers:**
- File does not exist yet — to be created by T19 (steps extraction).
- typed_ports_on() flag import from megaplan._pipeline.flags creates a minor entanglement resolved in M3a batch 7.

**Extraction prerequisites:**
- Extract AgentStep class with its neutral run(), _render_prompt(), and _write_versioned_output() methods.

**First extraction unit:** `arnold/pipeline/steps/agent.py`

---

#### arnold/pipeline/steps/panel.py
**Disposition:** `arnold-core` | **Granularity:** `file` | **Target:** arnold/pipeline/steps/panel.py

**Reason:** Neutral panel reviewer step — one reviewer within a fan-out panel, scoped to a persona.  Writes per-reviewer versioned output.  Generic pipeline composition primitive extracted from megaplan/_pipeline/steps/panel.py.

**Blockers:**
- File does not exist yet — to be created by T19 (steps extraction).
- typed_ports_on() flag import from megaplan._pipeline.flags creates a minor entanglement resolved in M3a batch 7.

**Extraction prerequisites:**
- Extract PanelReviewerStep class with neutral run() and output-writing logic.

**First extraction unit:** `arnold/pipeline/steps/panel.py`

---

#### arnold/pipeline/steps/human_gate.py
**Disposition:** `arnold-core` | **Granularity:** `file` | **Target:** arnold/pipeline/steps/human_gate.py

**Reason:** Neutral human decision gate step — pause-and-resume with awaiting_user.json.  Extracted from megaplan/_pipeline/steps/human_gate.py.  Deferred to M7 for full neutralization of resume conventions (_pipeline_name, _pipeline_version carry Megaplan branding).

**Blockers:**
- File does not exist yet — to be created by T19 (steps extraction).
- Pause file path and state keys (_pipeline_name, _pipeline_version) carry Megaplan branding; deferred to M7 for full neutralization.

**Extraction prerequisites:**
- Extract HumanDecisionStep class; defer resume convention neutralization to M7.

**First extraction unit:** `arnold/pipeline/steps/human_gate.py`

---

### arnold-service-interface (1 rows)

#### megaplan/_pipeline/discovery/manifest.py
**Disposition:** `arnold-service-interface` | **Granularity:** `file` | **Target:** arnold/pipeline/discovery/manifest.py

**Reason:** The manifest reader defines the public import-free discovery contract for pipeline modules, which belongs at the Arnold SDK surface.

**Blockers:**
- The identity schema string is still `megaplan.pipeline-manifest.v1`.
- Resource-directory and `SKILL.md` sibling conventions are still derived from the current Megaplan package layout.

**Import policy:**
- Forbidden: `megaplan.handlers`, `megaplan.execute`, `megaplan.finalize`

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| manifest_fields | arnold | ['name', 'description', 'default_profile', 'supported_modes', 'driver', 'entrypoint', 'arnold_api_version', 'capabilities'] |

**String policy:**
| Category | Owner | Policy |
| --- | --- | --- |
| schema_identity | megaplan | Rename identity schema and filesystem conventions before final Arnold extraction. |

**Extraction prerequisites:**
- Publish Arnold-neutral schema and filesystem naming for pipeline manifests.

**First extraction unit:** `arnold/pipeline/discovery/manifest.py`

**Tests / gates:**
- static-gate: arnold/pipeline/discovery/manifest.py must not import modules to read manifests

---

### arnold-adapter (3 rows)

#### megaplan/drivers/subprocess_isolated.py
**Disposition:** `arnold-adapter` | **Granularity:** `file` | **Target:** arnold/runtime/drivers/subprocess_isolated.py

**Reason:** `drivers/subprocess_isolated.py` is mostly a neutral subprocess driver, but today it adapts through Megaplan's `_supervise_subprocess` watcher and `_pipeline.types` step contracts.

**Blockers:**
- The file imports `_supervise_subprocess` from `megaplan.auto` instead of an Arnold-owned watcher module.
- The public contract is still `StepContext` / `StepResult` from `megaplan._pipeline.types`.
- `ctx.plan_dir` is the active working-directory source, so the adapter still speaks current Megaplan pipeline context.

**Import policy:**
- Allowed: `megaplan.runtime.process`, `megaplan.auto`, `megaplan._pipeline.types`
- Forbidden: `megaplan.handlers`, `megaplan.execute`, `megaplan.profiles`

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| driver_primitives | arnold | ['SubprocessIsolatedDriver', 'argv', 'idle_cap', 'wall_cap'] |

**String policy:**
| Category | Owner | Policy |
| --- | --- | --- |
| watcher_bindings | megaplan | Rebind the adapter to the Arnold-owned watcher seam before extracting the file. |

**Extraction prerequisites:**
- Move `_supervise_subprocess()` fully under Arnold and import it from the extracted runtime watcher module.
- Replace `_pipeline.types` step contracts with Arnold-neutral driver contracts.

**First extraction unit:** `arnold/runtime/drivers/subprocess_isolated.py`

**Tests / gates:**
- static-gate: arnold/runtime/drivers/subprocess_isolated.py must not import `megaplan.auto` or `_pipeline.types`

---

#### megaplan/store/compat.py
**Disposition:** `arnold-adapter` | **Granularity:** `file` | **Target:** arnold/store/compat.py

**Reason:** `store/compat.py` is an adapter layer that exposes the legacy dict-based Arnold store API on top of the new typed Store and BlobStore seams.

**Blockers:**
- The adapter still hardcodes the legacy Arnold method names, idempotency-key synthesis, and dict-dump conventions expected by current callers.

**Import policy:**
- Allowed: `megaplan.store.base`, `megaplan.store.blob`, `megaplan.schemas`
- Forbidden: `megaplan.handlers`, `megaplan.cli`

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| compatibility_adapters | arnold | ['ArnoldStoreAdapter', 'ArnoldBlobAdapter'] |

**String policy:**
| Category | Owner | Policy |
| --- | --- | --- |
| idempotency_prefixes | arnold | The compatibility bridge may retain Arnold-facing prefixes while the live dict API still exists. |

**Extraction prerequisites:**
- Keep this adapter surface until callers no longer expect the legacy dict-shaped Arnold store API.

**First extraction unit:** `arnold/store/compat.py`

**Tests / gates:**
- static-gate: arnold/store/compat.py must stay limited to compatibility translation over the typed store seam

---

#### megaplan/pricing
**Disposition:** `arnold-adapter` | **Granularity:** `directory` | **Target:** arnold/adapters/pricing

**Reason:** `pricing/` provides external provider pricing tables and cost computations for Claude, Codex, and Fireworks. Each module (claude.py, codex.py, fireworks.py) is a pure external-service wrapper with no Megaplan-specific imports or vocabulary. The __init__.py re-exports cost_from_usage helpers for each provider. This is a textbook Arnold adapter — wrap an external service's pricing model behind a unified Arnold API.

**Blockers:**
- Pricing tables embed provider-specific rate structures that must be kept in sync with upstream sources (Fireworks docs, Anthropic pricing pages, Codex session counters).
- No Megaplan dependency, but extraction requires establishing an Arnold adapter contract for cost_from_usage(prompt_tokens, completion_tokens, model) signature.

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| pricing | arnold | ['cost_from_usage', 'PRICING', 'CLAUDE_PRICING', 'FIREWORKS_PRICING', 'DEFAULT_PRICING', 'estimate_tokens_from_cost'] |

**Extraction prerequisites:**
- Define Arnold adapter interface for pricing before extraction.

**Tests / gates:**
- tests/test_claude_pricing.py exercises Claude pricing

---

### arnold-shared-leaf (12 rows)

#### megaplan/_pipeline/envelope.py
**Disposition:** `arnold-shared-leaf` | **Granularity:** `file` | **Target:** arnold/pipeline/envelope.py

**Reason:** `RunEnvelope` and its join/JSON transport primitives are Arnold-neutral execution carriers; the remaining coupling is naming-level rather than planning-policy logic.

**Blockers:**
- Environment variable and stderr tag names are still `MEGAPLAN_*` branded.
- Helper functions still persist files under plan-dir conventions that need neutral naming review.

**Import policy:**
- Forbidden: `megaplan.handlers`, `megaplan.execute`, `megaplan.finalize`

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| envelope_fields | arnold | ['taint', 'cost', 'lineage', 'deadline', 'cancellation', 'retry_budget', 'lease_id', 'fencing_token', 'capacity_grant'] |

**String policy:**
| Category | Owner | Policy |
| --- | --- | --- |
| env_names | megaplan | Rename transport env/tag constants before promoting the file under Arnold. |

**Extraction prerequisites:**
- Rename the transport env var and stderr tag to Arnold-neutral identifiers.

**First extraction unit:** `arnold/pipeline/envelope.py`

**Tests / gates:**
- static-gate: arnold/pipeline/envelope.py must not import planning handlers or stage modules

---

#### megaplan/_pipeline/contracts.py
**Disposition:** `arnold-shared-leaf` | **Granularity:** `file` | **Target:** arnold/pipeline/contracts.py

**Reason:** The contract ledger, coercion table, and typed-port binder are generic pipeline primitives without Megaplan planning vocabulary.

**Blockers:**
- The binder is still typed directly to `_pipeline.types.Pipeline`, `Stage`, and `ParallelStage` instead of an Arnold-owned interface module.
- Judge-manifest integration should stay optional so the shared binder is not forced to own plugin-specific discovery flows.

**Import policy:**
- Forbidden: `megaplan.handlers`, `megaplan.execute`, `megaplan.finalize`

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| typed_port_contracts | arnold | ['ContractLedger', 'PortBindError', 'BindResult', 'RepairGradient'] |

**Extraction prerequisites:**
- Repoint binder type imports at Arnold-owned carrier interfaces.
- Keep judge-manifest discovery glue outside the shared contract layer.

**First extraction unit:** `arnold/pipeline/contracts.py`

**Tests / gates:**
- static-gate: arnold/pipeline/contracts.py must remain free of planning stage literals

---

#### megaplan/runtime/governor.py
**Disposition:** `arnold-shared-leaf` | **Granularity:** `file` | **Target:** arnold/runtime/governor.py

**Reason:** `runtime/governor.py` is a generic execution-budget primitive over envelope cost/depth/concurrency/fanout state, without Megaplan plan-dir, profile, queue, or routing policy.

**Blockers:**
- The public type still imports `RunEnvelope` from `megaplan._pipeline.envelope` instead of an Arnold-neutral envelope carrier.
- Some comments name current Megaplan call sites (`KeyPool.acquire`, `pattern_dynamic`, `restorable_boundary`) even though the mechanics are generic.

**Import policy:**
- Allowed: `megaplan._pipeline.envelope`
- Forbidden: `megaplan.handlers`, `megaplan.cli`, `megaplan.profiles`, `megaplan.workers`

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| budget_primitives | arnold | ['Governor', 'ExceedReason', 'BudgetExceeded', 'current_governor', 'set_governor'] |

**String policy:**
| Category | Owner | Policy |
| --- | --- | --- |
| budget_dimensions | arnold | Budget dimensions are Arnold-owned runtime vocabulary. |

**Extraction prerequisites:**
- Repoint the envelope type import to an Arnold-neutral carrier module.
- Trim call-site-specific Megaplan commentary from the Arnold-owned copy.

**First extraction unit:** `arnold/runtime/governor.py`

**Tests / gates:**
- static-gate: arnold/runtime/governor.py must not import plan-dir/profile/worker routing policy

**Configurable seams:**
| Declaration Site | Inherits Via | Override Mechanism | Meaning Owner | Supported State |
| --- | --- | --- | --- | --- |
| megaplan/runtime/governor.py (ContextVar attachment helpers) | execution-tree ContextVar scope | callers install/remove a governor explicitly per execution tree | arnold | supported |

---

#### megaplan/drivers/in_process.py
**Disposition:** `arnold-shared-leaf` | **Granularity:** `file` | **Target:** arnold/runtime/drivers/in_process.py

**Reason:** `drivers/in_process.py` is a straightforward in-process step runner with generic exception-to-result translation and no Megaplan path/profile/queue policy.

**Blockers:**
- It still types the contract in terms of `megaplan._pipeline.types.StepContext` and `StepResult`.
- The `kind` literal set currently mirrors Megaplan pipeline step categories.

**Import policy:**
- Allowed: `megaplan._pipeline.types`
- Forbidden: `megaplan.auto`, `megaplan.handlers`, `megaplan.profiles`

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| driver_primitives | arnold | ['InProcessDriver', 'run_step'] |

**String policy:**
| Category | Owner | Policy |
| --- | --- | --- |
| crash_isolation | arnold | The in-process crash-isolation trade is Arnold-owned runtime behavior. |

**Extraction prerequisites:**
- Repoint `StepContext` / `StepResult` imports to Arnold-neutral driver contracts.
- Decide whether step-kind literals stay on the neutral contract or become plugin-declared metadata.

**First extraction unit:** `arnold/runtime/drivers/in_process.py`

**Tests / gates:**
- static-gate: arnold/runtime/drivers/in_process.py must not import Megaplan auto/handler policy

---

#### megaplan/observability/__init__.py
**Disposition:** `arnold-shared-leaf` | **Granularity:** `file` | **Target:** arnold/observability/__init__.py

**Reason:** Pure re-export module aggregating sibling observability symbols; owns no logic, just the public surface.

**Blockers:**
- Re-exports symbols from both shared-leaf (event_sink, composition_obs) and megaplan-plugin (introspect, cost, doctor) siblings.
- Must be regenerated after split extraction to only re-export shared-leaf symbols.

**Import policy:**
- Forbidden: `megaplan.planning`, `megaplan.control_interface`, `megaplan._core`, `megaplan._pipeline`

**Extraction prerequisites:**
- Complete extraction of megaplan-plugin siblings before regenerating __init__.py exports.

**Tests / gates:**
- none-yet: static-gate verifying __init__.py only imports from allowed shared-leaf sibling modules

---

#### megaplan/observability/event_sink.py
**Disposition:** `arnold-shared-leaf` | **Granularity:** `file` | **Target:** arnold/observability/event_sink.py

**Reason:** Generic EventSink Protocol with NdjsonBackend and StoreBackend implementations; defers run_id resolution to events._resolve_run_id via lazy import, carries no planning or phase vocabulary.

**Blockers:**
- NdjsonBackend lazily imports `megaplan.observability.events.emit` — shared-leaf sibling, acceptable post-extraction.
- StoreBackend lazily imports `megaplan.observability.events._resolve_run_id` — shared-leaf sibling, acceptable post-extraction.

**Import policy:**
- Allowed: `megaplan.observability.events`
- Forbidden: `megaplan.planning`, `megaplan.control_interface`, `megaplan._core`, `megaplan._pipeline`, `megaplan.store`

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| sink_protocol | arnold | ['EventSink', 'EventEnvelope', 'NdjsonBackend', 'StoreBackend'] |

**String policy:**
| Category | Owner | Policy |
| --- | --- | --- |
| sink_kinds | arnold | EventSink emit() `kind` strings are caller-owned; this module defines no fixed kind vocabulary. |

**Extraction prerequisites:**
- Ensure events.py shared-leaf extraction is complete so NdjsonBackend's lazy import resolves against Arnold-owned events module.

**First extraction unit:** `arnold/observability/event_sink.py`

**Tests / gates:**
- static-gate: Arnold-shared event_sink.py must not import megaplan.store, megaplan._pipeline, or megaplan.planning

---

#### megaplan/observability/composition_obs.py
**Disposition:** `arnold-shared-leaf` | **Granularity:** `file` | **Target:** arnold/observability/composition_obs.py

**Reason:** Pure CompositionObservability Protocol + InMemoryCompositionObs reference implementation; zero megaplan imports, no phase or planning vocabulary.

**Blockers:**
- _(none)_


**Import policy:**
- Forbidden: `megaplan.planning`, `megaplan._core`, `megaplan._pipeline`, `megaplan.store`

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| composition_interface | arnold | ['CompositionObservability', 'InMemoryCompositionObs', 'step_boundary', 'decision', 'retry', 'budget_delta', 'piece_identity'] |

**String policy:**
| Category | Owner | Policy |
| --- | --- | --- |
| composition_beats | arnold | Composition beat names (step_boundary, decision, retry, budget_delta, piece_identity) are Arnold-owned interface vocabulary. |

**First extraction unit:** `arnold/observability/composition_obs.py`

**Tests / gates:**
- none-yet: smoke test verifying InMemoryCompositionObs.emit round-trip

---

#### megaplan/observability/evaluand.py
**Disposition:** `arnold-shared-leaf` | **Granularity:** `file` | **Target:** arnold/observability/evaluand.py

**Reason:** Generic EvaluandRecord / ModelIORef / verify-judge infrastructure with canonical serialization and event round-trip; imports only from within observability, carries no planning or phase vocabulary.

**Blockers:**
- References `megaplan.observability.events.emit_state_wal` in docstring only (line 40) — not an import, just documentation of the journal target.

**Import policy:**
- Allowed: `megaplan.observability.events`
- Forbidden: `megaplan.planning`, `megaplan.control_interface`, `megaplan._core`, `megaplan._pipeline`, `megaplan.store`

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| evaluation_types | arnold | ['EvaluandRecord', 'ModelIORef', 'RecordedModelIO', 'RecordedIOScorer', 'RecordedIOUnavailable', 'ReJudgeOutcome', 'BetterResult'] |

**String policy:**
| Category | Owner | Policy |
| --- | --- | --- |
| evaluand_kinds | arnold | Evaluand event kind `evaluand_recorded` is Arnold-owned; the module defines no fixed kind vocabulary beyond using the shared EventKind enum. |

**Extraction prerequisites:**
- Ensure events.py EventKind enum is available in Arnold shared-leaf before extraction.

**First extraction unit:** `arnold/observability/evaluand.py`

**Tests / gates:**
- static-gate: Arnold-shared evaluand.py must not import megaplan.store, megaplan._pipeline, or megaplan.planning

---

#### megaplan/store/base.py
**Disposition:** `arnold-shared-leaf` | **Granularity:** `file` | **Target:** arnold/store/base.py

**Reason:** `store/base.py` defines the backend-neutral Store protocol, transaction shape, idempotency helper, and storage record wrappers over shared schemas.

**Blockers:**
- The store-facing models and enums still import from `megaplan.schemas`, `megaplan.schemas.arnold`, and `megaplan.schemas.sprint1`.

**Import policy:**
- Allowed: `megaplan.schemas`, `megaplan.schemas.arnold`, `megaplan.schemas.base`, `megaplan.schemas.sprint1`
- Forbidden: `megaplan.handlers`, `megaplan.cli`, `megaplan.execute`, `megaplan.finalize`

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| store_contracts | arnold | ['Store', 'Transaction', 'StoredEvent', 'deterministic_idempotency_key', 'validate_plan_artifact_name'] |

**String policy:**
| Category | Owner | Policy |
| --- | --- | --- |
| schema_modules | megaplan | Arnold can own the storage seam once the imported schema modules are re-homed behind Arnold-neutral package paths. |

**Extraction prerequisites:**
- Re-home the store-facing schema and enum imports behind Arnold-neutral module paths.

**First extraction unit:** `arnold/store/base.py`

**Tests / gates:**
- static-gate: arnold/store/base.py must stay limited to storage contracts and schema-backed value types
- static-gate: arnold/store/base.py must not import handler, CLI, or planning modules

---

#### megaplan/store/blob.py
**Disposition:** `arnold-shared-leaf` | **Granularity:** `file` | **Target:** arnold/store/blob.py

**Reason:** `store/blob.py` is a backend-agnostic blob-storage seam plus concrete local-directory and Supabase implementations.

**Blockers:**
- Local file writes still depend on `prepare_journal_transaction`, `journal_blob_promotion`, and `commit_journal_transaction` from `megaplan._core.io`.
- Backend configuration still uses current Megaplan/Supabase environment keys and default roots.

**Import policy:**
- Allowed: `megaplan._core.io`, `megaplan.schemas`
- Forbidden: `megaplan.handlers`, `megaplan.cli`, `megaplan.execute`, `megaplan.finalize`

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| blob_primitives | arnold | ['BlobStore', 'BlobRef', 'BlobStat', 'BlobMissingError', 'LocalDirBlobStore', 'SupabaseStorageBlobStore'] |

**String policy:**
| Category | Owner | Policy |
| --- | --- | --- |
| env_vars | megaplan | Neutralize current deployment variable names and default blob roots before promoting this seam under Arnold. |

**Extraction prerequisites:**
- Re-export journal blob-promotion helpers from an Arnold-neutral core I/O module.
- Rename backend configuration environment keys and default blob roots.

**First extraction unit:** `arnold/store/blob.py`

**Tests / gates:**
- static-gate: arnold/store/blob.py must stay free of planning, CLI, and handler imports

---

#### megaplan/routing
**Disposition:** `arnold-shared-leaf` | **Granularity:** `directory` | **Target:** arnold/shared/routing

**Reason:** `routing/` provides stable model-output routing identity (ModelIdentity, compute_identity, prompt_hash, params_hash) and Store-backed cache helpers (cache_get, cache_set). identity.py is pure stdlib hashing — zero megaplan imports. cache.py imports megaplan.schemas (Arnold-owned) and megaplan.store (Arnold-owned). No Megaplan vocabulary or policy. This is Arnold-neutral routing infrastructure suitable as a shared leaf.

**Blockers:**
- cache.py imports `megaplan.schemas.CodeArtifact` and `megaplan.store.Store` — both Arnold-owned, but the import paths must be updated on extraction.

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| routing | arnold | ['ModelIdentity', 'compute_identity', 'prompt_hash', 'params_hash', 'cache_get', 'cache_set', 'identity_cache_key', 'MODEL_PARAM_KEYS'] |

**Extraction prerequisites:**
- Update megaplan.schemas and megaplan.store imports to Arnold paths.

**Tests / gates:**
- none-yet: routing identity has no dedicated test file

---

#### megaplan/agent_runtime
**Disposition:** `arnold-shared-leaf` | **Granularity:** `directory` | **Target:** arnold/agent_runtime

**Reason:** Public vendorable agent runtime contract surface (AgentDispatcher, EventEmitter, KeySource, FanoutResult, etc.) with minimal Megaplan coupling. Forms a reusable contract boundary.

**Blockers:**
- process_fanout.py imports megaplan._core types for fan-out dispatch.
- adapters.py references megaplan.types for error propagation.

**Import policy:**
- Forbidden: `megaplan.agent`, `megaplan.planning`, `megaplan.handlers`

**Extraction prerequisites:**
- Decouple process_fanout from megaplan._core dispatch types.

**First extraction unit:** `arnold/agent_runtime`

---

### megaplan-plugin (93 rows)

#### megaplan/cli/status_view.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/pipelines/megaplan/status_view.py

**Reason:** `status_view.py` is a planning-specific projection over Megaplan states, finalize artifacts, blocker recovery, receipts, and planning control bindings rather than an Arnold-generic status surface.

**Blockers:**
- Imports `planning_control_binding()`, `planning_run_state_view()`, `RunOutcome`, `STATE_BLOCKED`, and `StepResponse`.
- Reads `finalize.json`, execution batch artifacts, `.plan.lock`, phase-result recovery data, and override command suggestions using current Megaplan plan-dir conventions.
- Synthesizes planning-specific next steps such as `override recover-blocked`, `override force-proceed`, `resume`, `feedback workflow`, and tiebreaker commands.

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| state_keys | megaplan | ['blocked', 'awaiting_human', 'failed', 'pending_human'] |
| override_actions | megaplan | ['recover-blocked', 'force-proceed', 'add-note', 'resume-clarify'] |
| artifact_names | megaplan | ['finalize.json', 'execution_batch_*.json', '.plan.lock', 'awaiting_user.json'] |

**String policy:**
| Category | Owner | Policy |
| --- | --- | --- |
| artifact_names | megaplan | This status projection currently depends on Megaplan plan-dir artifact names and should stay plugin-local. |

**Extraction prerequisites:**
- Define an Arnold-neutral status contract first; until then keep the entire status-view projector plugin-local.

**Tests / gates:**
- static-gate: Arnold-neutral status surfaces must not import planning control bindings or finalize/execution artifact conventions

---

#### megaplan/workers/_mock_payloads.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/pipelines/megaplan/workers/mock_payloads.py

**Reason:** `workers/_mock_payloads.py` fabricates Megaplan plan/prep/gate/finalize/execute/review payloads, including task IDs, sense-check IDs, and current execution-batch contracts.

**Blockers:**
- The file hardcodes Megaplan phase names, task IDs, sense-check IDs, and execution/finalize/review payload structure.
- Prompt-override parsing is specific to the current `Only produce task_updates...` / `sense_check_acknowledgments...` instructions.

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| execution_payloads | megaplan | ['task_updates', 'sense_check_acknowledgments', 'success_criteria', 'sense_checks'] |

**String policy:**
| Category | Owner | Policy |
| --- | --- | --- |
| execution_payloads | megaplan | Plugin-owned only. |

**Extraction prerequisites:**
- Keep mock payload generation plugin-local unless Arnold later standardizes an execution-batch/testing contract.

**Tests / gates:**
- static-gate: Arnold-neutral worker modules must not fabricate Megaplan execute/finalize/review payloads

---

#### megaplan/workers/hermes.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/pipelines/megaplan/workers/hermes.py

**Reason:** `workers/hermes.py` is a Megaplan-specific worker implementation with phase prompts, plan-dir artifacts, observability hooks, provider fallback policy, and execution-batch reconstruction logic.

**Blockers:**
- It depends on `PlanState`, Megaplan prompt factories, `_core` helpers, observability events, and execution checkpoint files.
- The worker reconstructs execute payloads from `execution_batch_*.json` and current plan-dir/git-state conventions.
- Provider fallback, reasoning-depth handling, and schema enforcement are wired through current Megaplan worker policy.

**Import policy:**
- Allowed: `megaplan.runtime.sandbox`, `megaplan.runtime.key_pool`

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| worker_routes | megaplan | ['Hermes', 'OpenRouter', 'MiniMax', 'execution_batch_*.json'] |

**String policy:**
| Category | Owner | Policy |
| --- | --- | --- |
| execution_artifacts | megaplan | Plan-dir checkpoint/reconstruction behavior stays plugin-local. |
| provider_policy | megaplan | Current provider fallback and reasoning-depth behavior are plugin-owned worker policy. |

**Extraction prerequisites:**
- Keep Hermes-specific provider/session/phase behavior inside the Megaplan plugin.

**Tests / gates:**
- static-gate: Arnold-neutral worker runtime modules must not reconstruct Megaplan execute payloads or read execution batch checkpoints

---

#### megaplan/workers/shannon.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/pipelines/megaplan/workers/shannon.py

**Reason:** `workers/shannon.py` is a Megaplan-specific interactive Claude worker with tmux/session strategy, prompt-file wiring, trusted-workspace handling, and structured-output instructions tied to current batch/sense-check contracts.

**Blockers:**
- The file is built around the vendored Shannon fork, Claude session strategy, `.megaplan` run artifacts, and Megaplan prompt files.
- It injects explicit `task_updates` / `sense_check_acknowledgments` scope rules into the prompt.
- Workspace trust preparation and tmux session naming are tied to current Megaplan worktree/runtime conventions.

**Import policy:**
- Allowed: `megaplan.runtime.process`

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| worker_routes | megaplan | ['Shannon', 'Claude', 'task_updates', 'sense_check_acknowledgments', '.megaplan'] |

**String policy:**
| Category | Owner | Policy |
| --- | --- | --- |
| execution_payloads | megaplan | Structured-output instructions for execution tasks and sense checks remain plugin-owned. |
| worker_routes | megaplan | Shannon/Claude session strategy and trusted-workspace behavior remain plugin-local. |

**Extraction prerequisites:**
- Keep the interactive Claude/Shannon worker entirely plugin-local unless Arnold later adopts the same session/prompt contract.

**Tests / gates:**
- static-gate: Arnold-neutral worker runtime modules must not inject Megaplan execution-task/sense-check prompt rules

---

#### megaplan/observability/trace.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/pipelines/megaplan/observability/trace.py

**Reason:** CLI trace formatter importing `megaplan._core.find_plan_dir`, carrying Megaplan phase/kind labels in `_kind_label`, and operating on `events.ndjson` plan-dir files — intrinsically a Megaplan CLI tool.

**Blockers:**
- Imports `megaplan._core.find_plan_dir` for plan resolution.
- `_kind_label` maps 24+ Megaplan event kind strings to display labels.
- `handle_trace` is a `megaplan trace` CLI entry point wired through Megaplan's CLI surface.
- Operates on `plan_dir/events.ndjson` with Megaplan-sidecar conventions.

**Import policy:**
- Allowed: `megaplan.observability.events`

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| cli_tool | megaplan | ['megaplan trace', 'handle_trace'] |

**String policy:**
| Category | Owner | Policy |
| --- | --- | --- |
| cli_entry_points | megaplan | `megaplan trace` is a Megaplan-owned CLI surface; Arnold provides its own trace/observability CLI. |

**Extraction prerequisites:**
- Keep the CLI trace formatter entirely plugin-local; Arnold observability will provide its own trace surface.

**Tests / gates:**
- static-gate: Arnold shared-leaf must not import or re-export trace.py

---

#### megaplan/observability/introspect.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/pipelines/megaplan/observability/introspect.py

**Reason:** CLI introspection tool importing `megaplan.control_interface.read_valid_targets`, `megaplan.planning.control_binding`, `megaplan.run_outcome.RunOutcome`, and `megaplan.profiles`; deeply wired into Megaplan planning state, control bindings, and profile directory conventions.

**Blockers:**
- Imports `megaplan.control_interface.read_valid_targets` — planning control surface.
- Imports `megaplan.planning.control_binding.planning_control_binding` and `planning_run_state_view` — planning bindings.
- Imports `megaplan.run_outcome.RunOutcome` — Megaplan outcome type.
- Uses `megaplan.profiles.__file__` for profile directory resolution (line 149-151).
- Uses `.megaplan/plans/<name>/` path convention for project-root derivation (line 447-455).
- Sets `MEGAPLAN_NO_COLOR=1` env var for subprocess calls (line 123).

**Import policy:**
- Allowed: `megaplan.observability.events`

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| cli_tool | megaplan | ['megaplan introspect', 'handle_introspect'] |

**String policy:**
| Category | Owner | Policy |
| --- | --- | --- |
| cli_entry_points | megaplan | `megaplan introspect` is a Megaplan-owned CLI surface. |
| megaplan_env_vars | megaplan | `MEGAPLAN_NO_COLOR` env var is Megaplan-owned; Arnold uses its own env-var namespace. |
| megaplan_paths | megaplan | `.megaplan/plans/<name>/` path convention is Megaplan-owned. |

**Extraction prerequisites:**
- Keep the introspect CLI entirely plugin-local; Arnold introspection will use Arnold-owned control interfaces and state views.

**Tests / gates:**
- static-gate: Arnold shared-leaf must not import megaplan.control_interface, megaplan.planning.control_binding, or megaplan.run_outcome

---

#### megaplan/observability/cost.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/pipelines/megaplan/observability/cost.py

**Reason:** CLI cost breakdown tool importing `megaplan._core.find_plan_dir` and `megaplan._pipeline.flags.unified_emit_on/unified_evaluand_on`; wired into Megaplan flag infrastructure and plan-dir resolution.

**Blockers:**
- Imports `megaplan._core.find_plan_dir` for plan resolution.
- Imports `megaplan._pipeline.flags.unified_emit_on` and `unified_evaluand_on` for R5-aware vendor lookup.
- Uses `_vendor_for_cost_event` that branches on Megaplan feature flags.
- Operates on `plan_dir/events.ndjson` with Megaplan conventions.

**Import policy:**
- Allowed: `megaplan.observability.events`

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| cli_tool | megaplan | ['megaplan cost', 'handle_cost'] |

**String policy:**
| Category | Owner | Policy |
| --- | --- | --- |
| cli_entry_points | megaplan | `megaplan cost` is a Megaplan-owned CLI surface. |

**Extraction prerequisites:**
- Keep the cost CLI entirely plugin-local; Arnold cost tracking uses its own cost attribution surface.

**Tests / gates:**
- static-gate: Arnold shared-leaf must not import megaplan._core, megaplan._pipeline.flags, or re-export cost.py

---

#### megaplan/observability/doctor.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/pipelines/megaplan/observability/doctor.py

**Reason:** CLI diagnostic tool importing `megaplan._core.workflow.workflow_next`, `megaplan._core.find_plan_dir`, and `megaplan.profiles`; deeply wired into Megaplan workflow, profile, lock/state/flag conventions, and env vars.

**Blockers:**
- Imports `megaplan._core.workflow.workflow_next` for flag recovery resolution (line 407).
- Imports `megaplan._core.find_plan_dir` for plan resolution (line 686).
- Imports `megaplan.profiles` for profile directory checks (line 147-149).
- Uses `MEGAPLAN_NO_COLOR=1` env var for subprocess calls (line 121).
- Checks plan-dir artifacts: `.lock`, `state.json`, `gate_signals_v*.json`, `events.ndjson`.

**Import policy:**
- Allowed: `megaplan.observability.events`

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| cli_tool | megaplan | ['megaplan doctor', 'handle_doctor'] |

**String policy:**
| Category | Owner | Policy |
| --- | --- | --- |
| cli_entry_points | megaplan | `megaplan doctor` is a Megaplan-owned CLI surface. |
| megaplan_env_vars | megaplan | `MEGAPLAN_NO_COLOR` env var is Megaplan-owned; Arnold uses its own env-var namespace. |

**Extraction prerequisites:**
- Keep the doctor CLI entirely plugin-local; Arnold diagnostics will use Arnold-owned workflow/state interfaces.

**Tests / gates:**
- static-gate: Arnold shared-leaf must not import megaplan._core.workflow, megaplan._core.find_plan_dir, or megaplan.profiles

---

#### megaplan/observability/events_projection.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/pipelines/megaplan/observability/events_projection.py

**Reason:** Store-to-NDJSON projection importing `megaplan.store.StoredEvent` and `megaplan.store.Store`; intrinsically tied to Megaplan's multi-tenant Store backend and plan-id conventions.

**Blockers:**
- Imports `megaplan.store.StoredEvent` and `megaplan.store.Store` — Megaplan storage infrastructure.
- Materializes `events.ndjson` from Store events using `plan_id` key — Megaplan plan-naming convention.
- `_transaction_id` derives from `plan_id::phase::seq` — Megaplan id scheme.

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| projection_artifacts | megaplan | ['events.ndjson', 'StoredEvent', 'store_method'] |

**String policy:**
| Category | Owner | Policy |
| --- | --- | --- |
| artifact_names | megaplan | `events.ndjson` projection target and `store_method` field are Megaplan conventions. |

**Extraction prerequisites:**
- Keep the Store-to-NDJSON projection entirely plugin-local; Arnold shared-leaf uses direct NDJSON without a Store intermediary.

**Tests / gates:**
- static-gate: Arnold shared-leaf must not import megaplan.store or re-export events_projection.py

---

#### megaplan/observability/effect_enforcement.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/pipelines/megaplan/observability/effect_enforcement.py

**Reason:** Effect-Ledger at-most-once enforcement importing from `megaplan.observability.event_sink` and `megaplan.observability.events`, documenting cost-attribution seams in `megaplan._core.state.append_history` and worker-layer producers (`workers/_impl.py`, `workers/hermes.py`, `workers/shannon.py`); intrinsically Megaplan-specific enforcement.

**Blockers:**
- Documents cost-attribution write site at `megaplan._core.state.py:993-999` (append_history).
- References worker-layer producers: `workers/_impl.py:2229`, `workers/hermes.py:1201`, `workers/shannon.py:1589`.
- `_journal_intent` imports `megaplan.observability.event_sink.NdjsonBackend` and `StoreBackend`.
- Deferred pivot-recording targets reference `megaplan/orchestration/*::pr_create`.

**Import policy:**
- Allowed: `megaplan.observability.events`, `megaplan.observability.event_sink`, `megaplan.observability.effect_ledger`

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| enforcement_mechanism | megaplan | ['journal_then_execute', 'DoubleExecuteError', '_journal_intent'] |

**String policy:**
| Category | Owner | Policy |
| --- | --- | --- |
| effect_intent_kind | megaplan | `effect_intent` event kind emitted by journal_then_execute is a Megaplan-owned enforcement mechanism. |

**Extraction prerequisites:**
- Keep effect enforcement entirely plugin-local; Arnold shared-leaf provides the Effect type but enforcement is a Megaplan runtime concern.

**Tests / gates:**
- static-gate: Arnold shared-leaf must not import or re-export effect_enforcement.py

---

#### megaplan/execute
**Disposition:** `megaplan-plugin` | **Granularity:** `directory` | **Target:** megaplan/pipelines/megaplan/execute

**Reason:** `execute/` contains Megaplan-specific batch dispatch, tier routing, timeout, merge, and quality helpers that import deeply from `megaplan._core`, `megaplan.types`, `megaplan.store`, `megaplan.profiles`, and `megaplan.workers`. No reusable Arnold substrate found after inspection.

**Blockers:**
- Batch dispatch loop wired to Megaplan state-store, profile expansion, tier resolution, and execution-tracking metadata.
- Timeout helpers reference Megaplan phase conventions and plan-dir layout.
- Merge/aggregation modules depend on Megaplan-specific artifact paths and state shapes.

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| execution_primitives | megaplan | ['handle_execute_one_batch', 'handle_execute_auto_loop', 'BatchResult', '_run_and_merge_batch', 'build_monitor_hint'] |

**Extraction prerequisites:**
- Decouple batch dispatch from Megaplan state-store, profile resolution, and tier-routing before any Arnold extraction.

**Tests / gates:**
- none-yet: execute batch dispatch is deeply coupled to Megaplan planning infrastructure

---

#### megaplan/orchestration
**Disposition:** `megaplan-plugin` | **Granularity:** `directory` | **Target:** megaplan/pipelines/megaplan/orchestration

**Reason:** `orchestration/` contains Megaplan-specific phase-result transport, progress emission, evaluation, gate signals, completion contracts, iteration pressure, and recovery policy — all deeply importing `megaplan.schemas`, `megaplan.store`, and `megaplan.types`. No reusable Arnold substrate found after inspection.

**Blockers:**
- PhaseResult transport carries Megaplan ExitKind literals (blocked_by_quality, blocked_by_prereq, context_exhausted).
- Progress emission uses `MEGAPLAN_PROGRESS_*` env vars and Megaplan store backends.
- Recovery policy, gate signals, and iteration pressure are tied to Megaplan planning lifecycle semantics.

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| orchestration | megaplan | ['PhaseResult', 'ExitKind', 'ProgressContext', 'ProgressEvent', 'gate_signals', 'recovery_policy', 'iteration_pressure'] |

**String policy:**
| Category | Owner | Policy |
| --- | --- | --- |
| env_vars | megaplan | Keep `MEGAPLAN_PROGRESS_*` env vars plugin-local until Arnold defines neutral progress infrastructure. |

**Extraction prerequisites:**
- Replace Megaplan-specific phase-result vocabulary and progress infrastructure with Arnold-neutral equivalents before extraction.

**Tests / gates:**
- none-yet: orchestration is deeply coupled to Megaplan planning lifecycle

---

#### megaplan/handlers
**Disposition:** `megaplan-plugin` | **Granularity:** `directory` | **Target:** megaplan/pipelines/megaplan/handlers

**Reason:** `handlers/` contains Megaplan planning-stage handler implementations (plan, critique, gate, execute, finalize, review, override, tiebreaker, tickets, verifiability) that import heavily from `megaplan.types` (STATE_*), `megaplan._core`, `megaplan.execute`, `megaplan.store`, `megaplan.profiles`, `megaplan.audits`, `megaplan.review`, and `megaplan.calibration`. No reusable Arnold substrate found after inspection.

**Blockers:**
- Every handler module is wired to Megaplan state machine (STATE_AWAITING_HUMAN_VERIFY, STATE_BLOCKED, STATE_DONE, etc.).
- Handler dispatch depends on Megaplan phase conventions, plan-dir layout, and profile resolution.
- Shared handler helpers import from the full Megaplan stack.

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| planning_handlers | megaplan | ['handle_plan', 'handle_critique', 'handle_gate', 'handle_execute', 'handle_finalize', 'handle_review', 'handle_override', 'handle_tiebreaker'] |

**Extraction prerequisites:**
- Replace Megaplan state-machine coupling and phase conventions with plugin-owned adapters before any Arnold extraction.

**Tests / gates:**
- none-yet: handlers are the core of Megaplan planning pipeline state machine

---

#### megaplan/planning
**Disposition:** `megaplan-plugin` | **Granularity:** `directory` | **Target:** megaplan/pipelines/megaplan/planning

**Reason:** `planning/` contains Megaplan-specific control binding and planning topology glue that imports from `megaplan._core.workflow`, `megaplan._core.topology`, and other Megaplan modules. No reusable Arnold substrate found after inspection.

**Blockers:**
- Control binding wiring is tied to Megaplan workflow_next and topology conventions.
- Planning module defaults are specific to Megaplan CLI and runtime profiles.

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| planning_control | megaplan | ['control_binding', 'resolve_binding', 'planning_topology'] |

**Extraction prerequisites:**
- Decouple control binding from Megaplan workflow and topology before extraction.

**Tests / gates:**
- none-yet: planning control binding is Megaplan workflow-specific

---

#### megaplan/chain
**Disposition:** `megaplan-plugin` | **Granularity:** `directory` | **Target:** megaplan/pipelines/megaplan/chain

**Reason:** `chain/` contains Megaplan chain-CLI orchestration, CI hooks, git operations, hinge gates, and M3/M5 evaluation gates that import from `megaplan.auto`, `megaplan._core`, `megaplan.profiles`, `megaplan.types`, and `megaplan.observability.fold`. No reusable Arnold substrate found after inspection.

**Blockers:**
- Chain runner imports supervisor tier routing flags and Megaplan auto-driver defaults.
- Hinge gate and M3 dual-green are wired to Megaplan-specific run conventions.
- Git operations reference Megaplan CliError and plan-dir conventions.

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| chain_orchestration | megaplan | ['run_chain_cli', 'run_dual_green', 'hinge_gate', 'ci_hook', 'ChainSpec'] |

**Extraction prerequisites:**
- Decouple chain orchestration from Megaplan auto-driver and plan-dir conventions before extraction.

**Tests / gates:**
- none-yet: chain CLI is Megaplan-specific orchestration

---

#### megaplan/supervisor
**Disposition:** `megaplan-plugin` | **Granularity:** `directory` | **Target:** megaplan/pipelines/megaplan/supervisor

**Reason:** `supervisor/` contains Megaplan cross-run orchestration (driver protocol, autonomy ladder, bakeoff binding, chain runner, outcomes, state persistence) that imports from `megaplan.auto`, `megaplan._core`, `megaplan.control_interface`, `megaplan.run_outcome`, and `megaplan.types`. While the RunDriver/PackRunner protocols are clean, the package as a whole is Megaplan supervisor tier logic.

**Blockers:**
- Driver protocol wraps `megaplan.auto.drive` and uses Megaplan auto-driver constants.
- Autonomy ladder reads/writes Megaplan control interface transitions.
- State persistence uses `.megaplan/plans/.supervisor` paths.

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| supervisor | megaplan | ['RunDriver', 'PackRunner', 'RunNode', 'SupervisorState', 'NormalizedOutcome', 'Ladder', 'BakeoffBinding', 'ChainRunner'] |

**String policy:**
| Category | Owner | Policy |
| --- | --- | --- |
| megaplan_paths | megaplan | Supervisor state paths under `.megaplan/plans/.supervisor` are plugin-local. |

**Extraction prerequisites:**
- Replace Megaplan auto-driver wrapping and control-interface coupling with Arnold-neutral equivalents before extraction.

**Tests / gates:**
- none-yet: supervisor tier is Megaplan-specific cross-run orchestration

---

#### megaplan/bakeoff
**Disposition:** `megaplan-plugin` | **Granularity:** `directory` | **Target:** megaplan/pipelines/megaplan/bakeoff

**Reason:** `bakeoff/` contains Megaplan bakeoff infrastructure (comparison, judge, lifecycle, live status, merge, metrics, orchestrator, state, worktree) that imports from `megaplan._core`, `megaplan.types`, `megaplan.profiles`, and `megaplan.bakeoff` submodules. No reusable Arnold substrate found after inspection.

**Blockers:**
- Bakeoff state and lifecycle depend on Megaplan plan-state loading and persistence.
- Judge resolution uses Megaplan profile and agent-spec conventions.
- Worktree management uses Megaplan-specific paths and CliError.

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| bakeoff | megaplan | ['BakeoffState', 'BakeoffProfileRecord', 'JudgeVerdict', 'orchestrator', 'bakeoff_root', 'comparison', 'worktree'] |

**Extraction prerequisites:**
- Decouple bakeoff from Megaplan plan-state, profile, and path conventions before extraction.

**Tests / gates:**
- none-yet: bakeoff is Megaplan-specific comparison infrastructure

---

#### megaplan/review
**Disposition:** `megaplan-plugin` | **Granularity:** `directory` | **Target:** megaplan/pipelines/megaplan/review

**Reason:** `review/` contains Megaplan review infrastructure (parallel review, pre-checks, mechanical checks) that imports from `megaplan._core`, `megaplan.prompts.review`, `megaplan.types`, and `megaplan.audits.robustness`. No reusable Arnold substrate found after inspection.

**Blockers:**
- Review dispatch imports Megaplan worker scatter/gather and flag registry.
- Pre-checks and mechanical checks reference Megaplan PlanState and phase conventions.

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| review | megaplan | ['run_parallel_review', 'run_pre_checks', 'mechanical_checks', 'ReviewResult'] |

**Extraction prerequisites:**
- Replace Megaplan worker dispatch and PlanState coupling with Arnold-neutral equivalents before extraction.

**Tests / gates:**
- none-yet: review is Megaplan planning pipeline stage

---

#### megaplan/prompts
**Disposition:** `megaplan-plugin` | **Granularity:** `directory` | **Target:** megaplan/pipelines/megaplan/prompts

**Reason:** `prompts/` contains Megaplan-specific prompt templates for every planning stage (critique, execute, feedback, finalize, gate, planning, prep, review, tiebreaker) that import from `megaplan._core`, `megaplan.types`, `megaplan.forms`, `megaplan.audits`, `megaplan.orchestration`, and `megaplan.flags`. No reusable Arnold substrate found after inspection.

**Blockers:**
- Every prompt module is wired to Megaplan PlanState, phase conventions, and creative forms.
- Prompt templates contain Megaplan-specific vocabulary, iteration pressure tables, and flag summaries.

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| prompts | megaplan | ['build_critique_prompt', 'build_execute_prompt', 'build_finalize_prompt', 'build_planning_prompt', 'build_review_prompt'] |

**Extraction prerequisites:**
- Replace Megaplan-state and phase-coupling in prompts with plugin-owned template adapters before extraction.

**Tests / gates:**
- none-yet: prompts are Megaplan planning stage content

---

#### megaplan/loop
**Disposition:** `megaplan-plugin` | **Granularity:** `directory` | **Target:** megaplan/pipelines/megaplan/loop

**Reason:** `loop/` contains the MegaLoop iterative agent workflow engine that imports from `megaplan._core`, `megaplan.loop.*`, `megaplan.profiles`, `megaplan.workers`, and `megaplan.runtime.process`. No reusable Arnold substrate found after inspection.

**Blockers:**
- Loop engine is wired to Megaplan state persistence, artifact paths, and worker dispatch conventions.
- Loop types import Megaplan PlanConfig and SessionInfo.
- Git helpers and prompts reference Megaplan-specific conventions.

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| loop | megaplan | ['LoopSpec', 'LoopState', 'IterationResult', 'Observation', 'loop_engine', 'loop_prompts'] |

**Extraction prerequisites:**
- Decouple loop engine from Megaplan state-store, profile, and worker dispatch before extraction.

**Tests / gates:**
- none-yet: loop engine is Megaplan iterative agent workflow

---

#### megaplan/cloud
**Disposition:** `megaplan-plugin` | **Granularity:** `directory` | **Target:** megaplan/pipelines/megaplan/cloud

**Reason:** `cloud/` contains Megaplan cloud deployment infrastructure (CLI, spec, providers, templates, wrappers, auth, supervise) that imports from `megaplan.types` (CliError, DEFAULT_AGENT_ROUTING, KNOWN_AGENTS), `megaplan.cloud.*`, and uses `MEGAPLAN_*` env vars and `.megaplan/` paths throughout. No reusable Arnold substrate found after inspection.

**Blockers:**
- Cloud spec and CLI are wired to Megaplan agent routing, known agents, and CliError.
- Providers (railway, local, ssh) embed Megaplan-specific naming and deploy conventions.
- Wrapper scripts (mp-chain, mp-run, mp-heartbeat, mp-supervise) are Megaplan-branded command literals.
- Supervisor tick command hardcodes `megaplan chain start` and `MEGAPLAN_TRUSTED_CONTAINER=1`.
- Chain log paths use `.megaplan/cloud-chain.log` and `.megaplan/cloud-sessions` conventions.

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| cloud | megaplan | ['CloudSpec', 'RailwaySpec', 'RepoSpec', 'MegaplanSpec', 'load_spec', 'materialize_deploy_dir', 'cloud_cli', 'supervise'] |

**String policy:**
| Category | Owner | Policy |
| --- | --- | --- |
| wrapper_command_literals | megaplan | Wrapper script commands (mp-chain, mp-run, mp-heartbeat, mp-supervise) are plugin-branded and must remain plugin-local until Arnold defines neutral cloud command conventions. |
| cloud_providers | megaplan | Cloud provider identifiers (railway, local, ssh) are plugin-local deployment targets. |
| megaplan_paths | megaplan | Cloud chain log and session marker paths under `.megaplan/` are plugin-local. |
| megplan_env_vars | megaplan | MEGAPLAN_* env vars used in cloud wrapper commands are plugin-local. |

**Extraction prerequisites:**
- Replace Megaplan-branded wrapper commands, provider naming, and `.megaplan/` paths with Arnold-neutral cloud infrastructure before extraction.

**Tests / gates:**
- none-yet: cloud infrastructure is Megaplan-branded deployment layer

---

#### megaplan/audits
**Disposition:** `megaplan-plugin` | **Granularity:** `directory` | **Target:** megaplan/pipelines/megaplan/audits

**Reason:** `audits/` contains Megaplan audit infrastructure (audit engine, capabilities, critique evaluator, hermes vendoring, iteration, robustness, verifiability) that imports from `megaplan._core`, `megaplan.orchestration.verifiability`, `megaplan.types`, and `megaplan.store`. NOTE: `quality_gates.py` has zero megaplan imports and is a reusable Arnold substrate candidate (pure quality-gate primitives: file growth, duplicate detection, dead-import checks, test-coverage checks), but remains under this directory row until the package can be split.

**Blockers:**
- Audit engine is wired to Megaplan tiebreaker audit tracking and plan-dir conventions.
- Capabilities and robustness checks are tied to Megaplan phase-specific checklists.
- Critique evaluator and hermes vendoring reference Megaplan agent conventions.

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| audits | megaplan | ['audit_engine', 'record_tiebreaker_audit', 'CRITIQUE_CHECKS', 'validate_robustness', 'capabilities'] |

**Extraction prerequisites:**
- Decouple audit engine from Megaplan tiebreaker tracking and phase-specific checklists before extraction.

**Tests / gates:**
- none-yet: audit infrastructure is Megaplan planning pipeline checks

---

#### megaplan/editorial
**Disposition:** `megaplan-plugin` | **Granularity:** `directory` | **Target:** megaplan/pipelines/megaplan/editorial

**Reason:** `editorial/` contains Store-backed editorial operations (body editing, checklists, gating, lockdown, reads, sprints, events) that import from `megaplan.store`, `megaplan.schemas`, and `megaplan.store.snapshot`. NOTE: `errors.py` (generic editorial exception hierarchy) and `lockdown.py` (generic review lockdown phrase detection) have zero megaplan imports and are reusable Arnold substrate candidates, but remain under this directory row until the package can be split.

**Blockers:**
- Editorial operations are wired to Megaplan Store protocol and snapshot conventions.
- Body editing, checklists, and sprints depend on Megaplan-specific idempotency key and transaction patterns.

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| editorial | megaplan | ['EditorialOperation', 'EditorialResult', 'edit_section', 'update_body', 'evaluate_state_transition', 'transition_epic_state'] |

**Extraction prerequisites:**
- Replace Megaplan Store dependency with Arnold-neutral persistence contract before extraction.

**Tests / gates:**
- none-yet: editorial operations are Store-backed Megaplan infrastructure

---

#### megaplan/forms
**Disposition:** `megaplan-plugin` | **Granularity:** `directory` | **Target:** megaplan/pipelines/megaplan/forms

**Reason:** `forms/` mixes reusable creative-form data types (`__init__.py`, `stance.py`) with Megaplan-specific form implementations (`provocations.py`, `directors_notes.py`, `poem.py`, `joke.py`) that import from `megaplan._core`, `megaplan.types`, `megaplan.audits.robustness`, and `megaplan.forms`. NOTE: `__init__.py` (pure creative-form data types) and `stance.py` (generic stance validation helpers) have zero megaplan imports and are reusable Arnold substrate candidates, but remain under this directory row until the package can be split.

**Blockers:**
- Provocations and directors_notes import Megaplan creative_mode, latest_plan_path, robustness checks, and PlanState.
- Form implementations (poem, joke, provocations) are Megaplan creative pipeline content.

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| creative_forms_megaplan | megaplan | ['provocations', 'directors_notes', 'creative_form_id', 'select_active_checks'] |

**Extraction prerequisites:**
- Separate Arnold-neutral creative form types from Megaplan-specific form implementations before extraction.

**Tests / gates:**
- none-yet: forms mix reusable types with Megaplan-specific creative pipeline content

---

#### megaplan/receipts
**Disposition:** `megaplan-plugin` | **Granularity:** `directory` | **Target:** megaplan/pipelines/megaplan/receipts

**Reason:** `receipts/` contains Megaplan receipt construction, drift detection, query, and reporting that imports from `megaplan._core` (atomic_write_json, sha256_file, load_plan, resolve_plan_dir), `megaplan.receipts.*`, and references `.megaplan/` paths. NOTE: `canonical.py` (generic prompt canonicalization) and `extractors.py` (generic phase-metric extractors) have zero megaplan imports and are reusable Arnold substrate candidates, but remain under this directory row until the package can be split.

**Blockers:**
- Receipt construction depends on Megaplan phase conventions, plan-dir layout, and state loading.
- Drift detection and reporting reference `.megaplan/**` paths.
- Schema imports `megaplan._core.sha256_file`.

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| receipts | megaplan | ['Receipt', 'receipt_id', 'upstream_artifact_hashes', 'prompt_hash_canonical', 'drift_detection', 'receipt_report'] |

**Extraction prerequisites:**
- Replace Megaplan state/plan-dir coupling with Arnold-neutral receipt infrastructure before extraction.

**Tests / gates:**
- none-yet: receipt infrastructure is coupled to Megaplan plan-state conventions

---

#### megaplan/tickets
**Disposition:** `megaplan-plugin` | **Granularity:** `directory` | **Target:** megaplan/pipelines/megaplan/tickets

**Reason:** `tickets/` contains Megaplan ticket operations (create, edit, search, link, dismiss) that import from `megaplan.schemas`, `megaplan.store`, `megaplan.artifacts`, and use `.megaplan/tickets/` paths. NOTE: `identity.py` (generic git-based codebase identity helpers) has zero megaplan imports and is a reusable Arnold substrate candidate, but remains under this directory row until the package can be split.

**Blockers:**
- Ticket core imports Megaplan Store protocol and Ticket/TicketEpicLink schemas.
- File operations use `.megaplan/tickets/` paths and Megaplan artifact slug conventions.
- Registry references Megaplan known-repos paths.

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| tickets | megaplan | ['Ticket', 'TicketEpicLink', 'create_ticket', 'list_tickets', 'search_tickets', 'ticket_file_path', 'tickets_dir'] |

**String policy:**
| Category | Owner | Policy |
| --- | --- | --- |
| megaplan_paths | megaplan | Ticket storage under `.megaplan/tickets/` is plugin-local. |

**Extraction prerequisites:**
- Replace Megaplan Store coupling and `.megaplan/tickets/` path conventions with Arnold-neutral ticket infrastructure before extraction.

**Tests / gates:**
- none-yet: ticket infrastructure is Megaplan Store-backed

---

#### megaplan/calibration
**Disposition:** `megaplan-plugin` | **Granularity:** `directory` | **Target:** megaplan/pipelines/megaplan/calibration

**Reason:** `calibration/` contains the M5-cal calibration ledger (CapabilityClaim, EvaluandRef, ModelIdentity, RouteSuggestion, tier projection, decay/exploration) that defines Megaplan-specific calibration data types and query policy. While the `ledger.py` dataclasses are clean, the package as a whole is Megaplan calibration infrastructure.

**Blockers:**
- CapabilityClaim and EvaluandRef are Megaplan calibration concepts tied to evaluand events and tier routing.
- RouteSuggestion and tier projection are wired to Megaplan calibration query policy.
- Experiments module references Megaplan calibration conventions.

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| calibration | megaplan | ['CapabilityClaim', 'EvaluandRef', 'ModelIdentity', 'RouteSuggestion', 'project_tier_models', 'aggregate_weighted_tier', 'half_life_weight'] |

**Extraction prerequisites:**
- Decouple calibration ledger from Megaplan evaluand-event conventions and tier-routing policy before extraction.

**Tests / gates:**
- none-yet: calibration is Megaplan tier-routing infrastructure

---

#### megaplan/schemas/runtime.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/pipelines/megaplan/schemas/runtime.py

**Reason:** "JSON schema definitions for megaplan step outputs" per docstring. Contains Megaplan-specific schemas (STANCE_SCHEMA, STOP_SIGNAL_SCHEMA, PREP_RESEARCH_*, TOKEN_USAGE_SCHEMA, critique-evaluator schema, execution schemas), the SCHEMAS registry, and get_execution_schema_key which lazy-imports megaplan.forms and megaplan._core. The strict_schema helper is generic, but the file as a whole is Megaplan step-output schema infrastructure.

**Blockers:**
- strict_schema is a generic schema hardening helper that could be Arnold-shared-leaf; currently interleaved with Megaplan schemas.
- get_execution_schema_key lazy-imports megaplan.forms and megaplan._core, tying schema resolution to Megaplan mode infrastructure.

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| megaplan-schemas | megaplan | ['STANCE_SCHEMA', 'STOP_SIGNAL_SCHEMA', 'PREP_RESEARCH_AREA_SCHEMA', 'PREP_RESEARCH_FINDING_SCHEMA', 'TOKEN_USAGE_SCHEMA', 'SCHEMAS', 'get_execution_schema_key', 'strict_schema'] |

**Extraction prerequisites:**
- Extract strict_schema to Arnold-shared-leaf before splitting runtime.py.

**Tests / gates:**
- none-yet: runtime schemas exercised through pipeline integration tests

---

#### megaplan/schemas/sprint1.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/pipelines/megaplan/schemas/sprint1.py

**Reason:** "Sprint 1 storage extensions and Plan compatibility models." Directly imports megaplan.types (ActivePhase, PlanConfig, PlanMeta, PlanState, SessionInfo, validate_plan_current_state, etc.) and defines Megaplan-specific storage extensions: MigrationRun, PlanArtifact, ScheduledJob, CloudRun, ControlMessage models with Megaplan-domain enums (ControlIntent, ProgressEventKind, WorkerKind, etc.). Deeply tied to Megaplan plan lifecycle vocabulary.

**Blockers:**
- Imports megaplan.types for core state/enum vocabulary.
- Defines Megaplan-specific enums: ControlIntent, ProgressEventKind, WorkerKind, CloudRunStatus, PlanArtifactRole.

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| megaplan-plan-storage | megaplan | ['MigrationRun', 'PlanArtifact', 'ScheduledJob', 'CloudRun', 'ControlMessage', 'MigrationPhase', 'PlanArtifactKind', 'PlanArtifactRole', 'WorkerKind', 'ScheduledJobStatus', 'CloudRunStatus', 'CloudRunOperation', 'ControlIntent', 'ProgressEventKind', 'AutomationActorKind'] |

**Extraction prerequisites:**
- Decouple from megaplan.types state vocabulary before extraction.

**Tests / gates:**
- none-yet: sprint1 models exercised through Store integration tests

---

#### megaplan/__init__.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/__init__.py

**Reason:** Lazy-loading public API facade that maps all public Megaplan symbols (PlanState, STATE_* constants, handler functions, CLI entry points, utility functions) to their implementing modules via importlib. Hardcodes Megaplan-specific state constants, handler names, and SDK vocabulary. This is the public surface of the Megaplan SDK.

**Blockers:**
- Hardcodes Megaplan state constants (STATE_INITIALIZED through STATE_CANCELLED), handler names, and CLI entry points.
- Lazy-loading map ties public API to Megaplan-specific module structure.

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| megaplan-api | megaplan | ['STATE_INITIALIZED', 'STATE_PLANNED', 'STATE_CRITIQUED', 'STATE_GATED', 'STATE_FINALIZED', 'STATE_EXECUTED', 'STATE_REVIEWED', 'STATE_DONE', 'STATE_ABORTED', 'STATE_FAILED', 'STATE_BLOCKED', 'STATE_PAUSED', 'STATE_CANCELLED', 'PlanState', 'PlanConfig', 'PlanMeta', 'StepResponse', 'RunOutcome', 'handle_init', 'handle_plan', 'handle_critique', 'handle_revise', 'handle_gate', 'handle_finalize', 'handle_execute', 'handle_review', 'apply_transition', 'main', 'cli_entry'] |

---

#### megaplan/__main__.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/__main__.py

**Reason:** Trivial entry point: imports megaplan.cli.main and calls sys.exit(main()). Standard python -m megaplan entry. Megaplan-specific by virtue of invoking the Megaplan CLI.

**Blockers:**
- _(none)_


---

#### megaplan/types.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/types.py

**Reason:** "Type definitions, constants, and exceptions for megaplan." Defines all Megaplan state constants (STATE_INITIALIZED through STATE_TIEBREAKER_READY), PlanCurrentState literal type, PlanConfig/PlanMeta/PlanState TypedDicts, FlagRecord, CliError, and related domain types. This is the foundational type vocabulary that all Megaplan modules depend on. All symbols are Megaplan-specific plan lifecycle concepts.

**Blockers:**
- Defines Megaplan state-machine vocabulary consumed by every other Megaplan module.
- PlanConfig, PlanMeta, PlanState TypedDicts encode Megaplan plan structure.

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| megaplan-types | megaplan | ['STATE_INITIALIZED', 'STATE_PREPPED', 'STATE_PLANNED', 'STATE_CRITIQUED', 'STATE_GATED', 'STATE_FINALIZED', 'STATE_EXECUTED', 'STATE_REVIEWED', 'STATE_DONE', 'STATE_ABORTED', 'STATE_FAILED', 'STATE_BLOCKED', 'STATE_PAUSED', 'STATE_CANCELLED', 'STATE_AWAITING_PR_MERGE', 'STATE_AWAITING_HUMAN', 'STATE_TIEBREAKER_PENDING', 'STATE_TIEBREAKER_READY', 'PlanCurrentState', 'PlanConfig', 'PlanMeta', 'PlanState', 'FlagRecord', 'CliError', 'TERMINAL_STATES', 'ROBUSTNESS_LEVELS'] |

---

#### megaplan/flags.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/flags.py

**Reason:** Flag record management: next_flag_number, make_flag_id, resolve_severity, normalize_flag_record, update_flags_after_critique, update_flags_after_revise. Imports from megaplan._core (load/save_flag_registry), megaplan.orchestration.critique_status, and megaplan.types (FlagRecord, FlagRegistry). Megaplan-specific flag lifecycle infrastructure.

**Blockers:**
- Imports megaplan._core and megaplan.orchestration for flag registry I/O and critique integration.
- Flag IDs follow Megaplan convention (FLAG-001 format).

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| megaplan-flags | megaplan | ['next_flag_number', 'make_flag_id', 'resolve_severity', 'normalize_flag_record', 'update_flags_after_critique', 'update_flags_after_revise', 'FlagRecord', 'FlagRegistry'] |

---

#### megaplan/control.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/control.py

**Reason:** "Discord-independent control-plane target resolution and processing." Imports from megaplan._core, megaplan.auto, megaplan.control_interface, megaplan.handlers, megaplan.planning, megaplan.orchestration, megaplan.schemas, megaplan.store, megaplan.types. Orchestrates sprint runs, resume, and gate approval/rejection via the control interface. Deeply Megaplan-specific control-plane infrastructure.

**Blockers:**
- Imports across megaplan._core, megaplan.auto, megaplan.control_interface, megaplan.handlers, megaplan.planning, megaplan.orchestration, megaplan.schemas, megaplan.store, megaplan.types.
- Encodes Megaplan control intents (run_sprint, resume_plan, approve_gate, reject_gate).

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| megaplan-control | megaplan | ['SUPPORTED_CONTROL_INTENTS', 'GATE_CONTROL_INTENTS', 'process_control_message', 'claim_next_pending', 'mark_processed'] |

---

#### megaplan/artifacts.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/artifacts.py

**Reason:** "Shared local artifact helpers for files under .megaplan/." Provides markdown artifact parsing, slugify, artifact_dir, iter_markdown_artifacts, filter_keyword_artifacts. Zero megaplan code imports (stdlib + yaml only), but hardcodes .megaplan/ path convention in artifact_dir and iter_markdown_artifacts. The .megaplan/ path convention makes this Megaplan-specific despite the generic helper implementations.

**Blockers:**
- Hardcodes .megaplan/ path convention in artifact_dir and iter_markdown_artifacts.
- slugify and markdown helpers are generic but live under the .megaplan/ namespace.

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| megaplan-artifacts | megaplan | ['MarkdownArtifact', 'slugify', 'artifact_dir', 'parse_markdown_artifact', 'markdown_body', 'write_markdown_artifact', 'iter_markdown_artifacts', 'artifact_title', 'filter_keyword_artifacts', 'make_snippet'] |

**String policy:**
| Category | Owner | Policy |
| --- | --- | --- |
| megaplan_paths | megaplan | Hardcoded .megaplan/ path convention in artifact_dir. |

**Extraction prerequisites:**
- Replace .megaplan/ path convention with configurable artifact root before extraction.

---

#### megaplan/auto_escalation.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/auto_escalation.py

**Reason:** "Failure-category policy for execute-stage tier escalation." Pure-function module that classifies PhaseResult exit information into FailureCategory and extracts failing task IDs. Imports from megaplan.auto (DEFAULT_MAX_BLOCKED_RETRIES) and megaplan.orchestration.phase_result (BlockedTask, Deviation). Megaplan-specific auto-driver escalation policy.

**Blockers:**
- Imports from megaplan.auto and megaplan.orchestration.phase_result.
- FailureCategory enum values (blocked_by_prereq, blocked_by_quality, timeout, context_exhausted, internal_error, external_error) are Megaplan execute-stage concepts.

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| megaplan-escalation | megaplan | ['FailureCategory', 'classify_failure', 'DRIFT_KINDS', 'DRIFT_TOKENS'] |

---

#### megaplan/blocker_recovery.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/blocker_recovery.py

**Reason:** "Shared blocker recovery evaluation for execute/auto/operator commands." Evaluates blocked tasks against quality resolutions and user-action resolutions to determine recovery behavior. Imports from megaplan.orchestration.phase_result, megaplan.quality_resolutions, megaplan.resolution_contract, megaplan.user_actions. Megaplan-specific blocker recovery policy.

**Blockers:**
- Imports megaplan.quality_resolutions, megaplan.resolution_contract, megaplan.user_actions.
- Blocker recovery evaluation is tightly coupled to Megaplan resolution models.

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| megaplan-blocker-recovery | megaplan | ['PREREQUISITE', 'QUALITY', 'evaluate_blocked_tasks', 'evaluate_quality_blocked_tasks'] |

---

#### megaplan/briefs.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/briefs.py

**Reason:** "Canonical local brief/epic artifact helpers." Provides brief/epic directory management, listing, creation, and search under .megaplan/briefs/ and .megaplan/epics/. Imports from megaplan.artifacts (artifact_dir, slugify, iter_markdown_artifacts, etc.). Megaplan-specific brief/epic artifact conventions tied to .megaplan/ path.

**Blockers:**
- Hardcodes .megaplan/briefs/ and .megaplan/epics/ path conventions.
- Imports megaplan.artifacts for shared artifact helpers.

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| megaplan-briefs | megaplan | ['slugify', 'briefs_dir', 'epics_dir', 'list_briefs', 'list_epics', 'create_brief', 'search_briefs', 'brief_path', 'epic_path'] |

**String policy:**
| Category | Owner | Policy |
| --- | --- | --- |
| megaplan_paths | megaplan | Brief/epic storage under .megaplan/briefs/ and .megaplan/epics/. |

---

#### megaplan/run_outcome.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/run_outcome.py

**Reason:** "SDK-owned run outcome vocabulary and reducer mappings." Defines RunOutcome enum (SUCCEEDED, FAILED, ESCALATED, BLOCKED, AWAITING_HUMAN) and RunResultMetadata dataclass with BatchOutcome→RunResultMetadata mapping. Imports from megaplan.execute._binding.reducer (BatchOutcome). Megaplan-specific run outcome vocabulary, though the enum values are domain-neutral the mapping binds to Megaplan's execute binding.

**Blockers:**
- Imports megaplan.execute._binding.reducer for BatchOutcome mapping.
- RunOutcome enum is domain-neutral in vocabulary but bound to Megaplan execution pipeline.

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| megaplan-run-outcome | megaplan | ['RunOutcome', 'RunResultMetadata', 'run_metadata_from_batch_outcome', 'run_outcome_from_batch_outcome'] |

**Extraction prerequisites:**
- Decouple from megaplan.execute._binding.reducer BatchOutcome mapping before extraction.

---

#### megaplan/resolution_contract.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/resolution_contract.py

**Reason:** "Shared helpers for user-action and quality resolution contracts." Centralizes resolution semantics (valid states, behavior classification, event aggregation) shared between disk-backed and memory-backed user-action pipelines and quality-resolution pipeline. Lazy-imports megaplan._core.io (now_utc) and megaplan.types (CliError) for timestamp and error handling. The resolution contract logic is domain-neutral, but the lazy imports tie it to Megaplan infrastructure.

**Blockers:**
- Lazy-imports megaplan._core.io.now_utc and megaplan.types.CliError for validation helpers.
- Resolution state sets (SUPPORTED_USER_ACTION_RESOLUTION_STATES, SUPPORTED_QUALITY_RESOLUTION_STATES) encode Megaplan domain concepts.

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| megaplan-resolution-contract | megaplan | ['FALLBACK', 'HARD_BLOCK', 'OMIT', 'ADVANCE_WITH_DEBT', 'RERUN_REQUIRED', 'UNRESOLVED', 'RESOLVED', 'DISK_SOURCE', 'MEMORY_SOURCE', 'classify_resolution_behavior', 'latest_events_by_key', 'build_base_resolution_event'] |

**Extraction prerequisites:**
- Replace megaplan._core.io.now_utc and megaplan.types.CliError lazy imports with Arnold equivalents before extraction.

---

#### megaplan/resolutions.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/resolutions.py

**Reason:** "Shared resolution contract helpers for user action resolutions." Disk-source compatibility wrapper around megaplan.resolution_contract. Imports from megaplan._core.io (atomic_write_json, read_json) and megaplan.types (CliError). Re-exports shared constants from megaplan.resolution_contract. Megaplan-specific disk I/O for user action resolutions.

**Blockers:**
- Imports megaplan._core.io and megaplan.types for disk I/O and error handling.
- Resolution file path is Megaplan-local (.megaplan/plan_dir/user_action_resolutions.json).

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| megaplan-resolutions | megaplan | ['user_action_resolutions_file', 'read_user_action_resolutions', 'write_user_action_resolution', 'clear_user_action_resolutions'] |

**String policy:**
| Category | Owner | Policy |
| --- | --- | --- |
| megaplan_paths | megaplan | Resolution storage under plan directory (.megaplan/ plans). |

---

#### megaplan/quality_resolutions.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/quality_resolutions.py

**Reason:** "Resolution model for quality_gate_resolutions." Durable append-only events in state.json under meta.quality_gate_resolutions. Imports from megaplan.resolution_contract for shared resolution semantics. Megaplan-specific quality gate resolution model tied to state.json schema.

**Blockers:**
- Imports megaplan.resolution_contract for shared resolution semantics.
- Quality resolution events stored in state.json under Megaplan-specific meta.quality_gate_resolutions path.

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| megaplan-quality-resolutions | megaplan | ['latest_quality_resolutions', 'classify_quality_resolution_behavior', 'validate_quality_resolution_event', 'ACCEPTED_WITH_DEBT', 'FIXED'] |

---

#### megaplan/user_actions.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/user_actions.py

**Reason:** "Resolution model for finalized user-action prerequisites." Memory-source compatibility wrapper around megaplan.resolution_contract. Imports from megaplan.resolution_contract for shared resolution semantics. Megaplan-specific user action resolution model for operator-driven prerequisite resolution.

**Blockers:**
- Imports megaplan.resolution_contract for shared resolution semantics.
- User action resolution model is tied to Megaplan prerequisite workflow.

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| megaplan-user-actions | megaplan | ['effective_resolutions', 'validate_user_action_event', 'resolve_user_action', 'USER_ACTION_RESOLUTION_KINDS'] |

---

#### megaplan/agent
**Disposition:** `megaplan-plugin` | **Granularity:** `directory` | **Target:** megaplan/agent (retained)

**Reason:** The vendored Hermes agent subpackage is a Megaplan product surface (CLI, gateway, environments, tools, skills, tests). It carries the Hermes brand, Megaplan-specific runtime wiring, and user-facing product configuration.

**Blockers:**
- Vendored hermes-agent package with original top-level import assumptions.
- Agent runtime contracts interleave with Megaplan fan-out and state models.
- Platform adapters and skill scripts carry Hermes/Megaplan-branded paths and defaults.

---

#### megaplan/pipelines
**Disposition:** `megaplan-plugin` | **Granularity:** `directory` | **Target:** megaplan/pipelines (retained)

**Reason:** Pipeline instances (creative, doc, jokes, planning, select-tournament, epic_blitz, writing_panel_strict) are Megaplan product configurations with phase prompts, step definitions, and planning-mode vocabulary.

**Blockers:**
- Pipeline definitions embed Megaplan phase strings and handler routing.
- Prompt modules carry Megaplan-specific instruction wording and schemas.

---

#### megaplan/resident
**Disposition:** `megaplan-plugin` | **Granularity:** `directory` | **Target:** megaplan/resident (retained)

**Reason:** Resident Discord orchestration package is Megaplan-specific, binding Discord platform adapters, Megaplan profiles, cloud deployment, and tool registry to resident runtime seams.

**Blockers:**
- Resident profile/config carries Megaplan tenant and deployment defaults.
- Tool schemas and registry interleave with Megaplan runtime capabilities.

---

#### megaplan/tests/agentic
**Disposition:** `megaplan-plugin` | **Granularity:** `directory` | **Target:** megaplan/tests/agentic (retained)

**Reason:** Agentic test infrastructure imports Megaplan checks, adapters, and run harness for the Hermes agent test suite.

**Blockers:**
- Test adapter imports megaplan.agent runtime for agent-in-the-loop testing.
- megaplan_checks.py carries Megaplan-specific verification helpers.

---

#### megaplan/_pipeline/_forward_m2_m3.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/pipelines/megaplan/_forward_m2_m3.py

**Reason:** Megaplan-specific pipeline policy — planning stages, handler steps, demos, or Megaplan-branded configuration.

**Blockers:**
- Carries Megaplan phase vocabulary, handler references, or planning defaults.
- May reference megaplan.planning, megaplan.handlers, or megaplan.orchestration.

---

#### megaplan/_pipeline/behavioral_manifest.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/pipelines/megaplan/behavioral_manifest.py

**Reason:** Megaplan-specific pipeline policy — planning stages, handler steps, demos, or Megaplan-branded configuration.

**Blockers:**
- Carries Megaplan phase vocabulary, handler references, or planning defaults.
- May reference megaplan.planning, megaplan.handlers, or megaplan.orchestration.

---

#### megaplan/_pipeline/defaults.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/pipelines/megaplan/defaults.py

**Reason:** Megaplan-specific pipeline policy — planning stages, handler steps, demos, or Megaplan-branded configuration.

**Blockers:**
- Carries Megaplan phase vocabulary, handler references, or planning defaults.
- May reference megaplan.planning, megaplan.handlers, or megaplan.orchestration.

---

#### megaplan/_pipeline/demo_judges.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/pipelines/megaplan/demo_judges.py

**Reason:** Megaplan-specific pipeline policy — planning stages, handler steps, demos, or Megaplan-branded configuration.

**Blockers:**
- Carries Megaplan phase vocabulary, handler references, or planning defaults.
- May reference megaplan.planning, megaplan.handlers, or megaplan.orchestration.

---

#### megaplan/_pipeline/demos/doc_critique.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/pipelines/megaplan/demos/doc_critique.py

**Reason:** Megaplan-specific pipeline policy — planning stages, handler steps, demos, or Megaplan-branded configuration.

**Blockers:**
- Carries Megaplan phase vocabulary, handler references, or planning defaults.
- May reference megaplan.planning, megaplan.handlers, or megaplan.orchestration.

---

#### megaplan/_pipeline/demos/tournament.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/pipelines/megaplan/demos/tournament.py

**Reason:** Megaplan-specific pipeline policy — planning stages, handler steps, demos, or Megaplan-branded configuration.

**Blockers:**
- Carries Megaplan phase vocabulary, handler references, or planning defaults.
- May reference megaplan.planning, megaplan.handlers, or megaplan.orchestration.

---

#### megaplan/_pipeline/eval_judge_wrapper.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/pipelines/megaplan/eval_judge_wrapper.py

**Reason:** Megaplan-specific pipeline policy — planning stages, handler steps, demos, or Megaplan-branded configuration.

**Blockers:**
- Carries Megaplan phase vocabulary, handler references, or planning defaults.
- May reference megaplan.planning, megaplan.handlers, or megaplan.orchestration.

---

#### megaplan/_pipeline/judge_manifest.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/pipelines/megaplan/judge_manifest.py

**Reason:** Megaplan-specific pipeline policy — planning stages, handler steps, demos, or Megaplan-branded configuration.

**Blockers:**
- Carries Megaplan phase vocabulary, handler references, or planning defaults.
- May reference megaplan.planning, megaplan.handlers, or megaplan.orchestration.

---

#### megaplan/_pipeline/judge_manifest_discovery.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/pipelines/megaplan/judge_manifest_discovery.py

**Reason:** Megaplan-specific pipeline policy — planning stages, handler steps, demos, or Megaplan-branded configuration.

**Blockers:**
- Carries Megaplan phase vocabulary, handler references, or planning defaults.
- May reference megaplan.planning, megaplan.handlers, or megaplan.orchestration.

---

#### megaplan/_pipeline/override.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/pipelines/megaplan/override.py

**Reason:** Megaplan-specific pipeline policy — planning stages, handler steps, demos, or Megaplan-branded configuration.

**Blockers:**
- Carries Megaplan phase vocabulary, handler references, or planning defaults.
- May reference megaplan.planning, megaplan.handlers, or megaplan.orchestration.

---

#### megaplan/_pipeline/planning.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/pipelines/megaplan/planning.py

**Reason:** Megaplan-specific pipeline policy — planning stages, handler steps, demos, or Megaplan-branded configuration.

**Blockers:**
- Carries Megaplan phase vocabulary, handler references, or planning defaults.
- May reference megaplan.planning, megaplan.handlers, or megaplan.orchestration.

---

#### megaplan/_pipeline/planning_bindings.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/pipelines/megaplan/planning_bindings.py

**Reason:** Megaplan-specific pipeline policy — planning stages, handler steps, demos, or Megaplan-branded configuration.

**Blockers:**
- Carries Megaplan phase vocabulary, handler references, or planning defaults.
- May reference megaplan.planning, megaplan.handlers, or megaplan.orchestration.

---

#### megaplan/_pipeline/preflight.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/pipelines/megaplan/preflight.py

**Reason:** Megaplan-specific pipeline policy — planning stages, handler steps, demos, or Megaplan-branded configuration.

**Blockers:**
- Carries Megaplan phase vocabulary, handler references, or planning defaults.
- May reference megaplan.planning, megaplan.handlers, or megaplan.orchestration.

---

#### megaplan/_pipeline/profile.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/pipelines/megaplan/profile.py

**Reason:** Megaplan-specific pipeline policy — planning stages, handler steps, demos, or Megaplan-branded configuration.

**Blockers:**
- Carries Megaplan phase vocabulary, handler references, or planning defaults.
- May reference megaplan.planning, megaplan.handlers, or megaplan.orchestration.

---

#### megaplan/_pipeline/prompts.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/pipelines/megaplan/prompts.py

**Reason:** Megaplan-specific pipeline policy — planning stages, handler steps, demos, or Megaplan-branded configuration.

**Blockers:**
- Carries Megaplan phase vocabulary, handler references, or planning defaults.
- May reference megaplan.planning, megaplan.handlers, or megaplan.orchestration.

---

#### megaplan/_pipeline/resume.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/pipelines/megaplan/resume.py

**Reason:** Megaplan-specific pipeline policy — planning stages, handler steps, demos, or Megaplan-branded configuration.

**Blockers:**
- Carries Megaplan phase vocabulary, handler references, or planning defaults.
- May reference megaplan.planning, megaplan.handlers, or megaplan.orchestration.

---

#### megaplan/_pipeline/run_cli.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/pipelines/megaplan/run_cli.py

**Reason:** Megaplan-specific pipeline policy — planning stages, handler steps, demos, or Megaplan-branded configuration.

**Blockers:**
- Carries Megaplan phase vocabulary, handler references, or planning defaults.
- May reference megaplan.planning, megaplan.handlers, or megaplan.orchestration.

---

#### megaplan/_pipeline/runtime.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/pipelines/megaplan/runtime.py

**Reason:** Megaplan-specific pipeline policy — planning stages, handler steps, demos, or Megaplan-branded configuration.

**Blockers:**
- Carries Megaplan phase vocabulary, handler references, or planning defaults.
- May reference megaplan.planning, megaplan.handlers, or megaplan.orchestration.

---

#### megaplan/_pipeline/stages/critique.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/pipelines/megaplan/stages/critique.py

**Reason:** Megaplan-specific pipeline policy — planning stages, handler steps, demos, or Megaplan-branded configuration.

**Blockers:**
- Carries Megaplan phase vocabulary, handler references, or planning defaults.
- May reference megaplan.planning, megaplan.handlers, or megaplan.orchestration.

---

#### megaplan/_pipeline/stages/execute.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/pipelines/megaplan/stages/execute.py

**Reason:** Megaplan-specific pipeline policy — planning stages, handler steps, demos, or Megaplan-branded configuration.

**Blockers:**
- Carries Megaplan phase vocabulary, handler references, or planning defaults.
- May reference megaplan.planning, megaplan.handlers, or megaplan.orchestration.

---

#### megaplan/_pipeline/stages/finalize.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/pipelines/megaplan/stages/finalize.py

**Reason:** Megaplan-specific pipeline policy — planning stages, handler steps, demos, or Megaplan-branded configuration.

**Blockers:**
- Carries Megaplan phase vocabulary, handler references, or planning defaults.
- May reference megaplan.planning, megaplan.handlers, or megaplan.orchestration.

---

#### megaplan/_pipeline/stages/gate.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/pipelines/megaplan/stages/gate.py

**Reason:** Megaplan-specific pipeline policy — planning stages, handler steps, demos, or Megaplan-branded configuration.

**Blockers:**
- Carries Megaplan phase vocabulary, handler references, or planning defaults.
- May reference megaplan.planning, megaplan.handlers, or megaplan.orchestration.

---

#### megaplan/_pipeline/stages/handler_step.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/pipelines/megaplan/stages/handler_step.py

**Reason:** Megaplan-specific pipeline policy — planning stages, handler steps, demos, or Megaplan-branded configuration.

**Blockers:**
- Carries Megaplan phase vocabulary, handler references, or planning defaults.
- May reference megaplan.planning, megaplan.handlers, or megaplan.orchestration.

---

#### megaplan/_pipeline/stages/inprocess_step.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/pipelines/megaplan/stages/inprocess_step.py

**Reason:** Megaplan-specific pipeline policy — planning stages, handler steps, demos, or Megaplan-branded configuration.

**Blockers:**
- Carries Megaplan phase vocabulary, handler references, or planning defaults.
- May reference megaplan.planning, megaplan.handlers, or megaplan.orchestration.

---

#### megaplan/_pipeline/stages/plan.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/pipelines/megaplan/stages/plan.py

**Reason:** Megaplan-specific pipeline policy — planning stages, handler steps, demos, or Megaplan-branded configuration.

**Blockers:**
- Carries Megaplan phase vocabulary, handler references, or planning defaults.
- May reference megaplan.planning, megaplan.handlers, or megaplan.orchestration.

---

#### megaplan/_pipeline/stages/prep.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/pipelines/megaplan/stages/prep.py

**Reason:** Megaplan-specific pipeline policy — planning stages, handler steps, demos, or Megaplan-branded configuration.

**Blockers:**
- Carries Megaplan phase vocabulary, handler references, or planning defaults.
- May reference megaplan.planning, megaplan.handlers, or megaplan.orchestration.

---

#### megaplan/_pipeline/stages/review.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/pipelines/megaplan/stages/review.py

**Reason:** Megaplan-specific pipeline policy — planning stages, handler steps, demos, or Megaplan-branded configuration.

**Blockers:**
- Carries Megaplan phase vocabulary, handler references, or planning defaults.
- May reference megaplan.planning, megaplan.handlers, or megaplan.orchestration.

---

#### megaplan/_pipeline/stages/revise.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/pipelines/megaplan/stages/revise.py

**Reason:** Megaplan-specific pipeline policy — planning stages, handler steps, demos, or Megaplan-branded configuration.

**Blockers:**
- Carries Megaplan phase vocabulary, handler references, or planning defaults.
- May reference megaplan.planning, megaplan.handlers, or megaplan.orchestration.

---

#### megaplan/_pipeline/stages/tiebreaker.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/pipelines/megaplan/stages/tiebreaker.py

**Reason:** Megaplan-specific pipeline policy — planning stages, handler steps, demos, or Megaplan-branded configuration.

**Blockers:**
- Carries Megaplan phase vocabulary, handler references, or planning defaults.
- May reference megaplan.planning, megaplan.handlers, or megaplan.orchestration.

---

#### megaplan/_pipeline/steps/agent.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/pipelines/megaplan/steps/agent.py

**Reason:** Megaplan-specific pipeline policy — planning stages, handler steps, demos, or Megaplan-branded configuration.

**Blockers:**
- Carries Megaplan phase vocabulary, handler references, or planning defaults.
- May reference megaplan.planning, megaplan.handlers, or megaplan.orchestration.

---

#### megaplan/_pipeline/steps/human_gate.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/pipelines/megaplan/steps/human_gate.py

**Reason:** Megaplan-specific pipeline policy — planning stages, handler steps, demos, or Megaplan-branded configuration.

**Blockers:**
- Carries Megaplan phase vocabulary, handler references, or planning defaults.
- May reference megaplan.planning, megaplan.handlers, or megaplan.orchestration.

---

#### megaplan/_pipeline/steps/panel.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/pipelines/megaplan/steps/panel.py

**Reason:** Megaplan-specific pipeline policy — planning stages, handler steps, demos, or Megaplan-branded configuration.

**Blockers:**
- Carries Megaplan phase vocabulary, handler references, or planning defaults.
- May reference megaplan.planning, megaplan.handlers, or megaplan.orchestration.

---

#### megaplan/cli/__init__.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/cli/__init__.py (retained)

**Reason:** CLI infrastructure module for Megaplan command-line interface.

**Blockers:**
- May import megaplan planning, profiles, or handler subpackages.

---

#### megaplan/cli/__main__.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/cli/__main__.py (retained)

**Reason:** CLI infrastructure module for Megaplan command-line interface.

**Blockers:**
- May import megaplan planning, profiles, or handler subpackages.

---

#### megaplan/cli/feedback.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/cli/feedback.py (retained)

**Reason:** CLI infrastructure module for Megaplan command-line interface.

**Blockers:**
- May import megaplan planning, profiles, or handler subpackages.

---

#### megaplan/cli/parser.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/cli/parser.py (retained)

**Reason:** CLI infrastructure module for Megaplan command-line interface.

**Blockers:**
- May import megaplan planning, profiles, or handler subpackages.

---

#### megaplan/cli/resolutions.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/cli/resolutions.py (retained)

**Reason:** CLI infrastructure module for Megaplan command-line interface.

**Blockers:**
- May import megaplan planning, profiles, or handler subpackages.

---

#### megaplan/cli/roots.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/cli/roots.py (retained)

**Reason:** CLI infrastructure module for Megaplan command-line interface.

**Blockers:**
- May import megaplan planning, profiles, or handler subpackages.

---

#### megaplan/cli/setup.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/cli/setup.py (retained)

**Reason:** CLI infrastructure module for Megaplan command-line interface.

**Blockers:**
- May import megaplan planning, profiles, or handler subpackages.

---

#### megaplan/cli/skills.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/cli/skills.py (retained)

**Reason:** CLI infrastructure module for Megaplan command-line interface.

**Blockers:**
- May import megaplan planning, profiles, or handler subpackages.

---

#### megaplan/runtime/__init__.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/runtime/__init__.py (retained)

**Reason:** Runtime infrastructure with Megaplan coupling (budget/capability/lease/doc/key primitives).

**Blockers:**
- Imports from megaplan types, schemas, or _pipeline infrastructure.
- May reference Hermes or Megaplan-specific providers and paths.

---

#### megaplan/runtime/budget_authority.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/runtime/budget_authority.py (retained)

**Reason:** Runtime infrastructure with Megaplan coupling (budget/capability/lease/doc/key primitives).

**Blockers:**
- Imports from megaplan types, schemas, or _pipeline infrastructure.
- May reference Hermes or Megaplan-specific providers and paths.

---

#### megaplan/runtime/capabilities.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/runtime/capabilities.py (retained)

**Reason:** Runtime infrastructure with Megaplan coupling (budget/capability/lease/doc/key primitives).

**Blockers:**
- Imports from megaplan types, schemas, or _pipeline infrastructure.
- May reference Hermes or Megaplan-specific providers and paths.

---

#### megaplan/runtime/capacity_lease.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/runtime/capacity_lease.py (retained)

**Reason:** Runtime infrastructure with Megaplan coupling (budget/capability/lease/doc/key primitives).

**Blockers:**
- Imports from megaplan types, schemas, or _pipeline infrastructure.
- May reference Hermes or Megaplan-specific providers and paths.

---

#### megaplan/runtime/doc_assembly.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/runtime/doc_assembly.py (retained)

**Reason:** Runtime infrastructure with Megaplan coupling (budget/capability/lease/doc/key primitives).

**Blockers:**
- Imports from megaplan types, schemas, or _pipeline infrastructure.
- May reference Hermes or Megaplan-specific providers and paths.

---

#### megaplan/runtime/key_pool.py
**Disposition:** `megaplan-plugin` | **Granularity:** `file` | **Target:** megaplan/runtime/key_pool.py (retained)

**Reason:** Runtime infrastructure with Megaplan coupling (budget/capability/lease/doc/key primitives).

**Blockers:**
- Imports from megaplan types, schemas, or _pipeline infrastructure.
- May reference Hermes or Megaplan-specific providers and paths.

---

### legacy-hold (18 rows)

#### megaplan/_pipeline/builder.py
**Disposition:** `legacy-hold` | **Granularity:** `file` | **Target:** arnold/pipeline/builder.py (after policy-free builder extraction)

**Reason:** The fluent builder still bakes in Megaplan control-flow defaults such as critique/finalize/tiebreaker edges, panel reviewer ordering, and human-gate resume semantics.

**Blockers:**
- `gate()` and `tiebreaker()` hardcode Megaplan recommendation labels and stage targets.
- `human_gate()` and panel helpers embed Megaplan resume/output conventions rather than a neutral builder contract.
- The builder imports Megaplan stage implementations such as `TiebreakerStep` and panel-specific step classes directly.

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| builder_stage_defaults | megaplan | ['critique', 'finalize', 'tiebreaker', 'proceed', 'iterate', 'escalate'] |

**String policy:**
| Category | Owner | Policy |
| --- | --- | --- |
| stage_defaults | megaplan | Leave current defaults plugin-local until a policy-free builder API exists. |

**Extraction prerequisites:**
- Define a builder API that accepts stage-policy defaults from the plugin instead of hardcoding them.

**Tests / gates:**
- none-yet: neutral builder extraction is blocked on policy-free defaults

---

#### megaplan/_core/workflow.py
**Disposition:** `legacy-hold` | **Granularity:** `file` | **Target:** megaplan/_core/workflow.py (pending split of neutral robustness helpers from Megaplan workflow vocabulary)

**Reason:** `_core/workflow.py` mixes potentially reusable robustness helpers with Megaplan workflow states, brief/resume text, plan-dir lookup, and shared workflow-state vocabulary.

**Blockers:**
- Imports and re-exports `STATE_*` planning lifecycle names plus `WORKFLOW` / `_ROBUSTNESS_OVERRIDES`.
- `intent_brief_reference()` and prompt helpers embed Megaplan brief/planning wording.
- `find_plan_dir`, `Store`, and progress-event helpers keep the file coupled to current Megaplan plan storage and resume semantics.

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| workflow_states | megaplan | ['initialized', 'prepped', 'planned', 'critiqued', 'gated', 'finalized', 'executed', 'done', 'awaiting_human_verify', 'tiebreaker_pending', 'tiebreaker_ready'] |

**String policy:**
| Category | Owner | Policy |
| --- | --- | --- |
| brief_resume_text | megaplan | Leave current brief/resume phrasing plugin-local until workflow helpers are separated. |

**Extraction prerequisites:**
- Split neutral robustness helpers into an Arnold-owned helper module before extracting any part of workflow state logic.
- Leave workflow transitions and resume/brief vocabulary on the Megaplan side.

**Tests / gates:**
- none-yet: avoid Arnold-core directory coverage until workflow-state and resume helpers are separated

---

#### megaplan/workers/__init__.py
**Disposition:** `legacy-hold` | **Granularity:** `file` | **Target:** megaplan/pipelines/megaplan/workers/__init__.py

**Reason:** `workers/__init__.py` is currently just a broad re-export surface over `_impl.py`, so it cannot move cleanly until the mixed worker runtime/policy helpers are split underneath it.

**Blockers:**
- It re-exports both extractable worker primitives and Megaplan-specific dispatch/prompt/session helpers from `_impl.py`.

**Import policy:**
- Allowed: `megaplan.workers._impl`

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| package_exports | megaplan | ['run_step_with_worker', 'resolve_agent_mode', 'run_codex_prep_step'] |

**String policy:**
| Category | Owner | Policy |
| --- | --- | --- |
| package_exports | megaplan | Hold this export surface in place until the underlying split is complete. |

**Extraction prerequisites:**
- Complete the `_impl.py` split first, then rebuild a narrower Arnold export surface only for neutral worker helpers.

**Tests / gates:**
- static-gate: do not create an Arnold export barrel until mixed `_impl.py` helpers are split

---

#### megaplan/store/capsule.py
**Disposition:** `legacy-hold` | **Granularity:** `file` | **Target:** megaplan/store/capsule.py

**Reason:** `store/capsule.py` combines generic content-addressed record helpers with Megaplan-specific behavioral-manifest, legal-coercion, and epic-export inventory coupling.

**Blockers:**
- `build_capsule()` depends on `collect_epic_export()` and therefore on the current epic row/artifact/blob inventory.
- Contract checks import `capsule_definition_identity_projection` and `is_legal_coercion` from `_pipeline` modules.
- The MIME type and record helpers are reusable, but the file currently mixes them with plan/export/runtime-specific capsule evidence policy.

**Import policy:**
- Allowed: `megaplan._core.canonical`, `megaplan._pipeline.behavioral_manifest`, `megaplan._pipeline.contracts`, `megaplan.schemas`, `megaplan.store.blob`, `megaplan.store.export`
- Forbidden: `megaplan.handlers`, `megaplan.cli`

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| capsule_records | arnold | ['CapsuleRecordRef', 'CapsuleWriteResult', 'capsule_record_blob_id', 'put_capsule_record'] |
| capsule_policy | megaplan | ['collect_epic_export', 'capsule_definition_identity_projection', 'is_legal_coercion'] |

**String policy:**
| Category | Owner | Policy |
| --- | --- | --- |
| content_types | megaplan | Rename the Megaplan-branded Capsule MIME type when the reusable record helpers are split out. |

**Extraction prerequisites:**
- Split reusable content-addressed record helpers from epic-export evidence collection and `_pipeline` contract logic.
- Define an Arnold-neutral Capsule identity/contract surface before moving the generic helpers.

**Tests / gates:**
- static-gate: Arnold-neutral capsule helpers must not import `megaplan._pipeline.*` or epic-export inventory code

---

#### megaplan/store/db.py
**Disposition:** `legacy-hold` | **Granularity:** `file` | **Target:** megaplan/store/db_backend.py

**Reason:** `store/db.py` assembles the current DBStore around Supabase/Postgres deployment defaults, current storage schemas, and `_db/` slice mixins.

**Blockers:**
- The file imports the full current storage model surface from `megaplan.schemas` and composes the concrete `_db/` mixins directly.
- Connection/bootstrap behavior is wired to current deployment environment keys and actor-id rules.
- Blob setup still assumes current Supabase/local blob backend defaults.

**Import policy:**
- Allowed: `megaplan.schemas`, `megaplan.store.blob`, `megaplan.store._db`, `megaplan.store._db.common`
- Forbidden: `megaplan.handlers`, `megaplan.cli`

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| db_backend | megaplan | ['DBStore', 'SUPABASE_DB_URL', '_BOOTSTRAP_ACTOR_ID'] |

**String policy:**
| Category | Owner | Policy |
| --- | --- | --- |
| env_vars | megaplan | The DB backend stays plugin-local until deployment variable names and bootstrap actor rules are moved behind an Arnold-neutral backend contract. |

**Extraction prerequisites:**
- Separate Arnold-neutral store contracts from the current Postgres/Supabase deployment assembly and actor bootstrap policy.

**Tests / gates:**
- static-gate: Arnold-neutral store modules must not assemble the live DB backend or depend on current deployment env keys

---

#### megaplan/store/export.py
**Disposition:** `legacy-hold` | **Granularity:** `file` | **Target:** megaplan/store/export.py

**Reason:** `store/export.py` is tied to the current epic export manifest shape, row names, plan-artifact layout, and image/blob inventory semantics.

**Blockers:**
- `collect_epic_export()` hardcodes current row buckets such as `checklist_items`, `sprint_items`, `second_opinions`, and `epic_events`.
- The tarball writer assumes the current plan-artifact and blob manifest layout rather than an Arnold-neutral export contract.

**Import policy:**
- Allowed: `megaplan.store.snapshot`
- Forbidden: `megaplan.handlers`, `megaplan.cli`

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| export_formats | megaplan | ['megaplan-epic-export-v1', 'rows', 'plan_artifacts', 'blobs', 'manifest.json'] |

**String policy:**
| Category | Owner | Policy |
| --- | --- | --- |
| export_formats | megaplan | Keep the current epic export manifest and member-path vocabulary plugin-local until Arnold defines a neutral export contract. |

**Extraction prerequisites:**
- Define an Arnold-neutral export manifest schema independent of the current epic/plan/image row layout.

**Tests / gates:**
- static-gate: Arnold-neutral export helpers must not hardcode the current epic row buckets or plan-artifact member layout

---

#### megaplan/store/file.py
**Disposition:** `legacy-hold` | **Granularity:** `file` | **Target:** megaplan/store/file_backend.py

**Reason:** `store/file.py` builds the live FileStore around current journal helpers, repo-root ticket frontmatter integration, and the `_file/` slice mixins.

**Blockers:**
- The file backend imports current ticket frontmatter helpers and repository identity utilities from `megaplan.tickets.*`.
- It composes the entire current `_file/` slice set and assumes the current epic/tree directory layout.
- Blob, journal, and recovery behavior still route through `megaplan._core.io` and current `.megaplan`-rooted storage conventions.

**Import policy:**
- Allowed: `megaplan._core.io`, `megaplan.schemas`, `megaplan.tickets.files`, `megaplan.tickets.identity`, `megaplan.store._file`, `megaplan.store.snapshot`
- Forbidden: `megaplan.handlers`, `megaplan.cli`

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| file_backend | megaplan | ['FileStore', '_FileStoreTransaction', 'blobs', 'epics'] |

**String policy:**
| Category | Owner | Policy |
| --- | --- | --- |
| artifact_paths | megaplan | The current journaled file-tree layout remains plugin-local until Arnold defines neutral storage roots and ticket integration seams. |

**Extraction prerequisites:**
- Separate the generic journaling primitives from current file-tree layout, ticket frontmatter, and repo-root conventions.

**Tests / gates:**
- static-gate: Arnold-neutral store modules must not assemble the live file backend or import ticket frontmatter helpers

---

#### megaplan/store/identity.py
**Disposition:** `legacy-hold` | **Granularity:** `file` | **Target:** megaplan/store/identity.py

**Reason:** `store/identity.py` is a thin DB-write CLI helper layer tied to the current `MEGAPLAN_ACTOR_ID` environment variable and DBStore actor-registration flow.

**Blockers:**
- The module prints current operator-facing DB registration guidance and assumes the DBStore automation-actors table.

**Import policy:**
- Allowed: `megaplan.store.db`, `megaplan.schemas`
- Forbidden: `megaplan.handlers`, `megaplan.cli`

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| actor_identity | megaplan | ['MEGAPLAN_ACTOR_ID', 'create_automation_actor', 'automation_actors'] |

**String policy:**
| Category | Owner | Policy |
| --- | --- | --- |
| env_vars | megaplan | Keep the current actor-id env var and DB-table guidance plugin-local until Arnold owns a neutral automation-actor registration contract. |

**Extraction prerequisites:**
- Define an Arnold-neutral actor identity contract instead of the current DB-write CLI helper.

**Tests / gates:**
- static-gate: Arnold-neutral modules must not require `MEGAPLAN_ACTOR_ID` or print DBStore-specific registration guidance

---

#### megaplan/store/legacy_migration.py
**Disposition:** `legacy-hold` | **Granularity:** `file` | **Target:** megaplan/store/legacy_migration.py

**Reason:** `store/legacy_migration.py` exists to import pre-schema `.megaplan/<project>/plans` trees into the current FileStore/MultiStore plan model.

**Blockers:**
- The migration entrypoint hardcodes `.megaplan` plan roots, current legacy plan-id shaping, and the current FileStore/MultiStore target layout.
- It writes current legacy provenance metadata into new plan records and artifacts.

**Import policy:**
- Allowed: `megaplan.store.base`, `megaplan.store.file`, `megaplan.store.multi`
- Forbidden: `megaplan.handlers`, `megaplan.cli`

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| legacy_migration | megaplan | ['.megaplan', 'migrate-local-plans', 'legacy-local-plan'] |

**String policy:**
| Category | Owner | Policy |
| --- | --- | --- |
| artifact_paths | megaplan | Keep pre-schema `.megaplan` import layout and legacy provenance strings plugin-local. |

**Extraction prerequisites:**
- Retire or replace the current pre-schema local-plan migration path before considering Arnold ownership.

**Tests / gates:**
- static-gate: Arnold-neutral store modules must not import pre-schema `.megaplan` migration helpers

---

#### megaplan/store/multi.py
**Disposition:** `legacy-hold` | **Granularity:** `file` | **Target:** megaplan/store/multi_backend_router.py

**Reason:** `store/multi.py` is the current federated epic router and migration coordinator across FileStore and DBStore backends.

**Blockers:**
- The router depends on current `home_backend` semantics, file/db backend pairing, and live migration/tombstoning phases.
- It imports both concrete backends and coordinates the current migration-run lifecycle and warning surfaces.

**Import policy:**
- Allowed: `megaplan._core.io`, `megaplan.schemas`, `megaplan.store.base`, `megaplan.store.db`, `megaplan.store.file`
- Forbidden: `megaplan.handlers`, `megaplan.cli`

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| backend_routing | megaplan | ['MultiStore', 'home_backend', 'copying_meta', 'copying_blobs', 'verifying', 'cutting_over', 'tombstoning'] |

**String policy:**
| Category | Owner | Policy |
| --- | --- | --- |
| migration_phases | megaplan | The current file/db routing and migration-phase vocabulary stays plugin-local until Arnold defines a neutral multi-backend homing contract. |

**Extraction prerequisites:**
- Separate any reusable backend-routing abstractions from the current epic home-backend and migration-run policy.

**Tests / gates:**
- static-gate: Arnold-neutral store modules must not coordinate current file/db epic migration phases

---

#### megaplan/store/plan_repository.py
**Disposition:** `legacy-hold` | **Granularity:** `file` | **Target:** megaplan/plans/repository.py

**Reason:** `store/plan_repository.py` is bound to the current on-disk plan tree, artifact-role naming, plan-state writes, and events-projection integration.

**Blockers:**
- The repository reads and writes the live plan directory surface via `_core.io` helpers such as `find_plan_dir`, `plan_search_roots`, and `write_plan_state`.
- Artifact-role detection hardcodes current files such as `state.json`, `plan_v*.meta.json`, `review.json`, `gate.json`, `execution_batch_*.json`, and `finalize.json`.
- `from_plan_dir()` eagerly wires the current observability events projection for bound plan directories.

**Import policy:**
- Allowed: `megaplan._core.io`, `megaplan._core.state`, `megaplan.orchestration.feedback`, `megaplan.schemas`, `megaplan.store.base`, `megaplan.observability.events_projection`
- Forbidden: `megaplan.handlers`, `megaplan.cli`

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| plan_artifacts | megaplan | ['state.json', 'review.json', 'gate.json', 'execution_batch_*.json', 'finalize.json', 'contract.json'] |

**String policy:**
| Category | Owner | Policy |
| --- | --- | --- |
| artifact_paths | megaplan | Keep the current plan-tree file names and artifact-role classification plugin-local until Arnold defines a neutral plan repository contract. |

**Extraction prerequisites:**
- Split generic artifact-path helpers from current plan-tree naming, plan-state persistence, and events-projection side effects.

**Tests / gates:**
- static-gate: Arnold-neutral plan repositories must not classify the current Megaplan plan-tree artifact names

---

#### megaplan/store/snapshot.py
**Disposition:** `legacy-hold` | **Granularity:** `file` | **Target:** megaplan/store/snapshot.py

**Reason:** `store/snapshot.py` is reusable deterministic JSON machinery wrapped around the current `EpicSnapshot` projection contract and current epic child-entity inventory.

**Blockers:**
- `capture_epic_snapshot()` hardcodes the current epic projection shape: body, checklist items, sprints, sprint items, images, and second opinions.
- The protocol and return model still depend on `EpicSnapshot` from the current schemas package.

**Import policy:**
- Allowed: `megaplan.schemas`
- Forbidden: `megaplan.handlers`, `megaplan.cli`

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| snapshot_projection | megaplan | ['EpicSnapshot', 'checklist_items', 'sprints', 'sprint_items', 'images', 'second_opinions'] |

**String policy:**
| Category | Owner | Policy |
| --- | --- | --- |
| projection_fields | megaplan | Keep the current epic snapshot field set plugin-local until Arnold defines a neutral snapshot domain model. |

**Extraction prerequisites:**
- Separate generic canonicalization helpers from the current EpicSnapshot projection shape.

**Tests / gates:**
- static-gate: Arnold-neutral snapshot helpers must not hardcode the current epic child-entity projection fields

---

#### megaplan/store/warrant_sources.py
**Disposition:** `legacy-hold` | **Granularity:** `file` | **Target:** megaplan/store/warrant_source_inventory.py

**Reason:** `store/warrant_sources.py` inventories warrant inputs from the current plan-directory artifact set and current review/finalize/state/phase-result conventions.

**Blockers:**
- `inventory_warrant_sources()` reads current plan artifacts such as `finalize.json`, `review.json`, `state.json`, `phase_result.json`, and `step_receipt_*.json`.
- The helper infers behavioral-manifest and verified-result fields from current state/review/finalize payload conventions.

**Import policy:**
- Allowed: `megaplan.schemas`
- Forbidden: `megaplan.handlers`, `megaplan.cli`

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| warrant_source_inventory | megaplan | ['finalize.json', 'review.json', 'state.json', 'phase_result.json', 'step_receipt_'] |

**String policy:**
| Category | Owner | Policy |
| --- | --- | --- |
| artifact_paths | megaplan | Keep current plan-dir warrant inventory file names and field inference rules plugin-local. |

**Extraction prerequisites:**
- Define an Arnold-neutral source-inventory surface instead of deriving Warrant inputs from the current plan-dir artifact set.

**Tests / gates:**
- static-gate: Arnold-neutral warrant inventory helpers must not depend on current plan-dir artifact names

---

#### megaplan/store/__init__.py
**Disposition:** `legacy-hold` | **Granularity:** `file` | **Target:** megaplan/store/__init__.py

**Reason:** `store/__init__.py` is a mixed export barrel that re-exports Arnold-neutral seams together with live backends, adapters, plan-repository wiring, capsules, warrants, and identity helpers.

**Blockers:**
- The barrel currently merges neutral contracts with product-specific backends and mixed-lifecycle helpers, so any wholesale move would leak unresolved store policy.

**Import policy:**
- Allowed: `megaplan.store.base`, `megaplan.store.blob`, `megaplan.store.capsule`, `megaplan.store.compat`, `megaplan.store.db`, `megaplan.store.file`, `megaplan.store.identity`, `megaplan.store.multi`, `megaplan.store.plan_repository`, `megaplan.store.warrant`, `megaplan.schemas`
- Forbidden: `megaplan.handlers`, `megaplan.cli`

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| export_barrels | megaplan | ['DBStore', 'FileStore', 'MultiStore', 'PlanRepository', 'ArnoldStoreAdapter'] |

**Extraction prerequisites:**
- Split neutral exports from plugin-local backends and helper surfaces before introducing an Arnold-owned store barrel.

**Tests / gates:**
- static-gate: Arnold-neutral store barrels must not re-export live Megaplan backends or plan-repository helpers wholesale

---

#### megaplan/store/_db
**Disposition:** `legacy-hold` | **Granularity:** `directory` | **Target:** megaplan/store/db_backend_slices

**Reason:** Every tracked file under `store/_db/` is a DBStore slice or support module for the current Postgres schema, current product row tables, and migration/event side effects.

**Blockers:**
- The subtree hardcodes current table/column sets, JSONB fields, and row restoration logic for epics, plans, sprints, tickets, runtime media, and migration runs.
- Slice mixins still import the current storage schemas and, in the events slice, current ticket-resolution side effects.
- This directory row is intentionally conservative: it keeps all tracked `_db/*.py` files together under the same unresolved DB-backend ownership instead of hiding them with exclusions.

**Import policy:**
- Allowed: `megaplan.schemas`, `megaplan.store.base`, `megaplan.store.snapshot`, `megaplan.tickets`
- Forbidden: `megaplan.handlers`, `megaplan.cli`

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| db_tables | megaplan | ['migration_runs', 'plans', 'epics', 'checklist_items', 'sprints', 'images', 'second_opinions', 'automation_actors'] |

**String policy:**
| Category | Owner | Policy |
| --- | --- | --- |
| db_schema | megaplan | Keep current table/column names, migration phases, and ticket-resolution side effects plugin-local until Arnold defines a neutral persistence schema. |

**Extraction prerequisites:**
- Replace the current DB schema/table contract and side-effect assumptions with an Arnold-neutral persistence model before splitting these slices.

**Tests / gates:**
- static-gate: Arnold-neutral persistence modules must not hardcode the current DB table inventory or ticket side effects

---

#### megaplan/store/_file
**Disposition:** `legacy-hold` | **Granularity:** `directory` | **Target:** megaplan/store/file_backend_slices

**Reason:** Every tracked file under `store/_file/` is a FileStore slice or support module for the current journaled epic tree, ticket frontmatter, and current file-layout persistence rules.

**Blockers:**
- The subtree hardcodes the current on-disk epic tree, event-log framing, ticket frontmatter integration, repo-root lookup, and current file-model layout.
- Multiple slices still depend on current `megaplan._core.io` journal helpers, `megaplan.schemas`, and current ticket/codebase semantics.
- This directory row is intentionally conservative: it keeps all tracked `_file/*.py` files together under the same unresolved file-backend ownership instead of using exclusions.

**Import policy:**
- Allowed: `megaplan._core.io`, `megaplan.schemas`, `megaplan.store.base`, `megaplan.store.snapshot`, `megaplan.tickets`
- Forbidden: `megaplan.handlers`, `megaplan.cli`

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| file_tree | megaplan | ['epics', 'blobs', 'tickets', 'events.ndjson', 'prompt_snapshot', 'context_snapshot'] |

**String policy:**
| Category | Owner | Policy |
| --- | --- | --- |
| artifact_paths | megaplan | Keep the current journaled file-tree layout, ticket roots, and event-log naming plugin-local until Arnold defines neutral persistence roots and side effects. |

**Extraction prerequisites:**
- Replace the current file-tree layout, journal coupling, and ticket frontmatter side effects with an Arnold-neutral persistence contract before splitting these slices.

**Tests / gates:**
- static-gate: Arnold-neutral persistence modules must not hardcode the current file-tree roots or ticket frontmatter side effects

---

#### megaplan/vendor
**Disposition:** `legacy-hold` | **Granularity:** `directory` | **Target:** megaplan/vendor

**Reason:** `vendor/` contains vendored runtime assets: the Shannon CLI wrapper (@dexh/shannon v0.0.2) with 15 Megaplan-specific patches (P1..P15). The __init__.py is a one-line docstring. The shannon/ subtree contains index.ts (vendored+patched TypeScript), VENDOR.md (detailed provenance document: upstream source, verified-pristine path, patch catalog P1..P15, regeneration recipe, live proofs, test suite results), package.json (upstream metadata), bun.lock, and .gitignore. Provenance is explicitly recorded in VENDOR.md with verification anchors. No .py files beyond the stub __init__.py. Placed in legacy-hold because: (a) the vendored Shannon runtime is Megaplan's tmux-based Claude Code driver; (b) the 15 patches are Megaplan-specific (P1 timeout override, P2 tool-use guard, P3-P15 session/env/determinism); (c) extraction would require replacing the vendored runtime with an Arnold driver abstraction.

**Blockers:**
- Vendored Shannon CLI (@dexh/shannon v0.0.2) is a TypeScript runtime with 15 Megaplan-specific patches.
- Patches P1-P15 are load-bearing for Megaplan's tmux-based Claude Code driver (timeout control, tool-use row guard, root-safe args, paste-first-turn, stdin buffer, slash-completion, env scrubbing).
- Provenance is fully documented in VENDOR.md but the vendored artifact is not extractable as Python code.

**String policy:**
| Category | Owner | Policy |
| --- | --- | --- |
| megaplan_paths | megaplan | Vendor directory holds vendored runtime under megaplan/vendor/shannon/. Path convention is Megaplan-local. |

**Extraction prerequisites:**
- Replace vendored Shannon runtime with an Arnold driver abstraction before extraction.

**Tests / gates:**
- tests/test_workers_shannon.py (72 passed): exercises vendored Shannon through Python worker
- tests/test_shannon_stream_idle_timeout.py (8 passed)
- tests/test_shannon_wall_clock_timeout.py (1 passed)

---

#### megaplan/_legacy_subprocess
**Disposition:** `legacy-hold` | **Granularity:** `directory` | **Target:** megaplan/_legacy_subprocess (retained)

**Reason:** Legacy subprocess runner retained for backward compatibility; pending extraction or deletion.

**Blockers:**
- Carries legacy worker dispatch patterns that may still be referenced.
- No current extraction path defined.

**Tests / gates:**
- none-yet: legacy surface, no extraction planned

---

## Split-Required Rows (with children)

### megaplan/_pipeline
**Disposition:** `split-required` | **Granularity:** `split` | **Target:** arnold/pipeline (neutral executor/runtime surface) + megaplan/pipelines/megaplan (planning stages, handlers, policy)

**Reason:** The `_pipeline` subtree mixes extractable runtime primitives with Megaplan planning-stage policy, handler adapters, and stage-name vocabulary.

**Blockers:**
- Planning stage strings and override/gate decisions are still embedded in shared carrier modules.
- Discovery/runtime helpers still expose `megaplan`-branded schema IDs, paths, env vars, or capability names.
- Builder and planning glue still hardcode critique/finalize/tiebreaker control flow alongside generic pipeline composition helpers.

**Import policy:**
- Forbidden: `megaplan.handlers`, `megaplan.execute`, `megaplan.finalize`

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| phase_names | megaplan | ['prep', 'plan', 'critique', 'gate', 'revise', 'finalize', 'execute', 'review', 'tiebreaker', 'feedback'] |
| override_actions | megaplan | ['force_proceed', 'abort', 'replan', 'add_note'] |

**String policy:**
| Category | Owner | Policy |
| --- | --- | --- |
| phase_names | megaplan | Plugin-local only; Arnold runtime rows may not own Megaplan planning phase literals. |

**Extraction prerequisites:**
- Split neutral pipeline SDK files from Megaplan planning/compiler/runtime adapters.
- Move planning stage routing and handler invocation behind plugin-owned adapters.
- Rename `megaplan`-branded discovery/runtime wire formats before promoting them to Arnold.

**First extraction unit:** `arnold/pipeline`

**Tests / gates:**
- static-gate: arnold/pipeline/** must not embed Megaplan planning phase literals
- static-gate: arnold/pipeline/** must not import Megaplan handler modules directly

---

**Child symbols:** _(none)_

### megaplan/_pipeline/types.py
**Disposition:** `split-required` | **Granularity:** `split` | **Target:** arnold/pipeline/types.py (typed-port and carrier primitives) + megaplan/pipelines/megaplan/phase_runner.py (planning adapter and Megaplan policy)

**Reason:** `types.py` mixes Arnold-neutral typed-port/runtime carriers with Megaplan gate literals, override actions, in-process phase dispatch, and CLI/planning defaults.

**Blockers:**
- `GateRecommendation` and `OverrideAction` are public literals in the same file as neutral carrier types.
- `Pipeline.run_phase()` dispatches Megaplan phases through `find_plan_dir`, `handle_feedback`, and `CliError`.
- `_phase_arg_overrides()` and `_phase_namespace()` hardcode Megaplan CLI defaults such as `feedback`, `workflow`, `confirm_self_review`, and `_pipeline=planning`.

**Import policy:**
- Allowed: `megaplan._pipeline.envelope`
- Forbidden: `megaplan.cli.feedback`, `megaplan._core`, `megaplan.orchestration.progress`, `megaplan.types`

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| decision_keys | megaplan | ['proceed', 'iterate', 'tiebreaker', 'escalate'] |
| override_actions | megaplan | ['force_proceed', 'abort', 'replan', 'add_note'] |
| phase_names | megaplan | ['feedback', 'planning'] |
| typed_port_carriers | arnold | ['Port', 'PortRef', 'RoutingKey', 'ContentTypeRegistry', 'ReduceResult', 'SelectionResult', 'StateDelta'] |

**String policy:**
| Category | Owner | Policy |
| --- | --- | --- |
| phase_names | megaplan | Keep Megaplan phase labels in the plugin adapter; Arnold-owned carrier types must stay phase-agnostic. |
| cli_flags | megaplan | Keep phase-specific CLI defaults in the plugin adapter, not in Arnold-neutral carrier modules. |

**Extraction prerequisites:**
- Move `Pipeline.run_phase()` and `_phase_*` helpers into a Megaplan plugin adapter module.
- Re-export neutral typed-port/content-type/state-delta carriers from an Arnold-owned module.
- Replace direct `GateRecommendation` / `OverrideAction` literals with plugin-owned policy declarations.

**First extraction unit:** `arnold/pipeline/types.py`

**Tests / gates:**
- static-gate: arnold/pipeline/types.py must not import `megaplan._core`, `megaplan.cli.feedback`, or `megaplan.types`
- static-gate: arnold/pipeline/types.py must not contain `feedback|planning|confirm_self_review` literals

**Configurable seams:**
| Declaration Site | Inherits Via | Override Mechanism | Meaning Owner | Supported State |
| --- | --- | --- | --- | --- |
| megaplan/_pipeline/types.py (GateRecommendation) | direct import from _pipeline.types | plugin-owned gate recommendation mapping | megaplan | set; must move before Arnold extraction |
| megaplan/_pipeline/types.py (OverrideAction) | direct import from _pipeline.types | plugin-owned override action mapping | megaplan | set; must move before Arnold extraction |
| megaplan/_pipeline/types.py (Pipeline.run_phase / _phase_namespace) | in-process planning adapter | plugin-owned phase runner wrapper | megaplan | set; Arnold-neutral carrier module cannot own this path |

---

**Child symbols** (4):

- `megaplan/_pipeline/types.py` → megaplan policy symbol: GateRecommendation
- `megaplan/_pipeline/types.py` → megaplan policy symbol: OverrideAction
- `megaplan/_pipeline/types.py` → megaplan planning adapter symbol: Pipeline.run_phase
- `megaplan/_pipeline/types.py` → megaplan planning defaults symbols: _phase_arg_overrides / _phase_namespace

### megaplan/cli/arnold.py
**Disposition:** `split-required` | **Granularity:** `split` | **Target:** arnold/cli/arnold.py (pipeline discovery/list/check/new shell) + megaplan/pipelines/megaplan/cli_alias.py (planning alias routing and override forwarding)

**Reason:** `cli/arnold.py` mixes an Arnold-facing pipeline CLI surface with legacy Megaplan forwarding, planning-only override verbs, and a hardcoded planning-module default.

**Blockers:**
- `PLANNING_OVERRIDE_ACTIONS` hardcodes planning-only override verbs (`force-proceed`, `replan`, `recover-blocked`, `resume-clarify`).
- `PLANNING_MODULE_VERBS` and `_handle_module_verb()` special-case `override` for the `planning` module.
- `_megaplan_main()` forwards directly into `megaplan.cli.main`, preserving the legacy Megaplan command surface instead of an Arnold-neutral service boundary.
- `_handle_planning_override()` and `_handle_auto()` hardcode the `planning` module/default path.

**Import policy:**
- Allowed: `megaplan._pipeline.registry`
- Forbidden: `megaplan.cli`

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| cli_verbs | arnold | ['run', 'check', 'doctor', 'describe', 'auto', 'pipelines', 'list', 'new'] |
| override_actions | megaplan | ['force-proceed', 'replan', 'recover-blocked', 'resume-clarify', 'abort', 'add-note', 'set-robustness', 'set-profile', 'set-model', 'set-vendor'] |
| pipeline_aliases | megaplan | ['planning'] |

**String policy:**
| Category | Owner | Policy |
| --- | --- | --- |
| pipeline_aliases | megaplan | Keep the legacy `planning` alias and its default routing on the plugin side until Arnold owns neutral alias resolution. |
| override_actions | megaplan | Planning-only override verbs stay plugin-local; Arnold CLI should only expose generic dispatch verbs. |

**Extraction prerequisites:**
- Split discovery/list/check/new shell commands from the Megaplan legacy forwarding path.
- Move planning-only override and auto alias handling behind a plugin-owned CLI adapter.
- Replace `_megaplan_main()` forwarding with an Arnold-neutral command dispatch seam.

**First extraction unit:** `arnold/cli/arnold.py`

**Tests / gates:**
- static-gate: arnold/cli/arnold.py must not default an omitted module to `planning`
- static-gate: arnold/cli/arnold.py must not declare planning-only override verbs
- static-gate: arnold/cli/arnold.py must not import `megaplan.cli` directly

---

**Child symbols** (2):

- `megaplan/cli/arnold.py` → megaplan planning alias symbols: PLANNING_OVERRIDE_ACTIONS / PLANNING_MODULE_VERBS
- `megaplan/cli/arnold.py` → megaplan legacy forwarding symbols: _megaplan_main / _handle_planning_override / _handle_auto(planning default)

### megaplan/control_interface.py
**Disposition:** `split-required` | **Granularity:** `split` | **Target:** arnold/control_interface.py (neutral run-state/control carriers and transition facade) + megaplan/pipelines/megaplan/control_binding_bridge.py (planning binding default resolver)

**Reason:** `control_interface.py` defines an Arnold-facing control contract, but its default dispatch path still hardcodes the Megaplan planning binding and persists state via current Megaplan delta machinery.

**Blockers:**
- `_resolve_binding_and_state()` hardcodes the `planning` binding string and dynamically imports `megaplan.planning`.
- `apply_transition()` persists through `write_plan_state`, tying the shared facade to current Megaplan plan-dir state storage.
- `ControlTransitionResult.state_deltas` and `apply_transition()` depend directly on `_pipeline.types.StateDelta`, `StateDeltaConflict`, and `apply_delta`.
- Callers without an explicit binding still inherit planning-specific state shaping through `planning_run_state_view()`.

**Import policy:**
- Allowed: `megaplan.run_outcome`
- Forbidden: `megaplan.planning`, `megaplan._core.state`, `megaplan._pipeline.types`

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| control_contracts | arnold | ['ControlTarget', 'ControlProjection', 'RunStateView', 'ControlTransition', 'ControlTransitionRequest', 'ArtifactRequest', 'ControlTransitionResult', 'ControlBinding'] |
| pipeline_aliases | megaplan | ['planning'] |

**String policy:**
| Category | Owner | Policy |
| --- | --- | --- |
| pipeline_aliases | megaplan | The default binding string `planning` must move behind plugin registration before the shared control facade is Arnold-owned. |

**Extraction prerequisites:**
- Split neutral control carrier/protocol symbols from the planning default resolver.
- Replace direct `write_plan_state` persistence and `_pipeline.types` delta imports with Arnold-owned storage and delta interfaces.
- Make bindings explicit or registry-driven so the shared facade never imports `megaplan.planning` by default.

**First extraction unit:** `arnold/control_interface.py`

**Tests / gates:**
- static-gate: arnold/control_interface.py must not import `megaplan.planning` or default a binding string to `planning`
- static-gate: arnold/control_interface.py must not depend on `_pipeline.types` delta helpers

---

**Child symbols** (2):

- `megaplan/control_interface.py` → arnold control contract symbols: ControlTarget / ControlProjection / RunStateView / ControlTransition* / ArtifactRequest / ControlBinding
- `megaplan/control_interface.py` → megaplan planning binding symbols: _resolve_binding_and_state / read_valid_targets / synthesize_artifacts / apply_transition(default planning path)

### megaplan/auto.py
**Disposition:** `split-required` | **Granularity:** `split` | **Target:** arnold/runtime/auto_subprocess.py (_supervise_subprocess watcher seam) + megaplan/pipelines/megaplan/auto_driver.py (planning dispatch loop, overrides, and terminal policy)

**Reason:** `auto.py` contains one reusable subprocess/watcher seam, but the driver loop, phase dispatch, override behavior, and terminal-state policy are hardcoded to the Megaplan planning plugin.

**Blockers:**
- `_run_planning_phase()` resolves `PipelineRegistry().get("megaplan")` and calls `pipeline.run_phase(...)` directly.
- `drive()` imports `AUTOMATION_TERMINAL_STATES`, many `STATE_*` constants, `PlanRepository`, phase-result recovery policy, and emits planning-specific override/help text.
- Override helpers and command builders hardcode `override force-proceed`, `override abort`, `override add-note`, `feedback workflow`, and `recover-blocked`.
- Environment/path strings such as `MEGAPLAN_AUTO_HEARTBEAT_SECONDS`, `MEGAPLAN_ENVELOPE_IN`, and `python -m megaplan resume` keep the file anchored to Megaplan runtime conventions.

**Import policy:**
- Allowed: `megaplan.runtime.process`
- Forbidden: `megaplan.cli`, `megaplan.types`, `megaplan.orchestration.progress`, `megaplan.store`

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| state_keys | megaplan | ['done', 'aborted', 'failed', 'blocked', 'cancelled', 'paused', 'awaiting_human', 'awaiting_human_verify', 'tiebreaker_pending', 'tiebreaker_ready'] |
| override_actions | megaplan | ['force-proceed', 'abort', 'add-note', 'recover-blocked'] |
| env_names | megaplan | ['MEGAPLAN_AUTO_HEARTBEAT_SECONDS', 'MEGAPLAN_ENVELOPE_IN'] |

**String policy:**
| Category | Owner | Policy |
| --- | --- | --- |
| pipeline_aliases | megaplan | The planning pipeline name and auto-driver help text remain plugin-owned until Arnold exposes a generic auto-dispatch substrate. |
| env_names | megaplan | Rename Megaplan-branded environment variables before extracting the watcher seam under Arnold. |

**Extraction prerequisites:**
- Split `_supervise_subprocess()` and its watcher state into an Arnold-owned runtime helper.
- Replace hardcoded `planning` pipeline resolution with a plugin capability/registry seam.
- Move override command synthesis and Megaplan terminal-state policy behind the plugin-owned auto driver.

**First extraction unit:** `arnold/runtime/auto_subprocess.py`

**Tests / gates:**
- static-gate: Arnold-owned auto runtime helpers must not resolve `PipelineRegistry().get("megaplan")` or import Megaplan `STATE_*` constants
- static-gate: Arnold-owned auto runtime helpers must not synthesize `override` or `feedback workflow` commands

---

**Child symbols** (2):

- `megaplan/auto.py` → arnold subprocess watcher symbol: _supervise_subprocess
- `megaplan/auto.py` → megaplan auto-driver symbols: _run_planning_phase / _override_* / _phase_command / drive / run_auto

### megaplan/profiles/__init__.py
**Disposition:** `split-required` | **Granularity:** `split` | **Target:** arnold/profiles/loader.py (generic profile loading/inheritance/spec validation) + megaplan/pipelines/megaplan/profile_policy.py (phase-key validation, metadata semantics, vendor/depth rewrites)

**Reason:** `profiles/__init__.py` mixes generic TOML loading/inheritance and agent-spec validation with Megaplan phase-key validation, metadata semantics, stage-name vocabulary, and CLI rewrite policy.

**Blockers:**
- `VALID_PHASE_KEYS` derives from `DEFAULT_AGENT_ROUTING.keys()`, tying validation to Megaplan planning phase names.
- `PROFILE_METADATA_KEYS` and related validators hardcode Megaplan metadata semantics such as `vendor_locked`, `tier_models`, `prep_models`, `max_tasks_per_batch`, `adaptive_critique`, and `critic_model`.
- `DEPTH_AUTHOR_PHASES`, `CANONICAL_PREP_MODELS`, `KIMI_SPEC`, and deepseek-provider constants hardcode Megaplan stage names and model policy.
- Profile loaders and pipeline-local helpers still read `.megaplan/profiles.toml` and `~/.megaplan/pipelines/.../profiles`, preserving Megaplan path conventions.
- `apply_profile_expansion()` rewrites Megaplan-specific CLI flags and metadata (`--vendor`, `--critic`, `--depth`, `--deepseek-provider`, `tier_models`, `prep_models`).

**Import policy:**
- Forbidden: `megaplan.types`, `megaplan._pipeline`

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| profile_contracts | arnold | ['profiles', 'extends', 'default'] |
| profile_metadata | megaplan | ['vendor_locked', 'tier_models', 'prep_models', 'max_tasks_per_batch', 'adaptive_critique', 'critic_model'] |
| phase_names | megaplan | ['plan', 'revise', 'loop_plan', 'tiebreaker_researcher', 'tiebreaker_challenger', 'prep'] |

**String policy:**
| Category | Owner | Policy |
| --- | --- | --- |
| artifact_paths | megaplan | Rename `.megaplan` profile roots and pipeline-local profile paths before extracting generic loaders under Arnold. |
| cli_flags | megaplan | Vendor/critic/depth/provider rewrite flags remain plugin-owned until profile semantics are separated. |

**Extraction prerequisites:**
- Split generic profile loading/inheritance/spec validation from phase-key and metadata-policy validation.
- Replace `DEFAULT_AGENT_ROUTING`-derived phase validation with plugin-declared stage keys.
- Move vendor/critic/depth/tier/prep rewrites into a Megaplan plugin policy layer.

**First extraction unit:** `arnold/profiles/loader.py`

**Tests / gates:**
- static-gate: arnold/profiles/** must not validate phase keys from `DEFAULT_AGENT_ROUTING` or import `megaplan._pipeline`
- static-gate: arnold/profiles/** must not declare Megaplan metadata keys or vendor/depth rewrite policy

---

**Child symbols** (2):

- `megaplan/profiles/__init__.py` → arnold profile loader symbols: _parse_profiles_doc / _load_profiles_file / load_profile_sources / load_profiles / load_profile_metadata / resolve_profile / _resolve_extends_ref / _resolve_with_inheritance
- `megaplan/profiles/__init__.py` → megaplan profile-policy symbols: VALID_PHASE_KEYS / PROFILE_METADATA_KEYS / DEPTH_AUTHOR_PHASES / _extract_tier_models / _validate_tier_models / _load_pipeline_local_profiles / _load_pipeline_local_metadata / _resolve_tier_models_with_inheritance / apply_profile_expansion

### megaplan/runtime/process.py
**Disposition:** `split-required` | **Granularity:** `split` | **Target:** arnold/runtime/process.py (spawn/kill-group primitives) + megaplan/pipelines/megaplan/tmux_runtime.py (tmux session/orphan helpers for Shannon-style workers)

**Reason:** `runtime/process.py` combines Arnold-neutral subprocess/process-group primitives with tmux-session lifecycle helpers used by Megaplan's interactive worker stack.

**Blockers:**
- `TmuxSession`, `detect_orphans()`, and `pane_pids()` are specific to tmux-backed worker sessions rather than generic subprocess control.
- Shell-guard error text still points at `megaplan/runtime/process_guard.py`.
- The module docstring brands the surface as `for megaplan`, even though only the tmux tail is worker-policy-specific.

**Import policy:**
- Forbidden: `megaplan.workers`, `megaplan.types`

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| process_primitives | arnold | ['spawn', 'spawn_async', 'kill_group'] |
| tmux_runtime | megaplan | ['TmuxSession', 'detect_orphans', 'pane_pids'] |

**String policy:**
| Category | Owner | Policy |
| --- | --- | --- |
| shell_guard_ledgers | megaplan | Rename Megaplan-branded process-guard references before promoting the generic spawn helpers under Arnold. |
| worker_runtime | megaplan | Keep tmux-session and orphan-reaping wording on the plugin side of the split. |

**Extraction prerequisites:**
- Split `spawn()`, `spawn_async()`, and `kill_group()` from the tmux session/orphan helpers.
- Re-home tmux-specific teardown/audit helpers beside the interactive worker implementations that need them.
- Rename Megaplan-branded process-guard references in generic shell-injection errors.

**First extraction unit:** `arnold/runtime/process.py`

**Tests / gates:**
- static-gate: arnold/runtime/process.py must not own tmux session lifecycle helpers
- static-gate: arnold/runtime/process.py must not reference `megaplan/runtime/process_guard.py`

---

**Child symbols** (2):

- `megaplan/runtime/process.py` → arnold process symbols: spawn / spawn_async / kill_group
- `megaplan/runtime/process.py` → megaplan tmux-runtime symbols: OrphanDetectedError / TmuxSession / detect_orphans / pane_pids

### megaplan/runtime/sandbox.py
**Disposition:** `split-required` | **Granularity:** `split` | **Target:** arnold/runtime/tool_sandbox.py (generic tool-boundary enforcement) + megaplan/pipelines/megaplan/worker_prompt_guard.py (Megaplan worktree/template allowances and prompt-context framing)

**Reason:** `runtime/sandbox.py` contains reusable tool-sandbox mechanics, but inspection confirms the file also carries Megaplan-specific worker, worktree, and template-path vocabulary that prevents whole-file Arnold ownership.

**Blockers:**
- The module docstring explicitly references the Hermes worker, Megaplan phases, bakeoff profiles, and `.megaplan-worktrees/<plan>/<profile>/`.
- Read exemptions mention Megaplan-specific artifacts such as `/tmp/phase-6-idea.txt` and Megaplan template files.
- Tool-wrapper installation is generic, but the surrounding narrative and allowed-path assumptions are tied to current Megaplan prompt/worktree conventions.

**Import policy:**
- Forbidden: `megaplan.types`, `megaplan.profiles`, `megaplan.handlers`

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| sandbox_primitives | arnold | ['SANDBOX_CWD', 'SandboxViolation', 'validate_terminal_command', 'validate_write_path', 'validate_v4a_patch', 'install_sandbox'] |
| worker_runtime | megaplan | ['Hermes', 'bakeoff', '.megaplan-worktrees', 'phase-6-idea.txt'] |

**String policy:**
| Category | Owner | Policy |
| --- | --- | --- |
| worktree_paths | megaplan | Keep Megaplan worktree and template-path examples on the plugin side until Arnold has neutral project/worktree terminology. |
| tool_names | arnold | The terminal/write/patch enforcement surface can move once the Megaplan-specific prompt/worktree framing is split away. |

**Extraction prerequisites:**
- Split generic path-validation and tool-wrapper logic from Megaplan worker/worktree framing.
- Replace Megaplan-specific read-exemption examples and path conventions with neutral runtime vocabulary.
- Keep any plugin-specific prompt/worktree allowances beside the worker implementations that require them.

**First extraction unit:** `arnold/runtime/tool_sandbox.py`

**Tests / gates:**
- static-gate: arnold/runtime/tool_sandbox.py must not mention Hermes, bakeoff profiles, or `.megaplan-worktrees`
- static-gate: Arnold-owned sandbox helpers must stay limited to generic tool/path validation mechanics

**Configurable seams:**
| Declaration Site | Inherits Via | Override Mechanism | Meaning Owner | Supported State |
| --- | --- | --- | --- | --- |
| megaplan/runtime/sandbox.py (SANDBOX_CWD / install_sandbox) | ContextVar-scoped wrapper activation around worker tool calls | plugin-owned worker wrapper may supply project/worktree-specific framing while reusing the neutral validator | arnold | partial; validation is reusable but current path examples remain plugin-local |

---

**Child symbols** (1):

- `megaplan/runtime/sandbox.py` → arnold sandbox symbols: SANDBOX_CWD / SandboxViolation / validate_terminal_command / validate_write_path / validate_v4a_patch / install_sandbox

### megaplan/drivers
**Disposition:** `split-required` | **Granularity:** `split` | **Target:** arnold/runtime/drivers (step-execution substrates) + megaplan/pipelines/megaplan/unified_dispatch.py (flag gating, audit scopes, and pipeline binding policy)

**Reason:** The `drivers/` subtree mixes reusable step-execution substrates with Megaplan-specific unified-dispatch gating, audit scopes, and current pipeline type bindings.

**Blockers:**
- `drivers/__init__.py` gates selection on `unified_dispatch_on()` and audits `megaplan/_pipeline` plus legacy-subprocess escape patterns.
- `subprocess_isolated.py` still imports `_supervise_subprocess` from `megaplan.auto` and `StepContext` / `StepResult` from `megaplan._pipeline.types`.
- Current driver classes still encode Megaplan pipeline step-kind literals and plan-dir-oriented context wiring.

**Import policy:**
- Forbidden: `megaplan.handlers`, `megaplan.cli`, `megaplan.execute`

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| substrate_literals | arnold | ['in_process', 'subprocess_isolated', 'linear', 'fanout', 'dag'] |
| dispatch_policy | megaplan | ['unified_dispatch_on', 'legacy_supervise_subprocess', 'legacy_phase_command'] |

**String policy:**
| Category | Owner | Policy |
| --- | --- | --- |
| dispatch_flags | megaplan | Keep unified-dispatch flag/audit policy on the plugin side; the extracted Arnold driver layer should expose an ungated selection surface. |

**Extraction prerequisites:**
- Split generic driver classes from the Megaplan flag-gated selection/audit wrapper.
- Repoint subprocess supervision to the Arnold-owned watcher seam instead of `megaplan.auto`.
- Replace `_pipeline.types`-specific step contracts with Arnold-neutral driver interfaces.

**First extraction unit:** `arnold/runtime/drivers`

**Tests / gates:**
- static-gate: arnold/runtime/drivers/** must not call `unified_dispatch_on()` or audit `megaplan/_pipeline`
- static-gate: arnold/runtime/drivers/** must not import `megaplan.auto`

---

**Child symbols:** _(none)_

### megaplan/drivers/__init__.py
**Disposition:** `split-required` | **Granularity:** `split` | **Target:** arnold/runtime/drivers/select.py (neutral driver registry surface) + megaplan/pipelines/megaplan/unified_dispatch.py (Megaplan flag/audit wrapper)

**Reason:** `drivers/__init__.py` exposes generic substrate/topology literals, but selection is still wrapped in Megaplan's unified-dispatch flag and scoped legacy audit.

**Blockers:**
- `select_driver()` is gated by `unified_dispatch_on()` instead of being a neutral registry/selection surface.
- `scoped_legacy_audit()` hardcodes `megaplan/_pipeline` and `megaplan/drivers` audit scopes plus legacy-subprocess patterns.

**Import policy:**
- Allowed: `megaplan.drivers.in_process`, `megaplan.drivers.subprocess_isolated`
- Forbidden: `megaplan.handlers`, `megaplan.execute`

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| substrate_literals | arnold | ['Substrate', 'Topology', 'SUBSTRATES', 'TOPOLOGIES'] |
| dispatch_policy | megaplan | ['unified_dispatch_on', 'scoped_legacy_audit'] |

**String policy:**
| Category | Owner | Policy |
| --- | --- | --- |
| audit_scopes | megaplan | Scoped legacy-audit paths remain plugin-owned policy. |

**Extraction prerequisites:**
- Split ungated driver selection from the Megaplan unified-dispatch flag and audit helper.

**First extraction unit:** `arnold/runtime/drivers/select.py`

**Tests / gates:**
- static-gate: Arnold driver-selection surfaces must not hardcode Megaplan audit scopes or unified-dispatch flags

---

**Child symbols** (2):

- `megaplan/drivers/__init__.py` → arnold driver-selection symbols: Substrate / Topology / SUBSTRATES / TOPOLOGIES / current_substrate / reset_substrate
- `megaplan/drivers/__init__.py` → megaplan dispatch-policy symbols: select_driver(gated wrapper) / scoped_legacy_audit

### megaplan/workers
**Disposition:** `split-required` | **Granularity:** `split` | **Target:** arnold/runtime/workers (generic result/process/env helpers) + megaplan/pipelines/megaplan/workers (phase prompts, schema contracts, session policy, and vendor-specific worker implementations)

**Reason:** The `workers/` subtree contains some reusable worker runtime mechanics, but most of the package is still tightly coupled to Megaplan phase prompts, schema contracts, session policy, and provider-specific worker behavior.

**Blockers:**
- `_impl.py` mixes neutral helpers such as `WorkerResult` and `run_command()` with Megaplan prompts, schemas, `PlanState`, execution-batch payload normalization, and worker dispatch policy.
- `_mock_payloads.py`, `hermes.py`, and `shannon.py` all encode Megaplan phase/task/sense-check contracts and current worker-session behavior.
- `workers/__init__.py` re-exports both extractable helpers and plugin-local worker policy from one surface.

**Import policy:**
- Forbidden: `megaplan.handlers`, `megaplan.cli`, `megaplan.execute`, `megaplan.finalize`

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| worker_primitives | arnold | ['WorkerResult', 'CommandResult', 'run_command', 'set_work_dir_override'] |
| worker_policy | megaplan | ['task_updates', 'sense_check_acknowledgments', 'finalize.json', 'execution_batch_*.json', 'Shannon', 'Hermes'] |

**String policy:**
| Category | Owner | Policy |
| --- | --- | --- |
| execution_artifacts | megaplan | Execution-batch and finalize artifact names remain plugin-owned until Arnold has a neutral execution-report contract. |
| worker_routes | megaplan | Provider/session policy stays plugin-local until the generic worker runtime is split from phase/prompt semantics. |

**Extraction prerequisites:**
- Split generic worker result/process/env helpers from prompt/schema/state-aware dispatch policy.
- Re-home Megaplan execution-batch/finalize payload normalization and mock payload generation under the plugin.
- Keep vendor-specific worker launchers plugin-local until Arnold defines neutral worker routing and session policy seams.

**First extraction unit:** `arnold/runtime/worker_runner.py`

**Tests / gates:**
- static-gate: arnold/runtime/workers/** must not normalize `task_updates` / `sense_check_acknowledgments` payloads
- static-gate: arnold/runtime/workers/** must not import Megaplan prompt factories or execution schema names

---

**Child symbols:** _(none)_

### megaplan/workers/_impl.py
**Disposition:** `split-required` | **Granularity:** `split` | **Target:** arnold/runtime/worker_runner.py (generic command/result/env helpers) + megaplan/pipelines/megaplan/worker_policy.py (phase prompts, schemas, session recovery, and batch-payload normalization)

**Reason:** `workers/_impl.py` is the main mixed worker file: it contains reusable subprocess/result helpers, but also Megaplan prompt creation, schema routing, plan/work-dir policy, session handling, and execution-batch payload semantics.

**Blockers:**
- Imports `PlanState`, `DEFAULT_AGENT_ROUTING`, prompt factories, schemas, robustness checks, and many `_core` helpers tied to Megaplan phase execution.
- `validate_payload()` and `_normalize_worker_payload()` own Megaplan-specific `task_updates`, `sense_check_acknowledgments`, `finalize`, and `review` payload contracts.
- Codex/Claude/Hermes dispatch paths synthesize Megaplan-branded commands, warnings, environment variables, and work-dir/sandbox behavior.

**Import policy:**
- Allowed: `megaplan.runtime.process`
- Forbidden: `megaplan.handlers`, `megaplan.execute`, `megaplan.finalize`

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| worker_primitives | arnold | ['CommandResult', 'WorkerResult', 'run_command'] |
| execution_payloads | megaplan | ['task_updates', 'sense_check_acknowledgments', 'finalize', 'review', 'execution'] |
| artifact_paths | megaplan | ['finalize.json', 'execution_batch_*.json', '.megaplan'] |

**String policy:**
| Category | Owner | Policy |
| --- | --- | --- |
| execution_artifacts | megaplan | Batch/finalize/review payload contracts and artifact names remain plugin-owned. |
| env_names | megaplan | Megaplan-branded worker env names must stay on the plugin side of the split. |

**Extraction prerequisites:**
- Extract `CommandResult`, `WorkerResult`, and generic `run_command()` supervision helpers into an Arnold-owned runtime module.
- Leave prompt/schema/state-aware dispatch, payload normalization, and Megaplan env/work-dir policy in plugin-owned worker policy modules.
- Define a neutral worker output contract before moving any execution-batch normalization logic under Arnold.

**First extraction unit:** `arnold/runtime/worker_runner.py`

**Tests / gates:**
- static-gate: arnold/runtime/worker_runner.py must not import prompt factories, schemas, or `PlanState`
- static-gate: Arnold worker runtime helpers must not mention `task_updates`, `sense_check_acknowledgments`, or `finalize.json`

**Configurable seams:**
| Declaration Site | Inherits Via | Override Mechanism | Meaning Owner | Supported State |
| --- | --- | --- | --- | --- |
| megaplan/workers/_impl.py (set_work_dir_override / resolve_work_dir) | ContextVar-scoped worker working-directory selection | caller-supplied work-dir override before worker dispatch | megaplan | set; policy depends on current Megaplan project_dir/worktree semantics |

---

**Child symbols** (2):

- `megaplan/workers/_impl.py` → arnold worker-runtime symbols: CommandResult / WorkerResult / run_command
- `megaplan/workers/_impl.py` → megaplan worker-policy symbols: validate_payload / _normalize_worker_payload / resolve_agent_mode / run_step_with_worker / mock_worker_output

### megaplan/observability/events.py
**Disposition:** `split-required` | **Granularity:** `split` | **Target:** arnold/observability/events.py (generic journal) / megaplan/pipelines/megaplan/observability/events.py (Megaplan policy)

**Reason:** Core event journal with generic EventWriter/emit/read_events mechanics but embeds `.megaplan` path conventions, Store integration, and RunEnvelope context resolution that are Megaplan-specific.

**Blockers:**
- _default_store_root derives store location from `.megaplan` path structure (line 418-420)
- _default_store_for_plan_dir imports `megaplan.store.file.FileStore`
- _envelope_ctx ContextVar carries `megaplan._pipeline.envelope.RunEnvelope`
- _store_event dispatches to `record_epic_event`, `append_telemetry_event`, `log_system_event` — Megaplan Store surface
- Sidecar file names (`.events.seq`, `.events.init_ts`, `events.ndjson`, `telemetry.ndjson`) are Megaplan conventions

**Import policy:**
- Forbidden: `megaplan.planning`, `megaplan.handlers`, `megaplan.orchestration`

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| event_kinds | arnold | ['init', 'phase_start', 'phase_end', 'phase_retry', 'tier_drop', 'state_transition', 'state_written', 'lock_acquired', 'lock_released', 'plan_aborted', 'plan_finished', 'subprocess_spawned', 'subprocess_exited', 'subprocess_signaled', 'llm_call_start', 'llm_token_heartbeat', 'llm_call_end', 'llm_call_error', 'artifact_written', 'artifact_invalidated', 'override_applied', 'flag_raised', 'flag_resolved', 'note_added', 'tier_escalated', 'cost_recorded', 'evaluand_recorded', 'health_check_failed', 'drift_detected', 'activation_transitioned', 'state_cache_drift', 'capability_claim', 'calibration_experiment'] |
| artifact_names | megaplan | ['events.ndjson', '.events.seq', '.events.init_ts', 'telemetry.ndjson'] |
| megaplan_paths | megaplan | ['.megaplan', '.megaplan-event-store'] |

**String policy:**
| Category | Owner | Policy |
| --- | --- | --- |
| event_kinds | arnold | All 33 event kind string literals are Arnold-owned vocabulary. New kinds require Arnold schema approval. |
| artifact_names | megaplan | Per-plan-dir NDJSON/seq/telemetry sidecar names are Megaplan conventions; Arnold shared-leaf must use a configurable backing path. |
| megaplan_paths | megaplan | `.megaplan` and `.megaplan-event-store` directory conventions are Megaplan-owned; Arnold shared-leaf must accept a configurable store root. |

**Extraction prerequisites:**
- Extract Store integration behind a protocol/DI seam so the generic journal does not import megaplan.store.
- Replace `_default_store_root` with a configurable store-root parameter.
- Replace `_envelope_ctx` RunEnvelope dependency with a generic envelope protocol.
- Make sidecar file names configurable or parameterized.

**First extraction unit:** `arnold/observability/events.py`

**Tests / gates:**
- static-gate: Arnold-shared events.py must not import megaplan.store, megaplan._pipeline.envelope, or reference `.megaplan` paths

**Configurable seams:**
| Declaration Site | Inherits Via | Override Mechanism | Meaning Owner | Supported State |
| --- | --- | --- | --- | --- |
| megaplan/observability/events.py::_default_store_root | hardcoded .megaplan path convention | configurable store_root parameter | megaplan (current), arnold (after extraction) | megaplan-path-derived (current) |

---

**Child symbols** (5):

- `megaplan/observability/events.py` → arnold/observability/events.py::EventKind
- `megaplan/observability/events.py` → arnold/observability/events.py::EventWriter
- `megaplan/observability/events.py` → arnold/observability/events.py::read_events
- `megaplan/observability/events.py` → arnold/observability/events.py::emit
- `megaplan/observability/events.py` → megaplan/pipelines/megaplan/observability/events.py::_envelope_ctx

### megaplan/observability/fold.py
**Disposition:** `split-required` | **Granularity:** `split` | **Target:** arnold/observability/fold.py (generic fold) / megaplan/pipelines/megaplan/observability/fold.py (Megaplan policy)

**Reason:** Pure fold-over-events algorithm is generic, but `rebuild_state_from_wal` and `assert_fold_equiv` are wired to Megaplan `state.json` authority, plan-dir conventions, and gate_signals file patterns.

**Blockers:**
- `rebuild_state_from_wal` reads `plan_dir/state.json` — Megaplan state authority convention.
- `assert_fold_equiv` compares fold against live `state.json` — Megaplan-specific oracle.
- `read_events` calls `ensure_events_projection` from `events_projection.py`, which imports `megaplan.store`.
- `_NDJSON_FILE = events.ndjson` — Megaplan artifact name convention.

**Import policy:**
- Allowed: `megaplan.observability.events`, `megaplan.observability.events_projection`
- Forbidden: `megaplan.planning`, `megaplan.control_interface`, `megaplan._core`, `megaplan._pipeline`

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| fold_artifacts | megaplan | ['state.json', 'events.ndjson', 'gate_signals_v*.json'] |

**String policy:**
| Category | Owner | Policy |
| --- | --- | --- |
| megaplan_paths | megaplan | `state.json`, `events.ndjson`, and `gate_signals_v*.json` paths are Megaplan-owned plan-dir conventions. |
| artifact_names | megaplan | NDJSON file name `events.ndjson` is a Megaplan convention; Arnold shared-leaf fold must accept a configurable event source. |

**Extraction prerequisites:**
- Decouple fold algorithm from events_projection.py Store dependency; wire through a generic event-source protocol.
- Remove `state.json` authority assumption; make the fold target parameterizable.

**First extraction unit:** `arnold/observability/fold.py`

**Tests / gates:**
- static-gate: Arnold-shared fold.py must not import megaplan.store, megaplan.observability.events_projection, or reference `state.json`/`gate_signals_v*`

**Configurable seams:**
| Declaration Site | Inherits Via | Override Mechanism | Meaning Owner | Supported State |
| --- | --- | --- | --- | --- |
| megaplan/observability/fold.py::rebuild_state_from_wal | hardcoded plan_dir/state.json convention | parameterizable event source + fold target | megaplan (current), arnold (after extraction) | plan_dir-derived (current) |

---

**Child symbols** (4):

- `megaplan/observability/fold.py` → arnold/observability/fold.py::fold_events
- `megaplan/observability/fold.py` → arnold/observability/fold.py::read_events
- `megaplan/observability/fold.py` → megaplan/pipelines/megaplan/observability/fold.py::rebuild_state_from_wal
- `megaplan/observability/fold.py` → megaplan/pipelines/megaplan/observability/fold.py::assert_fold_equiv

### megaplan/observability/effect_ledger.py
**Disposition:** `split-required` | **Granularity:** `split` | **Target:** arnold/observability/effect_ledger.py (generic types) / megaplan/pipelines/megaplan/observability/effect_ledger.py (Megaplan policy)

**Reason:** Generic Effect/ReplayClass/NONCOMPENSABLE type skeleton is reusable, but docstring references `megaplan._pipeline.envelope.RunEnvelope.taint` and `megaplan.observability.events.emit_state_wal` as the journal target — Megaplan-specific wiring.

**Blockers:**
- Docstring references `megaplan._pipeline.envelope.RunEnvelope.taint` as the per-step pipeline taint concept (line 15-18).
- Docstring references `megaplan.observability.events.emit_state_wal` as the round-trip target (line 40).
- `NONCOMPENSABLE` sentinel string `__noncompensable__` is an Arnold-owned convention documentable in the shared portion.

**Import policy:**
- Forbidden: `megaplan._pipeline`, `megaplan.planning`, `megaplan.store`

**Vocabulary owned:**
| Kind | Owner | Terms |
| --- | --- | --- |
| effect_types | arnold | ['Effect', 'ReplayClass', 'NONCOMPENSABLE', 'pure', 'idempotent_keyed', 'at_most_once', 'pivot'] |

**String policy:**
| Category | Owner | Policy |
| --- | --- | --- |
| effect_replay_classes | arnold | ReplayClass enum values (pure, idempotent_keyed, at_most_once, pivot) are Arnold-owned type vocabulary. |
| compensation_sentinel | arnold | `NONCOMPENSABLE` sentinel string `__noncompensable__` is Arnold-owned; plugins must use this sentinel, not invent their own. |

**Extraction prerequisites:**
- Extract generic Effect/ReplayClass types without the _pipeline.envelope.RunEnvelope docstring reference.

**First extraction unit:** `arnold/observability/effect_ledger.py`

**Tests / gates:**
- static-gate: Arnold-shared effect_ledger.py must not reference megaplan._pipeline.envelope or megaplan.observability.events.emit_state_wal

---

**Child symbols** (3):

- `megaplan/observability/effect_ledger.py` → arnold/observability/effect_ledger.py::ReplayClass
- `megaplan/observability/effect_ledger.py` → arnold/observability/effect_ledger.py::Effect
- `megaplan/observability/effect_ledger.py` → arnold/observability/effect_ledger.py::NONCOMPENSABLE

### megaplan/_pipeline/executor.py
**Disposition:** `split-required` | **Granularity:** `split` | **Target:** arnold/pipeline/executor.py (neutral core loop + dispatch skeleton) + megaplan/pipelines/megaplan/executor_adapter.py (governor, activation, evaluand, port-binding policy)

**Reason:** The executor skeleton (stage walk, _atomic_write_json, _verify_outputs) is neutral, but the current file is deeply entangled with Megaplan policy — governor charging, activation lifecycle, evaluand transactions, port-binding dispatch, unified-dispatch flag gates, and in-process runtime install — all of which must stay in the Megaplan plugin.

**Blockers:**
- Governor charge/fold, activation lifecycle, evaluand transaction wrappers, and runtime-governor install are Megaplan policy.
- Port-binding dispatch (typed_ports_on) and unified-dispatch/evaluand feature-flag gates are entangled in the main loop.
- Imports megaplan._core.state, megaplan.types, megaplan.runtime.governor, megaplan._core.activation, megaplan.observability.events.

**Extraction prerequisites:**
- Extract the neutral stage-walk skeleton, atomic I/O helpers, and _verify_outputs into arnold/pipeline/executor.py.
- Leave governor, activation, evaluand, port-binding, and flag-gated dispatch paths behind in a Megaplan bridge adapter.

**First extraction unit:** `arnold/pipeline/executor.py`

---

**Child symbols** (5):

- `megaplan/_pipeline/executor.py` → arnold neutral symbol: _atomic_write_json
- `megaplan/_pipeline/executor.py` → arnold neutral symbol: _write_forensic_backup
- `megaplan/_pipeline/executor.py` → arnold neutral symbol: _verify_outputs
- `megaplan/_pipeline/executor.py` → arnold neutral symbol: _is_safe_for_parallel
- `megaplan/_pipeline/executor.py` → arnold neutral symbol: run_pipeline

### megaplan/_pipeline/pattern_topology.py
**Disposition:** `split-required` | **Granularity:** `split` | **Target:** arnold/pipeline/pattern_topology.py (neutral: panel_parallel, alternating_turns, subpipeline_call, iterate_until) + megaplan/pipelines/megaplan/pattern_topology_adapter.py (critique_revise_gate_loop, escalate_if, escalate_via_subpipeline, phase_zero_gate, mode_prompts)

**Reason:** panel_parallel, alternating_turns, subpipeline_call, and iterate_until are neutral topology builders, but critique_revise_gate_loop, escalate_if, escalate_via_subpipeline, phase_zero_gate, and mode_prompts embed Megaplan gate vocabulary (critique/gate/revise/tiebreaker/escalate/proceed/iterate/plan) and use Overlay (excluded from Arnold per SD2).

**Blockers:**
- critique_revise_gate_loop hardcodes Megaplan planning stage names and gate recommendation literals.
- escalate_if/escalate_via_subpipeline use Megaplan escalation vocabulary and restore_and_diverge sentinel.
- phase_zero_gate defaults on_pass='plan' (Megaplan phase name).
- mode_prompts produces Overlay instances excluded from Arnold per SD2.

**Extraction prerequisites:**
- Move neutral functions (panel_parallel, alternating_turns, subpipeline_call, iterate_until) into arnold/pipeline/pattern_topology.py.
- Leave Megaplan-gate functions and mode_prompts behind in a Megaplan adapter module.

**First extraction unit:** `arnold/pipeline/pattern_topology.py`

---

**Child symbols** (4):

- `megaplan/_pipeline/pattern_topology.py` → arnold neutral symbol: panel_parallel
- `megaplan/_pipeline/pattern_topology.py` → arnold neutral symbol: alternating_turns
- `megaplan/_pipeline/pattern_topology.py` → arnold neutral symbol: subpipeline_call
- `megaplan/_pipeline/pattern_topology.py` → arnold neutral symbol: iterate_until

### megaplan/_pipeline/registry.py
**Disposition:** `split-required` | **Granularity:** `split` | **Target:** arnold/pipeline/registry.py (PipelineRegistry CRUD + skill.md reading) + megaplan/pipelines/megaplan/registry_adapter.py (discovery, trust, manifest, budget)

**Reason:** The core PipelineRegistry dataclass (register, get, names, describe, metadata_for, read_skill_md, operation_registry_for) is neutral, but the discovery path hardcodes megaplan.pipelines scan roots, MEGAPLAN_M6_MANIFEST_DISCOVERY env vars, trust classification, manifest parsing, and budget authority integration — all Megaplan-branded policy.

**Blockers:**
- Discovery scan roots and env vars are branded 'megaplan'.
- Trust-tier classification and manifest parsing are coupled to Megaplan discovery conventions.
- Budget authority integration imports megaplan.runtime.budget_authority.
- CANONICAL_BUILTIN_PIPELINE = 'megaplan' and LEGACY_PIPELINE_ALIASES hardcode Megaplan names.

**Extraction prerequisites:**
- Extract PipelineRegistry class, global singleton, and skill.md reading into arnold/pipeline/registry.py.
- Leave discovery, trust, manifest, and budget authority behind in a Megaplan bridge adapter.

**First extraction unit:** `arnold/pipeline/registry.py`

---

**Child symbols** (10):

- `megaplan/_pipeline/registry.py` → arnold neutral symbol: PipelineRegistry
- `megaplan/_pipeline/registry.py` → arnold neutral symbol: _GLOBAL_REGISTRY
- `megaplan/_pipeline/registry.py` → arnold neutral symbol: register_pipeline
- `megaplan/_pipeline/registry.py` → arnold neutral symbol: get_pipeline
- `megaplan/_pipeline/registry.py` → arnold neutral symbol: registered_pipelines
- `megaplan/_pipeline/registry.py` → arnold neutral symbol: describe_pipeline
- `megaplan/_pipeline/registry.py` → arnold neutral symbol: pipeline_metadata
- `megaplan/_pipeline/registry.py` → arnold neutral symbol: read_skill_md
- `megaplan/_pipeline/registry.py` → arnold neutral symbol: operation_registry_for
- `megaplan/_pipeline/registry.py` → arnold neutral symbol: canonical_pipeline_name

## Import / String Policy Summary

### Forbidden Imports

- `megaplan` — forbidden from: `arnold/pipeline/__init__.py`, `arnold/pipeline/state.py`
- `megaplan._core` — forbidden from: `arnold/pipeline/resources.py`, `arnold/pipeline/types.py`, `megaplan/_pipeline/types.py`, `megaplan/observability/__init__.py`, `megaplan/observability/composition_obs.py` (+3 more)
- `megaplan._core.activation` — forbidden from: `arnold/pipeline/executor.py`
- `megaplan._core.state` — forbidden from: `megaplan/control_interface.py`
- `megaplan._pipeline` — forbidden from: `megaplan/observability/__init__.py`, `megaplan/observability/composition_obs.py`, `megaplan/observability/effect_ledger.py`, `megaplan/observability/evaluand.py`, `megaplan/observability/event_sink.py` (+2 more)
- `megaplan._pipeline.types` — forbidden from: `megaplan/control_interface.py`
- `megaplan.agent` — forbidden from: `megaplan/agent_runtime`
- `megaplan.auto` — forbidden from: `megaplan/drivers/in_process.py`
- `megaplan.cli` — forbidden from: `megaplan/auto.py`, `megaplan/cli/arnold.py`, `megaplan/drivers`, `megaplan/runtime/governor.py`, `megaplan/store/__init__.py` (+17 more)
- `megaplan.cli.feedback` — forbidden from: `arnold/pipeline/types.py`, `megaplan/_pipeline/types.py`
- `megaplan.control_interface` — forbidden from: `megaplan/observability/__init__.py`, `megaplan/observability/evaluand.py`, `megaplan/observability/event_sink.py`, `megaplan/observability/fold.py`
- `megaplan.execute` — forbidden from: `arnold/pipeline/resources.py`, `megaplan/_core/scheduler`, `megaplan/_pipeline`, `megaplan/_pipeline/contracts.py`, `megaplan/_pipeline/discovery/manifest.py` (+9 more)
- `megaplan.finalize` — forbidden from: `megaplan/_core/scheduler`, `megaplan/_pipeline`, `megaplan/_pipeline/contracts.py`, `megaplan/_pipeline/discovery/manifest.py`, `megaplan/_pipeline/discovery/trust.py` (+5 more)
- `megaplan.handlers` — forbidden from: `arnold/pipeline/resources.py`, `megaplan/_core/scheduler`, `megaplan/_pipeline`, `megaplan/_pipeline/contracts.py`, `megaplan/_pipeline/discovery/manifest.py` (+29 more)
- `megaplan.observability.events` — forbidden from: `arnold/pipeline/executor.py`
- `megaplan.orchestration` — forbidden from: `megaplan/observability/events.py`
- `megaplan.orchestration.progress` — forbidden from: `megaplan/_pipeline/types.py`, `megaplan/auto.py`
- `megaplan.pipelines` — forbidden from: `arnold/pipeline/registry.py`
- `megaplan.planning` — forbidden from: `megaplan/agent_runtime`, `megaplan/control_interface.py`, `megaplan/observability/__init__.py`, `megaplan/observability/composition_obs.py`, `megaplan/observability/effect_ledger.py` (+4 more)
- `megaplan.profiles` — forbidden from: `megaplan/drivers/in_process.py`, `megaplan/drivers/subprocess_isolated.py`, `megaplan/runtime/governor.py`, `megaplan/runtime/sandbox.py`
- `megaplan.prompts` — forbidden from: `megaplan/workers/_impl.py`
- `megaplan.runtime.budget_authority` — forbidden from: `arnold/pipeline/registry.py`
- `megaplan.runtime.governor` — forbidden from: `arnold/pipeline/executor.py`
- `megaplan.schemas` — forbidden from: `megaplan/workers/_impl.py`
- `megaplan.store` — forbidden from: `megaplan/auto.py`, `megaplan/observability/composition_obs.py`, `megaplan/observability/effect_ledger.py`, `megaplan/observability/evaluand.py`, `megaplan/observability/event_sink.py`
- `megaplan.types` — forbidden from: `arnold/pipeline/types.py`, `megaplan/_pipeline/types.py`, `megaplan/auto.py`, `megaplan/profiles/__init__.py`, `megaplan/runtime/process.py` (+1 more)
- `megaplan.workers` — forbidden from: `megaplan/runtime/governor.py`, `megaplan/runtime/process.py`

### Allowed Import Overrides

- `megaplan._core.canonical` — allowed in: `megaplan/store/capsule.py`, `megaplan/store/warrant.py`
- `megaplan._core.config_resolver` — allowed in: `megaplan/store/warrant.py`
- `megaplan._core.io` — allowed in: `megaplan/store/_file`, `megaplan/store/blob.py`, `megaplan/store/file.py`, `megaplan/store/multi.py`, `megaplan/store/plan_repository.py`
- `megaplan._core.state` — allowed in: `megaplan/store/plan_repository.py`
- `megaplan._pipeline.behavioral_manifest` — allowed in: `megaplan/store/capsule.py`
- `megaplan._pipeline.contracts` — allowed in: `megaplan/store/capsule.py`
- `megaplan._pipeline.envelope` — allowed in: `megaplan/_pipeline/types.py`, `megaplan/observability/events.py`, `megaplan/runtime/governor.py`
- `megaplan._pipeline.registry` — allowed in: `megaplan/cli/arnold.py`
- `megaplan._pipeline.types` — allowed in: `megaplan/drivers/in_process.py`, `megaplan/drivers/subprocess_isolated.py`
- `megaplan.auto` — allowed in: `megaplan/drivers/subprocess_isolated.py`
- `megaplan.drivers.in_process` — allowed in: `megaplan/drivers/__init__.py`
- `megaplan.drivers.subprocess_isolated` — allowed in: `megaplan/drivers/__init__.py`
- `megaplan.observability.effect_ledger` — allowed in: `megaplan/observability/effect_enforcement.py`
- `megaplan.observability.event_sink` — allowed in: `megaplan/observability/effect_enforcement.py`
- `megaplan.observability.events` — allowed in: `megaplan/observability/cost.py`, `megaplan/observability/doctor.py`, `megaplan/observability/effect_enforcement.py`, `megaplan/observability/evaluand.py`, `megaplan/observability/event_sink.py` (+3 more)
- `megaplan.observability.events_projection` — allowed in: `megaplan/observability/fold.py`, `megaplan/store/plan_repository.py`
- `megaplan.orchestration.feedback` — allowed in: `megaplan/store/plan_repository.py`
- `megaplan.run_outcome` — allowed in: `megaplan/control_interface.py`
- `megaplan.runtime.key_pool` — allowed in: `megaplan/workers/hermes.py`
- `megaplan.runtime.process` — allowed in: `megaplan/auto.py`, `megaplan/drivers/subprocess_isolated.py`, `megaplan/workers/_impl.py`, `megaplan/workers/shannon.py`
- `megaplan.runtime.sandbox` — allowed in: `megaplan/workers/hermes.py`
- `megaplan.schemas` — allowed in: `megaplan/store/__init__.py`, `megaplan/store/_db`, `megaplan/store/_file`, `megaplan/store/base.py`, `megaplan/store/blob.py` (+10 more)
- `megaplan.schemas.arnold` — allowed in: `megaplan/store/base.py`
- `megaplan.schemas.base` — allowed in: `megaplan/store/base.py`
- `megaplan.schemas.sprint1` — allowed in: `megaplan/store/base.py`
- `megaplan.store._db` — allowed in: `megaplan/store/db.py`
- `megaplan.store._db.common` — allowed in: `megaplan/store/db.py`
- `megaplan.store._file` — allowed in: `megaplan/store/file.py`
- `megaplan.store.base` — allowed in: `megaplan/store/__init__.py`, `megaplan/store/_db`, `megaplan/store/_file`, `megaplan/store/compat.py`, `megaplan/store/legacy_migration.py` (+2 more)
- `megaplan.store.blob` — allowed in: `megaplan/store/__init__.py`, `megaplan/store/capsule.py`, `megaplan/store/compat.py`, `megaplan/store/db.py`
- `megaplan.store.capsule` — allowed in: `megaplan/store/__init__.py`
- `megaplan.store.compat` — allowed in: `megaplan/store/__init__.py`
- `megaplan.store.db` — allowed in: `megaplan/store/__init__.py`, `megaplan/store/identity.py`, `megaplan/store/multi.py`
- `megaplan.store.export` — allowed in: `megaplan/store/capsule.py`
- `megaplan.store.file` — allowed in: `megaplan/store/__init__.py`, `megaplan/store/legacy_migration.py`, `megaplan/store/multi.py`
- `megaplan.store.identity` — allowed in: `megaplan/store/__init__.py`
- `megaplan.store.multi` — allowed in: `megaplan/store/__init__.py`, `megaplan/store/legacy_migration.py`
- `megaplan.store.plan_repository` — allowed in: `megaplan/store/__init__.py`
- `megaplan.store.snapshot` — allowed in: `megaplan/store/_db`, `megaplan/store/_file`, `megaplan/store/export.py`, `megaplan/store/file.py`
- `megaplan.store.warrant` — allowed in: `megaplan/store/__init__.py`
- `megaplan.tickets` — allowed in: `megaplan/store/_db`, `megaplan/store/_file`
- `megaplan.tickets.files` — allowed in: `megaplan/store/file.py`
- `megaplan.tickets.identity` — allowed in: `megaplan/store/file.py`
- `megaplan.workers._impl` — allowed in: `megaplan/workers/__init__.py`

### Vocabulary Ownership

- **arnold:** `AUTO_EXEC`, `ArnoldBlobAdapter`, `ArnoldStoreAdapter`, `ArtifactRequest`, `AutomationActor`, `BLESSED`, `Backend`, `BetterResult`, `BindResult`, `BlobMissingError`, `BlobRef`, `BlobStat`, `BlobStore`, `BotTurn`, `BudgetExceeded`, `CLAUDE_PRICING`, `Capsule`, `CapsuleContract`, `CapsuleDefinition`, `CapsuleEvidence`, `CapsuleLineage`, `CapsuleRecordRef`, `CapsuleWriteResult`, `ChecklistItem`, `CloudRun`, `CodeArtifact`, `CommandResult`, `CompositionObservability`, `ContentTypeRegistry`, `ContractLedger`, `ControlBinding`, `ControlMessage`, `ControlProjection`, `ControlTarget`, `ControlTransition`, `ControlTransitionRequest`, `ControlTransitionResult`, `DEFAULT_PRICING`, `Effect`, `Epic`, `EpicEvent`, `EpicLock`, `EpicSnapshot`, `EpicSummary`, `EvaluandRecord`, `EventEnvelope`, `EventKind`, `EventSink`, `ExceedReason`, `ExecutionLease`, `ExternalRequest`, `FIREWORKS_PRICING`, `Feedback`, `Governor`, `Image`, `InMemoryCompositionObs`, `InProcessDriver`, `LocalDirBlobStore`, `MODEL_PARAM_KEYS`, `Message`, `MigrationRun`, `ModelIORef`, `ModelIdentity`, `NONCOMPENSABLE`, `NdjsonBackend`, `NormalizedDict`, `NormalizedList`, `PRICING`, `Plan`, `PlanArtifact`, `PlanConfig`, `PlanMeta`, `PlanState`, `Port`, `PortBindError`, `PortRef`, `ProcessDriver`, `QUARANTINED`, `ReJudgeOutcome`, `RecordedIOScorer`, `RecordedIOUnavailable`, `RecordedModelIO`, `Reduce`, `ReduceResult`, `RepairGradient`, `ReplayClass`, `ResidentConversation`, `RoutingKey`, `RunStateView`, `SANDBOX_CWD`, `STATE_WRITTEN`, `SUBSTRATES`, `SandboxViolation`, `SecondOpinion`, `SelectionResult`, `Sprint`, `SprintItem`, `StateDelta`, `StorageModel`, `Store`, `StoreBackend`, `StoredEvent`, `SubprocessIsolatedDriver`, `Substrate`, `SupabaseStorageBlobStore`, `SystemLog`, `TOPOLOGIES`, `Ticket`, `ToolCall`, `Topology`, `Transaction`, `Warrant`, `WarrantAccount`, `WarrantAuthority`, `WarrantBuildResult`, `WarrantError`, `WarrantRationaleAnchor`, `WarrantSignature`, `WarrantSourceCompleteness`, `WarrantSourceProjection`, `WatcherState`, `WorkerResult`, `__noncompensable__`, `_supervise_subprocess`, `activation_transitioned`, `argv`, `arnold_api_version`, `artifact_invalidated`, `artifact_written`, `at_most_once`, `auto`, `budget_delta`, `build_warrant`, `cache_get`, `cache_set`, `calibration_experiment`, `cancellation`, `capabilities`, `capability_claim`, `capacity_grant`, `capsule_record_blob_id`, `check`, `compensation`, `compute_identity`, `cost`, `cost_from_usage`, `cost_recorded`, `current_governor`, `current_substrate`, `dag`, `deadline`, `decision`, `default`, `default_profile`, `describe`, `description`, `deterministic_idempotency_key`, `doctor`, `drift_detected`, `driver`, `effect_taint`, `entrypoint`, `estimate_tokens_from_cost`, `evaluand_recorded`, `extends`, `fanout`, `fencing_token`, `flag_raised`, `flag_resolved`, `fold_events`, `health_check_failed`, `idempotency_key`, `idempotent_keyed`, `identity_cache_key`, `idle_cap`, `in_process`, `init`, `install_sandbox`, `kill_group`, `lease_id`, `lineage`, `linear`, `list`, `llm_call_end`, `llm_call_error`, `llm_call_start`, `llm_token_heartbeat`, `lock_acquired`, `lock_released`, `name`, `new`, `note_added`, `override_applied`, `params_hash`, `phase_end`, `phase_retry`, `phase_start`, `piece_identity`, `pipelines`, `pivot`, `plan_aborted`, `plan_finished`, `profiles`, `prompt_hash`, `provenance`, `pure`, `put_capsule_record`, `replay_class`, `reset_substrate`, `retry`, `retry_budget`, `run`, `run_command`, `run_scheduler`, `run_step`, `schedule_batches`, `set_governor`, `set_work_dir_override`, `shadow-WAL`, `spawn`, `spawn_async`, `state_cache_drift`, `state_transition`, `state_written`, `step_boundary`, `subprocess_exited`, `subprocess_isolated`, `subprocess_signaled`, `subprocess_spawned`, `supported_modes`, `taint`, `tier_drop`, `tier_escalated`, `utc_now`, `validate_plan_artifact_name`, `validate_terminal_command`, `validate_v4a_patch`, `validate_write_path`, `verify_warrant`, `wall_cap`, `warrant_signed_envelope`
- **megaplan:** `.events.init_ts`, `.events.seq`, `.megaplan`, `.megaplan-event-store`, `.megaplan-worktrees`, `.plan.lock`, `ACCEPTED_WITH_DEBT`, `ADVANCE_WITH_DEBT`, `ArnoldStoreAdapter`, `AutomationActorKind`, `BakeoffBinding`, `BakeoffProfileRecord`, `BakeoffState`, `BatchResult`, `CRITIQUE_CHECKS`, `CapabilityClaim`, `ChainRunner`, `ChainSpec`, `Claude`, `CliError`, `CloudRun`, `CloudRunOperation`, `CloudRunStatus`, `CloudSpec`, `ControlIntent`, `ControlMessage`, `DBStore`, `DISK_SOURCE`, `DRIFT_KINDS`, `DRIFT_TOKENS`, `DoubleExecuteError`, `EditorialOperation`, `EditorialResult`, `EpicSnapshot`, `EvaluandRef`, `ExitKind`, `FALLBACK`, `FIXED`, `FailureCategory`, `FileStore`, `FlagRecord`, `FlagRegistry`, `GATE_CONTROL_INTENTS`, `HARD_BLOCK`, `Hermes`, `IterationResult`, `JudgeVerdict`, `Ladder`, `LoopSpec`, `LoopState`, `MEGAPLAN_ACTOR_ID`, `MEGAPLAN_AUTO_HEARTBEAT_SECONDS`, `MEGAPLAN_ENVELOPE_IN`, `MEMORY_SOURCE`, `MarkdownArtifact`, `MegaplanSpec`, `MigrationPhase`, `MigrationRun`, `MiniMax`, `ModelIdentity`, `MultiStore`, `NormalizedOutcome`, `OMIT`, `Observation`, `OpenRouter`, `OrphanDetectedError`, `PREP_RESEARCH_AREA_SCHEMA`, `PREP_RESEARCH_FINDING_SCHEMA`, `PREREQUISITE`, `PackRunner`, `PhaseResult`, `PlanArtifact`, `PlanArtifactKind`, `PlanArtifactRole`, `PlanConfig`, `PlanCurrentState`, `PlanMeta`, `PlanRepository`, `PlanState`, `ProgressContext`, `ProgressEvent`, `ProgressEventKind`, `QUALITY`, `RERUN_REQUIRED`, `RESOLVED`, `ROBUSTNESS_LEVELS`, `RailwaySpec`, `Receipt`, `RepoSpec`, `ReviewResult`, `RouteSuggestion`, `RunDriver`, `RunEnvelope`, `RunNode`, `RunOutcome`, `RunResultMetadata`, `SCHEMAS`, `STANCE_SCHEMA`, `STATE_ABORTED`, `STATE_AWAITING_HUMAN`, `STATE_AWAITING_PR_MERGE`, `STATE_BLOCKED`, `STATE_CANCELLED`, `STATE_CRITIQUED`, `STATE_DONE`, `STATE_EXECUTED`, `STATE_FAILED`, `STATE_FINALIZED`, `STATE_GATED`, `STATE_INITIALIZED`, `STATE_PAUSED`, `STATE_PLANNED`, `STATE_PREPPED`, `STATE_REVIEWED`, `STATE_TIEBREAKER_PENDING`, `STATE_TIEBREAKER_READY`, `STOP_SIGNAL_SCHEMA`, `SUPABASE_DB_URL`, `SUPPORTED_CONTROL_INTENTS`, `ScheduledJob`, `ScheduledJobStatus`, `Shannon`, `StepResponse`, `StoredEvent`, `SupervisorState`, `TERMINAL_STATES`, `TOKEN_USAGE_SCHEMA`, `Ticket`, `TicketEpicLink`, `TmuxSession`, `UNRESOLVED`, `USER_ACTION_RESOLUTION_KINDS`, `WorkerKind`, `_BOOTSTRAP_ACTOR_ID`, `_FileStoreTransaction`, `_envelope_ctx`, `_journal_intent`, `_phase_arg_overrides`, `_phase_namespace`, `_run_and_merge_batch`, `abort`, `aborted`, `adaptive_critique`, `add-note`, `add_note`, `aggregate_weighted_tier`, `apply_transition`, `artifact_dir`, `artifact_title`, `assert_fold_equiv`, `audit_engine`, `automation_actors`, `awaiting_human`, `awaiting_human_verify`, `awaiting_user.json`, `bakeoff`, `bakeoff_root`, `blobs`, `blocked`, `brief_path`, `briefs_dir`, `build_base_resolution_event`, `build_critique_prompt`, `build_execute_prompt`, `build_finalize_prompt`, `build_monitor_hint`, `build_planning_prompt`, `build_review_prompt`, `cancelled`, `capabilities`, `capsule_definition_identity_projection`, `checklist_items`, `ci_hook`, `claim_next_pending`, `classify_failure`, `classify_quality_resolution_behavior`, `classify_resolution_behavior`, `clear_user_action_resolutions`, `cli_entry`, `cloud_cli`, `collect_epic_export`, `comparison`, `confirm_self_review`, `context_snapshot`, `contract.json`, `control_binding`, `copying_blobs`, `copying_meta`, `create_automation_actor`, `create_brief`, `create_ticket`, `creative_form_id`, `critic_model`, `critique`, `critiqued`, `cutting_over`, `detect_orphans`, `directors_notes`, `done`, `drift_detection`, `edit_section`, `effective_resolutions`, `epic_path`, `epics`, `epics_dir`, `escalate`, `evaluate_blocked_tasks`, `evaluate_quality_blocked_tasks`, `evaluate_state_transition`, `events.ndjson`, `execute`, `executed`, `execution`, `execution_batch_*.json`, `failed`, `feedback`, `filter_keyword_artifacts`, `finalize`, `finalize.json`, `finalized`, `force-proceed`, `force_proceed`, `gate`, `gate.json`, `gate_signals`, `gate_signals_v*.json`, `gated`, `get_execution_schema_key`, `half_life_weight`, `handle_cost`, `handle_critique`, `handle_doctor`, `handle_execute`, `handle_execute_auto_loop`, `handle_execute_one_batch`, `handle_finalize`, `handle_gate`, `handle_init`, `handle_introspect`, `handle_override`, `handle_plan`, `handle_review`, `handle_revise`, `handle_tiebreaker`, `handle_trace`, `hinge_gate`, `home_backend`, `images`, `initialized`, `is_legal_coercion`, `iter_markdown_artifacts`, `iterate`, `iteration_pressure`, `journal_then_execute`, `latest_events_by_key`, `latest_quality_resolutions`, `legacy-local-plan`, `legacy_phase_command`, `legacy_supervise_subprocess`, `list_briefs`, `list_epics`, `list_tickets`, `load_spec`, `loop_engine`, `loop_plan`, `loop_prompts`, `main`, `make_flag_id`, `make_snippet`, `manifest.json`, `mark_processed`, `markdown_body`, `materialize_deploy_dir`, `max_tasks_per_batch`, `mechanical_checks`, `megaplan cost`, `megaplan doctor`, `megaplan introspect`, `megaplan trace`, `megaplan-epic-export-v1`, `migrate-local-plans`, `migration_runs`, `mock_worker_output`, `next_flag_number`, `normalize_flag_record`, `orchestrator`, `pane_pids`, `parse_markdown_artifact`, `paused`, `pending_human`, `phase-6-idea.txt`, `phase_result.json`, `plan`, `plan_artifacts`, `planned`, `planning`, `planning_topology`, `plans`, `prep`, `prep_models`, `prepped`, `proceed`, `process_control_message`, `project_tier_models`, `prompt_hash_canonical`, `prompt_snapshot`, `provocations`, `read_user_action_resolutions`, `rebuild_state_from_wal`, `receipt_id`, `receipt_report`, `record_tiebreaker_audit`, `recover-blocked`, `recovery_policy`, `replan`, `resolve_agent_mode`, `resolve_binding`, `resolve_severity`, `resolve_user_action`, `resume-clarify`, `retry_blocked_tasks`, `review`, `review.json`, `revise`, `rows`, `run_chain_cli`, `run_codex_prep_step`, `run_dual_green`, `run_id`, `run_metadata_from_batch_outcome`, `run_outcome_from_batch_outcome`, `run_parallel_review`, `run_phase`, `run_pre_checks`, `run_step_with_worker`, `scoped_legacy_audit`, `search_briefs`, `search_tickets`, `second_opinions`, `select_active_checks`, `sense_check_acknowledgments`, `sense_checks`, `set-model`, `set-profile`, `set-robustness`, `set-vendor`, `slugify`, `sprint_items`, `sprints`, `state.json`, `step_receipt_`, `store_method`, `strict_schema`, `success_criteria`, `supervise`, `task_updates`, `telemetry.ndjson`, `ticket_file_path`, `tickets`, `tickets_dir`, `tiebreaker`, `tiebreaker_challenger`, `tiebreaker_pending`, `tiebreaker_ready`, `tiebreaker_researcher`, `tier_models`, `tombstoning`, `transition_epic_state`, `unified_dispatch_on`, `update_body`, `update_flags_after_critique`, `update_flags_after_revise`, `upstream_artifact_hashes`, `user_action_resolutions_file`, `validate_quality_resolution_event`, `validate_robustness`, `validate_user_action_event`, `vendor_locked`, `verifying`, `worktree`, `write_markdown_artifact`, `write_user_action_resolution`

### String Policy Ownership

- **arnold** owns string categories: `budget_dimensions`, `compensation_sentinel`, `composition_beats`, `crash_isolation`, `effect_replay_classes`, `evaluand_kinds`, `event_kinds`, `idempotency_prefixes`, `sink_kinds`, `substrate_literals`, `tool_names`, `worker_primitives`
- **megaplan** owns string categories: `artifact_names`, `artifact_paths`, `audit_scopes`, `brief_resume_text`, `cli_entry_points`, `cli_flags`, `cloud_providers`, `config_keys`, `content_types`, `db_schema`, `decision_keys`, `discovery_paths`, `dispatch_flags`, `effect_intent_kind`, `env_names`, `env_vars`, `execution_artifacts`, `execution_payloads`, `export_formats`, `log_markers`, `megaplan_env_vars`, `megaplan_paths`, `megplan_env_vars`, `migration_phases`, `override_actions`, `package_exports`, `phase_names`, `pipeline_aliases`, `projection_fields`, `provider_policy`, `schema_identity`, `schema_modules`, `shell_guard_ledgers`, `stage_defaults`, `watcher_bindings`, `worker_routes`, `worker_runtime`, `worktree_paths`, `wrapper_command_literals`

## Parity Gates

_(none defined)_

## Runtime Settings Gates

_(none defined)_

## Next-Milestone Recommendations

- **Resolve 18 split-required row(s):** each split parent needs child symbols finalized before extraction can begin.
- **Investigate 18 legacy-hold row(s):** these files have unclear ownership and need deeper inspection.
- **Extract capability seams** (M2a) before relocating plugins (M2b) to `arnold/pipelines/megaplan/`.
- **Define parity gates:** no behavior-preservation gates have been declared.
- **Define runtime settings gates:** no configurable-seam gates have been declared.
