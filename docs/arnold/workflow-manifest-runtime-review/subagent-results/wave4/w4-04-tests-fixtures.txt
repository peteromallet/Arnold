Here is the inventory for `workflow-manifest-runtime` plan artifacts:

---

## Workflow-Manifest-Runtime — Plan Artifact Inventory

### 1. Tests

**Path/group** | **Current role** | **Final disposition** | **Plan hook** | **Gaps**
---|---|---|---|---
`tests/arnold/pipeline/test_*.py` (~70 files) | Neutral pipeline unit/integration tests (builder, executor, contracts, hooks, runner, artifacts, etc.) | **Migrate/refactor** — many will move to `tests/arnold/workflow/` and `tests/arnold/execution/`; builder/native tests get deleted or converted | M2/M3 — DSL and runner tests replace builder/executor-old tests | No `tests/arnold/workflow/` or `tests/arnold/execution/` directory exists yet
`tests/arnold/pipeline/native/test_*.py` (8 files) | Native compiler/runtime/checkpoint/decorator tests for `arnold.pipeline.native` | **Delete** — M6 purge; quarry-only, not canonical architecture | M6 — deleted after parity gates pass | 3 of 5 golden trace cases deferred (T14 `toy_megaplan.py` fixtures not yet landed)
`tests/arnold/runtime/test_*.py` (20 files) | Legacy `arnold.runtime` tests (driver, batch, replay, sandbox, envelope, etc.) | **Refactor-then-delete** — reusable pieces move to `tests/arnold/kernel/` and `tests/arnold/execution/`; rest deleted M6 | M3 — kernel/execution refactor; M6 — remaining surface deleted | No `tests/arnold/kernel/` directory exists; 20 tests still import old runtime
`tests/arnold/pipelines/megaplan/test_*.py` (~25 files) + `tests/pipelines/megaplan/**` (~20 files) | Megaplan product tests under legacy `arnold.pipelines.megaplan` | **Migrate** to `tests/arnold_pipelines/megaplan/` | M4 — product migration | Two test roots (`tests/arnold/pipelines/megaplan/` and `tests/pipelines/megaplan/`); need consolidation
`tests/pipelines/test_*_pipeline.py` (~10 files) | Shipped pipeline tests (creative, doc, jokes, live_supervisor, folder_audit, select_tournament, planning_routing) | **Migrate** — moved under new package layout with compile/dry-run/fake-run coverage | M5 — shipped pipelines migration | No explicit-node compile tests for any shipped pipeline; all still use old builder API
`tests/conftest.py`, `tests/__init__.py` | Root test configuration | **Keep/adapt** | All — fixtures evolve per milestone | —
`tests/characterization/` (13 test files + 2 corpuses) | Characterization/golden stability tests, corpus replay, CLI parser snapshots, import surface tests | **Keep and expand** — auto-drive corpus (35 JSON fixtures), supervisor replay corpus (5 JSON fixtures), golden recorders (3 files) | M1 (goldens), M4 (parity), M5 (snapshots) | CLI parser snapshot is a single JSON fixture, not generated
`tests/arnold/conformance/` (8 files) | Conformance gate tests (import coupling, behavioral suite, routing, evidence pack, megaplan coupling) | **Keep and burn down allowlists** — M6 must reduce 121-line `_allowlist.txt` + 4-line `_megaplan_coupling_allowlist.txt` to zero legacy exceptions | M1-M6 — ratchet baseline | 125 allowlist entries total; no burn-down tracking yet
`tests/arnold/agent/`, `tests/arnold/control/` | Agent adapter and control interface tests | **Keep** — neutral surfaces; adapt to capability/effect dispatch contracts | M3/M4 | —
`tests/oracle/`, `tests/oracles/` (14 files) | Oracle/dual-run/drift/replay tests | **Keep** — critical for M4 shadow/dual-run seam and M6 parity | M4, M6 | —
`tests/fixtures/` | Golden pipeline traces (3 JSON), state_json fixtures (3), legacy corpus (4 JSON), supervisor canary | **Keep golden fixtures; deprecate legacy corpus** — `tests/fixtures/corpus/` is superseded by characterization corpus | M1 (goldens), M4 (parity) | Legacy corpus marked deprecated but still in tree
`tests/bakeoff/`, `tests/boundary/`, `tests/m8/`, `tests/parity/`, `tests/orchestration/`, `tests/editorial_*.py` | Bakeoff supervisor, import-leak boundaries, m8 regression/benchmarks, graph parity, orchestration blast-radius, editorial lifecycle | **Mixed** — bakeoff/m8/parity likely survive; editorial may need migration | Various | —
Root `test_*.py` files (~250 files) | Top-level tests covering activation, chain, cloud, critique, dispatch, execute, finalize, governor, hermes, pipeline, receipts, review, supervisor, workers, etc. | **Triaged during M4/M5** — some migrate to `tests/arnold_pipelines/megaplan/`, some stay as integration smoke, some deleted | M4, M5, M6 | Massive undifferentiated root; ~250 test files without clear disposition

### 2. Fixtures & Golden Traces

| Path | Role | Disposition | Gap |
|---|---|---|---|
| `tests/fixtures/golden/pipeline_*.json` (3) | Golden behavior baselines (fresh run, iterate, resume-after-finalize) | **Keep** — M1 baselines; M4 parity gate | Only 3 golden routes; M1 needs more (tiebreaker, human/suspend, override/fallback, execute/review)
| `tests/characterization/auto_drive_corpus/` (35 JSON + MANIFEST) | Characterization oracle corpus — 35 deterministic DriverOutcome traces | **Keep** — M2.5 canonical corpus; regenerate via `pytest --write-fixture` | MANIFEST references `auto.py` line numbers; fragile to refactors
| `tests/characterization/supervisor_replay_corpus/` (5 JSON + MANIFEST) | Supervisor substrate-swap replay fixtures | **Keep** — M5d boundary | 5 fixtures; sparse coverage
| `tests/arnold/pipeline/native/data/` (2 JSON + README) | Golden graph traces for native/graph parity (full_run, resume) | **Delete with native** — M6 | 3 of 5 planned cases deferred; `_gen_golden_traces.py` depends on legacy native runtime imports
| `tests/fixtures/corpus/` (4 JSON) | Deprecated prototype corpus (happy, execute_stall, blocked_retry, escalate) | **Archive/delete** — superseded by characterization corpus | `_gen_corpus.py` marked DEPRECATED but still in tree

### 3. Conformance Allowlists

| Path | Entries | Role | Disposition | Gap |
|---|---|---|---|---|
| `arnold/conformance/_allowlist.txt` | 121 lines | Ratchet baseline for import-coupling, package-name-staleness, semantic-coupling, public-workflow-layering, never-port-artifacts | **Burn down to zero** by M6 | No burn-down tracking or per-entry owner/expiry; 5 categories with rationale comments but no M6 deletion mapping |
| `arnold/conformance/_megaplan_coupling_allowlist.txt` | 4 entries (arnold.cli + 3 agent adapters) | Known neutral-Arnold modules importing Megaplan code | **Burn down to zero** by M4 (product migration) | `arnold.cli` CLI forwarder coupling is acknowledged as intentional until M7

### 4. Legacy/Native Test Roots Keeping Old APIs Alive

| Root | Files | What it keeps alive | Plan hook |
|---|---|---|---|
| `tests/arnold/pipeline/native/test_*.py` | 8 test files | `arnold.pipeline.native` (compiler, runtime, decorators, checkpoint, graph_projection, routing, flags/context) | M6 — delete after parity; M1/M2 use as quarry only |
| `tests/arnold/pipeline/test_builder.py`, `test_executor.py`, `test_runner.py`, `test_steps.py`, etc. | ~30 files | `PipelineBuilder`, `Stage`, `Edge`, `ParallelStage`, `run_pipeline`, old executor/runner | M5 (inventory) → M6 (delete/convert) |
| `tests/arnold/runtime/test_*.py` | 20 files | `arnold.runtime` (driver, dry_run, process, sandbox, state_persistence, event_journal, etc.) | M3 (refactor to kernel/execution) → M6 (delete remainder) |
| `tests/arnold/pipelines/megaplan/test_*.py` + `tests/pipelines/megaplan/**` | ~45 files | `arnold.pipelines.megaplan` (legacy product surface) | M4 — migrate to `arnold_pipelines.megaplan` |
| `_gen_golden_traces.py`, `_gen_corpus.py` | 2 root scripts | Native runtime imports, legacy DriverOutcome | M6 — delete or archive |

### 5. Gaps Summary

1. **No `tests/arnold/workflow/`, `tests/arnold/kernel/`, `tests/arnold/execution/` exist** — M1/M2/M3 touchpoints are entirely aspirational.
2. **No canonical Megaplan-shaped manifest fixture** — required by M2 done-criteria #9 and M3 integration gate.
3. **No installed-wheel test scaffolding** — required by M4/M6 done criteria.
4. **125 conformance allowlist entries with no per-entry owner, expiry, or burn-down plan** — M6 requires zero.
5. **~250 root-level test files undifferentiated** — no M4/M5/M6 triage inventory exists.
6. **Legacy corpus (`tests/fixtures/corpus/`) is deprecated but not deleted** — keepalive risk.
7. **3 of 5 golden graph trace cases deferred** (T14 `toy_megaplan.py` fixtures not landed) — `_gen_golden_traces.py` blocked.
8. **No pipeline inventory/disposition artifacts committed** — M5 done criteria require closed migration/deletion/whitelist tables; none exist yet.
9. **No legacy surface inventory or bridge-caller inventory** — required by M4 done-criteria #8.
10. **CLI parser snapshot is a single hand-maintained JSON** (`tests/fixtures/cli_parser_snapshot.json`) — not regenerated from the CLI itself.
11. **Review wave artifacts exist** (`docs/arnold/workflow-manifest-runtime-review/`) but the 3-wave optimization described in `plan.md` is documented as complete in the synthesis — the briefs have been updated through all three waves.