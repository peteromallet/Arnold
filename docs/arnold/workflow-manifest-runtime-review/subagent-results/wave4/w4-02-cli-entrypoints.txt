Now I have a complete picture. Here's the inventory:

---

## CLI/Entrypoint Inventory — `workflow-manifest-runtime`

### 1. Console Entrypoints (installed-wheel)

| Surface | Role | Disposition | Plan Hook |
|---|---|---|---|
| `arnold` (via `python -m arnold`) | Thin dispatcher: `arnold/cli/__init__.py` → lazy-delegates to `arnold.pipelines.megaplan.cli.arnold` | **Survives** — canonical umbrella CLI | M5b audit; M6 must ensure no deleted import paths |
| `hermes` (`hermes_cli.main:main`) | Interactive agent CLI; in `agent/pyproject.toml` only | **External** — separate `hermes-agent` package, not in megaplan's wheel | Not in scope of this plan (separate sub-project) |
| `hermes-agent` (`run_agent:main`) | Agent entry; same sub-project | **External** | Not in scope |
| `hermes-acp` (`acp_adapter.entry:main`) | ACP server; same sub-project | **External** | Not in scope |

**Critical finding:** Root `pyproject.toml` has **no `[project.scripts]` section**. No `megaplan` or `arnold` console script is registered. `arnold/__main__.py` and `arnold/pipelines/megaplan/__main__.py` exist but only work via `python -m`. **M5/M6 must add `[project.scripts]` entries** for installed-wheel CLI behavior.

---

### 2. CLI Dispatch Surfaces

| Path | Role | Final Disposition | Plan Hook |
|---|---|---|---|
| `arnold/cli/__init__.py` | Lazy `cli_entry()`/`main()` → imports `arnold.pipelines.megaplan.cli.arnold.main` | **Survives** (thin dispatcher) | M5/M6 audit: must not lazy-import paths deleted by M6 |
| `arnold/pipelines/megaplan/cli/arnold.py` | Umbrella `arnold` CLI: `run`, `pipelines`, `pipeline`, `auto`, `override`, module dispatch | **Survives** as `arnold` CLI | M5: needs `arnold workflow` commands |
| `arnold/pipelines/megaplan/cli/parser.py` | `build_parser()` — megaplan subparser tree (~50 subcommands) | **Migrate** — still the megaplan parser surface | M4 migration; M5 parser snapshot test guards |
| `arnold/pipelines/megaplan/cli/__init__.py` | Legacy `megaplan` CLI: 2354-line `main()` dispatching to handlers | **Delete by M6** — product logic moves to `arnold_pipelines.megaplan` | M4: migrate; M6: delete path |
| `arnold/pipelines/megaplan/__main__.py` | `python -m arnold.pipelines.megaplan` | **Delete** (old megaplan surface) | M6 purge |
| `arnold/pipelines/megaplan/cli/__main__.py` | `python -m arnold.pipelines.megaplan.cli` | **Delete** | M6 purge |
| `arnold/pipelines/megaplan/cli/run.py` | `megaplan run <pipeline>` — registry-backed pipeline runner | **Migrate** into new layout | M4/M5 |
| `arnold/pipelines/megaplan/cli/feedback.py` | Feedback scaffold/edit/search | **Migrate** | M5 |
| `arnold/pipelines/megaplan/cli/setup.py` | `megaplan setup` — installs AGENTS.md, hooks, skills | **Migrate** | M5 > `arnold_pipelines` |
| `arnold/pipelines/megaplan/cli/skills.py` | Skill install/compose/regeneration | **Migrate** | M5 |
| `arnold/pipelines/megaplan/cli/status_view.py` | Status/progress/watch/audit handlers | **Migrate** | M5 |
| `arnold/pipelines/megaplan/cli/resolutions.py` | Quality-gate / user-action resolution handlers | **Migrate** | M4 |
| `arnold/pipelines/megaplan/cli/roots.py` | Project-root resolution helpers | **Migrate** | M4 |
| `arnold/pipelines/megaplan/cli/cursor_upgrade.py` | Resume cursor upgrade (graph→native) | **Migrate** | M4/M5 |
| `arnold/pipelines/megaplan/cloud/cli.py` | Lazy `cloud` subcommand | **Migrate or delete** | M5 inventory |
| `arnold/pipelines/megaplan/resident/cli.py` | Lazy `resident` subcommand | **Migrate or delete** | M5 inventory |
| `arnold/pipelines/megaplan/bakeoff/cli.py` | Lazy `bakeoff` subcommand | **Migrate or delete** | M5 inventory |

---

### 3. Parser Snapshot

| Item | Role | Disposition |
|---|---|---|
| `tests/characterization/test_cli_parser_snapshot.py` | Serializes `build_parser()` to JSON fixture; detects flag/subcommand drift | **Preserve** — guards M4/M5 refactors |
| `tests/fixtures/cli_parser_snapshot.json` | Committed golden parser shape | **Regenerate** after M5 split; keep as M6 gate |

---

### 4. Shell Scripts

| Path | Role | Disposition | Plan Hook |
|---|---|---|---|
| `sync-skills.sh` | Symlinks `_codex_skills/` into `~/.claude`, `~/.codex`, `~/.agents` | **Migrate or archive** | M5 script inventory |

---

### 5. Lazy Dispatchers & Bridge Surfaces (all DELETE by M6)

| Path | Role |
|---|---|
| `arnold/pipelines/megaplan/_bridge.py` | `run_pipeline_dispatch` — legacy bridge to old executor |
| `arnold/pipelines/megaplan/_compatibility.py` | Compatibility re-exports |
| `arnold/pipelines/megaplan/_forward_m2_m3.py` | Forward-port shims |
| `arnold/pipeline/native/` (entire package) | Old native decorator runtime (`NativeProgram`, `@pipeline`, `@phase`) |
| `arnold/pipeline/builder.py` | Legacy `PipelineBuilder`, `Stage`, `Edge`, `ParallelStage` |
| `arnold/pipeline/executor.py` | Old executor (hook lifecycle) |
| `arnold/runtime/` (public modules) | Legacy runtime surface |

---

### 6. Missing Plan Gaps

1. **No `[project.scripts]` in root `pyproject.toml`.** The `arnold` package has no console-script entrypoint. M5 must add `arnold = "arnold.cli:cli_entry"`; M6 must ensure old `megaplan` entrypoint either never exists or is proven deleted.

2. **`arnold_pipelines` namespace package does not exist yet.** No `arnold_pipelines/__init__.py` or `py.typed`. M4 must create it.

3. **No `arnold workflow` CLI commands exist.** M5 mandates `arnold workflow check`, `manifest`, `dot`, `dry-run`, `run`, `resume`. None implemented.

4. **`arnold.workflow`, `arnold.kernel`, `arnold.execution` packages do not exist.** M1-M3 create them; they're not scaffolded yet.

5. **No installed-wheel smoke tests** exist in the current tree for the `arnold` package. M1 done-criteria #10 calls for scaffolding; M6 requires them passing.

6. **`arnold/cli/__init__.py` lazy-imports `arnold.pipelines.megaplan.cli.arnold`** — this path is an M6 deletion target. The lazy dispatch must be re-pointed to `arnold_pipelines.megaplan.cli.arnold` before M6.

7. **The `hermes`/`hermes-agent` sub-project** has its own `pyproject.toml` with scripts but is excluded from wheel builds. Its CLI surface (`hermes_cli/main.py`, `gateway/run.py`, `acp_adapter/entry.py`) lives inside the megaplan tree but isn't part of this plan's migration scope — it's a separate deliverable.