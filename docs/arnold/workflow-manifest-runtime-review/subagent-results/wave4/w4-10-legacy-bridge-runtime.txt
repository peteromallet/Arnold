Now I have all the data needed. Here is the inventory:

---

## Workflow-Manifest-Runtime Plan Artifact Inventory

### Ⅰ. Plan Structure (`chain.yaml`)

**Path:** `.megaplan/briefs/workflow-manifest-runtime/chain.yaml`
**Baseline:** `origin/main` (not `native-python-pipelines`)
**Six milestones (M1–M6):**
| M# | Name | Role |
|----|------|------|
| M1 | Baseline manifest contract | Freeze `WorkflowManifest` v1, kernel contracts, guardrails |
| M2 | Explicit-node DSL & compiler | `arnold.workflow`, `arnold.patterns`, compile-to-manifest |
| M3 | Manifest runner & runtime | `arnold.execution`/`arnold.kernel` — fake backends, replay, resume |
| M4 | Megaplan product migration | Move to `arnold_pipelines.megaplan`, legacy/bridge inventories |
| M5 | Shipped pipelines, CLI, docs | Migrate examples, CLI commands, scaffolds, generated surfaces |
| M6 | Clean-break purge & conformance | Delete old `_pipeline`, bridge, native runner, compatibility shims |

**Review waves applied:** Wave 1 (10 end-state agents), Wave 2 (10 surface-edge agents), Wave 3 (3 Codex sequencing agents) — results synthesized back into briefs.

---

### Ⅱ. Inventory Class: Old Bridge/Native/Runtime Modules

| Module | Path | Role | Final Disposition (M6) |
|--------|------|------|------------------------|
| `_bridge.py` | `arnold/pipelines/megaplan/_bridge.py` | Routes `demo_judges` through neutral `arnold.pipeline.runner`; `_BridgeStep` wraps megaplan Steps; `run_pipeline_dispatch` routes bridge/native/legacy. Hard cap `_BRIDGED_PIPELINES = {'demo_judges'}`. | **Delete** — M6 clean-break purges bridge. Shadow-run scaffolding from M4 must be gone. |
| `_compatibility.py` | `arnold/pipelines/megaplan/_compatibility.py` | Leaf enum `CompatibilityMode(NATIVE/LEGACY)` — breaks import cycle between `step_contracts.py` and `model_seam.py`. | **Delete** — obsolete after migration. |
| `_forward_m2_m3.py` | `arnold/pipelines/megaplan/_forward_m2_m3.py` | Forward-declares `RoutingKey`, `Port`, `Graph` Protocol, `restore_and_diverge` sentinel, `_bridge_recommendation_to_routing_key`. All marked `# TODO(M2/M3)`. | **Delete** — M2/M3 implementations supersede forward declarations. |
| `_pipeline` (conceptual) | `arnold/pipelines/megaplan/` | The old Megaplan pipeline package (stages, routing, handlers, pipeline_types, pipeline.py). Currently the load-bearing product code. | **Migrate to `arnold_pipelines.megaplan`** — M4 relocates. Old path deleted in M6. |
| `native_runner.py` | `arnold/pipelines/megaplan/native_runner.py` | `NativeMegaplanRunner` — wraps Megaplan-specific hook/policy/schema-registry wiring around `arnold.pipeline.native.runtime.run_native_pipeline`. | **Delete** — superseded by `arnold.execution` manifest runner. |
| `native_hooks.py` | `arnold/pipelines/megaplan/native_hooks.py` | `MegaplanNativeRuntimeHooks` — Megaplan-specific hooks for native runtime walk-loop. | **Delete** — superseded by execution hooks in M3 runner. |
| `arnold.runtime` | `arnold/runtime/` | Neutral carrier types: `RuntimeEnvelope`, `ResumeCursor`, `StepwiseDriver`, `EventEnvelope`, `EventSink`, `NdjsonEventJournal`, `Effect`, `semantic_replay`, etc. 25 modules. | **Refactor reusable pieces into `arnold.kernel`/`arnold.execution`** (M3), mark remaining for deletion (M6). |
| `arnold.pipeline` (old) | `arnold/pipeline/` | Neutral dataclass/Protocol types, `PipelineBuilder`, `Stage`, `Edge`, `ParallelStage`, `run_pipeline`, `executor.py`, `native/` subpackage (compiler, runtime, hooks), `state.py`, `types.py`, `builder.py`, etc. 45+ modules. | **Superseded by `arnold.workflow`/`arnold.patterns`/`arnold.execution`** — M6 deletes `PipelineBuilder`, `Stage`, `Edge`, `ParallelStage`, `run_pipeline`. Native pipeline deleted or privatized. |
| `arnold.agent` shims | `arnold/agent/` | 22 re-export shims bridging to `arnold.pipelines.megaplan.agent.*` (e.g., `anthropic_adapter.py`, `redact.py`, `prompt_builder.py`, etc.). Audited in `docs/arnold/m7-agent-shim-audit.md`. | **Delete after consolidation** — separate Agent Runtime Extraction epic. Not strictly in this plan's scope but inventoried. |

---

### Ⅲ. Re-export Shims & Compatibility Maps

- **`arnold/pipeline/__init__.py`**: Re-exports ~160 symbols from `arnold.pipeline.*` plus cross-package re-exports from `arnold.runtime.envelope`, `arnold.runtime.resume`, `arnold.runtime.driver`. This is the PUBLIC neutral surface. **Refactored** (M1–M3), old exports deleted (M6).
- **`arnold/pipelines/megaplan/__init__.py`**: Lazy `__getattr__` exports `build_pipeline`, `compile_planning_pipeline`, plus `_SYMBOL_EXPORTS` dict mapping 40+ handler/routing symbols. **Migrated** to `arnold_pipelines/megaplan/` (M4).
- **`arnold/pipelines/megaplan/_core/__init__.py`**: Massive re-export hub (~100 symbols) from `io`, `canonical`, `state`, `registries`, `workflow`, `phase_runtime`, `dispatch`, `fanout`, `scheduler`. **Migrated** into `arnold_pipelines.megaplan` (M4).
- **Legacy shim tests**: `tests/test_legacy_phase_cli_compat.py`, `tests/test_legacy_plan_migration.py`, `tests/test_pipeline_legacy_profile_compat.py`, `tests/cloud/test_phase_command_shim.py`, `tests/arnold/pipelines/megaplan/test_legacy_pipeline_baseline.py`. **Convert or delete** (M5/M6).
- **`arnold/pipelines/megaplan/store/legacy_migration.py`**: Historical state migration tooling. **Consumed** during M4 state migration, then deletable.

---

### Ⅳ. Plan Hooks (Integration Gates Between Milestones)

| Gate | From → To | What Must Be True |
|------|-----------|-------------------|
| Manifest contract frozen | M1 → M2 | `WorkflowManifest` v1 committed; amendment protocol exists |
| Canonical Megaplan fixture | M2 → M3 | Named fixture at `tests/fixtures/workflow/canonical_megaplan_manifest.*` compiles to manifest |
| M3-to-M4 integration | M3 → M4 | Canonical fixture fake-runs through all routing families, suspension, retry, 3+ resume cursors, mismatch quarantine |
| Legacy/bridge inventory | M4 → M5 | `legacy-surface-inventory.md` and `state-authority-migration.md` committed with no undecided rows |
| M5 closed inventories | M5 → M6 | Pipeline migration/deletion/whitelist, script/tool, generated-artifact, legacy-test inventories all closed |
| M6 deletion subset | M6 final | Every M6 deletion traced to M5 outcomes + M4 bridge/state inventories |

---

### Ⅴ. Gaps Identified

1. **`_pipeline` doesn't exist as a discrete module** — the plan refers to it as inventory class but it's the conceptual "old Megaplan pipeline package," not a single file. M4 inventory must clarify whether it means `arnold/pipelines/megaplan/` (the current path) or something narrower.

2. **No committed `legacy-surface-inventory.md` yet** — required by M4 Scope (IN bullet 6). Must be generated during M4 implementation.

3. **`arnold/runtime/` has no explicit re-charter map** — M3 requires disposition of each `arnold.runtime` module (preserved in kernel/execution vs. deleted). The `__init__.py` docstring lists modules but doesn't classify them.

4. **`arnold.agent` shims are audited but deferred** — `m7-agent-shim-audit.md` classifies 22 shim files as "delete after consolidation" but ties them to a different epic (Agent Runtime Extraction). Risk of orphaned shims surviving M6.

5. **No `arnold_pipelines` namespace package yet** — M4 must create it. The repo currently only has `arnold/pipelines/megaplan/`. Wheel build configuration deferred to M4.

6. **`arnold/pipeline/__init__.py` is a massive chokepoint** — re-exports ~160 symbols including types that M6 will delete (`PipelineBuilder`, `Stage`, `Edge`, `ParallelStage`, `run_pipeline`). No deprecation path defined.