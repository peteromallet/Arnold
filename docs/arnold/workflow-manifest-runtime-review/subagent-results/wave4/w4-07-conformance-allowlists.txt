Here is the inventory.

---

## Workflow-Manifest-Runtime — Conformance & Boundary Inventory

### 1. Plan Artifacts (chain + 6 milestones + review synthesis)

| Item | Role | Disposition | Plan Hook |
|---|---|---|---|
| `chain.yaml` | 6-milestone dependency chain: M1→M2→M3→M4→M5→M6 | Active; `on_failure: stop_chain`, `merge_policy: review` | `driver.robustness: thorough`, max 120 iter |
| `m1-baseline-manifest-contract.md` | Freeze `WorkflowManifest` v1, kernel contracts, golden fixtures, quarry inventory, dispatch ownership table, amendment protocol | Active; base is `origin/main`, `native-python-pipelines` is quarry only | IN: manifest dataclasses, kernel vocab, conformance gates. OUT: no DSL, no runner, no deletion |
| `m2-explicit-node-dsl-compiler.md` | `arnold.workflow` DSL + `arnold.patterns` constructors → compile to manifest | Active; depends on M1 | IN: `workflow.Pipeline(...)`, reject closures/lambdas, dry-run/inspect, canonical Megaplan-shaped fixture. OUT: no runner |
| `m3-manifest-runner-runtime.md` | `arnold.execution` runtime executing manifests via append-only events, content-addressed artifacts, resume cursors | Active; depends on M2 | IN: fake-backend execution, generic suspension, capability/budget checks, replay. M2 fixture is integration gate |
| `m4-megaplan-product-migration.md` | Rewrite Megaplan product to `arnold_pipelines.megaplan`, consume manifest runtime | Active; depends on M3 | IN: `build_pipeline()`, golden/parity, shadow-run drift reports. OUT: no permanent shims |
| `m5-shipped-pipelines-cli-docs.md` | Migrate remaining shipped pipelines, CLI, docs, scaffolds, inventory scanners | Active; depends on M4 | IN: pipeline inventory scanners, closed migration/deletion/whitelist inventory, generated-artifact manifest |
| `m6-clean-break-purge-conformance.md` | Delete obsolete public surfaces, enforce installed-wheel/import-graph/golden conformance gates | Active; depends on M5 | IN: `import megaplan` must fail, `Stage`/`Edge`/`run_pipeline` must fail, allowlist burn-down to zero |
| `review-synthesis.md` | 3-wave review corrections applied to briefs | Applied; Wave 1-3 repo stored at `docs/arnold/workflow-manifest-runtime-review/` | Key accepted: generic suspension ≠ human gates, kernel vocabulary in neutral packages, M2 fixture → M3 gate, M6 blocks on unresolved drift reports |

### 2. Conformance Checks

| Item | Location | Disposition | Gaps |
|---|---|---|---|
| `arnold/conformance/__init__.py` | Neutral result types (`ConformanceCheckResult`, `ConformanceSuiteResult`) + thin assertion helpers | Active; zero Megaplan imports enforced | — |
| `arnold/conformance/checks.py` | Adapter protocol, ContractResult schema round-trip, generic Arnold↔Megaplan coupling ratchet (AST import scan, package-name staleness, semantic coupling, public-workflow layering, never-port-artifacts) | Active; `ACTIVE_MEGAPLAN_PACKAGE_NAMES = ("megaplan", "arnold.pipelines.megaplan", "arnold_pipelines.megaplan")` | `_megaplan_coupling_allowlist.txt` still has 4 entries (CLI + 3 adapters) |
| `arnold/conformance/suite.py` | Aggregates 5 domains: adapter protocol, contract schema, routing vocabulary, join delegation, anti-coupling | Active; calls `run_conformance_suite()` | Allowlist read from `_allowlist.txt` — see below |
| `arnold/conformance/routing.py` | Vocabulary coverage, edge-consistency, resolve-edge behavior | Active | — |
| `arnold/conformance/join.py` | Stage-join delegation conformance | Active | — |

### 3. Allowlists (ratcheted, stale entries fail)

| File | Entries | Risk |
|---|---|---|
| `arnold/conformance/_megaplan_coupling_allowlist.txt` | 4 modules: `arnold.cli`, `arnold.agent.adapters._oneshot`, `arnold.agent.adapters.codex`, `arnold.agent.adapters.shannon` | Low; documented as transitional. Burn-down blocked until M6 CLI retirement + adapter relocation |
| `arnold/conformance/_allowlist.txt` | ~40 entries across 5 checks (import-coupling, package-name-staleness, semantic-coupling, public-workflow-layering, never-port-artifacts) | **Elevated.** 30+ `package-name-staleness` entries for `arnold.agent.*` shim files that delegate to Megaplan via `_importlib.import_module()`. 25+ `semantic-coupling` entries for agent tools carrying `.megaplan`/handler-name vocabulary. M6 must burn these to zero |

### 4. Coupling Gates (test-enforced)

| Test File | What It Guards | Status |
|---|---|---|
| `tests/arnold/conformance/test_megaplan_coupling_gate.py` | Ratchet: current tree has exactly 4 coupled + 4 allowlisted modules; new generic Megaplan imports fail; stale allowlist entries fail; C1 generic pipeline modules (`step_io_handoff`, `executor`, `artifact_io`, `step_io_telemetry`) must not import Megaplan | **Passing.** Gate: 4 allowlisted = 4 coupled, zero unexpected, zero stale |
| `tests/arnold/conformance/test_conformance_gates.py` | Import-coupling, package-name-staleness, semantic-coupling, public-workflow-layering, never-port-artifacts all exercise ratchet with tmp_path fixtures | Passing |
| `tests/boundary/test_import_leak.py` | `arnold.agent` static surface must have zero `from arnold.pipelines.megaplan` imports; dynamic import must load zero Megaplan modules | **Static: PASS. Dynamic: XFAIL** (33 shim files use `_importlib.import_module("arnold.pipelines.megaplan.*")` at load time; blocked on T5-T11 tool-layer migration) |
| `tests/arnold/test_boundary_skeleton.py` | `arnold/pipeline/`, `arnold/runtime/`, `arnold/control/`, `arnold/supervisor/` must have zero Megaplan imports, zero forbidden string literals (`planning`, `tiebreaker`, `escalate`, `proceed`, `iterate`), zero raw-source tokens (`.megaplan`, `MEGAPLAN_`, `GateRecommendation`); evidence-pack isolation; per-module checks for `validator.py`, `pattern_dynamic.py`, `subpipeline.py` | Passing |
| `tests/test_contract_non_wiring.py` | Worker/stage files must not import M0b validator/registry modules; handlers must not call legacy `validate_payload()`; provider workers must route prompt assembly through `model_seam.render_prompt_for_dispatch` | Passing |

### 5. Import Scanners / Package-Boundary Tests

| Item | What It Scans | Status |
|---|---|---|
| `tests/characterization/test_import_surface.py` | Every `__all__` symbol in store, workers, execute, agent_runtime resolves; de-facto surface for cli, chain, evaluation, execute_core, cloud modules verified; SSH contract (4 symbols) type-checked; status-payload mandatory keys pinned | Passing |
| `tests/characterization/test_patterns_import_surface.py` | `arnold.pipelines.megaplan.patterns.__all__` = only `("dynamic_fanout",)`, deleted `_NODE_REGISTRY` must not reappear | Passing |
| `tests/test_agent_tool_compat_imports.py` | Agent tool compatibility imports | Active |
| `tests/m8/test_conformance_guard.py` | Prove `arnold.conformance` is importable from M8; all 7 check functions callable; default suite all-passes | Passing |
| `scripts/generate_arnold_docs.py --check` (CI) | Generated docs freshness | CI gate |
| `docs/arnold/package-disposition.yaml` | Disposition of every `megaplan/_pipeline` subtree: split-required, blockers documented (stage strings, Megaplan-branded schema IDs, critique/finalize/tiebreaker control flow hardcoded) | Active; generated into `package-disposition.md` |

### 6. Policy Files That Could Hide Old Surfaces

| Item | Risk |
|---|---|
| `arnold/pipelines/megaplan/_BRIDGE_CALLERS.md` | Inventory of ~20 callers still on legacy `run_pipeline`/`run_pipeline_with_policy` path (subloop, demo_judges, registry, human-gate resume, ~11 test modules). Deletion gate: all must be repointed before `executor.py` can be deleted. |
| `arnold/pipelines/megaplan/_bridge.py` | Compatibility bridge. M6 deletion target — if it survives, old execution path lives. |
| `arnold/runtime/` | Legacy runtime surface. M3 must inventory what's preserved under `arnold.kernel`/`arnold.execution`; M6 must delete or re-charter remainder. |
| `arnold/pipeline/__init__.py` | Currently exports `Stage`, `Edge`, `ParallelStage`, `PipelineBuilder`, `run_pipeline` — all M6 deletion targets per clean-break policy. |
| 33 agent shim files under `arnold/agent/` | Use `_importlib.import_module("arnold.pipelines.megaplan.*")` at load time — hidden Megaplan coupling not caught by static AST scans. Dynamic import-leak test is xfail. |

### Summary Gaps

1. **33 agent shims** — dynamic import-module coupling invisible to AST scanners; T5-T11 migration incomplete.
2. **~20 legacy `run_pipeline` callers** — bridged, not yet repointed; deletion blocked.
3. **Allowlist burn-down** — 40+ entries, mostly agent-tool semantic coupling; M6 target is zero but path unclear for agent shims.
4. **`arnold.runtime` disposition** — not yet inventoried per M3 spec; could carry hidden Megaplan coupling.
5. **No installed-wheel conformance scaffolding committed yet** — M1 requires it, M6 gates on it.
6. **`arnold.workflow`, `arnold.patterns`, `arnold.execution`, `arnold.kernel`** — not yet created. All conformance infrastructure exists but targets packages that don't exist yet.