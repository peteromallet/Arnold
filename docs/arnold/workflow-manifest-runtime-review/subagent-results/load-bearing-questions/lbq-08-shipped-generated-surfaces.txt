## Agreement: Yes, Substantially — With Two Minor Gaps

The written answer has three claims. The plan covers all three but with slightly less precision on whitelists. Here's the breakdown:

---

**1. "Public shipped pipelines migrate to `workflow.Pipeline` under final `arnold_pipelines` locations or are deleted."** — **Agree.** M5 explicitly lists every shipped pipeline root, declares `arnold_pipelines/<pipeline_name>/` as the default final location, and requires every surviving pipeline to expose `build_pipeline()` returning `workflow.Pipeline` (Done Criterion #1). M5 also requires explicit `migrate`/`delete`/`whitelist` classification for every entry. M6 then enforces deletion of old `arnold/pipelines/...` paths.

**2. "Whitelist is only for zero-public-surface internal coverage with owner, expiry, independent review, and no old-native teaching surface."** — **Agree with two missing nuances.**

- M5's Locked Decisions define whitelist criteria: no CLI, no import by shipped pipelines, no docs, no generated skill, no registry, no external `build_pipeline()`. Owner and expiry are required. **But "independent review" and "no old-native teaching surface" are not explicit M5 whitelist criteria.**
- "Independent review" appears only at M6 for conformance allowlists. "No old-native teaching surface" is implicit in zero-public-surface (no docs, no generated skills), but a whitelisted pipeline could technically pass public-surface checks yet still contain copy-pasteable native decorators or builder patterns that a developer would read.

**3. "Generated skills/docs/scaffolds are teaching surfaces and must be regenerated, provenance-stamped, semantically scanned, and fake-run."** — **Agree.** M5 requires regeneration of `_codex_skills`, composed rules, projection docs, scaffolds, and package metadata (IN scope, Done Criteria #5, #8). M1 defines the `GeneratedArtifactProvenance` contract (generator module, source hash, timestamp, input surfaces). M5 requires embedded examples to compile, dry-run, and fake-run; M6 adds provenance-freshness and regeneration-diff gates. Every code fence is checked.

---

## Plan Edits Needed

**In `.megaplan/briefs/workflow-manifest-runtime/m5-shipped-pipelines-cli-docs.md`, under "Locked Decisions,"** update the whitelist bullet:

> A pipeline may only be whitelisted instead of migrated if it is demonstrably non-public: no CLI entrypoint, no import by any shipped pipeline, no docs reference, no generated skill, no package registry row, and no external-facing `build_pipeline()`. Whitelists require owner, expiry before M6, **independent review**, a migrate/delete plan, **and must not contain or teach old-native patterns (no native decorators, PipelineBuilder/Stage/Edge usage, old-native import paths, or copy-pasteable legacy examples).**

---

## Confidence: **High (90%)**

The plan is substantially correct. The two gaps are modest tightening — "independent review" is already an M6 requirement for allowlists, so extending it upstream to M5 whitelists is natural. "No old-native teaching surface" is strongly implied by zero-public-surface but adding the explicit constraint closes the gap completely. No milestone reordering or new scope is needed.