## Agreement/Disagreement

**Agreement: the answer is correct and well-reflected.** The core mechanism — stable human alias (`Pipeline.id`) + compiler-computed `manifest_hash`, with every derived artifact (registries, discovery, trust, tenant, generated artifacts, resume cursors) keyed from `id + manifest_hash` — appears across all six milestones with high fidelity:

| Consumer | Where in plan |
|---|---|
| Registries | M5 criterion #10, M6 #14/#29 |
| Discovery manifests | M1 (identity derivation), M4, M6 #14 |
| Trust/tenant derivation | M1, M4, M5, M6 #14 |
| Generated artifacts | M1 (provenance contract), M5, M6 #13 |
| Resume cursors | M3 (manifest-coordinate cursors), M6 #29 |
| Final M6 recompile proof | M6 #14/#29, `chain.yaml` `post_merge_conformance` |

The `post_merge_conformance` block in `chain.yaml` directly echoes the answer: "prove canonical manifest hashes and identity registries match the final compiler output."

---

## Missing Nuance

1. **`Pipeline.version` is silent.** M2's API surface uses `Pipeline(id=..., version=...)`, but no milestone defines whether `version` feeds into `manifest_hash`, is independent metadata, or participates in identity derivation. The answer should clarify this.

2. **"CLI fixtures" are mentioned in the answer but not explicitly wired into the `id + manifest_hash` chain in the plan.** M5 regenerates CLI parser snapshots from installed wheels (criterion #12), and M6 audits CLI dispatch tables, but the plan never states "CLI fixtures derive from `id + manifest_hash`" with the same explicitness given to registries and resume cursors.

3. **"Normalized manifest" is undefined in the answer itself.** M1 defines a "locked golden normalization spec" covering volatile fields, ordering rules, seed handling, and amendment protocol — but the answer doesn't explain that normalization is the prerequisite for stable `manifest_hash` computation. A one-sentence bridge would help.

---

## Exact Plan Edits Needed

- **M1** (`docs/arnold/workflow-manifest-amendments.md` or identity derivation section): Add a sentence stating whether `Pipeline.version` is identity-significant or purely metadata. Example: *"`Pipeline.version` is authoring metadata only; `manifest_hash` is the sole runtime identity discriminant."*

- **M5** criterion #12 or touchpoints: Add "CLI fixtures" to the list of consumers that must trace to `id + manifest_hash`, consistent with the answer's scope. Currently the criterion covers parser snapshots from installed wheels but doesn't explicitly back-link to identity derivation.

- **`chain.yaml`** `post_merge_conformance`: Expand to mention CLI fixtures alongside identity registries: *"...prove canonical manifest hashes, identity registries, and CLI fixtures match the final compiler output."*

- **Load-bearing question answer itself** (optional polish): Append *"`manifest_hash` is computed over the normalized manifest per the locked golden normalization spec (M1); `Pipeline.version` is authoring metadata, not an identity discriminant."*

---

**Confidence: 85%.** The answer is substantively correct and the plan covers it. The gaps are precision/clarity issues, not architectural holes — `version` semantics and CLI fixture derivation are the only two threads that need explicit cross-referencing.