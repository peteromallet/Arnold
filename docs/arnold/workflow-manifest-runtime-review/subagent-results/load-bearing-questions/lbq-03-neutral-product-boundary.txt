## Analysis: Question 3 — Neutral/Product Boundary

**Verdict: Agree, with minor omissions in the written answer, not the plan.**

### Agreement

The plan reflects the written answer faithfully and in detail:

| Claim in Answer | Where the Plan Enforces It |
|---|---|
| `arnold.kernel`, `arnold.workflow`, `arnold.patterns`, `arnold.execution` define neutral contracts | M1–M3 consistently. M1 defines kernel contracts; M2 builds workflow/patterns as compile-only neutral surfaces; M3 makes execution neutral. |
| Product packages register through protocol registries | M3: "startup-registered protocol handlers"; M4: Megaplan registers content types, control meanings, capability handlers; M5: shipped pipelines register via pipeline ID registries. |
| Neutral runtime never imports product modules directly | Every milestone: M1 Done Criteria #5 (import-boundary conformance); M3 Done Criteria #5 (execution imports no product modules); M6 gates against dynamic `importlib`, `__getattr__`, and `TYPE_CHECKING` leaks. |
| No resolving manifest strings at runtime | M3: "manifest refs are identifiers, not runtime import instructions"; "must not call `importlib.import_module` on any manifest field." M6 reinforces with full import-tracing conformance. |

### Missing Nuance (Written Answer, Not Plan)

1. **`arnold.agent` omission.** M1 treats `arnold.agent`, `arnold.control`, and `arnold.conformance` as neutral surfaces if they survive, subject to the same import-boundary rules. M4 allows product to depend on `arnold.agent`. The answer should list `arnold.agent` alongside the four neutral packages — or explain it's implicitly included under `arnold.kernel`/`arnold.execution`.

2. **`TYPE_CHECKING` exceptions.** M1 provides a narrow escape hatch: "If `TYPE_CHECKING` imports across the neutral/product boundary are permitted, the exception must be explicit and scanner-enforced." The written answer's "never imports" is slightly too absolute; the plan allows a gated, auditable exception.

3. **Product-side beyond Megaplan.** The answer says "product packages such as `arnold_pipelines.megaplan`." M5 adds `arnold_pipelines/<pipeline_name>/` for standalone shipped pipelines. The "such as" covers this, but the plan entails multiple product packages, not one.

### Exact Plan Edits Needed

**None.** The plan fully operationalizes the boundary. The written answer, not the plan, is slightly incomplete. If polish is desired, add to Q3 in `load-bearing-questions.md`:

> `arnold.agent` is a neutral surface under the same import-boundary rules (M1). TYPE_CHECKING imports across the boundary require explicit scanner enforcement. The product side includes `arnold_pipelines.megaplan` and standalone `arnold_pipelines/<name>/` packages (M5).

### Confidence: High

The boundary is one of the most thoroughly reinforced concepts across all six milestones. Every milestone's constraints, done criteria, and locked decisions reiterate it at the appropriate granularity. There is no plan gap — only the written answer's brevity leaves out `arnold.agent` and the `TYPE_CHECKING` exception nuance.