## Analysis: Question 5 — Behavior Parity Proof

### Agreement

The written answer's eight proof mechanisms are all **present** in the plan, and nothing in the plan **contradicts** them. I agree the answer is substantially correct.

### Mapping Answer → Plan

| Answer Mechanism | Plan Coverage |
|---|---|
| Locked golden normalization | M1 IN (normalization spec, no-regression rule), M1 Anti-Scope, M6 constraints |
| Characterization traces | M4 IN: shadow/dual-run seam writes drift reports; M1 goldens characterize baseline |
| Canonical manifest fixture matrix | M2 IN explicitly defines it; M3 DC #10–11 gate M4 on running it |
| Semantic manifest diffs | M2 DC #11 requires semantic-diff check against locked expected shapes |
| Fake-run/replay/resume gates | M3 DC #1, #2, #10—core of M3 with explicit fixture-matrix gate |
| Live-backed smoke | M4 DC #7: fresh plan, resume, 3 gate iterations, tiebreaker vs real backend |
| Installed-wheel CLI/operator parity | M4 DC #8, M5 CLI commands, M6 installed-wheel conformance suite |
| Refusal to update goldens | M1 IN (explicit rule), M1 Anti-Scope, M6 constraints |

### Missing Nuance (3 gaps)

1. **"Characterization traces" is never named as such.** The plan has golden fixtures (M1) and a shadow/dual-run seam (M4 IN) that produces drift reports, but never calls this "characterization traces." If the term means automated old-vs-new trace comparison at the event/journal level, that specific technique is **not** prescribed — the shadow run is closer to side-by-side output comparison. The plan should either adopt the term explicitly or clarify whether the shadow seam covers characterization-trace semantics.

2. **Semantic manifest diffs appear only in M2, not downstream.** M2 DC #11 requires them, but M4 and M6 never mention semantic diffs as part of their parity evidence gates. A reviewer closing M4 or M6 would not know to check this.

3. **The answer omits the shadow/dual-run seam**, which is a distinct parity mechanism in the plan (M4 IN) not captured by the eight items listed.

### Exact Plan Edits Needed

- **M4 IN scope**: Add after the shadow-run sentence: *"The shadow seam produces characterization traces capturing event sequences, artifact hashes, and control transitions from both legacy and manifest-backed runs for structured diff."*
- **M4 DC #3**: Append: *"…and semantic manifest diffs pass against locked M2 expected shapes."*
- **M6 DC #15**: Append: *"…semantic manifest diffs, and characterization-trace comparison."*
- **load-bearing-questions.md #5**: Add *"shadow/dual-run characterization traces"* to the list after "characterization traces" (or replace "characterization traces" with the more specific plan term).

### Confidence: **85%**

The plan substantively delivers the answer's intent — all mechanisms are wired into at least one milestone. The gaps are terminology consistency and missing cross-references, not missing proof structure. The plan would still prove parity without these edits, but the edits make the proof strategy legible end-to-end.