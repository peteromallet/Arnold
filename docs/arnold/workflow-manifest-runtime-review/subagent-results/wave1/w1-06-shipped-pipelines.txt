## Verdict

**Position: Agree with the expectation, but the plan needs sharpening.** The M5/M6 division correctly routes shipped pipelines onto `arnold.workflow` / `arnold.patterns` and deletes legacy surfaces. However, M5's "Open Questions" (which legacy pipelines to delete vs. migrate, which CLI names survive, which whitelist entries are legitimate) are load-bearing decisions left unresolved. The plan defers answers into the sprint, risking M6 discovering unmigrated stragglers at deletion time.

---

## IN SCOPE — Must Be Named

**Shipped pipelines (from M5):**
`creative`, `doc`, `jokes`, `select_tournament`, `epic_blitz`, `live_supervisor`, `writing_panel_strict`, and retained `evidence_pack`, `folder_audit`, `deliberation`, `_template`.

**CLI surface:**
`arnold workflow check`, `manifest`, `dot`, `dry-run`, `run`, `resume`. Each needs parser/snapshot tests.

**Docs/scaffolds:**
`docs/arnold/workflow-authoring.md`, `docs/arnold/workflow-manifest.md`, `docs/arnold/workflow-runtime.md`, `docs/arnold/examples/`. Must teach only explicit-node `workflow.Pipeline(..., steps=[...])`.

**Inventory scanners:**
`scripts/check_workflow_pipeline_inventory.py` — must flag any shipped pipeline that doesn't expose `build_pipeline() → workflow.Pipeline`.

---

## OUT OF SCOPE

- **Restricted-Python generator DSL** — explicitly context-only, not canonical, may lower later. No public docs, no shipped pipelines, no CLI.
- **YAML-as-source authoring** — anti-scoped. YAML is generated output, not authoring.
- **Dead/unmaintained example pipelines** — M5 should decide deletion candidates before sprint execution, not during.
- **Internal-only test coverage** using legacy `PipelineBuilder`/`Stage`/`Edge` — requires explicit whitelist with per-entry rationale, not blanket allowance.

---

## Corrections by Milestone

**M5:** Resolve the open questions *before* sprint execution. Produce a concrete migration checklist: pipeline → action (migrate/delete/whitelist). Define CLI command mapping (old → new → migration-error). The "Open Questions" block shouldn't survive into execution — these are plan defects.

**M6:** Add a pre-deletion inventory gate derived from M5 outcomes. The deletion list must be a closed set, not discovered ad hoc. If M5 didn't decide to migrate `writing_panel_strict`, M6 cannot delete its legacy surface.

**Chain-level:** Add a cross-milestone proof that M6's deletion list ⊆ M5's migration outcomes. Currently no traceability between the two.

---

## Proof Gates

| Gate | Milestone | What proves it |
|------|-----------|----------------|
| Pipeline inventory scan | M5 exit | Every shipped pipeline is `workflow.Pipeline` or whitelisted |
| CLI snapshot | M5 exit | All `arnold workflow *` commands deterministic |
| Docs/scaffold scan | M5 exit | Zero public `PipelineBuilder`, `Stage`, `Edge` references |
| M5→M6 traceability | M6 entry | Deletion list is a closed subset of M5 outcomes |
| Installed-wheel import gate | M6 exit | `import megaplan` fails; `from arnold.pipeline import Stage` fails |
| Golden behavior parity | M6 exit | All migrated pipelines pass parity against pre-migration goldens |