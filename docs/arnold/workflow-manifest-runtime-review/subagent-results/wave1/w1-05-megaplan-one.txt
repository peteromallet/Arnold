## Verdict: Right Target, Incomplete Scope

End-state expectation 5 is directionally correct — Megaplan must become a product pipeline under `arnold_pipelines.megaplan`, built as explicit workflow data over the manifest runner. The listed stage behaviors (prep→plan→critique→gate→revise→tiebreaker→human→finalize→execute→post-review) capture the visible pipeline. But expectation 5 is not yet the right target for M4 as written — it omits load-bearing behaviors evident in the characterization corpus.

---

### Missing Behaviors

**Override/fallback routing.** Force-proceed, abort, escalate, and fallback edges are separate primitives from model gate recommendations. The 34-scenario auto_drive corpus proves these are real: `escalate_force_proceed`, `escalate_abort`, `force_proceed_failed`, `no_next_step_no_override`, `auto_escalate_up`.

**Dynamic topology.** Robustness levels change graph shape — reviewer counts, loop caps, optional prep/feedback paths. `iteration_cap`, `blocked_retry_quality_to_cap`, and `context_retry_exhausted` prove topology varies per config.

**Auto-supervisor behaviors.** `cost_cap_exceeded`, `stall_generic`, `idle_timeout`, `all_tasks_blocked`, `orphan_recovery`, `plan_locked_skip_continue`, `status_lookup_failed`, `phase_callback_failed`, `execute_callback_failure_recovery` — these are product-owned supervisor transitions, not generic runtime semantics. M4 must cover them explicitly.

**Prompt-as-code and subpipeline promotion.** Dynamic prompt builders and artifact subdirectory promotion between parent/child scopes are current primitives the M4 brief omits.

### Wrongly Included

Status/trace/resume belong primarily to M3 runtime, not M4 product migration. M4 must ensure they *work through manifest events*, but parity proof for those surfaces should gate at M3, not duplicate at M4.

---

### Corrections by Milestone

| Milestone | Correction |
|-----------|-----------|
| **M1** | Expand golden corpus to all 34 auto_drive + 5 supervisor replay scenarios *before* any package move. Freeze behavior against these. |
| **M3** | Ensure generic runtime supports override edges, fallback edges, dynamic topology overlay, subpipeline promotion contracts, and supervisor event routing — as policy-blind primitives. |
| **M4** | Add to explicit scope: feedback-as-phase, dynamic prompt builders via `module:qualname` callables, robustness-driven topology overlays, auto-supervisor transition behaviors, and execute-callback-failure recovery. |

### Proof Gates

1. **Golden parity gate**: All 34 auto_drive + 5 supervisor replay golden scenarios pass under manifest runner (M4 done criterion 3 strengthened).
2. **Topology hash stability**: Manifest hash matches between current graph and new explicit-node build for identical config (M4 done criterion 2).
3. **Import boundary gate**: `arnold.*` imports zero from `arnold_pipelines.megaplan` (M1 conformance, M4 re-verified).
4. **Override routing gate**: Force-proceed/abort/escalate preserve exact current routing behavior in parity tests.
5. **Dynamic topology gate**: Build pipeline with three robustness levels; verify graph shape, loop caps, and reviewer counts differ per spec.