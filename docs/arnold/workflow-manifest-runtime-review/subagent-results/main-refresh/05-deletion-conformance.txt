## Verdict: Needs Edits

M6 is exceptionally thorough (31 done criteria), but four new-mainline vectors from `9d8b2a4a..0035c231` are not explicitly caught. Below are the required amendments.

---

### 1. Allowlist glob-pattern burn-down

**File**: `.megaplan/briefs/workflow-manifest-runtime/m6-clean-break-purge-conformance.md`

**Section**: Done Criteria, after #10

**Add**:

> 10a. Every allowlist glob or broad-spectrum pattern (including `/**` and `fnmatch` wildcards) is expanded to its concrete file set against the final tree. Each matched file receives an individual migrate/delete/re-charter disposition. No glob entry survives M6 unless every concrete match is independently classified with owner and non-legacy rationale.

*Why*: New mainline `9adb4323` added `fnmatch`/`/**` support and the entry `never-port-artifacts .megaplan/plans/**`. A single glob could harbor unbounded undeleted artifacts.

---

### 2. `sys.modules` pre-loaded module inspection

**File**: `.megaplan/briefs/workflow-manifest-runtime/m6-clean-break-purge-conformance.md`

**Section**: Done Criteria, after #24

**Add**:

> 24a. Installed-wheel conformance enumerates all `sys.modules` keys after the full suite runs. Any key resolving to a deleted package, subpackage, or dotted path fails M6, regardless of whether a dynamic import was intercepted during tracing.

*Why*: New `_load_tool_registry()` in `_sandbox_wrappers.py` checks `sys.modules.get("tools.registry")` before any import — a pre-cached deleted path would bypass import tracing entirely.

---

### 3. Parser `--help` and CLI dispatch-chain scanning

**File**: `.megaplan/briefs/workflow-manifest-runtime/m6-clean-break-purge-conformance.md`

**Section**: Done Criteria, after #22

**Add**:

> 22a. Every surviving CLI command's `--help` text, shell-completion output, and nested subcommand dispatch chain is captured from the installed wheel and scanned. No deleted module path, package name, command name, or old public API surface appears in any help text, usage line, or dispatch target.

*Why*: New mainline added `arnold pipelines describe` and expanded CLI surfaces. Argparse-generated help text is a runtime artifact, not a static AST node — it can carry deleted surface references that AST scans miss.

---

### 4. Runtime filesystem-read paths beyond `.megaplan` state

**File**: `.megaplan/briefs/workflow-manifest-runtime/m6-clean-break-purge-conformance.md`

**Section**: Done Criteria, #28 — amend the existing bullet

**Replace** the first sentence with:

> 28. The historical-state filesystem-read trace proves no surviving runtime/operator path reads old `.megaplan` authority, old-format `state.json`, **or legacy-format skill/docs/discovery/template files** outside sunset-expired migration/projection modules; old locks and untranslated old resume cursors are absent or quarantined.

*Why*: New `_skill_md_sibling` in `manifest.py` tries legacy-flat `SKILL.md` paths as fallback. Any runtime path that resolves and serves old-format files outside `.megaplan/` should be caught.

---

These four additions close the gap between M6's stated coverage and the new mainline's specific legacy-survival vectors. No structural reorganization is needed.