## Verdict: **needs edits**

The M5/M6 briefs have broad CLI-inventory and conformance language that *incidentally* covers most of the diff, but the plan does **not** explicitly force `arnold pipelines describe` into the CLI/operator inventory or its downstream gates. The chain-notes themselves flag this gap. Additionally, two smaller parser-surface changes lack explicit disposition anchors.

---

### Required Edits

**File:** `.megaplan/briefs/workflow-manifest-runtime/m5-shipped-pipelines-cli-docs.md`

**1. Scope → IN bullets (after "Implement or update workflow CLI commands…"):**

> - Classify `arnold pipelines describe` (landed on main via `9209ed48`): decide migrate-to-`arnold workflow describe`, retain-as-operator-command with rationale, or delete-by-M6. Record in the old-to-new CLI command mapping table with transition behavior and final M6 disposition.

**2. Scope → IN bullets (after "parser snapshots…" / near the CLI dispatch-chain inventory bullet):**

> - Cover `arnold pipelines describe` in parser snapshots, CLI help text, installed-wheel smoke, shell-completion generators, and the dispatch-chain inventory.

**3. Done criteria → add or amend criterion 3:**

> 3\. Workflow CLI commands exist and have parser/snapshot tests. `arnold pipelines describe` is explicitly dispositioned (migrate/retain/delete) with parser snapshot, help text, and installed-wheel smoke coverage.

**4. Touchpoints → add:**

> - `arnold pipelines describe` command, parser snapshot, and any generated docs/skills that reference it.

---

**File:** `.megaplan/briefs/workflow-manifest-runtime/m6-clean-break-purge-conformance.md`

**5. Scope → IN bullets (after the CLI entrypoint audit bullet):**

> - Enforce the M5 disposition of `arnold pipelines describe`: if retained/migrated, prove it passes installed-wheel smoke and targets only surviving modules; if deleted, prove `arnold pipelines describe <module>` fails from the installed wheel.

---

### Why the rest is sufficient

- **Parser snapshot discovered modules** (`deliberation`, `evidence-pack`, `folder-audit`, `live-supervisor`, `simplify-writing`, `vibecomfy-executor`): covered by M5's explicit shipped-pipeline scope listing those roots and the "every shipped pipeline" migration inventory.
- **Chain CLI changes** (`--fresh`, `manual` merge policy, `--no-verify`, spec key validation, `current_milestone_base_sha`): Megaplan-internal chain behavior; covered by M4's Megaplan-specific operator command mapping and M5's "reconcile every CLI row with M4's inventory."
- **SKILL.md resolution change** (`manifest.py`): discoverable implementation detail under M5's generated-artifact regeneration and freshness gates.
- **Judge manifest** (`discovery/judge_manifest.py`): the chain-notes already route this to M1/M2/M5/M6; M5's discovery-trust and pipeline-ID-registry scope covers it.
- **`adopt-execution` override action, new override options**: covered by M4/M5 operator-command mapping and parser-snapshot regeneration gates.