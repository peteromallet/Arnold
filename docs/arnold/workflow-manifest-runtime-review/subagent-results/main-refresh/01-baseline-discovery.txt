## Verdict: **needs edits**

The plan acknowledges the commit in `chain-notes.md` and M1's scope covers discovery/judge-manifest mapping in broad strokes, but three gaps leave the plan incomplete for a reviewer trying to trace every pipeline/discovery/trust/identity row to a final manifest version.

---

### Gap 1: M1 must name the new neutral judge-manifest location as authoritative baseline

M1's "old-to-new identity/discovery map" scope bullet and touchpoints list don't reference the actual file `arnold/pipeline/discovery/judge_manifest.py`. The plan talks about `judge_manifest.py` sidecars abstractly but never acknowledges that on current mainline, the canonical primitives already live in a neutral discovery package, not in Megaplan product code.

**Edit: `.megaplan/briefs/workflow-manifest-runtime/m1-baseline-manifest-contract.md`**  
In **Scope → IN**, add a bullet after the existing discovery-mapping bullet:

> - Treat `arnold/pipeline/discovery/judge_manifest.py` (the neutral `JudgePieceManifest`, `JudgeManifestPort`, `compute_piece_version`, `compute_judge_version`, `make_judge_manifest`, `load_judge_manifest`, `dump_judge_manifest`) as the authoritative mainline baseline. The Megaplan bridge at `arnold/pipelines/megaplan/judge_manifest.py` (`from arnold.pipeline.discovery.judge_manifest import *`) is a temporary surface burned down by M6.

**Edit: same file, Touchpoints**, add:

> - `arnold/pipeline/discovery/judge_manifest.py`
> - `arnold/pipeline/discovery/manifest.py` (per-pipeline skill directory fallback logic from `387ca10c`)

---

### Gap 2: No concrete reconciliation of judge-manifest identity model with WorkflowManifest identity model

The plan says identity derives from `id + manifest_hash`, but judge manifests have an independent identity model (`piece_version`, `judge_version`, `rubric_hash`). The plan never states whether these survive, are absorbed, or are orthogonal.

**Edit: `.megaplan/briefs/workflow-manifest-runtime/m1-baseline-manifest-contract.md`**  
In **Scope → IN**, add a bullet after the identity derivation bullet:

> - Reconcile judge-manifest identity (piece_version, judge_version, rubric_hash) with WorkflowManifest identity (id + manifest_hash). Declare whether judge manifests survive as independent sidecar artifacts with their own hash lineage, are absorbed as `WorkflowManifest` sub-records keyed by manifest_hash, or are regenerated from the compiler's manifest_hash with explicit provenance. If orthogonal, define the boundary and cross-reference rules.

**Edit: `.megaplan/briefs/workflow-manifest-runtime/m6-clean-break-purge-conformance.md`**  
In **Scope → IN**, after the manifest-identity ledger bullet, add:

> - Resolve every surviving judge-manifest sidecar (`piece_version`/`judge_version`/`rubric_hash`) against either (a) the compiler's `manifest_hash` if absorbed, or (b) a locked regeneration gate if orthogonal. Stale judge-manifest hashes fail M6 with the same severity as stale pipeline registry hashes.

---

### Gap 3: No acceptance criterion that discovery trust gates resolve against the final manifest version

The plan defers the final identity ledger to M6, but no M1–M5 criterion explicitly gates discovery trust classification (auto-executable/quarantined/blessed) against the judge-manifest or workflow-manifest identity that will survive cleanup.

**Edit: `.megaplan/briefs/workflow-manifest-runtime/m1-baseline-manifest-contract.md`**  
In **Done Criteria**, add as item 13 (or renumber):

> 13. A committed table maps every discovery trust classification (auto-executable, quarantined, blessed) to its identity anchor: either `piece_version`/`judge_version` for surviving judge manifests, `manifest_hash` for workflow manifests, or both with explicit cross-reference rules. No trust row is identity-ambiguous.