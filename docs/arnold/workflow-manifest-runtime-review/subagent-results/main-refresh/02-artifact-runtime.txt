## Verdict: **needs edits**

The plan mentions the new mainline changes at a high level in `chain-notes.md` (e.g., "M1/M3/M4 should account for this artifact-helper shape"), but no milestone brief translates those mentions into concrete contract requirements. An implementer could faithfully follow M1–M6 and still produce artifact directories, versioning conventions, resume cursors, and engine-isolation semantics that diverge from what `origin/main@0035c231` actually shipped.

---

### Required Edits

**1. `m1-baseline-manifest-contract.md` — Scope → IN, before "Define shared kernel vocabulary"**

> - Capture the current mainline dual artifact-root model: `.megaplan/<kind>` for repo-root callers and `ctx.plan_dir/<kind>` for `StepContext` callers. The neutral `artifact_dir` contract must represent both.
> - Freeze the `vN.<ext>` versioned-artifact naming convention already live in mainline `artifacts.py`. M1 must lock the version-number derivation, `latest_version`/`next_version_path` semantics, and the rule that versioned paths are canonical for generated artifacts, golden fixtures, and content-type registry provenance.

**2. `m1-baseline-manifest-contract.md` — Scope → IN, after the resume/replay kernel contracts bullet**

> - Capture the mainline dual-cursor resume pattern (native-first cursor via `read_native_cursor`, falling back to legacy graph cursor). The contract must define how manifest-coordinate cursors resolve against both shapes during migration.

**3. `m3-manifest-runner-runtime.md` — Scope → IN, after the artifact-write bullet**

> - Artifact writes must follow the versioned `vN.<ext>` convention frozen in M1. Runtime writes to `artifact_dir(...)/v{latest+1}.{ext}`, reads resolve newest versioned path. Legacy flat-directory artifacts are migration input only.
> - Manifest-coordinate resume must resolve native-first cursors before legacy graph cursors, matching the mainline dual-resolution order in `evidence_pack/resume.py`.

**4. `m4-megaplan-product-migration.md` — Scope → IN, after historical-state bullet**

> - Product migration must re-route all `.megaplan/<kind>` artifact writes through the versioned-artifact convention and `ctx.plan_dir` paths where callers are `StepContext`-based, matching the arrived-at mainline contract.

**5. `chain-notes.md` — After the "Plan impact" paragraph, add:**

> - Concrete contract captures required: (a) M1 must freeze versioned `vN.ext` artifact convention and dual artifact-root model; (b) M3 must execute artifact writes and resume against those conventions; (c) M4 must migrate product call sites; (d) M3/M4 schema status enums must include `"pending"` (landed in `0035c231` schemas/runtime.py).

---

These four edits ensure M1–M6 will force final pipeline versions and artifact/resume semantics to line up with the concrete shapes that arrived on `origin/main`, not just acknowledge them in prose.