I now have a complete picture. Here's my analysis and verdict.

---

## Verdict: **Needs edits** — minor but material operational gaps

The `chain.yaml` itself is valid against the new validation (all keys in the allowed set; `merge_policy: review` is accepted; no DeepSeek provider specified so the narrowing to `("direct", "fireworks")` doesn't bite). But the plan docs are missing operational instructions for the new chain behaviors.

---

### Edit 1: `prep.md` — Add `--fresh` and base-refresh guidance

**Section**: "Run Shape" (after the recommended command block)

**Add bullet**:
- "If re-running after a partial or failed chain attempt, add `--fresh` to remove the stale registered worktree/branch before the chain creates a new one."
- "Before starting, verify you're on the refreshed base: `git fetch origin && git log --oneline -1 origin/main` should show `0035c231` or newer."

---

### Edit 2: `chain-notes.md` — Operationalize the base refresh

**Section**: "Main Base Refresh"

**Replace** the existing "Plan impact:" paragraph with more actionable wording:

- "**Operator checklist before launching M1:** (1) `git fetch origin` — confirm `origin/main` is at `0035c231` or newer. (2) The chain now auto-records `current_milestone_base_sha` in state; no manual action needed, but verify it after M1 starts. (3) If a prior chain run left a stale worktree or branch, pass `--fresh`."

---

### Edit 3: `chain-notes.md` — Clarify manual merge policy

**Section**: "Main Base Refresh", in the bullet listing chain/harness fixes

**Replace** the bullet "spec validation, manual merge policy, DeepSeek provider narrowing, state base SHA, `--fresh`, and automated push `--no-verify`" with:

- "spec validation (unknown keys now rejected), `manual` merge policy (accepted as a synonym for `review`; our `chain.yaml` already uses `review` so no change needed), DeepSeek provider narrowing to `direct`/`fireworks` (no impact — all milestones use `vendor: codex`), `current_milestone_base_sha` auto-recorded in state, `--fresh` flag for worktree/branch reset, and automated `--no-verify` on all pushes."

---

### What's already fine

- **chain.yaml** — parses cleanly under new validation; `merge_policy: review` is valid; no unknown keys; `driver` sub-keys are read permissively.
- **Profile/provider** — all milestones use `vendor: codex`, so the DeepSeek provider narrowing (`direct`/`fireworks`) is a no-op for this plan.
- **`--no-verify` push** — automatic; no operator action or doc change needed.
- **`current_milestone_base_sha`** — auto-populated by the chain; backward-compatible with old state files (defaults `None`).