Here is my complete analysis:

## Verdict: **needs edits**

M4's brief does not explicitly include eight newly-landed mainline behaviors as baseline. Since M4 is the migration/parity milestone, these behaviors must be in its parity inventory or the manifest-backed product will regress.

---

### Edits to `.megaplan/briefs/workflow-manifest-runtime/m4-megaplan-product-migration.md`

**Section: `## Scope` → `IN:` list** — add these bullets:

1. After *"Preserve all current gate condition semantics as product policy: every existing gate transition family..."*:
   - *"Preserve gate auto-downgrade: high-complexity unverifiable critique checks must block PROCEED and route to ITERATE with explicit downgrade rationale and force-proceed override, matching current `handlers/gate.py::has_high_complexity_unverifiable_checks` behavior."*

2. After *"Preserve override/fallback routing, feedback-as-phase, robustness-driven dynamic topology..."*:
   - *"Preserve finalize fallback blast-radius: when plan metadata lacks `test_blast_radius`, compute scoped pytest selectors from git diff against the execution baseline, matching current `handlers/finalize.py::_fallback_baseline_test_selection` behavior."*

3. After *"Preserve override/fallback routing..."*:
   - *"Preserve execution-evidence hardening: subprocess calls must handle `FileNotFoundError`/`TimeoutExpired`; task-satisfaction staleness must accept ancestor HEAD relations to prevent infinite re-execution loops from harness commits between batches, matching current `orchestration/execution_evidence.py` and `orchestration/task_satisfaction.py::_head_is_ancestor` behavior."*

4. After the plan-version/historical-state bullets:
   - *"Preserve plan-text newline normalization: decode escaped `\n`/`\r\n` in model-returned JSON plan strings to real newlines before Markdown validation, matching current `handlers/shared.py::_normalize_plan_text` behavior."*

5. After *"Preserve current 9-stage planning behavior..."*:
   - *"Preserve execute-prompt `pending` task status: `task_updates[].status` may return `pending` for multi-step work requiring separate tool calls, matching current `prompts/execute.py` behavior."*
   - *"Preserve critique-payload normalization: strip unknown check-level keys as defense against template/schema drift, matching current `handlers/critique.py::_normalize_critique_payload_for_recovery` behavior."*
   - *"Preserve review-payload normalization: populate `target` objects (kind, task_id, task_ids, id), default `deterministic_check` to None, and compute `task_ids` from concerned_task_ids, matching current `handlers/review.py::_prepare_review_payload` behavior."*

6. After *"Preserve state/resume behavior..."*:
   - *"Preserve dynamic megaplan manifest-hash resume: treat missing current hash for the canonical megaplan pipeline as matching the stored legacy sentinel, matching current `_core/workflow.py` behavior."*

**Section: `## Done Criteria`, criterion 3** — after *"resume-sensitive behavior"*, append:
- *", plan-text newline normalization, execute-prompt `pending` status, gate auto-downgrade for high-complexity unverifiable checks, finalize fallback blast-radius, critique-payload key-stripping, review-payload target/deterministic_check normalization, execution-evidence subprocess hardening, task-satisfaction ancestor-head staleness, and infrastructure_error characterization coverage"*

**Section: `## Scope` → `IN:` list** — add one more bullet near the characterization traces bullet:
- *"Preserve `infrastructure_error` characterization: auto-drive corpus and golden recipe for non-retryable infrastructure errors, matching current `tests/characterization/auto_drive_corpus/infrastructure_error.json` and `test_auto_drive.py` branch/recipe behavior."*