Working directory: `/Users/peteromalley/Documents/megaplan`

Read the workflow-manifest-runtime plan artifacts.

Inventory class: all CLI commands, parser surfaces, console entrypoints, lazy dispatchers, shell scripts, and installed-wheel CLI behavior.

Inspect the repo. Return an inventory with path/surface, role, final disposition, plan hook, and missing plan gaps. Focus on old `megaplan`, `arnold pipeline(s)`, `arnold workflow`, parser snapshots, and `pyproject.toml` scripts. Under 900 words.
