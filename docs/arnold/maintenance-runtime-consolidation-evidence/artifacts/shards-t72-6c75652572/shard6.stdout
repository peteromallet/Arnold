........................................................................ [  3%]
........................................................................ [  6%]
........................................................................ [  9%]
........................................................................ [ 12%]
........................................................................ [ 15%]
........................................................................ [ 18%]
........................................................................ [ 21%]
........................................................................ [ 24%]
........................................................................ [ 27%]
.FFFFFFFFFFFFFFFF....................................................... [ 30%]
........................................................................ [ 33%]
..........................F............................................. [ 36%]
........................................................................ [ 39%]
........................................................................ [ 42%]
........................................................................ [ 45%]
........................................................................ [ 48%]
........................................................................ [ 51%]
........................................................................ [ 54%]
........................................................................ [ 57%]
........................................................................ [ 60%]
........................................................................ [ 63%]
............................F........................................... [ 66%]
........................................................................ [ 69%]
...........................................F......F......F.........F.... [ 72%]
........................................................................ [ 75%]
........................................................................ [ 78%]
........................................................................ [ 81%]
........................................................................ [ 84%]
........................................................................ [ 87%]
........................................................................ [ 90%]
..............................................FF....F................... [ 93%]
........................................................................ [ 96%]
..............F..FFFFF.......F........FFFFFFF........................... [ 99%]
.......                                                                  [100%]
=================================== FAILURES ===================================
________________ test_cli_adopt_happy_path_and_idempotent_retry ________________

tmp_path = PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_cli_adopt_happy_path_and_0')
capsys = <_pytest.capture.CaptureFixture object at 0x17a824210>
adopt_tree = {'identity_path': PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-55...rs/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/adopt-tree0/.megaplan/plans/adopt-plan'), ...}
patch_verifier = None

    def test_cli_adopt_happy_path_and_idempotent_retry(
        tmp_path: Path,
        capsys: pytest.CaptureFixture[str],
        adopt_tree: dict,
        patch_verifier: None,
    ) -> None:
        tree = _fresh_tree(tmp_path, adopt_tree)
        argv = _adopt_argv(tree)
        rc, payload = _chain_cli(tree["root"], argv, capsys)
>       assert rc == 0, payload
E       AssertionError: {'error': 'capability_absent', 'message': 'occurrence_adoption requires a minted MutationCapability bound to action, occurrence, target, and fence epoch', 'success': False}
E       assert 1 == 0

tests/arnold_pipelines/megaplan/chain/test_occurrence_adopt.py:802: AssertionError
---------------------------- Captured stderr setup -----------------------------
huggingface/tokenizers: The current process just got forked, after parallelism has already been used. Disabling parallelism to avoid deadlocks...
To disable this warning, you can either:
	- Avoid using `tokenizers` before the fork if possible
	- Explicitly set the environment variable TOKENIZERS_PARALLELISM=(true | false)
huggingface/tokenizers: The current process just got forked, after parallelism has already been used. Disabling parallelism to avoid deadlocks...
To disable this warning, you can either:
	- Avoid using `tokenizers` before the fork if possible
	- Explicitly set the environment variable TOKENIZERS_PARALLELISM=(true | false)
huggingface/tokenizers: The current process just got forked, after parallelism has already been used. Disabling parallelism to avoid deadlocks...
To disable this warning, you can either:
	- Avoid using `tokenizers` before the fork if possible
	- Explicitly set the environment variable TOKENIZERS_PARALLELISM=(true | false)
huggingface/tokenizers: The current process just got forked, after parallelism has already been used. Disabling parallelism to avoid deadlocks...
To disable this warning, you can either:
	- Avoid using `tokenizers` before the fork if possible
	- Explicitly set the environment variable TOKENIZERS_PARALLELISM=(true | false)
huggingface/tokenizers: The current process just got forked, after parallelism has already been used. Disabling parallelism to avoid deadlocks...
To disable this warning, you can either:
	- Avoid using `tokenizers` before the fork if possible
	- Explicitly set the environment variable TOKENIZERS_PARALLELISM=(true | false)
huggingface/tokenizers: The current process just got forked, after parallelism has already been used. Disabling parallelism to avoid deadlocks...
To disable this warning, you can either:
	- Avoid using `tokenizers` before the fork if possible
	- Explicitly set the environment variable TOKENIZERS_PARALLELISM=(true | false)
huggingface/tokenizers: The current process just got forked, after parallelism has already been used. Disabling parallelism to avoid deadlocks...
To disable this warning, you can either:
	- Avoid using `tokenizers` before the fork if possible
	- Explicitly set the environment variable TOKENIZERS_PARALLELISM=(true | false)
huggingface/tokenizers: The current process just got forked, after parallelism has already been used. Disabling parallelism to avoid deadlocks...
To disable this warning, you can either:
	- Avoid using `tokenizers` before the fork if possible
	- Explicitly set the environment variable TOKENIZERS_PARALLELISM=(true | false)
huggingface/tokenizers: The current process just got forked, after parallelism has already been used. Disabling parallelism to avoid deadlocks...
To disable this warning, you can either:
	- Avoid using `tokenizers` before the fork if possible
	- Explicitly set the environment variable TOKENIZERS_PARALLELISM=(true | false)
_ test_cli_adopt_refuses_one_bit_cas_flip_zero_mutation[--expected-chain-state-sha256] _

tmp_path = PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_cli_adopt_refuses_one_bit0')
capsys = <_pytest.capture.CaptureFixture object at 0x17a8b8150>
adopt_tree = {'identity_path': PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-55...rs/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/adopt-tree0/.megaplan/plans/adopt-plan'), ...}
patch_verifier = None, flag = '--expected-chain-state-sha256'

    @pytest.mark.parametrize(
        "flag",
        [
            "--expected-chain-state-sha256",
            "--expected-plan-state-sha256",
            "--expected-latest-failure-sha256",
            "--expected-resume-cursor-sha256",
            "--expected-pause-authority-sha256",
            "--expected-runtime-manifest-sha256",
            "--expected-marker-sha256",
            "--expected-runtime-roots-sha256",
        ],
    )
    def test_cli_adopt_refuses_one_bit_cas_flip_zero_mutation(
        tmp_path: Path,
        capsys: pytest.CaptureFixture[str],
        adopt_tree: dict,
        patch_verifier: None,
        flag: str,
    ) -> None:
        tree = _fresh_tree(tmp_path, adopt_tree)
        argv = _adopt_argv(tree)
        index = argv.index(flag) + 1
        value = argv[index]
        flipped = "sha256:" + ("0" if value[7] != "0" else "1") + value[8:]
        argv[index] = flipped
        pre = _snapshot(tree["root"])
        rc, payload = _chain_cli(tree["root"], argv, capsys)
        assert rc == 1, (rc, payload)
        assert payload is not None and payload["success"] is False, payload
>       assert payload["error"] == "cas_mismatch", payload
E       AssertionError: {'error': 'capability_absent', 'message': 'occurrence_adoption requires a minted MutationCapability bound to action, occurrence, target, and fence epoch', 'success': False}
E       assert 'capability_absent' == 'cas_mismatch'
E         
E         - cas_mismatch
E         + capability_absent

tests/arnold_pipelines/megaplan/chain/test_occurrence_adopt.py:880: AssertionError
_ test_cli_adopt_refuses_one_bit_cas_flip_zero_mutation[--expected-plan-state-sha256] _

tmp_path = PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_cli_adopt_refuses_one_bit1')
capsys = <_pytest.capture.CaptureFixture object at 0x17ab86b10>
adopt_tree = {'identity_path': PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-55...rs/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/adopt-tree0/.megaplan/plans/adopt-plan'), ...}
patch_verifier = None, flag = '--expected-plan-state-sha256'

    @pytest.mark.parametrize(
        "flag",
        [
            "--expected-chain-state-sha256",
            "--expected-plan-state-sha256",
            "--expected-latest-failure-sha256",
            "--expected-resume-cursor-sha256",
            "--expected-pause-authority-sha256",
            "--expected-runtime-manifest-sha256",
            "--expected-marker-sha256",
            "--expected-runtime-roots-sha256",
        ],
    )
    def test_cli_adopt_refuses_one_bit_cas_flip_zero_mutation(
        tmp_path: Path,
        capsys: pytest.CaptureFixture[str],
        adopt_tree: dict,
        patch_verifier: None,
        flag: str,
    ) -> None:
        tree = _fresh_tree(tmp_path, adopt_tree)
        argv = _adopt_argv(tree)
        index = argv.index(flag) + 1
        value = argv[index]
        flipped = "sha256:" + ("0" if value[7] != "0" else "1") + value[8:]
        argv[index] = flipped
        pre = _snapshot(tree["root"])
        rc, payload = _chain_cli(tree["root"], argv, capsys)
        assert rc == 1, (rc, payload)
        assert payload is not None and payload["success"] is False, payload
>       assert payload["error"] == "cas_mismatch", payload
E       AssertionError: {'error': 'capability_absent', 'message': 'occurrence_adoption requires a minted MutationCapability bound to action, occurrence, target, and fence epoch', 'success': False}
E       assert 'capability_absent' == 'cas_mismatch'
E         
E         - cas_mismatch
E         + capability_absent

tests/arnold_pipelines/megaplan/chain/test_occurrence_adopt.py:880: AssertionError
_ test_cli_adopt_refuses_one_bit_cas_flip_zero_mutation[--expected-latest-failure-sha256] _

tmp_path = PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_cli_adopt_refuses_one_bit2')
capsys = <_pytest.capture.CaptureFixture object at 0x17ab32450>
adopt_tree = {'identity_path': PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-55...rs/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/adopt-tree0/.megaplan/plans/adopt-plan'), ...}
patch_verifier = None, flag = '--expected-latest-failure-sha256'

    @pytest.mark.parametrize(
        "flag",
        [
            "--expected-chain-state-sha256",
            "--expected-plan-state-sha256",
            "--expected-latest-failure-sha256",
            "--expected-resume-cursor-sha256",
            "--expected-pause-authority-sha256",
            "--expected-runtime-manifest-sha256",
            "--expected-marker-sha256",
            "--expected-runtime-roots-sha256",
        ],
    )
    def test_cli_adopt_refuses_one_bit_cas_flip_zero_mutation(
        tmp_path: Path,
        capsys: pytest.CaptureFixture[str],
        adopt_tree: dict,
        patch_verifier: None,
        flag: str,
    ) -> None:
        tree = _fresh_tree(tmp_path, adopt_tree)
        argv = _adopt_argv(tree)
        index = argv.index(flag) + 1
        value = argv[index]
        flipped = "sha256:" + ("0" if value[7] != "0" else "1") + value[8:]
        argv[index] = flipped
        pre = _snapshot(tree["root"])
        rc, payload = _chain_cli(tree["root"], argv, capsys)
        assert rc == 1, (rc, payload)
        assert payload is not None and payload["success"] is False, payload
>       assert payload["error"] == "cas_mismatch", payload
E       AssertionError: {'error': 'capability_absent', 'message': 'occurrence_adoption requires a minted MutationCapability bound to action, occurrence, target, and fence epoch', 'success': False}
E       assert 'capability_absent' == 'cas_mismatch'
E         
E         - cas_mismatch
E         + capability_absent

tests/arnold_pipelines/megaplan/chain/test_occurrence_adopt.py:880: AssertionError
_ test_cli_adopt_refuses_one_bit_cas_flip_zero_mutation[--expected-resume-cursor-sha256] _

tmp_path = PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_cli_adopt_refuses_one_bit3')
capsys = <_pytest.capture.CaptureFixture object at 0x17aeff350>
adopt_tree = {'identity_path': PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-55...rs/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/adopt-tree0/.megaplan/plans/adopt-plan'), ...}
patch_verifier = None, flag = '--expected-resume-cursor-sha256'

    @pytest.mark.parametrize(
        "flag",
        [
            "--expected-chain-state-sha256",
            "--expected-plan-state-sha256",
            "--expected-latest-failure-sha256",
            "--expected-resume-cursor-sha256",
            "--expected-pause-authority-sha256",
            "--expected-runtime-manifest-sha256",
            "--expected-marker-sha256",
            "--expected-runtime-roots-sha256",
        ],
    )
    def test_cli_adopt_refuses_one_bit_cas_flip_zero_mutation(
        tmp_path: Path,
        capsys: pytest.CaptureFixture[str],
        adopt_tree: dict,
        patch_verifier: None,
        flag: str,
    ) -> None:
        tree = _fresh_tree(tmp_path, adopt_tree)
        argv = _adopt_argv(tree)
        index = argv.index(flag) + 1
        value = argv[index]
        flipped = "sha256:" + ("0" if value[7] != "0" else "1") + value[8:]
        argv[index] = flipped
        pre = _snapshot(tree["root"])
        rc, payload = _chain_cli(tree["root"], argv, capsys)
        assert rc == 1, (rc, payload)
        assert payload is not None and payload["success"] is False, payload
>       assert payload["error"] == "cas_mismatch", payload
E       AssertionError: {'error': 'capability_absent', 'message': 'occurrence_adoption requires a minted MutationCapability bound to action, occurrence, target, and fence epoch', 'success': False}
E       assert 'capability_absent' == 'cas_mismatch'
E         
E         - cas_mismatch
E         + capability_absent

tests/arnold_pipelines/megaplan/chain/test_occurrence_adopt.py:880: AssertionError
_ test_cli_adopt_refuses_one_bit_cas_flip_zero_mutation[--expected-pause-authority-sha256] _

tmp_path = PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_cli_adopt_refuses_one_bit4')
capsys = <_pytest.capture.CaptureFixture object at 0x17a80d950>
adopt_tree = {'identity_path': PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-55...rs/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/adopt-tree0/.megaplan/plans/adopt-plan'), ...}
patch_verifier = None, flag = '--expected-pause-authority-sha256'

    @pytest.mark.parametrize(
        "flag",
        [
            "--expected-chain-state-sha256",
            "--expected-plan-state-sha256",
            "--expected-latest-failure-sha256",
            "--expected-resume-cursor-sha256",
            "--expected-pause-authority-sha256",
            "--expected-runtime-manifest-sha256",
            "--expected-marker-sha256",
            "--expected-runtime-roots-sha256",
        ],
    )
    def test_cli_adopt_refuses_one_bit_cas_flip_zero_mutation(
        tmp_path: Path,
        capsys: pytest.CaptureFixture[str],
        adopt_tree: dict,
        patch_verifier: None,
        flag: str,
    ) -> None:
        tree = _fresh_tree(tmp_path, adopt_tree)
        argv = _adopt_argv(tree)
        index = argv.index(flag) + 1
        value = argv[index]
        flipped = "sha256:" + ("0" if value[7] != "0" else "1") + value[8:]
        argv[index] = flipped
        pre = _snapshot(tree["root"])
        rc, payload = _chain_cli(tree["root"], argv, capsys)
        assert rc == 1, (rc, payload)
        assert payload is not None and payload["success"] is False, payload
>       assert payload["error"] == "cas_mismatch", payload
E       AssertionError: {'error': 'capability_absent', 'message': 'occurrence_adoption requires a minted MutationCapability bound to action, occurrence, target, and fence epoch', 'success': False}
E       assert 'capability_absent' == 'cas_mismatch'
E         
E         - cas_mismatch
E         + capability_absent

tests/arnold_pipelines/megaplan/chain/test_occurrence_adopt.py:880: AssertionError
_ test_cli_adopt_refuses_one_bit_cas_flip_zero_mutation[--expected-runtime-manifest-sha256] _

tmp_path = PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_cli_adopt_refuses_one_bit5')
capsys = <_pytest.capture.CaptureFixture object at 0x17ae95350>
adopt_tree = {'identity_path': PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-55...rs/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/adopt-tree0/.megaplan/plans/adopt-plan'), ...}
patch_verifier = None, flag = '--expected-runtime-manifest-sha256'

    @pytest.mark.parametrize(
        "flag",
        [
            "--expected-chain-state-sha256",
            "--expected-plan-state-sha256",
            "--expected-latest-failure-sha256",
            "--expected-resume-cursor-sha256",
            "--expected-pause-authority-sha256",
            "--expected-runtime-manifest-sha256",
            "--expected-marker-sha256",
            "--expected-runtime-roots-sha256",
        ],
    )
    def test_cli_adopt_refuses_one_bit_cas_flip_zero_mutation(
        tmp_path: Path,
        capsys: pytest.CaptureFixture[str],
        adopt_tree: dict,
        patch_verifier: None,
        flag: str,
    ) -> None:
        tree = _fresh_tree(tmp_path, adopt_tree)
        argv = _adopt_argv(tree)
        index = argv.index(flag) + 1
        value = argv[index]
        flipped = "sha256:" + ("0" if value[7] != "0" else "1") + value[8:]
        argv[index] = flipped
        pre = _snapshot(tree["root"])
        rc, payload = _chain_cli(tree["root"], argv, capsys)
        assert rc == 1, (rc, payload)
        assert payload is not None and payload["success"] is False, payload
>       assert payload["error"] == "cas_mismatch", payload
E       AssertionError: {'error': 'capability_absent', 'message': 'occurrence_adoption requires a minted MutationCapability bound to action, occurrence, target, and fence epoch', 'success': False}
E       assert 'capability_absent' == 'cas_mismatch'
E         
E         - cas_mismatch
E         + capability_absent

tests/arnold_pipelines/megaplan/chain/test_occurrence_adopt.py:880: AssertionError
_ test_cli_adopt_refuses_one_bit_cas_flip_zero_mutation[--expected-marker-sha256] _

tmp_path = PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_cli_adopt_refuses_one_bit6')
capsys = <_pytest.capture.CaptureFixture object at 0x17a6c2610>
adopt_tree = {'identity_path': PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-55...rs/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/adopt-tree0/.megaplan/plans/adopt-plan'), ...}
patch_verifier = None, flag = '--expected-marker-sha256'

    @pytest.mark.parametrize(
        "flag",
        [
            "--expected-chain-state-sha256",
            "--expected-plan-state-sha256",
            "--expected-latest-failure-sha256",
            "--expected-resume-cursor-sha256",
            "--expected-pause-authority-sha256",
            "--expected-runtime-manifest-sha256",
            "--expected-marker-sha256",
            "--expected-runtime-roots-sha256",
        ],
    )
    def test_cli_adopt_refuses_one_bit_cas_flip_zero_mutation(
        tmp_path: Path,
        capsys: pytest.CaptureFixture[str],
        adopt_tree: dict,
        patch_verifier: None,
        flag: str,
    ) -> None:
        tree = _fresh_tree(tmp_path, adopt_tree)
        argv = _adopt_argv(tree)
        index = argv.index(flag) + 1
        value = argv[index]
        flipped = "sha256:" + ("0" if value[7] != "0" else "1") + value[8:]
        argv[index] = flipped
        pre = _snapshot(tree["root"])
        rc, payload = _chain_cli(tree["root"], argv, capsys)
        assert rc == 1, (rc, payload)
        assert payload is not None and payload["success"] is False, payload
>       assert payload["error"] == "cas_mismatch", payload
E       AssertionError: {'error': 'capability_absent', 'message': 'occurrence_adoption requires a minted MutationCapability bound to action, occurrence, target, and fence epoch', 'success': False}
E       assert 'capability_absent' == 'cas_mismatch'
E         
E         - cas_mismatch
E         + capability_absent

tests/arnold_pipelines/megaplan/chain/test_occurrence_adopt.py:880: AssertionError
_ test_cli_adopt_refuses_one_bit_cas_flip_zero_mutation[--expected-runtime-roots-sha256] _

tmp_path = PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_cli_adopt_refuses_one_bit7')
capsys = <_pytest.capture.CaptureFixture object at 0x17aee3610>
adopt_tree = {'identity_path': PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-55...rs/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/adopt-tree0/.megaplan/plans/adopt-plan'), ...}
patch_verifier = None, flag = '--expected-runtime-roots-sha256'

    @pytest.mark.parametrize(
        "flag",
        [
            "--expected-chain-state-sha256",
            "--expected-plan-state-sha256",
            "--expected-latest-failure-sha256",
            "--expected-resume-cursor-sha256",
            "--expected-pause-authority-sha256",
            "--expected-runtime-manifest-sha256",
            "--expected-marker-sha256",
            "--expected-runtime-roots-sha256",
        ],
    )
    def test_cli_adopt_refuses_one_bit_cas_flip_zero_mutation(
        tmp_path: Path,
        capsys: pytest.CaptureFixture[str],
        adopt_tree: dict,
        patch_verifier: None,
        flag: str,
    ) -> None:
        tree = _fresh_tree(tmp_path, adopt_tree)
        argv = _adopt_argv(tree)
        index = argv.index(flag) + 1
        value = argv[index]
        flipped = "sha256:" + ("0" if value[7] != "0" else "1") + value[8:]
        argv[index] = flipped
        pre = _snapshot(tree["root"])
        rc, payload = _chain_cli(tree["root"], argv, capsys)
        assert rc == 1, (rc, payload)
        assert payload is not None and payload["success"] is False, payload
>       assert payload["error"] == "cas_mismatch", payload
E       AssertionError: {'error': 'capability_absent', 'message': 'occurrence_adoption requires a minted MutationCapability bound to action, occurrence, target, and fence epoch', 'success': False}
E       assert 'capability_absent' == 'cas_mismatch'
E         
E         - cas_mismatch
E         + capability_absent

tests/arnold_pipelines/megaplan/chain/test_occurrence_adopt.py:880: AssertionError
_____________________ test_cli_adopt_refuses_non_operator ______________________

tmp_path = PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_cli_adopt_refuses_non_ope0')
capsys = <_pytest.capture.CaptureFixture object at 0x17a974810>
adopt_tree = {'identity_path': PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-55...rs/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/adopt-tree0/.megaplan/plans/adopt-plan'), ...}
patch_verifier = None

    def test_cli_adopt_refuses_non_operator(
        tmp_path: Path,
        capsys: pytest.CaptureFixture[str],
        adopt_tree: dict,
        patch_verifier: None,
    ) -> None:
        tree = _fresh_tree(tmp_path, adopt_tree)
        argv = _adopt_argv(tree, actor="not-operator")
        rc, payload = _chain_cli(tree["root"], argv, capsys)
>       assert rc == 1 and payload is not None and payload["error"] == "actor_forbidden"
E       AssertionError: assert (1 == 1 and {'error': 'capability_absent', 'message': 'occurrence_adoption requires a minted MutationCapability bound to action, occurrence, target, and fence epoch', 'success': False} is not None and 'capability_absent' == 'actor_forbidden'
E         
E         - actor_forbidden
E         + capability_absent)

tests/arnold_pipelines/megaplan/chain/test_occurrence_adopt.py:893: AssertionError
_____________________ test_cli_adopt_refuses_missing_pause _____________________

tmp_path = PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_cli_adopt_refuses_missing0')
capsys = <_pytest.capture.CaptureFixture object at 0x17a690250>
adopt_tree = {'identity_path': PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-55...rs/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/adopt-tree0/.megaplan/plans/adopt-plan'), ...}
patch_verifier = None

    def test_cli_adopt_refuses_missing_pause(
        tmp_path: Path,
        capsys: pytest.CaptureFixture[str],
        adopt_tree: dict,
        patch_verifier: None,
    ) -> None:
        tree = _fresh_tree(tmp_path, adopt_tree)
        state = load_chain_state(tree["spec_path"], verify_execution_binding=False)
        state.metadata.pop("operator_pause", None)
        save_chain_state(tree["spec_path"], state)
        argv = _adopt_argv(tree)
        from arnold_pipelines.megaplan.chain.spec import _state_path_for
    
        argv[argv.index("--expected-chain-state-sha256") + 1] = (
            "sha256:" + _sha256_file(_state_path_for(tree["spec_path"]))
        )
        rc, payload = _chain_cli(tree["root"], argv, capsys)
>       assert rc == 1 and payload is not None and payload["error"] == "chain_not_paused"
E       AssertionError: assert (1 == 1 and {'error': 'capability_absent', 'message': 'occurrence_adoption requires a minted MutationCapability bound to action, occurrence, target, and fence epoch', 'success': False} is not None and 'capability_absent' == 'chain_not_paused'
E         
E         - chain_not_paused
E         + capability_absent)

tests/arnold_pipelines/megaplan/chain/test_occurrence_adopt.py:913: AssertionError
------------------------------ Captured log call -------------------------------
WARNING  megaplan:spec.py:2299 M7 chain-state projection append blocked by cursor mismatch: Projection cursor mismatch for 'chain-state': record_count 58 → 48, digest sha256:5abd15108... → sha256:b47f13fec.... State file is intact; projection history may need reconciliation.
______________ test_cli_adopt_refuses_non_null_existing_identity _______________

tmp_path = PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_cli_adopt_refuses_non_nul0')
capsys = <_pytest.capture.CaptureFixture object at 0x17a690a50>
adopt_tree = {'identity_path': PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-55...rs/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/adopt-tree0/.megaplan/plans/adopt-plan'), ...}
patch_verifier = None

    def test_cli_adopt_refuses_non_null_existing_identity(
        tmp_path: Path,
        capsys: pytest.CaptureFixture[str],
        adopt_tree: dict,
        patch_verifier: None,
    ) -> None:
        tree = _fresh_tree(tmp_path, adopt_tree)
        plan_payload = json.loads(tree["plan_path"].read_text(encoding="utf-8"))
        plan_payload["meta"]["repair_identity"] = repair_identity(
            session=SESSION,
            plan=PLAN,
            failure_kind="deterministic_phase_failure",
            phase="gate",
            task="phase:gate",
        )
        tree["plan_path"].write_text(json.dumps(plan_payload, indent=2) + "\n", encoding="utf-8")
        argv = _adopt_argv(tree)
        rc, payload = _chain_cli(tree["root"], argv, capsys)
        assert rc == 1 and payload is not None
>       assert payload["error"] == "repair_identity_already_present", payload
E       AssertionError: {'error': 'capability_absent', 'message': 'occurrence_adoption requires a minted MutationCapability bound to action, occurrence, target, and fence epoch', 'success': False}
E       assert 'capability_absent' == 'repair_ident...ready_present'
E         
E         - repair_identity_already_present
E         + capability_absent

tests/arnold_pipelines/megaplan/chain/test_occurrence_adopt.py:935: AssertionError
___________________ test_cli_adopt_refuses_ambiguous_failure ___________________

tmp_path = PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_cli_adopt_refuses_ambiguo0')
capsys = <_pytest.capture.CaptureFixture object at 0x17a669c10>
adopt_tree = {'identity_path': PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-55...rs/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/adopt-tree0/.megaplan/plans/adopt-plan'), ...}
patch_verifier = None

    def test_cli_adopt_refuses_ambiguous_failure(
        tmp_path: Path,
        capsys: pytest.CaptureFixture[str],
        adopt_tree: dict,
        patch_verifier: None,
    ) -> None:
        tree = _fresh_tree(tmp_path, adopt_tree)
        plan_payload = json.loads(tree["plan_path"].read_text(encoding="utf-8"))
        plan_payload["latest_failure"] = [plan_payload["latest_failure"]]
        tree["plan_path"].write_text(json.dumps(plan_payload, indent=2) + "\n", encoding="utf-8")
        argv = _adopt_argv(tree)
        rc, payload = _chain_cli(tree["root"], argv, capsys)
>       assert rc == 1 and payload is not None and payload["error"] == "ambiguous_failure"
E       AssertionError: assert (1 == 1 and {'error': 'capability_absent', 'message': 'occurrence_adoption requires a minted MutationCapability bound to action, occurrence, target, and fence epoch', 'success': False} is not None and 'capability_absent' == 'ambiguous_failure'
E         
E         - ambiguous_failure
E         + capability_absent)

tests/arnold_pipelines/megaplan/chain/test_occurrence_adopt.py:950: AssertionError
_____________________ test_cli_adopt_refuses_unequal_roots _____________________

tmp_path = PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_cli_adopt_refuses_unequal0')
capsys = <_pytest.capture.CaptureFixture object at 0x17a66b750>
adopt_tree = {'identity_path': PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-55...rs/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/adopt-tree0/.megaplan/plans/adopt-plan'), ...}
patch_verifier = None

    def test_cli_adopt_refuses_unequal_roots(
        tmp_path: Path,
        capsys: pytest.CaptureFixture[str],
        adopt_tree: dict,
        patch_verifier: None,
    ) -> None:
        tree = _fresh_tree(tmp_path, adopt_tree)
        argv = _adopt_argv(tree)
        argv[argv.index("--candidate-root") + 1] = "/elsewhere/root"
        rc, payload = _chain_cli(tree["root"], argv, capsys)
>       assert rc == 1 and payload is not None and payload["error"] == "runtime_roots_unequal"
E       AssertionError: assert (1 == 1 and {'error': 'capability_absent', 'message': 'occurrence_adoption requires a minted MutationCapability bound to action, occurrence, target, and fence epoch', 'success': False} is not None and 'capability_absent' == 'runtime_roots_unequal'
E         
E         - runtime_roots_unequal
E         + capability_absent)

tests/arnold_pipelines/megaplan/chain/test_occurrence_adopt.py:963: AssertionError
_________ test_cli_adopt_crash_after_record_and_after_request_converge _________

tmp_path = PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_cli_adopt_crash_after_rec0')
capsys = <_pytest.capture.CaptureFixture object at 0x17ac974d0>
adopt_tree = {'identity_path': PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-55...rs/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/adopt-tree0/.megaplan/plans/adopt-plan'), ...}
patch_verifier = None
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x17ac95010>

    def test_cli_adopt_crash_after_record_and_after_request_converge(
        tmp_path: Path,
        capsys: pytest.CaptureFixture[str],
        adopt_tree: dict,
        patch_verifier: None,
        monkeypatch: pytest.MonkeyPatch,
    ) -> None:
        """Crash injection after the adoption authority / request writes converges.
    
        The raw OSError propagates (the process dies); on retry the persisted
        adoption record + request are reused and exactly one accepted decision is
        recorded.
        """
        tree = _fresh_tree(tmp_path, adopt_tree)
        argv = _adopt_argv(tree)
    
        real_wrapper = repair_requests.enqueue_owner_adopted_repair_request
    
        def boom_wrapper(*a, **kwargs):
            raise OSError("injected crash before enqueue")
    
        monkeypatch.setattr(repair_requests, "enqueue_owner_adopted_repair_request", boom_wrapper)
>       with pytest.raises(OSError):
E       Failed: DID NOT RAISE <class 'OSError'>

tests/arnold_pipelines/megaplan/chain/test_occurrence_adopt.py:988: Failed
______________ test_cli_adopt_two_concurrent_adopters_one_record _______________

tmp_path = PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_cli_adopt_two_concurrent_0')
adopt_tree = {'identity_path': PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-55...rs/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/adopt-tree0/.megaplan/plans/adopt-plan'), ...}
patch_verifier = None

    def test_cli_adopt_two_concurrent_adopters_one_record(
        tmp_path: Path,
        adopt_tree: dict,
        patch_verifier: None,
    ) -> None:
        import threading
    
        tree = _fresh_tree(tmp_path, adopt_tree)
        argv = _adopt_argv(tree)
        errors: list[BaseException] = []
    
        def adopter() -> None:
            # stdout is intentionally NOT captured (redirect_stdout is not
            # thread-safe); the CLI writes to the pytest capture and the outcome
            # is asserted from the durable files after both threads finish.
            try:
                run_chain_cli(tree["root"], build_parser().parse_args(argv))
            except BaseException as exc:  # noqa: BLE001 - captured for assertion
                errors.append(exc)
    
        threads = [threading.Thread(target=adopter), threading.Thread(target=adopter)]
        for thread in threads:
            thread.start()
        for thread in threads:
            thread.join()
        assert errors == [], errors
        # Exactly ONE adoption record whose identity is the ONLY request.
        adoptions = list((tree["plan_dir"] / "evidence" / "occurrence-adoptions").glob("*.json"))
>       assert len(adoptions) == 1
E       assert 0 == 1
E        +  where 0 = len([])

tests/arnold_pipelines/megaplan/chain/test_occurrence_adopt.py:1068: AssertionError
----------------------------- Captured stdout call -----------------------------
{
  "success": false,
  "error": "capability_absent",
  "message": "occurrence_adoption requires a minted MutationCapability bound to action, occurrence, target, and fence epoch"
}
{
  "success": false,
  "error": "capability_absent",
  "message": "occurrence_adoption requires a minted MutationCapability bound to action, occurrence, target, and fence epoch"
}
_ test_handle_outcome_retries_stale_awaiting_human_with_satisfied_resolutions __

tmp_path = PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_handle_outcome_retries_st0')

    def test_handle_outcome_retries_stale_awaiting_human_with_satisfied_resolutions(
        tmp_path: Path,
    ) -> None:
        plan_dir = tmp_path / ".megaplan" / "plans" / "stale-plan"
        plan_dir.mkdir(parents=True)
        (plan_dir / "state.json").write_text(
            (
                '{"name":"stale-plan","current_state":"awaiting_human","meta":{},'
                '"resume_cursor":{"phase":"execute"}}'
            ),
            encoding="utf-8",
        )
        (plan_dir / "finalize.json").write_text(
            '{"user_actions":[{"id":"ua-1","phase":"before_execute"}]}',
            encoding="utf-8",
        )
        (plan_dir / "user_action_resolutions.json").write_text(
            '{"ua-1":{"action_id":"ua-1","state":"satisfied","created_at":"2026-06-28T00:00:00Z","created_by":"test"}}',
            encoding="utf-8",
        )
    
        spec = SimpleNamespace(
            on_failure_policy=SimpleNamespace(
                retry="retry_milestone",
                escalate="bump_profile",
                abort="stop_chain",
            ),
            on_escalate_policy=SimpleNamespace(
                retry="retry_milestone",
                escalate="bump_profile",
                abort="stop_chain",
            ),
            robustness="standard",
        )
        milestone = SimpleNamespace(label="m7", profile=None, robustness=None)
        state = ChainState()
    
        decision = _handle_outcome(
            DriverOutcome(
                status="awaiting_human",
                plan="stale-plan",
                final_state="awaiting_human",
                iterations=1,
                reason="stale blocked gate",
            ),
            spec=spec,
            writer=lambda _message: None,
            milestone=milestone,
            state=state,
            root=tmp_path,
        )
    
>       assert decision == "retry"
E       AssertionError: assert 'stop' == 'retry'
E         
E         - retry
E         + stop

tests/arnold_pipelines/megaplan/test_chain_awaiting_human_retry.py:248: AssertionError
_______ test_live_execute_worker_is_not_a_finalized_state_contradiction ________

    def test_live_execute_worker_is_not_a_finalized_state_contradiction() -> None:
        target = {
            "authoritative_source": "chain_state",
            "current_refs": {"current_plan_name": "s3-boundary-coverage"},
            "plan_state": {"present": True, "fingerprint": "sha256:live-plan"},
            "chain_state": {
                "present": True,
                "fingerprint": "sha256:stale-chain",
                "milestone_total": 10,
                "completed_count": 2,
            },
            "active_step_heartbeat": {
                "active": True,
                "worker_pid": "1004788",
                "pid_live": True,
            },
        }
    
>       assert repair_contract._has_terminality_contradiction(target) is False
E       AssertionError: assert True is False
E        +  where True = <function _has_terminality_contradiction at 0x121d98f40>({'active_step_heartbeat': {'active': True, 'pid_live': True, 'worker_pid': '1004788'}, 'authoritative_source': 'chain_...le-chain', 'milestone_total': 10, 'present': True}, 'current_refs': {'current_plan_name': 's3-boundary-coverage'}, ...})
E        +    where <function _has_terminality_contradiction at 0x121d98f40> = repair_contract._has_terminality_contradiction

tests/cloud/test_repair_custody.py:669: AssertionError
_________ test_real_editable_launch_and_noneditable_supervisor_vectors _________

tmp_path = PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_real_editable_launch_and_0')

    def test_real_editable_launch_and_noneditable_supervisor_vectors(
        tmp_path: Path,
    ) -> None:
        source = Path(__file__).resolve().parents[2]
        revision = subprocess.check_output(
            ["git", "-C", str(source), "rev-parse", "HEAD"],
            text=True,
        ).strip()
        launch_python = _install_test_runtime(
            tmp_path / "launch-venv",
            source,
            editable=True,
        )
        supervisor_runtime = tmp_path / "supervisor-venv"
>       supervisor_python = _install_test_runtime(
            supervisor_runtime,
            source,
            editable=False,
        )

tests/cloud/test_runtime_attestation.py:512: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/cloud/test_runtime_attestation.py:493: in _install_test_runtime
    shutil.copytree(source / package, site_dir / package)
../../../.pyenv/versions/3.11.11/lib/python3.11/shutil.py:573: in copytree
    return _copytree(entries=entries, src=src, dst=dst, symlinks=symlinks,
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

entries = [<DirEntry 'megaplan'>, <DirEntry 'run_authority'>, <DirEntry '.DS_Store'>, <DirEntry '_template'>, <DirEntry 'discovery.py'>, <DirEntry '__init__.py'>, ...]
src = PosixPath('/Users/peteromalley/Documents/.megaplan-worktrees/runtime-convergence-execution/arnold_pipelines')
dst = PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_real_editable_launch_and_0/supervisor-venv/lib/python3.11/site-packages/arnold_pipelines')
symlinks = False, ignore = None, copy_function = <function copy2 at 0x100de0ae0>
ignore_dangling_symlinks = False, dirs_exist_ok = False

    def _copytree(entries, src, dst, symlinks, ignore, copy_function,
                  ignore_dangling_symlinks, dirs_exist_ok=False):
        if ignore is not None:
            ignored_names = ignore(os.fspath(src), [x.name for x in entries])
        else:
            ignored_names = ()
    
        os.makedirs(dst, exist_ok=dirs_exist_ok)
        errors = []
        use_srcentry = copy_function is copy2 or copy_function is copy
    
        for srcentry in entries:
            if srcentry.name in ignored_names:
                continue
            srcname = os.path.join(src, srcentry.name)
            dstname = os.path.join(dst, srcentry.name)
            srcobj = srcentry if use_srcentry else srcname
            try:
                is_symlink = srcentry.is_symlink()
                if is_symlink and os.name == 'nt':
                    # Special check for directory junctions, which appear as
                    # symlinks but we want to recurse.
                    lstat = srcentry.stat(follow_symlinks=False)
                    if lstat.st_reparse_tag == stat.IO_REPARSE_TAG_MOUNT_POINT:
                        is_symlink = False
                if is_symlink:
                    linkto = os.readlink(srcname)
                    if symlinks:
                        # We can't just leave it to `copy_function` because legacy
                        # code with a custom `copy_function` may rely on copytree
                        # doing the right thing.
                        os.symlink(linkto, dstname)
                        copystat(srcobj, dstname, follow_symlinks=not symlinks)
                    else:
                        # ignore dangling symlink if the flag is on
                        if not os.path.exists(linkto) and ignore_dangling_symlinks:
                            continue
                        # otherwise let the copy occur. copy2 will raise an error
                        if srcentry.is_dir():
                            copytree(srcobj, dstname, symlinks, ignore,
                                     copy_function, ignore_dangling_symlinks,
                                     dirs_exist_ok)
                        else:
                            copy_function(srcobj, dstname)
                elif srcentry.is_dir():
                    copytree(srcobj, dstname, symlinks, ignore, copy_function,
                             ignore_dangling_symlinks, dirs_exist_ok)
                else:
                    # Will raise a SpecialFileError for unsupported file types
                    copy_function(srcobj, dstname)
            # catch the Error from the recursive copytree so that we can
            # continue with other files
            except Error as err:
                errors.extend(err.args[0])
            except OSError as why:
                errors.append((srcname, dstname, str(why)))
        try:
            copystat(src, dst)
        except OSError as why:
            # Copying file access times may fail on Windows
            if getattr(why, 'winerror', None) is None:
                errors.append((src, dst, str(why)))
        if errors:
>           raise Error(errors)
E           shutil.Error: [('/Users/peteromalley/Documents/.megaplan-worktrees/runtime-convergence-execution/arnold_pipelines/megaplan/skills/babysit/SKILL.md', '/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_real_editable_launch_and_0/supervisor-venv/lib/python3.11/site-packages/arnold_pipelines/megaplan/skills/babysit/SKILL.md', "[Errno 2] No such file or directory: '/Users/peteromalley/Documents/.megaplan-worktrees/runtime-convergence-execution/arnold_pipelines/megaplan/skills/babysit/SKILL.md'")]

../../../.pyenv/versions/3.11.11/lib/python3.11/shutil.py:527: Error
----------------------------- Captured stderr call -----------------------------
huggingface/tokenizers: The current process just got forked, after parallelism has already been used. Disabling parallelism to avoid deadlocks...
To disable this warning, you can either:
	- Avoid using `tokenizers` before the fork if possible
	- Explicitly set the environment variable TOKENIZERS_PARALLELISM=(true | false)
huggingface/tokenizers: The current process just got forked, after parallelism has already been used. Disabling parallelism to avoid deadlocks...
To disable this warning, you can either:
	- Avoid using `tokenizers` before the fork if possible
	- Explicitly set the environment variable TOKENIZERS_PARALLELISM=(true | false)
huggingface/tokenizers: The current process just got forked, after parallelism has already been used. Disabling parallelism to avoid deadlocks...
To disable this warning, you can either:
	- Avoid using `tokenizers` before the fork if possible
	- Explicitly set the environment variable TOKENIZERS_PARALLELISM=(true | false)
_____________ test_ensure_runtime_launch_seed_refuses_drifted_head _____________

tmp_path = PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_ensure_runtime_launch_see2')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x17ab27a10>

    def test_ensure_runtime_launch_seed_refuses_drifted_head(
        tmp_path: Path,
        monkeypatch: pytest.MonkeyPatch,
    ) -> None:
        """G14 fail-closed: the live runtime HEAD MUST equal the manifest pin."""
        env = _ensure_seed_env(tmp_path, monkeypatch)
        assert isinstance(env["manifest"], Path)
        manifest = env["manifest"]
        paths = env["paths"]
        assert isinstance(paths, dict)
        state = env["state"]
        assert isinstance(state, dict)
        monkeypatch.setattr(attestation, "_git_revision", lambda _root: "e" * 40)
    
        with pytest.raises(CliError, match="HEAD does not match the manifest pin"):
>           attestation.ensure_runtime_launch_seed(
                manifest_path=manifest,
                chain_spec_path=paths["chain_spec"],
                marker_path=paths["marker"],
                chain_runtime_identity=env["identity"],
                seed_dir=env["seed_dir"],
                supervisor_receipt_path=paths["receipt"],
                hot_env_path=paths["hot_env"],
            )

tests/cloud/test_runtime_attestation.py:1130: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

    def ensure_runtime_launch_seed(
        *,
        manifest_path: Path,
        chain_spec_path: Path,
        marker_path: Path,
        chain_runtime_identity: Mapping[str, Any] | None = None,
        seed_dir: Path | None = None,
        supervisor_receipt_path: Path | None = None,
        hot_env_path: Path | None = None,
        expected_branch: str | None = None,
        expected_ancestry_base: str | None = None,
    ) -> Path:
        """Build or refresh the canonical runtime launch seed for one per-epic runtime.
    
        The per-session runtime manifest (``ARNOLD_RUNTIME_MANIFEST``) is the
        runtime selector (G4): ``epic.runtime_root`` and ``epic.expected_head``
        pin the seeded runtime, and the live checkout HEAD MUST equal the pin
        (else :class:`CliError`).  The marker's
        ``runtime_binding.current_identity`` must agree with the live provenance
        at the expected revision and with the chain execution binding; a stale
        marker is rebound through the CAS-protected marker/runtime cutover helper
        (never hand-edited).  The seed is rebuilt whenever it is missing, not
        release-ready, content-digest-invalid, or pinned to a different
        root/revision.  On success returns the seed path; the caller exports it
        as ``MEGAPLAN_RUNTIME_LAUNCH_SEED`` for every child worker/watchdog.
        """
        from arnold_pipelines.megaplan.cloud.runtime_manifest import (
            ManifestError,
            load_manifest,
        )
    
        try:
            manifest = load_manifest(manifest_path)
        except ManifestError as exc:
            raise CliError(
                RUNTIME_ATTESTATION_ERROR,
                f"runtime manifest {manifest_path} is invalid: {exc}",
            ) from exc
        epic = manifest.epic
        runtime_root = str(epic.get("runtime_root") or "").strip()
        expected_revision = str(epic.get("expected_head") or "").strip()
        if not runtime_root:
            raise CliError(
                RUNTIME_ATTESTATION_ERROR,
                "runtime manifest lacks nonempty epic.runtime_root",
            )
        root = Path(runtime_root).expanduser().resolve()
        live_head = _git_revision(root)
        if not live_head:
            raise CliError(
                RUNTIME_ATTESTATION_ERROR,
                "runtime root HEAD is unreadable",
            )
        # T4.3: expected_head is telemetry, not a launch pin. Same-import_root
        # commits after cutover must not require a HEAD-equality dance.
        if expected_revision and live_head != expected_revision:
            expected_revision = live_head
        elif not expected_revision:
            expected_revision = live_head
        live_identity = _live_runtime_identity(
            root=root,
            expected_revision=expected_revision,
        )
        if chain_runtime_identity is not None:
            from arnold_pipelines.megaplan.cloud.runtime_cutover import (
                normalize_runtime_identity,
            )
    
            chain_identity = normalize_runtime_identity(chain_runtime_identity)
            # Compare the launch-relevant identity (grok consult, d58701026410):
            # import_root + source_revision, resolved. The digest is a derived
            # view; root+rev are the facts the tree determines. Equal root+rev
            # with different diagnostic shapes (editable/pth/imports populated vs
            # None depending on which writer stored them) is the same runtime.
            # A manifest GENERATION ADVANCE on the same import_root is a normal
            # operator action and must be a NON-EVENT for the next worker launch:
            # the launch adopts the live manifest-pinned head (grok consult
            # 2026-08-18: "JUST RELAUNCH"). A different import_root or a
            # generation downgrade still fail closed.
            bound_identity = _adopt_or_refuse_launch_identity(
                chain_identity,
                live_identity,
                recorded_generation=_recorded_seed_generation(chain_spec_path),
                live_generation=int(manifest.generation),
            )
            if (
                str((bound_identity.get("source_revision") or ""))
                != str((chain_identity.get("source_revision") or ""))
            ):
                _persist_adopted_chain_runtime_identity(
                    chain_spec_path=chain_spec_path,
                    bound_identity=bound_identity,
                    reason="manifest_generation_adopt",
                )
        else:
            bound_identity = live_identity
        marker = _json_file(marker_path, label="cloud session marker")
        _rebind_marker_if_stale(
            marker_path,
            marker,
            live_identity=live_identity,
            source_branch=str(epic.get("branch") or ""),
        )
        # Codex fix 2026-08-17: content-address the accepted generation. The seed
        # lives at runtime-launch-seeds/<runtime-id>/<generation>-<head>-<sha>.json
        # and is written once (O_EXCL); a separate atomic dispatch-current.json
        # pointer selects the newest ready seed. Running workers retain the
        # absolute immutable path they were dispatched with.
        store_dir = ((seed_dir or _launch_seed_store_dir()) / manifest.runtime_id).resolve(
            strict=False
        )
        generation = int(manifest.generation)
        manifest_sha256 = _canonical_sha256(manifest.to_dict())
        dep_generation = manifest.epic.get("dependency_generation")
        dep_generation = (
            dict(dep_generation) if isinstance(dep_generation, Mapping) else None
        )
        existing = _find_current_seed(
            store_dir,
            root=root,
            expected_revision=expected_revision,
            marker_path=marker_path,
            manifest_path=manifest_path,
            generation=generation,
        )
        if existing is not None:
            return existing
        payload = build_runtime_launch_seed(
            expected_root=root,
            expected_revision=expected_revision,
            supervisor_receipt_path=supervisor_receipt_path
            or SUPERVISOR_RECEIPT_DEFAULT_PATH,
            hot_env_path=hot_env_path or CLOUD_HOT_ENV_DEFAULT_PATH,
            marker_path=marker_path,
            chain_spec_path=chain_spec_path,
            seed_doc_paths=(),
            expected_branch=expected_branch,
            expected_ancestry_base=expected_ancestry_base,
            manifest_path=manifest_path,
            chain_runtime_identity=bound_identity,
            manifest_generation=generation,
            manifest_sha256=manifest_sha256,
            dependency_generation=dep_generation,
        )
        if not bool(payload.get("ready")):
>           raise CliError(
                RUNTIME_ATTESTATION_ERROR,
                "runtime launch seed is not release-ready: "
                + ", ".join(str(item) for item in payload.get("errors") or []),
            )
E           arnold_pipelines.megaplan.types.CliError: runtime launch seed is not release-ready: chain_runtime_revision_mismatch, marker_runtime_revision_mismatch

arnold_pipelines/megaplan/cloud/runtime_attestation.py:1242: CliError

During handling of the above exception, another exception occurred:

tmp_path = PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_ensure_runtime_launch_see2')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x17ab27a10>

    def test_ensure_runtime_launch_seed_refuses_drifted_head(
        tmp_path: Path,
        monkeypatch: pytest.MonkeyPatch,
    ) -> None:
        """G14 fail-closed: the live runtime HEAD MUST equal the manifest pin."""
        env = _ensure_seed_env(tmp_path, monkeypatch)
        assert isinstance(env["manifest"], Path)
        manifest = env["manifest"]
        paths = env["paths"]
        assert isinstance(paths, dict)
        state = env["state"]
        assert isinstance(state, dict)
        monkeypatch.setattr(attestation, "_git_revision", lambda _root: "e" * 40)
    
>       with pytest.raises(CliError, match="HEAD does not match the manifest pin"):
E       AssertionError: Regex pattern did not match.
E         Expected regex: 'HEAD does not match the manifest pin'
E         Actual message: 'runtime launch seed is not release-ready: chain_runtime_revision_mismatch, marker_runtime_revision_mismatch'

tests/cloud/test_runtime_attestation.py:1129: AssertionError
----------------------------- Captured stderr call -----------------------------
huggingface/tokenizers: The current process just got forked, after parallelism has already been used. Disabling parallelism to avoid deadlocks...
To disable this warning, you can either:
	- Avoid using `tokenizers` before the fork if possible
	- Explicitly set the environment variable TOKENIZERS_PARALLELISM=(true | false)
huggingface/tokenizers: The current process just got forked, after parallelism has already been used. Disabling parallelism to avoid deadlocks...
To disable this warning, you can either:
	- Avoid using `tokenizers` before the fork if possible
	- Explicitly set the environment variable TOKENIZERS_PARALLELISM=(true | false)
huggingface/tokenizers: The current process just got forked, after parallelism has already been used. Disabling parallelism to avoid deadlocks...
To disable this warning, you can either:
	- Avoid using `tokenizers` before the fork if possible
	- Explicitly set the environment variable TOKENIZERS_PARALLELISM=(true | false)
huggingface/tokenizers: The current process just got forked, after parallelism has already been used. Disabling parallelism to avoid deadlocks...
To disable this warning, you can either:
	- Avoid using `tokenizers` before the fork if possible
	- Explicitly set the environment variable TOKENIZERS_PARALLELISM=(true | false)
huggingface/tokenizers: The current process just got forked, after parallelism has already been used. Disabling parallelism to avoid deadlocks...
To disable this warning, you can either:
	- Avoid using `tokenizers` before the fork if possible
	- Explicitly set the environment variable TOKENIZERS_PARALLELISM=(true | false)
huggingface/tokenizers: The current process just got forked, after parallelism has already been used. Disabling parallelism to avoid deadlocks...
To disable this warning, you can either:
	- Avoid using `tokenizers` before the fork if possible
	- Explicitly set the environment variable TOKENIZERS_PARALLELISM=(true | false)
huggingface/tokenizers: The current process just got forked, after parallelism has already been used. Disabling parallelism to avoid deadlocks...
To disable this warning, you can either:
	- Avoid using `tokenizers` before the fork if possible
	- Explicitly set the environment variable TOKENIZERS_PARALLELISM=(true | false)
huggingface/tokenizers: The current process just got forked, after parallelism has already been used. Disabling parallelism to avoid deadlocks...
To disable this warning, you can either:
	- Avoid using `tokenizers` before the fork if possible
	- Explicitly set the environment variable TOKENIZERS_PARALLELISM=(true | false)
huggingface/tokenizers: The current process just got forked, after parallelism has already been used. Disabling parallelism to avoid deadlocks...
To disable this warning, you can either:
	- Avoid using `tokenizers` before the fork if possible
	- Explicitly set the environment variable TOKENIZERS_PARALLELISM=(true | false)
__ test_attestation_disable_without_seed_does_not_authorize_production_launch __

monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x17c0d76d0>

    def test_attestation_disable_without_seed_does_not_authorize_production_launch(
        monkeypatch: pytest.MonkeyPatch,
    ) -> None:
        """P3 follow-up: explicit attestation-disable (MEGAPLAN_RUNTIME_ATTESTATION_REQUIRED=0)
        without a launch seed must NOT authorize a production launch — the gate
        still fails closed because the launch seed is mandatory.
        """
        monkeypatch.delenv("MEGAPLAN_RUNTIME_LAUNCH_SEED", raising=False)
        monkeypatch.delenv("MEGAPLAN_RUNTIME_PROCESS_ATTESTATION", raising=False)
        monkeypatch.setenv("MEGAPLAN_RUNTIME_ATTESTATION_REQUIRED", "0")
    
>       with pytest.raises(CliError, match="required but missing"):
E       Failed: DID NOT RAISE <class 'arnold_pipelines.megaplan.types.CliError'>

tests/cloud/test_runtime_attestation.py:1335: Failed
________ test_chain_runtime_cutover_composes_with_marker_runtime_update ________

tmp_path = PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_chain_runtime_cutover_com0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x17abf9490>

    def test_chain_runtime_cutover_composes_with_marker_runtime_update(
        tmp_path: Path,
        monkeypatch: pytest.MonkeyPatch,
    ) -> None:
        """T-0101c composition: after ``chain runtime-cutover`` moves the chain's
        engine_root, the marker-side ``update_marker_runtime`` accepts the SAME
        new identity with a relaunch command binding the new root — the chain's
        new runtime binding and the marker's runtime binding converge."""
        spec_path, state, old_runtime, new_runtime = _composition_chain_state(
            tmp_path, monkeypatch
        )
    
>       cutover = cutover_runtime_identity(
            spec_path,
            state,
            expected_previous_runtime_sha256=old_runtime["content_sha256"],
            expected_active_runtime_sha256=new_runtime["content_sha256"],
            expected_current_milestone="c1",
            expected_current_plan="c1-plan",
            reason="chain cutover then marker sync",
        )

tests/cloud/test_runtime_cutover.py:401: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
arnold_pipelines/megaplan/chain/execution_binding.py:2123: in cutover_runtime_identity
    return rebind_runtime_identity(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

spec_path = PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_chain_runtime_cutover_com0/chain.yaml')
state = namespace(metadata={'execution_binding': {'schema': 'arnold.megaplan.chain_execution_binding.v1', 'launched_identity':...': '/workspace/runtime-a'}}, current_milestone_index=0, current_plan_name='c1-plan', last_state='paused', completed=[])

    def rebind_runtime_identity(
        spec_path: Path,
        state: Any,
        *,
        expected_previous_runtime_sha256: str | None = None,
        expected_active_runtime_sha256: str | None = None,
        expected_previous_import_root: str | None = None,
        expected_previous_interpreter: str | None = None,
        expected_active_import_root: str | None = None,
        expected_active_interpreter: str | None = None,
        expected_current_milestone: str,
        expected_current_plan: str,
        reason: str,
        actor: str = "operator",
        direction: str = "cutover",
        verified_external_runtime_identity: Mapping[str, Any] | None = None,
        update_engine_root: bool = False,
        capability: object | None = None,
    ) -> dict[str, Any]:
        """Adopt or roll back an exact runtime without rewriting the spec binding.
    
        When ``update_engine_root`` is set (the ``chain runtime-cutover`` path),
        ``metadata.execution_environment.engine_root`` is moved old->new in the
        same in-memory transaction: the recorded root must CAS-match the previous
        runtime identity's ``import_root`` before ANY mutation, and the new root
        is taken from the adopted runtime identity's ``import_root``.  Non-metadata
        chain fields are untouched, so the CLI's post-mutation operational-field
        check keeps the write fail-closed.
        """
    
        if direction not in {"cutover", "rollback"}:
            raise CliError(
                RUNTIME_DRIFT_ERROR, "runtime rebind direction must be cutover or rollback"
            )
        sha_cas = bool(expected_previous_runtime_sha256 or expected_active_runtime_sha256)
        root_cas = bool(
            expected_previous_import_root
            or expected_previous_interpreter
            or expected_active_import_root
            or expected_active_interpreter
        )
        if sha_cas and not root_cas:
>           raise CliError(
                RUNTIME_DRIFT_ERROR,
                "runtime rebind CAS is import_root plus generation interpreter, not SHA-256",
            )
E           arnold_pipelines.megaplan.types.CliError: runtime rebind CAS is import_root plus generation interpreter, not SHA-256

arnold_pipelines/megaplan/chain/execution_binding.py:1720: CliError
_ test_non_codex_continuation_worker_resumes_exact_session[hermes-zhipu:glm-5.2-resident_0123456789abcdef0123456789abcdef---resume-session] _

tmp_path = PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_non_codex_continuation_wo0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x17c13ddd0>
caller_provenance = {'applicability': 'applicable', 'channel_id': None, 'conversation_key': 'discord:dm:42', 'correlation_id': 'discord-corr-f637319be9c04cbb4f7e4faf', ...}
backend = 'hermes', model = 'zhipu:glm-5.2'
session_id = 'resident_0123456789abcdef0123456789abcdef'
resume_flag = '--resume-session'

    @pytest.mark.parametrize(
        ("backend", "model", "session_id", "resume_flag"),
        [
            (
                "hermes",
                "zhipu:glm-5.2",
                "resident_0123456789abcdef0123456789abcdef",
                "--resume-session",
            ),
            (
                "claude",
                "opus",
                "019f5d2e-d5da-75f3-a617-4712a1c57cc5",
                "--resume",
            ),
        ],
    )
    def test_non_codex_continuation_worker_resumes_exact_session(
        tmp_path: Path,
        monkeypatch,
        caller_provenance: dict,
        backend: str,
        model: str,
        session_id: str,
        resume_flag: str,
    ) -> None:
        _write_run(
            tmp_path,
            backend=backend,
            model=model,
            session_id=session_id,
        )
        monkeypatch.setattr(subagent.subprocess, "Popen", lambda *a, **k: _Supervisor())
        result = subagent.follow_up_managed_subagent(
            run_id=TARGET_RUN_ID,
            message=f"Resume {backend} now.",
            project_dir=tmp_path,
            workspace_root=None,
        )
        captured: dict[str, object] = {}
    
        class _Provider:
            pid = 9877
    
            def wait(self, timeout=None):
                captured["timeout"] = timeout
                return 0
    
            def poll(self):
                return 0
    
        def fake_provider(argv, **kwargs):
            captured["argv"] = list(argv)
            captured["env"] = kwargs["env"]
            output = kwargs["stdout"]
            if backend == "hermes":
                output.write(b"CONTINUED\n")
                Path(argv[argv.index("--metadata-file") + 1]).write_text(
                    json.dumps(
                        {
                            "session_id": session_id,
                            "resolved_model": model,
                            "toolsets": ["file", "web", "terminal"],
                            "usage": {"output_tokens": 2},
                            "events": [],
                        }
                    )
                )
            else:
                output.write(
                    (
                        json.dumps(
                            {
                                "type": "system",
                                "subtype": "init",
                                "session_id": session_id,
                                "model": model,
                                "tools": ["Read", "Bash"],
                            }
                        )
                        + "\n"
                        + json.dumps(
                            {
                                "type": "result",
                                "subtype": "success",
                                "session_id": session_id,
                                "is_error": False,
                                "result": "CONTINUED",
                                "usage": {"output_tokens": 2},
                            }
                        )
                        + "\n"
                    ).encode()
                )
            output.flush()
            return _Provider()
    
        monkeypatch.setattr(subagent.subprocess, "Popen", fake_provider)
        child_path = Path(result.continuation_manifest_path)
        assert subagent._run_managed_manifest(child_path) == 0
    
        child = json.loads(child_path.read_text())
        argv = captured["argv"]
>       assert resume_flag in argv
E       AssertionError: assert '--resume-session' in ['python3', '/workspace/runtime-candidates/arnold-r7-fresh-child-20260805/arnold_pipelines/megaplan/skills/subagent-launcher/launch_hermes_agent.py', '--model', 'deepseek:deepseek-v4-flash', '--toolsets', 'file,web', ...]

tests/resident/test_subagent_followup.py:562: AssertionError
----------------------------- Captured stdout call -----------------------------

[managed-provider-raw begin backend=hermes path=/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_non_codex_continuation_wo0/.megaplan/plans/resident-subagents/subagent-20260822-030401-ab6a17e7/provider.raw]
CONTINUED

[managed-provider-raw end backend=hermes]
_ test_non_codex_continuation_worker_resumes_exact_session[claude-opus-019f5d2e-d5da-75f3-a617-4712a1c57cc5---resume] _

tmp_path = PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_non_codex_continuation_wo1')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x17a851b90>
caller_provenance = {'applicability': 'applicable', 'channel_id': None, 'conversation_key': 'discord:dm:42', 'correlation_id': 'discord-corr-f637319be9c04cbb4f7e4faf', ...}
backend = 'claude', model = 'opus'
session_id = '019f5d2e-d5da-75f3-a617-4712a1c57cc5', resume_flag = '--resume'

    @pytest.mark.parametrize(
        ("backend", "model", "session_id", "resume_flag"),
        [
            (
                "hermes",
                "zhipu:glm-5.2",
                "resident_0123456789abcdef0123456789abcdef",
                "--resume-session",
            ),
            (
                "claude",
                "opus",
                "019f5d2e-d5da-75f3-a617-4712a1c57cc5",
                "--resume",
            ),
        ],
    )
    def test_non_codex_continuation_worker_resumes_exact_session(
        tmp_path: Path,
        monkeypatch,
        caller_provenance: dict,
        backend: str,
        model: str,
        session_id: str,
        resume_flag: str,
    ) -> None:
        _write_run(
            tmp_path,
            backend=backend,
            model=model,
            session_id=session_id,
        )
        monkeypatch.setattr(subagent.subprocess, "Popen", lambda *a, **k: _Supervisor())
        result = subagent.follow_up_managed_subagent(
            run_id=TARGET_RUN_ID,
            message=f"Resume {backend} now.",
            project_dir=tmp_path,
            workspace_root=None,
        )
        captured: dict[str, object] = {}
    
        class _Provider:
            pid = 9877
    
            def wait(self, timeout=None):
                captured["timeout"] = timeout
                return 0
    
            def poll(self):
                return 0
    
        def fake_provider(argv, **kwargs):
            captured["argv"] = list(argv)
            captured["env"] = kwargs["env"]
            output = kwargs["stdout"]
            if backend == "hermes":
                output.write(b"CONTINUED\n")
                Path(argv[argv.index("--metadata-file") + 1]).write_text(
                    json.dumps(
                        {
                            "session_id": session_id,
                            "resolved_model": model,
                            "toolsets": ["file", "web", "terminal"],
                            "usage": {"output_tokens": 2},
                            "events": [],
                        }
                    )
                )
            else:
                output.write(
                    (
                        json.dumps(
                            {
                                "type": "system",
                                "subtype": "init",
                                "session_id": session_id,
                                "model": model,
                                "tools": ["Read", "Bash"],
                            }
                        )
                        + "\n"
                        + json.dumps(
                            {
                                "type": "result",
                                "subtype": "success",
                                "session_id": session_id,
                                "is_error": False,
                                "result": "CONTINUED",
                                "usage": {"output_tokens": 2},
                            }
                        )
                        + "\n"
                    ).encode()
                )
            output.flush()
            return _Provider()
    
        monkeypatch.setattr(subagent.subprocess, "Popen", fake_provider)
        child_path = Path(result.continuation_manifest_path)
        assert subagent._run_managed_manifest(child_path) == 0
    
        child = json.loads(child_path.read_text())
        argv = captured["argv"]
>       assert resume_flag in argv
E       AssertionError: assert '--resume' in ['python3', '/workspace/runtime-candidates/arnold-r7-fresh-child-20260805/arnold_pipelines/megaplan/skills/subagent-launcher/launch_hermes_agent.py', '--model', 'deepseek:deepseek-v4-flash', '--toolsets', 'file,web', ...]

tests/resident/test_subagent_followup.py:562: AssertionError
----------------------------- Captured stdout call -----------------------------

[managed-provider-raw begin backend=claude path=/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_non_codex_continuation_wo1/.megaplan/plans/resident-subagents/subagent-20260822-030401-d9b1ad8b/provider.raw]
{"type": "system", "subtype": "init", "session_id": "019f5d2e-d5da-75f3-a617-4712a1c57cc5", "model": "opus", "tools": ["Read", "Bash"]}
{"type": "result", "subtype": "success", "session_id": "019f5d2e-d5da-75f3-a617-4712a1c57cc5", "is_error": false, "result": "CONTINUED", "usage": {"output_tokens": 2}}

[managed-provider-raw end backend=claude]
_ test_continuation_worker_resumes_exact_parent_session_and_records_acceptance _

tmp_path = PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_continuation_worker_resum0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x17a74d010>
caller_provenance = {'applicability': 'applicable', 'channel_id': None, 'conversation_key': 'discord:dm:42', 'correlation_id': 'discord-corr-f637319be9c04cbb4f7e4faf', ...}

    def test_continuation_worker_resumes_exact_parent_session_and_records_acceptance(
        tmp_path: Path, monkeypatch, caller_provenance: dict
    ) -> None:
        _write_run(tmp_path)
        monkeypatch.setattr(subagent.subprocess, "Popen", lambda *a, **k: _Supervisor())
        result = subagent.follow_up_managed_subagent(
            run_id=TARGET_RUN_ID,
            message="Use both summaries.",
            project_dir=tmp_path,
            workspace_root=None,
        )
        captured: dict[str, object] = {}
    
        class _Codex:
            pid = 9876
    
            def wait(self, timeout=None):
                return 0
    
            def poll(self):
                return 0
    
        def fake_codex(argv, **kwargs):
            captured["argv"] = list(argv)
            captured["env"] = kwargs["env"]
            return _Codex()
    
        monkeypatch.setattr(subagent.subprocess, "Popen", fake_codex)
        child_path = Path(result.continuation_manifest_path)
        assert subagent._run_codex_manifest(child_path) == 0
    
        child = json.loads(child_path.read_text())
        argv = captured["argv"]
>       assert argv[:3] == ["codex", "exec", "resume"]
E       AssertionError: assert ['python3', '...y', '--model'] == ['codex', 'exec', 'resume']
E         
E         At index 0 diff: 'python3' != 'codex'
E         
E         Full diff:
E           [
E         +     'python3',
E         +     '/workspace/runtime-candidates/arnold-r7-fresh-child-20260805/arnold_pipelines/megaplan/skills/subagent-launcher/launch_hermes_agent.py',
E         -     'codex',
E         ?      ^   ^
E         +     '--model',
E         ?      ^^^   ^
E         -     'exec',
E         -     'resume',
E           ]

tests/resident/test_subagent_followup.py:750: AssertionError
----------------------------- Captured stdout call -----------------------------

[managed-provider-raw begin backend=codex path=/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_continuation_worker_resum0/.megaplan/plans/resident-subagents/subagent-20260822-030401-47e73027/provider.raw]

[managed-provider-raw end backend=codex]
______________ test_release_evidence_record_is_structurally_valid ______________

    def test_release_evidence_record_is_structurally_valid() -> None:
>       validate(RECORD)

tests/test_post_m11_release_evidence.py:44: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

path = PosixPath('/Users/peteromalley/Documents/.megaplan-worktrees/runtime-convergence-execution/docs/megaplan/post-m11-release-evidence-20260731.json')

    def validate(path: Path) -> None:
        repo = Path(_git("rev-parse", "--show-toplevel", cwd=REPO_ROOT))
        data = json.loads(path.read_text(encoding="utf-8"))
    
        digest_path = path.with_name(path.name + ".sha256")
        if digest_path.is_file():
            expected_digest, expected_name = digest_path.read_text(
                encoding="utf-8"
            ).split()
            actual_digest = hashlib.sha256(path.read_bytes()).hexdigest()
            if expected_name != path.name or expected_digest != actual_digest:
                raise ValueError("release evidence SHA-256 sidecar mismatch")
    
        if data.get("schema") != "arnold.post_m11_release_evidence.v1":
            raise ValueError("unsupported schema")
        if data.get("record_status") not in ALLOWED_RECORD_STATUS:
            raise ValueError("invalid record_status")
        _validate_sha256_fields(data)
    
        authority = data["authority"]
        plan_path = repo / authority["plan_path"]
        if not plan_path.is_file():
            raise ValueError(f"missing plan path: {authority['plan_path']}")
    
        git_objects: set[str] = set()
        for field in (
            "plan_publication_commit",
            "origin_base_commit",
            "evidence_cut_commit",
            "evidence_cut_tree",
        ):
            git_objects.add(_require_sha1(authority[field], f"authority.{field}"))
    
        plan_blob = _require_sha1(authority["plan_blob"], "authority.plan_blob")
        actual_plan_blob = _git("hash-object", str(plan_path), cwd=repo)
        if actual_plan_blob != plan_blob:
            raise ValueError(
                f"plan blob mismatch: expected {plan_blob}, got {actual_plan_blob}"
            )
        git_objects.add(plan_blob)
        actual_evidence_tree = _git(
            "rev-parse", f"{authority['evidence_cut_commit']}^{{tree}}", cwd=repo
        )
        if actual_evidence_tree != authority["evidence_cut_tree"]:
            raise ValueError(
                "evidence cut tree mismatch: expected "
                f"{authority['evidence_cut_tree']}, got {actual_evidence_tree}"
            )
    
        source_refs = data.get("source_refs")
        if not isinstance(source_refs, list) or not source_refs:
            raise ValueError("source_refs must be a non-empty list")
        for index, item in enumerate(source_refs):
            git_objects.add(_require_sha1(item["sha"], f"source_refs[{index}].sha"))
            if item.get("classification") not in {"LAND", "KEEP_CHECKPOINT"}:
                raise ValueError(f"source_refs[{index}] has invalid classification")
    
        lineage = data.get("integration_lineage")
        if not isinstance(lineage, list) or not lineage:
            raise ValueError("integration_lineage must be a non-empty list")
        for index, item in enumerate(lineage):
            git_objects.add(
                _require_sha1(item["sha"], f"integration_lineage[{index}].sha")
            )
    
        for index, item in enumerate(data.get("validation_observations", [])):
            status = item.get("status")
            acceptance_effect = item.get("acceptance_effect")
            if status not in ALLOWED_OBSERVATION_STATUS:
                raise ValueError(
                    f"validation_observations[{index}] has invalid status"
                )
            if acceptance_effect not in ALLOWED_ACCEPTANCE_EFFECT:
                raise ValueError(
                    f"validation_observations[{index}] has invalid acceptance_effect"
                )
            if (
                status in NON_ACCEPTANCE_OBSERVATION_STATUS
                and acceptance_effect not in NON_ACCEPTANCE_EFFECT
            ):
                raise ValueError(
                    f"validation_observations[{index}] is a failure, superseded, "
                    "or discovery observation and cannot provide acceptance"
                )
            if "bound_commit" in item:
                git_objects.add(
                    _require_sha1(
                        item["bound_commit"],
                        f"validation_observations[{index}].bound_commit",
                    )
                )
            if "bound_tree" in item:
                git_objects.add(
                    _require_sha1(
                        item["bound_tree"],
                        f"validation_observations[{index}].bound_tree",
                    )
                )
    
        for collection_name in (
            "historical_superseded_attempts",
            "packaging_artifacts",
        ):
            for index, item in enumerate(data.get(collection_name, [])):
                if collection_name == "historical_superseded_attempts":
                    if item.get("status") not in ALLOWED_SUPERSEDED_STATUS:
                        raise ValueError(
                            f"{collection_name}[{index}] has invalid status"
                        )
                    if item.get("acceptance_effect") != "evidence_not_acceptance":
                        raise ValueError(
                            f"{collection_name}[{index}] cannot provide acceptance"
                        )
                elif (
                    item.get("acceptance_effect")
                    not in ALLOWED_PACKAGING_ACCEPTANCE_EFFECT
                ):
                    raise ValueError(
                        f"{collection_name}[{index}] has invalid acceptance_effect"
                    )
                if "bound_commit" in item:
                    git_objects.add(
                        _require_sha1(
                            item["bound_commit"],
                            f"{collection_name}[{index}].bound_commit",
                        )
                    )
    
        for index, item in enumerate(data.get("canonical_artifact_moves", [])):
            source = repo / item["from"]
            destination = repo / item["to"]
            if source.exists():
>               raise ValueError(
                    f"canonical_artifact_moves[{index}].from still exists: {item['from']}"
                )
E               ValueError: canonical_artifact_moves[0].from still exists: .megaplan/initiatives/megaplan-native-parity-corrective/GOLDEN_TRACE_CONTRACT.md

scripts/validate_post_m11_release_evidence.py:527: ValueError
----------------------------- Captured stderr call -----------------------------
huggingface/tokenizers: The current process just got forked, after parallelism has already been used. Disabling parallelism to avoid deadlocks...
To disable this warning, you can either:
	- Avoid using `tokenizers` before the fork if possible
	- Explicitly set the environment variable TOKENIZERS_PARALLELISM=(true | false)
huggingface/tokenizers: The current process just got forked, after parallelism has already been used. Disabling parallelism to avoid deadlocks...
To disable this warning, you can either:
	- Avoid using `tokenizers` before the fork if possible
	- Explicitly set the environment variable TOKENIZERS_PARALLELISM=(true | false)
huggingface/tokenizers: The current process just got forked, after parallelism has already been used. Disabling parallelism to avoid deadlocks...
To disable this warning, you can either:
	- Avoid using `tokenizers` before the fork if possible
	- Explicitly set the environment variable TOKENIZERS_PARALLELISM=(true | false)
__________ test_release_evidence_cannot_claim_done_with_pending_gates __________

tmp_path = PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_release_evidence_cannot_c0')

    def test_release_evidence_cannot_claim_done_with_pending_gates(
        tmp_path: Path,
    ) -> None:
        data = json.loads(RECORD.read_text(encoding="utf-8"))
        data["record_status"] = "complete"
        candidate = tmp_path / "release-evidence.json"
        candidate.write_text(json.dumps(data), encoding="utf-8")
    
        with pytest.raises(ValueError, match="residuals are pending"):
>           validate(candidate)

tests/test_post_m11_release_evidence.py:85: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

path = PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_release_evidence_cannot_c0/release-evidence.json')

    def validate(path: Path) -> None:
        repo = Path(_git("rev-parse", "--show-toplevel", cwd=REPO_ROOT))
        data = json.loads(path.read_text(encoding="utf-8"))
    
        digest_path = path.with_name(path.name + ".sha256")
        if digest_path.is_file():
            expected_digest, expected_name = digest_path.read_text(
                encoding="utf-8"
            ).split()
            actual_digest = hashlib.sha256(path.read_bytes()).hexdigest()
            if expected_name != path.name or expected_digest != actual_digest:
                raise ValueError("release evidence SHA-256 sidecar mismatch")
    
        if data.get("schema") != "arnold.post_m11_release_evidence.v1":
            raise ValueError("unsupported schema")
        if data.get("record_status") not in ALLOWED_RECORD_STATUS:
            raise ValueError("invalid record_status")
        _validate_sha256_fields(data)
    
        authority = data["authority"]
        plan_path = repo / authority["plan_path"]
        if not plan_path.is_file():
            raise ValueError(f"missing plan path: {authority['plan_path']}")
    
        git_objects: set[str] = set()
        for field in (
            "plan_publication_commit",
            "origin_base_commit",
            "evidence_cut_commit",
            "evidence_cut_tree",
        ):
            git_objects.add(_require_sha1(authority[field], f"authority.{field}"))
    
        plan_blob = _require_sha1(authority["plan_blob"], "authority.plan_blob")
        actual_plan_blob = _git("hash-object", str(plan_path), cwd=repo)
        if actual_plan_blob != plan_blob:
            raise ValueError(
                f"plan blob mismatch: expected {plan_blob}, got {actual_plan_blob}"
            )
        git_objects.add(plan_blob)
        actual_evidence_tree = _git(
            "rev-parse", f"{authority['evidence_cut_commit']}^{{tree}}", cwd=repo
        )
        if actual_evidence_tree != authority["evidence_cut_tree"]:
            raise ValueError(
                "evidence cut tree mismatch: expected "
                f"{authority['evidence_cut_tree']}, got {actual_evidence_tree}"
            )
    
        source_refs = data.get("source_refs")
        if not isinstance(source_refs, list) or not source_refs:
            raise ValueError("source_refs must be a non-empty list")
        for index, item in enumerate(source_refs):
            git_objects.add(_require_sha1(item["sha"], f"source_refs[{index}].sha"))
            if item.get("classification") not in {"LAND", "KEEP_CHECKPOINT"}:
                raise ValueError(f"source_refs[{index}] has invalid classification")
    
        lineage = data.get("integration_lineage")
        if not isinstance(lineage, list) or not lineage:
            raise ValueError("integration_lineage must be a non-empty list")
        for index, item in enumerate(lineage):
            git_objects.add(
                _require_sha1(item["sha"], f"integration_lineage[{index}].sha")
            )
    
        for index, item in enumerate(data.get("validation_observations", [])):
            status = item.get("status")
            acceptance_effect = item.get("acceptance_effect")
            if status not in ALLOWED_OBSERVATION_STATUS:
                raise ValueError(
                    f"validation_observations[{index}] has invalid status"
                )
            if acceptance_effect not in ALLOWED_ACCEPTANCE_EFFECT:
                raise ValueError(
                    f"validation_observations[{index}] has invalid acceptance_effect"
                )
            if (
                status in NON_ACCEPTANCE_OBSERVATION_STATUS
                and acceptance_effect not in NON_ACCEPTANCE_EFFECT
            ):
                raise ValueError(
                    f"validation_observations[{index}] is a failure, superseded, "
                    "or discovery observation and cannot provide acceptance"
                )
            if "bound_commit" in item:
                git_objects.add(
                    _require_sha1(
                        item["bound_commit"],
                        f"validation_observations[{index}].bound_commit",
                    )
                )
            if "bound_tree" in item:
                git_objects.add(
                    _require_sha1(
                        item["bound_tree"],
                        f"validation_observations[{index}].bound_tree",
                    )
                )
    
        for collection_name in (
            "historical_superseded_attempts",
            "packaging_artifacts",
        ):
            for index, item in enumerate(data.get(collection_name, [])):
                if collection_name == "historical_superseded_attempts":
                    if item.get("status") not in ALLOWED_SUPERSEDED_STATUS:
                        raise ValueError(
                            f"{collection_name}[{index}] has invalid status"
                        )
                    if item.get("acceptance_effect") != "evidence_not_acceptance":
                        raise ValueError(
                            f"{collection_name}[{index}] cannot provide acceptance"
                        )
                elif (
                    item.get("acceptance_effect")
                    not in ALLOWED_PACKAGING_ACCEPTANCE_EFFECT
                ):
                    raise ValueError(
                        f"{collection_name}[{index}] has invalid acceptance_effect"
                    )
                if "bound_commit" in item:
                    git_objects.add(
                        _require_sha1(
                            item["bound_commit"],
                            f"{collection_name}[{index}].bound_commit",
                        )
                    )
    
        for index, item in enumerate(data.get("canonical_artifact_moves", [])):
            source = repo / item["from"]
            destination = repo / item["to"]
            if source.exists():
>               raise ValueError(
                    f"canonical_artifact_moves[{index}].from still exists: {item['from']}"
                )
E               ValueError: canonical_artifact_moves[0].from still exists: .megaplan/initiatives/megaplan-native-parity-corrective/GOLDEN_TRACE_CONTRACT.md

scripts/validate_post_m11_release_evidence.py:527: ValueError

During handling of the above exception, another exception occurred:

tmp_path = PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_release_evidence_cannot_c0')

    def test_release_evidence_cannot_claim_done_with_pending_gates(
        tmp_path: Path,
    ) -> None:
        data = json.loads(RECORD.read_text(encoding="utf-8"))
        data["record_status"] = "complete"
        candidate = tmp_path / "release-evidence.json"
        candidate.write_text(json.dumps(data), encoding="utf-8")
    
>       with pytest.raises(ValueError, match="residuals are pending"):
E       AssertionError: Regex pattern did not match.
E         Expected regex: 'residuals are pending'
E         Actual message: 'canonical_artifact_moves[0].from still exists: .megaplan/initiatives/megaplan-native-parity-corrective/GOLDEN_TRACE_CONTRACT.md'

tests/test_post_m11_release_evidence.py:84: AssertionError
----------------------------- Captured stderr call -----------------------------
huggingface/tokenizers: The current process just got forked, after parallelism has already been used. Disabling parallelism to avoid deadlocks...
To disable this warning, you can either:
	- Avoid using `tokenizers` before the fork if possible
	- Explicitly set the environment variable TOKENIZERS_PARALLELISM=(true | false)
huggingface/tokenizers: The current process just got forked, after parallelism has already been used. Disabling parallelism to avoid deadlocks...
To disable this warning, you can either:
	- Avoid using `tokenizers` before the fork if possible
	- Explicitly set the environment variable TOKENIZERS_PARALLELISM=(true | false)
huggingface/tokenizers: The current process just got forked, after parallelism has already been used. Disabling parallelism to avoid deadlocks...
To disable this warning, you can either:
	- Avoid using `tokenizers` before the fork if possible
	- Explicitly set the environment variable TOKENIZERS_PARALLELISM=(true | false)
________ test_candidate_ready_allows_only_post_deploy_residuals_pending ________

tmp_path = PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_candidate_ready_allows_on0')

    def test_candidate_ready_allows_only_post_deploy_residuals_pending(
        tmp_path: Path,
    ) -> None:
>       validate(_write(tmp_path, _candidate_ready_record()))

tests/test_post_m11_release_evidence.py:181: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

path = PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_candidate_ready_allows_on0/release-evidence.json')

    def validate(path: Path) -> None:
        repo = Path(_git("rev-parse", "--show-toplevel", cwd=REPO_ROOT))
        data = json.loads(path.read_text(encoding="utf-8"))
    
        digest_path = path.with_name(path.name + ".sha256")
        if digest_path.is_file():
            expected_digest, expected_name = digest_path.read_text(
                encoding="utf-8"
            ).split()
            actual_digest = hashlib.sha256(path.read_bytes()).hexdigest()
            if expected_name != path.name or expected_digest != actual_digest:
                raise ValueError("release evidence SHA-256 sidecar mismatch")
    
        if data.get("schema") != "arnold.post_m11_release_evidence.v1":
            raise ValueError("unsupported schema")
        if data.get("record_status") not in ALLOWED_RECORD_STATUS:
            raise ValueError("invalid record_status")
        _validate_sha256_fields(data)
    
        authority = data["authority"]
        plan_path = repo / authority["plan_path"]
        if not plan_path.is_file():
            raise ValueError(f"missing plan path: {authority['plan_path']}")
    
        git_objects: set[str] = set()
        for field in (
            "plan_publication_commit",
            "origin_base_commit",
            "evidence_cut_commit",
            "evidence_cut_tree",
        ):
            git_objects.add(_require_sha1(authority[field], f"authority.{field}"))
    
        plan_blob = _require_sha1(authority["plan_blob"], "authority.plan_blob")
        actual_plan_blob = _git("hash-object", str(plan_path), cwd=repo)
        if actual_plan_blob != plan_blob:
            raise ValueError(
                f"plan blob mismatch: expected {plan_blob}, got {actual_plan_blob}"
            )
        git_objects.add(plan_blob)
        actual_evidence_tree = _git(
            "rev-parse", f"{authority['evidence_cut_commit']}^{{tree}}", cwd=repo
        )
        if actual_evidence_tree != authority["evidence_cut_tree"]:
            raise ValueError(
                "evidence cut tree mismatch: expected "
                f"{authority['evidence_cut_tree']}, got {actual_evidence_tree}"
            )
    
        source_refs = data.get("source_refs")
        if not isinstance(source_refs, list) or not source_refs:
            raise ValueError("source_refs must be a non-empty list")
        for index, item in enumerate(source_refs):
            git_objects.add(_require_sha1(item["sha"], f"source_refs[{index}].sha"))
            if item.get("classification") not in {"LAND", "KEEP_CHECKPOINT"}:
                raise ValueError(f"source_refs[{index}] has invalid classification")
    
        lineage = data.get("integration_lineage")
        if not isinstance(lineage, list) or not lineage:
            raise ValueError("integration_lineage must be a non-empty list")
        for index, item in enumerate(lineage):
            git_objects.add(
                _require_sha1(item["sha"], f"integration_lineage[{index}].sha")
            )
    
        for index, item in enumerate(data.get("validation_observations", [])):
            status = item.get("status")
            acceptance_effect = item.get("acceptance_effect")
            if status not in ALLOWED_OBSERVATION_STATUS:
                raise ValueError(
                    f"validation_observations[{index}] has invalid status"
                )
            if acceptance_effect not in ALLOWED_ACCEPTANCE_EFFECT:
                raise ValueError(
                    f"validation_observations[{index}] has invalid acceptance_effect"
                )
            if (
                status in NON_ACCEPTANCE_OBSERVATION_STATUS
                and acceptance_effect not in NON_ACCEPTANCE_EFFECT
            ):
                raise ValueError(
                    f"validation_observations[{index}] is a failure, superseded, "
                    "or discovery observation and cannot provide acceptance"
                )
            if "bound_commit" in item:
                git_objects.add(
                    _require_sha1(
                        item["bound_commit"],
                        f"validation_observations[{index}].bound_commit",
                    )
                )
            if "bound_tree" in item:
                git_objects.add(
                    _require_sha1(
                        item["bound_tree"],
                        f"validation_observations[{index}].bound_tree",
                    )
                )
    
        for collection_name in (
            "historical_superseded_attempts",
            "packaging_artifacts",
        ):
            for index, item in enumerate(data.get(collection_name, [])):
                if collection_name == "historical_superseded_attempts":
                    if item.get("status") not in ALLOWED_SUPERSEDED_STATUS:
                        raise ValueError(
                            f"{collection_name}[{index}] has invalid status"
                        )
                    if item.get("acceptance_effect") != "evidence_not_acceptance":
                        raise ValueError(
                            f"{collection_name}[{index}] cannot provide acceptance"
                        )
                elif (
                    item.get("acceptance_effect")
                    not in ALLOWED_PACKAGING_ACCEPTANCE_EFFECT
                ):
                    raise ValueError(
                        f"{collection_name}[{index}] has invalid acceptance_effect"
                    )
                if "bound_commit" in item:
                    git_objects.add(
                        _require_sha1(
                            item["bound_commit"],
                            f"{collection_name}[{index}].bound_commit",
                        )
                    )
    
        for index, item in enumerate(data.get("canonical_artifact_moves", [])):
            source = repo / item["from"]
            destination = repo / item["to"]
            if source.exists():
>               raise ValueError(
                    f"canonical_artifact_moves[{index}].from still exists: {item['from']}"
                )
E               ValueError: canonical_artifact_moves[0].from still exists: .megaplan/initiatives/megaplan-native-parity-corrective/GOLDEN_TRACE_CONTRACT.md

scripts/validate_post_m11_release_evidence.py:527: ValueError
----------------------------- Captured stderr call -----------------------------
huggingface/tokenizers: The current process just got forked, after parallelism has already been used. Disabling parallelism to avoid deadlocks...
To disable this warning, you can either:
	- Avoid using `tokenizers` before the fork if possible
	- Explicitly set the environment variable TOKENIZERS_PARALLELISM=(true | false)
huggingface/tokenizers: The current process just got forked, after parallelism has already been used. Disabling parallelism to avoid deadlocks...
To disable this warning, you can either:
	- Avoid using `tokenizers` before the fork if possible
	- Explicitly set the environment variable TOKENIZERS_PARALLELISM=(true | false)
huggingface/tokenizers: The current process just got forked, after parallelism has already been used. Disabling parallelism to avoid deadlocks...
To disable this warning, you can either:
	- Avoid using `tokenizers` before the fork if possible
	- Explicitly set the environment variable TOKENIZERS_PARALLELISM=(true | false)
__________ test_candidate_ready_rejects_a_pending_pre_deploy_residual __________

tmp_path = PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_candidate_ready_rejects_a0')

    def test_candidate_ready_rejects_a_pending_pre_deploy_residual(
        tmp_path: Path,
    ) -> None:
        data = _candidate_ready_record()
        residual = next(
            item
            for item in data["residuals"]
            if item["id"] == "content-addressed-runtime"
        )
        residual["status"] = "pending"
        residual.pop("completion_evidence", None)
        residual.pop("completion_receipt", None)
    
        with pytest.raises(ValueError, match="pre-deploy residuals are pending"):
>           validate(_write(tmp_path, data))

tests/test_post_m11_release_evidence.py:198: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

path = PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_candidate_ready_rejects_a0/release-evidence.json')

    def validate(path: Path) -> None:
        repo = Path(_git("rev-parse", "--show-toplevel", cwd=REPO_ROOT))
        data = json.loads(path.read_text(encoding="utf-8"))
    
        digest_path = path.with_name(path.name + ".sha256")
        if digest_path.is_file():
            expected_digest, expected_name = digest_path.read_text(
                encoding="utf-8"
            ).split()
            actual_digest = hashlib.sha256(path.read_bytes()).hexdigest()
            if expected_name != path.name or expected_digest != actual_digest:
                raise ValueError("release evidence SHA-256 sidecar mismatch")
    
        if data.get("schema") != "arnold.post_m11_release_evidence.v1":
            raise ValueError("unsupported schema")
        if data.get("record_status") not in ALLOWED_RECORD_STATUS:
            raise ValueError("invalid record_status")
        _validate_sha256_fields(data)
    
        authority = data["authority"]
        plan_path = repo / authority["plan_path"]
        if not plan_path.is_file():
            raise ValueError(f"missing plan path: {authority['plan_path']}")
    
        git_objects: set[str] = set()
        for field in (
            "plan_publication_commit",
            "origin_base_commit",
            "evidence_cut_commit",
            "evidence_cut_tree",
        ):
            git_objects.add(_require_sha1(authority[field], f"authority.{field}"))
    
        plan_blob = _require_sha1(authority["plan_blob"], "authority.plan_blob")
        actual_plan_blob = _git("hash-object", str(plan_path), cwd=repo)
        if actual_plan_blob != plan_blob:
            raise ValueError(
                f"plan blob mismatch: expected {plan_blob}, got {actual_plan_blob}"
            )
        git_objects.add(plan_blob)
        actual_evidence_tree = _git(
            "rev-parse", f"{authority['evidence_cut_commit']}^{{tree}}", cwd=repo
        )
        if actual_evidence_tree != authority["evidence_cut_tree"]:
            raise ValueError(
                "evidence cut tree mismatch: expected "
                f"{authority['evidence_cut_tree']}, got {actual_evidence_tree}"
            )
    
        source_refs = data.get("source_refs")
        if not isinstance(source_refs, list) or not source_refs:
            raise ValueError("source_refs must be a non-empty list")
        for index, item in enumerate(source_refs):
            git_objects.add(_require_sha1(item["sha"], f"source_refs[{index}].sha"))
            if item.get("classification") not in {"LAND", "KEEP_CHECKPOINT"}:
                raise ValueError(f"source_refs[{index}] has invalid classification")
    
        lineage = data.get("integration_lineage")
        if not isinstance(lineage, list) or not lineage:
            raise ValueError("integration_lineage must be a non-empty list")
        for index, item in enumerate(lineage):
            git_objects.add(
                _require_sha1(item["sha"], f"integration_lineage[{index}].sha")
            )
    
        for index, item in enumerate(data.get("validation_observations", [])):
            status = item.get("status")
            acceptance_effect = item.get("acceptance_effect")
            if status not in ALLOWED_OBSERVATION_STATUS:
                raise ValueError(
                    f"validation_observations[{index}] has invalid status"
                )
            if acceptance_effect not in ALLOWED_ACCEPTANCE_EFFECT:
                raise ValueError(
                    f"validation_observations[{index}] has invalid acceptance_effect"
                )
            if (
                status in NON_ACCEPTANCE_OBSERVATION_STATUS
                and acceptance_effect not in NON_ACCEPTANCE_EFFECT
            ):
                raise ValueError(
                    f"validation_observations[{index}] is a failure, superseded, "
                    "or discovery observation and cannot provide acceptance"
                )
            if "bound_commit" in item:
                git_objects.add(
                    _require_sha1(
                        item["bound_commit"],
                        f"validation_observations[{index}].bound_commit",
                    )
                )
            if "bound_tree" in item:
                git_objects.add(
                    _require_sha1(
                        item["bound_tree"],
                        f"validation_observations[{index}].bound_tree",
                    )
                )
    
        for collection_name in (
            "historical_superseded_attempts",
            "packaging_artifacts",
        ):
            for index, item in enumerate(data.get(collection_name, [])):
                if collection_name == "historical_superseded_attempts":
                    if item.get("status") not in ALLOWED_SUPERSEDED_STATUS:
                        raise ValueError(
                            f"{collection_name}[{index}] has invalid status"
                        )
                    if item.get("acceptance_effect") != "evidence_not_acceptance":
                        raise ValueError(
                            f"{collection_name}[{index}] cannot provide acceptance"
                        )
                elif (
                    item.get("acceptance_effect")
                    not in ALLOWED_PACKAGING_ACCEPTANCE_EFFECT
                ):
                    raise ValueError(
                        f"{collection_name}[{index}] has invalid acceptance_effect"
                    )
                if "bound_commit" in item:
                    git_objects.add(
                        _require_sha1(
                            item["bound_commit"],
                            f"{collection_name}[{index}].bound_commit",
                        )
                    )
    
        for index, item in enumerate(data.get("canonical_artifact_moves", [])):
            source = repo / item["from"]
            destination = repo / item["to"]
            if source.exists():
>               raise ValueError(
                    f"canonical_artifact_moves[{index}].from still exists: {item['from']}"
                )
E               ValueError: canonical_artifact_moves[0].from still exists: .megaplan/initiatives/megaplan-native-parity-corrective/GOLDEN_TRACE_CONTRACT.md

scripts/validate_post_m11_release_evidence.py:527: ValueError

During handling of the above exception, another exception occurred:

tmp_path = PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_candidate_ready_rejects_a0')

    def test_candidate_ready_rejects_a_pending_pre_deploy_residual(
        tmp_path: Path,
    ) -> None:
        data = _candidate_ready_record()
        residual = next(
            item
            for item in data["residuals"]
            if item["id"] == "content-addressed-runtime"
        )
        residual["status"] = "pending"
        residual.pop("completion_evidence", None)
        residual.pop("completion_receipt", None)
    
>       with pytest.raises(ValueError, match="pre-deploy residuals are pending"):
E       AssertionError: Regex pattern did not match.
E         Expected regex: 'pre-deploy residuals are pending'
E         Actual message: 'canonical_artifact_moves[0].from still exists: .megaplan/initiatives/megaplan-native-parity-corrective/GOLDEN_TRACE_CONTRACT.md'

tests/test_post_m11_release_evidence.py:197: AssertionError
----------------------------- Captured stderr call -----------------------------
huggingface/tokenizers: The current process just got forked, after parallelism has already been used. Disabling parallelism to avoid deadlocks...
To disable this warning, you can either:
	- Avoid using `tokenizers` before the fork if possible
	- Explicitly set the environment variable TOKENIZERS_PARALLELISM=(true | false)
huggingface/tokenizers: The current process just got forked, after parallelism has already been used. Disabling parallelism to avoid deadlocks...
To disable this warning, you can either:
	- Avoid using `tokenizers` before the fork if possible
	- Explicitly set the environment variable TOKENIZERS_PARALLELISM=(true | false)
huggingface/tokenizers: The current process just got forked, after parallelism has already been used. Disabling parallelism to avoid deadlocks...
To disable this warning, you can either:
	- Avoid using `tokenizers` before the fork if possible
	- Explicitly set the environment variable TOKENIZERS_PARALLELISM=(true | false)
___________ test_candidate_ready_requires_bound_acceptance_evidence ____________

tmp_path = PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_candidate_ready_requires_0')

    def test_candidate_ready_requires_bound_acceptance_evidence(
        tmp_path: Path,
    ) -> None:
        data = _candidate_ready_record()
        data.pop("final_acceptance")
    
        with pytest.raises(ValueError, match="requires final_acceptance"):
>           validate(_write(tmp_path, data))

tests/test_post_m11_release_evidence.py:208: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

path = PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_candidate_ready_requires_0/release-evidence.json')

    def validate(path: Path) -> None:
        repo = Path(_git("rev-parse", "--show-toplevel", cwd=REPO_ROOT))
        data = json.loads(path.read_text(encoding="utf-8"))
    
        digest_path = path.with_name(path.name + ".sha256")
        if digest_path.is_file():
            expected_digest, expected_name = digest_path.read_text(
                encoding="utf-8"
            ).split()
            actual_digest = hashlib.sha256(path.read_bytes()).hexdigest()
            if expected_name != path.name or expected_digest != actual_digest:
                raise ValueError("release evidence SHA-256 sidecar mismatch")
    
        if data.get("schema") != "arnold.post_m11_release_evidence.v1":
            raise ValueError("unsupported schema")
        if data.get("record_status") not in ALLOWED_RECORD_STATUS:
            raise ValueError("invalid record_status")
        _validate_sha256_fields(data)
    
        authority = data["authority"]
        plan_path = repo / authority["plan_path"]
        if not plan_path.is_file():
            raise ValueError(f"missing plan path: {authority['plan_path']}")
    
        git_objects: set[str] = set()
        for field in (
            "plan_publication_commit",
            "origin_base_commit",
            "evidence_cut_commit",
            "evidence_cut_tree",
        ):
            git_objects.add(_require_sha1(authority[field], f"authority.{field}"))
    
        plan_blob = _require_sha1(authority["plan_blob"], "authority.plan_blob")
        actual_plan_blob = _git("hash-object", str(plan_path), cwd=repo)
        if actual_plan_blob != plan_blob:
            raise ValueError(
                f"plan blob mismatch: expected {plan_blob}, got {actual_plan_blob}"
            )
        git_objects.add(plan_blob)
        actual_evidence_tree = _git(
            "rev-parse", f"{authority['evidence_cut_commit']}^{{tree}}", cwd=repo
        )
        if actual_evidence_tree != authority["evidence_cut_tree"]:
            raise ValueError(
                "evidence cut tree mismatch: expected "
                f"{authority['evidence_cut_tree']}, got {actual_evidence_tree}"
            )
    
        source_refs = data.get("source_refs")
        if not isinstance(source_refs, list) or not source_refs:
            raise ValueError("source_refs must be a non-empty list")
        for index, item in enumerate(source_refs):
            git_objects.add(_require_sha1(item["sha"], f"source_refs[{index}].sha"))
            if item.get("classification") not in {"LAND", "KEEP_CHECKPOINT"}:
                raise ValueError(f"source_refs[{index}] has invalid classification")
    
        lineage = data.get("integration_lineage")
        if not isinstance(lineage, list) or not lineage:
            raise ValueError("integration_lineage must be a non-empty list")
        for index, item in enumerate(lineage):
            git_objects.add(
                _require_sha1(item["sha"], f"integration_lineage[{index}].sha")
            )
    
        for index, item in enumerate(data.get("validation_observations", [])):
            status = item.get("status")
            acceptance_effect = item.get("acceptance_effect")
            if status not in ALLOWED_OBSERVATION_STATUS:
                raise ValueError(
                    f"validation_observations[{index}] has invalid status"
                )
            if acceptance_effect not in ALLOWED_ACCEPTANCE_EFFECT:
                raise ValueError(
                    f"validation_observations[{index}] has invalid acceptance_effect"
                )
            if (
                status in NON_ACCEPTANCE_OBSERVATION_STATUS
                and acceptance_effect not in NON_ACCEPTANCE_EFFECT
            ):
                raise ValueError(
                    f"validation_observations[{index}] is a failure, superseded, "
                    "or discovery observation and cannot provide acceptance"
                )
            if "bound_commit" in item:
                git_objects.add(
                    _require_sha1(
                        item["bound_commit"],
                        f"validation_observations[{index}].bound_commit",
                    )
                )
            if "bound_tree" in item:
                git_objects.add(
                    _require_sha1(
                        item["bound_tree"],
                        f"validation_observations[{index}].bound_tree",
                    )
                )
    
        for collection_name in (
            "historical_superseded_attempts",
            "packaging_artifacts",
        ):
            for index, item in enumerate(data.get(collection_name, [])):
                if collection_name == "historical_superseded_attempts":
                    if item.get("status") not in ALLOWED_SUPERSEDED_STATUS:
                        raise ValueError(
                            f"{collection_name}[{index}] has invalid status"
                        )
                    if item.get("acceptance_effect") != "evidence_not_acceptance":
                        raise ValueError(
                            f"{collection_name}[{index}] cannot provide acceptance"
                        )
                elif (
                    item.get("acceptance_effect")
                    not in ALLOWED_PACKAGING_ACCEPTANCE_EFFECT
                ):
                    raise ValueError(
                        f"{collection_name}[{index}] has invalid acceptance_effect"
                    )
                if "bound_commit" in item:
                    git_objects.add(
                        _require_sha1(
                            item["bound_commit"],
                            f"{collection_name}[{index}].bound_commit",
                        )
                    )
    
        for index, item in enumerate(data.get("canonical_artifact_moves", [])):
            source = repo / item["from"]
            destination = repo / item["to"]
            if source.exists():
>               raise ValueError(
                    f"canonical_artifact_moves[{index}].from still exists: {item['from']}"
                )
E               ValueError: canonical_artifact_moves[0].from still exists: .megaplan/initiatives/megaplan-native-parity-corrective/GOLDEN_TRACE_CONTRACT.md

scripts/validate_post_m11_release_evidence.py:527: ValueError

During handling of the above exception, another exception occurred:

tmp_path = PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_candidate_ready_requires_0')

    def test_candidate_ready_requires_bound_acceptance_evidence(
        tmp_path: Path,
    ) -> None:
        data = _candidate_ready_record()
        data.pop("final_acceptance")
    
>       with pytest.raises(ValueError, match="requires final_acceptance"):
E       AssertionError: Regex pattern did not match.
E         Expected regex: 'requires final_acceptance'
E         Actual message: 'canonical_artifact_moves[0].from still exists: .megaplan/initiatives/megaplan-native-parity-corrective/GOLDEN_TRACE_CONTRACT.md'

tests/test_post_m11_release_evidence.py:207: AssertionError
----------------------------- Captured stderr call -----------------------------
huggingface/tokenizers: The current process just got forked, after parallelism has already been used. Disabling parallelism to avoid deadlocks...
To disable this warning, you can either:
	- Avoid using `tokenizers` before the fork if possible
	- Explicitly set the environment variable TOKENIZERS_PARALLELISM=(true | false)
huggingface/tokenizers: The current process just got forked, after parallelism has already been used. Disabling parallelism to avoid deadlocks...
To disable this warning, you can either:
	- Avoid using `tokenizers` before the fork if possible
	- Explicitly set the environment variable TOKENIZERS_PARALLELISM=(true | false)
huggingface/tokenizers: The current process just got forked, after parallelism has already been used. Disabling parallelism to avoid deadlocks...
To disable this warning, you can either:
	- Avoid using `tokenizers` before the fork if possible
	- Explicitly set the environment variable TOKENIZERS_PARALLELISM=(true | false)
____________ test_candidate_ready_requires_complete_source_universe ____________

tmp_path = PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_candidate_ready_requires_1')

    def test_candidate_ready_requires_complete_source_universe(tmp_path: Path) -> None:
        data = _candidate_ready_record()
        data.pop("source_universe")
        with pytest.raises(ValueError, match="requires source_universe"):
>           validate(_write(tmp_path, data))

tests/test_post_m11_release_evidence.py:215: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

path = PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_candidate_ready_requires_1/release-evidence.json')

    def validate(path: Path) -> None:
        repo = Path(_git("rev-parse", "--show-toplevel", cwd=REPO_ROOT))
        data = json.loads(path.read_text(encoding="utf-8"))
    
        digest_path = path.with_name(path.name + ".sha256")
        if digest_path.is_file():
            expected_digest, expected_name = digest_path.read_text(
                encoding="utf-8"
            ).split()
            actual_digest = hashlib.sha256(path.read_bytes()).hexdigest()
            if expected_name != path.name or expected_digest != actual_digest:
                raise ValueError("release evidence SHA-256 sidecar mismatch")
    
        if data.get("schema") != "arnold.post_m11_release_evidence.v1":
            raise ValueError("unsupported schema")
        if data.get("record_status") not in ALLOWED_RECORD_STATUS:
            raise ValueError("invalid record_status")
        _validate_sha256_fields(data)
    
        authority = data["authority"]
        plan_path = repo / authority["plan_path"]
        if not plan_path.is_file():
            raise ValueError(f"missing plan path: {authority['plan_path']}")
    
        git_objects: set[str] = set()
        for field in (
            "plan_publication_commit",
            "origin_base_commit",
            "evidence_cut_commit",
            "evidence_cut_tree",
        ):
            git_objects.add(_require_sha1(authority[field], f"authority.{field}"))
    
        plan_blob = _require_sha1(authority["plan_blob"], "authority.plan_blob")
        actual_plan_blob = _git("hash-object", str(plan_path), cwd=repo)
        if actual_plan_blob != plan_blob:
            raise ValueError(
                f"plan blob mismatch: expected {plan_blob}, got {actual_plan_blob}"
            )
        git_objects.add(plan_blob)
        actual_evidence_tree = _git(
            "rev-parse", f"{authority['evidence_cut_commit']}^{{tree}}", cwd=repo
        )
        if actual_evidence_tree != authority["evidence_cut_tree"]:
            raise ValueError(
                "evidence cut tree mismatch: expected "
                f"{authority['evidence_cut_tree']}, got {actual_evidence_tree}"
            )
    
        source_refs = data.get("source_refs")
        if not isinstance(source_refs, list) or not source_refs:
            raise ValueError("source_refs must be a non-empty list")
        for index, item in enumerate(source_refs):
            git_objects.add(_require_sha1(item["sha"], f"source_refs[{index}].sha"))
            if item.get("classification") not in {"LAND", "KEEP_CHECKPOINT"}:
                raise ValueError(f"source_refs[{index}] has invalid classification")
    
        lineage = data.get("integration_lineage")
        if not isinstance(lineage, list) or not lineage:
            raise ValueError("integration_lineage must be a non-empty list")
        for index, item in enumerate(lineage):
            git_objects.add(
                _require_sha1(item["sha"], f"integration_lineage[{index}].sha")
            )
    
        for index, item in enumerate(data.get("validation_observations", [])):
            status = item.get("status")
            acceptance_effect = item.get("acceptance_effect")
            if status not in ALLOWED_OBSERVATION_STATUS:
                raise ValueError(
                    f"validation_observations[{index}] has invalid status"
                )
            if acceptance_effect not in ALLOWED_ACCEPTANCE_EFFECT:
                raise ValueError(
                    f"validation_observations[{index}] has invalid acceptance_effect"
                )
            if (
                status in NON_ACCEPTANCE_OBSERVATION_STATUS
                and acceptance_effect not in NON_ACCEPTANCE_EFFECT
            ):
                raise ValueError(
                    f"validation_observations[{index}] is a failure, superseded, "
                    "or discovery observation and cannot provide acceptance"
                )
            if "bound_commit" in item:
                git_objects.add(
                    _require_sha1(
                        item["bound_commit"],
                        f"validation_observations[{index}].bound_commit",
                    )
                )
            if "bound_tree" in item:
                git_objects.add(
                    _require_sha1(
                        item["bound_tree"],
                        f"validation_observations[{index}].bound_tree",
                    )
                )
    
        for collection_name in (
            "historical_superseded_attempts",
            "packaging_artifacts",
        ):
            for index, item in enumerate(data.get(collection_name, [])):
                if collection_name == "historical_superseded_attempts":
                    if item.get("status") not in ALLOWED_SUPERSEDED_STATUS:
                        raise ValueError(
                            f"{collection_name}[{index}] has invalid status"
                        )
                    if item.get("acceptance_effect") != "evidence_not_acceptance":
                        raise ValueError(
                            f"{collection_name}[{index}] cannot provide acceptance"
                        )
                elif (
                    item.get("acceptance_effect")
                    not in ALLOWED_PACKAGING_ACCEPTANCE_EFFECT
                ):
                    raise ValueError(
                        f"{collection_name}[{index}] has invalid acceptance_effect"
                    )
                if "bound_commit" in item:
                    git_objects.add(
                        _require_sha1(
                            item["bound_commit"],
                            f"{collection_name}[{index}].bound_commit",
                        )
                    )
    
        for index, item in enumerate(data.get("canonical_artifact_moves", [])):
            source = repo / item["from"]
            destination = repo / item["to"]
            if source.exists():
>               raise ValueError(
                    f"canonical_artifact_moves[{index}].from still exists: {item['from']}"
                )
E               ValueError: canonical_artifact_moves[0].from still exists: .megaplan/initiatives/megaplan-native-parity-corrective/GOLDEN_TRACE_CONTRACT.md

scripts/validate_post_m11_release_evidence.py:527: ValueError

During handling of the above exception, another exception occurred:

tmp_path = PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_candidate_ready_requires_1')

    def test_candidate_ready_requires_complete_source_universe(tmp_path: Path) -> None:
        data = _candidate_ready_record()
        data.pop("source_universe")
>       with pytest.raises(ValueError, match="requires source_universe"):
E       AssertionError: Regex pattern did not match.
E         Expected regex: 'requires source_universe'
E         Actual message: 'canonical_artifact_moves[0].from still exists: .megaplan/initiatives/megaplan-native-parity-corrective/GOLDEN_TRACE_CONTRACT.md'

tests/test_post_m11_release_evidence.py:214: AssertionError
----------------------------- Captured stderr call -----------------------------
huggingface/tokenizers: The current process just got forked, after parallelism has already been used. Disabling parallelism to avoid deadlocks...
To disable this warning, you can either:
	- Avoid using `tokenizers` before the fork if possible
	- Explicitly set the environment variable TOKENIZERS_PARALLELISM=(true | false)
huggingface/tokenizers: The current process just got forked, after parallelism has already been used. Disabling parallelism to avoid deadlocks...
To disable this warning, you can either:
	- Avoid using `tokenizers` before the fork if possible
	- Explicitly set the environment variable TOKENIZERS_PARALLELISM=(true | false)
huggingface/tokenizers: The current process just got forked, after parallelism has already been used. Disabling parallelism to avoid deadlocks...
To disable this warning, you can either:
	- Avoid using `tokenizers` before the fork if possible
	- Explicitly set the environment variable TOKENIZERS_PARALLELISM=(true | false)
_______ test_complete_release_requires_one_immutable_acceptance_binding ________

tmp_path = PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_complete_release_requires0')

    def test_complete_release_requires_one_immutable_acceptance_binding(
        tmp_path: Path,
    ) -> None:
        data = _completed_record()
>       validate(_write(tmp_path, data))

tests/test_post_m11_release_evidence.py:256: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

path = PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_complete_release_requires0/release-evidence.json')

    def validate(path: Path) -> None:
        repo = Path(_git("rev-parse", "--show-toplevel", cwd=REPO_ROOT))
        data = json.loads(path.read_text(encoding="utf-8"))
    
        digest_path = path.with_name(path.name + ".sha256")
        if digest_path.is_file():
            expected_digest, expected_name = digest_path.read_text(
                encoding="utf-8"
            ).split()
            actual_digest = hashlib.sha256(path.read_bytes()).hexdigest()
            if expected_name != path.name or expected_digest != actual_digest:
                raise ValueError("release evidence SHA-256 sidecar mismatch")
    
        if data.get("schema") != "arnold.post_m11_release_evidence.v1":
            raise ValueError("unsupported schema")
        if data.get("record_status") not in ALLOWED_RECORD_STATUS:
            raise ValueError("invalid record_status")
        _validate_sha256_fields(data)
    
        authority = data["authority"]
        plan_path = repo / authority["plan_path"]
        if not plan_path.is_file():
            raise ValueError(f"missing plan path: {authority['plan_path']}")
    
        git_objects: set[str] = set()
        for field in (
            "plan_publication_commit",
            "origin_base_commit",
            "evidence_cut_commit",
            "evidence_cut_tree",
        ):
            git_objects.add(_require_sha1(authority[field], f"authority.{field}"))
    
        plan_blob = _require_sha1(authority["plan_blob"], "authority.plan_blob")
        actual_plan_blob = _git("hash-object", str(plan_path), cwd=repo)
        if actual_plan_blob != plan_blob:
            raise ValueError(
                f"plan blob mismatch: expected {plan_blob}, got {actual_plan_blob}"
            )
        git_objects.add(plan_blob)
        actual_evidence_tree = _git(
            "rev-parse", f"{authority['evidence_cut_commit']}^{{tree}}", cwd=repo
        )
        if actual_evidence_tree != authority["evidence_cut_tree"]:
            raise ValueError(
                "evidence cut tree mismatch: expected "
                f"{authority['evidence_cut_tree']}, got {actual_evidence_tree}"
            )
    
        source_refs = data.get("source_refs")
        if not isinstance(source_refs, list) or not source_refs:
            raise ValueError("source_refs must be a non-empty list")
        for index, item in enumerate(source_refs):
            git_objects.add(_require_sha1(item["sha"], f"source_refs[{index}].sha"))
            if item.get("classification") not in {"LAND", "KEEP_CHECKPOINT"}:
                raise ValueError(f"source_refs[{index}] has invalid classification")
    
        lineage = data.get("integration_lineage")
        if not isinstance(lineage, list) or not lineage:
            raise ValueError("integration_lineage must be a non-empty list")
        for index, item in enumerate(lineage):
            git_objects.add(
                _require_sha1(item["sha"], f"integration_lineage[{index}].sha")
            )
    
        for index, item in enumerate(data.get("validation_observations", [])):
            status = item.get("status")
            acceptance_effect = item.get("acceptance_effect")
            if status not in ALLOWED_OBSERVATION_STATUS:
                raise ValueError(
                    f"validation_observations[{index}] has invalid status"
                )
            if acceptance_effect not in ALLOWED_ACCEPTANCE_EFFECT:
                raise ValueError(
                    f"validation_observations[{index}] has invalid acceptance_effect"
                )
            if (
                status in NON_ACCEPTANCE_OBSERVATION_STATUS
                and acceptance_effect not in NON_ACCEPTANCE_EFFECT
            ):
                raise ValueError(
                    f"validation_observations[{index}] is a failure, superseded, "
                    "or discovery observation and cannot provide acceptance"
                )
            if "bound_commit" in item:
                git_objects.add(
                    _require_sha1(
                        item["bound_commit"],
                        f"validation_observations[{index}].bound_commit",
                    )
                )
            if "bound_tree" in item:
                git_objects.add(
                    _require_sha1(
                        item["bound_tree"],
                        f"validation_observations[{index}].bound_tree",
                    )
                )
    
        for collection_name in (
            "historical_superseded_attempts",
            "packaging_artifacts",
        ):
            for index, item in enumerate(data.get(collection_name, [])):
                if collection_name == "historical_superseded_attempts":
                    if item.get("status") not in ALLOWED_SUPERSEDED_STATUS:
                        raise ValueError(
                            f"{collection_name}[{index}] has invalid status"
                        )
                    if item.get("acceptance_effect") != "evidence_not_acceptance":
                        raise ValueError(
                            f"{collection_name}[{index}] cannot provide acceptance"
                        )
                elif (
                    item.get("acceptance_effect")
                    not in ALLOWED_PACKAGING_ACCEPTANCE_EFFECT
                ):
                    raise ValueError(
                        f"{collection_name}[{index}] has invalid acceptance_effect"
                    )
                if "bound_commit" in item:
                    git_objects.add(
                        _require_sha1(
                            item["bound_commit"],
                            f"{collection_name}[{index}].bound_commit",
                        )
                    )
    
        for index, item in enumerate(data.get("canonical_artifact_moves", [])):
            source = repo / item["from"]
            destination = repo / item["to"]
            if source.exists():
>               raise ValueError(
                    f"canonical_artifact_moves[{index}].from still exists: {item['from']}"
                )
E               ValueError: canonical_artifact_moves[0].from still exists: .megaplan/initiatives/megaplan-native-parity-corrective/GOLDEN_TRACE_CONTRACT.md

scripts/validate_post_m11_release_evidence.py:527: ValueError
----------------------------- Captured stderr call -----------------------------
huggingface/tokenizers: The current process just got forked, after parallelism has already been used. Disabling parallelism to avoid deadlocks...
To disable this warning, you can either:
	- Avoid using `tokenizers` before the fork if possible
	- Explicitly set the environment variable TOKENIZERS_PARALLELISM=(true | false)
huggingface/tokenizers: The current process just got forked, after parallelism has already been used. Disabling parallelism to avoid deadlocks...
To disable this warning, you can either:
	- Avoid using `tokenizers` before the fork if possible
	- Explicitly set the environment variable TOKENIZERS_PARALLELISM=(true | false)
huggingface/tokenizers: The current process just got forked, after parallelism has already been used. Disabling parallelism to avoid deadlocks...
To disable this warning, you can either:
	- Avoid using `tokenizers` before the fork if possible
	- Explicitly set the environment variable TOKENIZERS_PARALLELISM=(true | false)
________ test_final_no_debt_rejects_partial_and_overlapping_shard_sets _________

tmp_path = PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_final_no_debt_rejects_par0')

    def test_final_no_debt_rejects_partial_and_overlapping_shard_sets(
        tmp_path: Path,
    ) -> None:
        data = _completed_record()
    
        partial = deepcopy(data)
        partial["final_acceptance"]["no_debt"]["terminal_inventories"].pop()
        with pytest.raises(ValueError, match="differs from terminal inventory union"):
>           validate(_write(tmp_path, partial))

tests/test_post_m11_release_evidence.py:345: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

path = PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_final_no_debt_rejects_par0/release-evidence.json')

    def validate(path: Path) -> None:
        repo = Path(_git("rev-parse", "--show-toplevel", cwd=REPO_ROOT))
        data = json.loads(path.read_text(encoding="utf-8"))
    
        digest_path = path.with_name(path.name + ".sha256")
        if digest_path.is_file():
            expected_digest, expected_name = digest_path.read_text(
                encoding="utf-8"
            ).split()
            actual_digest = hashlib.sha256(path.read_bytes()).hexdigest()
            if expected_name != path.name or expected_digest != actual_digest:
                raise ValueError("release evidence SHA-256 sidecar mismatch")
    
        if data.get("schema") != "arnold.post_m11_release_evidence.v1":
            raise ValueError("unsupported schema")
        if data.get("record_status") not in ALLOWED_RECORD_STATUS:
            raise ValueError("invalid record_status")
        _validate_sha256_fields(data)
    
        authority = data["authority"]
        plan_path = repo / authority["plan_path"]
        if not plan_path.is_file():
            raise ValueError(f"missing plan path: {authority['plan_path']}")
    
        git_objects: set[str] = set()
        for field in (
            "plan_publication_commit",
            "origin_base_commit",
            "evidence_cut_commit",
            "evidence_cut_tree",
        ):
            git_objects.add(_require_sha1(authority[field], f"authority.{field}"))
    
        plan_blob = _require_sha1(authority["plan_blob"], "authority.plan_blob")
        actual_plan_blob = _git("hash-object", str(plan_path), cwd=repo)
        if actual_plan_blob != plan_blob:
            raise ValueError(
                f"plan blob mismatch: expected {plan_blob}, got {actual_plan_blob}"
            )
        git_objects.add(plan_blob)
        actual_evidence_tree = _git(
            "rev-parse", f"{authority['evidence_cut_commit']}^{{tree}}", cwd=repo
        )
        if actual_evidence_tree != authority["evidence_cut_tree"]:
            raise ValueError(
                "evidence cut tree mismatch: expected "
                f"{authority['evidence_cut_tree']}, got {actual_evidence_tree}"
            )
    
        source_refs = data.get("source_refs")
        if not isinstance(source_refs, list) or not source_refs:
            raise ValueError("source_refs must be a non-empty list")
        for index, item in enumerate(source_refs):
            git_objects.add(_require_sha1(item["sha"], f"source_refs[{index}].sha"))
            if item.get("classification") not in {"LAND", "KEEP_CHECKPOINT"}:
                raise ValueError(f"source_refs[{index}] has invalid classification")
    
        lineage = data.get("integration_lineage")
        if not isinstance(lineage, list) or not lineage:
            raise ValueError("integration_lineage must be a non-empty list")
        for index, item in enumerate(lineage):
            git_objects.add(
                _require_sha1(item["sha"], f"integration_lineage[{index}].sha")
            )
    
        for index, item in enumerate(data.get("validation_observations", [])):
            status = item.get("status")
            acceptance_effect = item.get("acceptance_effect")
            if status not in ALLOWED_OBSERVATION_STATUS:
                raise ValueError(
                    f"validation_observations[{index}] has invalid status"
                )
            if acceptance_effect not in ALLOWED_ACCEPTANCE_EFFECT:
                raise ValueError(
                    f"validation_observations[{index}] has invalid acceptance_effect"
                )
            if (
                status in NON_ACCEPTANCE_OBSERVATION_STATUS
                and acceptance_effect not in NON_ACCEPTANCE_EFFECT
            ):
                raise ValueError(
                    f"validation_observations[{index}] is a failure, superseded, "
                    "or discovery observation and cannot provide acceptance"
                )
            if "bound_commit" in item:
                git_objects.add(
                    _require_sha1(
                        item["bound_commit"],
                        f"validation_observations[{index}].bound_commit",
                    )
                )
            if "bound_tree" in item:
                git_objects.add(
                    _require_sha1(
                        item["bound_tree"],
                        f"validation_observations[{index}].bound_tree",
                    )
                )
    
        for collection_name in (
            "historical_superseded_attempts",
            "packaging_artifacts",
        ):
            for index, item in enumerate(data.get(collection_name, [])):
                if collection_name == "historical_superseded_attempts":
                    if item.get("status") not in ALLOWED_SUPERSEDED_STATUS:
                        raise ValueError(
                            f"{collection_name}[{index}] has invalid status"
                        )
                    if item.get("acceptance_effect") != "evidence_not_acceptance":
                        raise ValueError(
                            f"{collection_name}[{index}] cannot provide acceptance"
                        )
                elif (
                    item.get("acceptance_effect")
                    not in ALLOWED_PACKAGING_ACCEPTANCE_EFFECT
                ):
                    raise ValueError(
                        f"{collection_name}[{index}] has invalid acceptance_effect"
                    )
                if "bound_commit" in item:
                    git_objects.add(
                        _require_sha1(
                            item["bound_commit"],
                            f"{collection_name}[{index}].bound_commit",
                        )
                    )
    
        for index, item in enumerate(data.get("canonical_artifact_moves", [])):
            source = repo / item["from"]
            destination = repo / item["to"]
            if source.exists():
>               raise ValueError(
                    f"canonical_artifact_moves[{index}].from still exists: {item['from']}"
                )
E               ValueError: canonical_artifact_moves[0].from still exists: .megaplan/initiatives/megaplan-native-parity-corrective/GOLDEN_TRACE_CONTRACT.md

scripts/validate_post_m11_release_evidence.py:527: ValueError

During handling of the above exception, another exception occurred:

tmp_path = PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_final_no_debt_rejects_par0')

    def test_final_no_debt_rejects_partial_and_overlapping_shard_sets(
        tmp_path: Path,
    ) -> None:
        data = _completed_record()
    
        partial = deepcopy(data)
        partial["final_acceptance"]["no_debt"]["terminal_inventories"].pop()
>       with pytest.raises(ValueError, match="differs from terminal inventory union"):
E       AssertionError: Regex pattern did not match.
E         Expected regex: 'differs from terminal inventory union'
E         Actual message: 'canonical_artifact_moves[0].from still exists: .megaplan/initiatives/megaplan-native-parity-corrective/GOLDEN_TRACE_CONTRACT.md'

tests/test_post_m11_release_evidence.py:344: AssertionError
----------------------------- Captured stderr call -----------------------------
huggingface/tokenizers: The current process just got forked, after parallelism has already been used. Disabling parallelism to avoid deadlocks...
To disable this warning, you can either:
	- Avoid using `tokenizers` before the fork if possible
	- Explicitly set the environment variable TOKENIZERS_PARALLELISM=(true | false)
huggingface/tokenizers: The current process just got forked, after parallelism has already been used. Disabling parallelism to avoid deadlocks...
To disable this warning, you can either:
	- Avoid using `tokenizers` before the fork if possible
	- Explicitly set the environment variable TOKENIZERS_PARALLELISM=(true | false)
huggingface/tokenizers: The current process just got forked, after parallelism has already been used. Disabling parallelism to avoid deadlocks...
To disable this warning, you can either:
	- Avoid using `tokenizers` before the fork if possible
	- Explicitly set the environment variable TOKENIZERS_PARALLELISM=(true | false)
_______ test_final_no_debt_rejects_every_non_acceptance_outcome[failed] ________

tmp_path = PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_final_no_debt_rejects_eve0')
field = 'failed'

    @pytest.mark.parametrize("field", ["failed", "errors", "skipped", "xfailed", "xpassed", "mutations"])
    def test_final_no_debt_rejects_every_non_acceptance_outcome(
        tmp_path: Path,
        field: str,
    ) -> None:
        data = _completed_record()
        data["final_acceptance"]["no_debt"]["terminal_inventories"][0]["counts"][
            field
        ] = 1
        with pytest.raises(ValueError, match="failure, skip, xfail/xpass, or mutation"):
>           validate(_write(tmp_path, data))

tests/test_post_m11_release_evidence.py:365: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

path = PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_final_no_debt_rejects_eve0/release-evidence.json')

    def validate(path: Path) -> None:
        repo = Path(_git("rev-parse", "--show-toplevel", cwd=REPO_ROOT))
        data = json.loads(path.read_text(encoding="utf-8"))
    
        digest_path = path.with_name(path.name + ".sha256")
        if digest_path.is_file():
            expected_digest, expected_name = digest_path.read_text(
                encoding="utf-8"
            ).split()
            actual_digest = hashlib.sha256(path.read_bytes()).hexdigest()
            if expected_name != path.name or expected_digest != actual_digest:
                raise ValueError("release evidence SHA-256 sidecar mismatch")
    
        if data.get("schema") != "arnold.post_m11_release_evidence.v1":
            raise ValueError("unsupported schema")
        if data.get("record_status") not in ALLOWED_RECORD_STATUS:
            raise ValueError("invalid record_status")
        _validate_sha256_fields(data)
    
        authority = data["authority"]
        plan_path = repo / authority["plan_path"]
        if not plan_path.is_file():
            raise ValueError(f"missing plan path: {authority['plan_path']}")
    
        git_objects: set[str] = set()
        for field in (
            "plan_publication_commit",
            "origin_base_commit",
            "evidence_cut_commit",
            "evidence_cut_tree",
        ):
            git_objects.add(_require_sha1(authority[field], f"authority.{field}"))
    
        plan_blob = _require_sha1(authority["plan_blob"], "authority.plan_blob")
        actual_plan_blob = _git("hash-object", str(plan_path), cwd=repo)
        if actual_plan_blob != plan_blob:
            raise ValueError(
                f"plan blob mismatch: expected {plan_blob}, got {actual_plan_blob}"
            )
        git_objects.add(plan_blob)
        actual_evidence_tree = _git(
            "rev-parse", f"{authority['evidence_cut_commit']}^{{tree}}", cwd=repo
        )
        if actual_evidence_tree != authority["evidence_cut_tree"]:
            raise ValueError(
                "evidence cut tree mismatch: expected "
                f"{authority['evidence_cut_tree']}, got {actual_evidence_tree}"
            )
    
        source_refs = data.get("source_refs")
        if not isinstance(source_refs, list) or not source_refs:
            raise ValueError("source_refs must be a non-empty list")
        for index, item in enumerate(source_refs):
            git_objects.add(_require_sha1(item["sha"], f"source_refs[{index}].sha"))
            if item.get("classification") not in {"LAND", "KEEP_CHECKPOINT"}:
                raise ValueError(f"source_refs[{index}] has invalid classification")
    
        lineage = data.get("integration_lineage")
        if not isinstance(lineage, list) or not lineage:
            raise ValueError("integration_lineage must be a non-empty list")
        for index, item in enumerate(lineage):
            git_objects.add(
                _require_sha1(item["sha"], f"integration_lineage[{index}].sha")
            )
    
        for index, item in enumerate(data.get("validation_observations", [])):
            status = item.get("status")
            acceptance_effect = item.get("acceptance_effect")
            if status not in ALLOWED_OBSERVATION_STATUS:
                raise ValueError(
                    f"validation_observations[{index}] has invalid status"
                )
            if acceptance_effect not in ALLOWED_ACCEPTANCE_EFFECT:
                raise ValueError(
                    f"validation_observations[{index}] has invalid acceptance_effect"
                )
            if (
                status in NON_ACCEPTANCE_OBSERVATION_STATUS
                and acceptance_effect not in NON_ACCEPTANCE_EFFECT
            ):
                raise ValueError(
                    f"validation_observations[{index}] is a failure, superseded, "
                    "or discovery observation and cannot provide acceptance"
                )
            if "bound_commit" in item:
                git_objects.add(
                    _require_sha1(
                        item["bound_commit"],
                        f"validation_observations[{index}].bound_commit",
                    )
                )
            if "bound_tree" in item:
                git_objects.add(
                    _require_sha1(
                        item["bound_tree"],
                        f"validation_observations[{index}].bound_tree",
                    )
                )
    
        for collection_name in (
            "historical_superseded_attempts",
            "packaging_artifacts",
        ):
            for index, item in enumerate(data.get(collection_name, [])):
                if collection_name == "historical_superseded_attempts":
                    if item.get("status") not in ALLOWED_SUPERSEDED_STATUS:
                        raise ValueError(
                            f"{collection_name}[{index}] has invalid status"
                        )
                    if item.get("acceptance_effect") != "evidence_not_acceptance":
                        raise ValueError(
                            f"{collection_name}[{index}] cannot provide acceptance"
                        )
                elif (
                    item.get("acceptance_effect")
                    not in ALLOWED_PACKAGING_ACCEPTANCE_EFFECT
                ):
                    raise ValueError(
                        f"{collection_name}[{index}] has invalid acceptance_effect"
                    )
                if "bound_commit" in item:
                    git_objects.add(
                        _require_sha1(
                            item["bound_commit"],
                            f"{collection_name}[{index}].bound_commit",
                        )
                    )
    
        for index, item in enumerate(data.get("canonical_artifact_moves", [])):
            source = repo / item["from"]
            destination = repo / item["to"]
            if source.exists():
>               raise ValueError(
                    f"canonical_artifact_moves[{index}].from still exists: {item['from']}"
                )
E               ValueError: canonical_artifact_moves[0].from still exists: .megaplan/initiatives/megaplan-native-parity-corrective/GOLDEN_TRACE_CONTRACT.md

scripts/validate_post_m11_release_evidence.py:527: ValueError

During handling of the above exception, another exception occurred:

tmp_path = PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_final_no_debt_rejects_eve0')
field = 'failed'

    @pytest.mark.parametrize("field", ["failed", "errors", "skipped", "xfailed", "xpassed", "mutations"])
    def test_final_no_debt_rejects_every_non_acceptance_outcome(
        tmp_path: Path,
        field: str,
    ) -> None:
        data = _completed_record()
        data["final_acceptance"]["no_debt"]["terminal_inventories"][0]["counts"][
            field
        ] = 1
>       with pytest.raises(ValueError, match="failure, skip, xfail/xpass, or mutation"):
E       AssertionError: Regex pattern did not match.
E         Expected regex: 'failure, skip, xfail/xpass, or mutation'
E         Actual message: 'canonical_artifact_moves[0].from still exists: .megaplan/initiatives/megaplan-native-parity-corrective/GOLDEN_TRACE_CONTRACT.md'

tests/test_post_m11_release_evidence.py:364: AssertionError
----------------------------- Captured stderr call -----------------------------
huggingface/tokenizers: The current process just got forked, after parallelism has already been used. Disabling parallelism to avoid deadlocks...
To disable this warning, you can either:
	- Avoid using `tokenizers` before the fork if possible
	- Explicitly set the environment variable TOKENIZERS_PARALLELISM=(true | false)
huggingface/tokenizers: The current process just got forked, after parallelism has already been used. Disabling parallelism to avoid deadlocks...
To disable this warning, you can either:
	- Avoid using `tokenizers` before the fork if possible
	- Explicitly set the environment variable TOKENIZERS_PARALLELISM=(true | false)
huggingface/tokenizers: The current process just got forked, after parallelism has already been used. Disabling parallelism to avoid deadlocks...
To disable this warning, you can either:
	- Avoid using `tokenizers` before the fork if possible
	- Explicitly set the environment variable TOKENIZERS_PARALLELISM=(true | false)
_______ test_final_no_debt_rejects_every_non_acceptance_outcome[errors] ________

tmp_path = PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_final_no_debt_rejects_eve1')
field = 'errors'

    @pytest.mark.parametrize("field", ["failed", "errors", "skipped", "xfailed", "xpassed", "mutations"])
    def test_final_no_debt_rejects_every_non_acceptance_outcome(
        tmp_path: Path,
        field: str,
    ) -> None:
        data = _completed_record()
        data["final_acceptance"]["no_debt"]["terminal_inventories"][0]["counts"][
            field
        ] = 1
        with pytest.raises(ValueError, match="failure, skip, xfail/xpass, or mutation"):
>           validate(_write(tmp_path, data))

tests/test_post_m11_release_evidence.py:365: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

path = PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_final_no_debt_rejects_eve1/release-evidence.json')

    def validate(path: Path) -> None:
        repo = Path(_git("rev-parse", "--show-toplevel", cwd=REPO_ROOT))
        data = json.loads(path.read_text(encoding="utf-8"))
    
        digest_path = path.with_name(path.name + ".sha256")
        if digest_path.is_file():
            expected_digest, expected_name = digest_path.read_text(
                encoding="utf-8"
            ).split()
            actual_digest = hashlib.sha256(path.read_bytes()).hexdigest()
            if expected_name != path.name or expected_digest != actual_digest:
                raise ValueError("release evidence SHA-256 sidecar mismatch")
    
        if data.get("schema") != "arnold.post_m11_release_evidence.v1":
            raise ValueError("unsupported schema")
        if data.get("record_status") not in ALLOWED_RECORD_STATUS:
            raise ValueError("invalid record_status")
        _validate_sha256_fields(data)
    
        authority = data["authority"]
        plan_path = repo / authority["plan_path"]
        if not plan_path.is_file():
            raise ValueError(f"missing plan path: {authority['plan_path']}")
    
        git_objects: set[str] = set()
        for field in (
            "plan_publication_commit",
            "origin_base_commit",
            "evidence_cut_commit",
            "evidence_cut_tree",
        ):
            git_objects.add(_require_sha1(authority[field], f"authority.{field}"))
    
        plan_blob = _require_sha1(authority["plan_blob"], "authority.plan_blob")
        actual_plan_blob = _git("hash-object", str(plan_path), cwd=repo)
        if actual_plan_blob != plan_blob:
            raise ValueError(
                f"plan blob mismatch: expected {plan_blob}, got {actual_plan_blob}"
            )
        git_objects.add(plan_blob)
        actual_evidence_tree = _git(
            "rev-parse", f"{authority['evidence_cut_commit']}^{{tree}}", cwd=repo
        )
        if actual_evidence_tree != authority["evidence_cut_tree"]:
            raise ValueError(
                "evidence cut tree mismatch: expected "
                f"{authority['evidence_cut_tree']}, got {actual_evidence_tree}"
            )
    
        source_refs = data.get("source_refs")
        if not isinstance(source_refs, list) or not source_refs:
            raise ValueError("source_refs must be a non-empty list")
        for index, item in enumerate(source_refs):
            git_objects.add(_require_sha1(item["sha"], f"source_refs[{index}].sha"))
            if item.get("classification") not in {"LAND", "KEEP_CHECKPOINT"}:
                raise ValueError(f"source_refs[{index}] has invalid classification")
    
        lineage = data.get("integration_lineage")
        if not isinstance(lineage, list) or not lineage:
            raise ValueError("integration_lineage must be a non-empty list")
        for index, item in enumerate(lineage):
            git_objects.add(
                _require_sha1(item["sha"], f"integration_lineage[{index}].sha")
            )
    
        for index, item in enumerate(data.get("validation_observations", [])):
            status = item.get("status")
            acceptance_effect = item.get("acceptance_effect")
            if status not in ALLOWED_OBSERVATION_STATUS:
                raise ValueError(
                    f"validation_observations[{index}] has invalid status"
                )
            if acceptance_effect not in ALLOWED_ACCEPTANCE_EFFECT:
                raise ValueError(
                    f"validation_observations[{index}] has invalid acceptance_effect"
                )
            if (
                status in NON_ACCEPTANCE_OBSERVATION_STATUS
                and acceptance_effect not in NON_ACCEPTANCE_EFFECT
            ):
                raise ValueError(
                    f"validation_observations[{index}] is a failure, superseded, "
                    "or discovery observation and cannot provide acceptance"
                )
            if "bound_commit" in item:
                git_objects.add(
                    _require_sha1(
                        item["bound_commit"],
                        f"validation_observations[{index}].bound_commit",
                    )
                )
            if "bound_tree" in item:
                git_objects.add(
                    _require_sha1(
                        item["bound_tree"],
                        f"validation_observations[{index}].bound_tree",
                    )
                )
    
        for collection_name in (
            "historical_superseded_attempts",
            "packaging_artifacts",
        ):
            for index, item in enumerate(data.get(collection_name, [])):
                if collection_name == "historical_superseded_attempts":
                    if item.get("status") not in ALLOWED_SUPERSEDED_STATUS:
                        raise ValueError(
                            f"{collection_name}[{index}] has invalid status"
                        )
                    if item.get("acceptance_effect") != "evidence_not_acceptance":
                        raise ValueError(
                            f"{collection_name}[{index}] cannot provide acceptance"
                        )
                elif (
                    item.get("acceptance_effect")
                    not in ALLOWED_PACKAGING_ACCEPTANCE_EFFECT
                ):
                    raise ValueError(
                        f"{collection_name}[{index}] has invalid acceptance_effect"
                    )
                if "bound_commit" in item:
                    git_objects.add(
                        _require_sha1(
                            item["bound_commit"],
                            f"{collection_name}[{index}].bound_commit",
                        )
                    )
    
        for index, item in enumerate(data.get("canonical_artifact_moves", [])):
            source = repo / item["from"]
            destination = repo / item["to"]
            if source.exists():
>               raise ValueError(
                    f"canonical_artifact_moves[{index}].from still exists: {item['from']}"
                )
E               ValueError: canonical_artifact_moves[0].from still exists: .megaplan/initiatives/megaplan-native-parity-corrective/GOLDEN_TRACE_CONTRACT.md

scripts/validate_post_m11_release_evidence.py:527: ValueError

During handling of the above exception, another exception occurred:

tmp_path = PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_final_no_debt_rejects_eve1')
field = 'errors'

    @pytest.mark.parametrize("field", ["failed", "errors", "skipped", "xfailed", "xpassed", "mutations"])
    def test_final_no_debt_rejects_every_non_acceptance_outcome(
        tmp_path: Path,
        field: str,
    ) -> None:
        data = _completed_record()
        data["final_acceptance"]["no_debt"]["terminal_inventories"][0]["counts"][
            field
        ] = 1
>       with pytest.raises(ValueError, match="failure, skip, xfail/xpass, or mutation"):
E       AssertionError: Regex pattern did not match.
E         Expected regex: 'failure, skip, xfail/xpass, or mutation'
E         Actual message: 'canonical_artifact_moves[0].from still exists: .megaplan/initiatives/megaplan-native-parity-corrective/GOLDEN_TRACE_CONTRACT.md'

tests/test_post_m11_release_evidence.py:364: AssertionError
----------------------------- Captured stderr call -----------------------------
huggingface/tokenizers: The current process just got forked, after parallelism has already been used. Disabling parallelism to avoid deadlocks...
To disable this warning, you can either:
	- Avoid using `tokenizers` before the fork if possible
	- Explicitly set the environment variable TOKENIZERS_PARALLELISM=(true | false)
huggingface/tokenizers: The current process just got forked, after parallelism has already been used. Disabling parallelism to avoid deadlocks...
To disable this warning, you can either:
	- Avoid using `tokenizers` before the fork if possible
	- Explicitly set the environment variable TOKENIZERS_PARALLELISM=(true | false)
huggingface/tokenizers: The current process just got forked, after parallelism has already been used. Disabling parallelism to avoid deadlocks...
To disable this warning, you can either:
	- Avoid using `tokenizers` before the fork if possible
	- Explicitly set the environment variable TOKENIZERS_PARALLELISM=(true | false)
_______ test_final_no_debt_rejects_every_non_acceptance_outcome[skipped] _______

tmp_path = PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_final_no_debt_rejects_eve2')
field = 'skipped'

    @pytest.mark.parametrize("field", ["failed", "errors", "skipped", "xfailed", "xpassed", "mutations"])
    def test_final_no_debt_rejects_every_non_acceptance_outcome(
        tmp_path: Path,
        field: str,
    ) -> None:
        data = _completed_record()
        data["final_acceptance"]["no_debt"]["terminal_inventories"][0]["counts"][
            field
        ] = 1
        with pytest.raises(ValueError, match="failure, skip, xfail/xpass, or mutation"):
>           validate(_write(tmp_path, data))

tests/test_post_m11_release_evidence.py:365: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

path = PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_final_no_debt_rejects_eve2/release-evidence.json')

    def validate(path: Path) -> None:
        repo = Path(_git("rev-parse", "--show-toplevel", cwd=REPO_ROOT))
        data = json.loads(path.read_text(encoding="utf-8"))
    
        digest_path = path.with_name(path.name + ".sha256")
        if digest_path.is_file():
            expected_digest, expected_name = digest_path.read_text(
                encoding="utf-8"
            ).split()
            actual_digest = hashlib.sha256(path.read_bytes()).hexdigest()
            if expected_name != path.name or expected_digest != actual_digest:
                raise ValueError("release evidence SHA-256 sidecar mismatch")
    
        if data.get("schema") != "arnold.post_m11_release_evidence.v1":
            raise ValueError("unsupported schema")
        if data.get("record_status") not in ALLOWED_RECORD_STATUS:
            raise ValueError("invalid record_status")
        _validate_sha256_fields(data)
    
        authority = data["authority"]
        plan_path = repo / authority["plan_path"]
        if not plan_path.is_file():
            raise ValueError(f"missing plan path: {authority['plan_path']}")
    
        git_objects: set[str] = set()
        for field in (
            "plan_publication_commit",
            "origin_base_commit",
            "evidence_cut_commit",
            "evidence_cut_tree",
        ):
            git_objects.add(_require_sha1(authority[field], f"authority.{field}"))
    
        plan_blob = _require_sha1(authority["plan_blob"], "authority.plan_blob")
        actual_plan_blob = _git("hash-object", str(plan_path), cwd=repo)
        if actual_plan_blob != plan_blob:
            raise ValueError(
                f"plan blob mismatch: expected {plan_blob}, got {actual_plan_blob}"
            )
        git_objects.add(plan_blob)
        actual_evidence_tree = _git(
            "rev-parse", f"{authority['evidence_cut_commit']}^{{tree}}", cwd=repo
        )
        if actual_evidence_tree != authority["evidence_cut_tree"]:
            raise ValueError(
                "evidence cut tree mismatch: expected "
                f"{authority['evidence_cut_tree']}, got {actual_evidence_tree}"
            )
    
        source_refs = data.get("source_refs")
        if not isinstance(source_refs, list) or not source_refs:
            raise ValueError("source_refs must be a non-empty list")
        for index, item in enumerate(source_refs):
            git_objects.add(_require_sha1(item["sha"], f"source_refs[{index}].sha"))
            if item.get("classification") not in {"LAND", "KEEP_CHECKPOINT"}:
                raise ValueError(f"source_refs[{index}] has invalid classification")
    
        lineage = data.get("integration_lineage")
        if not isinstance(lineage, list) or not lineage:
            raise ValueError("integration_lineage must be a non-empty list")
        for index, item in enumerate(lineage):
            git_objects.add(
                _require_sha1(item["sha"], f"integration_lineage[{index}].sha")
            )
    
        for index, item in enumerate(data.get("validation_observations", [])):
            status = item.get("status")
            acceptance_effect = item.get("acceptance_effect")
            if status not in ALLOWED_OBSERVATION_STATUS:
                raise ValueError(
                    f"validation_observations[{index}] has invalid status"
                )
            if acceptance_effect not in ALLOWED_ACCEPTANCE_EFFECT:
                raise ValueError(
                    f"validation_observations[{index}] has invalid acceptance_effect"
                )
            if (
                status in NON_ACCEPTANCE_OBSERVATION_STATUS
                and acceptance_effect not in NON_ACCEPTANCE_EFFECT
            ):
                raise ValueError(
                    f"validation_observations[{index}] is a failure, superseded, "
                    "or discovery observation and cannot provide acceptance"
                )
            if "bound_commit" in item:
                git_objects.add(
                    _require_sha1(
                        item["bound_commit"],
                        f"validation_observations[{index}].bound_commit",
                    )
                )
            if "bound_tree" in item:
                git_objects.add(
                    _require_sha1(
                        item["bound_tree"],
                        f"validation_observations[{index}].bound_tree",
                    )
                )
    
        for collection_name in (
            "historical_superseded_attempts",
            "packaging_artifacts",
        ):
            for index, item in enumerate(data.get(collection_name, [])):
                if collection_name == "historical_superseded_attempts":
                    if item.get("status") not in ALLOWED_SUPERSEDED_STATUS:
                        raise ValueError(
                            f"{collection_name}[{index}] has invalid status"
                        )
                    if item.get("acceptance_effect") != "evidence_not_acceptance":
                        raise ValueError(
                            f"{collection_name}[{index}] cannot provide acceptance"
                        )
                elif (
                    item.get("acceptance_effect")
                    not in ALLOWED_PACKAGING_ACCEPTANCE_EFFECT
                ):
                    raise ValueError(
                        f"{collection_name}[{index}] has invalid acceptance_effect"
                    )
                if "bound_commit" in item:
                    git_objects.add(
                        _require_sha1(
                            item["bound_commit"],
                            f"{collection_name}[{index}].bound_commit",
                        )
                    )
    
        for index, item in enumerate(data.get("canonical_artifact_moves", [])):
            source = repo / item["from"]
            destination = repo / item["to"]
            if source.exists():
>               raise ValueError(
                    f"canonical_artifact_moves[{index}].from still exists: {item['from']}"
                )
E               ValueError: canonical_artifact_moves[0].from still exists: .megaplan/initiatives/megaplan-native-parity-corrective/GOLDEN_TRACE_CONTRACT.md

scripts/validate_post_m11_release_evidence.py:527: ValueError

During handling of the above exception, another exception occurred:

tmp_path = PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_final_no_debt_rejects_eve2')
field = 'skipped'

    @pytest.mark.parametrize("field", ["failed", "errors", "skipped", "xfailed", "xpassed", "mutations"])
    def test_final_no_debt_rejects_every_non_acceptance_outcome(
        tmp_path: Path,
        field: str,
    ) -> None:
        data = _completed_record()
        data["final_acceptance"]["no_debt"]["terminal_inventories"][0]["counts"][
            field
        ] = 1
>       with pytest.raises(ValueError, match="failure, skip, xfail/xpass, or mutation"):
E       AssertionError: Regex pattern did not match.
E         Expected regex: 'failure, skip, xfail/xpass, or mutation'
E         Actual message: 'canonical_artifact_moves[0].from still exists: .megaplan/initiatives/megaplan-native-parity-corrective/GOLDEN_TRACE_CONTRACT.md'

tests/test_post_m11_release_evidence.py:364: AssertionError
----------------------------- Captured stderr call -----------------------------
huggingface/tokenizers: The current process just got forked, after parallelism has already been used. Disabling parallelism to avoid deadlocks...
To disable this warning, you can either:
	- Avoid using `tokenizers` before the fork if possible
	- Explicitly set the environment variable TOKENIZERS_PARALLELISM=(true | false)
huggingface/tokenizers: The current process just got forked, after parallelism has already been used. Disabling parallelism to avoid deadlocks...
To disable this warning, you can either:
	- Avoid using `tokenizers` before the fork if possible
	- Explicitly set the environment variable TOKENIZERS_PARALLELISM=(true | false)
huggingface/tokenizers: The current process just got forked, after parallelism has already been used. Disabling parallelism to avoid deadlocks...
To disable this warning, you can either:
	- Avoid using `tokenizers` before the fork if possible
	- Explicitly set the environment variable TOKENIZERS_PARALLELISM=(true | false)
_______ test_final_no_debt_rejects_every_non_acceptance_outcome[xfailed] _______

tmp_path = PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_final_no_debt_rejects_eve3')
field = 'xfailed'

    @pytest.mark.parametrize("field", ["failed", "errors", "skipped", "xfailed", "xpassed", "mutations"])
    def test_final_no_debt_rejects_every_non_acceptance_outcome(
        tmp_path: Path,
        field: str,
    ) -> None:
        data = _completed_record()
        data["final_acceptance"]["no_debt"]["terminal_inventories"][0]["counts"][
            field
        ] = 1
        with pytest.raises(ValueError, match="failure, skip, xfail/xpass, or mutation"):
>           validate(_write(tmp_path, data))

tests/test_post_m11_release_evidence.py:365: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

path = PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_final_no_debt_rejects_eve3/release-evidence.json')

    def validate(path: Path) -> None:
        repo = Path(_git("rev-parse", "--show-toplevel", cwd=REPO_ROOT))
        data = json.loads(path.read_text(encoding="utf-8"))
    
        digest_path = path.with_name(path.name + ".sha256")
        if digest_path.is_file():
            expected_digest, expected_name = digest_path.read_text(
                encoding="utf-8"
            ).split()
            actual_digest = hashlib.sha256(path.read_bytes()).hexdigest()
            if expected_name != path.name or expected_digest != actual_digest:
                raise ValueError("release evidence SHA-256 sidecar mismatch")
    
        if data.get("schema") != "arnold.post_m11_release_evidence.v1":
            raise ValueError("unsupported schema")
        if data.get("record_status") not in ALLOWED_RECORD_STATUS:
            raise ValueError("invalid record_status")
        _validate_sha256_fields(data)
    
        authority = data["authority"]
        plan_path = repo / authority["plan_path"]
        if not plan_path.is_file():
            raise ValueError(f"missing plan path: {authority['plan_path']}")
    
        git_objects: set[str] = set()
        for field in (
            "plan_publication_commit",
            "origin_base_commit",
            "evidence_cut_commit",
            "evidence_cut_tree",
        ):
            git_objects.add(_require_sha1(authority[field], f"authority.{field}"))
    
        plan_blob = _require_sha1(authority["plan_blob"], "authority.plan_blob")
        actual_plan_blob = _git("hash-object", str(plan_path), cwd=repo)
        if actual_plan_blob != plan_blob:
            raise ValueError(
                f"plan blob mismatch: expected {plan_blob}, got {actual_plan_blob}"
            )
        git_objects.add(plan_blob)
        actual_evidence_tree = _git(
            "rev-parse", f"{authority['evidence_cut_commit']}^{{tree}}", cwd=repo
        )
        if actual_evidence_tree != authority["evidence_cut_tree"]:
            raise ValueError(
                "evidence cut tree mismatch: expected "
                f"{authority['evidence_cut_tree']}, got {actual_evidence_tree}"
            )
    
        source_refs = data.get("source_refs")
        if not isinstance(source_refs, list) or not source_refs:
            raise ValueError("source_refs must be a non-empty list")
        for index, item in enumerate(source_refs):
            git_objects.add(_require_sha1(item["sha"], f"source_refs[{index}].sha"))
            if item.get("classification") not in {"LAND", "KEEP_CHECKPOINT"}:
                raise ValueError(f"source_refs[{index}] has invalid classification")
    
        lineage = data.get("integration_lineage")
        if not isinstance(lineage, list) or not lineage:
            raise ValueError("integration_lineage must be a non-empty list")
        for index, item in enumerate(lineage):
            git_objects.add(
                _require_sha1(item["sha"], f"integration_lineage[{index}].sha")
            )
    
        for index, item in enumerate(data.get("validation_observations", [])):
            status = item.get("status")
            acceptance_effect = item.get("acceptance_effect")
            if status not in ALLOWED_OBSERVATION_STATUS:
                raise ValueError(
                    f"validation_observations[{index}] has invalid status"
                )
            if acceptance_effect not in ALLOWED_ACCEPTANCE_EFFECT:
                raise ValueError(
                    f"validation_observations[{index}] has invalid acceptance_effect"
                )
            if (
                status in NON_ACCEPTANCE_OBSERVATION_STATUS
                and acceptance_effect not in NON_ACCEPTANCE_EFFECT
            ):
                raise ValueError(
                    f"validation_observations[{index}] is a failure, superseded, "
                    "or discovery observation and cannot provide acceptance"
                )
            if "bound_commit" in item:
                git_objects.add(
                    _require_sha1(
                        item["bound_commit"],
                        f"validation_observations[{index}].bound_commit",
                    )
                )
            if "bound_tree" in item:
                git_objects.add(
                    _require_sha1(
                        item["bound_tree"],
                        f"validation_observations[{index}].bound_tree",
                    )
                )
    
        for collection_name in (
            "historical_superseded_attempts",
            "packaging_artifacts",
        ):
            for index, item in enumerate(data.get(collection_name, [])):
                if collection_name == "historical_superseded_attempts":
                    if item.get("status") not in ALLOWED_SUPERSEDED_STATUS:
                        raise ValueError(
                            f"{collection_name}[{index}] has invalid status"
                        )
                    if item.get("acceptance_effect") != "evidence_not_acceptance":
                        raise ValueError(
                            f"{collection_name}[{index}] cannot provide acceptance"
                        )
                elif (
                    item.get("acceptance_effect")
                    not in ALLOWED_PACKAGING_ACCEPTANCE_EFFECT
                ):
                    raise ValueError(
                        f"{collection_name}[{index}] has invalid acceptance_effect"
                    )
                if "bound_commit" in item:
                    git_objects.add(
                        _require_sha1(
                            item["bound_commit"],
                            f"{collection_name}[{index}].bound_commit",
                        )
                    )
    
        for index, item in enumerate(data.get("canonical_artifact_moves", [])):
            source = repo / item["from"]
            destination = repo / item["to"]
            if source.exists():
>               raise ValueError(
                    f"canonical_artifact_moves[{index}].from still exists: {item['from']}"
                )
E               ValueError: canonical_artifact_moves[0].from still exists: .megaplan/initiatives/megaplan-native-parity-corrective/GOLDEN_TRACE_CONTRACT.md

scripts/validate_post_m11_release_evidence.py:527: ValueError

During handling of the above exception, another exception occurred:

tmp_path = PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_final_no_debt_rejects_eve3')
field = 'xfailed'

    @pytest.mark.parametrize("field", ["failed", "errors", "skipped", "xfailed", "xpassed", "mutations"])
    def test_final_no_debt_rejects_every_non_acceptance_outcome(
        tmp_path: Path,
        field: str,
    ) -> None:
        data = _completed_record()
        data["final_acceptance"]["no_debt"]["terminal_inventories"][0]["counts"][
            field
        ] = 1
>       with pytest.raises(ValueError, match="failure, skip, xfail/xpass, or mutation"):
E       AssertionError: Regex pattern did not match.
E         Expected regex: 'failure, skip, xfail/xpass, or mutation'
E         Actual message: 'canonical_artifact_moves[0].from still exists: .megaplan/initiatives/megaplan-native-parity-corrective/GOLDEN_TRACE_CONTRACT.md'

tests/test_post_m11_release_evidence.py:364: AssertionError
----------------------------- Captured stderr call -----------------------------
huggingface/tokenizers: The current process just got forked, after parallelism has already been used. Disabling parallelism to avoid deadlocks...
To disable this warning, you can either:
	- Avoid using `tokenizers` before the fork if possible
	- Explicitly set the environment variable TOKENIZERS_PARALLELISM=(true | false)
huggingface/tokenizers: The current process just got forked, after parallelism has already been used. Disabling parallelism to avoid deadlocks...
To disable this warning, you can either:
	- Avoid using `tokenizers` before the fork if possible
	- Explicitly set the environment variable TOKENIZERS_PARALLELISM=(true | false)
huggingface/tokenizers: The current process just got forked, after parallelism has already been used. Disabling parallelism to avoid deadlocks...
To disable this warning, you can either:
	- Avoid using `tokenizers` before the fork if possible
	- Explicitly set the environment variable TOKENIZERS_PARALLELISM=(true | false)
_______ test_final_no_debt_rejects_every_non_acceptance_outcome[xpassed] _______

tmp_path = PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_final_no_debt_rejects_eve4')
field = 'xpassed'

    @pytest.mark.parametrize("field", ["failed", "errors", "skipped", "xfailed", "xpassed", "mutations"])
    def test_final_no_debt_rejects_every_non_acceptance_outcome(
        tmp_path: Path,
        field: str,
    ) -> None:
        data = _completed_record()
        data["final_acceptance"]["no_debt"]["terminal_inventories"][0]["counts"][
            field
        ] = 1
        with pytest.raises(ValueError, match="failure, skip, xfail/xpass, or mutation"):
>           validate(_write(tmp_path, data))

tests/test_post_m11_release_evidence.py:365: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

path = PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_final_no_debt_rejects_eve4/release-evidence.json')

    def validate(path: Path) -> None:
        repo = Path(_git("rev-parse", "--show-toplevel", cwd=REPO_ROOT))
        data = json.loads(path.read_text(encoding="utf-8"))
    
        digest_path = path.with_name(path.name + ".sha256")
        if digest_path.is_file():
            expected_digest, expected_name = digest_path.read_text(
                encoding="utf-8"
            ).split()
            actual_digest = hashlib.sha256(path.read_bytes()).hexdigest()
            if expected_name != path.name or expected_digest != actual_digest:
                raise ValueError("release evidence SHA-256 sidecar mismatch")
    
        if data.get("schema") != "arnold.post_m11_release_evidence.v1":
            raise ValueError("unsupported schema")
        if data.get("record_status") not in ALLOWED_RECORD_STATUS:
            raise ValueError("invalid record_status")
        _validate_sha256_fields(data)
    
        authority = data["authority"]
        plan_path = repo / authority["plan_path"]
        if not plan_path.is_file():
            raise ValueError(f"missing plan path: {authority['plan_path']}")
    
        git_objects: set[str] = set()
        for field in (
            "plan_publication_commit",
            "origin_base_commit",
            "evidence_cut_commit",
            "evidence_cut_tree",
        ):
            git_objects.add(_require_sha1(authority[field], f"authority.{field}"))
    
        plan_blob = _require_sha1(authority["plan_blob"], "authority.plan_blob")
        actual_plan_blob = _git("hash-object", str(plan_path), cwd=repo)
        if actual_plan_blob != plan_blob:
            raise ValueError(
                f"plan blob mismatch: expected {plan_blob}, got {actual_plan_blob}"
            )
        git_objects.add(plan_blob)
        actual_evidence_tree = _git(
            "rev-parse", f"{authority['evidence_cut_commit']}^{{tree}}", cwd=repo
        )
        if actual_evidence_tree != authority["evidence_cut_tree"]:
            raise ValueError(
                "evidence cut tree mismatch: expected "
                f"{authority['evidence_cut_tree']}, got {actual_evidence_tree}"
            )
    
        source_refs = data.get("source_refs")
        if not isinstance(source_refs, list) or not source_refs:
            raise ValueError("source_refs must be a non-empty list")
        for index, item in enumerate(source_refs):
            git_objects.add(_require_sha1(item["sha"], f"source_refs[{index}].sha"))
            if item.get("classification") not in {"LAND", "KEEP_CHECKPOINT"}:
                raise ValueError(f"source_refs[{index}] has invalid classification")
    
        lineage = data.get("integration_lineage")
        if not isinstance(lineage, list) or not lineage:
            raise ValueError("integration_lineage must be a non-empty list")
        for index, item in enumerate(lineage):
            git_objects.add(
                _require_sha1(item["sha"], f"integration_lineage[{index}].sha")
            )
    
        for index, item in enumerate(data.get("validation_observations", [])):
            status = item.get("status")
            acceptance_effect = item.get("acceptance_effect")
            if status not in ALLOWED_OBSERVATION_STATUS:
                raise ValueError(
                    f"validation_observations[{index}] has invalid status"
                )
            if acceptance_effect not in ALLOWED_ACCEPTANCE_EFFECT:
                raise ValueError(
                    f"validation_observations[{index}] has invalid acceptance_effect"
                )
            if (
                status in NON_ACCEPTANCE_OBSERVATION_STATUS
                and acceptance_effect not in NON_ACCEPTANCE_EFFECT
            ):
                raise ValueError(
                    f"validation_observations[{index}] is a failure, superseded, "
                    "or discovery observation and cannot provide acceptance"
                )
            if "bound_commit" in item:
                git_objects.add(
                    _require_sha1(
                        item["bound_commit"],
                        f"validation_observations[{index}].bound_commit",
                    )
                )
            if "bound_tree" in item:
                git_objects.add(
                    _require_sha1(
                        item["bound_tree"],
                        f"validation_observations[{index}].bound_tree",
                    )
                )
    
        for collection_name in (
            "historical_superseded_attempts",
            "packaging_artifacts",
        ):
            for index, item in enumerate(data.get(collection_name, [])):
                if collection_name == "historical_superseded_attempts":
                    if item.get("status") not in ALLOWED_SUPERSEDED_STATUS:
                        raise ValueError(
                            f"{collection_name}[{index}] has invalid status"
                        )
                    if item.get("acceptance_effect") != "evidence_not_acceptance":
                        raise ValueError(
                            f"{collection_name}[{index}] cannot provide acceptance"
                        )
                elif (
                    item.get("acceptance_effect")
                    not in ALLOWED_PACKAGING_ACCEPTANCE_EFFECT
                ):
                    raise ValueError(
                        f"{collection_name}[{index}] has invalid acceptance_effect"
                    )
                if "bound_commit" in item:
                    git_objects.add(
                        _require_sha1(
                            item["bound_commit"],
                            f"{collection_name}[{index}].bound_commit",
                        )
                    )
    
        for index, item in enumerate(data.get("canonical_artifact_moves", [])):
            source = repo / item["from"]
            destination = repo / item["to"]
            if source.exists():
>               raise ValueError(
                    f"canonical_artifact_moves[{index}].from still exists: {item['from']}"
                )
E               ValueError: canonical_artifact_moves[0].from still exists: .megaplan/initiatives/megaplan-native-parity-corrective/GOLDEN_TRACE_CONTRACT.md

scripts/validate_post_m11_release_evidence.py:527: ValueError

During handling of the above exception, another exception occurred:

tmp_path = PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_final_no_debt_rejects_eve4')
field = 'xpassed'

    @pytest.mark.parametrize("field", ["failed", "errors", "skipped", "xfailed", "xpassed", "mutations"])
    def test_final_no_debt_rejects_every_non_acceptance_outcome(
        tmp_path: Path,
        field: str,
    ) -> None:
        data = _completed_record()
        data["final_acceptance"]["no_debt"]["terminal_inventories"][0]["counts"][
            field
        ] = 1
>       with pytest.raises(ValueError, match="failure, skip, xfail/xpass, or mutation"):
E       AssertionError: Regex pattern did not match.
E         Expected regex: 'failure, skip, xfail/xpass, or mutation'
E         Actual message: 'canonical_artifact_moves[0].from still exists: .megaplan/initiatives/megaplan-native-parity-corrective/GOLDEN_TRACE_CONTRACT.md'

tests/test_post_m11_release_evidence.py:364: AssertionError
----------------------------- Captured stderr call -----------------------------
huggingface/tokenizers: The current process just got forked, after parallelism has already been used. Disabling parallelism to avoid deadlocks...
To disable this warning, you can either:
	- Avoid using `tokenizers` before the fork if possible
	- Explicitly set the environment variable TOKENIZERS_PARALLELISM=(true | false)
huggingface/tokenizers: The current process just got forked, after parallelism has already been used. Disabling parallelism to avoid deadlocks...
To disable this warning, you can either:
	- Avoid using `tokenizers` before the fork if possible
	- Explicitly set the environment variable TOKENIZERS_PARALLELISM=(true | false)
huggingface/tokenizers: The current process just got forked, after parallelism has already been used. Disabling parallelism to avoid deadlocks...
To disable this warning, you can either:
	- Avoid using `tokenizers` before the fork if possible
	- Explicitly set the environment variable TOKENIZERS_PARALLELISM=(true | false)
______ test_final_no_debt_rejects_every_non_acceptance_outcome[mutations] ______

tmp_path = PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_final_no_debt_rejects_eve5')
field = 'mutations'

    @pytest.mark.parametrize("field", ["failed", "errors", "skipped", "xfailed", "xpassed", "mutations"])
    def test_final_no_debt_rejects_every_non_acceptance_outcome(
        tmp_path: Path,
        field: str,
    ) -> None:
        data = _completed_record()
        data["final_acceptance"]["no_debt"]["terminal_inventories"][0]["counts"][
            field
        ] = 1
        with pytest.raises(ValueError, match="failure, skip, xfail/xpass, or mutation"):
>           validate(_write(tmp_path, data))

tests/test_post_m11_release_evidence.py:365: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

path = PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_final_no_debt_rejects_eve5/release-evidence.json')

    def validate(path: Path) -> None:
        repo = Path(_git("rev-parse", "--show-toplevel", cwd=REPO_ROOT))
        data = json.loads(path.read_text(encoding="utf-8"))
    
        digest_path = path.with_name(path.name + ".sha256")
        if digest_path.is_file():
            expected_digest, expected_name = digest_path.read_text(
                encoding="utf-8"
            ).split()
            actual_digest = hashlib.sha256(path.read_bytes()).hexdigest()
            if expected_name != path.name or expected_digest != actual_digest:
                raise ValueError("release evidence SHA-256 sidecar mismatch")
    
        if data.get("schema") != "arnold.post_m11_release_evidence.v1":
            raise ValueError("unsupported schema")
        if data.get("record_status") not in ALLOWED_RECORD_STATUS:
            raise ValueError("invalid record_status")
        _validate_sha256_fields(data)
    
        authority = data["authority"]
        plan_path = repo / authority["plan_path"]
        if not plan_path.is_file():
            raise ValueError(f"missing plan path: {authority['plan_path']}")
    
        git_objects: set[str] = set()
        for field in (
            "plan_publication_commit",
            "origin_base_commit",
            "evidence_cut_commit",
            "evidence_cut_tree",
        ):
            git_objects.add(_require_sha1(authority[field], f"authority.{field}"))
    
        plan_blob = _require_sha1(authority["plan_blob"], "authority.plan_blob")
        actual_plan_blob = _git("hash-object", str(plan_path), cwd=repo)
        if actual_plan_blob != plan_blob:
            raise ValueError(
                f"plan blob mismatch: expected {plan_blob}, got {actual_plan_blob}"
            )
        git_objects.add(plan_blob)
        actual_evidence_tree = _git(
            "rev-parse", f"{authority['evidence_cut_commit']}^{{tree}}", cwd=repo
        )
        if actual_evidence_tree != authority["evidence_cut_tree"]:
            raise ValueError(
                "evidence cut tree mismatch: expected "
                f"{authority['evidence_cut_tree']}, got {actual_evidence_tree}"
            )
    
        source_refs = data.get("source_refs")
        if not isinstance(source_refs, list) or not source_refs:
            raise ValueError("source_refs must be a non-empty list")
        for index, item in enumerate(source_refs):
            git_objects.add(_require_sha1(item["sha"], f"source_refs[{index}].sha"))
            if item.get("classification") not in {"LAND", "KEEP_CHECKPOINT"}:
                raise ValueError(f"source_refs[{index}] has invalid classification")
    
        lineage = data.get("integration_lineage")
        if not isinstance(lineage, list) or not lineage:
            raise ValueError("integration_lineage must be a non-empty list")
        for index, item in enumerate(lineage):
            git_objects.add(
                _require_sha1(item["sha"], f"integration_lineage[{index}].sha")
            )
    
        for index, item in enumerate(data.get("validation_observations", [])):
            status = item.get("status")
            acceptance_effect = item.get("acceptance_effect")
            if status not in ALLOWED_OBSERVATION_STATUS:
                raise ValueError(
                    f"validation_observations[{index}] has invalid status"
                )
            if acceptance_effect not in ALLOWED_ACCEPTANCE_EFFECT:
                raise ValueError(
                    f"validation_observations[{index}] has invalid acceptance_effect"
                )
            if (
                status in NON_ACCEPTANCE_OBSERVATION_STATUS
                and acceptance_effect not in NON_ACCEPTANCE_EFFECT
            ):
                raise ValueError(
                    f"validation_observations[{index}] is a failure, superseded, "
                    "or discovery observation and cannot provide acceptance"
                )
            if "bound_commit" in item:
                git_objects.add(
                    _require_sha1(
                        item["bound_commit"],
                        f"validation_observations[{index}].bound_commit",
                    )
                )
            if "bound_tree" in item:
                git_objects.add(
                    _require_sha1(
                        item["bound_tree"],
                        f"validation_observations[{index}].bound_tree",
                    )
                )
    
        for collection_name in (
            "historical_superseded_attempts",
            "packaging_artifacts",
        ):
            for index, item in enumerate(data.get(collection_name, [])):
                if collection_name == "historical_superseded_attempts":
                    if item.get("status") not in ALLOWED_SUPERSEDED_STATUS:
                        raise ValueError(
                            f"{collection_name}[{index}] has invalid status"
                        )
                    if item.get("acceptance_effect") != "evidence_not_acceptance":
                        raise ValueError(
                            f"{collection_name}[{index}] cannot provide acceptance"
                        )
                elif (
                    item.get("acceptance_effect")
                    not in ALLOWED_PACKAGING_ACCEPTANCE_EFFECT
                ):
                    raise ValueError(
                        f"{collection_name}[{index}] has invalid acceptance_effect"
                    )
                if "bound_commit" in item:
                    git_objects.add(
                        _require_sha1(
                            item["bound_commit"],
                            f"{collection_name}[{index}].bound_commit",
                        )
                    )
    
        for index, item in enumerate(data.get("canonical_artifact_moves", [])):
            source = repo / item["from"]
            destination = repo / item["to"]
            if source.exists():
>               raise ValueError(
                    f"canonical_artifact_moves[{index}].from still exists: {item['from']}"
                )
E               ValueError: canonical_artifact_moves[0].from still exists: .megaplan/initiatives/megaplan-native-parity-corrective/GOLDEN_TRACE_CONTRACT.md

scripts/validate_post_m11_release_evidence.py:527: ValueError

During handling of the above exception, another exception occurred:

tmp_path = PosixPath('/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_final_no_debt_rejects_eve5')
field = 'mutations'

    @pytest.mark.parametrize("field", ["failed", "errors", "skipped", "xfailed", "xpassed", "mutations"])
    def test_final_no_debt_rejects_every_non_acceptance_outcome(
        tmp_path: Path,
        field: str,
    ) -> None:
        data = _completed_record()
        data["final_acceptance"]["no_debt"]["terminal_inventories"][0]["counts"][
            field
        ] = 1
>       with pytest.raises(ValueError, match="failure, skip, xfail/xpass, or mutation"):
E       AssertionError: Regex pattern did not match.
E         Expected regex: 'failure, skip, xfail/xpass, or mutation'
E         Actual message: 'canonical_artifact_moves[0].from still exists: .megaplan/initiatives/megaplan-native-parity-corrective/GOLDEN_TRACE_CONTRACT.md'

tests/test_post_m11_release_evidence.py:364: AssertionError
----------------------------- Captured stderr call -----------------------------
huggingface/tokenizers: The current process just got forked, after parallelism has already been used. Disabling parallelism to avoid deadlocks...
To disable this warning, you can either:
	- Avoid using `tokenizers` before the fork if possible
	- Explicitly set the environment variable TOKENIZERS_PARALLELISM=(true | false)
huggingface/tokenizers: The current process just got forked, after parallelism has already been used. Disabling parallelism to avoid deadlocks...
To disable this warning, you can either:
	- Avoid using `tokenizers` before the fork if possible
	- Explicitly set the environment variable TOKENIZERS_PARALLELISM=(true | false)
huggingface/tokenizers: The current process just got forked, after parallelism has already been used. Disabling parallelism to avoid deadlocks...
To disable this warning, you can either:
	- Avoid using `tokenizers` before the fork if possible
	- Explicitly set the environment variable TOKENIZERS_PARALLELISM=(true | false)
=============================== warnings summary ===============================
tests/arnold_pipelines/megaplan/test_chain_awaiting_human_retry.py: 23 warnings
  /Users/peteromalley/Documents/.megaplan-worktrees/runtime-convergence-execution/arnold_pipelines/megaplan/chain/spec.py:1996: UserWarning: merge_policy should only be set away from `auto` when the user explicitly requests a human PR merge gate after every milestone; `review` will park unattended/cloud chains at awaiting_pr_merge.
    return ChainSpec.from_dict(raw or {})

-- Docs: https://docs.pytest.org/en/stable/how-to/capture-warnings.html
=========================== short test summary info ============================
FAILED tests/arnold_pipelines/megaplan/chain/test_occurrence_adopt.py::test_cli_adopt_happy_path_and_idempotent_retry - AssertionError: {'error': 'capability_absent', 'message': 'occurrence_adoption requires a minted MutationCapability bound to action, occurrence, target, and fence epoch', 'success': False}
assert 1 == 0
FAILED tests/arnold_pipelines/megaplan/chain/test_occurrence_adopt.py::test_cli_adopt_refuses_one_bit_cas_flip_zero_mutation[--expected-chain-state-sha256] - AssertionError: {'error': 'capability_absent', 'message': 'occurrence_adoption requires a minted MutationCapability bound to action, occurrence, target, and fence epoch', 'success': False}
assert 'capability_absent' == 'cas_mismatch'
  
  - cas_mismatch
  + capability_absent
FAILED tests/arnold_pipelines/megaplan/chain/test_occurrence_adopt.py::test_cli_adopt_refuses_one_bit_cas_flip_zero_mutation[--expected-plan-state-sha256] - AssertionError: {'error': 'capability_absent', 'message': 'occurrence_adoption requires a minted MutationCapability bound to action, occurrence, target, and fence epoch', 'success': False}
assert 'capability_absent' == 'cas_mismatch'
  
  - cas_mismatch
  + capability_absent
FAILED tests/arnold_pipelines/megaplan/chain/test_occurrence_adopt.py::test_cli_adopt_refuses_one_bit_cas_flip_zero_mutation[--expected-latest-failure-sha256] - AssertionError: {'error': 'capability_absent', 'message': 'occurrence_adoption requires a minted MutationCapability bound to action, occurrence, target, and fence epoch', 'success': False}
assert 'capability_absent' == 'cas_mismatch'
  
  - cas_mismatch
  + capability_absent
FAILED tests/arnold_pipelines/megaplan/chain/test_occurrence_adopt.py::test_cli_adopt_refuses_one_bit_cas_flip_zero_mutation[--expected-resume-cursor-sha256] - AssertionError: {'error': 'capability_absent', 'message': 'occurrence_adoption requires a minted MutationCapability bound to action, occurrence, target, and fence epoch', 'success': False}
assert 'capability_absent' == 'cas_mismatch'
  
  - cas_mismatch
  + capability_absent
FAILED tests/arnold_pipelines/megaplan/chain/test_occurrence_adopt.py::test_cli_adopt_refuses_one_bit_cas_flip_zero_mutation[--expected-pause-authority-sha256] - AssertionError: {'error': 'capability_absent', 'message': 'occurrence_adoption requires a minted MutationCapability bound to action, occurrence, target, and fence epoch', 'success': False}
assert 'capability_absent' == 'cas_mismatch'
  
  - cas_mismatch
  + capability_absent
FAILED tests/arnold_pipelines/megaplan/chain/test_occurrence_adopt.py::test_cli_adopt_refuses_one_bit_cas_flip_zero_mutation[--expected-runtime-manifest-sha256] - AssertionError: {'error': 'capability_absent', 'message': 'occurrence_adoption requires a minted MutationCapability bound to action, occurrence, target, and fence epoch', 'success': False}
assert 'capability_absent' == 'cas_mismatch'
  
  - cas_mismatch
  + capability_absent
FAILED tests/arnold_pipelines/megaplan/chain/test_occurrence_adopt.py::test_cli_adopt_refuses_one_bit_cas_flip_zero_mutation[--expected-marker-sha256] - AssertionError: {'error': 'capability_absent', 'message': 'occurrence_adoption requires a minted MutationCapability bound to action, occurrence, target, and fence epoch', 'success': False}
assert 'capability_absent' == 'cas_mismatch'
  
  - cas_mismatch
  + capability_absent
FAILED tests/arnold_pipelines/megaplan/chain/test_occurrence_adopt.py::test_cli_adopt_refuses_one_bit_cas_flip_zero_mutation[--expected-runtime-roots-sha256] - AssertionError: {'error': 'capability_absent', 'message': 'occurrence_adoption requires a minted MutationCapability bound to action, occurrence, target, and fence epoch', 'success': False}
assert 'capability_absent' == 'cas_mismatch'
  
  - cas_mismatch
  + capability_absent
FAILED tests/arnold_pipelines/megaplan/chain/test_occurrence_adopt.py::test_cli_adopt_refuses_non_operator - AssertionError: assert (1 == 1 and {'error': 'capability_absent', 'message': 'occurrence_adoption requires a minted MutationCapability bound to action, occurrence, target, and fence epoch', 'success': False} is not None and 'capability_absent' == 'actor_forbidden'
  
  - actor_forbidden
  + capability_absent)
FAILED tests/arnold_pipelines/megaplan/chain/test_occurrence_adopt.py::test_cli_adopt_refuses_missing_pause - AssertionError: assert (1 == 1 and {'error': 'capability_absent', 'message': 'occurrence_adoption requires a minted MutationCapability bound to action, occurrence, target, and fence epoch', 'success': False} is not None and 'capability_absent' == 'chain_not_paused'
  
  - chain_not_paused
  + capability_absent)
FAILED tests/arnold_pipelines/megaplan/chain/test_occurrence_adopt.py::test_cli_adopt_refuses_non_null_existing_identity - AssertionError: {'error': 'capability_absent', 'message': 'occurrence_adoption requires a minted MutationCapability bound to action, occurrence, target, and fence epoch', 'success': False}
assert 'capability_absent' == 'repair_ident...ready_present'
  
  - repair_identity_already_present
  + capability_absent
FAILED tests/arnold_pipelines/megaplan/chain/test_occurrence_adopt.py::test_cli_adopt_refuses_ambiguous_failure - AssertionError: assert (1 == 1 and {'error': 'capability_absent', 'message': 'occurrence_adoption requires a minted MutationCapability bound to action, occurrence, target, and fence epoch', 'success': False} is not None and 'capability_absent' == 'ambiguous_failure'
  
  - ambiguous_failure
  + capability_absent)
FAILED tests/arnold_pipelines/megaplan/chain/test_occurrence_adopt.py::test_cli_adopt_refuses_unequal_roots - AssertionError: assert (1 == 1 and {'error': 'capability_absent', 'message': 'occurrence_adoption requires a minted MutationCapability bound to action, occurrence, target, and fence epoch', 'success': False} is not None and 'capability_absent' == 'runtime_roots_unequal'
  
  - runtime_roots_unequal
  + capability_absent)
FAILED tests/arnold_pipelines/megaplan/chain/test_occurrence_adopt.py::test_cli_adopt_crash_after_record_and_after_request_converge - Failed: DID NOT RAISE <class 'OSError'>
FAILED tests/arnold_pipelines/megaplan/chain/test_occurrence_adopt.py::test_cli_adopt_two_concurrent_adopters_one_record - assert 0 == 1
 +  where 0 = len([])
FAILED tests/arnold_pipelines/megaplan/test_chain_awaiting_human_retry.py::test_handle_outcome_retries_stale_awaiting_human_with_satisfied_resolutions - AssertionError: assert 'stop' == 'retry'
  
  - retry
  + stop
FAILED tests/cloud/test_repair_custody.py::test_live_execute_worker_is_not_a_finalized_state_contradiction - AssertionError: assert True is False
 +  where True = <function _has_terminality_contradiction at 0x121d98f40>({'active_step_heartbeat': {'active': True, 'pid_live': True, 'worker_pid': '1004788'}, 'authoritative_source': 'chain_...le-chain', 'milestone_total': 10, 'present': True}, 'current_refs': {'current_plan_name': 's3-boundary-coverage'}, ...})
 +    where <function _has_terminality_contradiction at 0x121d98f40> = repair_contract._has_terminality_contradiction
FAILED tests/cloud/test_runtime_attestation.py::test_real_editable_launch_and_noneditable_supervisor_vectors - shutil.Error: [('/Users/peteromalley/Documents/.megaplan-worktrees/runtime-convergence-execution/arnold_pipelines/megaplan/skills/babysit/SKILL.md', '/private/var/folders/_w/b3tthv192m77c760dbyzvk200000gn/T/pytest-of-peteromalley/pytest-559/test_real_editable_launch_and_0/supervisor-venv/lib/python3.11/site-packages/arnold_pipelines/megaplan/skills/babysit/SKILL.md', "[Errno 2] No such file or directory: '/Users/peteromalley/Documents/.megaplan-worktrees/runtime-convergence-execution/arnold_pipelines/megaplan/skills/babysit/SKILL.md'")]
FAILED tests/cloud/test_runtime_attestation.py::test_ensure_runtime_launch_seed_refuses_drifted_head - AssertionError: Regex pattern did not match.
  Expected regex: 'HEAD does not match the manifest pin'
  Actual message: 'runtime launch seed is not release-ready: chain_runtime_revision_mismatch, marker_runtime_revision_mismatch'
FAILED tests/cloud/test_runtime_attestation.py::test_attestation_disable_without_seed_does_not_authorize_production_launch - Failed: DID NOT RAISE <class 'arnold_pipelines.megaplan.types.CliError'>
FAILED tests/cloud/test_runtime_cutover.py::test_chain_runtime_cutover_composes_with_marker_runtime_update - arnold_pipelines.megaplan.types.CliError: runtime rebind CAS is import_root plus generation interpreter, not SHA-256
FAILED tests/resident/test_subagent_followup.py::test_non_codex_continuation_worker_resumes_exact_session[hermes-zhipu:glm-5.2-resident_0123456789abcdef0123456789abcdef---resume-session] - AssertionError: assert '--resume-session' in ['python3', '/workspace/runtime-candidates/arnold-r7-fresh-child-20260805/arnold_pipelines/megaplan/skills/subagent-launcher/launch_hermes_agent.py', '--model', 'deepseek:deepseek-v4-flash', '--toolsets', 'file,web', ...]
FAILED tests/resident/test_subagent_followup.py::test_non_codex_continuation_worker_resumes_exact_session[claude-opus-019f5d2e-d5da-75f3-a617-4712a1c57cc5---resume] - AssertionError: assert '--resume' in ['python3', '/workspace/runtime-candidates/arnold-r7-fresh-child-20260805/arnold_pipelines/megaplan/skills/subagent-launcher/launch_hermes_agent.py', '--model', 'deepseek:deepseek-v4-flash', '--toolsets', 'file,web', ...]
FAILED tests/resident/test_subagent_followup.py::test_continuation_worker_resumes_exact_parent_session_and_records_acceptance - AssertionError: assert ['python3', '...y', '--model'] == ['codex', 'exec', 'resume']
  
  At index 0 diff: 'python3' != 'codex'
  
  Full diff:
    [
  +     'python3',
  +     '/workspace/runtime-candidates/arnold-r7-fresh-child-20260805/arnold_pipelines/megaplan/skills/subagent-launcher/launch_hermes_agent.py',
  -     'codex',
  ?      ^   ^
  +     '--model',
  ?      ^^^   ^
  -     'exec',
  -     'resume',
    ]
FAILED tests/test_post_m11_release_evidence.py::test_release_evidence_record_is_structurally_valid - ValueError: canonical_artifact_moves[0].from still exists: .megaplan/initiatives/megaplan-native-parity-corrective/GOLDEN_TRACE_CONTRACT.md
FAILED tests/test_post_m11_release_evidence.py::test_release_evidence_cannot_claim_done_with_pending_gates - AssertionError: Regex pattern did not match.
  Expected regex: 'residuals are pending'
  Actual message: 'canonical_artifact_moves[0].from still exists: .megaplan/initiatives/megaplan-native-parity-corrective/GOLDEN_TRACE_CONTRACT.md'
FAILED tests/test_post_m11_release_evidence.py::test_candidate_ready_allows_only_post_deploy_residuals_pending - ValueError: canonical_artifact_moves[0].from still exists: .megaplan/initiatives/megaplan-native-parity-corrective/GOLDEN_TRACE_CONTRACT.md
FAILED tests/test_post_m11_release_evidence.py::test_candidate_ready_rejects_a_pending_pre_deploy_residual - AssertionError: Regex pattern did not match.
  Expected regex: 'pre-deploy residuals are pending'
  Actual message: 'canonical_artifact_moves[0].from still exists: .megaplan/initiatives/megaplan-native-parity-corrective/GOLDEN_TRACE_CONTRACT.md'
FAILED tests/test_post_m11_release_evidence.py::test_candidate_ready_requires_bound_acceptance_evidence - AssertionError: Regex pattern did not match.
  Expected regex: 'requires final_acceptance'
  Actual message: 'canonical_artifact_moves[0].from still exists: .megaplan/initiatives/megaplan-native-parity-corrective/GOLDEN_TRACE_CONTRACT.md'
FAILED tests/test_post_m11_release_evidence.py::test_candidate_ready_requires_complete_source_universe - AssertionError: Regex pattern did not match.
  Expected regex: 'requires source_universe'
  Actual message: 'canonical_artifact_moves[0].from still exists: .megaplan/initiatives/megaplan-native-parity-corrective/GOLDEN_TRACE_CONTRACT.md'
FAILED tests/test_post_m11_release_evidence.py::test_complete_release_requires_one_immutable_acceptance_binding - ValueError: canonical_artifact_moves[0].from still exists: .megaplan/initiatives/megaplan-native-parity-corrective/GOLDEN_TRACE_CONTRACT.md
FAILED tests/test_post_m11_release_evidence.py::test_final_no_debt_rejects_partial_and_overlapping_shard_sets - AssertionError: Regex pattern did not match.
  Expected regex: 'differs from terminal inventory union'
  Actual message: 'canonical_artifact_moves[0].from still exists: .megaplan/initiatives/megaplan-native-parity-corrective/GOLDEN_TRACE_CONTRACT.md'
FAILED tests/test_post_m11_release_evidence.py::test_final_no_debt_rejects_every_non_acceptance_outcome[failed] - AssertionError: Regex pattern did not match.
  Expected regex: 'failure, skip, xfail/xpass, or mutation'
  Actual message: 'canonical_artifact_moves[0].from still exists: .megaplan/initiatives/megaplan-native-parity-corrective/GOLDEN_TRACE_CONTRACT.md'
FAILED tests/test_post_m11_release_evidence.py::test_final_no_debt_rejects_every_non_acceptance_outcome[errors] - AssertionError: Regex pattern did not match.
  Expected regex: 'failure, skip, xfail/xpass, or mutation'
  Actual message: 'canonical_artifact_moves[0].from still exists: .megaplan/initiatives/megaplan-native-parity-corrective/GOLDEN_TRACE_CONTRACT.md'
FAILED tests/test_post_m11_release_evidence.py::test_final_no_debt_rejects_every_non_acceptance_outcome[skipped] - AssertionError: Regex pattern did not match.
  Expected regex: 'failure, skip, xfail/xpass, or mutation'
  Actual message: 'canonical_artifact_moves[0].from still exists: .megaplan/initiatives/megaplan-native-parity-corrective/GOLDEN_TRACE_CONTRACT.md'
FAILED tests/test_post_m11_release_evidence.py::test_final_no_debt_rejects_every_non_acceptance_outcome[xfailed] - AssertionError: Regex pattern did not match.
  Expected regex: 'failure, skip, xfail/xpass, or mutation'
  Actual message: 'canonical_artifact_moves[0].from still exists: .megaplan/initiatives/megaplan-native-parity-corrective/GOLDEN_TRACE_CONTRACT.md'
FAILED tests/test_post_m11_release_evidence.py::test_final_no_debt_rejects_every_non_acceptance_outcome[xpassed] - AssertionError: Regex pattern did not match.
  Expected regex: 'failure, skip, xfail/xpass, or mutation'
  Actual message: 'canonical_artifact_moves[0].from still exists: .megaplan/initiatives/megaplan-native-parity-corrective/GOLDEN_TRACE_CONTRACT.md'
FAILED tests/test_post_m11_release_evidence.py::test_final_no_debt_rejects_every_non_acceptance_outcome[mutations] - AssertionError: Regex pattern did not match.
  Expected regex: 'failure, skip, xfail/xpass, or mutation'
  Actual message: 'canonical_artifact_moves[0].from still exists: .megaplan/initiatives/megaplan-native-parity-corrective/GOLDEN_TRACE_CONTRACT.md'
39 failed, 2344 passed, 23 warnings in 94.04s (0:01:34)
