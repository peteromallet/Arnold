# NBF P0 Failure Ledger — living doc

Occurrence chain for plan `p0-mrc-closeout-intake-and-20260826-0049`
(epic `native-build-forward`, digest lineage `1ac805e5eef9` → …). All times UTC.
Sources: container `/tmp/watchdog-ensure.log`, babysitter receipts + stdout/stderr,
plan `events.ndjson`, plan `state.json latest_failure`, host `dmesg -T`,
host ftrace `/sys/kernel/tracing/trace` (sig==15), local Arnold commits.

## 2026-08-26 failures (chronological)

| # | When | What died / failed | Root cause | Class | Fix | Status |
|---|------|--------------------|-----------|-------|-----|--------|
| 1 | 00:49→01:17 | plan born; revise dispatch 1913264 (ox-alpha) starts then dies | memcg OOM kill of omp (`dmesg` 01:24:52, 76 GB virtual) — SIGKILL uncatchable → no failure record | resource | 16:xxZ `docker update --memory 16g`; commit `5fa0d32e76` on NBF (survive cgroup-killed phase workers) | FIXED |
| 2 | 01:17–10:30 | P0 sits dead ~9h, no fixer engages | watchdog env-pinned to megaplan-maintenance session + restack churn every minute + babysitter launch hung importing via supervisor venv python | machinery | marker-scoped observer, in-flight sweep guard, interpreter pin 3.11 (commits 1916884270-era, deployed 10:33Z) | FIXED |
| 3 | 11:23 → 11:28 | revise redispatch v4-pro worker 2384793 dies silently | same memcg OOM (`dmesg` 11:28:07 python 5.8 GB) | resource | same as #1 | FIXED |
| 4 | 10:34→10:57 attempt A1 | fixer turn dies rc=-15 at 1406s, silent | launcher/harness per-turn subprocess timeout sends SIGTERM (proven by later stdout `done in 1401.9s exit=143`) | self-timeout | commit `41bdef47aa` default 1800→7200 + `MEGAPLAN_TURN_TIMEOUT_SECS` | FIXED |
| 5 | 17:04 / earlier | fixers on ox-alpha fail instantly: `Model "openrouter/stealth/ox-alpha" not found` | model expired (provider removed); no usable key route | model rot | switched to openrouter/deepseek-chat → glm-5.1 → **glm-5.3-flash** (pin) | FIXED |
| 6 | 18:58 | fixer turn ends 118s writing handoff "missing tools" | false verdict: invoked nonexistent `megaplan` binary; never probed `python3 -m arnold_pipelines.megaplan` | prompt gap | goal TOOLING note + HARD TURN CONTRACT `167e5f7b08`/`00d956eae1` | FIXED |
| 7 | 13:46+ several | attempt finishes claiming "chain operating as expected" while checkin shows dead worker | goal allowed judgment-based healthy verdict without proof | prompt gap | HARD TURN CONTRACT items 1–2 (proof standard: live pid + seq delta <10 min) | FIXED |
| 8 | 14:2x | gate deterministic fail 3× `runtime_launch_attestation_mismatch: canonical seed missing` when run outside cloud dispatch wrapper | seed lives in dispatch-current.json pointed path; direct invocations lacked `MEGAPLAN_RUNTIME_LAUNCH_SEED` | identity/env | resolved within superfixer attempts via wrapper relaunch (seed propagated through supported seam) | RESOLVED |
| 9 | 14:2x–19:4x | finalize fails 3× `critique_finding_unresolved CF-53349D3C` → state blocked | content: revise must traceably resolve or invalidate flag; blocked needs fingerprint-bound recovery | chain content | glm fixer recovered (blocked→planned 20:06Z); resume drove fresh critique | RECOVERED |
| 10 | evening cycles | premature exits after 84s/7.8s "verdicts" | see #7 (pre-contract) | prompt gap | covered by contract | OBSERVED-FIXED |
| 11 | recurring | stale stdout spam: 64k lines `Model not found` from Aug 25 wrong-cwd era appended to active run logs | append-only logs reused across eras mislead readers | observability | accepted cosmetic trap; documented here + direction.py reads receipt not tail | DOC'D |
| 12 | 00:47→01:39 | plan parked finalized mid-execute (batches 1-4/5), T7 durably blocked on test-budget gate (240s>120s declared), batch 5 undispatched, events flat | prior babysitter turn killed by launcher self-timeout before dispatching batch 5 (ledger #4 family); T7 budget-gate block is live policy, bare --batch 5 refuses prerequisites | policy+self-timeout | NO source change needed (Sol tier1-only-now): arnold-chain wrapper --one auto-loop reset T7, compliant single `timeout 120 pytest` rerun passed, T8 closeout + review approved, auto-publish commit b9087cb55e, chain between_milestones idx1 @02:37Z | FIXED |


| 13 | 14:16→15:10 | p1 done but chain parked `authority_divergence` (22 milestones stranded): completion guard vetoed adoption on T3 stale `blocked` finalize row vs accepted-attempt authority + untracked-but-present claimed files misread as phantom claims | reader-side: branch 2 of `_chain_completion_shadow_disagreements` (naming diagnostic became enforcement, 0 test pins) + `authority_set` narrowed to committed range under declared base_ref | reader bug | commit `e57510d27e` (branch 2 deleted, coverage unioned with status, `files_claimed_worktree_only`); regressions red→green, 140+50 passed; manifest advanced; adopted → idx 2, p2 driving (worker pid 2732740 @15:10) | FIXED |
| 14 | 15:01:44→15:15:03 | p2 (p2-milestone-gate-bootstrap) drive `nbf-drive-944dd380108d` died 9s after a SUCCESSFUL plan phase (events seq 12-15, phase_result exit_kind=success), before critique dispatch; zero log output (cloud-chain.log untouched since drive start), latest_failure=null, restart=on-failure never fired (restarts=0), oom_kill unchanged 13 | hub last-omp teardown SIGKILL of a persist=false/detached=false drive when the owning babysitter omp session ended (~15:15) — 3rd custody recurrence (swarm-corroborated H1, H2/H3 ruled out; Sol-adjudicated) | controller custody / launch-contract | source fix `b91a81c183` (goal STEP 4 DRIVE CUSTODY CONTRACT: persist+detached+pty:false+restart:no, no ready matcher, attestation; regression red→green), manifest gen 10 advanced BEFORE relaunch; relaunch nbf-drive-a1555447f922-g2 pid 2744069 attested persist/detached/no-pty/restart=no; critique phase_start proven seq 18 @15:58:54Z, live worker pid 2744270 | FIXED (rank-2 lease+redrive backstop → ticket 01KYPT8PS81GTFC4CE2PFGVETM) |
| 15 | 16:29:05→17:58:34 | p2 drive `nbf-drive-a1555447f922-g2` memcg-OOM SIGKILLed mid-revise (exit 137, oom_kill 13→14, peak==max 16384 MiB); in-process phases → no failure record, latest_failure=null, events flat at seq 81 for 68 min. First relaunch (17:38Z) proved orphan recovery + typed attribution (events 82→101, worker_killed/cgroup_oom) then exposed a NEW class: `select_memory_safe_spec` refused every redispatch `prior_cgroup_oom` forever (single-spec config → permanent typed block on a transient OOM; docstring conceded "even when current memory later dropped"); plan-level resume only replays the recorded failure (repair_phase_contract fence); chain start suppresses replay of a durably-blocked plan | gate's learned-death policy had no expiry — a stale blocked disposition frozen at the death timestamp (checklist #1); wrapper had no local redrive so custody fell to the 12-min watchdog→babysitter path | engine gate + controller | `1016fd825f` wrapper redrive (flock lease + bounded 2 retries gated on exit==137 AND oom_kill advance AND post-backoff headroom ≥1 GiB; Sol-amended; 8-test regression); `648fc124ae` gate cooldown expiry (15 min, env-overridable, unparseable detected_at fails closed; +3 tests, 40 passed focused); manifest 10→11→12; `override recover-blocked --repair-commit 648fc124ae --failure-fingerprint e0399587... --repair-scope engine_runtime` re-admitted revise (blocked→critiqued); drive g4r4 pid 2776323 + worker 2776578 live, seq 114 @17:58:34 | FIXED |
## Current open items
- [x] RESOLVED 18:01Z: p2 16:29 OOM death + 17:38 stale gate block recovered (occurrence 1ac805e5eef9-gen4); wrapper redrive 1016fd825f + gate cooldown 648fc124ae shipped, manifest gen 12, revise re-dispatched (seq 114, worker 2776578); handoff.md = gen4.
- [x] RESOLVED 15:10Z: p1 adoption unblocked via reader fixes e57510d27e (occurrence 944dd380108d); chain idx 2, p2 driving.
- [x] RESOLVED 02:37Z: P0 completed end-to-end via drive-only recovery (occurrence 393c5c170925); review approved 0 flags; exactly one chain terminal record (events seq 694); follow-up ticket 01M10HANCY0BG6ZPBK9EGZFJTA for Tier-2 phantom_claims fix.
- [ ] `MEGAPLAN_SUPERVISOR_SOURCE_ROOT: unbound variable` noise in babysitter stderr (cosmetic, env seam).
- [ ] Sweep `ModuleNotFoundError: wrappers.repair_delegation` each scan (import-path defect; ticket).
- [ ] `~z-ai/glm-latest` / provider catalog drift generally: add catalog drift alarm so model expiry is caught at dispatch time, not via instant-exit loops.
| 16 | 17:58:32→18:11:12 | revise worker 2776578 (glm-5.3-flash) cgroup-OOM again (oom_kill 14→15, detected 18:10:55); every redispatch inside the 15-min learned-death cooldown was a typed `insufficient_memory_headroom/prior_cgroup_oom` refusal; auto loop retried ~8s apart → 3 identical signatures → `repeated_failure_signature` → **STATE_BLOCKED permanently** ~30s after any single-spec OOM; AND the recovery seam rejected the block: `validated_deterministic_phase_repair` knew only deterministic_phase_failure/provider_contract_failure → `missing_phase_result` CliError | breaker vs cooldown interaction: a time-bounded scheduling condition was counted as a deterministic phase failure by BOTH breakers (auto.py deterministic counter + repeated_failure_signature); no wait mechanism existed; recover-blocked seam gap for the repeated kind | engine (auto.py/memory_headroom/blocker_recovery) | commit `3c626ef807` (structured prior_cgroup_oom deferral: wait out remaining cooldown with both breaker families reset, via `memory_cooldown_wait_secs`; commit-bound recovery extended to repeated_failure_signature via `_COMMIT_BOUND_PHASE_REPAIRS` + `commit_bound_phase_repair_required` centralized in blocker_recovery, consumed by override.py + control_binding.py); 73 focused tests green (9 cooldown-wait, 3 drive deferral, 2 recover-blocked seam); 2 'omp'=='hermes' routing failures pre-exist on pristine HEAD; manifest gen 12→13 (expected_head 3c626ef807); recover-blocked admitted (phase_contract_repair receipt, fingerprint 77d19f80a5f7659d2a58ff67c386dd3e7f9d66111d74fdc4b53396f3c6141579); relaunch drive nbf-drive-8e4028a81152-gen5 pid 2789902 → revise worker 2790079 live, events 132→139+ | FIXED (movement proven; OOM root cause itself stays environmental — see row 15 q2 note) |

| 17 | 19:03→20:17 (cycle 19:24→20:17) | gen6 diagnosis: the OOM balloon is the chain-start SUPERVISOR (in-process revise), not the worker — RSS 102MB→13.5GB in ~40 min (sampled 19:31–20:10); each OOM restart re-leaks from scratch → gen5's deferral self-heals every cycle but can NEVER converge; revise restarts from zero each time (4th death detected 20:17:20Z, oom_kill 17) | unbounded prompt-event retention in RpcClient._events (pinned omp_rpc ca1411b): count-bounded (200k items) but not byte-bounded; one long agent turn = whole phase; turn reconstruction doubles it | engine/dependency | commit c53bba6ee0: worker RPC client subclass retains only a constant-size terminal agent_end marker, rebuilds turn from canonical get_messages(); listeners unaffected (reader-loop dispatch independent); usage aggregation keeps get_messages() (adapter tests proved turn.messages is not usage-bearing); manifest gen 13→14; regression 111 passed incl. 4 new test_omp_prompt_retention | SHIPPED-PROVEN (supervisor flat 116-122MB through 24+ min vs multi-GB+OOM before; oom_kill 17 unchanged; commit c53bba6ee0, manifest gen 14, authoritative per-slug manifest, recover-blocked re-admission, drive re-owned) |
| 19 | 03:12→04:06 (this session) | p2 review phase blocked the chain: `worker_structural_audit_failed` ×8 — (a) engine `_pre_check_flag` omitted required-string `evidence_file` when None, model copied the omission as `null`, strict pre-handler audit rejected it; (b) after fix (a), the model restated scratch field `review_completion_status` (equal value) at both ends of the object and the local-strict parse rejected ANY duplicate key before the scratch pop; breaker counted 3 identical signatures each time → `deterministic_phase_failure` (occurrence f8d3f9dc3ddb) | missing engine structure: engine injected schema-violating data + told the model to copy verbatim + audited strictly before any normalization; equal-value duplicate-key strictness conflicted with the documented scratch-field tolerance | engine (review/mechanical.py, workers/_payload.py, model_seam.py, schemas/runtime.py) | commits `ffcc3c2cfa` (producer emits string + schema-driven `x-engine-default` pre-audit normalizer on both capture boundaries) and `37711df283` (only CONFLICTING duplicate keys fail; equal-value repeats parse); 103 regressions + real-attempt raw replay approved; gens 24/25/26 + fingerprint-bound recover-blocked ×3; p2 review **approved**, plan done, chain idx 2→3, one terminal record; idx 3 driving (worker 3205421). Flash swarm gate: deepseek 402 balance persists. Handoff: recovery/handoff.md (gen8) | FIXED |

## Verification rule (§1.2)
Movement = `last_state` leaves critiqued-park AND failure fingerprint does not recur
AND a live worker exists ≥10 min with advancing events seq. PID/prose ≠ proof.

| 18 | 22:37→02:29 | openrouter glm-5.3-flash upstream idle-timeout outage on the large revise prompt (6× failures 22:37–00:00, more post-fix to 02:29); OLD drive loop then re-dispatched revise 3× from blocked, each rejected `invalid_transition`, breaker counted the synthetic rejections → `deterministic_phase_failure` masked the provider outage and durably parked the chain (occurrence 81827f38514f) | external failure not terminal in `drive()` + `_project_auto_dispatch` cursor fallback promoted a raw phase from blocked | engine (auto.py) | commit `7a1380ddee` (terminal-exit on external_error; no raw phase dispatch from blocked; pre-dispatch invariant; 2 regressions; 29/29 + 169 suites green); manifest gen 15 + seed rebind; post-fix behavior verified live (terminal block, no masking, seq 332); gen8 re-admission via `override recover-blocked` + canonical drive `nbf-drive-81827f38514f-gen8` pid 2872776 | FIXED (provider stability on large prompts remains environmental) |
| 20 | 2026-08-30 17:08Z | C1 `native-c1-completion-contract` review repeated `runtime_launch_attestation_mismatch` ×3; chain blocked | `runtime interpreter identity drifted` at the review dispatch boundary; current seed/runtime interpreter vector no longer matches the editable-install interpreter after rebind/profile changes | identity/dispatch | OPEN: feed to Codex Sol planner; determine whether seed rebuild should be automatic at every production dispatch or whether the active plan must be rebound before review; admin notification required |

Entry 20 follow-up: Codex Sol reviewed the failure and AMENDED criterion 1 in
`.oracle/agent_goal.md`: canonical launch-seed settlement must happen exactly
once under the manifest promotion/CAS lock before identity snapshots and
fingerprint reservation. Discord notification was attempted but failed with
HTTP 403 on both DM creation and guild fallback; status is explicitly
UNDELIVERED, not silently marked sent.
