# Cloud Prerequisite Resolution

Design notes for the cloud prerequisite-resolution chain. The chain covers structured prerequisite/quality resolution metadata, auto recovery, chain policy/status, cloud supervision, and slot-first watchdog hardening.

This document is intentionally brief inside the worktree; the executable milestone briefs under `briefs/cloud-prerequisite-resolution/` are the source of truth for the active chain.
