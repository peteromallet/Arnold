# Archive

This directory holds historical design notes, migration runbooks, archived YAML chain specs, and generated plans that are useful for context but are no longer current operator guidance.

Current docs should link here only when they need historical background. New setup, deployment, and workflow instructions belong in the live docs at `docs/`.
